# 修复XI 开发进度2（B 轮：官方库大卡片，2x 未解决②）

> 对应 spec `docs/specs/人工测试遗留问题修复XI设计.md` XI-2｜plan `docs/plans/人工测试遗留问题修复XI.plan.md` B 轮（B1/B2/B3/B4）
> 轮序：A 右键菜单 ✅ → B 官方库 ✅ → C 两级词汇 → D 组大节点 → E 文档收尾

## B1 后端 official 过滤参数（commit bc23dd8b）

- **实现**：[AssetPublicPoolController.java](../../../backend/src/main/java/com/superprogrammer/asset/controller/AssetPublicPoolController.java) `GET /public-pool` 增 `@RequestParam(required=false) Boolean official` → [AssetPublicPoolService.java](../../../backend/src/main/java/com/superprogrammer/asset/service/AssetPublicPoolService.java) `listPublic(userId, admin, official)` 条件链加 `.eq(Boolean.TRUE.equals(official), AssetProject::getPublishedByAdmin, true)`（服务端强制，不信前端）；不传=全量（现状回归）。前端 [api/assets.ts](../../../frontend/src/api/assets.ts) `publicPoolApi.list(params?: { official?: boolean })`。
- **AssetVO.mediaCategory**：核查既有列已透出（plan「缺则补」分支未触发，零改动）。
- **测试**：Service 测试 2 例断言 wrapper SQL 段含/不含 `published_by_admin`（需 `@BeforeAll TableInfoHelper.initTableInfo` 预热 lambda 缓存——MyBatis-Plus 纯单测无 Spring 上下文时的既有范式，RoleControllerHiddenFilterTest 同款）+ Controller 委托 1 例。

## B2 OfficialLibrary.vue 大卡片（commit 4997a767）

- **实现**（[OfficialLibrary.vue](../../../frontend/src/components/canvas/OfficialLibrary.vue) 新建）：n-modal 双栏——左官方项目卡列表（官方 badge/名称/`N 项资产`/发布者/日期/描述，usable 过滤，loading/error+重试/空态「暂无官方发布项目」）、右按项目 `mediaTypes` 词汇序分组资产行；词汇外 mediaType 尾组「其他」+空组隐藏；无词汇=单组「全部资产」；`mediaTypes` JSON 解析容错（string 元素/`{key}` 对象元素/坏 JSON 全收）。行复用 [AssetPickerRow.vue](../../../frontend/src/components/canvas/AssetPickerRow.vue)（四态缩略/Lightbox z3000 全继承）。会话隔离照 AssetPicker sessionId 范式（关开递增，旧响应不得回写）。
- **与 AssetPicker 反序**：选行只 `emit('picked', { asset })` 不自闭、不 resolve——节点须先存在才有 nodeId 传 resolve（B3 链序：选资产→建节点→resolve→写节点；失败删节点且卡片留）。
- **测试**（[OfficialLibrary.test.ts](../../../frontend/src/components/canvas/OfficialLibrary.test.ts) 6 用例）：official:true 请求+usable 过滤+卡片内容/词汇序分组+其他尾组+空组隐藏/坏 JSON 单组/双空态/emit picked 不自闭/pickingId 透传。
- **踩坑沉淀**：n-modal teleport 到 body——`wrapper.findAll` 全盲，须 `document.querySelectorAll` + 断言前 `flushPromises+nextTick` + `afterEach document.body.innerHTML=''`（残留 DOM 跨用例串染）。

## B3 CanvasView 插入链（commit 4997a767）

- **反向映射单源**（[paletteItems.ts](../../../frontend/src/components/canvas/paletteItems.ts) 增 `mediaToNodeType`）：六已知词汇直映（提示词→text/剧本→script/分镜→storyboard/图片→image/视频→video/音频→audio）+ 自定义类型按 mediaCategory 回落 + 全 miss 退 text（永不 undefined）。测试 3 例（清单唯一性/直映/回落）。
- **入口**：调色板尾分隔线+「官方库」条目（LibraryOutline，action 样式与节点条目区隔，aria-label 说明用途）。
- **插入链**（CanvasView `onOfficialPicked`）：`officialPickingId` 防重入 → `addNodeAtCenter({type, label})` → `assetBridgeApi.resolve(asset.id, {canvasId, nodeId})` → `applyAssetResolve`（AssetPicker 落点同款写节点）→ toast+关卡片；失败 toast+`abortNodeAdd(newId)`（**不入撤销栈**：pop 掉 'add' 栈顶+级联删连边/组边+仍发 structure-changed 落库）+卡片留。
- **CanvasBoard 新增 expose**：`addNodeAtCenter`（board rect 中心 project 换算）+ `abortNodeAdd`（上述回滚）。测试 3 用例：中心换算 {400,300}/pop 'add' 后 canUndo false+structure-changed/快照载入边级联删。

## B4 收口

- 全量 vitest **1040/1040**（+12：OfficialLibrary 6+palette 3+Board 3）+ vue-tsc **0 错**；后端全量 **2715/2715 BUILD SUCCESS**。
- commits：bc23dd8b（B1 后端）+ 4997a767（B2+B3 前端）。
- 安全清单核对：official 过滤服务端强制；资产浏览走既有 public-pool/assetApi 鉴权链零新端点；resolve 失败静默回滚不落半成品节点。
- 手测项（真实官方项目四态预览/大图盖卡片/插入成正确类型节点/刷新重现）并入 E 轮 S2 手测清单。

## 下一步

C 轮叙事角色两级词汇（C1 后端 RoleVocab+迁移 V169+双容错+normalize → C2 reassign 两级+筛选展开+分镜展开 → C3 VocabEditor 两级 → C4 消费点 → C5 收口）。
