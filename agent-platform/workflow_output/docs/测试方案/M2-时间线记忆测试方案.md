# M2 时间线记忆 · 测试方案

> UI 联动(temporal 询问/自定义合并/时间线渲染)+ 主观体验 → 需人工/playwright 交互测试。对齐 plan.md「功能联动点清单」。

## 测试环境
- 后端 8080 + sidecar 8090 + 前端 5173 + redis 6379 + pg 5432 全起。
- 账号 admin / admin123(或普通用户测本人 key 隔离)。

## 联动用例(含正向/反向/半选)
| # | 触发动作 | 预期联动 | 实测 |
|---|---|---|---|
| T1-正 | 首次「住址」key 走到 pending 冲突 | 卡片显 temporal 询问「这类按时间线记?」 | ✅(冲突77,卡显 时间线是/否 + 未设 + 5按钮,m2-conflict-card-temporal-ask.png)|
| T1-是 | 答「是」→ 点合并保留 | memory_key_meta 落 is_temporal=true;survivor value 各段带日期按序拼 `;` | ✅(冲突79 点是→meta home_approximate_location=true/USER_OVERRIDE 落;合并保留→survivor "2026-07-18 余杭区;萧山，西湖区",m2-temporal-merge-timeline.png)|
| T1-否 | 答「否」→ 点合并保留 | 落 is_temporal=false;value 中文逗号 join(现状) | ✅(冲突78 无标→默认非时序→合并保留 survivor "萧山，西湖区" 中文逗号 join)|
| T1-复 | 同 key 再次冲突 | 不显询问,直接按标 merge | ✅(冲突78 同住址域再冲突,卡不显「未设」,按已设标走)|
| T2-改标 | panel 改「住址」temporal 标 true→false | 标翻转;下次冲突按新标 | ✅(PUT key-meta API 验翻转 true→false source=USER_OVERRIDE;mergeValuesInto 读 meta 由 T1-是/否 端到端证;UI 按钮同 PUT 路径)|
| T3-时序 | 时序 key 合并保留 | value col 多段日期行渲染 | ✅(注入 "2026-06-25 住萧山;2027-01-01 住拱墅" → 行 2 日期段 + column flex div,m2-timeline-render.png)|
| T3-非时序 | 非时序 key 合并保留 | value col 原样串(无分段) | ✅(50 条既有记忆 value col 原样渲染,无分段)|
| T4-自定义 | resolve 选「自定义」 | input 默认填旧 value;改后落库,刷 updated_at + 重 embed | ✅(冲突80 点自定义→input 默认填 "2026-07-18 余杭区;萧山，西湖区";改 "临平区(用户自定义合并)" 确认→survivor 231 落该值 + updated_at 刷 + 298 删)|
| T4-空 | 自定义 input 清空提交 | 阻止提交(校验非空) | ✅(冲突80 清空 customValue 点确认→后端返 false 阻止,80 仍 PENDING)|
| T5-老数据 | 看老 value(无日期段)key | 时间线 parse 当单值渲染,不崩 | ✅(既有 50 条全无日期前缀,isTimelineValue=false 走原样分支,0 崩)|
| T6-流水账 | 用户发日记型消息(只 user) | daily_log key 时间线段入 value;assistant 回答不入 | ⬜(LLM 抽 daily_log key 非确定,未触发;isDailyLogKey 启发式 + 写侧 gate 由 review 静态验证,mergeValuesInto 无单测走 IT/E2E 惯例)|
| T7-日期召回 | 问「昨日/今日总结」 | 按日期 entities 命中对应段 | ⬜(extract 抽日期进 entities 是 prompt 改动,LLM 非确定难断言;逻辑靠 entities 召回既有机制)|

反向/半选:T4 空(阻止)、T1 未答直接合并(走默认非时序,不崩)、T5 老数据(单值)。

## 自动化覆盖
- 后端单测:`MemoryValueTimeline`(parse/join 两分支)+ merge 时间线/中文逗号两分支 + temporal 标读写。
- mvn test:MemoryServiceTest 21/21 + ChatSessionServiceTest 14/14 零回归 + 新增 M2 套件。
- vue-tsc:净(0 error);vitest 99 套件无回归。
- playwright-mcp 冒烟:面板加载 + 时间线渲染验过(m2-memory-panel.png / m2-timeline-render.png)。
- **Phase4 API 冒烟(2026-07-18)**:key-meta GET/PUT 4 组合 + V43 迁移 + 时间线 value 注入渲染,全绿。

## 降级验证
- `rag.memory.timeline.enabled=off` → merge 退回中文逗号 join,value 无日期前缀(现状)。

## 结论(出口)
联动清单每条有用例(含反/半),自动化 + 冒烟双绿后填实测列。
