# 修复IV 开发进度1 · chunk A 前端小件四项

- **日期**：2026-08-27　**commit**：`58f64beb`
- **plan**：[IV_A_前端小件四项](../../docs/plans/人工测试遗留问题修复IV_A_前端小件四项.plan.md)　**spec**：[IV设计 §3.1/§3.2/§4.2/§5.5](../../docs/specs/人工测试遗留问题修复IV设计.md)

## 实现内容

| 步骤 | 实现 | 关键位置 |
|---|---|---|
| A1 审计列（12x-2） | 用户列= username 快照（证据链）+（operatorName）括注 + 备注 tag；已删用户回落快照 | AuditLogView.vue 用户列 render（原 :159） |
| A2 全选/反选（17x-1） | listbox 底部 sticky 操作行（全选=并集/反选=翻转，作用域=当前候选）；候选 slice 20→50；「已选 N/候选 M」计数，≥50 显上限提示；搜索中禁用；chips 限高 96px 滚动 | UserPicker.vue 模板 `__bulk` 行 + selectAll/invertSelection |
| A3 下拉时机（17x-2） | `@focus=openList` → `@mousedown=openList`；onInput 即 open（tab 聚焦后打字场景）；↓ 键本就 open（moveActive） | UserPicker.vue input 绑定 + openList 注释 |
| A4 候选权限（17x-2b） | searchCandidates requireOwner → requireRole(MANAGER)，与邀请口径一致 | ProjectGroupService.java searchCandidates |
| A5 分辨率整行（C-5） | 分辨率独立 prop-panel__field 整行（照 C3 比例先例），时长单独一行 | PropertyPanel.vue 视频参数区 |

**顺手修（偏离记录）**：PropertyPanel 三处 capability 空档（sizePresets :1254 / toUpperOptions :1291 / optimizeModes :249）补 `?? []`——存量基线 3 个 unhandled rejection 根因，修后 vitest exit 0。

## 测试结果

- 前端：vitest 全量 **827/827 全绿 + 0 unhandled + exit 0**（UserPicker.test 重写：11 用例含 IV 新增 5——程序 focus 不弹/全选并入/反选翻转/换关键词既有不丢/loading 禁用）；vue-tsc 0 error
- 后端：`mvn test -Dtest=ProjectGroup*` 绿（A4 权限改动无回归）
- 人工待验（进 P4 测试方案）：邀请弹窗打开不弹候选/点搜索框才弹（L2）；审计列显示（L4）；批量充值页全选手感（L1）

## 下一步

chunk B 画布交互四项（C-1 两段式 / C-2 上游直显 / C-3 手柄热区 / C-6 面板拖宽）。
