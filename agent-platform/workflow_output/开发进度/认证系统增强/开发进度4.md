# 开发进度4 · 12x 开关回退：邮箱验证总开关默认关（2026-08-23）

> 背景：B1（注册强制邮箱码）+ B5（充值邮箱门）在真实邮箱通道接入前没法人工测试——注册/充值全卡死。
> 拍板：抽总开关默认关，代码不删，接入真邮后网页翻开即复活。

## 本轮产出（commit `f4d05a55`，双端全量绿后 commit）

| 改动 | 文件 | 说明 |
|---|---|---|
| 开关键 | `V146__mail_verification_required_switch.sql` | `auth.channel.mail.verification-required` 默认 false |
| 读取口 | `AuthChannelSettingService.isEmailVerificationRequired()` | +VO/UpdateRequest 字段 `verificationRequired`（复用认证通道配置管线） |
| 注册 | `AuthService.registerInternal` | 关=邮箱可选不验码，填了建 EMAIL 凭证 verified=FALSE；开=B1 原语义。`RegisterRequest` 邮箱/码去静态必填（服务端按开关判） |
| 发码 | `EmailService.sendRegisterCode` | 关=400「当前注册无需邮箱验证码」 |
| 充值门 | `PaymentOrderService.createOrder` | 关=B5 门整体跳过（通道未接无人能验，硬门=充值全废） |
| 公开透出 | `AuthPublicConfigController` /api/auth/channels | +`registerEmailCodeRequired`（免新端点/白名单） |
| 前端 | `LoginView` 传 prop → `RegisterModal` | 验证码行 v-if、邮箱必填规则 computed 动态化、notice 双文案、提交按开关带/不带 emailCode |
| admin UI | `AuthChannelSettingsTab` 邮件卡 | 「邮箱验证总开关」行（提示：先测试发信成功再开） |

## 测试
- 后端新增：AuthServiceTest+3（关/邮箱留空/开时空缺拒）、EmailServiceTest+1（关发码拒）、PaymentOrderServiceTest+1（关门跳过）、AuthChannelSettingServiceTest+1（开关 roundtrip）；既有 B1/B5 用例默认 stub 开，语义不回退。
- 前端新增：RegisterModal.test.ts 3 例（开/关/关但填邮箱）+ AuthChannelSettingsTab +1（开关行渲染+payload 携带）。
- 结果：后端全量 EXIT=0（0 失败）；前端 106 文件 734 绿 + vue-tsc 绿。

## 运维口径
- **接入真邮开启姿势**：SMTP 四要素 → 测试发信收到 → 开「启用邮件」→ 开「邮箱验证总开关」。顺序乱=注册/充值卡死。
- IT（AuthIntegrationTest）显式 jdbc 开开关覆盖 B1 链路；cleanup 删键回默认。

## 文档同步
- 12x 问题单：✅ 段后追加「开关回退」小节（状态表 + 开启姿势 + 改密码指引）。
- user-ops：§7/§9 加开关生效前置说明；§10 加第 6 步总开关。
- 速查表 01：12x 段加「开关回退」小节（键/消费点/透出/commit）。
- feature-map：追加开关回退段（代码位置 + 大白话 + Mockito/NModal 踩坑）。
