# P05 建造期：任务编排与大白话层 · 开发进度 4

> 本轮完成 S4 大白话翻译层（FR-036）。

## 已完成工作

### S4 大白话翻译层（FR-036）
- 新建 `src-ui/lib/translator.ts`：
  - `GLOSSARY` 本地术语表覆盖 build/commit/diff/lint/test/runner/checkpoint/token/sandbox/LLM/API/stderr/stdout/error/warning/deploy 等高频词。
  - `explainTerm(term)` 大小写不敏感命中本地表。
  - `translate(text, context?)` 先查内存缓存；未命中走 `claude-haiku-4` cheap 模型；失败回退原文。
  - `clearTranslateCache()` 供测试重置。
- 单元测试 `src-ui/lib/translator.test.ts`（mock `cloudApi.chatComplete`）：
  - 术语表命中、空文本、缓存命中、cheap 模型调用、失败降级。
- `src-ui/stores/ui.ts` 增加 `plainMode` 全局开关，持久化到 `localStorage.devpilot_plain_mode`。
- 新建 `src-ui/components/plain/TermBubble.tsx`：悬停显示术语解释（纯 CSS tooltip）。
- 新建 `src-ui/components/plain/PlainText.tsx`：
  - 根据 `plainMode` 决定显示原文或翻译。
  - 异步翻译显示「翻译中…」占位。
  - 自动把术语用 `TermBubble` 包裹。
- `src-ui/components/hud/HudTile.tsx`：`hint` 类型从 `string` 放宽到 `React.ReactNode`。
- 接入视图：
  - `src-ui/views/dashboard/Dashboard.tsx`：四个 HUD hint 包 `PlainText`。
  - `src-ui/views/Build.tsx`：阶段说明使用 `PlainText`，并加开关按钮。
- commit: `814cddc`

## 质量门结果

- `cargo fmt --all`：通过。
- `cargo clippy --workspace --all-targets -- -D warnings`：通过。
- `npx tsc --noEmit`：通过。
- `npm run test`：40/40 通过（含新增 translator 5 个用例）。

## 下一步

- S5 存档点列表与回滚（FR-037）。
