# 开发进度 2 —— Step 4~11（真机实现期）

日期：2026-08-24 ｜ 承接：开发进度1.md（Step 1~3，类型/安全层地基）

## 本轮完成

### Step 4 · koffi 基建 + 截图（FR-001）
- `src/driver/win/ffi.ts`：user32/gdi32/gdiplus 加载；**类型必须先于函数声明注册**（RECT/GUID/GdiplusStartupInput）；SetProcessDPIAware
- `src/driver/win/capture.ts`：PrintWindow(PW_RENDERFULLCONTENT) → GDI+ PNG → base64；临时文件 finally 删除（FR-016）
- 真机：44ms / 31KB ✅（目标 ≤300ms）

### Step 5 · UIA 元素树（FR-002/003）⚠决策检查点触发
- koffi 调 UIA COM 超预算 → 切 **PowerShell 常驻工作进程**（`src/driver/win/uia.ps1` + `uia.ts`），ADR-2 已修订
- 坑①：PS1 含中文必须 **UTF-8 BOM**（Node fs 写 `﻿`；git-bash printf 会写坏）
- 坑②：Node 写 UTF-8 到 PS stdin，PS5.1 默认按 GBK 解码 → `StreamReader(OpenStandardInput, UTF8)`
- 真机：Tauri 应用 1089ms / 记事本 1371ms（webview 树偏重，2s 内可控）

### Step 6 · UIA 直控 + 两层降级判定（FR-004/005/006/012）
- `uiaActions.ts`：Invoke>Select>Toggle>Expand 优先；SetValue 输入；不可达抛 `NeedsFallback`

### Step 7 · SendInput 前台执行层（FR-004~011）
- `input.ts`/`keymap.ts`：mouse_event/keybd_event、剪贴板中文输入、组合键 down+逆序 up、滚动、拖拽、wait≤10s
- 真机：moveTo/cursorPos 验证 ✅

### Step 8 · MCP 11 工具 + 统一错误码（FR-017）
- `src/tools/index.ts`：skyshot/tree/click/double_click/type/key/scroll/drag/move/wait/confirm_app；zod 校验；9 错误码
- `test/tools.test.ts`：MCP Client 真 stdio 连子进程，5 用例（白名单流/TARGET_BLOCKED/参数校验）✅

### Step 9 · 审计日志 + 脱敏（FR-015/016）
- `safety/audit.ts`：JSONL + 10MB×3 轮转 + Password 角色/密码类关键词脱敏

### Step 10 · SKILL.md（FR-018）
- `skill/SKILL.md`：工具速查/标准循环/敏感动作分级/提示注入防御；已同步 `.claude/skills/computer-use/`

### Step 11 · 记事本靶子集成（AC-007/009/012/013）
- `scripts/integ_notepad_run.mjs`：tree→定位 edit→输入→截图证据
- **本机老版记事本 Edit 控件未被 UIA ControlView 代理**（树只有 Window→Pane→Pane）→ 走两层降级（激活+点击+SendInput），FR-012 设计正好覆盖此场景
- 截图 38ms ✅；**⚠输入环节截图证据待补：机器处于锁屏（前台=Windows 默认锁屏界面），SendInput 不可达任何应用**，需解锁后重跑

## 测试与提交
- vitest：29 passed / 2 skipped（capture.integ CU_INTEG 门控）
- check_all（sh 版）全绿；npm audit 因镜像源无端点为 advisory
- commits：S4~S10 分步提交 + `feat: S11 记事本靶子集成…`（uia.ps1 编码修复 + 集成脚本）

## 下一步
1. **等解锁屏幕** → 重跑 integ_notepad_run.mjs 补输入证据（AC-013）
2. Step 12 dogfood：SKILL.md 装入 Claude Code，voice-to-text GUI 回归（AC-022/023）
3. 收尾：测试方案/README/Feature Map/User-Ops/AGENTS.md 沉淀
