# 视频 TOKEN 分辨率分档计价 · 测试方案

> 2026-08-27。规格《视频TOKEN分辨率分档计价》（V162）；plan 联动点 8 条逐条对应用例。
> 自动化基线（已全绿）：PricingServiceTest 36/36（+6 档位用例）、PricingConfigServiceTest 53/53（+9 校验/导入三态用例）、前端 vue-tsc 0 / PricingConfigView vitest 5/5 / vite build 过。
> commits：C1 迁移+实体 `acf887fe` / C2 videoCost 取档 `edae2308` / C3 配置侧全链 `df3f6c6a` / C4 前端 `b69f4a39`。
> 账号：admin/admin123。前置：后端以含 V162 代码运行（Flyway 162 已应用）。
> 本功能含真实扣费口径，**P4 需人工真跑**（自动化只到 mock usage 层）。

## 一、功能联动用例（L1-L8，对齐 plan 联动点清单）

### L1 kind 切换 → 槽位区显隐
| # | 操作 | 预期 | 档位 |
|---|---|---|---|
| 正1 | 编辑 VIDEO+TOKEN 行 | 显「通用每百万价」+4 档输入（480p/720p/1080p/4K） | 正向 |
| 正2 | kind 切 CHAT（新增流程选文本候选） | 槽位区消失，提交 payload 不带字段（F12 网络面板核对） | 反向 |
| 边 | 切回 VIDEO+TOKEN | 槽位草稿保留/复位正确，不残留他行值 | 往返 |

### L2 SECOND↔TOKEN 模式互斥
| 正1 | TOKEN 行配槽保存 | 成功；切 SECOND 再保存 → 槽被清（payload 恒 null） | 正向 |
| 反1 | 手工构造 SECOND+槽请求直调 API | 400「仅 VIDEO TOKEN 模式有效」 | 服务端拦 |
| 边 | SECOND 行编辑重进 | 无槽位输入，列表分档列显「—」 | 不回归 |

### L3 有/无参考两行各自配槽（不串价）
| 正1 | seedance 无参考行配 4k=111.2，有参考行配 4k=222 | 两行列表摘要各自正确 | 正向 |
| 正2 | 只配无参考行，提交带参考 4K 任务 | fallback 命中无参考行 → 按**该行** 4k=111.2 扣（不跨行） | 兜底取本行 |
| 边 | 有参考行 4k 空、无参考行 4k 有值 | 带参考任务回落**本行**通用价，绝不取无参考行档价 | 不跨行钉死 |

### L4 槽位保存 → 列表摘要联动
| 正1 | 配 480p=6.5 / 4k=111.2 保存 | 列表「分档价 ¥/百万」显 `480p 6.5 / 4K 111.2` | 正向 |
| 反1 | 清空全部档保存（全留空） | 保存成功，摘要回落「—」（=全按通用价） | 清空生效 |
| 边 | 编辑重进 | 各档回显正确；通用价列不受槽位影响 | 回填 |

### L5 导出带槽
| 正1 | 导出 JSON | 有槽行含 `tokenPricePerResolution`；无槽行不带字段 | 正向 |
| 边 | 旧前端/工具读新文件 | 未知字段被忽略不报错（Jackson ignoreUnknown） | 兼容 |

### L6 导入三态（null / {} / 非空）
| 正1 | 导入项**缺**该字段 | 库中现有槽**不动**（旧文件防误清） | 缺失=不动 |
| 正2 | 导入项 `tokenPricePerResolution: {}` | 库中槽**清空**（存 NULL） | 空对象=清空 |
| 正3 | 导入项只带 `{"4k":120}` | **整体覆盖**——其余档被清，只剩 4k | 非空=整替 |
| 反 | 一行带非法键（如 general/负数） | 仅该行进 errors，他行照常导入 | 逐行容错 |

### L7 编辑身份锁定 × 槽位可改
| 正 | 编辑行改槽位价保存 | 成功（槽位非身份字段）；providerId/model/kind/hasReference 仍锁定 | 非身份 |
| 边 | 判重/候选展开 | 配了槽后候选列表/判重键无变化（仍参考面×2） | 不回归 |

### L8 结算按档扣费（mock usage 自动化 + 真跑人工）
| 正1 | （自动化已盖）4K 任务 + 槽 4k=111.2 → cost=tokens÷1M×111.2 | 单测绿 | 精度 |
| 正2 | （自动化已盖）720p 未配档 → 回落通用价；脏 JSON → WARN+通用价 | 单测绿 | 兜底 |
| 正3 | **（人工）真跑**见下节 §二 | 流水金额=档价×token÷1M×比例 | 真链路 |

## 二、人工测试标记（自动化覆盖不了，P4 必做）

1. **配价三处一致**：admin 价表页给 seedance2.0 配「有参考/无参考 × 通用+4 档」→ 表单保存重进回显 / 列表摘要 / 导出 JSON 三处一致。
2. **真跑 4K 对账**：真提交 seedance 4K 任务 → Ark 回传 token → 流水/账单金额 = 4k 档价×token÷1M×阶梯比例；HOLD 预扣多退少补金额随档变。
3. **未配档回落**：改跑 720p（该行未配 720p 档）→ 按通用价扣，流水可对。
4. **旧导出不误清**：用**本功能上线前**导出的价表文件导入 → 已配槽位不被清（字段缺失=不动）。

## 三、通过标准

L1-L8 正向+反向+边界全过 + 人工 4 项过 → 勾销 人工测试问题/7_积分系统.md「未解决」条目并回写规格变更记录；任一失败记新「未解决」条目。

## 四、附：回滚口径（运维）

- 业务回滚：价表页清空全部档位（保存即 `{}` 清空）→ 行为回存量通用价口径，逐字节等价。
- DB 回滚：`ALTER TABLE pricing_rule DROP COLUMN token_price_per_resolution;`（迁移仅 ADD COLUMN nullable，无数据回填）。
