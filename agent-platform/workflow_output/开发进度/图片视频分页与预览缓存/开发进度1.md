# 图片视频分页与预览缓存 · 开发进度1

> 功能：[计划](../../docs/plans/图片视频模块历史分页与预览缓存.plan.md) · [规格](../../docs/specs/图片视频模块历史分页与预览缓存设计.md) · [测试方案](../../docs/测试方案/图片视频模块历史分页与预览缓存测试方案.md)
> 对应问题：4x#2（视频历史分页）、4x#3/6x#1（参考图悬浮+灯箱）、6x#2（图片两级缓存）

## 本轮（2026-08-18）· 功能级预处理 + Step 1 后端分页

### 产出文档
- 测试方案（人工交互项 + L1-L5 联动用例含反向/边界）：`docs/测试方案/图片视频模块历史分页与预览缓存测试方案.md`

### Step 1 代码（commit 26aa626）
**实现**：`GET /api/media/tasks` 分页化（4x#2）
- `MediaGenTaskMapper.java`：`selectHistory` 加 `offset` 参数（`LIMIT #{limit} OFFSET #{offset}`）；新增 `countHistory`（同 where 块，注释约定两方法同步维护）
- `MediaGenQueryService.java`：`list()` 两重载并入 `page(userId, admin, query, from, to, kind, page, pageSize, legacyLimit)` → `PageResult<MediaTaskVO>`；`resolvePageSize` 白名单 {5,10,20,50} 非法/缺省静默回落 10，legacyLimit 仅在 pageSize 缺省时生效（旧契约 1-100，允许 25 等非白名单值）；page<1/缺省归一为 1；count 与 list 同一组归一化参数（防 total 漂移）
- `MediaGenController.java`：list 端点加 `page`/`pageSize` 参数，返回 `R<PageResult<MediaTaskVO>>`（**破坏性变更**，javadoc 已注明前端须同版本适配）
- `MediaGenQueryServiceTest.java`：list_* 迁移为 page_*（12 用例），新增：limit 兼容三态（仅 limit / 双传 pageSize 胜 / 非法回落）、越界空页 total 保留、页码归一

**关键位置**：`resolvePageSize` ≈ `MediaGenQueryService.java:75`；mapper 双 SQL ≈ `MediaGenTaskMapper.java:25-80`
**测试**：`mvn -Dtest=MediaGenQueryServiceTest test` 全绿（12 list→page 迁移+新增，合计 28 用例）
**消费方盘点**（Step1 硬前提）：后端仅 `MediaGenController:141`；前端 `api/media.ts listTasks` → `ImageGenView.vue:558`、`VideoGenView.vue:938`、`media.test.ts`、`VideoGenView.test.ts`；`mediaEdit.ts` 是 `/api/media/edit` 另一端点不受影响
**偏离记录**：见计划「备注」1-3 条（PageResult 复用 / list 并入 page / 盘点结论），规格 §3.1 已回写
**安全清单**：输入校验（page/pageSize）✅、错误处理（count 同参数）✅；ETag/审计项归 Step 6

### Step 2 代码（commit 61e7e871）
**实现**：两页 remote 分页接入（4x#2）
- `api/media.ts`：`listTasks` 返回 `MediaTaskPage`（PageResult 语义包裹）；`MediaTaskQuery` 加 `page/pageSize`（`limit` 标 @deprecated 兼容保留）；新增共享拼装 `buildHistoryQuery`（`rangeType`：'day'=图片页含尾日全天、'datetime'=视频页精确尾时）
- `VideoGenView.vue`：n-data-table 改 `remote` + `historyPagination` computed（onChange/onUpdatePageSize 即时拉取；切条数回落第 1 页=L5）；筛选 watcher 重置 `historyPage=1`（L1）
- `ImageGenView.vue`：历史列表下受控 `NPagination`（page/pageSize/itemCount/page-sizes 5/10/20/50），同款 L1/L5 处理；`.history__pagination` 样式
- 测试：`media.test.ts` 9 用例绿（新增 page 透传 + buildHistoryQuery 4 用例）；`VideoGenView.test.ts` 2 用例绿（mock 改包裹结构）
**存量修复**：VideoGenView.test.ts mount 缺 NDialogProvider → `useDialog()` 抛 no provider（先于本 chunk 存在，stash 基线验证），补 `useDialog` mock
**验证**：`vue-tsc --noEmit` 零错

### Step 3 代码（commit eaa9ed18）
**实现**：共享预览组件两枚（画布计划 Step 8 / 反推计划 Step 3-4 的前置）
- `components/media/MediaLightbox.vue`：ImageGenView 内联灯箱抽取——Teleport + 遮罩/Esc/右上角钮三路关闭，点图不误关；src watch 挂/摘 keydown 防 listener 泄漏
- `components/media/HoverPreviewImage.vue`：n-popover 受控（trigger=manual）+ mouseenter 300ms 防抖（`delay` prop 可调）+ mouseleave 即关 + 重进重置计时 + unmount 清 timer；`previewSrc` 由调用方传已加载 objectURL → 悬浮零请求
- ImageGenView 替换内联灯箱（删原 `.lightbox` 样式）
- 单测 9 用例（组件目录）：三路关闭/监听器移除/防抖三态/delay 调参/占位文案；`components/media/` 全目录 16 用例绿

### Step 4 代码（2026-08-18）
**实现**：参考图预览接入两页
- ImageGenView 参考图 chip：包 HoverPreviewImage + 点击进既有 `previewSrc` 灯箱（L2）
- VideoGenView：首帧/尾帧/参考图瓦片同款 hover+灯箱（`lightboxSrc`）；参考视频瓦片静音预览 + ▶ 角标 + 点击 n-modal 播放弹窗（controls+autoplay，v-if 含 show 关闭即卸载停播；只读 url 不改表单=L4）；音频瓦片不动（已内联 controls）
- 历史回填瓦片走同一模板区域，自动获得预览能力
- 验证：`vue-tsc` 零错；VideoGenView.test + components/media 18 用例绿

### Step 5 代码（commit 2f12cfcd）
**实现**：会话内文件预览 LRU 缓存（6x#2 前半）
- `utils/blobLruCache.ts`（新）：LRU 工厂抽自 media.ts 手写模式——键→Blob、命中触碰（删+重插末尾）、满条数/满字节从最旧逐出、单条超字节上限不入池、重复键覆盖且字节记账修正；限额构造注入（单测用小限额跑真实逻辑）
- `api/file.ts`：`filePreviewCache = createBlobLruCache(80, 128MB)`；`fetchFilePreview` 命中→缓存 Blob 新建 objectURL（零请求），未命中→request blob→入池→objectURL；失败不缓存；导出 `clearFilePreviewCache`（测试出口）
- 与 `mediaBlobCache`（6 条/256MB）分池，合计 ≤384MB；fileId 不可变 → 会话内命中无过期
- 单测：`blobLruCache.test.ts` 8 用例（逐出序/字节上限/超限不入池/重复键记账/clear/多轮进出不漂移）+ `file.test.ts` 4 用例（带鉴权拉取/命中零请求且两次新 objectURL/不同 id 各请求/失败不缓存重试）
- **存量修复**：PricingConfigView.test 5 例失败（c8976eea 加 `useDialog` 未配 provider，非本 chunk 引入），mock `useDialog` 修复（同 VideoGenView.test 前例）
**验证**：`vue-tsc` 零错；全量 vitest 78 文件 520 用例绿

### 下一步
Step 6：`FileController` ETag/304——`Cache-Control: no-cache, private` + ETag `{fileId}-{size}`，If-None-Match 命中返 304 空体；鉴权与 @AuditLog 时序不动（对齐 `MediaGenController.serveFile`）；FileControllerTest 200/304/size 变 ETag 变
