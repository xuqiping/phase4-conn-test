# 修复XI 开发进度3（C 轮：叙事角色两级词汇，2x 未解决③）

> 对应 spec `docs/specs/人工测试遗留问题修复XI设计.md` XI-3｜plan `docs/plans/人工测试遗留问题修复XI.plan.md` C 轮（C1/C2/C3/C4/C5）
> 轮序：A 右键菜单 ✅ → B 官方库 ✅ → C 两级词汇 ✅ → D 组大节点 → E 文档收尾

## C1 后端词汇地基（commit 3cd40307）

- **shape**：`asset_projects.narrative_roles` JSONB `[{key,children:[]}]`。新建 [RoleVocab.java](../../../backend/src/main/java/com/superprogrammer/asset/dto/RoleVocab.java)（`parse` 读侧容错：string→无子一级/`{key,children}` 对象/坏 JSON 回落默认五桶；`fromArray` JsonNode 逐元素判型；`flatten` 父+子扁平全集保序）+ [RoleVocabDeserializer.java](../../../backend/src/main/java/com/superprogrammer/asset/dto/RoleVocabDeserializer.java)（入参侧同判，ProjectUpdateRequest 挂 `@JsonDeserialize`）——读/入参**双容错**，旧客户端发 string 数组仍可写。
- **迁移** [V169__asset_role_vocab_two_level.sql](../../../backend/src/main/resources/db/migration/V169__asset_role_vocab_two_level.sql)：string 元素→`{"key":s,"children":[]}`（WITH ORDINALITY 保序，幂等：纯对象行跳过）+ 列 DEFAULT 两级五桶 + COMMENT。
- **normalize**（[AssetProjectService.java](../../../backend/src/main/java/com/superprogrammer/asset/service/AssetProjectService.java)）：trim/名≤30/children≤20/一级重复与同父子类重复保序静默去重；**扁平全集全局唯一**——子撞任何一级或他父子类、一级撞已收子类 → 400「叙事角色「X」重名」；空词汇拒。
- **测试**：RoleVocabTest 5 例（旧 string 数组/对象 children/混合+坏 JSON 回落/flatten 保序/反序列化双 payload）。

## C2 后端行为（commit e800c0ed）

- **reassign 两级**（AssetProjectService `reassignOnRemovedRoles`）：删子类（父保留）→ 其 link 改挂父级；删一级（子类随删）→ 一级+子类的 link 归「通用」（通用也删则仅删）。分支前**项目资产 id 预筛**（role_links 无 projectId 列，防跨项目同名 key 误伤）。日志两分支 affected 数含 fallback。
- **筛选展开**（[AssetService.java](../../../backend/src/main/java/com/superprogrammer/asset/service/AssetService.java) list）：role=一级 key → `in` 集展开 `[key+其 children]`；role=子级 key → 只 `[key]`；未知 key 单发兜底。**一键分镜** getImageCatalog 实体集 = {人物,道具,场景} ∪ 各自在项目词汇中的 children。
- **测试** +13：reassign 删子归父/删一级归通用（断言 ensureRoleLink 调用）/一级筛选 wrapper `in` 含子类/子级筛选只含子级/目录含子类资产。**坑沉淀**：MyBatis-Plus wrapper `getParamNameValuePairs()` 惰性填充——断言参数前必须先 `getSqlSegment()` 触发渲染，否则单测假红。

## C3 前端类型 + VocabEditor 两级（commit 455cef60）

- **类型**：[types/asset.ts](../../../frontend/src/types/asset.ts) 新增 `NarrativeRoleVocab{key,children}`，AssetProjectVO/ProjectCreateRequest/ProjectUpdateRequest 三处翻两级。
- **共享 util** [utils/assetVocab.ts](../../../frontend/src/utils/assetVocab.ts) `buildRoleGroupOptions`：两级词汇→n-select 分组 options（「X（不细分）」=挂一级本身，子类各为选项）——AssetProjectView/SaveToAssetDialog 两下拉共用。
- **[VocabEditor.vue](../../../frontend/src/components/asset/VocabEditor.vue) 两级重写**：一级行（input+删除 popconfirm 计数=一级+子类合计「归通用（子类随删）」）+ 子类 chips（n-tag closable 删）+ 子类添加框（回车/失焦；≤20；isGlobalDup 撞任意一级或子类→清空+行内错误）；一级与所有子类同一命名空间失焦去重；save payload `{roles: NarrativeRoleVocab[], mediaTypes}`。
- **测试**：VocabEditor.test 两级 6 例重写 + 五处消费测试 mock 翻对象形状。

## C4 前端消费点（commit 6a2e37f2）

- **[AssetMatrixFilter.vue](../../../frontend/src/components/asset/AssetMatrixFilter.vue) 左栏两级**：每 vocab 一组=一级行+子类缩进行；点一级=大类筛（后端展开含子类）、点子类=精确筛；**一级徽标聚合** groupBadge=一级+Σ子类（选类型下钻同口径）；active 联动按 plan 口径「一级 active=自身或其任一子级选中」；移动端 group 横滑。
- AssetListView「N 个叙事角色」零改动（length=一级桶数语义自然保持）。提交挂父/子 key 走 SaveToAssetDialog 分组 options（C3 已落）。

## C5 收口（本 commit）

- 全量 vitest **1042/1042**（+2：VocabEditor 重写净增、MatrixFilter 子类例）+ vue-tsc **0 错**；后端全量 **2734/2734 BUILD SUCCESS**。
- **本地 PG V169 实测**：17 行存量 string 数组全转对象（still_string=0，id6 自定义「测试」桶保序保留）· DEFAULT/COMMENT 落列；API e2e——GET 项目回 `[{key,children}]`；PUT 两级（人物+老人/孩童）落库回读一致；PUT 子撞一级 400「叙事角色「人物」重名」；已还原原词汇。
- 安全清单：normalize 全局唯一+长度/数量上限服务端强制（前端仅前置）；reassign 项目 id 预筛防跨项目越权改挂；双容错不放大攻击面（未知元素跳过）。
- 手测项（编辑分类两级保存/删子类资产归父体感/矩阵两级筛/一键分镜含子类/画布存入库分组选）并入 E 轮 S 系列手测清单。
- commits：3cd40307（C1）+ e800c0ed（C2）+ 455cef60（C3）+ 6a2e37f2（C4）+ 本 commit（C5 文档）。

## 下一步

D 轮组=大节点（D1 组框点选+互斥 → D2 整组拖 → D3 剪贴板 groups 收集纯函数 → D4 粘贴重建+撤回 → D5 收口）。
