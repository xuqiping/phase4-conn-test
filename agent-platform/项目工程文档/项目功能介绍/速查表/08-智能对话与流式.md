# 08 - 智能对话与流式

## 功能简介
对话会话管理、聊天目标（Agent/工作流）选择、消息发送、SSE 流式回复、WebSocket 通道、历史消息。

## 后端 (backend)
- 控制器：[ChatController.java](../../backend/src/main/java/com/superprogrammer/chat/controller/ChatController.java)
  - 会话：`POST /api/chat/sessions` `GET /sessions` `GET /sessions/{id}` `DELETE /sessions/{id}` `DELETE /sessions/batch`(批量删，body=Long[]) `PUT /sessions/{id}/target`(切换 Agent/工作流目标) `GET /sessions/{id}/messages`
  - 聊天目标：`GET /targets`
  - 发消息：`POST /sessions/{id}/messages` `POST /messages`（非流式）`POST /sessions/{id}/messages/stream` `POST /messages/stream`（SSE 流式）
- 记忆自服务（对话联动，详见 [09-个人记忆与冲突解决](09-个人记忆与冲突解决.md)）：**记忆二期新栈，基路径单数 `/api/chat/memory/*`**；旧栈 `MemoryController` + `/api/chat/memories`（复数）全族端点已整类移除（404）。
  - turns 流水账：[MemoryTurnController.java](../../backend/src/main/java/com/superprogrammer/chat/controller/MemoryTurnController.java)（含 `POST /turns/raw/batch-delete`）
  - 冲突裁决/总结：[MemoryConsolidationController.java](../../backend/src/main/java/com/superprogrammer/chat/controller/MemoryConsolidationController.java) — `GET /conflicts/pending` `POST /conflicts/{id}/resolve` `GET/PUT /consolidation/auto`
  - 召回：[MemoryRecallController.java](../../backend/src/main/java/com/superprogrammer/chat/controller/MemoryRecallController.java) — `POST /recall/preview`(注入预览) `GET/PUT /recall/scope`
  - 通知：[MemoryNotificationController.java](../../backend/src/main/java/com/superprogrammer/chat/controller/MemoryNotificationController.java) — `POST /notifications/{id}/ack`
  - **状态轮询（前端常驻轮询）**：`GET /conflicts/pending-count` + `GET /notifications/count`（旧 `/memories/status`、`/memories/incident`、`/memories/rag-mode` 已删）
- WebSocket：
  - [WebSocketConfig.java](../../backend/src/main/java/com/superprogrammer/chat/config/WebSocketConfig.java)
  - [ChatWebSocketHandler.java](../../backend/src/main/java/com/superprogrammer/chat/websocket/ChatWebSocketHandler.java)
  - [WebSocketAuthInterceptor.java](../../backend/src/main/java/com/superprogrammer/chat/config/WebSocketAuthInterceptor.java)（`chat/config/`，非 websocket/）
- 服务：`chat/service/` ChatSessionService、ChatTargetService（编排走 [engine](11-工作流执行与编排引擎.md)）
- 并发闸（9x#11）：[ChatConcurrencyGate.java](../../backend/src/main/java/com/superprogrammer/chat/service/ChatConcurrencyGate.java) — 每 userId+sessionId 一把 `putIfAbsent` 闸，`doStream`/`confirmInclusion` 入口抢闸，撞闸回 ERROR(“上一条回复还在生成中…”) + DONE；线程 finally 释放。前端 store 另有消息队列兜底（见下）。
- 对话完成审计（9x#7）：`chat_completed` 由 LlmBillingService 在流式裸线程落——**无线程上下文（MDC/SecurityContext 皆空）**，username 按 userId 反查 users 补齐（一次一条，低频）；traceId/clientIp 仍空。
- DTO：ChatRequest、ChatResponse、StreamEvent、SessionVO、ChatTargetVO
- **聊天计费（B2/B3，Q2/Q3/Q4=B，2026-08-26）**：[LlmBillingService.java](../../backend/src/main/java/com/superprogrammer/billing/service/LlmBillingService.java)
  - 发消息=HOLD 全额预扣（`holdChat`：个人钱包或组池按预估扣住，流水 CHAT-HOLD）；流结束按实际 usage 多退少补；扣尽差额挂 `debt_points` 欠款（DEBT/DEBT_REPAY 流水腿，充值自动冲抵），余额恒 ≥0。
  - **PROGRESS 流中报数**（Q2=B server 侧）：[LlmGateway.java:260](../../backend/src/main/java/com/superprogrammer/llm/LlmGateway.java#L260) 按累计已产字符（净化后口径=用户实见）节流发 `PROGRESS` 事件（字段 chars/estimatedTokens/estimatedPoints）；`estimateCharsPoints` 字符÷系数折算，估价失败返 null 不发。前端计数跳动仅参考，**DONE.usage 到达后以精确值为准**（ChatSessionService:657 滤内部事件仅透传 PROGRESS）。
  - **取消折算**（Q3=B）：取消/中断按已产字符折算积分，实耗=min(折算, 预扣)，差额退（REFUND CHAT-HOLD，幂等键 `chat-cancel-{ref}`）。
  - **缓存命中计价**（9x-1，2026-08-26）：两家 Provider `cachedTokens` 口径归一 → `llm_usage_logs.cached_tokens` 落库 → HOLD/结算三腿计价（输入/输出/缓存各自每百万价）；价表「缓存价」留空=按输入价（老价表行为不变）。DONE.usage 含 `cachedTokens`，前端消耗明细显「含缓存命中 N」。
- 实体：`chat/entity/` ChatSession、ChatMessage

## 前端 (frontend)
- 视图：[ChatView.vue](../../frontend/src/views/ChatView.vue)
- 组件：[chat/](../../frontend/src/components/chat/) — ChatInput、SessionList、TargetSelector、ModelSelector、MessageBubble、MarkdownContent(9x#13)、SaveChatToAssetDialog(9x#12)、ExecutionDetail
- 渲染（9x#13）：[utils/markdown.ts](../../frontend/src/utils/markdown.ts) — markdown-it（`html:false` 防 XSS，linkify+breaks，链接 `target=_blank rel=noopener`）；助手气泡/流式块走 `<MarkdownContent v-html>`，用户消息保持纯文本。
- 入库（9x#12）：MessageBubble 助手气泡「入库」按钮 → SaveChatToAssetDialog（项目过滤=OWNER/EDITOR 且含文本类媒体类型；content 包 `{body}`，剧本类包 `{synopsis}`）→ `POST /assets/projects/{id}/assets`。
- 消息队列（9x#11）：[stores/chat.ts](../../frontend/src/stores/chat.ts) `messageQueue`——`sendStreamingMessage` 入口 `sending=true` 时入队；DONE/ERROR/stop/REST 完成各端 `scheduleQueueDrain()` 300ms 后续发；撞后端闸的 ERROR(“生成中，请稍候”) 会回收气泡并回队首。切会话清队列。ChatInput 生成中**不禁输入**（占位提示排队），ChatView 有「排队 N 条 + 清空队列」行。
- API：[chat.ts](../../frontend/src/api/chat.ts)、[chatTarget.ts](../../frontend/src/api/chatTarget.ts)
- 状态：[chat.ts (store)](../../frontend/src/stores/chat.ts)

## Sidecar
对话若指向工作流，触发 Sidecar 执行（见 [12-Runtime-Sidecar执行](12-Runtime-Sidecar执行.md)）。

## ⚠️ 调试注意
- 流式路径在 reactor-http-nio 线程，**禁 `.block()`**；LLM/WebClient 须 `subscribeOn(boundedElastic)`。复现 bug 必须打 streaming 端点，non-stream servlet 跑通会掩盖问题。
- **`doStream` 用裸 `new Thread()`**（[ChatController.java:139](../../backend/src/main/java/com/superprogrammer/chat/controller/ChatController.java#L139)）而非线程池，每条流式请求起一个无上限线程，`blockLast(120s)`；LLM 挂则线程挂 120s，异常还 fallback 同步 `sendMessage` 再调一次 LLM。高并发或 LLM 慢时**线程无限膨胀**——是 [RB-001 死亡螺旋](../项目漏洞核查/运行时bug.md) 的线程耗尽贡献源之一。
- 记忆轮询（现 `conflicts/pending-count` + `notifications/count`）本应秒回。若它们开始 15s 超时 = **Tomcat 线程池被打满**（被挂死的 LLM/streaming 调用吃光），不是端点本身慢——见 [运行时bug.md](../项目漏洞核查/运行时bug.md) RB-001（旧栈 `/memories/status`/`incident` 轮询端点已删，当时现象同理）。

## 数据表
`chat_sessions`、`chat_messages`


## 待增修改
- 可以像vscode一样增加页，可以生成展示对比，比如同一套提示词，用不同的大模型，同一套提示词用不同的智能体