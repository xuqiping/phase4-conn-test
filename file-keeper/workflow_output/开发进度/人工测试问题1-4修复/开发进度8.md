# 人工测试问题 1—4 修复 · 开发进度 8

> 日期：2026-09-03｜对应计划：Chunk 7｜状态：✅ 代码与文档完成｜代码提交：`1d95de8`

## 本轮实现

- 剪贴板左侧新增全部、未分组、自定义组和管理入口，主页面筛选写入本地存储。
- 新增分组管理弹窗，支持新建、重命名、删除二次确认、错误提示和初始焦点。
- 右键/键盘菜单支持置顶、取消和移动；批量置顶/移动把全部 ID 一次提交。
- Pinia 先更新界面，失败按快照恢复；每个条目记录操作版本，较旧失败不能覆盖较新成功。
- 自定义组中移出的记录立即消失并清理不可见选择，失败时恢复记录和选择。
- 快捷面板改用独立 `quickPanelItems/query`，固定查询全部分组，不覆盖主页面列表。
- 新 UI 全部进入中英文语言包；分组导航使用 `aria-current`，类型按钮使用 `aria-pressed`，菜单支持菜单键/`Shift+F10`、`Esc` 和焦点返回。
- 本地脱敏日志只含操作编号、动作、数量、耗时和结果码。

## TDD 与验证

- RED：先新增乐观排序、失败回滚、乱序响应、筛选移出、筛选持久化、快捷面板隔离和组件入口测试，共出现 10 个预期失败及缺失组件错误。
- GREEN：API/Store/组件/快捷面板定向 79/79；前端完整 45 文件、294/294；`npm run build` 通过。
- Rust：`clipboard_storage` 21/21、`floating_ball_position` 4/4，`cargo check --bin file-keeper` 通过。
- 完整 `cargo test` 未通过：既有 `src/office/tests/state_machine_tests.rs` 调用当前状态机不存在的 `complete_with_validated_summary`，产生 10 个编译错误；本轮未修改 Office 状态机或测试，未越界修复。

## 文档交付

- 重写 4 份 Feature Map，删除旧商业门禁、布尔关闭和“缺少置顶 UI”等过时描述。
- 新建公共基础设施、文件管理、进程管理、剪贴板 4 份 User-Ops。
- 新建本功能 README；更新人工测试方案、实施计划和两级进度总览。
- 人工测试问题原文仍保持“待 Phase 4 实测”，未提前标记已解决。

## 安全与运维结论

- 未新增依赖、服务端请求或上传路径；分组错误使用通用提示，不回显正文。
- `.lnk` 删除边界、参数绑定、事务删组、窗口标题文本渲染、本地悬浮球和数据不上传均已有代码/自动化证据。
- 无远程告警或后台运维页；本地操作日志与失败降级符合计划决定。

## 下一步

Phase 3 计划内开发已结束。进入 Phase 4 前需先决定是否单独修复既有 Office 状态机测试编译不一致；随后执行 M1—M13、L1—L6、E1—E6 实机验收和性能测量。
