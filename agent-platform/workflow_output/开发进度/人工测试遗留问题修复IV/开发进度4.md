# 修复IV 开发进度4 · chunk D 项目组默认额度与成员可见（17x-3 / 17x-4）

- **日期**：2026-08-27　**commit**：`75920ed0`
- **plan**：[IV_D_项目组默认额度与成员可见](../../docs/plans/人工测试遗留问题修复IV_D_项目组默认额度与成员可见.plan.md)　**spec**：[IV设计 §3.3/§3.4](../../docs/specs/人工测试遗留问题修复IV设计.md)

## D1 逐接口权限核对清单（只读产物）

ProjectGroupController 全端点第一层均 `@RequirePermission("project-group:manage")`；第二层 service 口径：

| 端点 | service 闸口 | 本轮变化 |
|---|---|---|
| GET /{id}（detail） | getDetail：原 MANAGER+ → **MEMBER+/组长/admin** | ✅ 放宽+裁剪 |
| GET /{id}/overview | getDetail 同上 + 流水分页 | ✅ 放宽+流水裁剪 |
| GET /{id}/outputs | outputs：组长/管理/admin 全量，成员强制 self，非成员 403 | 不动 |
| 其余（成员 CRUD/流水写/审批/设置/邀请/移除/划拨/回收…） | requireRole(MANAGER)/requireOwner 不变 | 不动 |

overview 聚合含管理数据（组池余额/在途/全员流水）→ 按 spec §3.4 同口径裁剪：MEMBER 流水仅本人行 + balanceAfter null + detail 余额/在途 null。

## 实现内容

| 步骤 | 实现 | 关键位置 |
|---|---|---|
| D2 默认 0+不限冻结 | invite：`quotaOrDefault = quota ?? ZERO`（归一化放 V156 预检**之后**——管理有限额时 null 仍先报「须填限额」），revive/新插均落该值；accept：存量 PENDING 邀请 quota null 兜底落 0；pool decide：审批通过 `insertMemberRow(..., ZERO, owner)`（V156 毒化顾虑消失，预算仍挂组长）；updateQuota：null → 400「不限额度已停用…」（requireRole 后、signum 前）；建组组长行仍 null（决策 7 豁免，未动） | ProjectGroupInviteService.java invite/accept；ProjectGroupPoolService.java decide；ProjectGroupService.java updateQuota |
| D3 受限视图（后端） | getDetail 全重写：权限 MEMBER+/组长/admin（403 话术不变「非本项目组成员」）；**单点裁剪**在 members 组装处——`crop = memberView && 行≠本人`：额度/已用/名下/欠款×2/可分配/分配人/allowedKinds/可见性覆盖 → null；username/displayName/remark/role/joinedAt/owner 保留；本人行完整；memberView 时组池 balance/inflight → null。鉴权与裁剪共用同一次成员行查询 | ProjectGroupService.java getDetail |
| D3 overview 同口径 | overview 复用 getDetail；新增管理视角判定（admin ‖ owner ‖ MANAGER）——非管理视角：流水 wrapper `.eq(actorUserId=viewer)` + 行 VO balanceAfter→null | ProjectGroupQueryService.java overview |
| D4 前端跟随 | 邀请弹窗默认 0+文案；调额 prompt 必填数值（空→本地拦「不限额度已停用」）+存量 null 显「不限（遗留）」；成员 tab 拆两层——tab 对组内成员开放（默认落地成员 tab），邀请按钮/邀请列表/流水/审批/设置仍 canManage；memberColumns 拆分：非管理=受限列集（用户/角色/加入时间+本人行额度内联「X · 已用 Y」，他人显 '-'）+管理=全列（限额列 null 显「不限（遗留）」）；loadOverview 去 canManage 闸；decideJoin 提示「该成员当前额度 0，请在成员表调限额后可用」 | ProjectGroupsView.vue（tabs 结构/邀请弹窗/openQuota/memberColumns/loadOverview/onTabChange/openGroup/decideJoin/openAddMember） |

**偏离记录**：无。plan D4「MEMBER 视图本人行额度内联」实现为独立「额度」列（他人 '-'、本人 `限额 · 已用`），与受限列集并排——比内联进用户列清晰。

## 测试结果

- 后端 `mvn test` 全量 **2634/2634，0 failures 0 errors**（基线 2550+ → +9：getDetail 三角色矩阵 4 例——MEMBER 他人行 9 字段裁空+组织信息保留+本人行完整 / MANAGER 全显 / 组长全显 / admin 全显；overview 成员/管理两视角 2 例；邀请默认 0 三例——组长不填落 0 / 被限额管理 null 仍先报须填限额 / 存量 PENDING null 兜底 0；池审批落 0 一例（新 ProjectGroupPoolServiceTest）；调额 null 400 改写 2 处旧断言——原「不能不限」被前置 400 涵盖、原「转不限 QUOTA_ADJUST」流水腿删除）
- 中途一坑：QueryService overview 的 managerView 在 lambda 里捕获需 final——改 `boolean mgr` 临时变量 + `final boolean managerView` 拷贝
- 前端 vue-tsc **0 error**；vitest 全量 **846/846 + exit 0**（基线不回归；D4 为视图层改动，按 plan D4 验证走人工三账号）
- 人工待验（进 P4 测试方案 L13/L14）：L13 邀请不填→接受→额度 0→消耗被拦→配额放行 / 池申请→批→0+提示话术 / 调额空→400 / 存量 null 显「不限（遗留）」行为不变 / 建组组长行仍 null；L14 MEMBER 开组=受限成员 tab（他人 '-'、本人额度内联）/ 流水/审批/设置 tab 不可见 / 组长/admin/admin 代管全显不回归 / 非成员 403

## 下一步

chunk E 按备注汇总统计（12x-1：独立视图+备注列+keyword 扩 remark）。
