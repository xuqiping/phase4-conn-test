# P05 建造期：任务编排与大白话层 · 开发进度 2

> 本轮完成 S2 chunk 任务编排器，并把 execute_build 接入 scheduler。

## 已完成工作

### S2 chunk 任务编排器（FR-003/032）
- 新建 `core-orchestrator/src/task_scheduler.rs`：
  - `Scheduler::run_round` 按 `chunk_no` 顺序执行 pending tasks。
  - 构造 prompt（AGENTS + spec_cards + plan_chunks + 上游摘要 + task title/instructions）。
  - `HttpLlmClient` 调用云端 `/gateway/complete`；支持 mock `LlmClient`  trait 便于测试。
  - 解析 `{files, summary, clarifying_questions}` JSON；追问时返回 `RunResult::NeedClarify`。
  - 文件路径经 `SandboxPolicy` 校验，越界直接失败。
  - 调用 `core_exec::run_task` 执行 runner 闭环；事件经回调透传。
  - 回写 `tasks.status/cost_cents/finished_at`，创建 `checkpoints`（title + status success/failed）。
- Prompt 模板：`core-orchestrator/src/prompts/task_impl.txt`。
- `core-orchestrator/Cargo.toml` 增加 reqwest/async-trait/serde 等依赖；`Cargo.lock` 同步更新。
- `core-orchestrator/src/lib.rs` 导出 `task_scheduler`。
- Tauri `execute_build` 改造：接收 `access_token` + `cloud_base`，实例化 `HttpLlmClient`，调用 scheduler；结果映射为状态机推进/失败/追问。
- `src-ui/lib/ipc.ts` 同步 `executeBuild` 签名，新增 `runTask`。
- 单元测试：mock LLM 覆盖顺序执行、失败即停、追问分支、JSON markdown 包裹解析。
- commit: 待打 tag

## 质量门结果

- `bash scripts/check_all.sh`：EXIT=0。
- `cargo test -p core-orchestrator`：5/5 通过。
- `cargo test --workspace`：全绿。

## 下一步

- S3 diff 大白话摘要（FR-013）。
