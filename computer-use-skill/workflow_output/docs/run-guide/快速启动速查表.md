# 快速启动速查表 · computer-use-skill

> 项目级 · 2026-08-26 跑通时产出

## 项目组成总览
| 部分 | 是什么 | 代码在哪 |
|---|---|---|
| MCP Server | Node stdio 服务，暴露 13 个 GUI 操作工具（升级v2 增 memory_list/memory_forget） | src/index.ts（入口）→ dist/index.js（构建产物）|
| 驱动层 | koffi FFI（截图/输入注入）+ PowerShell UIA 常驻进程 + PostMessage 后台层 | src/driver/win/ |
| 安全层 | 白名单/黑名单/审计脱敏 | src/safety/ |
| 记忆层 | 控件锚点库（本机 JSON，memory/<app>.json） | src/memory/anchors.ts |
| 技能包装 | 给 Agent 的使用说明 | skill/SKILL.md |
| 测试 | vitest 57 用例 + 真机集成脚本 | test/、scripts/ |

无数据库、无 Redis、无网络端口（stdio 传输）。

## 启动步骤
```bash
cd "d:/AI Projects/AI-Projects/computer-use-skill"
npm install
npm run build          # tsc + 拷贝 uia.ps1（自动带 UTF-8 BOM）
node dist/index.js     # 即 MCP Server（宿主 Agent 以 stdio 启动它）
```
接入 Claude Code（.mcp.json 片段）：
```json
{ "mcpServers": { "computer-use": { "command": "node", "args": ["d:/AI Projects/AI-Projects/computer-use-skill/dist/index.js"] } } }
```
技能：复制 skill/SKILL.md → .claude/skills/computer-use/SKILL.md

## 端口占用速查
**无端口**（stdio）。PowerShell 子进程随 Server 生命周期自动启停。

## 查进程 & 关闭
```powershell
# 查 Server / PS 工作进程
Get-Process node,powershell | Where-Object {$_.StartTime -gt (Get-Date).AddHours(-1)}
taskkill /IM node.exe /F   # 慎用（会杀所有 node）；建议按 PID
```

## 常见启动报错速查
| 报错 | 原因 | 处置 |
|---|---|---|
| uia.ps1 中文乱码/解析错误 | dist 里 uia.ps1 丢了 BOM | 重新 `npm run build`（build 内置拷贝）|
| 输入无效/截图空白 | **屏幕锁定** 或目标最小化 | 解锁屏幕；工具会自动还原最小化窗口 |
| CONFIRMATION_REQUIRED | 白名单未放行 | 属正常安全流，confirm_app 放行 |
| TARGET_BLOCKED | 目标是终端/宿主自身 | 安全设计，不可绕过 |
| APP_NOT_FOUND | 应用没开 / 标题不含关键词 | 先启动应用，用窗口标题关键词 |
| 后台操作没反应（LAYER2_NO_EFFECT） | 该应用是 Chromium/Electron 系，忽略后台寄信 | 正常，自动退回前台；要关掉后台尝试：config.toml 加 `layer2_enabled=false` |

## 运维开关 & 数据目录
- config.toml：`layer2_enabled`（后台层）、`memory_enabled`（学习记忆），默认都开
- 学习记忆：`<CU_SKILL_HOME>/memory/<app>.json`（只存控件名+位置，无截图）；`memory_forget` 或删文件即清空
- 审计日志：logs/ 下 JSONL（10MB×3 轮转），via 字段：memory/uia/postmessage/sendinput

## 回归验证命令
```bash
npm test                                # 单测 57
node scripts/integ_upgrade_v2.mjs       # 真机双Fixture（记事本+V2T），需屏幕解锁
node scripts/phase4_smoke.mjs           # v1 冒烟 14 项（需解锁+记事本白名单已放行）
```
