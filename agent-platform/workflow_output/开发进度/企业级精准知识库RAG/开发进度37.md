# 企业级精准知识库 RAG · 开发进度 37

- 主题：P4 `/ask` SSE 接入 Grounded Answer 与六态状态事件。
- RED：新增 `KnowledgeAskControllerTest`，要求事件固定为 `CHUNK → CITATION → RAG_STATE → DONE`，且 Controller 不再直接调用未校验的 `chatStream`；缺少 Grounded Ask API、状态事件且方法不可测试而编译失败。
- GREEN：`RagRetrievalService.retrieveGroundedAnswer` 复用多 KB 证据检索，再执行事实提炼、Citation 白名单、冲突识别和最终合成；Controller 只发送完成校验后的答案。
- 协议：新增 `RAG_STATE`；拒答映射为 `OUT_OF_SCOPE/INSUFFICIENT/RETRIEVAL_FAILED`，成功为 `SUPPORTED/CONFLICT`。
- 前端：API 类型和问答面板接收 `confidenceState/RAG_STATE`；生产构建通过。
- 取舍：取消未校验 token 实时透传，以保证用户不会先看到后来被 Citation 校验判错的内容。
- 验证：后端 11/11；前端 `npm run build` 通过。
- 下一步：完成 P4 组合回归并进入 P5，逐步替换评测、Shadow、Release Gate、Rollout、Feedback 的占位实现。
