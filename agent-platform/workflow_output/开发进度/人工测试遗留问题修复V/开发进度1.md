# 开发进度 · 人工测试遗留问题修复V（增补轮：上游播放键 PB-1）

> 2026-08-27。此前 V 轮（A/B 组 2x/17x）已于 `487a45b0`/`e7e86564` 收尾；本轮为 V 设计 §6 增补的单 chunk 微修。

## PB-1 · 上游视频 ▶ 播放标居中（commit `44cb4045`）

- **问题**：无限画布上游面板视频卡的 ▶ 播放标贴在缩略图旁而非居中。根因：SCSS `&__up-play` 嵌在 `&__up-thumb` 块内，编译成 `.prop-panel__up-thumb__up-play`（双元素类）永不命中模板 :59。
- **修复**（`frontend/src/components/canvas/PropertyPanel.vue` style 块单处）：规则块移到 `.prop-panel` 平级（与 `__up-card`/`__up-meta` 兄弟）；font-size 13→16px 对齐参考区。模板/逻辑/点击手势零改动。
- **验证**：PropertyPanel vitest 全绿（span 存在断言 :337）+ vue-tsc 0 + vite build 过（SCSS 编译验证）；**人工视觉验收**（▶ 居中/hover 锚定/点击 Lightbox/图片卡不回归）挂 V测试方案 **L7**，待 P4。
- **沉淀坑**（已注在代码注释 + V 设计 §6）：BEM 元素类在 SCSS 里必须与兄弟平级——`&__x` 嵌 `&__y` = `.…__y__x`；vitest 测不出 CSS 编译错，UI 类改动须 vite build + 人工看。

## 下一步

P4 人工视觉过 L7 → 勾销；失败记 人工测试问题/ 新条目。
