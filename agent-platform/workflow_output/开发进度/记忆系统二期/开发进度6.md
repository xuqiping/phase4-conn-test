# 记忆系统二期 · 开发进度6（5x 第三轮问题修复 · Chunk1-4）

> 本文件：`5x. 记忆二期问题.md` 第三轮修复（#2/#4/#7/#8 四项，#6 拍板缓办）实现记录。
> 上一文件：[开发进度5.md](开发进度5.md)（P4 总结共享）。plan：[5x记忆二期_第三轮问题修复.plan.md](../../docs/plans/5x记忆二期_第三轮问题修复.plan.md)。

## 本轮概况（2026-08-16）

| Chunk | 问题 | 内容 | commit | 测试 |
|---|---|---|---|---|
| C1 | #2 | V125 回填 source_turn_input_id | `fde4c168` | 迁移幂等已应用 |
| C2 | #8 | 用户级 pattern 唯一（409）+ 路由先建规则赢 | `586847a8` | 单测绿 |
| C3 | #4 | 项目总结 direction（V126）四向打通 | `eb65eec6` | 新测绿，套件绿 |
| C4 | #7 | 收录命中确认式回复 MVP（V127） | `b768261f` | 8 新测绿；chat 套件 451 绿（2 预存基线失败：MemoryAssetRecallServiceTest deepRead×2）；前端 vue-tsc 0 错 + vitest 487 过（VideoGen×2/Pricing×5 预存，与本改动无关） |

## C1 · V125 回填（#2）

- `source_turn_input_id` 建列时漏回填历史：V125 按 turn 方向收敛回填（有配对输入轮→该轮 id；纯输出→NULL；含附件→条目已带 input_id 不动）。

## C2 · 用户级查重 + 先建者赢（#8）

- 建规则撞同用户已有项目的同义 pattern（trim+小写归一）→ 409 CONFLICT 文案只含本人项目名。
- 异步路由命中多项目时**先建规则赢**（createdAt 升序，同刻按 id），口径与展示一致。

## C3 · 项目总结 direction（#4，V126）

- `memory_project_entries.direction`（INPUT/OUTPUT/BOTH，默认 BOTH 兼容历史；V126 幂等：IF NOT EXISTS + DO 块约束守卫 + 按 convergence 回填，手工已应用、Flyway replay 安全）。
- **写侧**：RoutingInput 带 direction（成对轮→BOTH / 纯输出→OUTPUT / 纯输入→INPUT；文件→BOTH）。
- **读侧**：summarizeProjectScope 方向过滤（同向 OR BOTH OR null）。
- **coverage 幂等**：findCoveredEntryIds JOIN memory_summaries `direction IN (#{direction},'BOTH')`——INPUT 总结不挡 OUTPUT 重压；冲突查重两 scope 同口径带 direction。

## C4 · 收录确认式回复 MVP（#7，V127）

- **范围拍板**：快检只做**确定性匹配**=附件文件名 vs 硬规则（trim+小写子串，同 routeFilenameHardRule）；纯文本零命中（语义路由维持异步，不拖首字延迟）。
- 后端：`InclusionQuickCheckService`（规则 TTL **15s** 内存缓存；每项目至多一条、先建优先）；`ChatSessionService` REST/流式两咽喉插确认分支（命中→模板确认消息**不调 LLM 不写记忆**，metadata.inclusionConfirm 存 originalText/model/attachmentFileIds **服务端存档防篡改**；状态机 PENDING→ANSWERED/DECLINED 落库防重复点选）；`confirmInclusion` SSE 端点（ANSWER 携存档原文走 generateReplyStream——拆出的全量生成方法，**天然跳快检防死循环、不重插 USER**；DECLINE 收尾消息不调 LLM）；V127 开关 `rag.memory.inclusion-confirm.enabled` 默认开、异常 fail-open、同 chat_send 限流桶。
- 前端：`stores/chat.ts` 捕获 INCLUSION_CONFIRM 帧随 DONE 落 metadata + `confirmInclusion` 流式 action（本地即时置状态防重复点）；`MessageBubble.vue` 渲染「✅ 需要回答 / 🚫 不用了」按钮与已点选状态标；`api/chat.ts` confirmInclusionStream。
- 人工测试方案：[收录命中确认式回复测试方案.md](../../docs/测试方案/收录命中确认式回复测试方案.md)（5 联动 + 5 边界）。
- **偏离 plan**：规则缓存未做「保存即 evict」（避免 RuleService↔QuickCheckService 循环依赖），纯 TTL 15s（plan 写 30s，窗口减半语义不变——仅确认文案滞后，实际收录以异步路由为准；测试方案 TC4 已注明）。

## 沉淀

- 确认式回复防死循环范式：**点选重答直调拆出的全量生成方法**（跳过触发确认的前置检查），而非给请求打 flag——零状态渗透。
- Mockito 严格桩坑：被测方法给 mock 传 `Set` 时桩匹配器不能用 `anyList()`（类型不符=PotentialStubbingProblem），用 `anyCollection()`。

## Chunk5 · 文档回写（已完成）

- **速查表09 三篇**（`项目工程文档/项目功能介绍/速查表/`）：主表刷 9 页签/开关表（V76/V127）/二期表清单（V71 起撞号改号+V75–V86+V100/V125/V126/V127 全列）；设计与原理刷数据模型表（rules/entries/links/asset 改号+direction+硬规则唯一）+P4 项目总结重建段（旧「整体下线」叙述已过时）+scope 模型；演进与待办头部补三轮记档。
- **feature-map/user-ops**（`docs/`）：feature-map 补三轮四行（InclusionQuickCheck/确认端点/查重 409+先建者赢/direction 请求参数化）+改号清理+`source_direction`→`direction` 修正+条目状态修正（ACTIVE/PENDING_REVIEW，弃=软删）；user-ops 补第 13 节收录确认式回复+第 10 节硬规则/409/先建者赢+第 1/4 节 V125/V126 口径。**偏离**：两文档历史即超 5000 token 硬限（feature-map ~15k/user-ops ~8k），本轮增量回写未拆分，记 README 遗留。
- **5x 状态回写**：#2/#5 已解决、#4/#7/#8 已修待复测（问题单已注 commit/测试方案）。
- **README**：`开发进度/记忆系统二期/README.md` 新建（用户地图+功能清单+运维要点）。
- **总览行**：开发进度总览「记忆系统二期 · 5x 第三轮修复」→ 文档回写毕，转人工复测。

## 冒烟实测（2026-08-16 晚 · Playwright + curl，当前构建）

- **启动**：按 `项目工程文档/0_快速启动.md`（`local-dev-env.ps1` dot-source + `mvn spring-boot:run`）→ 登录 200。教训：`powershell -Command "& script" *> log` 嵌套包裹链路会弄丢/弄脏 env（JAVA_TOOL_OPTIONS banner 见 `\r\n` 注入 → token 失效 → JWT_SECRET 判空/104bit 假象），此前多轮"启动失败"全是该包裹层问题，文档直启法一次通。
- **#8 撞重 409（curl）**：项目2 规则清空后重放 `["  SMOKE2026  "]` → `409 文件名硬规则「smoke2026」已被项目「测试」收录，需先在该项目删除后再添加`（归一 trim+小写、文案只含本人项目名 ✓）；对照唯一 pattern `zzz-unique-probe` 200 ✓，验后已还原空 patterns。
- **C4 收录确认式回复（Playwright 全链）**：登录 admin → 会话 169（项目3「测试」）→ 传 `smoke2026-report.txt` → 发消息 → **22ms** 内秒回模板确认（msg 846→847，零 LLM 延迟）「已命中项目「测试」（附件《smoke2026-report.txt》命中规则『smoke2026』）…需要我基于你的输入进行回答吗？」+ ✅/🚫 双按钮 → 点「需要回答」→ 按钮变「✅ 已选择回答」防重点 → 流式全量回答（思考过程引用服务端存档原文 ✓）+ 🗂 文件记忆卡片回显；异步收录落 `memory_project_entries` id=7 **ACTIVE/BOTH**。
- 机器侧验证至此全绿；**测试方案五联动人工复测仍为关单门**。

## 下一步

- 人工复测走 [收录命中确认式回复测试方案](../../docs/测试方案/收录命中确认式回复测试方案.md) 五联动 + #4 项目总结方向/#8 硬规则 409 手测。
- 遗留（可选）：feature-map/user-ops 按 5000 token 规则拆分；#6 自动总结拍板缓办项。
