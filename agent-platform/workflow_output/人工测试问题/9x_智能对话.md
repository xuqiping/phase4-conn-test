# 已解决

# 待运维配置确认
1. Doubao CHAT 已从“超时”定位为“上游 404”：`/api/coding/v3/chat/completions` 真实测试约 475ms 返回 404，不是系统超时。需在火山方确认当前 API Key 是否开通 Coding 套餐，以及 `doubao-seed-2.1-code` 对应的正确协议/完整 endpoint；平台侧已保留可随时修改的 Provider 配置入口。

# 待复验
（已全部复验通过，见上方已解决 7/8）

# 已解决（2026-08-26 勾销，修复设计 §4，Q2=B/Q3=B/Q10=A）
1. **实时 token 消耗 + 并行任务用超管控** ✅ —— 三件套：
   - **实时可见**：聊天流式协议新增 PROGRESS 事件（服务端周期推送已产 token/估算积分，Q2=B），
     智能对话页生成中实时显示消耗；对话结束落 usage（llm_usage_logs）可查明细（commit `50ac125f`）。
   - **并行用超**：聊天走 **HOLD 预扣**（发起即按预算预扣，Q4=B 全额预扣）；余额不足=扣尽+差额挂 **DEBT 欠款**
     （不产生负余额，充值自动冲抵）。预扣在先，两路并行也不会「都通过预检、结算时都不够扣」。
   - **取消口径**：中途取消按已产 token 计费（Q3=B）——已发生的推理成本照扣，未产部分不收。
   - 速查表 `08-智能对话与流式.md` 已同步 PROGRESS/usage/计费口径（D6）。

# 参考速查表
- agent-platform\项目工程文档\项目功能介绍\速查表\08-智能对话与流式.md
>解决完后需要同步更新对应的速查表

# 已解决（2026-08-26 勾销，修复设计II §6，Plan D；速查表 08 已同步）
1. **缓存 tokens 获取+计价** ✅ —— 两家 Provider 口径归一取 `cachedTokens` → `llm_usage_logs.cached_tokens` 落库 → HOLD 预估/结算三腿计价（输入/输出/缓存）→ 聊天 DONE 显「含缓存命中 N」；价表增「缓存价/闲时缓存」输入，**留空=按输入价**（老价表行为不变，回归矩阵锁死）（D3-D5，`8e914911`/`e649889c`/`daa31f6c`）。

## 遗留观察（不修）

- glm-5.1/5.3 忽略 `thinking:{"type":"disabled"}` 与 `budget_tokens`，内部 JSON 调用仍可能因思考过长撞新预算（实证 2026-08-17 00:06:31 WARN：glm-5.3 非流式 max_tokens 截断，重试/降级链兜住）。判定域已通过 `memory.judge.model`=k3 避开；若管理员把全局默认对话模型配回 glm 且未配判定专用模型，思考风险回归。

# 已解决（2026-08-31 起已实现，挂「待人工验证（修复IX）」——过测试方案 O1-O9 / Q1-Q7 后逐项勾销）
- 是否能选择思考强度？
  > **已实现，待人工验证（修复IX）**（2026-08-31，Chunk A，commit `2e2da33`/`0c6d40c7`/`3d9019f6`）：思考强度三档（关/标准/深度）全链贯通——
  > Claude 协议三档（budget 2048/8192 + max_tokens 只抬不降）；OpenAI 系 provider config `thinking` 节声明制
  > （toggle=两档 / effort=三档，未声明=零参数现状不误伤）；模型列表下发 thinkingLevels 前端按模型显隐回落；
  > HOLD 预扣按档位 ×2/×4 联动（封顶不超显式 max_tokens）。运维手册 §十一 有声明 SQL 三例。
  > 人工用例 `docs/测试方案/人工测试遗留问题修复IX测试方案.md` O1-O9。
- 可以开始准备联网搜索接入了（查看开发进度总览）
  > **已实现（开发 2026-07-19 已收，本轮修复IX 只补部署清单+验证方案）**：代码链路（Tavily/Serper/Bing key 制 +
  > SearXNG builtin + 全失败降级回纯模型）早已上线；**dev 库现状=零配置**（无任何 search.* 行、SearXNG base-url 未配，
  > 开 🌐 即降级不炸）。部署两路清单见运维手册 §十一（Tavily key 快路 / SearXNG Docker `formats:[json]` 抗封路 +
  > env `SEARCH_SEARXNG_BASE_URL`），验证用例 Q1-Q7。