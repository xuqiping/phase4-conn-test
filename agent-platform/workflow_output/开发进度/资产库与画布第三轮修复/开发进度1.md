# 开发进度 1 · C1-C2（画布框选+一键关联）

> 功能：资产库与画布第三轮问题修复｜[plan](../../docs/plans/资产库与画布_第三轮问题修复.plan.md)｜[测试方案](../../docs/测试方案/资产库与画布第三轮问题修复测试方案.md)
> 日期：2026-08-16｜分支：beifen

## C1 框选多节点接管 ✅ commit `61630fdc`

- **实现**：Shift 框选（vue-flow 原生 selectionKeyCode）→ `onSelectionEnd` 同步 `multiSelectedIds`；≥2 上报 `nodes-selected` → CanvasView 顶部批量工具条（已选 N + 批量删除）；1 个归一回单选面板。
- **双删隐患修复**：`<VueFlow :delete-key-code="null">`——库默认 Backspace 会删内部选中节点而应用无感知，现删除只走应用层。
- **其他**：Esc 清多选；节点点击/边点击/空白/右键/chip聚焦 全入口清多选；节点被外部删除（父组件批量删）时 watch 修剪多选集。
- **涉及文件**：`components/canvas/CanvasBoard.vue`（选集接管+delete-key-code+prune watch）、`views/CanvasView.vue`（工具条+useDialog 批量删除+CSS）。
- **测试**：vue-tsc 0 错。人工用例 L1-1~L1-6 待 Phase4。

## C2 一键关联 ✅ commit `eaf32378`

- **实现**：`utils/autoAssociate.ts` 纯函数 `buildProposals`（快照读，不写数据）+ `applyProposals`（逆序替换防位移）；`AutoAssociateDialog.vue` 预览确认弹窗（全选/逐行取消/跳过清单）；CanvasView 工具条「一键关联」按钮接入。
- **匹配规则**：文本类节点（text→prompt / script→synopsis / storyboard→description）× 祖先节点 label+资产徽标 assetName（≥2 字）；长名优先防「主角1」吞「主角10」；已有 `@{{...}}` 区间占用不二次包装；同名单处命中。
- **配套**：`interpolateForRun` 补 `description` 插值（分镜描述此前不插值，@会漏解析）。
- **涉及文件**：`utils/autoAssociate.ts`（新）、`utils/autoAssociate.test.ts`（新）、`components/canvas/AutoAssociateDialog.vue`（新）、`views/CanvasView.vue`。
- **测试**：autoAssociate vitest 10/10（长名优先/不二次包装/assetName候选/三种skip原因/逆序替换/外部改动防御/多目标分组）；vue-tsc 0 错。人工用例 L2-1~L2-7 待 Phase4。

## 设计要点（对齐 plan D3）

- 属性面板直编 node.data（reactive 实时）→ 框选节点即便正在编辑，提案读到即最新值，无需额外同步（plan L2-7 边界）。
- 0 提案不弹窗：`message.info` 显示首个 skip 原因（无上游/名称未出现/非文本类）。

## 下一步

C3 每用户生成任务并发上限（后端，V123 + MediaInflightGateService + 15x 回写）。
