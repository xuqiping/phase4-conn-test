# 人工测试遗留问题修复IX · 开发进度1

> 对应规格 `docs/specs/人工测试遗留问题修复IX设计.md` + 计划 `docs/plans/人工测试遗留问题修复IX.plan.md`。
> 范围：IX-1 思考强度可选（9x）、IX-2 画布连线保留开关（2x 增补④）。联网搜索（IX-3）零开发，仅部署核验（见 spec 运维清单）。
> 分支 feat/gsl-port。本轮 = Chunk A（A1-A6）+ Chunk B（B1-B3）全部完成。

## Chunk A：思考强度三档（IX-1）

| 步 | 内容 | commit |
|---|---|---|
| A1 | `llm/dto/ThinkingLevel`（OFF/STANDARD/DEEP）+ 三跳透传：ChatRequest `@Pattern` 白名单 → ChatSessionService.resolveThinkingLevel（双站点 sync/stream，非法串容错 null）→ ExecutionContext → DefaultChatStrategy → LlmRequest.thinkingLevel（null=零参数现状；10+ 处 disableThinking 内存调用方不动，优先级 thinkingLevel > disableThinking > 无） | 2e2da33 |
| A2 | ClaudeProvider（ANTHROPIC 协议三档原生）：OFF→thinking.type=disabled；STANDARD/DEEP→enabled+budget_tokens（LlmThinkingProperties budget 2048/8192，clamp ≥1024；max_tokens≤budget 时抬 budget+1024 只抬不降）；9 参 ctor 旧 7 参委托 | 同 A1 commit |
| A3 | OpenAI 系声明制：`ThinkingSpec.parse`（config jsonb `thinking:{style:toggle|effort,models?}`，坏 JSON warn+null，构造时解析一次缓存）；TOGGLE→thinking.type disabled/enabled（无深度态，levelsFor 只发 [OFF,STANDARD]）；EFFORT→reasoning_effort low/medium/high；未声明/白名单外=零参数。能力下发 AvailableModelVO.thinkingLevels（ANTHROPIC=三档全零声明；用户级 provider 继承同名全局行 config） | 2e2da33 |
| A4 | HOLD 预扣档位放大：LlmBillingService holdChat 8 参重载（旧 7 参委托 null=现状），est 出量 STANDARD×2/DEEP×4（系数可配 llm.thinking.hold-factor-*），放大后仍受请求 maxTokens 帽截断防过度冻结；LlmGateway chat/chatStream 透传；PROGRESS 估算核验=实产字符折算天然随实耗，无需联动 | 0c6d40c7 |
| A5 | 前端：api 层类型+签名位；stores/chat 全链透传（REST/SSE×2/队列/回放/REST 回退，顺带修回退两处漏传 kbIds/gid 旧缺陷）；ChatView 🧠 选择器（selectedModelLevels 未声明不渲染、effectiveThinking 档位回落 levels[0]、默认 STANDARD、localStorage CHAT_THINKING_LEVEL、aria-label） | 3d9019f6 |
| A6 | 收口：后端 mvn 全量 2706/2706 绿；前端 vitest 973/973 + vue-tsc 0 错 | 随 A5 |

## Chunk B：画布连线保留开关（IX-2，Q4 拍板）

| 步 | 内容 | commit |
|---|---|---|
| B1 | canvasClipboard：CanvasClipboard.crossEdges **恒收集**（恰一端在选中集的非组边，复制时点不看开关）+ remapCrossEdges 单侧重映射（集内端→新 id、集外端保原 id=副本接入原上下文；悬挂防护集外端已删丢边；两端同侧退化双保险丢；平行重复边并存不 dedup；剥会话 class）+5 测试 | fb0e3981 |
| B2 | utils/canvasPrefs singleton ref keepLinksOnCopy（localStorage，默认开，非法回落）+ CanvasBoard 工具条 ⛓ 按钮（aria-pressed/title 双口径）+ pasteSubgraph 粘贴时点判定（复制后切开关按粘贴当下态生效）+ CanvasView.onCloneNode 同开关门（关=创建副本零边） | 2152c81b |
| B3 | 收口：CanvasBoard +6 用例（含正反切换/悬挂/平行边/按钮）47/47；全量 988/988 + vue-tsc 0 错 | 2152c81b |

## 测试与验证

- 后端：DefaultChatStrategyTest +2、ClaudeProviderTest +4、ThinkingSpecTest 5（新）、OpenAICompatibleProviderTest +5、LlmBillingServiceTest +2、LlmGateway( Route)Test ctor 适配；mvn 全量 2706/2706。
- 前端：chat.test +2、canvasClipboard +5、canvasPrefs 4（新）、CanvasBoard +6/适配 2；vitest 988/988，vue-tsc 0。
- 待人工：SSE+WS 双路发消息带 thinkingLevel 实测；🧠 选择器三档真模型验证；⛓ 开关两处联动实测（开发环境 Playwright 见 memory 四坑）。

## 规范沉淀

- LlmRequest 新参数模式：nullable 枚举 + provider 侧优先级链（新参 > 旧布尔 > 无），老调用方零改动。
- 前端「模型能力声明」模式：能力字段（thinkingLevels）随模型列表下发，未声明不渲染 UI、请求省略字段=后端零参数——三处对齐（VO/api/视图守卫）。
- 画布开关治理模式：utils 单例 ref + 双入口（粘贴/副本）同一 gate + 恒收集/用时判定分离（剪贴板存全量，粘贴时点决定用不用）。

## 下一步

- Chunk C（文档收口：速查表 08、feature-map、user-ops、测试方案 O/P/Q、运维手册 thinking JSON 示例+联网部署清单、问题单挂待验）——待授权。
- spec 回写两处细化：crossEdges 恒收集+粘贴时点判定；canvasPrefs singleton（随本轮已按此实现）。
