# 已解决
0. 内置大模型配置员账号 + 角色/权限码 UI 隐藏 ✅（2026-08-23，V147）—— 预置账号 `zgdx18158690628`（密码 bcrypt 落库，明文记 数据库设计文档.md「初始数据汇总」），持 `llm_config` 角色，部署时 Flyway 自动创建（ON CONFLICT 幂等）；同时 `RoleController` 列表查询把 `llm_config` 角色与 `llm:config` 权限码对 admin **隐藏**（角色管理页/分配角色弹窗/权限配置树均不可见，无人能 UI 分配/摘除/改权），DB 行保留、持有者 JWT 权限烘焙不受影响。单测 `RoleControllerHiddenFilterTest` 3 例绿。
1. 只能有一个系统管理员 ✅（2026-08-20 核验：功能本就支持，属使用方式问题）—— `user_roles` 复合主键 `(user_id, role_id)`（V1 建表）天然支持一人多角色/多人同角色；`PUT /api/users/{id}/roles` 全量替换该用户角色集（可同时勾选多个管理员），`user:manage` 权限 + `@AuditLog(assign_roles)` 审计。**操作指引**：系统管理 → 用户管理 → 目标用户「分配角色」→ 勾选「系统管理员」（可与其他角色并存）→ 保存即生效（用户重新登录后 JWT 带新权限）。已同步速查表 03。
2. 普通账号（aa64221886）进图片生成/视频生成/无限画布报 403 ✅（2026-08-20）—— 根因：`media:gen`/`canvas:write`/`asset:write`/`media:edit` 四个权限码在 V54/V55/V58/V87 建表时只 INSERT 给了 admin（历史 INSERT ... SELECT 均带 `WHERE r.code='admin'`），`user` 角色从未获得；普通账号 JWT 里没有这些权限 → 页面能进菜单但接口层 403。修复：V136 `INSERT INTO role_permissions ... WHERE r.code='user' ON CONFLICT DO NOTHING` 一次性补授存量 + 新库自动生效；**需重新登录**（权限烘焙在 JWT，旧 token 仍是旧权限集）。注意：`canvas:write` 授予后普通用户可建自己的画布（仍只能看/改自己创建的，画布归属由 CanvasService.loadOwned 咽喉控制）。
3. 目前有几个系统管理员账号？ ✅（2026-08-22 实测查询）—— **3 个**，均 ACTIVE：`admin`(id=1)、`admin123`(id=172)、`admin12345`(id=174)。查询口径：`users ⋈ user_roles ⋈ roles(code='admin')` 且未软删。
4. 大模型配置独立角色 + admin 失去该权限 ✅（2026-08-22）——
   - **根因**：「设置 → 全局模型供应商」全系端点（供应商 CRUD/连通性测试/reload/导入导出 + 默认模型读写 + 媒体供应商测试，共 13 处）历史借道 `role:manage`，配模型的人必须同时握「管角色/管部门/管系统设置」大权。
   - **V145 迁移**：新增权限码 `llm:config` + 新角色 **大模型配置员（`llm_config`）**（只持此码，别无他权）；**admin 刻意不授**——admin 的授权全是逐码显式行（V2 全量授权只覆盖当时已存在的码），不 INSERT 即天然失去，无需 DELETE。
   - **后端 13 端点改挂 `llm:config`**：LlmController ×10、MediaGenController 媒体供应商测试 ×1、SystemSettingController 默认模型读写 ×2（默认模型划给配置员——它就在供应商 tab 里保存，且属「配置大模型」本义）。价表配置 `pricing:manage` 本就独立码，仍归 admin，不动。
   - **前端**：`/settings` 路由 meta 加 `requireAnyPerm: ['llm:config']`（accessGuard 规则 4 新增豁免分支）；「全局模型供应商」tab 改为仅 `llm:config` 持有者可见（**admin 不再可见**，避免点了 403 像 bug）；Sidebar 设置入口对配置员放开。配置员进设置只见「安全设置 + 全局模型供应商」两个 tab。
   - **生效提示**：权限烘焙在 JWT，加挂/摘除角色后**重新登录**生效。若 admin 谁也不授 `llm_config` 角色 → 全平台无人能配大模型（刻意保护，非 bug）。
   - **操作指引**：系统管理 → 用户管理 → 目标用户「分配角色」→ 勾选「大模型配置员」→ 保存 → 该用户重新登录 → 左侧「设置」→「全局模型供应商」。
   - **验证**：契约测试 `LlmControllerContractTest` 锁 13 端点挂码（防回改）；accessGuard +4 例（持码放行/无码拦截/无豁免参数回归/admin 不受影响）；后端全量 + 前端全量绿。

# 未解决
（无）

# 参考速查表
- agent-platform\项目工程文档\项目功能介绍\速查表\03-角色与权限RBAC.md

>解决完后已同步更新速查表 03（2026-08-20）：多管理员操作指引 + user 角色权限范围变更（V136）。
>再次同步（2026-08-22）：大模型配置独立角色（V145）小节——llm_config 角色/13 端点换码/admin 刻意不授/操作指引。



# 设置大模型账户
zgdx18158690628
!Aa6