---
description: "迭代 H · 收尾（禁用旧端点 + DROP 旧表 V49 + 速查表 09 重写）"
created-date: 2026-07-27
---

# 计划12 · H · 收尾

> 子 plan。主索引：[计划12-个人记忆重设计-实现开发计划.md](计划12-个人记忆重设计-实现开发计划.md)。设计：[总体设计 §9](../设计/个人记忆重设计-总体设计.md)。

## 目标
旧端点 404；旧表 DROP；速查表 09 三件套重写为新模型；文档自洽。

## 依赖
A-F、I2-I4。

## 文件清单（≤20）
- `db/migration/V49__drop_legacy_memory_tables.sql`：DROP `user_memories` / `user_memory_projects`
- 删 `chat/entity/UserMemory.java`（路径 Phase3 确认）+ `UserMemoryMapper.java`（+XML）
- 删 `chat/service/internal/MemoryBlockClassifier.java`
- 改 `chat/controller/MemoryController.java`：移除旧 `/api/chat/memories/*` 端点
- 改 `chat/service/MemoryService.java`：移除旧事实抽取链路
- 改 `chat/service/MemoryConflictService.java`：移除旧冲突机模型残留
- 文档：`项目功能介绍/速查表/09-个人记忆与冲突解决.md` 三件套（速查/原理/演进 或 速查/原理/落地——按现有结构）重写为新模型

## 动作（伪代码）
1. **V49 DROP**：`DROP TABLE IF EXISTS user_memories, user_memory_projects;`（旧 Flyway V29-V45 历史保留不修改）。
2. **删旧代码**：`UserMemory` / `UserMemoryMapper`(+XML) / `MemoryBlockClassifier` 删除。
3. **移除旧端点**：`/api/chat/memories/*` 路由移除 → 调用返 404。
4. **清理旧链路**：MemoryService 旧事实抽取、MemoryConflictService 旧冲突机模型残留代码删干净。
5. **速查表 09 重写**：基于新模型（L0/L1/L2 + 标签归一 + 总结 + 冲突 pending + 流水账多挂 ACL）重写三件套，删除旧 user_memories 描述。

## 本迭代相关坑点
| 坑 | 规避 | 验证 |
|---|---|---|
| 旧表 DROP 后老数据不可恢复 | 接受（项目未上线，全测试数据） | 上线前确认无生产数据 |
| 删旧代码漏引用导致编译崩 | 全局搜 `UserMemory`/`MemoryBlockClassifier` 引用 | 编译全绿 |

## 本迭代相关安全向量
- [ ] 向量 10：旧端点 `/api/chat/memories/*` 404。

## 本迭代相关联动点
- 无（收尾清理）。

## 本迭代相关运维
- **可回滚**：V49 DROP 不可逆（接受，记录在主索引备注）。后续再说。

## 验证（出口条件）
- [ ] 旧端点 `/api/chat/memories/*` 404 单测。
- [ ] V49 DROP 后全链路回归绿（A-I4 所有 IT/E2E 重跑）。
- [ ] 编译全绿（无旧代码残留引用）。
- [ ] 速查表 09 三件套自洽（无旧模型残留描述）。
