# 知识库RAG检索能力增强 · 开发进度9

对应 plan：[WP6_连接器](../../docs/plans/知识库RAG检索能力增强_WP6_连接器.plan.md)｜上轮：[开发进度8](开发进度8.md)

## 上轮收口：WP5 多模态全毕（Step0-4，末 bdaeacd4，后端全量 2881/2881）

Step1 多模态 embed 协议（+3）→ Step2 IMAGE 索引双写（V173 分表+UPSERT_IMAGE，+8）→ Step3 IMAGE 检索通道 RRF 第三列表+docL1Sim 槽升义 max(L1,IMAGE)（+3）→ Step4 ColPali 预留（V174 KB 级开关+三重闸+探活自动禁用，+2）。单测合计 +16 超计划 ~8；手测剧本（传截图→图被召回等）留 Phase4。

## 本轮：WP6 Step1 数据层与凭证加密（测试 4/4，commit 见 git log）

- **V175 两表**：`knowledge_connectors`（type/name/config_cipher/schedule_cron/**sync_on_source_delete** 超前落/SYNC 摘要列；BaseEntity 全审计列+deleted 逻辑删；CHECK 类型三值/状态三值）+`knowledge_connector_docs`（增量账本 uq(connector_id,external_id)+etag+**manual_deleted** 防复活+FK CASCADE）。生活比喻：连接器=订阅合同，账本=签收登记本。
- **凭证加密复用不新建**：`AesEncryptService`（llm 同款 AES-GCM 主密钥+生产态弱密钥 fail-fast）直接注入——计划原列 ConnectorCryptoService，偏离记 plan。config Map 明文→**类型结构校验**（S3 endpoint+bucket+AK/SK 等）→Jackson→密文；**凭证只写不读**（VO 零 config 字段）。
- **权限 KB 治理级**：isOwnerOrAdmin 复用（收紧于计划的 canManage——同 KB 改名/删除口径）；Controller 四端点 @RequirePermission(knowledge:manage)+@AuditLog（审计落库走既有切面）。类型创建后不可变；cron Spring 六段校验默认每天 04:00；删除=逻辑删+文档孤儿化保留。
- 测试 +4：密文落库不含 endpoint/AK/SK 任何明文+**真 AES 实例**解密回读/非 owner 403 零 insert/结构校验四态（缺 bucket·ftp 种子·非法类型·非法 cron）/类型不可变+config null 保留密文。
- 坑：ErrorCode.getCode() 返 int（403/422）；R 包在 common.result。

## 本轮 2：WP6 Step2 SPI 与三连接器（后端全量 2894/2894，commit 见 git log）

- **SPI 简化**：`KnowledgeConnectorSpi`（type/list/fetch/close+`ExternalDoc(externalId,etag,displayName)` record）——list() 返**全量条目**非 cursor 流（增量差分交 Step3 worker 对账本；URL 站点/PROPFIND 本无游标语义，S3 continuation 只是分页细节）。
- **SafeHttpFetch**（URL_SITE/WebDAV 共用底层）：`followRedirects(NEVER)` 手动跟 ≤3 跳且**每跳 Location 重过 urlGuard**（自动跟随=校验过首跳 302 直进内网——坑点表头号变体，测试 mock 302→169.254.169.254 锁死）；非 2xx 异常只带状态号不带响应体（防内网信息回显）。
- **三连接器**：`UrlSiteConnector`（BFS 同域 scheme+authority 全等、深度 ≤2 但**到顶页仍提链接为条目**只不下钻、成环去重、≤50 页、白名单七后缀、etag=ETag 或 `LM:`Last-Modified）；`WebDavConnector`（PROPFIND Depth:1 逐目录递归 ≤5/≤500 条、Basic 认证、**jsoup xmlParser+按本地名递归**——CSS select 不匹配 `d:response` 命名空间前缀标签，getetag 嵌 propstat/prop 三层须无条件递归）；`S3Connector`（**AWS SDK v2 2.54.11**+url-connection 传输 pom 排除 apache/netty 控树；**必须显式 `.httpClient(UrlConnectionHttpClient.create())`** 否则 service-load 到残缺 apache5 报 NoClassDefFoundError；endpointOverride+forcePathStyle 兼容 MinIO/OSS/COS；分页闸 50 页）。
- **FetchLimiter**：1 req/s+200MB 总字节闸，同步阻塞式（worker 单线程顺序拉取）。SsrfGuard 生产装配留 Step3 工厂（urlGuard 策略注入：测试 mock 源站绑 127.0.0.1 须放行 guard 才能测爬取；SSRF 语义另用真 SsrfGuard 直测五渗透字面量）。
- 测试 +9（计划 ×3）：SafeHttpFetch 2（五渗透字面量/302 跳内网）+UrlSite 3（成环去重+白名单+深度语义/页数闸/fetch 字节）+WebDav 2（递归+etag/Basic 下载）+S3 2（**AWS SDK 只发不验响应签名→HttpServer 应答裸 XML 即可 mock**：prefix 过滤+etag 透传+目录占位跳过/下载）。真实 S3 桶/WebDAV 源验证留 Phase4 手测。
- 坑：SsrfGuard.validate 返 void 不能方法引用作 Predicate 须包 lambda；jsoup HTML 解析器小写化/重排树必须 xmlParser。

## 本轮 3：WP6 Step3 ConnectorSyncWorker（后端全量 2909/2909，commit 见 git log）

- **V176**：`sync_error_streak` 列（连续错误跨轮存活；第 3 轮置 ERROR 停调度，成功清零）。
- **ConnectorSyncWorker**：@Scheduled 60s（`knowledge.connector.poll-ms`）→cron 到期判定（Spring `CronExpression.next()`）+**确定性抖动 [0,600)s 错峰**→tryClaim（**乐观短事务 FOR UPDATE SKIP LOCKED**：锁窗口只盖复核+推进 last_sync_at，同步分钟级不占行锁）→list 全量对账本 etag 差分。
- **四态动作**：新增=`upload()` 全管线（store 白名单/magic/配额/初始版本/解析事件——`InMemoryMultipartFile` 适配非旁路，KnowledgeDocumentService 零改动）；变更=createVersion（版本+1 可回溯）→activate→`resetDocForResync`（换 fileRef+**清旧节点三池向量**——writeNodes 不清旧坑）→PENDING+event 重解析；复活（ISOLATED 源恢复 etag 未变，零抓取）；重试（FAILED 同文件自愈）。**单轮 50 动作预算**（externalId 排序多轮消化）。
- **ISOLATED=复用 QUARANTINED+前缀分流**：置隔离态+清节点向量（不召回）+账本留 etag 供复活比对；**安全隔离（注入命中）按前缀识别绝不触碰**。源删开关开→既有治理删除链+账本行删（再现按新文档）；手工删/用户删→账本 manual_deleted 永不复活。
- **运维**：摘要「新增/更新/复活/重试/隔离/删除/跳过/错误」脱敏落 last_sync_summary（`sanitize` 剥 URL userinfo）；轮级失败与全败零进展轮计 streak，部分成功=成功带错误计数；ConnectorFactory 生产 SSRF 装配（S3 endpoint 建客户端前 validate）。
- 测试 +15（worker 编排 10+tx 记账 5）。坑：CronExpression API=`next()`；Mockito `never()` 不在 ArgumentMatchers；MP 单测须 `TableInfoHelper.initTableInfo`+断言前先 `getSqlSegment()`。

## 本轮 4：WP6 Step4 连接器可视管理（后端 2915/2915+前端 1103/1103+vue-tsc 0，commit 见 git log）

- **后端启停/立即同步**：enable/disable/syncNow（KB 治理级权限）；enable 顺带清 streak（ERROR 停摆重启=全新机会）；syncNow 对 ERROR 先复位 ENABLED 再触发；`triggerManualSync @Async(knowledgeTaskExecutor)` 复核+tryClaim 后跑同一 syncConnector，受理即返 **202**；service→worker→factory→service 循环依赖用 **ObjectProvider 惰性注入**解（工厂试改静态破环——毁测试可 mock 性，回退）。VO +syncErrorStreak；NEW 分支补 markConnectorOrigin 落 sourceType/sourceUri。
- **前端 ConnectorPanel**（新）：列表（类型中文名/状态 tag·**ERROR 红标+×N 连败 tooltip**/周期/最近同步/结果摘要 tooltip）+启停/立即同步（202 提示+5s 延迟刷新）/删除二次确认（已同步文档保留归手工管理）；新建弹窗类型切换表单（URL/S3/WebDAV 各自字段）+cron 四预设（每小时取 17 分错峰+自定义六段）+源删同步删开关+**canSubmit 禁存门**（与后端结构校验同口径）。挂载：KB 行「连接器」按钮（canDestroy）→右侧抽屉（非计划「详情 Tab」——本页 Tab 已 6 个，按钮+抽屉与「文档」一致）。文档列表 **🔌 徽标**（与 📎 共存）。
- 测试 +13：后端 +6（启停/syncNow ERROR 复位/manualSync 停用跳过·认领即跑）+前端 +7（列表/红标/表单分支/禁存门/透传/分流/延迟刷新）。坑：**NModal 传送 document.body 须断 body.textContent**；useDialog 无 provider 直接 throw 须 mock。

## 本轮 5：WP6 Step5 运维入口与收尾（WP6 全勾，连接器测试 22/22）

- **历史轮次不做**（偏离记 plan）：last_sync_summary 单轮快照+streak+ERROR 红标已覆盖「查现状→修→重试」闭环，spec 8.4 仅列最近结果；audit_log connector_* 三动作另可追查。脱敏口径收窄：凭证只写不读+sanitize 剥 userinfo 保留；内网地址不脱（owner/admin 自配置已知，脱了伤排障）。HELP/user-ops 并入功能收尾。
- **依赖安全**（定向复核，NVD 离线拉库失败）：AWS SDK 2.54.11 无直接 CVE；SDK 已知 CVE 全经 netty 传输——本项目 awssdk 子树零 netty（netty 4.1.109 仅来自 lettuce 既有基线）；**顺手排除 s3 传递 runtime 依赖 apache5-client**（Step2 service-loader 残缺实现 NoClassDefFoundError 坑的根因），排除后 pom 注释更新+连接器三测试类 22/22 绿。
- 验证汇总全 tick：单测实际 **+41**（4+9+15+13）；手测剧本=测试方案 S15-S17+L6（Phase4）。

## 下一步

全功能收尾（六 WP 全毕后）：功能 README（B/C 类：用户地图+技术说明）+feature-map（含 V173-V176 表注解）+user-ops 手册（含连接器操作节+HELP 增补）+主 plan 收口。WP3 Step4 影子对比仍等用户给选库+黄金集。
