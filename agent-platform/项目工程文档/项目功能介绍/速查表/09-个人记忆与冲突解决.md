# 09 - 个人记忆与冲突解决

## 功能简介
对话中抽取用户事实记忆、分类（用户记忆块）、新旧记忆冲突裁决（保留/合并/替换）、用户可在面板查看/删除/批量解决冲突。

## 记忆检索模式（V28/V31，用户可选）
召回入口 [MemoryService.buildMemoryContext(userId, query)](../../backend/src/main/java/com/superprogrammer/chat/service/MemoryService.java) 按系统设置 `rag.memory.retrieval-mode` 分流：
- **LLM_FULL_CONTEXT**（默认，向后兼容）：全量 `confidence≥0.5` 记忆灌入 LLM 上下文。简单但易联想泄漏（没问也输出）。
- **EMBEDDING_VECTOR**：当前 query 经 `LlmGateway.embed(query, MEMORY_EMBED_MODEL)` → pgvector `<=>` 余弦距离 top-K（`similarity=1-distance≥0.35`，`LIMIT 5`），仅注入强相关记忆；**无命中 → 不注入**（return null，不回退全量）；空 query → 不注入。复用写入侧 embed 链，召回侧不再裸跑全量。
- **VECTOR_KEYWORD**（V31）：向量 top-K ∪ 关键词(实体列) 召回 → 去重截断（`HYBRID_MAX=8`）注入；**并集 0 命中 → LLM-key 兜底**。专治纯向量漏的"实体桥接"类（记忆"女儿3岁" `key=child_age value=3岁`，query"带女儿去玩"靠实体"女儿"召回）。见下文「混合检索原理」。
- **LLM_KEY**（**V38 新增，推荐**）：**锚点语义两阶段**召回（百万 key 规模）。① QueryExpansion 多 qHalf 扩 query 提 recall；② 每 qHalf 跑 anchor 向量 top-K + BM25（`anchor_tokens_tsv`）→ RRF 加权融合（向量权重 > 词法）→ 粗筛 top-N；③ LLM 三维（key × key_zh × block，**一次调用**）精排（`rerank=true`，可关）；④ 注入选中 key+value，TTL 60s 缓存。anchor 锚点 = `block_label + memory_key_zh + memory_key + entities(展开)`，独立于 value embedding（不替代）。见下文「锚点语义召回（V38）」节。
- 设置项：[SystemSettingService.getMemoryRetrievalMode()](../../backend/src/main/java/com/superprogrammer/system/service/SystemSettingService.java)（key `rag.memory.retrieval-mode`），前端 [RagMemorySettingsTab.vue](../../frontend/src/components/settings/RagMemorySettingsTab.vue) 下拉，见 [21-系统设置](21-系统设置.md)。LLM_KEY 额外旋钮：`llm-key.coarse-top-n`（粗筛候选数，默认 40）、`llm-key.rerank`（精排开关，默认 true）、`keyword-max`（关键词通道上限，默认 8，0=不限）。
- 向量检索 SQL：[UserMemoryMapper.findTopKByVector](../../backend/src/main/java/com/superprogrammer/chat/mapper/UserMemoryMapper.java)（`embedding IS NOT NULL`，仿 `findNearestBlock`；FLAGGED 行 conflictId≠null 也参与检索）。
- 线程：`buildMemoryContext` 在 ChatSessionService 方法体内同步调（与 `resolveRagForChat` 同 scope，stream/non-stream 均 servlet 线程，非 reactor-nio），embed/LLM 兜底阻塞调用同既有 RAG 检索，不新增 block 风险。

### 混合检索原理（VECTOR_KEYWORD，V31）
纯向量只看"表面语义像不像"，不看"逻辑上相不相关"——"女儿3岁" embed 靠近 {年龄/儿童}，"带女儿去玩" embed 靠近 {游玩/景点}，余弦距离远 → 漏召。任何纯向量方案救不了，换 embed 模型也不行。
混合召回三段式（[MemoryService.buildHybridContext](../../backend/src/main/java/com/superprogrammer/chat/service/MemoryService.java)）：
1. **向量 top-K**（既有链路，`findTopKByVector`）。
2. **关键词召回**（[UserMemoryMapper.findByKeyword](../../backend/src/main/java/com/superprogrammer/chat/mapper/UserMemoryMapper.java)）：query 分词（CJK 2-gram + 字母数字整段，[MemoryService.tokenize](../../backend/src/main/java/com/superprogrammer/chat/service/MemoryService.java)，`KEYWORD_MAX=8`）→ ILIKE 命中 `entities::text` / `memory_key` / `memory_value` / `memory_key_zh` / `block_label`（五列并查，无 top-K LIMIT，仅 LIMIT 200 防病态）。**per-block_label 阈值筛选**（[MemoryService.applyKeywordPerBlockThreshold](../../backend/src/main/java/com/superprogrammer/chat/service/MemoryService.java)）：按 block_label 分组，各组各自阈值 N（设置 `rag.memory.keyword-per-block-threshold`，默认10，0=禁用）；组内 ≤ N 全留，组内 > N 则命中 `entities`/`memory_key`/`memory_key_zh`（高优）的行不卡阈值全留、低优（`memory_value`/`block_label` 命中）补到 N。取代旧全局 LIMIT 10。噪声 gram（如"去玩"）天然自滤——不是任何召回词子串就撞不上五列。
3. **并集 0 命中 → LLM-key 兜底**（[MemoryService.filterRelevantKeys](../../backend/src/main/java/com/superprogrammer/chat/service/MemoryService.java) → [selectRelevantKeysBlocks](../../backend/src/main/java/com/superprogrammer/chat/service/internal/MemoryConflictJudge.java)）：拉全部 clean 记忆，**一次 LLM 三维**（keys/keys_zh/blocks）挑相关，按选中 key 装载 value 注入。即"先加载标签→LLM 判→再加载内容"的正确落地，**仅漏召时触发，非常驻路径**（省 token+加速，不每轮加 LLM）。
- 任一子链路异常不致命：向量挂→仅关键词；关键词挂→仅向量；都空→兜底；兜底也空→不注入（return null）。
- LLM 兜底 prompt（`SELECT_KEYS_PROMPT`）召回优先（"宁多选"），避免漏。

### 实体标签（V31）
- 列：`user_memories.entities` JSONB（[V31 迁移](../../backend/src/main/resources/db/migration/V31__user_memories_entities.sql)，可空，老行 NULL=仅参与向量）。
- 写入：[MemoryConflictJudge.extract](../../backend/src/main/java/com/superprogrammer/chat/service/internal/MemoryConflictJudge.java) 顺带抽实体（复用既有 extract LLM 调用，**0 额外调用**），存 `["女儿","北京"]`。落库经 [MemoryService.applyClean](../../backend/src/main/java/com/superprogrammer/chat/service/MemoryService.java) → `entitiesJson` 序列化 → mapper `::jsonb`。
- 用途：VECTOR_KEYWORD 关键词召回的索引源。其他模式不读此列。
- **老数据回填**：启用 V31 前写入的老记忆 entities=NULL。admin 在 [RagMemorySettingsTab.vue](../../frontend/src/components/settings/RagMemorySettingsTab.vue) 点「回填实体标签」→ `POST /api/system/settings/rag-memory/backfill-entities`（[SystemSettingController](../../backend/src/main/java/com/superprogrammer/system/controller/SystemSettingController.java)，`role:manage`）→ [MemoryService.backfillEntitiesAsync](../../backend/src/main/java/com/superprogrammer/chat/service/MemoryService.java) 异步跑（fire-and-forget 到 `memoryTaskExecutor`，无 HTTP 超时）。
  - batch LLM 调用（≤20 条/批，[MemoryConflictJudge.batchExtractEntities](../../backend/src/main/java/com/superprogrammer/chat/service/internal/MemoryConflictJudge.java)）。
  - 幂等：只查 `entities IS NULL`；LLM 抽不出实体的行落 `entities='[]'`（标记已处理，重跑跳过，不残留 NULL）。
  - 不 bump `updated_at`（不扰动记忆列表排序）；进度见后端日志 `memoryBackfill`。
  - **排坑（别和 RAG 索引轮询混）**：点完按钮后日志刷屏 `SELECT ... knowledge_index_jobs ... FOR UPDATE SKIP LOCKED`（Total: 0）是 [IndexJobWorker.poll()](../../backend/src/main/java/com/superprogrammer/knowledge/service/IndexJobWorker.java) 每 5s（`knowledge.index.poll-ms`）认领 **RAG 索引队列**的正常背景噪声，**和记忆回填无关**，永远响。回填日志走独立线程 `memoryTaskExecutor`、前缀 `memoryBackfill`，两者并行不阻塞。验证回填是否真跑了：grep `memoryBackfill`——0 行会打 `无待回填行（全部已处理）`，跑了会打 `待回填 N 行` / `chunk 完成` / `完成，更新 X 行`；info 级，日志级别 ≥ WARN 看不到。
- **老记忆关键词重抽**（与回填互补，维护用）：改 entities/召回词袋/上位词 prompt 后，回填只补 NULL 吃不到老数据 → admin 点「重抽关键词」→ `POST /api/system/settings/rag-memory/reextract-entities`（[SystemSettingController](../../backend/src/main/java/com/superprogrammer/system/controller/SystemSettingController.java)，`role:manage`）→ [MemoryService.reextractEntitiesAsync](../../backend/src/main/java/com/superprogrammer/chat/service/MemoryService.java) 全量重抽 entities（[findAllMemories](../../backend/src/main/java/com/superprogrammer/chat/mapper/UserMemoryMapper.java) 无 NULL 过滤，≤20 条/批 [batchExtractEntities](../../backend/src/main/java/com/superprogrammer/chat/service/internal/MemoryConflictJudge.java)）。保留 key_zh 不动、重 embed anchor；fail-safe 抽空保留旧 entities 防回归；不 bump `updated_at`；进度日志 `memoryReextract`（`待重抽 N 行` / `chunk 完成` / `完成，重抽 X 行，跳过 Y 行`）。

### 中文标签 + 召回词袋变体（V32）
治"实体桥接"漏召的根因补救：英文 `memory_key`（如 `child_name`）+ value 可能只是专名（"啊闪"），桥接词"女儿"无处可搜 → query「带女儿去玩」0 命中（纯从 value 抽 entities 永远抽不到"女儿"，因为 value 里没有）。V32 双管齐下：
- **列**：`user_memories.memory_key_zh` TEXT（[V32 迁移](../../backend/src/main/resources/db/migration/V32__user_memories_memory_key_zh.sql)，可空）存中文主标签（"女儿"）。
- **entities 扩成召回词袋**：V31 原只抽原文字面词；V32 改抽三类 = ① key_zh 标签（必含）② 同义变体 1-3（女儿→孩子/小孩/闺女；公司→单位/企业）③ value 专名。例 `value="啊闪" key=child_name key_zh="女儿"` → `entities:["女儿","孩子","小孩","啊闪"]`。**角色词必含**（value 只有专名时从 key 语义补角色词+变体），否则 query 用称谓时召回不到。变体污染（"孩子"同时命中儿女）可接受，LLM 下游消歧。`readEntities` 上限 6→10 容纳变体（V38 第四类上位词加入后再抬 10→20，见下文 F1/F2）。
- **三列并查**：[findByKeyword](../../backend/src/main/java/com/superprogrammer/chat/mapper/UserMemoryMapper.java) 现搜 `entities::text OR memory_key OR memory_value OR memory_key_zh OR block_label`（五列并查），任一命中即召回。`memory_key` 让英文键（child_name）也成为召回锚点；`block_label` 让 query 命中信息块标签（如「家庭」「个人信息」）即召回整块记忆。**per-block_label 阈值筛选（V34）**：[applyKeywordPerBlockThreshold](../../backend/src/main/java/com/superprogrammer/chat/service/MemoryService.java) 按 block_label 分组各自阈值 N（默认10，0=禁用），组内 > N 时优先留高优（entities/memory_key/memory_key_zh 命中，不卡阈值），低优（memory_value/block_label）补到 N。
- **写入**：[MemoryConflictJudge.extract](../../backend/src/main/java/com/superprogrammer/chat/service/internal/MemoryConflictJudge.java) 顺带抽 key_zh + 变体词袋（**0 额外调用**，复用 extract LLM）；[ExtractedFact](../../backend/src/main/java/com/superprogrammer/chat/service/internal/ExtractedFact.java) record 加 `keyZh`；[applyClean](../../backend/src/main/java/com/superprogrammer/chat/service/MemoryService.java) 落 `memory_key_zh`（insert/updateCleanMemory 均带）。
- **老数据回填**：复用既有「回填实体标签」端点（`POST /api/system/settings/rag-memory/backfill-entities`），查询扩为 `entities IS NULL OR memory_key_zh IS NULL`（补 V31 老数据 + V31 回填过但缺 key_zh 的行）；[batchExtractEntities](../../backend/src/main/java/com/superprogrammer/chat/service/internal/MemoryConflictJudge.java) 返回 `Map<Long,BackfillRow(entities,keyZh)>`；mapper `updateEntities`→`updateEntitiesAndKeyZh`（同时更两列，不 bump updated_at）；空 entities→`"[]"`、空 keyZh→`""`（非 null，幂等标记，重跑跳过）。
- **前端**：记忆面板加「名称」列（key_zh，[chat.ts](../../frontend/src/api/chat.ts) `UserMemory` 加 `memoryKeyZh`）+「信息块」列（blockLabel）+「创建」列（createdAt），复用既有 `formatTime`（绝对时间；微信相对样式是后续要求，未做）。

## 后端 (backend)
- 控制器：[MemoryController.java](../../backend/src/main/java/com/superprogrammer/chat/controller/MemoryController.java)
  - `GET /rag-mode`(用户级只读全局开关，非 admin) `GET /api/chat/memories` `DELETE /{id}` `DELETE /batch`(批量删，body=Long[]，ownership 过滤，返实删条数) `DELETE`(清空) `GET /conflicts` `GET /incident` `PUT /conflicts/{id}/resolve` `POST /conflicts/batch-resolve`
- 服务：`chat/service/` [MemoryService.java](../../backend/src/main/java/com/superprogrammer/chat/service/MemoryService.java)（抽取/读/写）、[MemoryConflictService.java](../../backend/src/main/java/com/superprogrammer/chat/service/MemoryConflictService.java)
  - key 归一：extract 前查用户既有 `memory_key` 列表注入 LLM prompt 做**语义复用**（同概念复用既有 key，不造变体），取代手写 alias 表。归一在 extract 源头完成 → 下游 `eq(memoryKey)` 去重/冲突/UI 天然正确。
  - processFacts 性能：三段式——预去重+classify（embed **并行**，跑独立 [memoryTaskExecutor](../../backend/src/main/java/com/superprogrammer/chat/config/MemoryTaskExecutorConfig.java) `mem-task-` 线程，core4/max8，与 KB 索引隔离）→ 按 block 聚合 → 每块一次 batch judge。embed N 次并行、judge N 次→#blocks 次。注：processMemory 已在 servlet/`mem-task-` 上（非 reactor-nio），可安全 `join()`。
  - clean 写入 `applyClean`：判"非冲突"（含细化/补充，如 杭州→杭州滨江区西兴）时，**同 key 既有 clean 行 → `updateCleanMemory` 原地 UPDATE**（value+confidence+embedding+block）；否则 INSERT，INSERT 撞唯一约束 → 兜底 UPDATE。取代旧 `insertClean`（旧版只会插新行，撞 `(user_id,memory_key)` clean 唯一约束就静默吞 → 细化值全丢）。

## 异步与缓存（扩容优化）
- **异步模式**：`getMemoryProcessMode` 全局设置（[SystemSettingService](../../backend/src/main/java/com/superprogrammer/system/service/SystemSettingService.java)）控制。
  - `ASYNC`（默认）：stream + 非 stream 路径都 fire-and-forget 到 `memoryTaskExecutor`，**不 gate 回复**；冲突落 DB 后靠前端轮询浮现。
  - `HYBRID`：同步 `processMemory`，askText 追问即时追加进回复（gate 回复），保留为可选。
  - `interceptConflict`（PENDING 答复判定）**保持同步**——仅 PENDING 存在时触发，语义须回复前判定。
- **前端浮现**：[chat store](../../frontend/src/stores/chat.ts) 常驻 3s 轮询 `GET /chat/memories/conflicts`（抄 DocumentManager conditional setInterval 范式），`activeConflictCount` 驱动 ChatView 记忆按钮 `n-badge` 角标。无 SSE/WS push 通道。
- **写入异常不静默**：applyClean 真异常（插不进也更新不了）→ `MemoryService.memoryIncidents`（per-user ConcurrentMap）记消息 + ERROR 日志，不再 catch 吞。前端轮询顺带取 `GET /chat/memories/incident`（取即清）→ ChatView `watch(memoryIncident)` 弹 `useMessage().error` 6s。
- **Redis 查询缓存**：[MemoryQueryCache](../../backend/src/main/java/com/superprogrammer/chat/service/internal/MemoryQueryCache.java)（`StringRedisTemplate`，prefix `mem:`，TTL 60s）缓存 `findDistinctKeys` + `findCleanByBlock`，砍每轮 DB 往返。**写时失效**（`evictUser`）：applyClean / createFlagged / flag / resolve / deleteMemory / clearMemories；纯 TTL 不够（会漏冲突），正确性靠 evict。范式照抄 VisibilitySetService。
- 内部组件：`chat/service/internal/`
  - [MemoryBlockClassifier.java](../../backend/src/main/java/com/superprogrammer/chat/service/internal/MemoryBlockClassifier.java) — 记忆分类
  - [MemoryConflictJudge.java](../../backend/src/main/java/com/superprogrammer/chat/service/internal/MemoryConflictJudge.java) — 冲突裁决
  - ExtractedFact、JudgeResult、RouteResult（DTO）

## 冲突状态机（MemoryConflictService）
- **PENDING**：new 事实暂存快照（不入 user_memories），锁会话（每 session+user 仅 1 个 PENDING，`findActivePending` 只查 PENDING），带 askText 追加进当轮回复问用户。超时未答 → 懒触发 `flag()` 转 FLAGGED。
- **FLAGGED**：new 立即带 `conflict_id` 入库 + old 同组打标（绕 `(user_id, memory_key)` clean 唯一约束，新旧双值共存）。面板可见（`findActiveByUser` 含 FLAGGED），用户 batch-resolve。**不占 PENDING 锁**。
- **RESOLVED**：用户决定（KEEP_NEW / KEEP_OLD / KEEP_BOTH / DISCARD）后落库。**KEEP_BOTH = 合并成一条 clean**（value 去重 join `，`，survivor 清 conflict_id，删多余行，重 embed），非旧"双行共存"——见「待增修改 6」。
- **一句话多冲突**：首轮冲突建 PENDING + 单一 askText（一句话只问一次）；同轮后续冲突 `createFlagged` 直建 FLAGGED（不丢事实、不阻塞、进面板待办），不再降级 clean。

- 实体：`chat/entity/` UserMemory、MemoryConflict
- DTO：UserMemoryVO、MemoryCandidateVO、MemoryBlockHit、MemoryConflictVO、MemoryConflictResolveRequest、MemoryConflictBatchResolveRequest

## 前端 (frontend)
- 组件：[MemoryManagerPanel.vue](../../frontend/src/components/chat/MemoryManagerPanel.vue)（挂在 ChatView 记忆抽屉，`n-drawer` width=720）
- API：[chat.ts](../../frontend/src/api/chat.ts)（memory 子接口）
- 触发：对话流式回复后自动抽取记忆
- **批量删除（chat 窗口）**：panel 表格首列 `type:'selection'` 复选框 + 头部「删除选中 (N)」按钮 → `chatApi.batchDeleteMemories(ids)` → `DELETE /chat/memories/batch`。坑：`actions` 列 `fixed:'right'` 进 scroll-x，selection 列**必须显式设 `width`**（已设 45），否则塌缩 0 宽复选框不可见、批量删用不了（n-data-table fixed 列通用陷阱）。
- **记忆开关持久化 + 会话/全局联动（两个开关）**：
  - **全局开关**（[RagMemorySettingsTab.vue](../../frontend/src/components/settings/RagMemorySettingsTab.vue)「记忆模式（全局）」，admin）：`PUT /system/settings/rag-memory` 写 DB `rag.memory.enabled`，`onMounted` load 回读。`handleSave` **不能 guard `!enabled`**——关开关也要入库，否则 OFF 永不持久化、刷新回弹 ON（已修）。Vue 编译器把 `v-model:value` + `@update:value` 合并成数组、v-model 先跑，故 enabled ref 先更新再 handleSave。
  - **会话级开关**（[ChatView.vue](../../frontend/src/views/ChatView.vue) 输入栏「记忆模式」，普通用户）——**三态联动全局**（修"刷新重置为关闭"）：
    - `ragPref: boolean|null`（null=继承全局）。null 时开关显示全局值（`ragEffective = ragPref ?? globalRag`），发消息**省略** `ragEnabled` 字段 → 后端 `ChatRequest.ragEnabled=null` → `ChatSessionService` 不改 `session.rag_enabled` → [RagModeResolver](../../backend/src/main/java/com/superprogrammer/knowledge/service/RagModeResolver.java) 继承 global。非 null 时覆盖（随消息写 `session.rag_enabled`）。
    - `globalRag` 来自用户级只读端点 `GET /api/chat/memories/rag-mode`（[MemoryController](../../backend/src/main/java/com/superprogrammer/chat/controller/MemoryController.java) → [ChatRagModeVO](../../backend/src/main/java/com/superprogrammer/chat/dto/ChatRagModeVO.java)，**非 admin**，因 `/system/settings/rag-memory` 要 role:manage 普通用户 403）；`onMounted` 拉取。
    - 用户点开关 → `onRagToggle` 写 localStorage `chat_rag_enabled`（持久化覆盖）；「跟随」按钮 → `resetRagToGlobal` 清键回 null（恢复继承）。`clearAuthStorage` 不清此键。
    - **旧 bug**：`ragEnabled` 默认 `false`（非 null）每条消息发 `ragEnabled:false` → 显式关盖掉全局 ON → "全局开了但不生效/刷新回关"。三态 null 修复。

## Sidecar
无（记忆逻辑在后端对话服务内）。

## 系统设置联动
记忆开关由系统设置 `rag-memory` 控制，见 [21-系统设置](21-系统设置.md)。检索模式（全量/向量/混合）同由 `rag-memory.retrievalMode` 控制，见本文「记忆检索模式」节。

## 数据表
`user_memories`、`memory_conflicts`

### 标签语言 + 全量超阈值两阶段（V33）
两个新系统设置（均在 [RagMemorySettingsTab.vue](../../frontend/src/components/settings/RagMemorySettingsTab.vue)，admin）：
- **记忆标签语言** `rag.memory.key-language` = `EN`(默认,向后兼容) / `ZH` / **`BOTH`（中英双选）**。控制**注入上下文里 key 的展示语言**——`[MemoryService.formatLine](../../backend/src/main/java/com/superprogrammer/chat/service/MemoryService.java)`：EN 用 `memory_key`；ZH 用 `memory_key_zh`（空回退英文 key）；**BOTH 中英并列** `memory_key_zh(memory_key)`（如 `女儿(child_name)`，key_zh 空则仅英文兜底）。仅展示层，不影响 key 匹配/去重/冲突（这些始终用英文 `memory_key`）。
- **全量记忆阈值** `rag.memory.full-context-threshold` = int（默认 20，0=禁用）。仅 **LLM_FULL_CONTEXT** 模式生效：记忆条数 > 阈值 且 query 非空 → 不再全量灌 value，改**三维合并筛选**（见下文「三维合并筛选（key × key_zh × block_label，一次 LLM，V38）」节）：① `memory_key`（带 key_zh）+ `block_label` 各剔重；② **一次 LLM** 同时选 keys/keys_zh/blocks；③ 取 **key 命中 且 key_zh 命中 且 block 命中** 的记忆行；④ 注入只保留 **key（按标签语言 EN/ZH/BOTH）+ value**，丢 `category`/`block_label`。省 token + 治联想泄漏。LLM 失败/三维均空 → 不注入（宁缺毋滥）；query 空/未超阈值 → 走原全量。VECTOR_KEYWORD / EMBEDDING_VECTOR 已是定向召回，不受此阈值影响。
- 设置走 `system_settings` KV 表 upsert，**无需 Flyway 迁移**。VO/Request 加 `keyLanguage` + `fullContextThreshold` 两字段（[RagMemorySettingsVO](../../backend/src/main/java/com/superprogrammer/system/dto/RagMemorySettingsVO.java)）。

### 三维合并筛选（key × key_zh × block_label，一次 LLM，V38）
> 演进：单维 key 筛 → 双维度（key+block，2 次 LLM）→ **三维合并（key+key_zh+block，1 次 LLM）**。
> 入口 [MemoryService.filterRelevantKeys](../../backend/src/main/java/com/superprogrammer/chat/service/MemoryService.java) → `selectRelevantMemories`（三路径共用）+
> [MemoryConflictJudge.selectRelevantKeysBlocks](../../backend/src/main/java/com/superprogrammer/chat/service/internal/MemoryConflictJudge.java)（一次 LLM 出 keys/keys_zh/blocks 三数组）。

**动机（迭代理由）**：
- 单维 key 筛：同 key 多行重复刷 token + block 维未筛。
- 双维度 2 次 LLM：key 维、block 维分开调，多花一次 round-trip + token；且 LLM 只看英文 key+value，**看不到中文 key_zh**，英文名（`spouse_name`）语义弱时易漏选。
- 三维合并：① 一次 LLM 同时选 keys/keys_zh/blocks，**砍第二次调用**；② prompt 输入带 `妻子(spouse_name): 阿斐`（key_zh 进**选择**，不再只展示），LLM 一眼懂语义；③ 三维 AND 交集，key_zh 维对英文名弱场景双保险。

**新三步（Java 剔重 + 1 次 LLM）**：
1. **剔重**：`distinct memory_key`（每行带 key_zh/value）+ `distinct block_label`（null→""）。
2. **一次 LLM 三选**：[selectRelevantKeysBlocks](../../backend/src/main/java/com/superprogrammer/chat/service/internal/MemoryConflictJudge.java) 灌 `key_zh(key): value` 列表 + block 列表 → 输出 `{"keys":[...], "keys_zh":[...], "blocks":[...]}`（召回优先，宁多选；prompt `SELECT_KEYS_BLOCKS_PROMPT`）。
3. **三维 AND 交集装配**：候选 = `memory_key∈keys 且 memory_key_zh∈keys_zh 且 block_label∈blocks` 的记忆行。key_zh 空白行（老数据无标签）通配通过该维，不误杀。
4. **注入收口**：只输出 **key（按 `key-language`：EN=memory_key / ZH=memory_key_zh / BOTH=中英并列）+ value**；`category`/`block_label`/`entities`/`confidence` 全丢。

**生效范围（三路径共用 `filterRelevantKeys`）**：
- **LLM_FULL_CONTEXT 超阈值两阶段**（本文）。
- **VECTOR_KEYWORD 0 命中 LLM-key 兜底**（[buildHybridContext](../../backend/src/main/java/com/superprogrammer/chat/service/MemoryService.java) → `llmFallbackContext`，共享同一筛，自动受益）。
- **LLM_KEY 模式 LLM 精排**（[buildLlmKeyContext](../../backend/src/main/java/com/superprogrammer/chat/service/MemoryService.java) step ③）。

**容错**：单次 LLM 失败/解析失败 → 不注入（`return null`，宁缺毋滥）。三维任一为空集 → AND 结果空 → 不注入（LLM 判该维无相关）。剔重 Java 端（`Stream`），不新增 DB 查询。

## 锚点语义召回 + 预览完善（V38）
> 详尽开发清单见 [09-...-优化升级开发落地](09-个人记忆与冲突解决-优化升级开发落地.md)。目标：百万 key 规模下治「纯向量/混合漏召」+ 预览透出召回全过程。阶段 A–D + F1/F2 已落地，E（联调）/F3-F4（backfill force + 联调）待续。

### 数据层（V38 迁移 `V38__user_memories_anchor_recall.sql`）
- 召回锚点列：`anchor_embedding halfvec(2048)`（语义粗筛主通道）+ `anchor_tokens text` + `anchor_tokens_tsv tsvector GENERATED STORED`（BM25 通道，jieba 分词后空格串）。
- 索引：`idx_user_memories_anchor_hnsw`（halfvec 余弦 HNSW）+ `idx_user_memories_anchor_tsv`（GIN）。
- 锚点文本 = `block_label + memory_key_zh + memory_key + entities(展开)`（[MemoryService.buildAnchorText](../../backend/src/main/java/com/superprogrammer/chat/service/MemoryService.java)，null 安全），embed 模型同 value（`MEMORY_EMBED_MODEL`），**独立于 value embedding 不替代**。
- mapper（[UserMemoryMapper](../../backend/src/main/java/com/superprogrammer/chat/mapper/UserMemoryMapper.java)）：`findTopKByAnchor`（`anchor_embedding <=>` top-K）+ `findAnchorBm25`（`ts_rank(anchor_tokens_tsv, plainto_tsquery)`，带 `SCOPE_FILTER`）。`<=>` 须 XML 转义 `&lt;=>`（V33 踩过）。IT [UserMemoryMapperAnchorIT](../../backend/src/test/java/com/superprogrammer/chat/mapper/UserMemoryMapperAnchorIT.java) 9/9。
- 写入侧：applyClean / processFacts embed 段 / merge 同步落 anchor 两列（复用 embed 调用，0 额外 LLM）。

### LLM_KEY pipeline（[MemoryService.buildLlmKeyContext](../../backend/src/main/java/com/superprogrammer/chat/service/MemoryService.java)）
① [QueryExpansionService](../../backend/src/main/java/com/superprogrammer/knowledge/service/QueryExpansionService.java) 多 qHalf 扩 query → ② 每 qHalf `findTopKByAnchor` + `findAnchorBm25` → [RrfFusion](../../backend/src/main/java/com/superprogrammer/knowledge/service/internal/RrfFusion.java) 加权融合（向量 > 词法）→ 粗筛 top-N（默认 40）→ ③ LLM 三维精排（key × key_zh × block，[selectRelevantKeysBlocks](../../backend/src/main/java/com/superprogrammer/chat/service/internal/MemoryConflictJudge.java) **一次调用**出三数组；`rerank` 可关跳③直注 top-N）→ ④ 注入选中 key+value → ⑤ [MemoryQueryCache](../../backend/src/main/java/com/superprogrammer/chat/service/internal/MemoryQueryCache.java).getRerankKeys 缓存（TTL 60s）。异常降级：扩展挂→单 qHalf；向量挂→仅 BM25；都空→`return null`（不回退全量）。

### 上位词召回词袋（F1/F2）
[MemoryConflictJudge.extract](../../backend/src/main/java/com/superprogrammer/chat/service/internal/MemoryConflictJudge.java) entities 抽取加第四类「所属类别泛称/上位词」，**9 类覆盖全部 block 标签，每类列 9 个 2+ 字上位词候选**：配偶/孩子/父母/宠物→家人/家庭/亲属；公司/职位/单位→工作/职业/职场；过敏/病史/身高体重→健康/身体/体质；住址/城市/搬家→居住地/住址/居住；学校/专业/学历→教育/学历/学业；爱好/口味/运动→偏好/爱好/喜好；电话/邮箱/微信→联系方式/联系/通讯；收入/工资/房贷→财务/收入/资产；名字/年龄/生日→基本信息/个人/档案。**上位词至少 5 个、能多则多往 8-10 靠（上限 10），确实不足 5 才减，不止于候选可自行补**，**严禁单字**（单字不进 2-gram 分词召不回 + 高频噪声）。**0 额外 LLM 调用**（复用 V32 extract 顺带抽）；总词袋上限 `共 ≤20 个`（硬，`readEntities` 截断 20、每词 ≤8 字符）、上位词「上限 10」是 prompt 软约束（模型听不听看脾气，超 20 由 Java 硬切）。**EXTRACT_PROMPT（新对话）+ BATCH_ENTITIES_PROMPT（回填/重抽）两 prompt 同步此规则**——改 prompt 后老数据吃新规则靠「重抽关键词」端点（见上文）。救 VECTOR_KEYWORD：query「我想带我家人出去玩」关键词通道（[findByKeyword](../../backend/src/main/java/com/superprogrammer/chat/mapper/UserMemoryMapper.java) ILIKE `entities::text`）命中 entities 含「家人」；LLM_KEY 则靠 entities 烤进 anchor 文本（[buildAnchorText](../../backend/src/main/java/com/superprogrammer/chat/service/MemoryService.java)）→ anchor 向量 + BM25 两通道受益。

### 预览透出召回过程（D1–D3，前端 D4–D6）
[MemoryContextPreviewVO](../../backend/src/main/java/com/superprogrammer/chat/dto/MemoryContextPreviewVO.java) 加三字段：`candidates`（粗筛 top-N，含 key_zh/value 预览/block/scope/命中通道）+ `selectedKeys`（LLM 精排选中 key 列表）+ `channels`（vector/keyword/bm25/llmFallback 命中统计）。仅预览路径收集，真实注入路径零开销。IT [MemoryPreviewDebugIT](../../backend/src/test/java/com/superprogrammer/chat/service/MemoryPreviewDebugIT.java) 2/2（真 PG + `@MockBean` 引擎）。
- 前端 [MemoryManagerPanel.vue](../../frontend/src/components/chat/MemoryManagerPanel.vue) 预览卡加「预览范围」scope 选择器（默认当前会话 scope，可切 global-only/全部可读项目/指定项目）+「召回过程」折叠区（通道命中 tag + 粗筛候选列表，✓ 标 LLM 精排选中）。
- 前端 [RagMemorySettingsTab.vue](../../frontend/src/components/settings/RagMemorySettingsTab.vue) 检索模式加 `LLM_KEY`、标签语言加 `BOTH`、加 LLM_KEY 旋钮组（coarse-top-n + rerank + keyword-max）。

### 关键词召回 per-block 阈值（V34）
- **设置** `rag.memory.keyword-per-block-threshold` = int（默认 10，0=禁用）。仅 **VECTOR_KEYWORD** 模式生效。前端 [RagMemorySettingsTab.vue](../../frontend/src/components/settings/RagMemorySettingsTab.vue) 「关键词召回块阈值」（混合模式才可改）。
- **作用**：[MemoryService.applyKeywordPerBlockThreshold](../../backend/src/main/java/com/superprogrammer/chat/service/MemoryService.java) 把关键词召回结果按 `block_label` 分组（null 视作 ""），各组各自阈值 N：
  - 组内命中 ≤ N → 全留（如"职业"块只命中 3 条，3 条全要）。
  - 组内 > N → 命中高优字段（`entities`/`memory_key`/`memory_key_zh`，由 [isHighPriorityHit](../../backend/src/main/java/com/superprogrammer/chat/service/MemoryService.java) 对 tokens 子串判定）的行**不卡阈值全留**（哪怕 15 条都留），低优（仅 `memory_value`/`block_label` 命中）补到 N。
- **动机**：用户记忆可能某块爆炸（"偏好"几十条），无脑全灌噪声大、token 费；高优字段命中 = 该行确实被 query 指名道姓撞上（实体/标签/键），低优只是值/块模糊蹭中。优先级排序后既不漏强相关，又压住噪声块。N=0 禁用 = 回退旧"仅 HYBRID_MAX 截断"行为。
- **链路位置**：[findByKeyword](../../backend/src/main/java/com/superprogrammer/chat/mapper/UserMemoryMapper.java) 去掉旧全局 `LIMIT 10`（改 LIMIT 200 防病态），per-block 筛选在 Java 端做（SQL 不带行级优先级标记，Java 用 tokens 复判高优/低优）；筛选后并入向量 top-K → `HYBRID_MAX=8` 截断 → 0 命中 LLM 兜底。
- 设置走 KV 表 upsert，**无迁移**。VO/Request 加 `keywordPerBlockThreshold` 字段。

### 记忆注入可观测性 + 预览（V33）
- **后端日志**：[MemoryService.buildMemoryContext](../../backend/src/main/java/com/superprogrammer/chat/service/MemoryService.java) 入口/出口打一条 info，含 `mode/keyLang/threshold/query.len/注入字符数/首条样本`——三个设置生效与否从日志一眼可见（不注入也有 `→ 不注入` 行）。标签语言看首条样本 key（中/英）。
- **前端预览**（用户自验证，无需看日志）：[MemoryManagerPanel.vue](../../frontend/src/components/chat/MemoryManagerPanel.vue) 顶部「记忆注入预览」卡——输入测试问题 → `POST /api/chat/memories/preview`（[MemoryController](../../backend/src/main/java/com/superprogrammer/chat/controller/MemoryController.java) → [MemoryService.previewContext](../../backend/src/main/java/com/superprogrammer/chat/service/MemoryService.java)）→ 返回 mode/keyLang/threshold/记忆总数/是否两阶段 + **实际注入 LLM 的「用户记忆:」上下文原文**（pre 展示）。不注入时显示「无命中/LLM判无关」。普通用户可看自己的（按 userId 隔离，非 admin）。

## 项目记忆 scope（V33，待增修改 8）
引入「项目」概念：**总记忆(global) + 单独项目记忆**。对话可选 scope；**一条记忆可同时属多个 scope**（global + 多项目，多对多可见性）；新事实落当前会话 project；用户可手动改记忆 scope 归属。老数据/老会话向后兼容（= 今天行为）。

### 锁定设计
1. **Project = 全新实体**，与 Agent 解耦。
2. **归属 = 私有 + 可共享**（`project_members`）。鉴权走 service（owner/member/admin），不用 `@RequirePermission`。
3. **Scope = 多对多可见性，非分区**。`user_memories.is_global` + 关联表 `user_memory_projects`。唯一索引 V29 `(user_id,memory_key) WHERE conflict_id IS NULL` **不动**（冲突按 user+key 在可见集判，与 scope 正交）。
4. **读/写分离**：
   - 读（注入）= 扁平对称开关集（`mem_include_global` 勾 + `mem_read_project_ids` 每项各勾）。三组合全支持：关总记忆只读项目 / 关某项目读其他 / 同开 global+多项目。
   - 写（新事实落点）= 单独 `session.project_id`（null=global）。
5. **新事实**：`is_global=(project_id==null)`；project 写目标 → 挂 `user_memory_projects`。

### 数据表（[V33__project_memory_scope.sql](../../backend/src/main/resources/db/migration/V33__project_memory_scope.sql)）
- `projects`（owner=created_by，照 BaseEntity + agents 列约定）。
- `project_members`（共享：role `OWNER/EDITOR/VIEWER`，UNIQUE(project_id,user_id)）。
- `user_memory_projects`（memory↔project 多对多可见性标签，PK(memory_id,project_id)）。
- `user_memories.is_global BOOLEAN NOT NULL DEFAULT true`（老行=今天行为）。
- `chat_sessions` 三列：`project_id`（写目标，NULL=global 会话）/ `mem_include_global`（读开关，默认 true）/ `mem_read_project_ids BIGINT[]`（读开关：开启读取的项目集）。

### 后端
- **Project 领域**：实体 [Project](../../backend/src/main/java/com/superprogrammer/project/entity/Project.java) / [ProjectMember](../../backend/src/main/java/com/superprogrammer/project/entity/ProjectMember.java)（均 extends BaseEntity）；Mapper `ProjectMapper` / `ProjectMemberMapper`；DTO ProjectVO/ProjectCreateRequest/ProjectMemberVO/ProjectShareRequest；[ProjectService](../../backend/src/main/java/com/superprogrammer/project/service/ProjectService.java)（CRUD + 共享 + `canAccess`/`canManage`/`listAccessibleProjectIds`）；Controller `/api/projects`（list/get/create/update/delete/members）。
- **scope 解析**：[MemoryScope](../../backend/src/main/java/com/superprogrammer/chat/service/internal/MemoryScope.java) record `(userId, includeGlobal, enabledProjectIds)` + `globalOnly(userId)` 工厂；[MemoryScopeResolver](../../backend/src/main/java/com/superprogrammer/chat/service/MemoryScopeResolver.java) `resolveReadScope` / `resolveWriteScope` / `resolveWriteTarget`，项目集 ∩ `ProjectService.listAccessibleProjectIds` 过滤（防撤权泄漏）。照 RagScopeResolver 范式。
- **mapper 全 scope 化**：[UserMemoryMapper](../../backend/src/main/java/com/superprogrammer/chat/mapper/UserMemoryMapper.java) 常量 `SCOPE_FILTER`（`AND (1=0 [OR m.is_global] [OR id IN user_memory_projects])`）内联进 `findNearestBlock` / `findTopKByVector` / `findByKeyword` / `findCleanByBlock` / `findDistinctKeys` + 新 `findFullContext` / `findAllClean` / `countByScope`。关联方法 `insertMemoryProjects` / `deleteMemoryProjects` / `findProjectIdsByMemory` / `findProjectIdsByMemories`（批量避 N+1）/ `updateIsGlobal`。`insertMemory` 加 `is_global`（`COALESCE(#{m.isGlobal},true)` 兜底，冲突行默认 global）。
- **service 全签名换 scope**：[MemoryService](../../backend/src/main/java/com/superprogrammer/chat/service/MemoryService.java) `buildMemoryContext(scope,query)` / `previewContext` / `processMemory(scope,writeTarget,...)` / `extractFacts` / `applyClean`（写 is_global + 挂项目）/ 块聚类限 scope。[MemoryConflictService.createFlagged](../../backend/src/main/java/com/superprogrammer/chat/service/MemoryConflictService.java) 加 `writeTargetProjectId`（冲突行也落正确 scope）。
- **会话串联**：[ChatSessionService](../../backend/src/main/java/com/superprogrammer/chat/service/ChatSessionService.java) 8 调用点全穿 scope（sendMessage 2 + doSendMessageStream 2 + streamWorkflow 2 + buildMemoryContext 2），**lambda 外解析**（reactor 闭包安全；admin=false 记忆私有）。`createSession` / 消息级 `persistMemoryScope`（LambdaUpdateWrapper 显式 set 三列，projectId null 能清）。
- **端点**：`GET/PUT /api/chat/memories/{id}/scopes`（替换全部 scope：统一覆盖升级 global / 加项目 / 关 global）+ preview 带 scope。
- **DTO**：MemoryScopeUpdateRequest、MemoryScopeVO、MemoryPreviewRequest 加 scope 字段、UserMemoryVO 加 `isGlobal`+`projectIds`、ChatRequest/ChatSession 加 scope 字段、MemoryProjectRow。

### 前端
- [api/project.ts](../../frontend/src/api/project.ts)：Project/ProjectMember 类型 + projectApi 全 CRUD/共享。
- [api/chat.ts](../../frontend/src/api/chat.ts)：`ChatSendRequest` 加 scope 字段、`UserMemory` 加 `isGlobal`/`projectIds`、`MemoryScopeVO` 类型、`previewMemoryContext(query,scope?)`、`getMemoryScopes`/`updateMemoryScopes`。
- 组件：[ProjectSelector.vue](../../frontend/src/components/chat/ProjectSelector.vue)（写目标单选，克隆 TargetSelector）+ [ProjectManagerModal.vue](../../frontend/src/components/chat/ProjectManagerModal.vue)（项目 CRUD：新建/列表/删除，owner 才能删）。
- [stores/chat.ts](../../frontend/src/stores/chat.ts)：scope 态 `memProjectId`/`memIncludeGlobal`/`memReadProjectIds` + `memoryScopePayload` computed，穿进 sendMessage/sendStreamingMessage（新旧会话两条 stream 路径）。
- [ChatView.vue](../../frontend/src/views/ChatView.vue)：tools 区加「记忆范围」组（ProjectSelector + 含总记忆 switch + 项目多选 + 项目管理入口按钮）+ ProjectManagerModal。
- [MemoryManagerPanel.vue](../../frontend/src/components/chat/MemoryManagerPanel.vue)：加「归属」列（global tag + project tags，N+1 用 `findProjectIdsByMemories` 批量）+ 行操作「归属」按钮 + scope 编辑 modal（总记忆勾 + 项目多选 → `updateMemoryScopes`）。

### IT（真 PG + pgvector，H2 跑不了 halfvec/BIGINT[]/ILIKE-JSONB）
- [UserMemoryMapperScopeIT](../../backend/src/test/java/com/superprogrammer/chat/mapper/UserMemoryMapperScopeIT.java)（10 测）：SCOPE_FILTER 在 keyword/向量 top-K/同块 clean/count 四路径 + 四开关组合（globalOnly/projectOnly/combined/allOff）+ 跨用户隔离 + 多 scope 可见 + 关联 round-trip。
- [MemoryScopeInjectionIT](../../backend/src/test/java/com/superprogrammer/chat/mapper/MemoryScopeInjectionIT.java)（5 测，RANDOM_PORT 真 HTTP）：preview 四开关组合 + `/messages/stream` @MockBean 引擎抓 ExecutionContext，断言 system prompt「用户记忆」按 session scope 注入/排除。reactor-nio 约束确认（scope 在 lambda 外解析；controller 普通 `new Thread`+`blockLast`，非 reactor-http-nio；service 无 `.block()`）。
- 跑法：`mvn test -Dsurefire.excludedGroups= -Dtest=UserMemoryMapperScopeIT,MemoryScopeInjectionIT`（须 PG `agent_platform_it` + Redis 活）。

### IT 抓出的 2 个 V33 生产回归（已修，mock 单测全漏）
> 共同点：都在 `<script>` XML / BIGINT[] 这类 H2+mock 跑不出的真实 PG 行为上；`mvn test`（mock mapper）317/317 照过，只有 it profile 真 PG + 全 context 起来才暴露。
1. **[UserMemoryMapper](../../backend/src/main/java/com/superprogrammer/chat/mapper/UserMemoryMapper.java) 向量查询 `<=>` 未 XML 转义**：V33 把 SCOPE_FILTER（含 `<if>`/`<foreach>`）内联进 `findNearestBlock`/`findTopKByVector`，强制进 `<script>` XML 模式；halfvec 余弦算子 `embedding <=> #{halfvec}` 的裸 `<` 在 XML 文本非法 → `SAXParseException` → **userMemoryMapper bean 创建失败 → 整个 ApplicationContext 启动崩（生产起不来）**。修：4 处 `<=>` → `&lt;=>`（MyBatis 解析后还原）。详见记忆 `reference_mybatis_script_halfvec_escape`。
2. **[ChatSessionService.persistMemoryScope](../../backend/src/main/java/com/superprogrammer/chat/service/ChatSessionService.java) BIGINT[] 漏 typeHandler**：LambdaUpdateWrapper `.set(ChatSession::getMemReadProjectIds, list)` **不读**实体 `@TableField(typeHandler=LongArrayTypeHandler.class)`，裸 `List` 进 PG cast `bigint[]` 失败 → `DataIntegrityViolation` → 被 controller catch 吞 → **streaming 静默走 fallback sendMessage，scope 不生效**。修：改 3 参 `set(col, val, "typeHandler=com.superprogrammer.common.typehandler.LongArrayTypeHandler")`（projectId/memIncludeGlobal 标准类型不动）。

### 已知限制（v1，可后续补）
- FLAGGED 冲突行经 `flag()` 懒过期路径插入时，`is_global` 走 COALESCE 默认 true（global 可见），未按写目标 project 精确归 scope（需 `memory_conflicts.project_id` 列 + flag 读 c.projectId，本版未加）。
- scope 编辑不 re-embed（仅改可见性，value/embedding 不变）——正确，记录在案。
- 老数据 `is_global` 默认 true = 今天行为，无需回填（dev 库实测 user 1：all_clean 23 = globalOnly 命中 23，零召回损失）。

## 项目记忆 home + 可见性混合（V34，取代 V33 纯可见性标签）
> V33「一条记忆属多 scope」是可见性标签语义，但唯一索引 V29 仍 `(user_id,memory_key)` scope-orthogonal → **用户实测撞墙**：global 已有 `child_name`、写目标切到项目 1 再加女儿名 → INSERT 撞 `uk_user_memories_user_key_clean` 报「记忆写入失败：child_name」。根因：dedup 查询 `findSameKeyClean` scope-filtered（只看写目标 scope），但唯一索引无视 scope → 写目标看不到的老行把 INSERT 顶爆，catch 兜底 `againSames` 也 scope-blind → 走「真异常」弹 incident。用户需求明确为**两件事都要**：① 每个项目独立存自己的 `child_name`（global/P1/P2 各一条共存）；② 一条记忆也能跨项目共享（改一次多处生效）。纯可见性标签模型（V33）和纯分区模型都只能满足其一 → 改**混合模型**。

### 两套正交概念
| 概念 | 列 | 管什么 |
|---|---|---|
| **home（写归属/唯一性槽）** | `user_memories.home_project_id`（NULL=global home） | key 在 `(user, home)` 内唯一；决定"能不能再插" |
| **可见性（读注入/共享）** | `is_global` + `user_memory_projects`（V33 既有，不动） | 决定"读时哪些 scope 拉它" |

一条记忆 = 恰好一个 home + 任意多可见性标签。
- **每项目独立**：`(home=NULL, key=child_name)` / `(home=P1, key=child_name)` / `(home=P2, key=child_name)` 三条不同 home → 唯一索引放行共存。
- **跨项目共享**：`(home=P1, key=child_name)` 想让 P2 也见 → `user_memory_projects` 加 P2 行；想让 global 也见 → `is_global=true`。改 value 一次全可见 scope 生效。home 仍只 P1 → 不与 P2-native 那条撞唯一约束。

### 读语义（确认方案 A，保持现状）
读侧零改动。`SCOPE_FILTER` 先按开关踢无关项目（`mem_include_global` + `mem_read_project_ids[]`），剩的里面同 key 多条 → **全注入** LLM（global 一条 + 项目一条都进 prompt，符合"总记忆+项目记忆并存"心智，不项目级覆盖）。home 不参与读可见性过滤。

### 数据表（[V34__user_memories_home_scope.sql](../../backend/src/main/resources/db/migration/V34__user_memories_home_scope.sql)）
- `user_memories.home_project_id BIGINT NULL`（写归属；NULL=global home）。
- 删旧 scope-orthogonal 索引：`DROP INDEX uk_user_memories_user_key_clean`。
- 新 home-aware 唯一（`COALESCE` 解 PG NULL distinct 问题，-1 哨兵）：
  ```sql
  CREATE UNIQUE INDEX uk_user_memories_user_key_home
    ON user_memories (user_id, memory_key, COALESCE(home_project_id, -1))
    WHERE conflict_id IS NULL;
  ```
- 回填：`is_global=true` 行 `home_project_id` 默认 NULL（global home，无需动）；`is_global=false` 行从 `user_memory_projects` 取首个 `project_id` 作 home（多挂取最小 id，保唯一约束不撞）。**坑（IT 抓出）**：回填 UPDATE 初版带 `m.deleted = 0` 过滤 → Flyway `42703` 炸（**user_memories 表无 `deleted` 列**——不走 BaseEntity 软删约定，与 project_members 等不同），整 ApplicationContext 起不来；删该过滤后通。又一次"mock 单测验不出、真 PG 起来才暴露"，迁移 SQL 已注释标明。
- `is_global` / `user_memory_projects` / `chat_sessions` 三列**全保留**（可见性 + 写目标 + 读开关，语义不变）。

### 后端改动
- 实体 [UserMemory](../../backend/src/main/java/com/superprogrammer/chat/entity/UserMemory.java) 加 `homeProjectId`（Long）。
- mapper [UserMemoryMapper](../../backend/src/main/java/com/superprogrammer/chat/mapper/UserMemoryMapper.java)：
  - `insertMemory` 加 `home_project_id` 列（值 `#{m.homeProjectId}`）。
  - 新 home-aware dedup（取代 scope-filtered `findAllClean` + Java filter）：
    ```java
    @Select("SELECT * FROM user_memories WHERE user_id=#{userId} AND memory_key=#{key} "
          + "AND conflict_id IS NULL AND COALESCE(home_project_id,-1)=COALESCE(#{homeId},-1)")
    List<UserMemory> findCleanByHomeKey(@Param("userId") Long userId, @Param("key") String key, @Param("homeId") Long homeId);
    ```
  - `SCOPE_FILTER` 及所有读方法（`findNearestBlock`/`findTopKByVector`/`findByKeyword`/`findCleanByBlock`/`findDistinctKeys`/`findFullContext`/`findAllClean`/`countByScope`）**不动**——读可见性仍走 `is_global`+`user_memory_projects`。
- service [MemoryService](../../backend/src/main/java/com/superprogrammer/chat/service/MemoryService.java)：
  - `findSameKeyClean` 改调 `findCleanByHomeKey(userId, key, writeTargetProjectId)`（3 调用点：预去重 `processFacts:241` + applyClean 主路径 `:264` + catch 兜底 `:287` 全换）。
  - `applyClean` INSERT 设 `m.setHomeProjectId(writeTargetProjectId)`；UPDATE 分支 scope 不变（refinement 只覆 value/embedding/block/entities/key_zh，home 与可见性标签不动）。
  - 共享（跨 scope 加可见性）仍走 `PUT /api/chat/memories/{id}/scopes` 手动加标签——写时不自动跨 scope。
- `MemoryConflictService.createFlagged` 冲突行也设 `homeProjectId=writeTargetProjectId`（与 V33 既设 is_global 对齐）。

### IT（已补已过，mock 单测验不出跨 home 共存 + COALESCE 表达式索引）
[UserMemoryMapperHomeScopeIT](../../backend/src/test/java/com/superprogrammer/chat/mapper/UserMemoryMapperHomeScopeIT.java)（5 测，真 PG+pgvector）：
- `crossHome_sameKey_allCoexist` — global/P1/P2 三条同 key child_name 不同 home 全部 INSERT 成功（用户撞墙场景正面验证）。
- `sameHome_sameKey_blockedByUniqueIndex` — 同 home 同 key 第二次 INSERT 必撞 `uk_user_memories_user_key_home`（`DuplicateKeyException`），global + P1 两 home 各验。
- `findCleanByHomeKey_isHomeScoped` — 每个 home 查只返自己那条，未知 home 返空，不串（修 V33 dedup scope-filtered 漏判根因）。
- `findCleanByHomeKey_skipsFlagged` — `conflict_id` 非空 FLAGGED 行不参与 dedup（与索引 `WHERE conflict_id IS NULL` 对齐）。
- `insertMemory_setsHomeColumn` — P1 写目标落 `home_project_id=P1`，global 写目标落 NULL。

跑法：`mvn test -Dsurefire.excludedGroups= -Dtest=UserMemoryMapperHomeScopeIT`（须 PG `agent_platform_it` + Redis 活）。
结果：5/5 过；连同 V33 两 scope IT 共 20/20，mock 单测 317/317。

### 浏览器冒烟（真 LLM 抽取链，已过）
admin 已有 global `child_name`（啊闪、啊然）前提下：写目标切「测试」项目发「我女儿叫小红」→ INSERT `home=测试`（id 251）；切「个人记忆」发「我女儿叫小芳」→ INSERT `home=个人记忆`（id 252）。终态 3 条同 key 跨 home 共存，global 行(190)全程未动，**零「记忆写入失败」弹窗、零 `DuplicateKeyException`**——即原 bug 场景（global 有 child_name + 切项目再加同 key）旧版必崩，现修好。LLM 经 key 归一复用既有 `child_name`（不造变体），`writeTarget=project_id` 入 `applyClean` → `findCleanByHomeKey` 查同 home 空 → INSERT 新 home 行。

## 冲突链两个关键 bug 修复（V35）

用户实测报：「我说女儿叫小红，再说叫小美，记忆直接被改了，然后才出来 pending，选保留新却提示『冲突不存在』」。playwright-mcp 复现 + DB 验证，拆出两个独立 bug，都修。

### Bug 1：跨块同 key 静默覆盖（"记忆直接被修改了"）
- **根因**：[MemoryService.processFacts](../../backend/src/main/java/com/superprogrammer/chat/service/MemoryService.java) 分流只看**块成员**（`getCleanByBlock` block-scoped）。新事实归块 A，但同 key 既有 clean 记忆在**别的块** → 块 A 成员空 → 走 `immediateInserts` → `applyClean` 用 `findSameKeyClean`（home-scoped，**跨块**）找到别块同类行 → 当"细化" `updateCleanMemory` 原地 UPDATE 覆盖，**绕过冲突判定**（不建 PENDING、不问用户）。
- **复现**：admin 全局 `child_name=啊闪、啊然`（**基本信息**块）→ 发「女儿叫小美」（LLM 归**家庭信息**块）→ `啊闪、啊然` 被静默改成小美，零冲突。
- **修**：分流候选集改为 **块成员 ∪ 跨块同 key 记忆**（`findCleanByHomeKey`，home-scoped）：②二者皆空才 `immediateInserts`（真·新块直插）；③batch judge 的 comparison set 也并入跨块同 key 行（让 LLM 看到别块同类记忆，否则漏判）；conflictingIds 空时回退**该 fact 的跨块同 key 记忆**（不再误把整块成员当冲突对象）。
- **不影响**：同块同 key（既有正常路径）仍走 judge；真·新 key（无同 key 无块成员）仍直插。

### Bug 2：保留新→冲突不存在（PENDING resolve 静默回滚）
- **根因**：[MemoryConflictService.buildFromSnap](../../backend/src/main/java/com/superprogrammer/chat/service/MemoryConflictService.java)（PENDING 路径 `insertSnap` + `flag()` + `mergePendingKeepBoth` 兜底都用）**没设 `homeProjectId`/`is_global`**——V34 home 语义改 `createFlagged` 时漏改它。→ 新行落默认 `home=null`(global)、`is_global=true`、不挂项目 → 若同 user 同 key 已有 global clean 行（V34 唯一索引 `uk_user_memories_user_key_home` = `(user,key,COALESCE(home,-1)) WHERE conflict_id IS NULL`）→ `DuplicateKeyException` → `@Transactional` **整体回滚**（`hardDelete(oldIds)` 也回滚）→ `resolve` 返 false → controller 文案「冲突不存在或无权操作」。
- **复现**：项目「测试」(home=3) 冲突 `child_name` 小红→小蓝，全局另有 `child_name=小绿`(home=null)；点保留新 → `insertSnap` 落小蓝 home=null → 撞小绿 → 回滚 → conflict 仍 PENDING、253 未删、提示"冲突不存在"。
- **修**：`buildFromSnap` 加 `homeProjectId`+`isGlobal` 参数；新 `insertSnapScoped(...home, isGlobal, projectIds)` 落新行后挂 `user_memory_projects`；三个调用点全部从 **old 行继承 scope**——`resolve` PENDING 在 `hardDelete` **前**读 old 行（删完查不到）取首行的 `home_project_id`/`is_global`/`findProjectIdsByMemory`；`flag()` 同；`mergePendingKeepBoth` 兜底同。新行 = 替换 old → 理应同 scope（同 home、同可见性），不撞唯一约束。
- **FLAGGED 路径不受影响**：`createFlagged`（V34 已正确设 home+is_global+挂项目）插的新行 scope 正确，FLAGGED 的 KEEP_NEW 只 `hardDelete(old)+setConflictId(new,null)`，不走 `buildFromSnap`。
- **playwright 验证（修后）**：同一项目「测试」冲突点保留新 → 旧小红删、新小蓝落 **home=3**/is_global=f/挂 project 3，零撞库，conflict RESOLVED KEEP_NEW，"已解决"无"冲突不存在"。

### 单测
`MemoryServiceTest` 21/21 + `ChatSessionServiceTest` 14/14 过（共 35/35，零回归）。processFacts 候选集改动不破既有同块路径。冲突 resolve/flag 的真 PG 行为（V34 home 唯一索引 + COALESCE）mock 单测仍跑不出——复现须打 streaming + 真 PG + 跨 home/scope 数据，照 [reference_mybatis_script_halfvec_escape](../../../../C:/Users/Administrator/.claude/projects/e--workspace/memory/reference_mybatis_script_halfvec_escape.md) 范式。

## 我自己标注需注意的
1. ~~现在LLM全量加载记忆的方式，是直接把user_memories中本用户的所有的key、value、category~~ → 已由 V33 两阶段 + 标签语言设置收口（见上）。

## 待增修改功能

> 已完成项保留原 ✅ 编号备查；原未完成 11 项（原 4/9/10/14/15/16/17/18/19/20/21）已按相关性合并为 **M1–M5**（见本节末「待办（合并后）」）。**M1 已落地**（原 4/9/18），剩 M2–M5 待办。原编号行改为 ⏳/✅ 重定向条目溯源。内部交叉引用「待增修改 6/8」（已完成项）编号不变。

1. ✅ chat窗口可批量删除 — panel 复选框 + 「删除选中」→ `DELETE /chat/memories/batch`；selection 列补 `width:45` 修 scroll-x 塌缩。
2. ✅ 记忆开关持久化 + 会话/全局联动 — 全局开关移除 `if(!enabled) return` guard（OFF 也入库）；会话开关改三态（null=继承全局），新增用户级只读端点 `GET /chat/memories/rag-mode` 供联动显示。修旧 bug：会话默认发 `false` 盖掉全局 ON。
3. ✅ 记忆中文标签 memory_key_zh + 召回词袋变体 + 面板时间列 — V32 迁移加 `memory_key_zh` 列；extract LLM 顺带抽中文标签 + 召回词袋(含同义变体，角色词必含)；`findByKeyword` 加搜 `memory_key_zh`（三列并查）；前端 panel 加「名称」(key_zh)+「创建」(createdAt) 列。
4. ✅ 微信相对样式时间 → 并入 **M1**（时间展示子项）。原：未做 → M1 已落地。
5. ✅ 标签语言设置 + 全量超阈值两阶段 — 新增 `rag.memory.key-language`(EN/ZH，控注入展示用中/英 key) + `rag.memory.full-context-threshold`(默认20，全量模式记忆>阈值改「先key→LLM选相关→再装value」两阶段，复用 selectRelevantKeys)；设置走 KV 表无迁移；[MemoryService.buildFullContext(userId,query)](../../backend/src/main/java/com/superprogrammer/chat/service/MemoryService.java) 签名加 query。见本文「标签语言 + 全量超阈值两阶段（V33）」。
6. ✅ KEEP_BOTH 合并成一条 — 「合并保留」不再双行共存。PENDING：同 key old 行 value 追加 new（去重 join `，`），survivor 清 `conflict_id` 变 clean，删多余同 key 行；FLAGGED：全组值合入 survivor（原 clean 行），删组内其余。merged value 重 embed（失败 COALESCE 保旧向量）。[MemoryConflictService.mergePendingKeepBoth/mergeFlaggedKeepBoth](../../backend/src/main/java/com/superprogrammer/chat/service/MemoryConflictService.java) + [UserMemoryMapper.mergeIntoRow](../../backend/src/main/java/com/superprogrammer/chat/mapper/UserMemoryMapper.java)；前端按钮「都保留」→「合并保留」（[MemoryManagerPanel.vue](../../frontend/src/components/chat/MemoryManagerPanel.vue)）。decision 串 `KEEP_BOTH` 不变（免迁移）。
   - **flagged 隐身陷阱**：抽取去重/冲突判定只看 `conflict_id IS NULL` 行（[MemoryService.classifyFact](../../backend/src/main/java/com/superprogrammer/chat/service/MemoryService.java) `isNull(conflictId)`）。旧 KEEP_BOTH 留下的带标行（conflict 已 RESOLVED 但 `conflict_id` 未清）对抽取**隐身** → 同值被当新事实反复落库（用户实测 3 行 name = 2 旧带标 + 1 新 clean）。
   - **历史脏数据清理**（修上述残留）：admin 在 [RagMemorySettingsTab.vue](../../frontend/src/components/settings/RagMemorySettingsTab.vue) 「记忆冲突残留清理」→ `POST /api/system/settings/rag-memory/cleanup-memory-residue`（[SystemSettingController](../../backend/src/main/java/com/superprogrammer/system/controller/SystemSettingController.java)，`role:manage`）→ [MemoryConflictService.cleanupResolvedResidue](../../backend/src/main/java/com/superprogrammer/chat/service/MemoryConflictService.java) 异步跑（fire-and-forget 到 `memoryTaskExecutor`）：找 `conflict_id` 非空且其 conflict 已 RESOLVED 的行（[findResolvedFlaggedRows](../../backend/src/main/java/com/superprogrammer/chat/mapper/UserMemoryMapper.java)），按 `(user_id, memory_key)` 分组合并成一条 clean（复用 merge helpers，单行仅清 `conflict_id`），重 embed。幂等（再跑无残留）；进度日志 `memoryCleanup`。
7. ✅ 记忆处理状态条（顶部低调标签）— [ChatView.vue](../../frontend/src/views/ChatView.vue) header 下方 strip：抽取进行中显「记忆记录中…」（muted + spin），有冲突显「N 个记忆产生冲突，点击处理」（warn 色，点击开抽屉）。任一 >0 才显，冲突归零消失。后端 per-user in-flight 计数（[MemoryService.processMemory](../../backend/src/main/java/com/superprogrammer/chat/service/MemoryService.java) 入 inc/finally dec）+ 冲突计数（[MemoryConflictMapper.countActiveByUser](../../backend/src/main/java/com/superprogrammer/chat/mapper/MemoryConflictMapper.java)）经 `GET /api/chat/memories/status`（[MemoryStatusVO](../../backend/src/main/java/com/superprogrammer/chat/dto/MemoryStatusVO.java)）返回；前端 3s 轮询改走 status（[chat store](../../frontend/src/stores/chat.ts) `memoryProcessing` + `activeConflictCount`，省去每轮拉全量冲突 list）。
8. ✅ 加入项目概念（总记忆 + 单独项目记忆，可关总记忆/加多项目）— V33 项目记忆 scope（**纯可见性标签模型，已被 V34 混合模型取代，见下**）。Project 全新实体 + `user_memories.is_global` + `user_memory_projects` 多对多可见性；读=扁平对称开关集（`mem_include_global` + `mem_read_project_ids[]`），写=`session.project_id`（null=global）；一条记忆可同属 global+多项目。老数据 is_global 默认 true = 今天。详见本文「项目记忆 scope（V33，待增修改 8）」节。IT 抓出并修 2 个生产回归（向量 `<=>` XML 转义 + persistMemoryScope BIGINT[] typeHandler）。**V33 遗留 bug**：唯一索引 scope-orthogonal 但 dedup scope-filtered → 写目标切项目再添同 key 撞墙，详见 V34 节。
11. ✅ V34 home+可见性混合模型（取代 V33 纯可见性，修上述撞墙 bug + 支持每项目独立 key + 跨项目共享并存）— 加 `home_project_id` 列作写归属/唯一性槽，换唯一索引为 `(user_id,memory_key,COALESCE(home_project_id,-1)) WHERE conflict_id IS NULL`；dedup 改 `findCleanByHomeKey`（home-aware）；读路径/可见性标签/共享端点全不动（读仍按开关全注入）。IT 5/5（[UserMemoryMapperHomeScopeIT](../../backend/src/test/java/com/superprogrammer/chat/mapper/UserMemoryMapperHomeScopeIT.java)）+ 浏览器冒烟 3-home 共存全过；IT 抓出迁移坑（user_memories 无 `deleted` 列，回填 UPDATE 不能带 `m.deleted=0`）已修。详见本文「项目记忆 home + 可见性混合（V34，取代 V33 纯可见性标签）」节。
12. ✅ ProjectSelector 下拉打开 reload 项目列表 — 修「项目管理 modal 新建项目后写目标下拉不刷新」（[ProjectSelector.vue](../../frontend/src/components/chat/ProjectSelector.vue) 内部 list 仅 onMounted 拉一次，与 ChatView 的 projectOptions 各自独立，modal @changed 只刷新 ChatView list）。加 `@update:show` → 开下拉即 `projectApi.list()` reload，自洽不依赖外部传 prop。
9. ✅ 面板行内编辑 + 字段筛选 → 并入 **M1**（编辑/筛选子项）。M1 已落地。
10. ⏳ 时间线记忆（创建/修改展示 + KEEP_BOTH 带日期） → 并入 **M2**（机制侧，与原 16 流水账一起设计）。
13. ✅（V38 已落地）标签语言加 `BOTH` 中英双选 + 超阈值两阶段改「三维合并筛选」（memory_key + key_zh + block_label 各剔重 → **一次 LLM** 同时选 keys/keys_zh/blocks → 三维 AND 交集 → 注入只留 key+value，丢 category/block）。三路径共用同一 `filterRelevantKeys` 改造：LLM_FULL_CONTEXT 超阈值、VECTOR_KEYWORD 0命中兜底、LLM_KEY 精排。演进：单维 key → 双维度 2 次 LLM → 三维 1 次 LLM（砍第二次调用 + key_zh 进选择）。详见本文「三维合并筛选（key × key_zh × block_label，一次 LLM，V38）」节 + [09-...-优化升级开发落地](09-个人记忆与冲突解决-优化升级开发落地.md)。
14. ⏳ RAG 设置按检索模式动态显隐 → 并入 **M3**（与原 19 entities 计数同改设置 tab）。
15. ⏳ 读侧 gate：agent/工作流提示词不触发记忆读取 → 并入 **M5**。
16. ⏳ 流水账记忆设计 → 并入 **M2**（时间线用例侧）。
17. ⏳ 写侧 gate：回答不入记忆除非主动要求 → 并入 **M5**。
18. ✅ 共享项目在 panel 看不到 → 并入 **M1**（归属列显示子项）。M1 已落地。
19. ⏳ entities 总数/变体/专名/上位词计数用户可配 → 并入 **M3**。
20. ⏳ 预览指定项目时总记忆仍注入 → 并入 **M4**（scope 语义澄清）。
21. ⏳ 对话底栏写/读选择器语义不清 → 并入 **M4**。

### 待办（合并后）

**M1 — 记忆面板：行内编辑 + 字段筛选 + 归属列显示共享项目 + 时间相对格式**（合并原 9 + 18 + 4）✅ 已落地
- 合并理由：三件都落在 [MemoryManagerPanel.vue](../../frontend/src/components/chat/MemoryManagerPanel.vue)，一次改收口，零跨模块。
- **已落地内容**（前后端 mvn compile / vue-tsc 双绿）：
  - **编辑端点** `PUT /api/chat/memories/{id}`（[MemoryController](../../backend/src/main/java/com/superprogrammer/chat/controller/MemoryController.java) → [MemoryService.updateMemory](../../backend/src/main/java/com/superprogrammer/chat/service/MemoryService.java) → [UserMemoryMapper.updateMemoryEdit](../../backend/src/main/java/com/superprogrammer/chat/mapper/UserMemoryMapper.java)）：改 key/key_zh/value/block_label，**home-aware 重复检查**（`findCleanByHomeKey` 排除自身，撞 `(user,key,home)` 返 `CONFLICT` 业务错误不静默），**条件重 embed**（value 变→重算 value 向量；key/key_zh/block 变→重算 anchor；entities 不动沿用旧词袋），embed 失败 COALESCE 保旧向量。`updated_at` 由 mapper `now()` 刷新（自定义 @Update 不走 MetaObjectHandler）。DTO [MemoryEditRequest](../../backend/src/main/java/com/superprogrammer/chat/dto/MemoryEditRequest.java)。
  - **编辑 modal**：[MemoryManagerPanel.vue](../../frontend/src/components/chat/MemoryManagerPanel.vue) 操作列加「编辑」→ 4 字段（key/名称/值/信息块，block 可选可输）→ `chatApi.updateMemory`；[chat.ts](../../frontend/src/api/chat.ts) 加 `MemoryEditRequest` 类型。
  - **字段筛选**：表上方加关键词输入（命中 key/key_zh/value）+ 信息块下拉（distinct blockLabel），纯前端 `filteredMemories` computed 筛；显「N/总 条」计数 + 筛选空态。
  - **归属列 home/共享区分**：[UserMemoryVO](../../backend/src/main/java/com/superprogrammer/chat/dto/UserMemoryVO.java) 透出 `homeProjectId`（`toVO` 装配），归属列改渲染「{home}（归属）」info tag + 「全局可见」+ 「{pid}（共享）」可见性 tag（`projectIds` 排除 home），整列 NTooltip 说明「归属=存于哪个 scope（key 唯一性）/ 共享=可被哪些项目读取」。原 18 诉求经核实 = `projectIds` 已含共享项目，实为认知问题 → 加 home 区分收口。
  - **时间相对格式**：抽 [utils/time.ts](../../frontend/src/utils/time.ts) `formatRelativeTime`（刚刚/N分钟前/N小时前/昨天/前天/MM-DD/YYYY-MM-DD）+ `formatAbsoluteTime`；创建/更新列文本相对、hover NTooltip 显绝对时间。
- 子项：
  - **编辑**（原 9）：panel 行操作加「编辑」→ modal 改 `memory_key_zh / memory_key / memory_value / block_label`，保存走 `PUT /api/chat/memories/{id}`（新增端点），后端 `updated_at` 由 MetaObjectHandler 自动刷新（不手填）。改 `memory_key`/`block_label` 须同步重 embed（value 不变也重算，因 dedup/召回依赖 key+block）。**改 key 风险**：触发 `(user_id,memory_key,home)` 唯一约束 / 与跨块同 key 记忆新冲突 → 编辑端点须 home-aware 重复检查，撞约束返业务错误不静默。
  - **筛选**（原 9 尾句）：表头加按字段过滤（key/key_zh/block_label/value 关键词 + 创建时间范围），复用 n-data-table filter，前端纯筛或加 `GET /chat/memories?keyword=&block=` 后端 LIKE（数据量小倾向前端筛）。
  - **归属列显示共享项目**（原 18）：「归属」列已有（V33 `findProjectIdsByMemories` 批量渲染 global tag + project tags）。原 18 诉求 = 共享给指定项目的记忆，panel 看不到加进了哪个项目——**先核实**：`projectIds`（可见性标签集）是否已含共享项目？若已渲染则原 18 实为「认知/文案问题」→ 在归属 tag 加 tooltip「可见于：项目X（共享）/ 项目Y（归属）」区分 home vs 可见性；若 `projectIds` 漏了共享项则补渲染。
  - **时间相对格式**（原 4）：现有 `formatTime` 绝对时间，加微信相对样式（「刚刚/N分钟前/昨天/前天/MM-DD/YYYY」），hover tooltip 显绝对时间。与 M2 时间线展示协同（M1 管格式，M2 管时序内容）。

**M2 — 时间线记忆：KEEP_BOTH 带日期 + 流水账场景建模**（合并原 10 + 16）
- 合并理由：原 10 是**机制**（冲突合并时 value 带日期），原 16 是**用例**（流水账「今天X明天Y」= 10 的现成解）。一起设计 value schema 避二次返工。
- 设计要点：
  - **value schema**：时序事实（住址/工作/状态类）value 改存结构化「日期+内容」段，建议 `value` 存 `2026-06-25 住萧山；2027-01-01 住拱墅`（分号分段 + 行首 ISO 日期），展示层 parse 回时间线列表。非时序事实（名字/偏好）不套日期，schema 兼容「无日期=单值」。
  - **KEEP_BOTH 走时间线**（原 10）：冲突 merge 时 new value 带**其 updated_at（= 当次写入时间）** 前缀，old value 保原 updated_at 前缀，按日期排序拼接 → 时间线。需 LLM 或规则判「该事实是否时序」（住址=是，孩子数量=否），时序才带日期，否则维持现有「去重 join `，`」。复用待增修改 6 的 `mergePendingKeepBoth/mergeFlaggedKeepBoth`。
  - **替换走原路径**（原 10 尾句）：KEEP_NEW 直接覆 value + 刷 updated_at，不带时间线（仅最新值）。
  - **流水账**（原 16）：日记型记忆（「今天做了X」）天然时序，归同 key（如 `daily_log` 或按日期 key）+ 时间线 value；召回时按 query 提到的日期/相对词（今天/昨天/本周）筛段。
  - **存量兼容**：老 value 无日期前缀 → 视作单值/无时间线，不破坏。建议不回填老行，只新数据生效。
  - panel 时间线展示（原 10 首句「展示创建/修改时间」）：列表加「创建」「修改」两列（修改=updated_at），时序 value 展开成多段日期行。

**M3 — RAG 记忆设置：按检索模式动态显隐 + entities 计数用户可配**（合并原 14 + 19）
- 合并理由：都改 [RagMemorySettingsTab.vue](../../frontend/src/components/settings/RagMemorySettingsTab.vue) + 后端设置 KV + extract prompt，一波前后端。
- 子项：
  - **动态显隐**（原 14）：检索模式 select 的 `@update:value` 驱动 `v-if` 显隐无关区块。映射：`LLM_FULL_CONTEXT` → 显「全量阈值」「标签语言」；`EMBEDDING_VECTOR` → 无全量阈值/关键词阈值（纯向量）；`VECTOR_KEYWORD` → 显「关键词召回块阈值」、隐全量阈值；`LLM_KEY` → 显 LLM_KEY 旋钮组（coarse-top-n/rerank/keyword-max）。全量阈值仅 LLM_FULL_CONTEXT 有效，其余模式隐藏防误配。
  - **entities 计数可配**（原 19）：新增设置组 `rag.memory.entities-config` = `{totalMax, variantMin/Max, properNounMin/Max, hypernymMin/Max}`（默认沿用 V38 硬上限：总≤20、上位词≤10）。设置读入 [MemoryConflictJudge.extract](../../backend/src/main/java/com/superprogrammer/chat/service/internal/MemoryConflictJudge.java) 动态拼 prompt（现 prompt 硬编码「共≤20/上位词≤10」改读设置）；Java 硬切 `readEntities` 截断改读 `totalMax`。前端 tab 加旋钮组（仅 LLM_KEY/VECTOR_KEYWORD 模式有意义，动态显隐呼应上一项）。变设置后老数据吃新配靠「重抽关键词」端点（既有）。

**M4 — scope 语义澄清：预览总记忆开关 + 对话底栏写/读选择器区分**（合并原 20 + 21）
- 合并理由：根因同——用户不懂「写目标(home)」vs「读范围(可见性开关)」分离。统一加文案 + 可视区分，预览/对话两入口一起修。
- 子项：
  - **预览总记忆开关**（原 20）：[MemoryManagerPanel.vue](../../frontend/src/components/chat/MemoryManagerPanel.vue) 预览卡「预览范围」选「指定项目」时，当前总记忆仍注入。修：scope 选择器显式含「包含总记忆」开关（`memIncludeGlobal`），选指定项目默认关总记忆或显式提示「当前含总记忆 N 条」。后端 preview 已支持 scope（[MemoryScopeResolver](../../backend/src/main/java/com/superprogrammer/chat/service/MemoryScopeResolver.java)），纯前端 scope payload 补 `memIncludeGlobal=false`。
  - **对话底栏区分**（原 21）：[ChatView.vue](../../frontend/src/views/ChatView.vue) tools 区两个项目选择器语义不清。改：① 写目标单选器改名「记忆落库于」（= 新事实存哪，单一）；② 读范围多选器改名「读取记忆范围」（= 召回哪些项目记忆，多选 + 独立「含总记忆」switch）。加分组标题/分隔线/tooltip 说明「落库」vs「读取」差异。可合并为一个「记忆范围」面板（落库单选 + 读取多选分区）降认知负担。

**M5 — 记忆触发边界收紧：读侧 agent/工作流不注入 + 写侧回答不抽取**（合并原 15 + 17）
- 合并理由：都属「何时碰记忆」边界收紧，机制独立（read gate vs write gate）但同一遍对话主链路梳理。动主链路谨慎，需单测 + IT 覆盖。
- 子项：
  - **读侧 gate**（原 15）：[MemoryService.buildMemoryContext](../../backend/src/main/java/com/superprogrammer/chat/service/MemoryService.java) 注入须仅针对**用户 chat 输入**消息。agent/工作流路径（[AgentRoutingStrategy](../../backend/src/main/java/com/superprogrammer/engine/strategy/AgentRoutingStrategy.java) 等引擎侧）及系统提示词、agent/工作流自带提示词**不触发记忆读取**。实现：记忆注入入口判来源（message source / 是否经 agent 路由），非用户直发跳过注入。需定「触发点」语义：按消息 role=USER 且非 agent 代理转发判。
  - **写侧 gate**（原 17）：抽取只针对用户输入内容，**回答/LLM 生成内容不入记忆**，除非用户显式要求（如「记一下…」）。现状确认：extract 触发源是用户消息还是整轮对话？若含 assistant 回复需收口为仅 user message。用户主动要求 = 检测 user 消息含「记/记住/更新记忆」等意图，或显式「保存为记忆」按钮。
  - 交叉：M5 与 M2/M3 无依赖可独立推进；但改触发判定影响所有记忆模式（LLM_KEY/VECTOR/全量），回归面广。

### 合并优先级建议
- **M1 + M4**：纯前端、用户痛点明确，先做。
- **M2**：需设计决策（value schema 影响存量数据 + 时序判定），中优先。
- **M3**：前端 tab + extract prompt，中优先。
- **M5**：动对话主链路触发判定，回归面广，最后做并配足测试。


### 新增
22. 在pending的时候，给保留新/旧/合并的时候，还需要给一个自定义选项（自己填，input框中默认带上旧记忆）
23. user、memory、soul、agent等必加入的文件需要让用户编辑
24. 每日记账（如今天对话每一句提交了什么，得到了什么答案写入到YYYY-MM-DD hh:mm:ss），可以配置是否自动读取前N天
25. 针对一些用户说的，总结是否可以总结成技能供后续重复使用的，像记忆一样，生成会自动读取的skill（可主动删除，编辑，是否可读，是否共享等）
26. 现在有哥问题，就是记忆冲突保留新记忆后，其他的标签好像会丢失，比如memory_key_zh
27. 记忆value的设计，是否可以设计为json格式（比如表哥相关的都在她的json中{工作:...,年龄:...}）
28. 客户协同办公中，比如文档有地方要修改的，不可能会在记忆中说一句，请帮我把A修改B，或者A是不对的，需要修改B，然后等你半天去修改记忆，是否有什么更好的方式可以结合