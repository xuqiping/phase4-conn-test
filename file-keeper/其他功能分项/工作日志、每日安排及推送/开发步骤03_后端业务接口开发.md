# 开发步骤 03：后端业务接口开发

> 本步骤完成后端核心业务接口，包括工作记录、每日安排、报告配置、报告实例的 CRUD，以及管理后台接口。

> **状态：✅ 已完成**（2026-06-21）

---

## 1. 目标

- 实现工作记录、每日安排、报告配置、报告实例的完整 CRUD
- 所有客户端接口必须校验 `work-report` 模块授权
- 实现管理后台模板管理接口
- 完成单元测试

---

## 2. 前置依赖

- [`开发步骤02_数据库设计与Flyway迁移.md`](开发步骤02_数据库设计与Flyway迁移.md) 已完成
- 实体类、Mapper 已就绪

---

## 3. 涉及文件

| 文件 | 路径 |
|---|---|
| `WorkReportClientController.java` | `file-keeper/server/src/main/java/com/superprogrammer/workreport/controller/WorkReportClientController.java` |
| `AdminWorkReportController.java` | `file-keeper/server/src/main/java/com/superprogrammer/workreport/controller/admin/AdminWorkReportController.java` |
| Service 层 | `file-keeper/server/src/main/java/com/superprogrammer/workreport/service/*.java` |
| DTO/Request | `file-keeper/server/src/main/java/com/superprogrammer/workreport/dto/*.java` |
| 单元测试 | `file-keeper/server/src/test/java/com/superprogrammer/workreport/*.java` |

---

## 4. 详细任务

### 4.1 DTO 设计

创建以下 DTO：

```java
// WorkLogDto.java
public record WorkLogDto(
    Long id,
    LocalDate logDate,
    String content,
    String tags,
    String source,
    Integer sortOrder,
    LocalDateTime createdAt,
    LocalDateTime updatedAt
) {}

// WorkPlanDto.java
public record WorkPlanDto(
    Long id,
    LocalDate planDate,
    String content,
    String priority,
    Boolean completed,
    Integer sortOrder
) {}

// ReportTemplateDto.java
public record ReportTemplateDto(
    Long id,
    String name,
    String type,
    String content,
    Boolean isDefault
) {}

// ReportConfigDto.java
public record ReportConfigDto(
    Long id,
    String name,
    String reportType,
    Long templateId,
    String templateName,
    String cronExpression,
    String timezone,
    Boolean enabled,
    Boolean aiEnabled,
    List<ReportPushTargetDto> pushTargets
) {}

// ReportPushTargetDto.java
public record ReportPushTargetDto(
    Long id,
    String platform,
    String targetType,
    String targetId,
    // credential 不返回明文
    Boolean hasCredential
) {}

// WorkReportDto.java
public record WorkReportDto(
    Long id,
    String reportType,
    String title,
    String content,
    LocalDateTime generatedAt,
    String status
) {}
```

### 4.2 Service 层实现

创建以下 Service：

| Service | 核心方法 |
|---|---|
| `WorkLogService` | `listByUserAndDate`, `create`, `update`, `delete` |
| `WorkPlanService` | `listByUserAndDate`, `createOrUpdate`, `toggleComplete`, `delete` |
| `ReportTemplateService` | `listDefaults`, `listByUser`, `create`, `update`, `delete` |
| `ReportConfigService` | `listByUser`, `create`, `update`, `delete`, `getById` |
| `WorkReportService` | `listByUser`, `getById`, `delete` |

> 注意：`WorkReportService.generate()` 在 Step 4 中实现，本步骤可先留接口或简单拼接实现。

### 4.3 客户端 Controller

实现 [`WorkReportClientController.java`](../../server/src/main/java/com/superprogrammer/workreport/controller/WorkReportClientController.java)：

```java
@RestController
@RequestMapping("/api/client/work-report")
@RequiredArgsConstructor
public class WorkReportClientController {

    private final AuthorizationService authorizationService;
    private final WorkLogService workLogService;
    private final WorkPlanService workPlanService;
    private final ReportTemplateService reportTemplateService;
    private final ReportConfigService reportConfigService;
    private final WorkReportService workReportService;

    private boolean checkModuleAuth(Authentication auth, String deviceId) {
        AuthPrincipal principal = (AuthPrincipal) auth.getPrincipal();
        AuthorizationSnapshot snapshot = authorizationService.authenticatedSnapshot(
            principal.userId(), deviceId, System.currentTimeMillis()
        );
        return snapshot.modules().stream()
            .anyMatch(m -> AuthConstants.MODULE_WORK_REPORT.equals(m.moduleCode()) && m.allowed());
    }

    // ==================== 工作记录 ====================
    @GetMapping("/logs")
    public R<List<WorkLogDto>> listLogs(
            Authentication auth,
            @RequestParam String deviceId,
            @RequestParam @DateTimeFormat(iso = DateTimeFormat.ISO.DATE) LocalDate date) {
        if (!checkModuleAuth(auth, deviceId)) {
            return R.fail(ErrorCode.FORBIDDEN, "未授权访问工作汇报模块");
        }
        AuthPrincipal principal = (AuthPrincipal) auth.getPrincipal();
        return R.ok(workLogService.listByUserAndDate(principal.userId(), date));
    }

    @PostMapping("/logs")
    public R<WorkLogDto> createLog(
            Authentication auth,
            @RequestParam String deviceId,
            @RequestBody @Valid CreateWorkLogRequest request) {
        if (!checkModuleAuth(auth, deviceId)) {
            return R.fail(ErrorCode.FORBIDDEN, "未授权访问工作汇报模块");
        }
        AuthPrincipal principal = (AuthPrincipal) auth.getPrincipal();
        return R.ok(workLogService.create(principal.userId(), request));
    }

    // ... PUT /logs/{id}, DELETE /logs/{id}

    // ==================== 每日安排 ====================
    @GetMapping("/plans")
    public R<List<WorkPlanDto>> listPlans(
            Authentication auth,
            @RequestParam String deviceId,
            @RequestParam @DateTimeFormat(iso = DateTimeFormat.ISO.DATE) LocalDate date) {
        if (!checkModuleAuth(auth, deviceId)) {
            return R.fail(ErrorCode.FORBIDDEN, "未授权访问工作汇报模块");
        }
        AuthPrincipal principal = (AuthPrincipal) auth.getPrincipal();
        return R.ok(workPlanService.listByUserAndDate(principal.userId(), date));
    }

    // ... POST /plans, PUT /plans/{id}, DELETE /plans/{id}

    // ==================== 报告模板 ====================
    @GetMapping("/templates")
    public R<List<ReportTemplateDto>> listTemplates(
            Authentication auth,
            @RequestParam String deviceId) {
        if (!checkModuleAuth(auth, deviceId)) {
            return R.fail(ErrorCode.FORBIDDEN, "未授权访问工作汇报模块");
        }
        AuthPrincipal principal = (AuthPrincipal) auth.getPrincipal();
        return R.ok(reportTemplateService.listAvailable(principal.userId()));
    }

    // ==================== 报告配置 ====================
    @GetMapping("/configs")
    public R<List<ReportConfigDto>> listConfigs(
            Authentication auth,
            @RequestParam String deviceId) {
        if (!checkModuleAuth(auth, deviceId)) {
            return R.fail(ErrorCode.FORBIDDEN, "未授权访问工作汇报模块");
        }
        AuthPrincipal principal = (AuthPrincipal) auth.getPrincipal();
        return R.ok(reportConfigService.listByUser(principal.userId()));
    }

    @PostMapping("/configs")
    public R<ReportConfigDto> saveConfig(
            Authentication auth,
            @RequestParam String deviceId,
            @RequestBody @Valid SaveReportConfigRequest request) {
        if (!checkModuleAuth(auth, deviceId)) {
            return R.fail(ErrorCode.FORBIDDEN, "未授权访问工作汇报模块");
        }
        AuthPrincipal principal = (AuthPrincipal) auth.getPrincipal();
        return R.ok(reportConfigService.save(principal.userId(), request));
    }

    @DeleteMapping("/configs/{id}")
    public R<Void> deleteConfig(
            Authentication auth,
            @RequestParam String deviceId,
            @PathVariable Long id) {
        if (!checkModuleAuth(auth, deviceId)) {
            return R.fail(ErrorCode.FORBIDDEN, "未授权访问工作汇报模块");
        }
        AuthPrincipal principal = (AuthPrincipal) auth.getPrincipal();
        reportConfigService.delete(principal.userId(), id);
        return R.ok();
    }

    // ==================== 报告实例 ====================
    @GetMapping("/reports")
    public R<PageResult<WorkReportDto>> listReports(
            Authentication auth,
            @RequestParam String deviceId,
            @RequestParam(defaultValue = "1") int page,
            @RequestParam(defaultValue = "20") int size) {
        if (!checkModuleAuth(auth, deviceId)) {
            return R.fail(ErrorCode.FORBIDDEN, "未授权访问工作汇报模块");
        }
        AuthPrincipal principal = (AuthPrincipal) auth.getPrincipal();
        return R.ok(workReportService.pageByUser(principal.userId(), page, size));
    }
}
```

### 4.4 管理后台 Controller

实现 [`AdminWorkReportController.java`](../../server/src/main/java/com/superprogrammer/workreport/controller/admin/AdminWorkReportController.java)：

```java
@RestController
@RequestMapping("/api/admin/work-report")
@RequiredArgsConstructor
public class AdminWorkReportController {

    private final ReportTemplateService reportTemplateService;
    private final AdminAuditLogService adminAuditLogService;

    @GetMapping("/templates")
    @RequirePermission("work_report:manage")
    public R<List<ReportTemplateDto>> listSystemTemplates() {
        return R.ok(reportTemplateService.listSystemTemplates());
    }

    @PostMapping("/templates")
    @RequirePermission("work_report:manage")
    public R<ReportTemplateDto> createSystemTemplate(
            @RequestBody @Valid SaveReportTemplateRequest request,
            @AuthenticationPrincipal AdminPrincipal principal) {
        adminAuditLogService.log(principal.getUserId(), "CREATE_SYSTEM_REPORT_TEMPLATE", request.name());
        return R.ok(reportTemplateService.createSystemTemplate(request));
    }

    // ... PUT /templates/{id}, DELETE /templates/{id}
}
```

### 4.5 单元测试

必须编写以下测试：

- `WorkReportClientControllerTest`
  - 未授权访问返回 403
  - 授权后工作记录 CRUD 正常
  - 授权后报告配置 CRUD 正常
  - 用户只能访问自己的数据

- `ReportConfigServiceTest`
  - 保存配置时同步保存推送目标
  - 删除配置时逻辑删除
  - cron 表达式校验

- `WorkLogServiceTest`
  - 按日期查询
  - 创建/更新/删除

---

## 5. 验收标准

- [x] 所有客户端接口都有模块授权校验
- [x] 未授权用户调用任何接口返回 403
- [x] 工作记录 CRUD 正常
- [x] 每日安排 CRUD 正常
- [x] 报告配置可保存/查询/删除
- [x] 报告模板列表可查询
- [x] 历史报告分页查询正常
- [x] 管理后台模板接口受角色权限保护（项目使用 `/api/admin/**` 的 `SUPER_ADMIN` 角色限制，`@RequirePermission` 注解机制未在项目中实现）
- [x] 高风险操作记录审计日志
- [ ] 单元测试覆盖率 > 60%（当前已覆盖核心路径，未统计精确覆盖率）

> 验证结果：`WorkReportClientControllerTest` 7 个、`WorkLogServiceTest` 4 个、`ReportConfigServiceTest` 5 个，共 16 个测试全部通过。

---

## 6. 验证命令

```bash
# 后端测试
mvn -f "file-keeper/server/pom.xml" test -Dtest=WorkReportClientControllerTest,ReportConfigServiceTest,WorkLogServiceTest

# 全量测试
mvn -f "file-keeper/server/pom.xml" test
```

---

## 7. 预计工时

**5 天**

---

## 8. 风险与注意事项

| 风险 | 说明 |
|---|---|
| 授权校验代码重复 | 每个接口都写 `checkModuleAuth` 很冗余，建议抽成 AOP 或方法级注解，但需与项目现有风格保持一致 |
| 用户数据隔离 | 所有查询必须带 `user_id` 条件，防止越权 |
| 推送目标凭据返回 | DTO 中不要返回 `credential` 明文，只返回 `hasCredential` |
| cron 表达式校验 | 使用 `CronExpression` 工具类校验格式 |

---

## 9. 下一Step

完成本步骤后，可并行执行：
- [`开发步骤04_AI总结服务与定时调度.md`](开发步骤04_AI总结服务与定时调度.md)
- [`开发步骤06_桌面端前端开发.md`](开发步骤06_桌面端前端开发.md)

建议先完成 Step 4，再开始 Step 5。

---

*文档版本：v1.0*  
*编写日期：2026-06-21*
