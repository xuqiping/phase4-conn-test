# 自助充值支付与账单总览 开发进度2（2026-08-21，支付渠道网页配置）

> 追加需求：用户拍板「支付密钥由 admin 网页自填，不再只走环境变量」。计划：`docs/plans/支付渠道网页配置.plan.md`。
> 范围切割：只做**配置管理**；支付宝/微信 SDK 下单验签实现不在本轮（商户号到位后另起，渠道 available() 维持 false）。

## Step 1 后端 ✅ `736ca763`

- `V143__payment_channel_config.sql`：channel CHECK 白名单（ALIPAY/WECHAT）+ 部分唯一索引；`config_encrypted` TEXT（AES 密文）+ `config_tails` VARCHAR(500)（脱敏回显）+ BaseEntity 六列；回滚块。
- `V144__payment_config_permission.sql`：`payment:config` 授 admin（V142 模式）。
- `billing/service/PaymentChannelConfigService.java`：
  - `listMasked()` 读只出 tails，**永不解密出库**；
  - `save()` merge 语义（留空=保持原值）+ 渠道/字段键双白名单（REQUIRED_KEYS）+ 必填全集校验 + AES 整体加密 + tails 生成（≤4 全遮，否则尾 4）；
  - `getDecrypted()` 内部桥：DB 优先、env 兜底（@Value 六变量与 application.yml 同名占位）。
- `billing/controller/PaymentChannelAdminController.java`：GET 脱敏列表 + PUT 保存（@RequirePermission payment:config + @AuditLog）。
- 单测 `PaymentChannelConfigServiceTest` 8/8：密文无明文 / 短值全遮 / 缺键·未知键·未知渠道 400 / merge 覆盖与保持 / env 兜底 / 脱敏列表。
- **全量回归 2348 Tests BUILD SUCCESS**（V143/V144 在 IT 库 validate+apply 通过）。

## Step 2 前端 ✅ `7faa5127`

- `api/billing.ts`：adminPaymentChannels / savePaymentChannelConfig + PaymentChannelConfigVO + PAYMENT_CHANNEL_FIELDS（表单真相源，与后端 REQUIRED_KEYS 对齐）。
- `views/admin/PaymentChannelConfigView.vue`：双卡片；placeholder 显 `****尾4`；留空不进 payload；密钥字段 password 输入（眼睛切换）；保存二次确认（明示字段清单+加密+审计）；保存成功清空表单+刷新；顶部 alert 明示「配置齐全≠渠道上线」。
- 注册三处：modules.ts `paymentAdmin`→`payment:config`；router `/admin/payment-channels` meta module；Sidebar admin 区「支付渠道」（KeyOutline）。
- vitest +5（脱敏回显不出明文 / 留空不进 payload / 确认流调参 / 空表单不弹 / accessGuard 双态）；**全量 104 文件 / 719 绿 + vue-tsc 绿**。

## Step 3 文档 ✅（本 commit）

- 7x 问题单：已解决区补第 13 条；接入指南 §五改「网页自填首选」+§六流程更新+「填好≠上线」警示。
- `积分计费系统.feature-map.md`：补「支付渠道网页配置」小节 + 变更记录行。
- `部署手册终.md`：支付 env 行注「首选网页配置，env 兜底（DB 优先）」。

## 遗留（下一轮 SDK 接入时做）

1. 渠道类 precreate/verifyAndParse 真实实现（官方 SDK）；`available()` 改读 `getDecrypted()` 配置齐 + IMPLEMENTED 翻转。
2. 微信支付可能需 API 证书文件上传位（写码时定）。
3. 真实渠道 0.01 元小额链路验收（7x 指南 §六 步骤 4）。
