# 开发进度45：P5 灰度发布状态 PostgreSQL 持久化

- 日期：2026-08-14
- 分支：`beifen`

## 已完成

- 新增 `V116__rag_rollout_state.sql`，保存每个 KB 当前灰度配置及可回滚上一配置。
- 新增 Mapper/Repository，使用 upsert 原子更新当前/上一状态。
- `RagRolloutService` 生产路径改用 PostgreSQL，重启后 status、稳定分桶和 rollback 仍可恢复。
- 测试构造保留内存 Repository，便于纯领域测试；生产不再依赖 JVM Map。

## TDD 与验证

- RED：Repository 测试因 Mapper/Repository/公开历史协议不存在而编译失败。
- GREEN：Repository、Rollout、迁移版本测试共 4/4 通过。

## 下一步

让回滚同时驱动真实 route/read alias 与相关缓存失效，并把 Release Gate 与真实 Evaluation Run 绑定。
