# 修复XI 开发进度4（D 轮：组=大节点，2x 未解决④）

> 对应 spec `docs/specs/人工测试遗留问题修复XI设计.md` XI-4｜plan `docs/plans/人工测试遗留问题修复XI.plan.md` D 轮（D1-D5）
> 轮序：A 右键菜单 ✅ → B 官方库 ✅ → C 两级词汇 ✅ → D 组大节点 ✅ → E 文档收尾

## D1 组框点选分层（commit ae72f41c）

- **探层结论**：组层 `.canvas-board__groups` z-5 在 VueFlow（z-4）**上层**，但维持框体 `pointer-events:none` 穿透（修复VIII 设计不动）——组空白点击的 DOM 落点就是 pane，改在 **boardRoot 捕获段**按坐标命中 `groupBoxes` 记候选，pane-click 转正选中（拖框/平移时 pane-click 不来，候选自然作废）。比 plan 预想的「空白垫层+成员区打洞」少一层结构。
- `selectGroup`：清节点单选/多选（互斥）+高亮 `groupbox--selected`；五条清路径=点 pane/点成员/点边/框选起手/Esc（不吞事件）；watch groups 悬挂清理（解散/整体重载清空）。
- Delete/Backspace 组选态零动作（Q5）；顺修存量隐患：Delete 守卫 `t.closest` 无 instanceof 保护（window target 崩）→ 统一 `isTypingTarget`。
- 测试 +7（[CanvasBoard.test.ts](../../../frontend/src/components/canvas/CanvasBoard.test.ts) D1 describe）。

## D2 整组拖动（commit c3ca8382）

- 捕获段命中组空白 → `stopPropagation` 接管（vue-flow pane 平移/框选不响应）+开拖动会话；**越阈 4px 才算拖**，越阈首帧只定基线（吞首段=标准拖动节流语义）；已选中=pushHistory('move')+逐 move 累增量 rAF 合帧批改全成员 position（÷zoom 换算，包围盒 rAF 既有跟随）；**未选中=先选中不拖**（Q6 点选分层，本会话不再位移，下一次按住才拖）。
- 松手/pointercancel/blur：冲刷尾帧 + 恰一次 `structure-changed`（拖动中零 emit）；onUnmounted 摘监听零残留。
- **坑沉淀**：尾帧冲刷须**先于**会话置空——`applyGroupMoveDelta` 从会话读组 id，先空后冲=丢帧（rAF 先到的手工链测不出，pointerup 先到的 helper 链才暴露：多测一条路径多兜一层底）。

## D3 剪贴板纯函数（commit 8e5c78cd）

- [canvasClipboard.ts](../../../frontend/src/components/canvas/canvasClipboard.ts)：`CanvasClipboard` 增 `groups[{name,color,memberIds 旧id}]`（**完全包含**且非空才收，Q7；空数组向后兼容 VII/IX/X）+ `groupCrossEdges[{fromKey,toKey}]`（key=旧节点 id|`group:${下标}`）。
- `buildCopySet` 第 4 参 groups（默认 []）——组端点边**分治**：组进板→统一走 groupCrossEdges（成员↔本组内边口径/组↔板外对端保旧 id/组↔组各记下标）；组不进板（半含）→照 X-3 恰一端走 crossEdges **连原组**（X 回归锚不动）。
- 新 `remapGroupCrossEdges`：`group:${idx}`→新组伪 id、板内节点→新 id、板外对端保旧+aliveNodeIds 校验（已删丢边不产断边）。纯函数单测 +7（[canvasClipboard.test.ts](../../../frontend/src/components/canvas/canvasClipboard.test.ts)）。

## D4 粘贴重建组（commit f1463dc9）

- [CanvasBoard.vue](../../../frontend/src/components/canvas/CanvasBoard.vue) `onCopyKeydown` 传第 4 参；`pasteSubgraph` 增组重建段：新 `group-{ts}-{seq}` id+`uniqueLabel` 名去重（撞名空格序号）+memberIds 重映射（与原组零共享）+color 拷贝；**无条件建壳**（细化2 ⛓ 治边不治壳）；不走 createGroup（防二次入栈+掏旧组成员）——同 'paste' 栈帧=组+节点+边一步撤。
- 组级边随 ⛓ 开关：关=零组级边；开→remapGroupCrossEdges 恒落 groupEdges 池（伪 id 绝不进 v-model）。诱导边纯节点恒粘（VII 口径，既有 test 597 锚）。新 emit `groups-pasted` → [CanvasView.vue](../../../frontend/src/views/CanvasView.vue) toast「已粘贴 N 个组（M 个节点）」。
- 测试 +6：粘组全链三态/⛓关壳照建边零/悬挂防护/一步撤/连贴+32 且组名 2/3 各自去重/半含=现状平节点+X-3 连原组。

## D5 收口（本 commit）

- 全量 vitest **1068/1068**（C 轮 1042 → D 轮 +26：D1 7+D2 6+D3 7+D4 6）+ vue-tsc **0 错**；后端零改动不重跑。
- 安全清单（D 轮相关）：剪贴板纯前端会话态不入库不跨画布（规格 §6/§8 口径不变）；粘贴产物 memberIds/边端点全经重映射+alive 校验（不产断边/不误引用已删对象）；无新端点/新依赖。
- 手测项并入 E 轮 S15-S19（点组选组/整组拖跟手/完全包含粘新组连上下游/半含平节点/Delete 无动作/解散/刷新重现）。
- commits：ae72f41c（D1）+ c3ca8382（D2）+ 8e5c78cd（D3）+ f1463dc9（D4）+ 本 commit（D5 文档）。

## 下一步

E 文档收尾（feature-map/user-ops 两文件增补节 + S1-S19 测试方案 + 问题单四项挂「已实现待人工验证（修复XI）」+ help 21-assets-basics 两级一行）。
