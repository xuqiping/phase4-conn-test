# 已解决（2026-08-11）
1. ✅ 不再开放"我的模型"由个人自己配置大模型了
   - 前端：SettingsView 移除 `my-models` Tab（`UserProviderTab.vue` 文件保留备用）
   - 后端：`LlmGateway.findProvider` 停用用户级 override，chat 一律走全局 CHAT 注册表；`UserLlmController` / `user_llm_providers` 表保留不删
   - commit：874ad9c0（前端）、57a1f58（后端）
2. ✅ 全局模型供应商要求可导出，可上传
   - 后端：`GET /api/llm/providers/export`（含明文 key 下载，@AuditLog 留痕）、`POST /api/llm/providers/import`（按 name upsert，size≤200）
   - 前端：ProviderManageTab 加导出/导入按钮，二次确认 + 预览新增/更新/非法计数
   - commit：5a3d5b3（导出）、导入端点提交、aee25529（前端）
3. ✅ 非管理员/超级管理员，不再前端显示设置这个模块
   - Sidebar 「设置」入口仅 `isAdmin` 可见；路由守卫 `requireAdmin` 非 admin 重定向落地页
   - 注：项目无"超级管理员"角色，统一按 `roles.includes('admin')` 处理
   - commit：fb689977（Sidebar）、Chunk C 路由守卫
4. ✅ 如果不具备权限的相关前端模块，不再展示（如普通用户不开放Agent相关、工作流相关所有权限，那么Agent大厅、工作流等对应模块前端就不展示了，也不可选）
   - `config/modules.ts` 的 `MODULE_PERMISSION_MAP` + Sidebar `canSeeModule` + 路由守卫三层过滤
   - commit：e4a47d01（开关）、fb689977（Sidebar）、Chunk C（路由守卫）
5. ✅ 本项目中暂时用不到Agent大厅、工作流、执行监控这几个模块，对所有人隐藏并不允许访问，包括管理员/最高管理员（保留后端功能和前端功能，只是该项目不展示且不可用）
   - `ENABLED_MODULES` 中 agentHall/workflow/execution 置 false；菜单 + 路由 + 入口全断；后端 Controller 与前端 view 文件全部保留
   - commit：e4a47d01、fb689977、Chunk C

# 参考速查表
- agent-platform\项目工程文档\项目功能介绍\速查表\21-系统设置.md
> 解决完后需要同步更新对应的速查表（已更新 ✅）
