# 2026-06-10 商业授权后续开发进度

## 背景

- 已根据实际阶段 4 开发进度生成调整版实施计划：`file-keeper/项目相关文档/商业售卖版账号权限与授权系统实施计划-新.md`。
- 当前开始执行后续计划的阶段 5A：联调前置配置收口。
- 当前分支：`lianwangban`。
- 当前未创建 worktree，按用户要求在当前工作区继续推进。

## 阶段 5A：联调前置配置收口

### Task 5A-1：统一开发环境服务端地址

目标：统一服务端、管理后台、桌面端的开发环境 API 地址，避免真实联调时服务端默认 `8088`，前端仍请求 `8080`。

#### 进度

- [x] 创建本进度文件。
- [x] 更新管理后台开发 API 地址为 `http://localhost:8088`。
- [x] 新增桌面端开发环境变量 `VITE_FILE_KEEPER_SERVER_URL=http://localhost:8088`。
- [x] 更新桌面端服务端地址兜底值为 `http://localhost:8088`。
- [x] 运行桌面端测试。
- [x] 运行桌面端构建。
- [x] 运行管理后台构建。

#### 当前记录

- 2026-06-10：已创建进度文件，准备开始修改阶段 5A 配置。
- 2026-06-10：已将管理后台开发环境 API 地址从 `http://localhost:8080` 改为 `http://localhost:8088`。
- 2026-06-10：已新增桌面端开发环境变量文件 `file-keeper/.env.development`，内容为 `VITE_FILE_KEEPER_SERVER_URL=http://localhost:8088`。
- 2026-06-10：已将桌面端 `App.vue` 中商业授权服务端兜底地址改为 `http://localhost:8088`。
- 2026-06-10：桌面端测试通过：`npm --prefix "file-keeper" test`，28 个测试文件、162 个测试全部通过。
- 2026-06-10：桌面端构建通过：`npm --prefix "file-keeper" run build`。Vite 输出已有动态导入 chunk 提示，不影响构建结果。
- 2026-06-10：管理后台构建通过：`npm --prefix "file-keeper/admin-web" run build`。Vite 输出 chunk 体积和插件耗时提示，不影响构建结果。

#### 阶段 5A 结论

- Task 5A-1 已完成。
- 开发环境服务端地址已统一到 `http://localhost:8088`。
- 下一步进入阶段 5B：桌面端账号 UI 与认证 Store。

## 阶段 5B：桌面端账号 UI 与认证 Store

### Task 5B-1：新增桌面端客户端认证 API

目标：封装桌面端验证码、注册、登录、刷新 token、退出登录 API，为后续认证 Store 和账号 UI 提供基础能力。

#### 进度

- [x] 新增客户端认证 API 测试 `file-keeper/src/api/__tests__/auth.test.ts`。
- [x] 确认红灯：认证 API 文件缺失时测试失败。
- [x] 新增客户端认证 API `file-keeper/src/api/auth.ts`。
- [x] 对齐后端真实契约：验证码接口使用 `contactType/contact`，用户摘要包含 `emailVerified/phoneVerified`。
- [x] 对齐结构化错误处理：`AuthApiError` 保留 `status/code`。
- [x] 运行客户端认证 API 测试。
- [x] 完成规格复查。
- [x] 完成代码质量复查。

#### 当前记录

- 2026-06-10：已新增 `sendVerificationCode`、`checkVerificationCode`、`register`、`login`、`refresh`、`logout` 六个桌面端客户端认证 API。
- 2026-06-10：已以后端实际接口为准，验证码请求体使用 `{ contactType, contact }` / `{ contactType, contact, code }`。
- 2026-06-10：已补齐 `UserSummary.emailVerified` 和 `UserSummary.phoneVerified`，测试状态值使用真实的 `pending_review`。
- 2026-06-10：已新增 `AuthApiError`，认证 API 业务错误和 HTTP 错误保留 `status/code`，网络错误继续透传。
- 2026-06-10：认证 API 测试通过：`npm --prefix "file-keeper" test -- src/api/__tests__/auth.test.ts`，1 个测试文件、9 个测试全部通过。
- 2026-06-10：规格复查通过，无规格缺口；代码质量复查初次要求补齐真实字段和结构化错误，修复后复查通过。

#### Task 5B-1 结论

- Task 5B-1 已完成。
- 下一步进入 Task 5B-2：新增桌面端认证 Store。

### Task 5B-2：新增桌面端认证 Store

目标：新增桌面端账号认证 Store，负责登录态保存、会话恢复、退出登录，并驱动商业授权 Store 切换登录授权或匿名授权。

#### 进度

- [x] 新增认证 Store 测试 `file-keeper/src/stores/__tests__/authStore.test.ts`。
- [x] 确认红灯：认证 Store 文件缺失时测试失败。
- [x] 新增认证 Store `file-keeper/src/stores/authStore.ts`。
- [x] 实现登录态内存状态：`accessToken`、`refreshToken`、`user`、`loading`、`error`、`isAuthenticated`。
- [x] 使用 `file-keeper-auth.json` 的 `authSession` key 持久化登录态。
- [x] 登录成功后调用 `commercialAuthStore.initializeAuthenticated(baseUrl, accessToken)`。
- [x] 退出登录后清空登录态并调用 `commercialAuthStore.initializeAnonymous(baseUrl)`。
- [x] 启动恢复会话时使用 refresh token 获取新 token 并初始化登录授权。
- [x] refresh 失败或无本地会话时回退匿名授权。
- [x] 注册和验证码方法仅代理 API，不自动登录。
- [x] 修复质量复查发现的失败路径：登录授权初始化失败、登录持久化失败、退出匿名初始化失败、无本地会话匿名初始化失败。
- [x] 运行认证 Store 测试。
- [x] 完成规格复查。
- [x] 完成代码质量复查并按反馈修复。

#### 当前记录

- 2026-06-10：已新增 Pinia composition store `useAuthStore`，Store id 为 `auth`。
- 2026-06-10：已实现 `login`、`logout`、`restoreSession`、`register`、`sendVerificationCode`、`checkVerificationCode`。
- 2026-06-10：登录态持久化到 `file-keeper-auth.json` 的 `authSession`，清理时写入 `null` 并保存。
- 2026-06-10：`login()` 在认证 API 成功后保存会话并初始化登录授权；若持久化或登录授权初始化失败，会清空内存会话并尽量清理持久化状态，避免 UI 显示“登录失败但已登录”。
- 2026-06-10：`logout()` 即使服务端 logout 失败，也会清理本地登录态并回到匿名授权；匿名初始化失败时保证 `loading=false`。
- 2026-06-10：`restoreSession()` 无本地会话时只执行一次匿名初始化；refresh 失败时清空登录态并回退匿名授权。
- 2026-06-10：认证 Store 测试通过：`npm --prefix "file-keeper" test -- src/stores/__tests__/authStore.test.ts`，1 个测试文件、14 个测试全部通过。
- 2026-06-10：规格复查通过；代码质量复查初次要求修复登录残留、登出 loading、匿名重复初始化，修复并补充失败路径测试后通过。

#### Task 5B-2 结论

- Task 5B-2 已完成。
- 下一步进入 Task 5B-3：新增登录/注册弹窗 UI。

### Task 5B-3：新增登录/注册弹窗 UI

目标：在桌面端顶部增加账号入口，提供登录、注册、退出能力，并在应用启动时通过认证 Store 恢复登录态或回退匿名授权。

#### 进度

- [x] 新增登录/注册弹窗测试 `file-keeper/src/components/__tests__/authDialog.test.ts`。
- [x] 确认红灯：登录/注册组件缺失时测试失败。
- [x] 新增弹窗容器 `file-keeper/src/components/AuthDialog.vue`。
- [x] 新增登录表单 `file-keeper/src/components/LoginForm.vue`。
- [x] 新增注册表单 `file-keeper/src/components/RegisterForm.vue`。
- [x] 修改 `file-keeper/src/App.vue`，顶部全局操作区增加账号入口。
- [x] 未登录时显示“登录”按钮，点击打开登录弹窗。
- [x] 已登录时显示邮箱或手机号，并提供退出按钮。
- [x] 登录成功后关闭弹窗，并继续校正授权 Tab 与剪贴板监听状态。
- [x] 注册支持邮箱/手机号、发送验证码、校验验证码、提交注册。
- [x] 注册提交前要求验证码已校验；联系类型、联系内容或验证码变化后校验状态失效。
- [x] 注册成功显示“注册成功，等待管理员审核”，不自动登录。
- [x] 应用启动流程改为 `authStore.restoreSession(commercialServerUrl)`，不再直接调用匿名授权初始化。
- [x] 运行登录/注册弹窗组件测试。
- [x] 运行桌面端全量测试。
- [x] 运行桌面端构建。
- [x] 完成规格复查；初次复查发现可跳过验证码校验，修复后复查通过。
- [x] 完成主会话代码质量检查；未引入新依赖或大规模重构。

#### 当前记录

- 2026-06-10：已新增 `AuthDialog` 控制弹窗和登录/注册 Tab，`LoginForm` 只处理账号密码登录，`RegisterForm` 只处理联系方式、验证码和密码注册。
- 2026-06-10：`App.vue` 已引入 `useAuthStore` 和 `AuthDialog`，创建 `authStore`，顶部全局操作区渲染账号入口。
- 2026-06-10：`App.vue` 启动时已改为调用 `authStore.restoreSession(commercialServerUrl)`，由认证 Store 决定恢复登录授权或匿名授权，避免重复匿名初始化。
- 2026-06-10：退出登录调用 `authStore.logout(commercialServerUrl)`，成功后执行 `ensureAuthorizedTab()` 和 `syncClipboardMonitor()`。
- 2026-06-10：规格复查发现注册表单可不点击“校验验证码”直接提交，已补充失败测试并修复为必须先校验验证码。
- 2026-06-10：登录/注册弹窗测试通过：`npm --prefix "file-keeper" test -- src/components/__tests__/authDialog.test.ts`，1 个测试文件、6 个测试全部通过。
- 2026-06-10：桌面端全量测试通过：`npm --prefix "file-keeper" test`，31 个测试文件、191 个测试全部通过。输出中仍有既有测试 mock 的 stderr/warn 日志，不影响通过结果。
- 2026-06-10：桌面端构建通过：`npm --prefix "file-keeper" run build`。Vite 仍输出既有动态导入 chunk 提示，不影响构建结果。

#### Task 5B-3 结论

- Task 5B-3 已完成。
- 阶段 5B 桌面端账号 UI 与认证 Store 主体已完成。
- 下一步进入阶段 5C：桌面端授权状态展示。

## 阶段 5C：桌面端授权状态展示

### Task 5C-1：新增授权状态组件

目标：在桌面端展示当前授权状态，让用户能理解匿名试用、免费模块、等待审核、商业授权、离线缓存以及账号/设备拒绝等状态。

#### 进度

- [x] 新增授权状态组件测试 `file-keeper/src/components/__tests__/entitlementStatus.test.ts`。
- [x] 确认红灯：授权状态组件缺失时测试失败。
- [x] 新增授权状态组件 `file-keeper/src/components/EntitlementStatus.vue`。
- [x] 修改 `file-keeper/src/App.vue`，在顶部账号入口附近挂载授权状态组件。
- [x] 运行授权状态组件测试。
- [x] 运行桌面端全量测试。
- [x] 运行桌面端构建。
- [ ] 完成规格复查。
- [ ] 完成代码质量复查。
- [x] 根据代码审查补充边界失败测试。
- [x] 修复授权状态边界显示问题。

#### 当前记录

- 2026-06-11：已新增授权状态组件测试，覆盖匿名试用、匿名过期未选免费模块、匿名免费模块、等待审核、商业授权模块状态、离线缓存截止时间、账号/设备拒绝错误展示。
- 2026-06-11：红灯已确认：`npm --prefix "file-keeper" test -- src/components/__tests__/entitlementStatus.test.ts` 因 `../EntitlementStatus.vue` 缺失失败，符合预期。
- 2026-06-11：已新增 `EntitlementStatus.vue`，使用 `useAuthStore` 和 `useCommercialAuthStore` 展示当前授权状态，不在组件内发起网络请求。
- 2026-06-11：已修改 `App.vue`，在顶部全局操作区、账号入口前方挂载 `EntitlementStatus`。
- 2026-06-11：授权状态组件测试通过：`npm --prefix "file-keeper" test -- src/components/__tests__/entitlementStatus.test.ts`，1 个测试文件、7 个测试全部通过。
- 2026-06-11：桌面端全量测试通过：`npm --prefix "file-keeper" test`，32 个测试文件、198 个测试全部通过。输出中仍有既有测试 mock 的 stdout/stderr 日志，不影响通过结果。
- 2026-06-11：桌面端构建通过：`npm --prefix "file-keeper" run build`。Vite 仍输出既有动态导入 chunk 提示，不影响构建结果。
- 2026-06-11：代码审查发现 3 个重要边界：非登录态残留 `pending_review` 不应覆盖匿名状态、登录态等待授权时不应回落显示旧匿名状态、过期离线授权不应显示为有效商业授权。已补充对应失败测试，定向测试 10 个用例中 3 个按预期失败。
- 2026-06-11：已修复授权状态边界显示：登录态与匿名态互斥展示，商业授权展示只使用未过期的登录授权快照。修复后授权状态组件测试通过：1 个测试文件、10 个测试全部通过。
- 2026-06-11：边界修复后重新验证通过：`npm --prefix "file-keeper" test`，32 个测试文件、201 个测试全部通过；`npm --prefix "file-keeper" run build` 构建通过，仍仅有既有 Vite 动态导入 chunk 提示。

#### Task 5C-1 结论

- Task 5C-1 已完成。
- 阶段 5C 桌面端授权状态展示已完成。
- 下一步进入阶段 5D：匿名试用过期后的免费模块选择。

## 阶段 5D：匿名试用过期后的免费模块选择

### Task 5D-1：扩展 commercialAuth API 支持免费模块选择

目标：封装匿名试用过期后的免费模块选择和更换 API，为 Store 和 UI 提供基础能力。

#### 进度

- [x] 扩展 `file-keeper/src/api/__tests__/commercialAuth.test.ts`，新增选择免费模块、更换免费模块、30 天限制错误透传测试。
- [x] 确认红灯：API 函数缺失时测试失败。
- [ ] 扩展 `file-keeper/src/api/commercialAuth.ts`，新增 `selectFreeModule` 和 `changeFreeModule`。
- [ ] 运行 commercialAuth API 测试。

#### 当前记录

- 2026-06-11：已新增 5D-1 API 测试，覆盖 `POST /api/anonymous/trial/select-free-module`、`POST /api/anonymous/trial/change-free-module`，以及后端 30 天限制错误消息透传。
- 2026-06-11：红灯已确认：`npm --prefix "file-keeper" test -- src/api/__tests__/commercialAuth.test.ts` 中新增用例因 `selectFreeModule` / `changeFreeModule` 未实现失败。
