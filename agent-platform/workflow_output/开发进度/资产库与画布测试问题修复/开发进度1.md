# 资产库与画布测试问题修复 · 开发进度1（C4/C6/C1a/C3/C5）

> 对应 [plan](../../docs/plans/资产库与画布测试问题修复.plan.md)。worktree `khfz1`。
> 本轮按推荐序 C4→C6→C1a→C3→C5 推进，剩 C1b（schema 大改）+ C2 留下一会话。

## 本轮完成（5 chunk，全部 commit + 测试绿）

| chunk | 内容 | commit | 测试 |
|---|---|---|---|
| **C4** | @占位符蓝/黄 chip 高亮（mirror overlay 层） | `5705f08d` | +4（MentionTextarea） |
| **C6** | 节点头显 label + 双击画布空白处快速加节点 | `7304ba29` | +4（TextNode） |
| **C1a** | 叙事角色桶编辑入口（VocabEditor 弹窗） | `4fe56871` | +5（VocabEditor） |
| **C3** | 剧本资产 UI（正文编辑+AI 分场+scenes 渲染） | `0e092cad` | +5（AssetDetailDrawer+ScriptScenes） |
| **C5** | 节点选模型（text/script=chat + video=MEDIA，D2 新端点） | `df0167d7` | +3（PropertyPanel） |

前端累计 **217 测试绿**（baseline 200 + 本轮 +17），vue-tsc 净，后端编译通过。

## 各 chunk 关键决策与落点

### C4 @占位符蓝高亮
- `MentionTextarea.vue` 加 mirror 镜像层（垫 textarea 下，字体/padding/border/wrap 像素级对齐）；textarea 透明叠层，文字+光标可见，占位符处显蓝底。
- `segments` computed 本地正则切分（parseMentions 不返 index 故不复用）；chip 蓝底，断链加 `is-broken` 黄底。
- 复用既有 `brokenMentions: string[]`（CanvasView 已算好传入 PropertyPanel）做黄底判定，零新 API。`@scroll` 双向同步视口。
- **坑**：mirror `overflow:hidden` + `pointer-events:none`；textarea `position:relative;z-index:1` 叠上。

### C6 节点头显 label + 双击加节点
- `CanvasNodeBase` 加 `label` 显示位（类型标签旁，空=「未命名」灰字，`flex:1` 把资产/状态徽标推右）。5 节点传 `:label=data.label`；body 移除 label 兜底（避免头/体重复）。
- 双击：`CanvasBoard boardRoot @dblclick` → 仅空白处触发（`closest('.vue-flow__node/edge/handle')` 排除节点/连线/句柄）→ project(clientXY − vueFlow 偏移) → emit quick-add。复用 onDrop 坐标范式。
- `CanvasView` 内联 n-modal 搜索框：type 过滤调色板 + ↑↓/Enter/Esc + 选定在双击坐标 `addNode`。
- **坑**：dblclick 与拖拽/单击不冲突（target 判定排除节点）；节点用 vue-flow `<Handle>` 独立 mount 须 stub Handle（测试）。

### C1a 叙事角色桶编辑
- 新 `VocabEditor.vue`：n-tabs 分类管理弹窗（叙事角色 Tab 实现；媒体类型 Tab 占位待 C1b）。
- 草稿模式（开盖拷贝 narrativeRoles，`immediate` 兼容 mount 即 show）+ 失焦与他行去重 + 删除 n-popconfirm（`roleAssetCounts` 显迁移数）+ normalized（trim/去空/去重保序）+ 删到<1 禁存。
- `AssetProjectView`：canWrite 显「编辑分类」按钮；`roleAssetCounts` 由矩阵 cells 按 roleKey 聚合；保存 `projectApi.update({narrativeRoles})` → loadProject+reload。
- **复用既有**：ProjectUpdateRequest.narrativeRoles / 后端 reassignOnRemovedRoles L185 / SaveToAssetDialog 开盖即 projectApi.list() 拉新角色（同源自洽）/ AssetMatrixFilter reactive 自洽。**无新后端**。

### C3 剧本资产 UI
- 新 `ScriptScenes.vue`：分场列表纯展示（场号+描述）。
- `AssetDetailDrawer` SCRIPT 分支替换 JSON dump：n-input 正文编辑（EDITOR 可改/VIEWER 只读）+ 保存正文 + AI 分场 + ScriptScenes。
- `parseScriptContent` 解析 content{synopsis,scenes?}（旧态非 JSON 当 synopsis）；状态机动作后重解析（防 meta-only 覆盖丢 scenes）。
- **关键**：编辑正文复用既有 `versionApi.create`（createVersion L117 调 updateContent 同步 asset.content）→ breakdown 即读最新。**无新后端**。runBreakdown 正文脏先 warning 拦截。
- **坑**：n-drawer teleport → 测试断言走 document.body；messageMock 须加 `warning`。

### C5 节点选模型
- 后端 D2：`UserLlmController` 加 `GET /models/video`（镜像 /models/available 反向过滤，仅 CATEGORY_MEDIA 全局 provider）。两端点互补：available 排 MEDIA（chat）/ video 只收 MEDIA。
- 前端：api/llm.ts 加 listVideoModels；PropertyPanel 三面板加模型下拉（text/script=chatModelOptions，video=videoModelOptions），按 providerName 分组，失败静默，空选=不写 data.model→后端回落。
- `CanvasView.onRunVideo` submitVideo 透传 `model: data.model`（MediaSubmitRequest.model 已可选）。
- **安全记档**：模型白名单兜底（防非法 model 串）是既有面（runner 早读 data.model，C5 仅暴露 UI），记 backlog，非 C5 引入。

## 下一会话开工（C1b + C2）

### C1b 媒体类型两层设计（schema 大改，后置独立验证）
- **DB V60**：assets 加 `media_category`(系统固定 TEXT/IMAGE/VIDEO/AUDIO，决定处理) + asset_projects 加 `media_types` JSONB（项目级受控词汇 {key,category}，默认 5 项）；删旧 chk_asset_media CHECK（media_type 改任意字符串，走应用层校验）；backfill CASE WHEN 5 类。
- 后端：Asset entity 加 mediaCategory + AssetProject mediaTypes JSONB；AssetProjectService create/update mediaTypes + normalize + reassignOnRemovedMediaTypes；AssetService isTextType/isFileType→读 category、validateMediaType 走受控词汇、validateFileMime 按 category、buildUploadGenMeta 按 category、toVO 返 mediaCategory；AssetCanvasBridgeService.mapMediaType 改返 {category,defaultType}。
- 前端 ~9 文件：types MediaCategory/MediaTypeDef、AssetMatrixFilter 顶栏去写死从 mediaTypes 派生、AssetProjectView 上传/新建下拉按 category 过滤、VocabEditor 媒体类型 Tab 激活、AssetCard 图标按 category、SaveToAssetDialog 同源。
- **坑点**（plan 技术坑点）：自定义 type 击穿 isTextType/mime/mapMediaType 分类链 → category 决定处理、type 仅分类；删 type 资产悬挂 → reassign 归同 category 首项；mime↔category 校验缺口 → 上传按 mime 推断 category；backfill 漏数据 → CASE 全 5 类 + DEFAULT 'TEXT' 兜底。
- **必须新会话开工**（DB schema + 多服务重构，风险最高，需满上下文预算）。

### C2 卡片缩略图懒加载（性能收尾）
- 后端 AssetService.toVO 列表态补 fileId（文件类）；前端 AssetCard 加预览渲染（IMAGE `<img>`/VIDEO `<video #t=0.1 preload=metadata>` 首帧）+ IntersectionObserver 视口内懒加载 + 模块级 Map 缓存 objectURL + unmount/滚出 revoke。复用 /api/files/{fileId}（归属咽喉点已校验）。建议 fetchCanvasPreview 提到 api/file.ts 共用。

## 本轮沉淀（拟入规范，待 C1b 完一并织入）
- **textarea 高亮 overlay 范式**（C4）：mirror 镜像层垫 textarea 下像素级对齐 + 透明文字染色载体 + textarea 透明叠层 + pointer-events:none 穿透 + scroll 同步。凡 textarea 内 token 高亮照此。
- **dblclick 画布加节点排除法**（C6）：`closest('.vue-flow__node/edge/handle')` 判定空白处，非 e.target===boardRoot（VueFlow pane 是子元素）。
