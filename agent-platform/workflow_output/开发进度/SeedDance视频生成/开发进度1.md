# SeedDance 2.0 视频生成 · 开发进度1

> Phase3 实现记录。来源 plan [../docs/plans/SeedDance视频生成.plan.md](../../docs/plans/SeedDance视频生成.plan.md) + spec [../docs/specs/SeedDance视频生成.md](../../docs/specs/SeedDance视频生成.md)。
> 本轮：Step1-6 后端全栈落地编译绿；Step7 前端 + 收尾文档 + 单测留下轮。

## 本轮完成（Step1-6）

| Step | 产出 | 文件 |
|---|---|---|
| 1 建表+权限 | V54 `media_gen_tasks`(append-only+locked_until/attempt for SKIP LOCKED) + `media:gen` gated seed(仅 admin) | `db/migration/V54__media_gen_tasks.sql` |
| 2 provider 抽象 | `MediaGenProvider` 接口 + `ArkSeedanceProvider`(Ark 任务端点,doubao key AES 复用,WebClient 指纹缓存) + DTO | `media/provider/*`、`media/dto/MediaGenRequest|MediaGenResult` |
| 3 worker | `MediaTaskExecutorConfig`(core2/max4/queue100/AbortPolicy) + `MediaGenProperties`(media.*) + `MediaGenTask` entity + mapper + `MediaGenTaskTxService`(SKIP LOCKED claim) + `MediaGenTaskService.submit` + `MediaGenTaskWorker`(@Scheduled poll+退避轮询+超时FAILED) | `media/config/*`、`media/entity/*`、`media/mapper/*`、`media/service/internal/*`、`media/service/MediaGenTask*` |
| 4 下载落盘 | `MediaStorageService.downloadAndStore`(Ark URL→stored_files source=MEDIA) + `FileStorageService.storeStream`(InputStream 入口,复用咽喉点) + `StoredFileEntity.SOURCE_MEDIA` | `media/service/MediaStorageService`、`file/service/FileStorageService`、`file/entity/StoredFileEntity` |
| 5 usage 记账 | worker `resolveUsage`:Ark `usage.total_tokens` 真值(SUCCESS) / 不返则 Ark 官方 token/秒 费率表估算(ESTIMATED,5s/720p≈30.88万) | `media/service/MediaGenTaskWorker` |
| 6 REST API | `MediaGenController`(POST /video + GET /tasks/{id} + GET /tasks + GET /tasks/{id}/download Content-Disposition) + `MediaGenQueryService`(ownership 过滤) + VO | `media/controller/*`、`media/service/MediaGenQueryService`、`media/dto/MediaSubmitRequest|MediaTaskVO` |

**编译**：`mvn -o compile` 全绿。零回归(纯新 `media/` 包；旧文件仅 2 处纯增量：`FileStorageService.storeStream` 新增方法、`StoredFileEntity.SOURCE_MEDIA` 新增常量)。

## 关键设计决策（落地版）

1. **worker 纯 poll 而非 submit 即派发**（偏离 plan Step3 措辞，更低风险）：照抄 `IndexJobWorker` 验证模式——`@Scheduled` poll 认领 PENDING + 锁过期 RUNNING(`FOR UPDATE SKIP LOCKED`)，分发 `mediaTaskExecutor`。崩溃恢复免费（重启下次 poll 自动续跑），无需 `@PostConstruct`。代价：首次 createTask 最多延迟 pollMs(5s)，视频生成本身分钟级，可忽略。
2. **usage 估算用 Ark 官方 token/秒 费率表**（非 spec 字面像素公式）：spec 给的像素公式与 Ark 真实计费在 720p 恰好等价(5s/720p/24fps≈30.88万)，但 Ark 实际按 token/秒 费率(720p=61760/s)。用费率表更贴合 Ark 真实账单，且满足 spec 断言常量。Ark 返 `usage.total_tokens` 时用真值，估算仅作 fallback(ESTIMATED 标记)。
3. **图生视频参考图走 file_id→data URI**：复用 `POST /api/files` 上传(单一咽喉点归属校验)，submit 带 `refFileId`；worker `MediaStorageService.readAsDataUri` 读字节转 base64(≤8MB 校验)喂 Ark `image_url`。不在视频端点重做 multipart。
4. **media_gen_tasks 不继承 BaseEntity**：append-only 任务表不软删(spec §6 明确不加 deleted)，故 standalone entity(同 `StoredFileEntity` 思路)，加 `locked_until/attempt` 列支撑 SKIP LOCKED claim。
5. **videoUrl 不暴露 Ark 临时 URL**：VO 的 `videoUrl` 指向 `/api/media/tasks/{id}/download`(Content-Disposition 附件)，Ark OSS 临时链接仅 worker 下载时用一次即弃。

## Step7 完成（前端 + 后端单测 + 收尾文档）

| Step | 产出 | 文件 | commit |
|---|---|---|---|
| 7 前端 | `api/media.ts`(submitVideo/getTask/listTasks/uploadRefImage+fetchVideoBlob 带 token 拉 blob 转 objectURL，下载端点 @RequirePermission 需 auth header) + `VideoGenView.vue`(文生/图生切换+参考图上传+duration/resolution 选择+3s 轮询+`<video>` 播放+历史列表+移动端 useBreakpoints) + router `/video-gen` + Sidebar 菜单 4 层权限显隐 | `frontend/src/api/media.ts`、`frontend/src/views/VideoGenView.vue`、`frontend/src/router/index.ts`、`frontend/src/components/Sidebar.vue` | 623ee0e0 |
| 后端单测 | ownership 硬过滤 + videoUrl 不暴露 Ark 临时 URL(9 用例)；状态机全分支 + usage 真值/费率估算两分支 + 下载失败(6 用例) | `backend/.../media/service/MediaGenQueryServiceTest`、`MediaGenTaskWorkerTest` | cde464ed |
| 收尾文档 | README(用户地图+技术) + FeatureMap(V54 表 SQL 注解+调用链) + UserOps(操作手册) + 测试方案(联动用例) | 见下文件清单 | 本轮 |

**前端验证**：`npm run build`(vue-tsc+vite) 绿；`VideoGenView` chunk 7.44kB；`npm test` 105 测试零回归。

**后端验证**：`mvn -o test -Dtest='MediaGenQueryServiceTest,MediaGenTaskWorkerTest'` 15/15 绿。后端 src 本轮零改动（仅新增测试），无回归。

**4 层权限显隐落地**：① Sidebar 菜单 `v-if="canGenVideo"`(`hasPermission('media:gen')`) 无权限看不到入口；② 页内 `canGen` 不满足显 `n-empty`「无权限」；③ 后端 `@RequirePermission("media:gen")` 403 兜底防绕过 URL 直访；④ 路由 meta 仅 `requiresAuth`（平台惯例不按权限卡路由）。

**关键设计决策（Step7 落地版）**：① 下载端点带 auth header → `<video src>`/`<a download>` 无法携带 → 前端走 `fetchVideoBlob`(axios 拦截器自动注 JWT) 拉 blob 转 `URL.createObjectURL`，卸载/换任务时 `revokeObjectURL` 释放；② 参考图复用 `POST /api/files/upload` 单一咽喉点拿 `fileId`，submit 带 `refFileId`，不在视频端点重做 multipart；③ 轮询 3s 间隔，终态(SUCCEEDED/FAILED/DOWNLOAD_FAILED)停轮询并刷新历史，网络错误依赖 request.ts 熔断(连续 5 次跳登录)防轮询风暴。

## 下轮待办

- **provider status 映射单测**（唯一单测缺口）：`ArkSeedanceProvider.parseQueryResult/mapStatus` 私有 + WebClient 内建，需引 MockWebServer(okhttp) 依赖或抽 parse 为包级可见再测。优先级低（纯解析，已被 worker 间接覆盖）。
- **Phase4 真 Ark 端到端**（人工依赖）：Ark 账号已开通 SeedDance 2.0，curl 任务端点 200 → 提交→轮询→播放→下载；10 并发压测轮询 QPS；杀进程重启崩溃恢复；本地盘占用。
- 提交后人工配置 `application.yml` `media:` 段（均可省用默认）+ 确认 doubao provider 配置 Ark key/endpoint/seedance 模型。

## 部署/配置必做（落地后）

- `application.yml`(或环境变量)新增 `media:` 段(均可省，用默认)：`gen-enabled/max-duration/max-res/poll-ms/lock-minutes/task-timeout-seconds`。
- Flyway 启动自动跑 V54；确认 `media:gen` 仅 admin 有(普通 user 按需授)。
- doubao provider 须已在 LLM 供应商配置(Ark key+endpoint+models 含 seedance 模型)。
