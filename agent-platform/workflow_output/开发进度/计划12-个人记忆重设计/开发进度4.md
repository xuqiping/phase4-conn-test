# 计划12 · 个人记忆重设计 · 开发进度4

> 迭代 C（生成写入）。plan：[计划12-C-生成写入.md](../../../项目工程文档/计划/计划12-C-生成写入.md)。主索引：[计划12-个人记忆重设计-实现开发计划.md](../../../项目工程文档/计划/计划12-个人记忆重设计-实现开发计划.md)。
> 规范：[Phase3.1_开发实现速查.md](../../../../2_编程类可迭代workflow/Phase3.1_开发实现速查.md)。

## 本轮做了什么（迭代 C 全量落地，5 chunk · 54 单测 + 6 IT 实跑全绿）

L0/L1/L2 生成 + 写入链 + gen 开关 + 前置过滤。A/B/I1 已就绪供 C 用。C 拆 5 chunk（Phase3.1 铁律#2 禁大块单体），每 chunk TDD（red→green→commit）。

### C-2 前置过滤（commit c7905733）
- 新 [MemoryPrefilter](../../../backend/src/main/java/com/superprogrammer/chat/service/internal/MemoryPrefilter.java)：INPUT/OUTPUT 各侧独立判（过短/纯标点/语气词/纯确认/致谢/套话/免责），**敏感黑名单六类预置不可清空**（密码/支付/验证码/身份证/银行卡/token），用户加项 `memory.prefilter.blacklist-extra`。
- `FilterResult.bothSkipped/anySkipped` 供下游决策（双侧跳不调 LLM 不写 raw / 单侧跳出未跳侧 / 双侧未跳一次出双方三层）。
- SystemSettingService 加 `getMemoryPrefilterBlacklistExtra/update` typed 访问器。
- TDD：MemoryPrefilterTest **21 绿**（Mockito，按侧独立+黑名单大小写不敏感+用户加项+组合态）。

### C-3 gen 开关判定（commit 825640fb）
- 新 [MemoryGenToggleService](../../../backend/src/main/java/com/superprogrammer/chat/service/internal/MemoryGenToggleService.java)：非项目读 `rag.memory.gen.personal.enabled` 兜底（默认 true）；项目 = owner 表 AND 会员覆写表皆默认 true（无行=未显式关=开），任一关 → 只写 raw（gen_done=false）。
- 新 MemoryProjectSettingMapper / MemoryProjectUserSettingMapper（BaseMapper，查询走 wrapper）。
- SystemSettingService 加 `RAG_MEMORY_GEN_PERSONAL_ENABLED` key + typed 访问器；RagMemorySettingsVO/UpdateRequest 加 `genPersonalEnabled`；SystemSettingController GET/PUT 透出+更新。
- TDD：MemoryGenToggleServiceTest **8 绿**（非项目兜底/owner关/会员覆写关/双开/null字段默认/owner无行）。

### C-4 记忆生成器（commit e3b444a7）
- 新 [MemoryGenerator](../../../backend/src/main/java/com/superprogrammer/chat/service/internal/MemoryGenerator.java)：一次 LLM 出双侧三层（L0 subject:topic+label / L1 / L2），各侧独立（被前置过滤跳过的侧不送 LLM）。
- **applyClean 兜底**：strip fence + 截首`{`+截尾`}`，容忍 LLM 前后塞文字；schema 校验每侧须非空 topic+label；3 次重试 temperature=0；全失败 → null（写 raw 降级）。
- **prompt 注入防护**（向量12）：用户原文 `<memory_data>` 包裹 + 声明「数据非指令」。
- TDD：MemoryGeneratorTest **8 绿**（双侧/单侧/脏JSON fence/非JSON重试/null topic/异常/bothSkipped/L2缺失容忍）。

### C-5 写入链编排（commit daf94b60）
- 新 [MemoryGenerationService](../../../backend/src/main/java/com/superprogrammer/chat/service/internal/MemoryGenerationService.java)：`processTurnAsync`（memoryTaskExecutor 异步 fire-and-forget，队列满降级日志）+ `processTurn`（同步，包可见供 IT）。
- 编排链：prefilter → toggle → generator → tagResolver → turnMapper.insert；两侧均跳 → 0 写入；gen 关/失败 → 写 raw gen_done=false；生成 turn 保留 raw_content（溯源/12h 用）。
- **写入目标与召回解耦**：born_personal 定死（勾个人→true / 仅项目→false / **卸空转 true**）；非项目会话恒个人 + projectIds 清空；project_ids 叠加共享。
- 缓存 evict：写入后立即 `evictUser(userId)`（向量9）。
- TDD：MemoryGenerationServiceTest **11 绿**（Mockito 全分支）+ MemoryGenerationWriteIT **6 绿实跑**（PG16，**验证 BaseMapper.insert 把 Long[] 正确序列化 bigint[]** 即 V33 相邻坑 + born_personal 矩阵 + 两侧跳 0 写 + 一侧跳单写）。

### C-6 流水账控制器（commit 5ffcaef5）
- 新 [MemoryTurnController](../../../backend/src/main/java/com/superprogrammer/chat/controller/MemoryTurnController.java)（`/api/chat/memory/turns`）：`GET /raw` 列本人 raw（gen_done=false，无导出）+ `DELETE /{id}` 软删（作者本人）+ `POST /raw/batch-delete` 批量软删。
- 向量7 IDOR：{id} ownership 校验，不存在/无权统一 NOT_FOUND（防存在性探测）；向量13 批量 wrapper 强制 user_id=self 返实际有权条数；向量15 审计 log.info。
- 新 DTO：MemoryRawView / MemoryRawBatchDeleteRequest。
- 12h 规则 + summary 级联（§3.8）留 E（summary 层未建）。
- TDD：MemoryTurnControllerTest **6 绿**（Mockito+SecurityContext，listRaw/不存在/非owner/owner软删/批量实数/未登录）。

## 偏离裁决（记）
1. **新建 MemoryGenerationService（非改 MemoryService.java）**：legacy MemoryService 1278 行基 `user_memories` 旧表；新链基 `memory_turns` 新表。混入 = god-class + 新旧纠缠。隔离 = testability + H 收尾 legacy 整块废（设计§9）。
2. **新建 MemoryTurnController（非改 MemoryController）**：legacy MemoryController 是 `/api/chat/memories`（plural，H 收尾 404）；新流水账走 `/api/chat/memory/turns`（同 MemoryTagController 命名空间）。

## 延后项（记）
- **ChatSessionService 活流程接线**：`processTurnAsync` 机制已验证（async_submitsToExecutor + runnableDelegates 单测），但未接入聊天流——需 feature flag（避免与 legacy MemoryService 双写）。编排器已就绪可调，接线是薄一步。
- **写入目标独立端点**（MemoryWriteTargetRequest）：UI 耦合（弹确认/持久化默认选择），延后 F（前端）迭代做；C-5 service 已接收 writePersonal+projectIds 参数。

## 测试结果
- 5 chunk 单测合计 **54/54 绿**（合并跑 BUILD SUCCESS）。
- MemoryGenerationWriteIT **6/6 实跑绿**（PG16 + pgvector，env `DB_PASSWORD`+`JWT_SECRET` 来自 `agent-platform/local-dev-env.ps1`；跑法 `-Dsurefire.excludedGroups= -Dtest=MemoryGenerationWriteIT`）。
- 全程 TDD：每 chunk 先写测试看 RED → 最小实现看 GREEN → commit 存档。

## 沉淀规范
- **BaseMapper.insert 走 `@TableField(typeHandler=...)` 正常**（计划12C IT 实跑验证）：与 V33 教训的 `LambdaUpdateWrapper.set` 不读 typeHandler **不同**，insert（实体 `autoResultMap=true`）会正确序列化 Long[]/TEXT[]。结论：**写入用 insert 或 XML 显式 typeHandler；更新特型列不用 LambdaUpdateWrapper.set**。已入 [通用约束.md](../项目规范约束/通用约束.md)。

## 下一步
- **D 召回 pipeline（七步）**（依赖 A+B+C+**I1 已就绪**）：过滤→聚合标签→LLM 选标签→优先读总结→reflect→拼流水账→装配。C 写入产物就绪可供召回读。
- **E 周期总结 worker + 冲突 pending**（依赖 A+B+C+I1）：含 §3.8 12h 规则 + summary 级联（C-6 turn 删除的级联在此补齐）。
- 建议先 D（召回闭环可 preview 验证 C 写入的产物）。
