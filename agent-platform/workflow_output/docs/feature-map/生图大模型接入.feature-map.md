# 生图大模型接入 · Feature Map

> 代码-功能速查表。两模型：Seedream 5.0 lite（≤14 参考图/组图 max15/联网/stream/2K3K4K+WxH/standard）+ 5.0 pro（≤10 参考图/无组图联网流式/2K3K+WxH/guidance[1,10]/standard+fast）。
>
> **架构关键**：图片 API 是**同步**（POST 一次返 data[].url），与视频异步 create/poll 不同。故 `ArkImageProvider` 是独立 `@Component`（不实现 MediaGenProvider），worker 按 task_type 分流——视频路径零回归，图片路径并列。

## 后端代码文件

| 文件 | 作用 | 关键点 |
|---|---|---|
| `db/migration/V84__image_gen_result_meta.sql` | 加 result_meta 列 | `ALTER TABLE media_gen_tasks ADD COLUMN result_meta JSONB`。存 `{imageFileIds[],generatedImages,outputTokens}`。task_type 列无 CHECK（V54 TEXT），新值 TEXT2IMAGE/IMAGE2IMAGE 直接可用。**无 provider/价表 seed**——admin 自填 |
| `media/provider/ArkImageProvider.java` | 同步调 ctaigw 图片端点 | `generate(MediaImageRequest)→MediaImageResult`。WebClient POST `{endpoint}`，body 按模型 manifest 裁剪；response_format=url/stream=false 固定。`buildBody`/`parseResult` 包级可见（单测直测）。AES key 复用 `llmProviderService.getDecryptedApiKey` |
| `media/config/ImageModelCapability.java` | 单模型能力 manifest（@Data @Builder） | 字段：refImageMax/refImageFormats/sizePresets/supportsWhSize/supportsSequential/maxSequentialImages/supportsWebSearch/supportsStream/outputFormats/optimizeModes/supportsGuidanceScale/guidanceMin/guidanceMax/watermarkDefault |
| `media/config/MediaModelCapabilityService.java` | 能力默认+覆盖 | `resolveImage`/`defaultsForImage`（seedream+lite→14ref/2K3K4K/seq15/webSearch/stream；seedream+pro→10ref/2K3K/guidance[1,10]/standard+fast）/`applyImageOverride`（provider config JSON 精确覆盖） |
| `media/service/MediaModelService.java` | 模型目录聚合 | `listImageModels()` 跨 IMAGE provider 聚合（一次查内存组装，无 N+1）；`resolveImageProviderByModel` 反查；`listImageProviders` |
| `media/service/MediaGenTaskService.java` | 提交+校验 | `submitImage`：requireAffordable 预检→`validateImage`（不支持参数拒/参考图张数 MIME/size 合法性/pro 塞 sequential→400）→task_type 分类（无参考图=TEXT2IMAGE/有=IMAGE2IMAGE）→request_config JSONB→insert PENDING。`isValidSize`（preset 或 WxH 正则）、`checkImageRef`（cap+归属） |
| `media/service/MediaGenTaskWorker.java` | IMAGE 分支处理 | `process()` 按 `isImageTask(taskType)` 分流（视频零回归）。`processImage`：同步 generate→`handleImageSucceeded`（逐张 downloadImageAndStore→imageFileIds→buildImageResultMeta→`chargeMedia(KIND_IMAGE,imageCount=generated_images)`→markImageSucceeded，失败 refundMedia）。`buildImageRequest`（requestConfig→请求体+refFileIds→dataURI） |
| `media/service/MediaStorageService.java` | 图片下载落盘 | `downloadImageAndStore(url,userId,nameHint)`：64MB `imageDownloadClient`，`URI.create` 避双编码，storeStream(SOURCE_MEDIA)。`buildImageDownloadClient`（64MB 缓冲，区别视频 16MB） |
| `media/service/MediaGenQueryService.java` | 查询+下载+入库咽喉 | `toVO` IMAGE 分支（SUCCEEDED+mayAccessFile→imageUrls=`/api/media/tasks/{id}/images/{i}/download`+generatedImages+outputTokens）。`loadImageForImport(id,idx,userId,admin)` 单一咽喉（归属+SUCCEEDED+idx 越界）→`ImageImportContext(task,fileId)`。`readImageFileIds` 从 result_meta 取。**第二轮**：list 7 参加 `kind` 白名单（IMAGE/VIDEO/null，非法 400，normalizeKind）+ 6 参兼容重载；详情透出 `submittedRequest`（request_config 剥 providerRequestSnapshot+data URI 脱敏，仅 get 单查，列表不透防膨胀——视频第三轮 55f2ed48 落地，图片复用） |
| `media/mapper/MediaGenTaskMapper.java` | 历史筛选 SQL | `selectHistory(userId,admin,query,from,to,limit,kind)`：kind=IMAGE→`task_type IN ('TEXT2IMAGE','IMAGE2IMAGE')`，VIDEO 同理，null 全量。**过滤在 SQL 层**（前端先 LIMIT 再内存过滤会行数不足/仍混杂）；kind 只进 `<if>` 等值比较无注入面 |
| `media/controller/MediaGenController.java` | REST | `POST /api/media/image`（submit）、`GET /api/media/image/models`（模型目录）、`GET /tasks`（q/from/to/limit/**kind**）、`GET /tasks/{id}`（含 submittedRequest）、`GET /tasks/{id}/images/{idx}/download`（流式+Content-Disposition）。全 `@RequirePermission("media:gen")`。`serveFile` 私有 helper（视频/图片共享） |
| `media/dto/ImageSubmitRequest.java` | 提交请求体 | model/prompt/refFileIds/size/outputFormat/watermark/guidanceScale/optimizeMode/sequential/maxImages/webSearch |
| `media/dto/MediaTaskVO.java` | 查询响应（加图字段） | imageUrls/generatedImages/outputTokens/size/outputFormat |
| `asset/service/AssetMediaBridgeService.java` | 生成→库桥 | `importFromMediaTask`：requireWrite(目标项目)→loadImageForImport（媒体咽喉）→建 IMAGE 资产 v1（fileId 复用不拷贝）+genMeta.source=MEDIA。**无重复检测**（同一图可多次入库独立资产）。跨包单向依赖：asset 只读 media，media 不 import asset |
| `asset/controller/AssetMediaBridgeController.java` | 入库 REST | `POST /api/assets/from-media` `@RequirePermission("asset:write")` |
| `asset/dto/MediaImportRequest.java`/`MediaImportVO.java` | 入库请求/响应 | taskId/imageIdx/projectId/name/description；VO created 恒 true（无三态） |

## 前端代码文件

| 文件 | 作用 | 关键点 |
|---|---|---|
| `views/ImageGenView.vue` | manifest 驱动动态表单 + 历史区 | 模型 select→`cap` computed→条件渲染控件。参考图 AssetFilePicker。提交→pollOnce 2.5s→ensureImages→网格+下载/入库。**第二轮历史区**：筛选条（q+时间范围，300ms 防抖+requestSeq 竞态守卫+kind=IMAGE 服务端过滤；NDatePicker 须 `close-on-select + update-value-on-close`——默认 actions 含 confirm 时点击日期只暂存 pendingValue 不提交 v-model；to 换算 `range[1]+24h-1ms` 含结束日整天，否则同日区间 from==to 后端 400）；行右侧首图缩略（MediaTaskImageThumb）；点历史先 getTask 详情→`parseImageRestore` 逐项直赋 form（**不调 onModelChange** 防重置吞值）+参考图逐张 fetchFilePreview 回填+模型下线 NAlert；结果区挂 MediaTaskRequestDetails 核对提交参数 |
| `utils/imageGenParams.ts` | 历史参数还原引擎（纯函数） | `parseImageRestore(task{model,submittedRequest}, models)→patch|null`：size 预设/自定义 WxH（`__custom__` 回填）/不支持自定义回退首预设+告警；参考图超 cap 截断+告警；模型下线值保真+告警；缺省字段回能力默认。9 测 |
| `components/media/MediaTaskImageThumb.vue` | 历史行首图缩略组件 | 与 MediaTaskVideoPreview 同构：IntersectionObserver 入视口才 fetchMediaBlob；卸载/换行 revoke objectURL；版本守卫防竞态；点缩略 emit preview（stop 冒泡不触发行点击）。4 测 |
| `components/media/MediaTaskRequestDetails.vue` | 请求参数弹窗（复用自视频第三轮） | 「平台收到的提交参数」（submittedRequest 含 refFileIds）+「实际发给模型已脱敏」两页签；图片路径 worker 不落快照→后者显「未记录发送快照」不冒充。加 `title` prop 复用 |
| `components/imagegen/SaveImageToAssetDialog.vue` | 一键入库弹窗 | 克隆画布 SaveToAssetDialog，去叙事角色/去重复三态。项目下拉 filter OWNER/EDITOR。POST /assets/from-media |
| `api/media.ts` | 图片 API+类型 | listImageModels/submitImage/fetchMediaBlob（token blob→objectURL）。MediaTaskQuery 加 `kind?: 'IMAGE'\|'VIDEO'`；MediaTaskVO 含 `submittedRequest`/`providerRequestSnapshot`（仅详情）。MediaTaskType 加 TEXT2IMAGE/IMAGE2IMAGE |
| `api/assets.ts` | 入库 API | `assetBridgeApi.importFromMedia` |
| `types/asset.ts` | 入库类型 | MediaImportRequest/MediaImportVO |
| `router/index.ts`+`components/Sidebar.vue` | 路由/菜单 | `/image-gen` + 菜单项（media:gen gated） |

## 关键调用链

```
提交：ImageGenView.onSubmit → mediaApi.submitImage(POST /media/image)
  → MediaGenController.submitImage → MediaGenTaskService.submitImage
  → requireAffordable + validateImage + insert PENDING
  → worker processImage（mediaTaskExecutor 线程）
  → ArkImageProvider.generate（同步 POST ctaigw）
  → handleImageSucceeded → 逐张 downloadImageAndStore → result_meta
  → chargeMedia(KIND_IMAGE, generated_images) → markImageSucceeded

查询：pollOnce → GET /tasks/{id} → MediaGenQueryService.toVO(IMAGE分支)
  → imageUrls[] → 前端 ensureImages fetchMediaBlob 逐张

入库：图片格「入库」→ SaveImageToAssetDialog → POST /assets/from-media
  → AssetMediaBridgeController → AssetMediaBridgeService.importFromMediaTask
  → requireWrite + loadImageForImport → 建 IMAGE 资产 v1（fileId 复用）

参考图：AssetFilePicker（资产库选图）→ refFileIds → buildImageRequest 转 dataURI

历史（第二轮）：
  列表：loadHistory → GET /tasks?kind=IMAGE&q=&from=&to= → selectHistory SQL 层过滤
  缩略：行滚入视口 → MediaTaskImageThumb → fetchMediaBlob(imageUrls[0]) → objectURL（卸载 revoke）
  还原：点行 → GET /tasks/{id}（submittedRequest 仅详情透出）
    → parseImageRestore 逐项直赋 form（绕开 onModelChange）+ fetchFilePreview 回填参考图
  核对：「请求参数」→ MediaTaskRequestDetails 弹窗（提交参数页签含 refFileIds）
```

## 表与 SQL 注解

- **media_gen_tasks**（V54 建表，V84 加列）：图片任务复用此表。`task_type` 新值 TEXT2IMAGE/IMAGE2IMAGE；`result_meta` JSONB 存 `{imageFileIds[],generatedImages,outputTokens}`（视频行留空，避免 N 列）。索引复用 V54（user_id,status,ark_task_id）。
- **stored_files**：逐张图下载落 source=MEDIA 行，fileId 存入 result_meta.imageFileIds。入库时复用此行（不拷贝）。
- **llm_providers**：IMAGE 类 provider 行（admin 自建，category=IMAGE）。**无 seed**。
- **pricing_rule**（V66 建表）：两模型 price_per_image（admin 配，**无 seed**）。
- **Flyway 规则**：V84 加列附 drop 回滚；已执行脚本不可改，改结构加新版本。

## 计费原理（大白话）

图片按**张**收费（区别视频按时长/Token）。`chargeMedia` 的 `imageCount` 参数 = Ark 返回的 `usage.generated_images`（实际生成张数），不是请求数——组图 4 张就算 4 张。后扣（生成完才扣），允许瞬时欠款，下次提交 requireAffordable 预检拦截。缺价表 → PRICING_NOT_FOUND 被 MediaBillingService 吞 → usage 记 FAILED 但不阻生成。

## 2x 第三轮增补（2026-08-16，C3：每用户生成并发闸，31a8e2a6）

| 文件 | 作用 |
|---|---|
| `media/service/internal/MediaInflightGateService.java`（新） | Redis INCR `inflight:media:{type}:u:{userId}` 原子预占 + 首占 EXPIRE 30min（进程崩溃泄漏自愈归零）；超限抛 `MEDIA_CONCURRENT_LIMIT 42904`（429，内部已回退计数）；`release` 终态释放，floor 0 |
| `MediaGenTaskService.submit/submitImage` | requireAffordable 之后 tryAcquire；既有失败 catch 路径补 release |
| `MediaGenTaskWorker` | 终态（SUCCEEDED/FAILED/DOWNLOAD_FAILED）仅状态迁移真正发生才 release（`UPDATE ... WHERE status IN(PENDING,RUNNING)` 影响行=1 才放）——防重试路径重复 DECR 超卖 |
| `db/migration/V123__media_inflight_index.sql` | `media_gen_tasks(user_id,type,status)` 部分索引（每小时对账 Redis 计数 vs DB count，差>2 warn） |

- 上限读 `system_settings`：`media.concurrent.video`（默认 2）/`media.concurrent.image`（默认 3），管理员后台运行时可调不重启（D5 拍板）；Redis 异常 fail-open + warn（同 RateLimiter 语义）。
- 15 单测（上限拒绝+回滚/video-image 独立计数/TTL 自愈/热改配置生效/Redis 停机降级/重复终态不二次放）。
- 前端配合：画布批量生成滑动窗口 2 + 429 指数退避，见 [无限画布创作页.feature-map.md](无限画布创作页.feature-map.md)。
