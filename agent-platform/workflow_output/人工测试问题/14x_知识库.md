# 已解决（2026-08-14）

## 1. 为什么rag检索要这么久 —— 已解释 + 两处真实性能修复

**耗时构成（Phase4/14x 实测，trace 逐段计量）**：

| 阶段 | 实测耗时 | 说明 |
|---|---|---|
| Query 向量化（qwen3-vl-embedding） | ~300ms | 必要开销 |
| 混合召回 + PG 复核 | ~150ms | 含权限/版本/Hash 复核 |
| qwen3-rerank 重排 | ~250ms | 专用模型，快 |
| **小计：纯检索** | **~0.7-2s** | 「检索调试」Tab 即此量级 |
| 事实提炼（glm-5.1） | 4-17s | **大头**：两次 LLM 生成调用之一 |
| 答案合成（glm-5.1） | 1-4s | |

**"久"的不是检索，是 Grounded Answer 的两次 LLM 调用**；且为保证引用经过校验，页面等完整答案生成后一次显示（不逐 token 展示未验证文本），体感更久。

**本轮顺带修掉的两个真实延迟/失败问题**：
1. 事实提炼 maxTokens=1200 被截断 → JSON 断尾报「未返回合法 JSON」（`RagConfig` 放宽至 3000）；
2. 无长度约束时 glm-5.1 对长证据逐条复述 → 输出 1200+ token，生成 >30s 撞超时（提炼提示词加强精简约束：只输出 JSON、value≤60字、只提关键事实）。修复后同类问题 4-8s 返回。

**想快的路径**：「检索调试」Tab 只跑检索不生成答案（~1-2s）。

## 2. 还需要在智能对话中被引用 —— 已实现（前端补齐，后端本就绪）

后端链路（`ChatRequest.kbIds` → 会话持久化 → RAG 检索证据注入 → CITATION 帧）Phase3 就绪，本轮补前端：
- 智能对话顶栏新增「知识库」多选下拉（联网搜索开关旁，localStorage 持久化）；
- 选中后每条消息带 kbIds，回答带 📎 引用来源（编号/标题/下载原件）；多选与可读权限在后端求交。
- 浏览器实测：选中库问 P0 上报 → 回答正确引用 [1]《服务器值班制度.txt》。

## 3. 直接输入内容进知识库（非上传）—— 已实现

文档管理新增「直接输入文本入库」按钮 + 弹窗（标题可选 + 正文 ≤4000 字）：
- 复用 `indexMode=MANUAL` 链路，正文作为唯一索引内容，构造内存 .txt 留档原件（可下载）；
- 浏览器实测：输入《服务器值班制度（试行）》→ INDEXED → RAG 问答正确命中并引用 doc#27。

## 4. 支持哪些文件上传 —— 已回答 + 修复白名单不一致

- **结构化解析**：`.md/.markdown`（按标题切节点）、`.txt`、`.pdf`（引用带页码）、`.docx`、`.xlsx/.xls`（按数据行切节点/可选Sheet）、`.html/.htm`、图片 `.png .jpg .jpeg .gif .webp .bmp`（AUTO=视觉识图 / MANUAL=手填文本）。
- **通用兜底**（Tika，当 FILE 处理）：`.csv .json .ppt .pptx .srt .vtt` 等白名单内全部类型。
- **修复**：前端 accept 曾列 `.html/.markdown` 但后端白名单没有 → 必被拒。已把 `markdown/html/htm` 加入上传白名单（下载侧仍强制 attachment+nosniff），实测 html 上传→INDEXED 通过；前端 accept 同步补齐。

## 5. L0 摘要和原文一样？L1/L2 是什么？参与检索吗？ —— 已解释（数据库实证）

三层语义（以差旅制度文档实测）：
- **L1 = 文档级摘要**：`SUMMARIZING` 阶段 LLM 生成，存 `knowledge_documents.l1_metadata`（summary/outline/importantRules）。你在检索调试看到的浓缩摘要就是它。**参与检索**（L1 摘要向量化粗筛 + 注入上下文）。
- **L0 = 章节级节点（SECTION）**：承载章节标题+聚合内容，用于结构层级与引用定位（如「第一章 报销标准」）。**当前不单独向量化**（实测 embed 数 L0=0）。
- **L2 = 段落级原文（PARAGRAPH）**：**核心检索单元**（实测 embed 数 L2=5/5，向量检索全部落在 L2）。

**为什么看起来 L0=原文**：本文档每章恰只有 1 个段落 → 章节聚合内容 = 该段落原文，属结构重合而非摘要失效；真正的摘要在 L1 层（对比 l1_metadata 的浓缩 summary 即可确认）。章节含多段落的大文档中 L0 会是整章聚合。

## 6. 一个问题涉及多条答案会怎样？能全召回吗？ —— 已验证（浏览器实测）

QueryPlan 按问题类型动态定证据预算：精确题 ~2 条、流程题 ~10 条、**列表/比较题最多 20 条**，且限制单一文档挤满上下文（来源多样性）+ 缺主题时补检索 1-2 轮（继承原权限/版本范围）。
实测「列出全部条款」：三章全部召回，逐条回答（机票/酒店/餐补/审批/时限/生效），引用 [1][2][3]，并自动做冲突核查说明（无冲突）。

## 7. 各反馈类型提交后会发生什么？ —— 已解释（代码实证）

提交 → `PENDING` 入待审核队列（**不直接改变线上排序/召回**）。管理员在反馈审核里一次性裁决：
- **通过（APPROVED）**：成为**黄金集候选**，供评测数据集（Recall/MRR/nDCG 与发布门禁）使用——影响的是下一轮 Pipeline 评测与灰度，不是即时排序；
- **拒绝（REJECTED）**：留档，不进黄金集；
- 状态只能从 PENDING 变更一次，防重复审核。
四种类型的语义：结果不相关（召回质量问题）/ 内容已过期（数据治理，提示更新文档版本）/ 引用错误（Citation 校验问题）/ 答案不完整（证据预算/补检索问题）。

- ~~知识库里不能选择embedding模型和llm模型~~ ✅已修待复测——建库/编辑弹窗两下拉：Embedding（启用 EMBEDDING 列表+空=管理员默认）、问答模型（启用 CHAT 列表+空=跟随全局默认，V131 answer_model）；下线自动回退全局默认；换 embedding 已有文档强提示重建索引（见[开发进度](../开发进度/知识库模型选择与保密权限/开发进度1.md)）
- ~~授权给别人读的权限，别人也能进行编辑~~ ✅已修待复测——谓词档位严格化：canRead 仅读、canWrite 才可传、canManage 治理；改名/删库仅 OWNER/admin；前端按钮同步显隐（commit 16d4ae5d/390b78af）
- ~~需要解决别人全量获取知识库内容（知识库是我的保密文件，只能召回所有涉及到的相关内容）~~ ✅已修待复测——库级「保密库」开关（V131 confidential）：成员列表剔原件链接、asset/nodes/检索调试全 403，RAG 问答=唯一内容出口且引用不带下载钮；OWNER/admin 不受限；PUBLIC 互斥（commit 3752bb16/3bce17f3）

# 未解决
- 有些文件、图片等可以作为被召回的附件，即进知识库的时候就是整个文件进去（需要有描述，来决定什么时候将这个文件被召回，图片同理），目前有这个设计吗？还是只有解析文件、图片，召回文件内容片段
- 在添加知识库内容的时候，可以有个关联选项，关联其他文档后，可以选择必须引用，按需引用，必须被引用，按需被引用是否效果会更好？如果更好，需要关注以下几点：
    - 二、四种关系的落地语义：
        - 必须引用：命中A文档时B强制一起进上下文（硬绑定、打包召回）。场景：术语表、合规声明、前置背景说明。
        - 按需引用：B作为候选进入rerank，由检索器判断相关性后带上，相当于默认行为的显式化。
        - 必须被引用：反向约束，只要B被召回，A必须跟着出现。场景：免责条款、版权说明、司法解释跟着法条走。
        - 按需被引用：弱关联，作为"相关文档"推荐露出，不进主上下文。
    - 三、检索流程改造点：在召回后加一层"关系图后处理"——命中文档后遍历边表，强制类直接注入prompt，按需类进rerank队列。token预算不足时优先级：必须引用 > 原始命中 > 按需引用。
    - 四、必须防的坑：
        - 循环引用：A必须引用B且B必须引用A，去重即可；链式传导（A→B→C）建议限1跳，否则token爆炸。
        - 权限穿透：被强制带出的文档可能超出当前用户权限，注入前要做权限校验。
        - 关系维护成本：图谱会随文档增多膨胀，需要"关联推荐"辅助（根据共召回记录自动建议关联）。

# 参考feature-map、user-ops
- agent-platform\workflow_output\docs\user-ops\企业级精准知识库RAG用户操作手册.md
- agent-platform\workflow_output\docs\feature-map\企业级精准知识库RAG.feature-map.md

>解决完后已同步更新对应的feature-map和user-ops（2026-08-14）

# 修复设计规格（2026-08-17，未解决项已规格化）
- [知识库模型选择与保密权限设计.md](../docs/specs/知识库模型选择与保密权限设计.md) —— embedding/问答LLM 下拉可选；只读越权根因（hasGrant 谓词 `canRead‖(requireWrite&&canWrite)`）修复；拍板：库级保密开关（成员仅 RAG 问答召回，禁切片原文/下载原件/检索调试旁路）
- [知识库RAG检索能力增强设计.md](../docs/specs/知识库RAG检索能力增强设计.md) —— 2026-09-03 未解决项 #1（附件式整文件召回 C2）/#2（文档关联关系 C1）+ 顶级对比差距五项（C3 多轮检索/C4 上下文嵌入升级/C5 多模态/C6 连接器/C7 全局问答）七项一体规格化；用户拍板：七项全做、附件=描述召回+按需内容注入、关联四种全做限1跳、contextual 新文档生效+存量可选重建
