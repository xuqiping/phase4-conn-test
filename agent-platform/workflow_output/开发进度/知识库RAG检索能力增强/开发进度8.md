# 知识库RAG检索能力增强 · 开发进度8

对应 plan：[WP5_多模态](../../docs/plans/知识库RAG检索能力增强_WP5_多模态.plan.md)｜上轮：[开发进度7](开发进度7.md)

## 本轮：WP4 收口（Step3 9c7f08d3 + Step4 385ecd55，详见开发进度7）

WP4 全局问答四步全毕：L-KB 数据层/Worker → GLOBAL 分类+map-reduce → 混合跟进轮（JDK `\b` Unicode 判词坑：无空格「V2.1第十条」版本锚点静默漏提→环视替代）→ 前端可见性（调试面板 GLOBAL 标识条+chat 📄 文档级引用跳转）。后端 2865/2865，前端 1096/1096+vue-tsc 0。

## 本轮：WP5 Step0 原件生命周期核实（docs-only 01ba1bb）

`IndexJobWorker.cleanOriginalFileAfterIndex`（:248-272）：IMAGE/FILE docType **明确跳过清理**（原件=回显资产必须保留，:257-259）；其余 docType 受 `app.files.retain-after-index`（默认清）。**结论：IMAGE 原件永久保留至文档删除，IMAGE 向量 job 任意阶段读原件安全，无时序风险。**

## 本轮：WP5 Step1 多模态 embed 协议扩展（commit 2ffe581f，provider 28/route 8/gateway 25 绿）

- **基建复用**：多模态协议探测（端点含 `/multimodal-embedding/`）、DashScope 形制（contents 数组+enable_fusion+2048 维校验+/output/embeddings 解析+input_tokens usage）**已在既有代码半落地**（文本-only contents）——Step1 只补图段与出口。
- **`EmbedContentPart`**（新 DTO）：text/image 二段工厂；image=URL 或裸 Base64（DashScope 口径，不带 data: 前缀，原样透传）。
- **`LlmProviderInterface`** 默认多模态 `embedWithUsage(List, model)`：不支持即抛 UnsupportedOperationException（调用方失败关闭禁 IMAGE 通道，不回落文本）。
- **`OpenAICompatibleProvider`** contents 重载：多模态端点→text/image 段按序组装 contents+fusion/2048；**普通 /embeddings 端点+图片段立即拒零 HTTP**（协议不同形不盲试）、纯文本段拼接回退既有单串；响应解析两协议共用抽 `parseEmbedResult`；**熔断**：多模态失败一次→静态表 provider|model→openUntil 开断 10min fast-fail（进程级，自动恢复，防不可用模型反复打挂索引链路）。
- **`LlmGateway.embedMultimodal`**：路由（EMBEDDING 行）/计费（KIND_EMBED 归户 owner，SUCCESS 真值/ESTIMATED 文本估算——图片段不参与估算兜底）/trace（IMAGE_EMBEDDING）全同 embed() 口径。
- **坑（新）**：接口加 List 重载后，既有 5 处 mockito `embedWithUsage(any(), any())` **重载歧义编译炸**（any() 无法在 String/List 间定靶）→ 收紧 `anyString()`。
- 测试 +3：contents 拼装融合（两段按序透传+fusion+2048+usage 13）/普通端点拒图（IllegalArgumentException+`server.getRequestCount()==0` 零请求）/熔断 fast-fail（500 后第二笔被拦，请求数恰 1）。

## 本轮：WP5 Step2 IMAGE 向量索引双写（通道分表 V173+UPSERT_IMAGE job 型，后端全量 2876/2876）

- **关键偏离（plan 实现注已记）**：计划假设 embeddings 单表加 modality 列——实际 schema=**per-model 分表**（knowledge_embeddings_doubao，node_id UNIQUE），加列须动分表+upsert 冲突键；照 **V36 L1 先例改通道分表**：`V173__knowledge_image_embeddings.sql` 新表 `knowledge_image_embeddings_doubao`（document_id UNIQUE，每 IMAGE 文档 1 行图片向量，HNSW cosine+kb 索引）+新 `KnowledgeImageEmbeddingMapper`（upsert ON CONFLICT(document_id)/deleteByDocument）。
- **job 型 `UPSERT_IMAGE`**（doc 级，document_id 锚定）：`KnowledgeNodeWriter.writeNodes` IMAGE 文档且 fileRef 非空即入队（幂等键含 fileRef 指纹 sha256(fileRef)，换图→新 job 接管）；claimBatch/countClaimable 清单补型；`IndexJobWorker.processUpsertImage`：loadPath(归属咽喉点，doc.createdBy) 读原件→裸 Base64→`embedMultimodal`；**失败关闭**：模型不支持图输入（UnsupportedOperation/IllegalArgument）/维度≠2048→voidJob+WARN 不重试（通道自然禁用=无行，文本通道不受影响），暂态异常→failJob 退避；`IndexJobTxService.completeUpsertImage` tx 内复校 docType+fileRef 未换→upsert→DONE（markDocIndexedIfDone 计入→doc INDEXED 需图片向量完成或作废）。
- **modality 透传**：`OpenSearchChunkDocument` +modality 字段（node.modality，V171 列）随 chunk 双写 OpenSearch（动态映射自吸纳，过滤消费在 Step3）。
- **删除联动**：doc 软删（KnowledgeDocumentService.delete）+注入隔离（DocumentParserService QUARANTINED）挂 imageEmbeddingMapper.deleteByDocument；对账不扩（图片行 FK CASCADE+软删显式清双兜底，无 orphan 路径）。
- **坑（新）**：@RequiredArgsConstructor 服务加字段→4 处测试显式构造/@InjectMocks 断裂（ConfidentialGuard/InjectionScan/MetadataGovernance/**@InjectMocks 的 DocumentServiceTest——无显式构造不炸编译只炸运行时 NPE，全量才现形**）逐处补 mock。
- 测试 +8：Worker 4（成功链路裸 Base64 断言/不支持图输入 void 不重试/维度不符 void/fileRef 漂移 void 不 embed）+TxService 2（upsert+可能 INDEXED/换图 void 不 upsert）+Writer 2（IMAGE 入队 doc 级 job 全字段/TEXT 存量永不入队）。

## 本轮：WP5 Step3 IMAGE 检索通道接入 RRF（commit 9ff5a737，后端全量 2879/2879）

- **检索面**：`RagRetrievalQueryMapper.denseRecallImage`——全镜像 L1 SQL 换源 `knowledge_image_embeddings_doubao`（Step2 分表「表即 modality」免过滤列；kb/可见集/docTypes/e.embedding_model=kb.embedding_model 同过滤，cosine 排序）；返回行/记录**双复用** `L1RecallRow`/`L1DocHit`（同为 doc 级锚，零新 DTO）。
- **通道接线**：`multiDenseRecallImage` 多 query half 合并（同 L1 口径）——**单库/多库/补充检索三路径全接**；abstain 空判「l0+l1」→「l0+l1+img」（纯图库不再误拒）；`fuseTopDDocs` 升三参，RRF 同参 k=60，新权重 `rag.recall.rrf.weight-image-vector=0.8`（yml 可调，空通道零影响）。
- **槽位升级不增槽**：`docL1Sim` 升义「doc 级锚 sim = max(L1 元数据向量, IMAGE 图片向量)」（merge Math::max）——L2Candidate 记录零波及，rerank 语义自然收编；img 独中（L0 未进 topM）doc 走 l1OnlyDocs 同路 `fetchL2ChildrenByDoc` 拉 L2，**识图描述节点即证据**（evidence=fileRef inline 现状零改动，fetchL1Metadata 已 JOIN stored_files）。
- **同 node 去重**：L0 已进 topM 的 doc 不入 l1Only（topDDocIds 差集口径）+byNode 按 nodeId 去重现状复用。
- **RrfFusion 不改**：fuseWeighted 通用 List<WeightedList> 天然任意通道数，通道「注册」经 props 权重表达——无注册表需求（plan 原列 RrfFusion 文件，实未触碰）。
- 测试 +3（RagRetrievalServiceTest 21/21）：imageOnlyDoc 三通道独立救回（L0/L1 全空不 abstain+evidence 含识图描述节点）/imageChannel 空仅 TEXT 库回归门（verify 通道已接线+行为不变）/同 doc 双通道（fetchL2ChildrenByDoc 零调用+证据恰 1 次）。Mockito 未打桩 List 方法默认空 → 既有测试零改过。

## 本轮：WP5 Step4 ColPali 接口预留+WP5 收口（commit 见 git log，后端全量 2881/2881）

- **三重闸**：全局 `rag.visual.colpali.enabled`（新 `ColpaliProperties`，spec 钉 key 须 yml 可覆盖→不入常量类 RagConfig，仿 RagShadowProperties）∧ KB 级 `knowledge_bases.colpali_enabled`（**V174**，默认 FALSE；Entity/Request/VO/Service 全接线，update null=不动同 confidential 口径）∧ `healthy()` 探活——`availableFor(kb)` 单点串联。
- **`multimodal/ColpaliGateway`**（单类=接口+骨架，YAGNI 无拆分）：探活 GET /health 2s 超时**冷却缓存 60s**（volatile 双检+synchronized 探测段，期内零重打），失败**自动禁用 WARN 一次**（warnedDown：恢复 INFO 重置，再失败重告警不刷屏不哑）；全局关→恒 false **零出站**；embedPage 骨架 POST /embed base64→patchVectors（MaxSim 打分留 sidecar/OpenSearch 侧，pgvector 不承载多向量）；HTTP java.net.http+测试注入双构造器（SidecarHealthIndicator 先例）。
- **影子对比接线点预留**：javadoc 指向 RagShadowCoordinator challenger 形式+rag_shadow_comparisons 只记录不生效——检索主链零接线零波及。
- 测试 +2：探活失败自动禁用+冷却期 times(1) 零重打/全局关 verifyNoInteractions 零出站；KB 三测试类 24/24 回归过。
- **WP5 收口**：Step0-4 全勾；验证汇总三项全过（单测 **+16**（3+8+3+2）超计划 ~8；手测剧本留 Phase4：传截图问「这个界面哪里改配置」→图被召回等四条；ColPali 仅接口+开关+探测降级，sidecar 部署不在本版）；联动点四条核对表入 plan（换模型重建不含 IMAGE doc 级 job 靠重解析触发/模型下线熔断+失败关闭/C2 双路同 node 去重/删除双兜底）。

## 下一步

WP6 连接器生态（C6）：外部数据源（URL 站点/S3/WebDAV）定时同步进库——SSRF 防护/凭证加密/etag 增量/并发认领；另 WP3 Step4 影子对比仍挂起（等用户给选库+黄金集）。
