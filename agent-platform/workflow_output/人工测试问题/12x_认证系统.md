

# 参考feature-map、user-ops
> **本条目对应文档**：
> - feature-map：`agent-platform/workflow_output/docs/feature-map/认证系统增强.feature-map.md`
> - user-ops：`agent-platform/workflow_output/docs/user-ops/认证系统增强用户操作手册.md`
> - （参考）测试方案：`agent-platform/workflow_output/docs/测试方案/认证系统增强测试方案.md`；原始规格：`docs/specs/认证系统增强设计.md`
> - 现状发信传输只有**阿里云 DM** 一种（`EmailService.sendMail` 走 SingleSendMailRequest），配置面在 设置→认证通道→邮件通道（`system_settings` 键 `auth.channel.mail.*`，密钥 AES 落库、DB 优先 env 兜底）。
> 2026-08-26 修复III（§10 Chunk E 解锁/姓名/UserPicker 批量）已同步增补至：认证系统增强、积分计费系统、日志系统 各 feature-map 与用户操作手册「2026-08-26 增补（修复III）」节。



# 已解决（2026-08-25，2 项）
1. **邮箱+密码登录** ✅（`4c278a98`）—— 此前登录只认用户名。现密码登录框支持「用户名或邮箱」：含 @ 的输入先按邮箱凭证查（大小写不敏感），查不到再按 users.email 列兜底（老账号没建邮箱凭证的也能登），密码与账号相同。安全上：邮箱登录解析出真实用户名后，失败计数/账号锁/滑块统一归并到账号键——攻击者交替用用户名/邮箱两条道也摊不薄计数。
2. **真实重置密码** ✅ —— 链路本来就在（忘记密码→收邮件→点链接→重置页填新密码），真正的坑是：邮箱验证总开关关着时，测试期注册的账号邮箱是「未验证」状态，系统不给未验证邮箱发重置信（防冒领），所以收不到。已放宽：开关关时未验证凭证→users.email 列兜底照发。**重置密码 URL = `{前端域名}/reset-password`**（如 `http://localhost:5173/reset-password`），邮件会自动拼 `?token=...` 跳转到这页。

# 已解决（2026-08-26 勾销，修复设计 §6/§7）
- **用户备注字段** ✅（§6，Q6=A，commit `ae8290e9`）—— 三入口：
  ① 注册弹窗可填（≤64 字，如「A 班」）；② 个人中心「资料设置」可改（≤128 字）；
  ③ 管理员用户管理列表：备注列 + 备注编辑 + keyword 筛选（用户名/姓名/备注任一命中，LIKE 通配符已转义防全表命中）。
- **找回密码用邮箱** ✅（§7，commit `482cd517`）—— 邮箱找回链路本就存在（见上方已解决 #2），本期补齐两个死角：
  重置邮件按钮下方加**纯文本链接 + 复制提示**（QQ 邮箱等客户端拦截按钮跳转时可复制打开）；
  直达 /reset-password 无 token 时不再落到「手机号短信」死路表单——按认证通道开关分流：
  邮件开=引导页「从邮件链接进入/去登录页重发」，邮件关=提示联系管理员（SMS 后端仍未实现，Q8 明确本期不做）。

# 已解决（2026-08-26 勾销，修复设计II §8，Plan C）
1. **验证码间隔与刷新倒计时** ✅ —— 间隔 60s（可配）：刷新页面后按钮**仍是倒计时禁用态**且秒数=真实剩余（localStorage 持久化，按邮箱分 key）；间隔内点发送 → 429「请 N 秒后再试」（N=Redis TTL 真实剩余，非写死 60）且按钮进倒计时；被拒不发第二封、不耗 IP 配额、不触发滑块；短信页同款（C1-C4，`980f77e0`/`7626787c`/`56ca73a4`/`46f5544f`）。

# 已解决（2026-08-26 勾销，修复设计III §10，Chunk E，commit `0d43f74f`）
2. **账号锁定可见+管理员解锁** ✅ —— 锁定 15min（自动）。已锁账号（DB 落 locked_until）登录报错显「账号已锁定，将于 MM-dd HH:mm 自动解锁，可联系管理员提前解锁」（未落库的纯计数锁仍固定话术，防账号存在性探测）；管理员用户管理锁定行「解锁」按钮（确认弹窗显到期时间）→ `PUT /users/{id}/unlock` 立即可登+审计落「提前解锁账号」。封禁/禁用行不显解锁（走「启用」，语义分离）。
3. **管理员设置姓名/昵称** ✅ —— 用户管理行「姓名」按钮（≤64 字，空=清除）+`PUT /users/{id}/name`；个人中心本人改名 17x 已有（限长统一 64）；全站 displayName（name 优先回落账号）：项目组成员表「姓名（账号）」、组产出用户列；审计列表用户列保持账号快照（证据链不随改名漂），详情弹窗括注现姓名+备注。
4. **按备注筛选拿人+备注展示** ✅ —— 统一选人组件 `UserPicker`（搜索命中用户名/姓名/备注，行显「姓名·账号·[备注tag]」，多选 chips、键盘导航）：项目组邀请成员（多选批量发邀请）、批量充值（开关多选→逐人同额到账，每人独立幂等键，失败名单可单独重充）。备注展示：项目组成员表灰 tag 悬浮全文、审计详情操作人行、充值用户下拉。数据源三处后端均补备注模糊匹配（LIKE 转义）。

# 已解决（2026-08-27 勾销，修复设计IV §4，commit `98d0eaf2`/`58f64beb`）
1. **按备注汇总统计** ✅（E 轮，commit `98d0eaf2`，决策 4「独立汇总视图」）—— 账单页新
   「按备注汇总」tab：同组织备注（users.remark）用户一桶，显 人数/余额合计/充值积分/充值金额（全量累计）
   + 消耗积分/调用次数（跟随顶部日期区间，默认近 30 天/上限 365 天）；空备注归「未填备注」桶（行首色条区分）；
   LIMIT 1000 兜底。配套：**四视图补备注列**（用户排行/充值记录/用户余额/项目组分配——灰 tag 悬浮全文，空显「—」）；
   三处 keyword 筛选扩为 用户名/姓名/备注 任一命中（LIKE 转义防通配）。实库对账：桶 Σ=原表直查（消耗/调用/人数/余额四口径全平）。
2. **审计日志显姓名与备注** ✅（A 轮，commit `58f64beb`）—— 审计列表操作人列改为
   「账号（现姓名）」（username 快照在前+operatorName 括注）+ 备注灰 tag 悬浮全文；与 III 期「详情弹窗括注现姓名+备注」同口径，
   列表页不再只显裸账号（账号快照保留在详情证据链，不随改名漂移）。

# 未解决（2026-08-29 起以下 2 项已实现纵深+备案，挂「待人工验证（修复VIII）」——过测试方案 N1-N9 后按口径勾销）
- 账号密码登录都是明文传输吗？有没有安全问题？被抓包是不是账号密码全泄露了？
  > **已排查+纵深已实现，待人工验证（修复VIII）**（2026-08-29，B 轮，commit `cf1327df`）：**结论=是明文 JSON 提交，会不会泄露取决于入口协议**——按部署手册 HTTPS（域名+证书）部署=线缆全密文抓包无用；当前无域名 `http://IP` 降级入口/本机 dev=账号密码、JWT、全量数据可被抓包（入口协议问题，非代码缺口）。本轮纵深三件：WS 首消息鉴权（token 出 URL/出 nginx 日志）、验证码永不进日志（勘误：排查指认的短信 code= 实为阿里云应答码，已改名 respCode=）、LLM 明文 Key 导出改 POST+登录密码二次确认。**HTTPS 落地+RSA 密码加密+邮件链接 https 前缀已拍板进备案**（`docs/specs/人工测试遗留问题修复VIII设计.md` §9，域名到位后实现即彻底勾销）。人工用例 N1-N8。
- 整个系统还有没有任何明文传输的内容，有可能被抓包抓出数据的内容？
  > **已排查出全清单（修复VIII §1a 七面表）**：HTTPS 部署下全部安全；http 降级入口下明文面=密码/JWT/WS/邮件链接 token/导出 Key/日志（日志面已收口）。同上：纵深三件已实现待验证，治本三项在备案。人工用例 N1/N8/N9。