# 项目资产库 · 开发进度 4

> 第 4 轮对话（2026-08-05）。S9 项目列表页 + 分享弹窗 + S10 资产详情抽屉 落地，前端 vitest 123/123（+18）零回归，vue-tsc 绿。

## 本轮对话信息

| 字段 | 内容 |
|---|---|
| 日期 | 2026-08-05 |
| 功能 | 项目资产库（含画布打通+节点@引用） |
| 对应 plan.md 步骤 | S9 项目列表页 + 分享弹窗 / S10 资产详情抽屉 + 一致性包 + 版本时间线 |
| 本轮目标 | /assets 卡片网格+分享弹窗 / 资产详情抽屉（预览+状态机+usages+版本时间线+一致性包） |
| 完成状态 | ✅ S9 + S10 完成（9a/9b/10a/10b 四 chunk，S11-S13 待做） |

## 本轮完成内容（代码类）

| 实现功能 | plan 编号 | 涉及文件 | 关键位置 | 测试 | commit |
|---|---|---|---|---|---|
| 项目列表页（Tab 我的/共享 + 卡片网格 + 新建 + 删除） | S9 / FR-001 | `views/AssetListView.vue` | role 拆 Tab（OWNER→我的；EDITOR/VIEWER→共享）；卡片首字母色块封面+叙事角色计数+role NTag；新建项目内联弹窗（name trim+空 desc→undefined）；删除 useDialog.warning 二次确认（L4 级联软删） | AssetListView 4/4 | 9d06b774 |
| 分享/成员管理弹窗 | S9 / FR-002 / L1 | `components/asset/ShareDialog.vue` | 成员表（owner 行角色锁定 NTag+不可移除；成员行 NSelect 改角色+移除+转让）；邀请栏（多选 filterable+已成员过滤+角色选择→逐个 invite+reload）；移除→memberApi.remove（L1）；转让→projectApi.transfer+旧 owner 降 editor | ShareDialog 5/5 | 23737506 |

**前端测试**：vue-tsc 绿；vitest 114/114（S8 后 105 + 9a 4 + 9b 5 = 114）零回归。

## 关键设计决策

- **两 Tab 按 ProjectVO.role 拆**（设计 §九 9.2）：`role==='OWNER'` → 我的项目；`role!=='OWNER'`（EDITOR/VIEWER）→ 共享给我。后端 `projectApi.list()` 一次返 owner+member 各带 role，前端纯客户端拆分，不发两次请求。
- **新建项目 narrativeRoles 走后端默认**：表单只收 name+description；五桶默认（人物/道具/场景/风格/通用）由后端 S2 落地（设计 §九 198），前端不重复硬编，单一事实源在后端。空 description trim 后送 `undefined`（避免传空串）。
- **封面 MVP 用首字母色块**：coverFileId 上传通道留到 S11 项目详情页（与资产上传共线），S9 不引封面上传，避免 fetchCanvasPreview blob 的 revoke 生命周期复杂度提前进来。
- **ShareDialog 逐操作即时调 API，非批量保存**（设计 §七 成员操作为离散 owner 操作）：invite/changeRole/remove/transfer 各自调端点 + reloadMembers，区别于 AgentPermissionModal 的批量 save。语义贴合后端端点（memberApi 无 batch save）。
- **MemberVO 无 username → adminApi.listUsers 建映射联显**：MemberVO 仅 userId/role/isOwner；ShareDialog 拉 `listUsers(1,200)` 建 userId→username Map（同 AgentPermissionModal/KbPermissionModal 范式；无用户模糊搜索接口，客户端 filterable 过滤）。
- **转让触发父列表重载**（@changed）：转让后旧 owner 降 editor，项目可能从「我的」迁「共享给我」；ShareDialog emit `changed` → AssetListView.loadData 重拉 list。移除/改角色同样 emit changed（角色态可能影响 owner 视角的卡片操作按钮）。
- **稳定单例 mock（测试）**：naive-ui useDialog mock 须返稳定对象（非工厂新实例），否则组件 setup 捕获的 dialog 与测试断言非同一实例，`warning.mock.calls` 取不到。defineExpose 暴露的 ref 在 VTU `wrapper.vm` 上自动解包，可直接赋值。

## 文件清单（新增/改动）

| 文件 | 类型 | 说明 |
|---|---|---|
| `frontend/src/views/AssetListView.vue` | 改（替换 S8 占位） | Tab+网格+新建+删除+接入 ShareDialog |
| `frontend/src/views/AssetListView.test.ts` | 新 | 4 单测（拆 Tab/403/创建/删除） |
| `frontend/src/components/asset/ShareDialog.vue` | 新 | 成员管理弹窗 |
| `frontend/src/components/asset/ShareDialog.test.ts` | 新 | 5 单测（候选排除/邀请/改角色/移除 L1/转让） |

## S10 追加内容（同会话续，commit ec3197fc + 52767264）

| 实现功能 | plan 编号 | 涉及文件 | 关键位置 | 测试 | commit |
|---|---|---|---|---|---|
| 资产详情抽屉 | S10 / FR-006/011 | `components/asset/AssetDetailDrawer.vue` | n-drawer 右抽屉：状态/类型/版本 NTag；按 mediaType 预览（文本 JSON 美化；IMAGE/VIDEO/AUDIO `fetchCanvasPreview`→objectURL，关抽屉/换资产 revoke）；元数据 n-descriptions；状态机动作（canEdit，L2/L3：DRAFT→[定稿][归档]、LOCKED→[解锁][归档]、ARCHIVED→[取消归档]、文件类[下载] `/files/{id}` blob→`<a download>`）；usages 使用记录列表（双向追溯，viewer 可读） | AssetDetailDrawer 5/5 | ec3197fc |
| 版本时间线 | S10 / FR-006 | `components/asset/VersionTimeline.vue` | `versionApi.list` 倒序版本列表 + 点选 `versionApi.get` 拉只读历史内容（回滚查看，快照不可变）；当前版高亮 | VersionTimeline 2/2 | 52767264 |
| 一致性包编辑 | S10 / FR-007 / 设计 §五 | `components/asset/ConsistencyPack.vue` | 主参考图 fileId/图集 fileIds(n-dynamic-tags)/标准描述/参数基线 四字段 → `versionApi.saveConsistencyPack` 局部合并产新版本（空串→null）；抽屉仅在 roleKeys∈{人物,道具,场景} 额染（解析 content.consistency） | ConsistencyPack 2/2 | 52767264 |

**前端测试（S10 累计）**：vue-tsc 绿；vitest 123/123（S9 后 114 + AssetDetailDrawer 5 + VersionTimeline 2 + ConsistencyPack 2 = 123）零回归。

### 关键设计决策（S10）

- **预览 objectURL 生命周期**：IMAGE/VIDEO/AUDIO 走 `fetchCanvasPreview`（带 JWT 拉 `/files/{id}` blob，`<img>/<video>` src 无法带 header）；watch show/assetId 关抽屉或换资产时 `URL.revokeObjectURL` 释放，防内存泄漏（同 CanvasView 范式）。
- **状态机按钮按 status 条件渲染（L2/L3）**：后端 `AssetService` 强制状态跃迁（非法→400），前端按 `asset.status` 渲染对应动作（DRAFT→定稿/归档；LOCKED→解锁/归档；ARCHIVED→取消归档），定稿=引用锁定当前版本快照语义（L2，resolve 走指定版本）。canEdit 由父（S11）按项目角色传，viewer 隐藏写动作（设计 §7.2）。
- **下载走 `/files/{fileId}` blob → `<a download>`**：同 CanvasView 下载范式；createObjectURL→click→revoke 即时释放。
- **版本时间线=只读回滚查看（非 mutate）**：版本快照不可变（设计 §六）；点选历史版本仅 `versionApi.get` 拉该版 content 预览，不做实际回滚。当前版本高亮（assetCurrentVersion prop）。
- **一致性包仅人物/道具/场景额染（设计 §五）**：drawer `showConsistency` 判 roleKeys∩{人物,道具,场景}；初始值从 `asset.content.consistency` 解析（JSON.parse）；保存 `saveConsistencyPack` 局部合并入 content.consistency + 产新版本（后端 S5 已落），空串→null（清空语义）。
- **稳定单例 mock + vi.hoisted（测试）**：useDialog/useMessage 稳定单例；`request.get` mock 须 `vi.hoisted` 提升（vi.mock 工厂 hoist 不能引顶层变量）；下载测试 `createElement` spy 仅对 `'a'` 标签返假 anchor + 挂载后再 spy（否则破坏 Vue 渲染）。

### 文件清单（S10 新增）

| 文件 | 类型 | 说明 |
|---|---|---|
| `frontend/src/components/asset/AssetDetailDrawer.vue` | 新 | 资产详情抽屉（预览+元数据+状态机+usages+下载+接入一致性包/版本时间线） |
| `frontend/src/components/asset/AssetDetailDrawer.test.ts` | 新 | 5 单测 |
| `frontend/src/components/asset/VersionTimeline.vue` | 新 | 版本时间线（只读回滚查看） |
| `frontend/src/components/asset/VersionTimeline.test.ts` | 新 | 2 单测 |
| `frontend/src/components/asset/ConsistencyPack.vue` | 新 | 一致性包编辑 |
| `frontend/src/components/asset/ConsistencyPack.test.ts` | 新 | 2 单测 |

## 待解决 / 阻塞

- [ ] 视频时长/分辨率 javacv 懒提取（S4b 遗留）——顺延
- [ ] Flyway V56-V59 真实库执行验证（Phase 4 起库）
- [ ] 资产端点双 `assets` 段路径（`/api/assets/assets/{id}`）S14 核对
- [ ] 移动端 390px ShareDialog/卡片实测（Phase 4，本轮已写响应式 media query）
- [ ] coverFileId 封面上传通道（S11 引入，本轮首字母色块占位）

## 下一轮计划

1. **S11 项目详情矩阵页**：`views/AssetProjectView.vue`（替换 S8 占位）+ `components/asset/AssetCard.vue` + `AssetMatrixFilter.vue`——顶 Tab 类型 × 左栏角色矩阵筛选 + 计数徽章（`assetApi.countMatrix`）+ 搜索框 + 资产卡片网格 + 上传/新建提示词·剧本入口；内嵌 S10 AssetDetailDrawer（点卡片开抽屉）。验证：矩阵交集筛选正确；计数与列表一致；空态双引导；定稿/归档后矩阵计数即变（L2/L3 联动）。
2. **S12 画布打通前端**（入库弹窗调 canvas-import + AssetPicker 调 resolve + 徽标）→ **S13 节点@引用** → **S14 文档收尾**。

## 引用与关联

- 设计方案：[../../docs/plans/项目资产库设计方案.md](../../docs/plans/项目资产库设计方案.md) §九/§七 7.2
- 实现计划：[../../docs/plans/项目资产库.plan.md](../../docs/plans/项目资产库.plan.md) S9
- 上一轮：[开发进度3.md](开发进度3.md)
- Feature Map / User-Ops / README：S14 收尾产出

## Token 使用

| 文件 | 约 tokens | 接近 5000？ |
|---|---|---|
| 本文件 | ~3800 | 🟡 接近预警（S11 起新建 开发进度5.md） |
