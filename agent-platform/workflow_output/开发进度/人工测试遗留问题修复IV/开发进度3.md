# 修复IV 开发进度3 · chunk C 画布保存与副本三项

- **日期**：2026-08-27　**commit**：`d843b8a6`
- **plan**：[IV_C_画布保存与副本三项](../../docs/plans/人工测试遗留问题修复IV_C_画布保存与副本三项.plan.md)　**spec**：[IV设计 §5.4/§5.7/§5.8](../../docs/specs/人工测试遗留问题修复IV设计.md)

## 实现内容

| 步骤 | 实现 | 关键位置 |
|---|---|---|
| C1a 新增即保存（C-4 缺口1） | addNode 尾部统一 `emit('structure-changed')`——三路新增（调色板/拖入/快速加）都进防抖落库 | CanvasBoard.vue addNode |
| C1b 失焦保存（C-4 缺口2） | MentionTextarea 新增 `blur-committed`（候选行 mousedown.prevent 不触发 blur，能 blur 即非点候选）；PropertyPanel 六处主文本框（text/image/video prompt、storyboard description、script synopsis/storyboardSpec）绑 `@blur-committed→emit('data-changed')`；onRenameBlur 查重后追加 data-changed | MentionTextarea.vue onBlur/emits；PropertyPanel.vue 六处 + onRenameBlur |
| C1c 参数变更保存（C-4 缺口3） | 视频比例/时长/分辨率 + 音频来源四处 `v-model` 直绑改 `:value + @update:value`（写 data + emit data-changed），与离散选择器同模式，无 watch 深比较 | PropertyPanel.vue 视频参数区 + 音频来源 |
| C2 新建即定型（C-7） | addNode 对 image/video 预置 `data.width/height=320`（携宽高入口——粘贴/副本——不覆盖；`nodeSizeStyle(data)` 同拍推导 style）；updateNodeData 完成分支保留兜底存量；types/canvas.ts 注释口径更新 | CanvasBoard.vue addNode；types/canvas.ts:106-117 |
| C3 副本完全独立（C-8 决策4） | RESET_KEYS 重排：**保留** fileId/previewUrl/coverPreviewUrl/outputText（副本即显产物）+ firstFrameNodeId（结构引用）；**清空** assetId/assetName/assetVersion/assetHasUpdate + taskId/mediaTaskId/startedAt/finishedAt + changeLog/localizeWarning/errorMsg；`status = (previewUrl || outputText) ? 'success' : 'idle'` | nodeClone.ts（全文重写注释同步） |

**偏离记录**：① plan C1a 另要求 CanvasView.confirmQuickAdd 无连线分支补 scheduleSave——addNode 单点 emit 后该分支已被覆盖（View 既有 `@structure-changed="scheduleSave"` 绑定），不加冗余调用；有连线分支显式 scheduleSave 保留不动（双触发被防抖合并）。② undo/redo 走 applyHistoryState 快照还原不经 addNode，emit 无循环；undo 后不落库是既有行为且不在 C-4 三缺口口径内，未动（记入 F 轮遗留观察）。

## 测试结果

- vitest 全量 **846/846 + 0 unhandled + exit 0**（基线 836 → +10：nodeClone 重写 7 例——产物四件保留/资产任务链清空/status 三分支/firstFrameNodeId 保留/深拷贝/错开；CanvasBoard +3——addNode emit structure-changed/媒体预置 320/文本不预置+携宽高不覆盖；PropertyPanel +3——名称 blur 报存/blur-committed 上抛/改分辨率 data+data-changed 恰一次；MentionTextarea +1——blur→blur-committed）
- vue-tsc **0 error**（中途一次 TS2741：addNode 中间变量 Record 需 `as CanvasNodeData`，已修）
- 人工待验（进 P4 测试方案 L10-L12）：L10 三路新增→保存徽标转已保存→刷新在 / 输入文本直接关页签→重进在 / undo 回归不劣化；L11 三路新建即 320 方盒+生成完成不跳变；L12 副本即显产物+success / 副本入库=新资产不命中已入库 / 副本重生成=新任务新图 / 删原件副本产物仍显

## 下一步

chunk D 项目组默认额度+受限视图（17x-3 默认 0+不限冻结 / 17x-4 组织信息可见）。
