# 企业级精准知识库 RAG · 开发进度 34

- 主题：P4 精确 Citation locator 接入真实生成链路。
- RED：扩展 `RagRetrievalServiceTest`，要求节点 metadata 中的第 3 页与标题路径“安装”进入 Citation；因 `HashVerifyRow` 不承载 metadata 而编译失败。
- GREEN：`reverifyNode` 在最终 Hash 复核时同时读取节点 metadata；服务解析 page/article/sheet/cellRange/bbox，并在 Citation 输出前执行 `CitationVerifier`。
- 行为：缺失、空白或损坏 locator 的证据仍可参与检索调试，但不会伪装成可定位 Citation；生成回答路径只返回校验通过的引用。
- 安全：locator 来自已授权、当前 Hash 的 PG 节点；解析失败日志不记录 metadata 正文、Query、Prompt 或 Chunk。
- 验证：`mvn '-Dtest=RagRetrievalServiceTest,CitationVerifierTest,CitationCheckerTest' test`，20/20 通过。
- 下一步：接入 `GroundedAnswerService`，实现分批事实、引用不丢失、冲突检测与完整六态回答协议。
