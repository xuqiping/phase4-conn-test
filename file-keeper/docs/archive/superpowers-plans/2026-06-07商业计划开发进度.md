# 2026-06-07 商业计划开发进度

## 商业售卖版账号权限与授权系统

- 已确认本轮开发保留在原仓库 `main` 工作区下执行，不创建独立 worktree。
- 已确认商业售卖版第一版采用“授权服务 + Web 管理后台 + 桌面客户端权限门禁”的方案。
- 已确认阶段 1 先做 `file-keeper/server/` 服务端基础工程，后续再做注册登录、授权 API、管理后台和桌面端门禁。
- 已确认阶段 1 测试策略：开发/生产面向 PostgreSQL，自动化测试使用 H2 兼容模式。

## 已创建文档

- 已新增阶段 1 设计规格：`file-keeper/docs/superpowers/specs/2026-06-07-file-keeper-server-phase-1-design.md`。
- 已新增阶段 1 实施计划：`docs/superpowers/plans/2026-06-06-file-keeper-server-phase-1.md`。
- 已新增本进度文件：`file-keeper/docs/superpowers/plans/2026-06-07商业计划开发进度.md`。

## 开发环境准备

- 初次运行 Maven 验证时发现当前环境缺少 `java` 和 `mvn` 命令。
- 已通过 winget 安装 Eclipse Temurin JDK 17。
- 已从 Apache 官方归档下载安装 Maven 3.9.11 到 `C:\Users\19536\.local\tools\apache-maven-3.9.11`。
- 当前会话中使用显式环境变量运行 Maven：
  - `JAVA_HOME=/c/Program Files/Eclipse Adoptium/jdk-17.0.19.10-hotspot`
  - `MAVEN_HOME=/c/Users/19536/.local/tools/apache-maven-3.9.11`

## 阶段 1：服务端基础与核心数据模型

### Task 1：Spring Boot 服务端骨架

- 已创建 `file-keeper/server/pom.xml`。
- 已创建 `file-keeper/server/src/main/java/com/superprogrammer/FileKeeperServerApplication.java`。
- 已创建服务端配置文件：
  - `file-keeper/server/src/main/resources/application.yml`
  - `file-keeper/server/src/main/resources/application-dev.yml`
  - `file-keeper/server/src/test/resources/application-test.yml`
- 已完成骨架验证：
  - 命令：`mvn -f "file-keeper/server/pom.xml" test`
  - 结果：`BUILD SUCCESS`
  - 当时尚无测试类，验证目标是 Maven 工程可编译、资源可加载。

### Task 2：Flyway 迁移测试和数据库结构

- 已新增 `file-keeper/server/src/test/java/com/superprogrammer/db/FlywayMigrationTest.java`。
- 已按 TDD 执行红灯验证：迁移文件不存在时测试失败，失败点为 `users table should exist`。
- 已新增 `file-keeper/server/src/main/resources/db/migration/V1__create_auth_schema.sql`。
- 迁移文件创建 6 张核心表：
  - `users`
  - `user_module_entitlements`
  - `user_devices`
  - `anonymous_device_trials`
  - `system_settings`
  - `admin_audit_logs`
- 已完成绿灯验证：
  - 命令：`mvn -f "file-keeper/server/pom.xml" test -Dtest=FlywayMigrationTest`
  - 结果：`Tests run: 1, Failures: 0, Errors: 0, Skipped: 0`

### Task 3：通用基础设施和实体

- 已新增通用基础类：
  - `file-keeper/server/src/main/java/com/superprogrammer/common/R.java`
  - `file-keeper/server/src/main/java/com/superprogrammer/common/PageResult.java`
  - `file-keeper/server/src/main/java/com/superprogrammer/common/ErrorCode.java`
  - `file-keeper/server/src/main/java/com/superprogrammer/common/BusinessException.java`
  - `file-keeper/server/src/main/java/com/superprogrammer/common/BaseEntity.java`
- 已新增配置类：
  - `file-keeper/server/src/main/java/com/superprogrammer/config/MyBatisPlusConfig.java`
  - `file-keeper/server/src/main/java/com/superprogrammer/config/MetaObjectHandlerConfig.java`
  - `file-keeper/server/src/main/java/com/superprogrammer/config/PasswordEncoderConfig.java`
- 已新增实体类：
  - `file-keeper/server/src/main/java/com/superprogrammer/user/entity/User.java`
  - `file-keeper/server/src/main/java/com/superprogrammer/user/entity/UserModuleEntitlement.java`
  - `file-keeper/server/src/main/java/com/superprogrammer/device/entity/UserDevice.java`
  - `file-keeper/server/src/main/java/com/superprogrammer/device/entity/AnonymousDeviceTrial.java`
  - `file-keeper/server/src/main/java/com/superprogrammer/settings/entity/SystemSetting.java`
  - `file-keeper/server/src/main/java/com/superprogrammer/audit/entity/AdminAuditLog.java`
- 已完成验证：
  - 命令：`mvn -f "file-keeper/server/pom.xml" test`
  - 结果：`Tests run: 1, Failures: 0, Errors: 0, Skipped: 0`

### Task 4：超级管理员初始化

- 已新增 `file-keeper/server/src/test/java/com/superprogrammer/bootstrap/SuperAdminInitializerTest.java`。
- 已按 TDD 执行红灯验证：初始化器不存在时查询不到 `role = 'super_admin'` 用户，测试失败。
- 已新增 `file-keeper/server/src/main/java/com/superprogrammer/bootstrap/SuperAdminInitializer.java`。
- 初始化规则：
  - 配置了超级管理员密码，且邮箱或手机号至少一个存在时才初始化。
  - 已存在同邮箱或同手机号的 `super_admin` 时不重复创建。
  - 新建用户状态为 `active`，角色为 `super_admin`，联系方式标记为已验证。
  - 使用 BCrypt 生成 `password_hash`，不保存明文密码。
- 已完成绿灯验证：
  - 命令：`mvn -f "file-keeper/server/pom.xml" test -Dtest=SuperAdminInitializerTest`
  - 结果：`Tests run: 1, Failures: 0, Errors: 0, Skipped: 0`
- 已重新运行迁移测试确认未被初始化逻辑影响：
  - 命令：`mvn -f "file-keeper/server/pom.xml" test -Dtest=FlywayMigrationTest`
  - 结果：`Tests run: 1, Failures: 0, Errors: 0, Skipped: 0`

### Task 5：最终验证

- 已运行阶段 1 完整服务端测试：
  - 命令：`mvn -f "file-keeper/server/pom.xml" test`
  - 结果：`Tests run: 2, Failures: 0, Errors: 0, Skipped: 0`
  - Maven 输出：`BUILD SUCCESS`

## 当前工作区状态

- 当前工作区在原仓库 `main` 下。
- 当前阶段 1 改动尚未提交。
- 当前新增内容包括：
  - `file-keeper/docs/superpowers/specs/2026-06-07-file-keeper-server-phase-1-design.md`
  - `docs/superpowers/plans/2026-06-06-file-keeper-server-phase-1.md`
  - `file-keeper/server/`
  - `file-keeper/docs/superpowers/plans/2026-06-07商业计划开发进度.md`

## 下一步建议

1. 提交阶段 1 改动，建议提交信息：`feat: 初始化商业授权服务端基础工程`。
2. 开始阶段 2：账号注册、联系方式验证、登录、token 刷新、管理员审核 API。
3. 阶段 2 开发前先补充或拆分阶段 2 设计/实施计划，避免一次性把注册、登录、审核、Redis token 撤销全部混在一起实现。

## 2026-06-07 本轮新增进展

### 阶段 2：账号注册、登录、管理员审核 API 计划

- 已按要求创建阶段 2 独立详细实施计划：`file-keeper/docs/superpowers/plans/2026-06-07-file-keeper-server-phase-2-auth.md`。
- 阶段 2 计划范围限定为：
  - 验证码发送/校验。
  - 客户端注册。
  - 客户端登录、refresh、logout。
  - 管理员登录、refresh、logout。
  - 管理员用户列表/详情/审核。
  - 用户禁用/启用。
  - 禁用后撤销 refresh token。
  - 管理员审核/禁用/启用审计日志。
- 阶段 2 计划明确排除：模块授权、设备绑定、匿名试用、管理后台前端、桌面端登录 UI、支付和套餐。
- 阶段 2 计划已拆成 6 个 TDD 任务：
  1. JWT 与安全基础。
  2. 验证码与客户端注册。
  3. 客户端登录和 refresh token。
  4. 管理员登录。
  5. 管理员用户列表、详情、审核和审计日志。
  6. 用户禁用/启用和 refresh token 撤销。

### 阶段 2 Task 1：JWT 与安全基础

- 已按 TDD 完成 Task 1：先新增失败测试，再实现生产代码，再运行验证。
- 已新增或修改配置：
  - `file-keeper/server/pom.xml`：新增 JJWT 依赖。
  - `file-keeper/server/src/main/resources/application.yml`：新增 `file-keeper.auth.*` 配置，并将 JWT secret 默认值改为空，避免生产环境漏配时使用固定密钥。
  - `file-keeper/server/src/test/resources/application-test.yml`：新增测试用 JWT、refresh token、验证码固定码配置。
- 已新增安全基础代码：
  - `file-keeper/server/src/main/java/com/superprogrammer/common/GlobalExceptionHandler.java`
  - `file-keeper/server/src/main/java/com/superprogrammer/config/AuthProperties.java`
  - `file-keeper/server/src/main/java/com/superprogrammer/security/AuthConstants.java`
  - `file-keeper/server/src/main/java/com/superprogrammer/security/AuthPrincipal.java`
  - `file-keeper/server/src/main/java/com/superprogrammer/security/JwtService.java`
  - `file-keeper/server/src/main/java/com/superprogrammer/security/JwtAuthenticationFilter.java`
  - `file-keeper/server/src/main/java/com/superprogrammer/security/SecurityConfig.java`
- 已新增安全测试：
  - `file-keeper/server/src/test/java/com/superprogrammer/security/JwtServiceTest.java`
  - `file-keeper/server/src/test/java/com/superprogrammer/security/SecurityConfigTest.java`
  - `file-keeper/server/src/test/java/com/superprogrammer/config/AuthPropertiesValidationTest.java`
- 当前 Task 1 实现能力：
  - JWT access token 使用 `userId` 作为 subject，包含 `role` 和 `status` claims。
  - 可解析合法 token，拒绝格式错误 token 和错误签名 token。
  - Spring Security 使用无状态 session，禁用 CSRF。
  - 公开计划中的客户端/管理员 auth 入口。
  - `/api/admin/**` 要求 `ROLE_SUPER_ADMIN`。
  - 其它接口要求已认证。
  - `JwtAuthenticationFilter` 支持大小写不敏感的 Bearer scheme，并 trim token。
  - JWT 解析失败时清理 `SecurityContext`，并用 debug 日志记录原因。
  - 未认证返回 `401 + R.fail(UNAUTHORIZED)`。
  - 权限不足返回 `403 + R.fail(FORBIDDEN)`。
- 代码质量复查曾发现的问题并已修复：
  - 固定默认 JWT secret 可能导致生产环境可伪造 token。
  - JWT secret 缺少启动期校验。
  - 安全路径测试覆盖不足。
  - Bearer scheme 大小写敏感。
  - JWT 解析异常完全吞掉，不利于排查。
- 已补充启动期 JWT secret 校验：
  - secret 不能为空。
  - UTF-8 字节长度不少于 32。
  - 拒绝默认示例密钥 `change-this-file-keeper-jwt-secret-at-least-32-bytes`。
  - 测试 profile 使用合法测试密钥。
- 已完成复查：
  - Task 1 规格符合性复查：通过，无超范围实现。
  - Task 1 代码质量复查：第一轮发现安全问题，修复后第二轮通过。

### 阶段 2 Task 1 验证结果

- 阶段 1 基线验证：
  - 命令：`mvn -f "file-keeper/server/pom.xml" test`
  - 结果：`BUILD SUCCESS`，`Tests run: 2, Failures: 0, Errors: 0, Skipped: 0`
- Task 1 初始 JWT 测试绿灯验证：
  - 命令：`mvn -f "file-keeper/server/pom.xml" test -Dtest=JwtServiceTest`
  - 结果：`BUILD SUCCESS`，`Tests run: 2, Failures: 0, Errors: 0, Skipped: 0`
- Task 1 安全修复后目标验证：
  - 命令：`mvn -f "file-keeper/server/pom.xml" test -Dtest=JwtServiceTest,SecurityConfigTest,AuthPropertiesValidationTest`
  - 结果：`BUILD SUCCESS`，`Tests run: 11, Failures: 0, Errors: 0, Skipped: 0`
- Task 1 安全修复后完整服务端验证：
  - 命令：`mvn -f "file-keeper/server/pom.xml" test`
  - 结果：`BUILD SUCCESS`，`Tests run: 13, Failures: 0, Errors: 0, Skipped: 0`

### 当前工作区状态补充

- 用户已明确要求“不提交阶段一，直接按这份开发计划开始执行”。
- 当前仍在原仓库 `main` 工作区下执行，未创建 worktree。
- 当前阶段 1 改动、阶段 2 计划文档、阶段 2 Task 1 代码均尚未提交。
- 本轮未执行 `git add` / `git commit`。
- 当前相关未提交内容包括：
  - `file-keeper/server/` 下阶段 1 基础工程和阶段 2 Task 1 认证安全基础。
  - `file-keeper/docs/superpowers/plans/2026-06-07-file-keeper-server-phase-2-auth.md`。
  - `file-keeper/docs/superpowers/plans/2026-06-07商业计划开发进度.md`。
- 工作区还存在此前已有的大量文档删除状态，本轮未处理这些删除。

## 更新后的下一步计划

1. 暂不提交阶段 1，也不提交 Task 1，继续按用户要求在当前 `main` 工作区推进阶段 2。
2. 从阶段 2 Task 2 开始：验证码发送/校验和客户端注册。
3. Task 2 执行方式：
   - 先新增 `UserRegistrationTest` 并确认红灯失败。
   - 实现验证码 DTO、验证码 store、验证码服务、用户 repository、注册服务、验证码 controller、客户端注册 controller。
   - 使用测试 profile 下的内存验证码 store，生产/开发使用 Redis store。
   - 注册成功后创建 `role = user`、`status = pending_review` 的用户。
   - 注册必须先完成同联系方式验证码校验。
4. Task 2 验证命令：
   - `mvn -f "file-keeper/server/pom.xml" test -Dtest=UserRegistrationTest`
   - `mvn -f "file-keeper/server/pom.xml" test`
5. Task 2 完成并复查通过后，再继续：
   - Task 3：客户端登录、refresh token、logout。
   - Task 4：管理员登录。
   - Task 5：管理员用户列表/详情/审核和审计日志。
   - Task 6：用户禁用/启用和 refresh token 撤销。
6. 每个 Task 继续遵循 TDD：先红灯测试，再最小实现，再完整验证；每个 Task 后做规格复查和代码质量复查。

### 阶段 2 Task 2：验证码与注册

- 已按 TDD 完成 Task 2：先新增 `UserRegistrationTest`（3 个用例），确认红灯失败，再实现生产代码，再运行验证。
- 已新增生产代码：
  - `file-keeper/server/src/main/java/com/superprogrammer/user/dto/SendVerificationRequest.java`
  - `file-keeper/server/src/main/java/com/superprogrammer/user/dto/CheckVerificationRequest.java`
  - `file-keeper/server/src/main/java/com/superprogrammer/user/dto/VerificationCheckResponse.java`
  - `file-keeper/server/src/main/java/com/superprogrammer/user/dto/RegisterRequest.java`
  - `file-keeper/server/src/main/java/com/superprogrammer/user/dto/UserSummary.java`
  - `file-keeper/server/src/main/java/com/superprogrammer/user/repository/UserRepository.java`
  - `file-keeper/server/src/main/java/com/superprogrammer/user/service/VerificationCodeStore.java`
  - `file-keeper/server/src/main/java/com/superprogrammer/user/service/RedisVerificationCodeStore.java`
  - `file-keeper/server/src/main/java/com/superprogrammer/user/service/VerificationService.java`
  - `file-keeper/server/src/main/java/com/superprogrammer/user/service/UserAuthService.java`
  - `file-keeper/server/src/main/java/com/superprogrammer/user/controller/VerificationController.java`
  - `file-keeper/server/src/main/java/com/superprogrammer/user/controller/ClientAuthController.java`
- 已新增测试支撑：
  - `file-keeper/server/src/test/java/com/superprogrammer/support/InMemoryVerificationCodeStore.java`
  - `file-keeper/server/src/test/java/com/superprogrammer/support/TestStoreConfig.java`
  - `file-keeper/server/src/test/java/com/superprogrammer/user/UserRegistrationTest.java`
- 调试修复：Task 2 引入的 `VerificationService` 依赖 `VerificationCodeStore`，但 `RedisVerificationCodeStore` 在 `test` profile 被排除，旧测试启动失败。根因修复：给 `InMemoryVerificationCodeStore` 加上 `@Component @Profile("test")`，test profile 自动注入。
- Task 2 当前能力：
  - `POST /api/client/verification/send`：接受 email/phone，已注册返回 409。
  - `POST /api/client/verification/check`：验证码正确返回 `data.verified=true`。测试 profile 固定码 `123456`。
  - `POST /api/client/auth/register`：注册需要恰好一个联系方式、先完成验证码校验；创建 `role=user, status=pending_review`。
  - email 标准化 lowercase+trim，phone 标准化 trim。
  - BCrypt 密码加密，响应不暴露 `password_hash`。
- 已完成复查：
  - 规格符合性复查：通过。15 个文件全部存在，所有 API 端点、业务规则符合计划。无超范围实现。`insert ... returning` 改为 `INSERT + SELECT` 以兼容 H2，功能等价。
  - 代码质量复查：通过。无 Critical/Important 问题。两个 Minor 项（验证码 store 的 consumeVerified 非原子操作）是已知局限，可后续用 Redis Lua 脚本优化。

### 阶段 2 Task 3：客户端登录、refresh token、logout

- 已按 TDD 完成 Task 3：新增 `UserLoginTest`（3 个用例），实现生产代码，完整验证通过。
- 已新增 DTO：
  - `file-keeper/server/src/main/java/com/superprogrammer/user/dto/LoginRequest.java`
  - `file-keeper/server/src/main/java/com/superprogrammer/user/dto/RefreshTokenRequest.java`
  - `file-keeper/server/src/main/java/com/superprogrammer/user/dto/AuthResponse.java`
- 已新增 refresh token 基础设施：
  - `file-keeper/server/src/main/java/com/superprogrammer/security/RefreshTokenStore.java`
  - `file-keeper/server/src/main/java/com/superprogrammer/security/RedisRefreshTokenStore.java`
  - `file-keeper/server/src/main/java/com/superprogrammer/security/RefreshTokenService.java`
  - `file-keeper/server/src/test/java/com/superprogrammer/support/InMemoryRefreshTokenStore.java`
- 已修改（扩展）：
  - `file-keeper/server/src/main/java/com/superprogrammer/user/repository/UserRepository.java`：新增 `findByIdentifier()`、`findById()`、`requireById()`、`toSummary()`、`userMapper()`。
  - `file-keeper/server/src/main/java/com/superprogrammer/user/service/UserAuthService.java`：新增 `clientLogin()`、`refresh()`、`logout()`、`createAuthResponse()`。
  - `file-keeper/server/src/main/java/com/superprogrammer/user/controller/ClientAuthController.java`：新增 `/login`、`/refresh`、`/logout` 端点。
- 已新增测试：
  - `file-keeper/server/src/test/java/com/superprogrammer/user/UserLoginTest.java`
- Task 3 当前能力：
  - `POST /api/client/auth/login`：`pending_review` 和 `active` 用户可登录；`disabled` 用户返回 403；密码错误返回 401。返回 access token（15 分钟）和 refresh token（7 天）。
  - `POST /api/client/auth/refresh`：用 refresh token 换新 access token，refresh token 不变。
  - `POST /api/client/auth/logout`：删除 refresh token，之后 refresh 返回 401。
  - Access token 使用 JWT，subject=userId，claims=role/status。
  - Refresh token 使用 opaque 随机 hex，SHA-256 哈希后存入 store（Redis 生产 / 内存测试）。
- 调试修复：`InMemoryRefreshTokenStore` 初始缺少 Spring 注解 import，编译失败后立即修复。

### 阶段 2 Task 2-3 验证结果

- Task 2 注册测试：
  - 命令：`mvn -f "file-keeper/server/pom.xml" test -Dtest=UserRegistrationTest`
  - 结果：`Tests run: 3, Failures: 0, Errors: 0, Skipped: 0`
- Task 3 登录测试：
  - 命令：`mvn -f "file-keeper/server/pom.xml" test -Dtest=UserLoginTest`
  - 结果：`Tests run: 3, Failures: 0, Errors: 0, Skipped: 0`
- Task 2-3 完成后完整服务端测试：
  - 命令：`mvn -f "file-keeper/server/pom.xml" test`
  - 结果：`Tests run: 19, Failures: 0, Errors: 0, Skipped: 0, BUILD SUCCESS`
  - 包含：阶段 1（2）+ Task 1（11）+ Task 2（3）+ Task 3（3）

### 当前工作区状态（更新）

- 用户已明确要求不提交阶段 1，直接在 main 工作区按计划执行。
- 阶段 1 + Task 1 + Task 2 + Task 3 代码均尚未提交，未执行 `git add` / `git commit`。
- 当前相关未提交内容包括：
  - `file-keeper/server/` 下阶段 1 基础工程和阶段 2 Task 1-3 全部代码。
  - `file-keeper/docs/superpowers/plans/2026-06-07-file-keeper-server-phase-2-auth.md`
  - `file-keeper/docs/superpowers/plans/2026-06-07商业计划开发进度.md`
- 工作区还存在此前已有的大量文档删除状态，本轮未处理。

## 最新下一步计划

1. 继续在当前 `main` 工作区推进阶段 2，不提交。
2. Task 4：管理员登录。
   - 扩展 `UserAuthService` 新增 `adminLogin()` 和 `adminRefresh()`。
   - 新增 `AdminAuthController`：`POST /api/admin/auth/login`、`/refresh`、`/logout`。
   - 新增 `AdminAuthControllerTest`：super_admin 可登录，普通 user 返回 403，admin 端点需 token。
   - 只有 `role=super_admin` 且 `status=active` 才能通过管理员登录。
3. Task 5：管理员用户列表/详情/审核 + 审计日志。
   - 新增 `AdminAuditLogService`、`AdminUserService`、`AdminUserController`、`UserReviewRequest` DTO。
   - 扩展 `UserRepository` 新增 `list()`、`updateStatus()`。
   - 新增 `AdminUserReviewTest`。
4. Task 6：用户禁用/启用 + refresh token 撤销。
   - 扩展 `AdminUserService` 和 `AdminUserController`。
   - 新增 `AdminUserDisableEnableTest`。
5. 全部 6 个 Task 完成后运行最终验证，更新进度文档。
6. 每个 Task 继续遵循 TDD，规格复查和代码质量复查在会话上下文允许时执行。
