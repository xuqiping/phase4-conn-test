# 修复IV 开发进度2 · chunk B 画布交互四项

- **日期**：2026-08-27　**commit**：`b88f61fb`
- **plan**：[IV_B_画布交互四项](../../docs/plans/人工测试遗留问题修复IV_B_画布交互四项.plan.md)　**spec**：[IV设计 §5.1-§5.3/§5.6](../../docs/specs/人工测试遗留问题修复IV设计.md)

## 实现内容

| 步骤 | 实现 | 关键位置 |
|---|---|---|
| B1 两段式点击（C-1） | onNodeClick 删直弹分支只选中；CanvasBoard `provide('canvasMediaPreview')`（同 canvasNodeResized 范式，payload 口径不变）；新组合式 `useMediaPreviewClick`——未选中放行冒泡只选中、已选中 stopPropagation 经 inject 弹 Lightbox、pointerdown→click 位移>5px 判拖拽尾不弹 | CanvasBoard.vue onNodeClick/provide；nodes/useMediaPreviewClick.ts；ImageNode 缩略图、VideoNode 视频本体绑 @pointerdown+@click |
| B2 上游直显+配色（C-2） | 删 showFarUpstream 折叠态（模板/状态/watch）；depth>1 同区直显小卡，徽标带 `·N` 层级号；类型徽标与缩略占位 `data-kind` 七色着色（hue 字面量同节点状态色风格）；--far 左缩进+主题色边 | PropertyPanel.vue 上游区模板 / upItems computed / `__up-kind`/`__up-ph` data-kind CSS |
| B3 resize 热区+四边（C-3） | 角柄 8px 视觉+`::before inset:-7px` 隐形热区（~22px 命中）；新增 RESIZE_EDGES 四边 `NodeResizeControl variant="Line"` 单轴拖边（线两端各缩 16px、z-index 2）；连线 Handle 配 z-index 4 命中优先（两处成对注释，改一处须同步） | CanvasNodeBase.vue 模板/RESIZE_EDGES/`__resize-control` 系列样式；CanvasBoard.vue `:deep(.vue-flow__handle)` |
| B4 面板拖宽（C-6） | aside 左缘 `role=separator` 拖拽条（视觉 4px 高亮线+10px 热区，col-resize）；pointermove 全程 `clampPanelWidth` 260-560（NaN/越界回落 260）；pointerup 写 `localStorage['canvas.propPanel.width']`；body user-select 防选中；onBeforeUnmount 清监听 | PropertyPanel.vue `__gutter` + PANEL_* 常量 + onGutterDown/Move/Up |

**偏离记录**：① plan B1 伪代码写 `@click.stop` 一刀切，实现改为「未选中不拦截（借冒泡走既有选中链）/ 已选中才 stop」——否则第一击选中链要自己重造；拖拽误触按 plan 坑点预案用 pointerdown+位移阈值兜底。② plan B2 写 `KIND_BADGE → {label, colorCssVar}`，实现用 CSS `data-kind` 属性着色替代 JS 色值注入——少一条 JS/CSS 耦合，断言走 attributes('data-kind')。③ 视频选中后行内 controls 让位给 Lightbox（决策 6 严格两段式的自然推论），已注明进 user-ops 待 F 轮写明。

## 测试结果

- vitest 全量 **836/836 + 0 unhandled + exit 0**（基线 827 → +9：mediaPreviewClick.test 新 5 例——未选中不弹/已选中弹带 id/拖拽尾不弹/视频同口径/无 provide 裸挂不炸；PropertyPanel.test B2 直显改写 1 例 + B4 拖宽新增 4 例——默认 260+aria/拖 100px 落 360+持久化/出界钳下界/非法存储回落）
- vue-tsc **0 error**
- 人工待验（进 P4 测试方案 L6-L9）：L6 两段式六档（含视频选中后 Lightbox 内播控）；L7 多级上游直显+切节点不残留+七类配色；L8 全档手感（拖角/拖边单轴/min 卡住/刷新保持/**左缘中点连线 vs 拖边命中优先**——z-index 4 vs 2 方案实测，若仍抢按 plan 预案降级为 hover 渐显边线）；L9 拖宽 260/560 两档下上游卡换行目检+刷新恢复

## 下一步

chunk C 画布保存与副本三项（C-4 自动保存补缺 / C-7 新建即定型尺寸 / C-8 副本完全独立）。
