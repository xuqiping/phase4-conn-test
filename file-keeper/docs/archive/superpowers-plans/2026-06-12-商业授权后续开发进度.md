# 2026-06-12 商业授权后续开发进度

## 背景

- 已根据实际阶段 4 开发进度生成调整版实施计划：`file-keeper/项目相关文档/商业售卖版账号权限与授权系统实施计划-新.md`。
- 当前开始执行后续计划的阶段 5A：联调前置配置收口。
- 当前分支：`lianwangban`。
- 当前未创建 worktree，按用户要求在当前工作区继续推进。

## 具体实施全计划
具体实施全计划目录：file-keeper\项目相关文档\集体实时计划

## 阶段 5D：匿名试用过期后的免费模块选择

### Task 5D-1：扩展 commercialAuth API 支持免费模块选择

目标：封装匿名试用过期后的免费模块选择和更换 API，为 Store 和 UI 提供基础能力。

#### 进度

- [x] 扩展 `file-keeper/src/api/__tests__/commercialAuth.test.ts`，新增选择免费模块、更换免费模块、30 天限制错误透传测试。
- [x] 确认红灯：API 函数缺失时测试失败。
- [x] 扩展 `file-keeper/src/api/commercialAuth.ts`，新增 `selectFreeModule` 和 `changeFreeModule`。
- [x] 运行 commercialAuth API 测试。

#### 当前记录

- 2026-06-11：已新增 5D-1 API 测试，覆盖 `POST /api/anonymous/trial/select-free-module`、`POST /api/anonymous/trial/change-free-module`，以及后端 30 天限制错误消息透传。
- 2026-06-11：红灯已确认：`npm --prefix "file-keeper" test -- src/api/__tests__/commercialAuth.test.ts` 中新增用例因 `selectFreeModule` / `changeFreeModule` 未实现失败。
- 2026-06-12：已在 `commercialAuth.ts` 实现 `selectFreeModule` / `changeFreeModule`，请求体包含 `deviceId`、`fingerprintHash`、`freeModuleCode`。
- 2026-06-12：已扩展 `commercialAuthStore`，新增选择、更换免费模块与刷新匿名/登录授权状态的 action，后端限制错误会写入 `error`。

### Task 5D-2：扩展 commercialAuthStore 支持选择免费模块

目标：让 Store 能调用免费模块选择 API，更新试用状态，并刷新匿名授权快照。

#### 进度

- [x] 扩展 `file-keeper/src/stores/__tests__/commercialAuthStore.test.ts`，覆盖选择免费模块、刷新匿名授权、30 天限制错误、登录授权刷新。
- [x] 扩展 `file-keeper/src/stores/commercialAuthStore.ts`，新增 `selectFreeModule`、`changeFreeModule`、`refreshAnonymousAuthorization`、`refreshAuthenticatedAuthorization`。
- [x] 重新运行 commercialAuthStore 测试确认当前工作区绿灯。

### Task 5D-3：新增免费模块选择 UI

目标：匿名试用过期且没有可用模块时，桌面端显示免费模块选择入口。

#### 进度

- [x] 新增 `file-keeper/src/components/FreeModuleSelector.vue`。
- [x] 新增 `file-keeper/src/components/__tests__/freeModuleSelector.test.ts`。
- [x] 完成 `file-keeper/src/App.vue` 挂载收口。
- [x] 运行组件测试、完整测试和构建。
- [x] 启动应用并尽可能手动验证免费模块选择流程。

#### 当前记录

- 2026-06-12：已完成 `App.vue` 集成：匿名试用过期、未登录、未选择免费模块且无可用模块时显示 `FreeModuleSelector`；登录用户不被免费模块选择阻塞。
- 2026-06-12：已运行阶段 5D 目标测试：`npm --prefix "file-keeper" test -- src/api/__tests__/commercialAuth.test.ts src/stores/__tests__/commercialAuthStore.test.ts src/components/__tests__/freeModuleSelector.test.ts`，结果 3 个测试文件、27 个用例通过。
- 2026-06-12：已运行完整测试：`npm --prefix "file-keeper" test`，结果 33 个测试文件、212 个用例通过。
- 2026-06-12：已运行构建：`npm --prefix "file-keeper" run build`，结果通过；仅有既有 Vite 动态/静态导入 chunk 提示。
- 2026-06-12：已启动 Vite 前端并用 Edge DevTools 驱动真实页面验证：匿名过期且无可用模块时显示三个选项；选择 `files` 后调用 `POST /api/anonymous/trial/select-free-module`，随后刷新匿名授权，选择器关闭，授权状态显示“匿名免费模块 文件”。
