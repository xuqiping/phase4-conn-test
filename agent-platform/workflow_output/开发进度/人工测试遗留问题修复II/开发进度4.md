# 开发进度4 —— Plan D 价表三改（7x-4 缓存价 / 7x-5 闲时价 / 9x-1 缓存口径，V160）

> 主题：缓存命中读 token 作第三腿单独计价 + 闲时时段价 + 去掉已废弃维度残留。计划：`docs/plans/人工测试遗留问题修复II_D_价表三改.plan.md`。

## 实现记录（commit → 内容）

| commit | 内容 |
|---|---|
| b54c9e0d | D1 `V160__pricing_offpeak_cached_deresolution.sql`：pricing_rule +4 列（off_peak_input/output/cached、price_cached 全 NULL 默认=存量逐分不变）；llm_usage_logs +cached_tokens；SECOND 去分辨率合并（按 provider/model/has_reference 留 effective_from 最新一行，其余物理 DELETE+NOTICE 价差留痕——表无 deleted 列 append-only，偏差已记录）；询价索引去 resolution 段。已在 scratch 库实测（多行组 4→1、单行去身份、CHAT 不动、索引重建） |
| 69efa487 | **插入提交**（非计划内）：补提交 8-25 遗留在工作区的 estimateVideoYuan fail-closed 实现（SECOND 未配秒价/TOKEN 未配预估秒价 → 抛 PRICING_NOT_FOUND）。已提交测试依赖此实现，先行落地以解耦 D2 提交 |
| c9e1e43b | D2 计价核心：computeCost +cachedTokens 重载 / computeCostAt +moment；textCost 三腿（in+cached+out，cached=null/0 退化两腿）；缓存价回落链 闲时缓存→忙时缓存→当前输入价；isOffPeak 读 `billing.off-peak.schedule`（Asia/Shanghai、周末判定、跨零点拆两段、非法回退忙时）；EMBED/RERANK 强制 cached=null。PricingRuleEntity +4 字段（ALWAYS 策略）。测试 29/29，含「老口径逐分一致」硬门槛 |
| 8e914911 | D3 口径归一：TokenUsage +cachedTokens；OpenAICompatibleProvider.buildUsage（prompt−cached clamp≥0，prompt_tokens_details.cached_tokens，缺失=null）；ClaudeProvider.buildUsage（input+cache_creation 并入输入基数，cache_read=命中腿，1.25 溢价不建模）。两家流式/非流式全接。30/30。偏差：embed/rerank 响应无缓存字段不解析（D2 已强制 null，语义不变） |
| e649889c | D4 落库+协议透传：LlmUsageLogEntity/UsageDetailVO +cachedTokens；UsageCollector record 14→15 参重载；LlmUsageLogMapper.xml pageDetail +cached_tokens；StreamEvent.usage +cached（null 省略）；前端 chat.ts usageFromDone→metadata.usage；MessageBubble「本次消耗 N tokens ≈ X 积分（其中缓存命中 M）」；BillingAdminView 明细 +缓存命中列。8/8+67 回归。偏差：batchInsert XML 死代码无调用方未补列 |
| daa31f6 | D5 计费链：onSuccess 11→12 参、settleChatHeld 11→12 参（+cachedTokens 尾参）委托新实现调 10 参 computeCost+15 参 record；LlmGateway 全调用点透传 usage.getCachedTokens()（embed/rerank 不动）。holdChat/settleChatCancelled/FAILED 保持 7 参+11 参 record——hold 时缓存命中不可预知按 0 保守预估，settle 真实值多退少补。26/26 |
| 9b6fd509 | D6 配置面去分辨率：行身份 (provider,model,kind,hasReference)；availablePricingModels VIDEO 候选=参考面×2+hint；判重 countConflicting…HasRef 3 维（带 resolution 旧 SQL 删）；SECOND+resolution 拒绝；导入忽略旧 resolution（老导出兼容）；stale 分辨率行禁编辑；EST_RESOLUTION_SLOTS 仅 est 校验；findEffectiveWithResolution 保留在途兜底。44/44 |
| 6898526a | D7 价表三改前端：先补后端 Request/VO/ExportItem 四列映射（D2 只落 entity）；表单 +闲时入/出（CHAT/EMBED/RERANK）、缓存价/闲时缓存价（CHAT）留空回落；分辨率下拉删、表格闲时/缓存摘要列；sanitize 非文本清四列+SECOND 置 null resolution。44/44+vue-tsc 0 |
| af78fc95 | D8 闲时时段配置：system_settings 键 billing.off-peak.schedule；读侧宽容（缺失/损坏→disabled，计费宁多收不少收）/写侧严格（HH:mm 00:00-24:00、每日≤4 段拒保存）；timezone 强制；GET/PUT /api/settings/billing offPeak 部分更新；BillingSettingsTab 闲时段卡（time-picker 增删行、半填拦截、24:00 收敛 23:59）。39/39 |
| 18076939 | D9 est 偏差：EstDeviationVO+selectVideoEstDeviation7d（SQL 内 Σ实耗/Σ预估−1 聚合不拉明细，实耗=usage SUCCESS 行 points 之和，样本<3 不出行）；GET /pricing/est-deviation；价表弹窗 est 槽位旁 tag 偏高/偏低 N%+样本数，<5% 或无数据隐藏。SQL 本地库实测。44/44 |
| fdf1c9f | D10 收口：legacy_pricing_allNewColumnsNull_matrix_sameAsBefore 六 kind 逐分矩阵（CHAT 3.00/EMBED 1.00/RERANK 1.60/VIDEO TOKEN 3.00/VIDEO SECOND 5.00/IMAGE 0.30=V159 前口径）；收口套件 250/250（billing 9 类+SystemSetting）。§5.5④ HOLD 忙发闲结=computeCostAt 计价×settle 差额两测试组合覆盖 |

## 关键代码位置

- `billing/service/PricingService.java` — computeCostAt(109)/textCost(245)/pickPrice(279)/isOffPeak(299)/OFF_PEAK_SCHEDULE_KEY(289)
- `billing/entity/PricingRuleEntity.java` — D 段 4 字段(86-100)
- `billing/service/PricingServiceTest.java` — D2 段 9 测试
- `llm/dto/TokenUsage.java` — cachedTokens + 口径 javadoc
- `llm/provider/OpenAICompatibleProvider.java` / `ClaudeProvider.java` — buildUsage
- `billing/service/LlmBillingService.java` — onSuccess 12 参/settleChatHeld 12 参透传
- `llm/LlmGateway.java` — USAGE 尾/settle/onSuccess 透传点
- `frontend/src/components/chat/MessageBubble.vue` — usageLine

## 设计口径（沉淀）

- **存量逐分不变**是三改放行硬前提：新列全 NULL → pickPrice 取忙时列；cachedTokens=null → 两腿；测试 `chat_cachedNull_degeneratesTwoLeg_sameAsBefore` 锁死。
- **闲时判定每请求实时读** system_settings（与 daily-cap 同哲学；计费=结算频次，低频不加缓存）。
- **宁多收不少收**：配置缺失/损坏 → 忙时价。
- **promptTokens=计费输入基数**（协议归一后落 TokenUsage，下游不再做按家数学）：OpenAI=prompt−cached；Claude=input+cache_creation。cachedTokens=null 表示「未上报」非「0」→ 两腿退化。
- **hold 保守 settle 校正**：hold 不赌缓存命中（按 0），结算用真实 cachedTokens 多退少补——HOLD 幂等键路径回归通过。

## 下一步

Plan D 全勾（D1-D10）。剩：Plan E 实时刷新（E1-E7 /ws/events）→ 功能级收尾（测试方案/README/FeatureMap/UserOps/勾销 7x-4、7x-5、9x-1/速查表同步）。
