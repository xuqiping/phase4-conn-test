

# 参考feature-map、user-ops
- agent-platform\workflow_output\docs\feature-map\项目资产库.feature-map.md
- agent-platform\workflow_output\docs\user-ops\项目资产库用户操作手册.md

>解决完后已同步更新对应的feature-map和user-ops（2026-08-14）

# 未解决 → 已解决（2026-08-26 勾销，修复设计 §3/§5）
1. **选参后显示预估 + 不足即拒** ✅ —— 三处落地：
   - 独立图片/视频生成页：GET /api/media/estimate 按所选参数（模型/时长/分辨率/比例/张数）实时估积分，
     提交按钮下方显示「预估消耗 X 积分」；预估 > 余额/组池红字预警（C 计划，commit `bcadec5`→`a3d3721`）。
   - **无限画布**：画布图片/视频节点提交前同样走预估接口展示 + 不足直接拒（C 计划，规格 §3 画布缺口）。
   - 提交侧服务端硬拦：余额/组池/成员配额（项目内剩余 quota−used，17x-2 口径）< 预估 → 任务不创建直接报错
     （A/C 计划，commit `30d11f05`/`7271f1c8`）——前端提示只是体验，服务端才是闸门。
   - 提交成功即 HOLD 预扣，防并行任务「预检都过、结算不够扣」（C 计划）。
