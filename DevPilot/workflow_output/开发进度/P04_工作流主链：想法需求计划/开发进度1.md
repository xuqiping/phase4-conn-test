# P04 工作流主链：想法需求计划 · 开发进度 1

> 本轮完成 P04 S6~S8 全部剩余 chunk，以及 S8 文档归档。`check_all` 全绿。

## 已完成工作

### S6 状态机联动（FR-031/032）
- 在 `src-tauri/src/commands.rs` 新增 `auto_pass_gate` / `auto_unpass_gate` / `emit_current_state` 三个内部辅助函数。
- `update_spec_card`：全部需求卡 confirmed/skipped 后自动 `pass_gate("requirement_confirm")`。
- `approve_plan`：创建 tasks 后自动 `pass_gate("kickoff")` 并推送状态快照。
- `revoke_plan_approval`：回退 `kickoff` 门禁，避免无 tasks 也能进入 build。
- commit: `8310bf5`

### S7 主动反问澄清（FR-044）
- 新增 `src-ui/hooks/useClarifyRound.ts`，统一控制每阶段最多 3 轮追问。
- `Idea.tsx` / `Spec.tsx` / `Plan.tsx` 全部接入该钩子；超限提示「信息仍不足，建议人工梳理后重试」。
- commit: `00b0c27`

### S8 冒烟、文档、CI 与归档
- 补充 `commands::tests` 两个状态机联动单测（requirement_confirm / kickoff 自动过门与撤销）。
- 产出：
  - `workflow_output/docs/feature-map/P04_工作流主链：想法需求计划.feature-map.md`
  - `workflow_output/docs/user-ops/P04_工作流主链：想法需求计划用户操作手册.md`
  - `workflow_output/开发进度/P04_工作流主链：想法需求计划/README.md`
- 回写 `workflow_output/docs/specs/db_schema.md`，新增 L7 三张表说明。
- 更新 `workflow_output/docs/plans/00_MVP实现计划总索引.md`，P04 标记为已交付。
- plan.md 中 S6~S8 全部标 ✅。
- commit: `3435d8c`

## 质量门结果

- `bash scripts/check_all.sh`：EXIT=0（cargo/npm audit 因本地网络/镜像降级为 WARN，CI 兜底）。
- Rust workspace：80+ 单测全绿。
- 桌面前端：35 单测全绿。
- 云端：e2e 40/40 全绿。

## 下一张 plan

P05_建造期：任务编排与大白话层（FR-013、015、036、037、038）。
