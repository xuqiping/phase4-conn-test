# 修复V · 开发进度1（A+B+C 全轮，2026-08-27）

> SDD P3 全 chunk 完成。总览见同目录 README.md。

## Chunk A · 画布两项（commit `3c7926b0`）

- A1 上游视频首帧：`frontend/src/components/canvas/PropertyPanel.vue` 上游卡 video 分支（`preload="metadata" muted playsinline` + pointer-events:none）；CSS hover 放大含 video。
- A2 副本重进 fileId 兜底：`frontend/src/views/CanvasView.vue` hydrateVideoPreviews 加 fallbackToFile 闭包（无 taskId/查询失败 → fetchCanvasPreview 补 previewUrl+status；running 跳过；失效静默）。
- 测试：`PropertyPanel.test.ts` +2（video 渲染 src/preload、无源回落占位）；Playwright 后置 P4（plan 已注明）。

## Chunk B · 组池流水筛选+CSV 导出（commit `671bea37`）

- B1 筛选：`ProjectGroupQueryService.overview` 扩 5 参；LedgerFilter record + normalizeFilter（LEDGER_TYPES 白名单 14，非法 400）；applyLedgerFilter（keyword `.apply("{0}")` 参数化子查询三 LIKE+ESCAPE；`.last("LIMIT 常量")` 无注入面）。
- B2 导出：`exportLedger`（managerViewOf 403 → LIMIT 50001 截断+count 注记 → selectBatchIds → BOM+RFC4180 CSV → log.info 审计）；`ProjectGroupController` GET `/{id}/ledger/export`（权限注解+@AuditLog+attachment 文件名）。
- B3 前端：`api/projectGroup.ts` GroupLedgerQuery/overview(q)/exportLedger(blob)；`ProjectGroupsView.vue` 筛选行+导出按钮（分页归 1/清空/loading）。
- 测试：`ProjectGroupQueryServiceTest` 16/16（@BeforeAll TableInfoHelper.initTableInfo 解 lambda 缓存；EXPORT_ROW_LIMIT 常量内联→50001 行真数据测截断）；新 `ProjectGroupLedgerFilterIT` 6/6 真 PG（`-Dsurefire.excludedGroups= -Dtest=ProjectGroupLedgerFilterIT test`，jdbc OVERRIDING SYSTEM VALUE 造数，ID 段 991700001）。
- 补档：V156 迁移脚本前轮漏提交，单独 `d4895c63` 补上。

## Chunk C · 文档勾销（本 commit）

- `人工测试问题/2x. 资产库和无限画布.md`：2 项未解决→已解决（修复V §3）。
- `人工测试问题/17x_项目组.md`：流水筛选导出→已解决（修复V §4）。
- feature-map/user-ops「2026-08-27 增补（修复V）」节：无限画布创作页、项目资产库、项目组与积分划拨 共 6 文件。
- `docs/测试方案/人工测试遗留问题修复V测试方案.md`（L1-L6 联动用例+冒烟路径+通过标准）。
- 本 README + 开发进度1。

## 基线

vue-tsc 0 / vitest 848 全绿 / 后端单测 16/16 + IT 6/6。待 P4：后端以 V 代码重启后 Playwright 冒烟（L1-L6）。
