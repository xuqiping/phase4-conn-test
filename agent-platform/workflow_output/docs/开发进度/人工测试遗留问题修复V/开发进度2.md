# 修复V · 开发进度2（P4 Playwright 冒烟实测）

> 2026-08-27。环境：前端 5173 / 后端 8080（V 代码重启）PG agent_platform；admin + smk4_leader/smk4_mem（组 33=id4 流水 13 行）。

## 冒烟结果（对照 测试方案 L1-L6，全过）

| 用例 | 操作 | 结果 |
|---|---|---|
| L1 正1 关键词「兜底」 | 输入+回车 | 1 行 ✅ |
| L1 正2 类型=兜底垫付 | 下拉 | 3 行 ✅ |
| L1 正3 操作人=成员 aa… | 下拉 | 5 行 ✅ |
| L1 正4 时间窗（近 7 天外） | 起止 | 0 行 ✅ |
| L1 反1 清空 | 按钮 | 10 行（本页 size=10）✅ |
| L1 边2 空集关键词 | 乱码串 | 0 行 ✅ |
| L1 边3 组合 AND（类型×操作人不相交） | 双维 | 0 行 ✅ |
| L2 全量导出 | 导出 CSV | 1518B BOM(EF BB BF)+表头 7 列+13 行 CRLF，操作人「姓名（账号）」格式 ✅ |
| L2 筛选联动导出 | 关键词+导出 | 表头+1 行（含 1776.7）✅ |
| L3 成员视角 | smk4_mem 登录 | **无流水 tab**（整 tab 在 `v-if="canManage"` 内，比计划更严，非缺陷）；直调 export 403「仅组长/管理员可导出」；overview 带恶意筛选参数被后端忽略，仅本人行 total 0 ✅ |
| L4 副本重进 | canvas/1 刷新 | 3→4 视频；reload 后 4/4 readyState≥2（505ms 内）；网络证据双腿：`/api/media/tasks/{id}`（原 taskId 腿）+ `/api/files/*.mp4`（fileId 兜底腿）✅ |
| L5 上游首帧 | 点视频节点 | `.prop-panel__up-video` readyState 4 零「视」占位；点击开 Lightbox 1280w controls ✅ |
| L6 画布回滚链 | — | **live 跳过**：入口「版本」按钮=故事板节点版本抽屉非画布回滚；fileId 兜底与 taskId 腿共用 hydrateVideoPreviews 同函数（CanvasView.vue），L4 已实测该函数，代码同链已证。如实记档 |

性能（后端 RequestLogFilter costMs）：overview 7–19ms / export 6ms（13 行；5 万行截断路径 IT 已证）。未设 p50/p95 目标基线，本轮实测远低于可感知阈值。

## 第二 AI review（caveman-reviewer）：0🔴 5🟡，处置

1. 🟡 **导出 15s 全局超时掐断大导出** → 修：exportLedger 加 `timeout: 120000`（projectGroup.ts）。
2. 🟡 **`<video preload="metadata">` WebKit 首帧不可靠** → 修：上游卡 `src + '#t=0.1'` 时间锚（PropertyPanel.vue）。
3. 🔴→🟡实锤 **兜底腿 fileId 抹除**：`fileId: r.data.data.resultFileId ?? undefined` 经 Object.assign（CanvasBoard.vue:1011）拿 undefined 覆盖既有 fileId，杀兜底 → 修：条件展开 `...(resultFileId ? {fileId} : {})`（CanvasView.vue）。
4. 🟡 **任务在但 videoUrl 空（异常态）** 原走空分支 → 修：同落 fileId 兜底（CanvasView.vue）。
5. 🟡 **CSV 公式注入**（备注首字符 `=+-@`）→ 不修：方案设计期已决策「导出=忠实留痕，不改写业务数据」，Excel 打开提示由客户端承担。记档维持。

回归：vue-tsc 0 / vitest 848/848（含 #t=0.1 断言改）；导出复测 BOM/表头/13 行复现 ✅；#t=0.1 实机 `src="blob:…#t=0.1"` readyState 4 ✅。

reviewer 无缺陷确认：LEDGER_TYPE/TYPE_LABEL 14/14 对齐、`.apply` 参数化无注入、MEMBER 无越权、objectURL 生命周期、`+86399999` 时间窗含当日全秒。

## 结论

修复V 三缺陷（2x#1/2x#2/17x#1）冒烟全过 + review 四项修复一项记档，P4 出口条件满足，放行 P5。P4 修复 commit 见 `fix: 修复V P4 冒烟回归`。

遗留低优（前轮 review 已档，不阻断）：mine() 池余额截断、QueryServiceTest actorUserId 断言加固、PropertyPanel 260 clamp。
