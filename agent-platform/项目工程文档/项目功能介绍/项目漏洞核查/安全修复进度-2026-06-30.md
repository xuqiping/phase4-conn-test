# 安全漏洞 + 运行时 Bug 修复进度 · 2026-06-30

> 范围：《安全漏洞审计-2026-06-28.md》（11 项）+《运行时bug.md》（RB-001 死亡螺旋）
> 状态：**16/16 全部落地，`mvn -o compile` 全绿，改动均未 commit（main 分支工作树）**
> 验证方式：3 次 maven 编译均 exit 0（RB-001 后 / #1-#4 后 / #6-#8 后）。IDE 报的 Lombok 错误全为噪声（NetBeans LSP annotation processor 坏），以 maven 为准。

---

## ✅ 已完成（核实 + 编译通过）

### RB-001 死亡螺旋（P0 运行时）

| 子项 | 改动 | 位置 |
|---|---|---|
| ① WebClient 超时 | HttpClient connect 10s / response 30s + `.block(Duration)` | `llm/provider/OpenAICompatibleProvider.java`、`llm/provider/ClaudeProvider.java`（常量 `CONNECT_TIMEOUT_MS`/`RESPONSE_TIMEOUT`） |
| ② 拒绝策略 | `CallerRunsPolicy → AbortPolicy`；3 处 chat 提交点 + 3 处 admin 异步点全部 try/catch `RejectedExecutionException` → recordIncident | `chat/config/MemoryTaskExecutorConfig.java`、`chat/service/ChatSessionService.java`（sendMessage/doSendMessageStream/streamWorkflow）、`chat/service/MemoryService.java`（backfill/reextract/cleanupResolved，新增私有 `submitMemoryTask`） |
| ③ 抽取静默吞 | catch 内 `recordIncident(userId, 固定话术)`；新增 public `recordIncident(Long,String)` | `chat/service/MemoryService.extractFacts` |
| ④ 前端风暴 | 连续网络错误计数 + 5s 节流 toast + 达阈值(5) 跳 login | `frontend/src/api/request.ts`（`consecutiveNetErrors`/`NET_ERROR_CIRCUIT_THRESHOLD`） |

### 安全审计

| # | 级别 | 改动 | 关键文件 |
|---|---|---|---|
| 1 | 🔴 P0 | 新增 `RuntimeCallbackSecurityFilter`（`X-Runtime-Token` 校验，fail-closed，非 @Component 防 servlet 自动注册）；SecurityConfig addFilterBefore 注入；application.yml 加 `runtime.callback.token:${RUNTIME_CALLBACK_TOKEN:}`；service 注入 ExecutionLogService + `resolveTrustedUserId`（executionId→execution_logs.triggeredBy 反查），executeNode 开头 override `request.setUserId` | `runtime/security/RuntimeCallbackSecurityFilter.java`（新）、`auth/security/SecurityConfig.java`、`runtime/service/RuntimeNodeCallbackService.java`、`application.yml` |
| 2 | 🔴 P0 | DB password / JWT secret 默认值清空（`${DB_PASSWORD:}` / `${JWT_SECRET}` 无默认）；`@PostConstruct validateSecret` 校验空/弱密钥→拒启动，`KNOWN_WEAK_SECRETS` 含旧 base64 + 明文 | `application.yml`、`auth/security/JwtUtil.java` |
| 3 | 🟠 high | 新增 `SsrfGuard`（http/https only，InetAddress 全量校验：loopback/siteLocal/linkLocal/multicast/anyLocal + IPv6 fc/fd + CGNAT 100.64/10）；`LlmGateway.createProviderInstance` 开头调；embed 两处不再回显 responseJson（改 log.warn 截断 + 固定异常话术） | `common/security/SsrfGuard.java`（新）、`llm/LlmGateway.java`、`llm/provider/OpenAICompatibleProvider.java` |
| 4 | 🟠 high | service 加 `listByWorkflowIdScoped(workflowId,triggeredBy)`；controller listByWorkflow 用 scoped + rejectExecution 先 getVisibleExecutionLog 校验归属再 failExecution | `execution/service/ExecutionLogService.java`、`execution/controller/ExecutionController.java` |
| 5 | 🟡 med | **已修（V40 load 咽喉点 owner 校验），代码核实无误** | `file/service/FileStorageService.java`、`file/controller/FileController.java` |
| 6 | 🟡 med | 9 处 chat/记忆原文 INFO→DEBUG（改打 length/id，不落原文） | `engine/strategy/AgentRoutingStrategy.java:35`、`chat/service/MemoryService.java`（107/165/203/224/280/310/477）、`chat/service/internal/MemoryBlockClassifier.java:35`、`chat/service/internal/MemoryConflictJudge.java:198` |
| 7 | 🟡 med | 兜底异常 + WS 错误 + SSE 错误全改固定话术（detail 仅 log.error）；ChatController 加 @Slf4j；BusinessException message 保留（受控） | `common/exception/GlobalExceptionHandler.java`、`chat/websocket/ChatWebSocketHandler.java:70,86`、`chat/controller/ChatController.java`（SSE catch） |
| 8 | 🟡 med | AgentController 8 端点补 `@RequirePermission("agent:read")`：listAgents/getAgentDetail/listAgentSkills/getSkillDetail/getAgentAccess/listAgentPermissions/listAgentKbBindings/copyAgent；prompt 遮蔽已由 service 实现（AgentService:134 canReadPrompt→setConfig(null)，:150 redactSkillDetail），无需补 | `agent/controller/AgentController.java`、`agent/service/AgentService.java`（已存在） |
| 10 | 🟢 low | **已修（D5 delete 删字节+删行 / cleanAfterIndex 清 orphan），代码核实无误** | `file/service/FileStorageService.java` |
| 11 | 🟢 low | **已核实安全**：`WebSocketAuthInterceptor.beforeHandshake` 验 `jwtUtil.isTokenValid`，失败 return false 拒升级。无需改 | `chat/config/WebSocketAuthInterceptor.java` |
| 9 | 🟡 med | AuthService.register 顶部加 `checkRegisterRateLimit`：IP + 用户名双维度，Redis 固定窗口 60s/5 次，超阈值抛 `RATE_LIMIT(429)`；Redis 故障降级放行；IP 取 `X-Forwarded-For` 首段（经 Nginx 反代）。常量 `RATE_LIMIT_IP_PREFIX`/`RATE_LIMIT_USER_PREFIX`/`REGISTER_MAX_PER_IP=5`/`REGISTER_MAX_PER_USERNAME=5` | `auth/service/AuthService.java` |

---

## ✅ 全部完成（16/16）

无未完成项。残留风险见下。

---

## ⚠️ 部署前必做（运维动作，代码无法替代）

1. **轮换密钥**（#2）：改 PostgreSQL 密码 + `openssl rand -base64 64` 重生成 JWT 密钥。git 历史里的旧值（DB `aa64221886` / JWT `bXlT...2026==`）永久可追溯，光改代码不够
2. **设环境变量**，否则启动 fail-closed：
   - `DB_PASSWORD` / `DB_USER` / `DB_HOST` / `DB_NAME`
   - `JWT_SECRET`（不可等于旧弱默认值）
   - `RUNTIME_CALLBACK_TOKEN`（新生成，sidecar 出站请求须带 `X-Runtime-Token` 同值）
3. **sidecar 改造**（#1）：Python sidecar 回调 `/api/runtime/callbacks/**` 时必须加 Header `X-Runtime-Token: <同 RUNTIME_CALLBACK_TOKEN>`，否则全 401

---

## ⚠️ 残留风险（已记录，非阻塞）

- **#3 SSRF**：未防 DNS rebinding（校验时公网 A 记录，请求时改内网）；需连接期 IP 绑定才完全防。已覆盖直接填内网地址场景
- **#1 回调**：未加 IP 白名单（sidecar 可能跨主机，避免误杀）；共享密钥为主要屏障
- **#11 WS**：握手拒绝未显式设 HTTP 401 状态码（Spring 默认行为），但「协议不升级」安全目标已达成
- **#2 副作用**：本地开发现在必须显式配 `DB_PASSWORD` / `JWT_SECRET` 环境变量才能启动（之前靠默认值）

---

## 下一窗口操作

1. 本读本文件
2. `git status` / `git diff` 核实未提交改动是否齐全（main 工作树）
3. 全部 16 项已落地，无待办。可补残留风险项（#3 DNS rebinding / #11 显式 401 状态码）
4. 部署前运维动作见《项目部署需要文件/部署傻瓜流程-服务器端.md》Step 9 + Step 12
