# 人工测试遗留问题修复 · 开发进度4（2026-08-26）

> 对应 plan：[plans/人工测试遗留问题修复.plan.md](../../docs/plans/人工测试遗留问题修复.plan.md)
> 本轮：子计划 D 全部完成（D1-D6：备注三入口 → 账单昵称 → 项目组分配 tab → 项目组对账 tab → 找回密码两修复 → 文档勾销）。

## 本轮完成

### D1 用户备注三入口 + 管理筛选（12x，commit ae8290e9）
- `user_remark` 列（V156）：注册页 / 个人中心 / 用户管理编辑三处可填改；本人改自己 + admin 改任何（Q6=A）。
- admin 用户列表 +「有备注」筛选 + 备注列；`auth/dto` + remark 字段透传。
- **偏离**：spec §6.3 原只规划注册页——个人中心/管理页同需求，属同一用户故事，spec 已补记。

### D2 账单昵称三处（20x-1，commit d20500ca）
- 用户余额/充值记录/用户排行三处行带 name，显示「昵称（账号）」；keyword 筛选支持按姓名/昵称命中（LIKE 通配符转义）。

### D3 账单「项目组分配」tab（20x-2，commit e791c2d5）
- 后端：新 `GroupAllocationMapper`（members×users×groups JOIN + MEMBER_* 流水聚合子查询，ref_id CAST BIGINT=成员 userId）+ `GroupAllocationRowVO`；`BillingQueryService.groupAllocations`（关键词转义/分页封顶 100）+ `GET /billing/admin/group-allocations`（`usage:view`）。
- 前端：`BillingAdminView.vue` 新 tab——每用户每组的 累计上限/已消耗/项目内剩余/累计被分配（毛额）/已收回/净额/最近分配时间；组长行额度「不限」。
- 单测 +2（关键词转义+分页、零总数短路+封顶）。

### D4 账单「项目组对账」tab（20x-3/Q9=A，commit c2cea2ec）
- 后端：新 `GroupReconcileMapper` 单 SQL（组池白名单 `ALLOCATE/RECLAIM/CONSUME/REFUND` 子查询 + points_ledger `GROUP_ALLOCATE/GROUP_RECLAIM` 个人腿交叉）+ `GroupReconcileRawVO/RowVO/VO`；`BillingReconcileService.groupReconcile`：恒等式 `组池余额=划入净额+退款−消耗`，diff≠0 或双账本差≠0 入异常组并落审计（`group_reconcile_diff`，FAIL）。
- **白名单钉死**：MEMBER_*（非资金腿）/BACKSTOP（组长个人兜底不动组池）/ADMIN_ADJUST（delta=0）必须排除——含任一即永久假警报。单测用 @Select 注解反射钉住该契约。
- 前端：新 tab——「账平/发现异常组」状态卡 + 五合计卡 + 异常组表（差值/双账本差标红悬停口径）。
- 单测 +4（全平/恒等式 diff/交叉 diff/SQL 白名单反射）。

### D5 找回密码两修复（12x，commit 482cd517）
- 重置邮件 HTML 加灰底纯文本链接块（word-break）——部分客户端长 token 链接超宽不可复制。
- `ResetPasswordView` 无 token 进入按 `GET /auth/channels` 分支：SMS 开→SMS 表单；EMAIL 开→guide「从邮件链接进入+去登录页重发」；邮件关→admin 引导。**偏离**：持 token 者即便邮件开关关仍进 EMAIL 表单（token 即凭证）；channels 接口失败→全开兜底防死白屏。
- 前端测试：新 `ResetPasswordView.test.ts` 5 例（token/SMS/guide-email/guide-admin/接口失败兜底）。

### D6 文档勾销与漂移清理（本轮，见下 commit）
- 7 份人工测试文件「未解决」逐条勾销（7x/6x/2x/9x/12x/17x/20x，注修复设计 §节 + commit）。
- spec《积分计费系统.md》4 处口径同步：B1/B6/决策2/字段说明——余额恒 ≥0、HOLD/DEBT、媒体预估硬拦（V80 CHECK/V157 debt）。
- feature-map 增补 2026-08-26 节：积分计费系统（HOLD/DEBT+三 tab）、项目组与积分划拨（A 降职缩额+MEMBER_* 流水+D3/D4）、认证系统增强（D1/D5）。
- user-ops 增补 2026-08-26 节 ×3：积分计费（HOLD/欠款/两新 tab）、项目组（降职缩额/留痕/成员剩余拦截）、认证（纯文本链接/重置页引导/备注）。
- 速查表 `08-智能对话与流式.md`：+聊天计费段（HOLD 预扣/PROGRESS 流中报数/取消折算，Q2/Q3/Q4=B）。

## 回归
- 后端全量单测 **EXIT=0**（后台任务，含全部本轮新旧用例）。
- 前端 vitest 全绿 + `vue-tsc --noEmit` 0 错（D3/D5 后各跑一轮）。

## 实现期修正（记录到 spec/feature-map）
1. D1 admin 编辑备注端点超出 §6.3 字面——同一用户故事，spec 补记。
2. D5 token 持有者不 channels 拦（token 即凭证）；channels 拉取失败全开兜底。
3. D4 白名单经代码读证：BACKSTOP 落流水但不动组池——排除出恒等式，否则永久假警报（单测钉死）。

## 关键代码位置
- `billing/mapper/GroupAllocationMapper.java`、`billing/dto/GroupAllocationRowVO.java`、`BillingQueryService.groupAllocations`、`BillingController` `/admin/group-allocations`
- `billing/mapper/GroupReconcileMapper.java`、`billing/dto/GroupReconcile*.java`、`BillingReconcileService.groupReconcile`
- `auth/service/EmailService`（纯文本链接块）、前端 `views/ResetPasswordView.vue` + `.test.ts`
- 前端 `views/BillingAdminView.vue`（两新 tab）、`api/billing.ts`

## 下一步
无——子计划 A/B/C/D 全部完成，plan 全勾，主计划收口。Phase 4（sdd-phase4-run）人工验证待用户安排。
