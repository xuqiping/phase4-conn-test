

# 参考feature-map、user-ops
- agent-platform\workflow_output\docs\feature-map\项目资产库.feature-map.md
- agent-platform\workflow_output\docs\user-ops\项目资产库用户操作手册.md

>解决完后已同步更新对应的feature-map和user-ops（2026-08-14）
>2026-08-26 修复III（§9 Chunk C/D 九项）已同步增补至：无限画布创作页、项目资产库 各 feature-map 与用户操作手册「2026-08-26 增补（修复III）」节
>2026-08-27 修复V（上游视频首帧/副本重进恢复）已同步增补至：无限画布创作页、项目资产库 各 feature-map 与用户操作手册「2026-08-27 增补（修复V）」节

# 已解决（2026-08-26 勾销，修复设计 §3/§5）
1. **选参后显示预估 + 不足即拒** ✅ —— 三处落地：
   - 独立图片/视频生成页：GET /api/media/estimate 按所选参数（模型/时长/分辨率/比例/张数）实时估积分，
     提交按钮下方显示「预估消耗 X 积分」；预估 > 余额/组池红字预警（C 计划，commit `bcadec5`→`a3d3721`）。
   - **无限画布**：画布图片/视频节点提交前同样走预估接口展示 + 不足直接拒（C 计划，规格 §3 画布缺口）。
   - 提交侧服务端硬拦：余额/组池/成员配额（项目内剩余 quota−used，17x-2 口径）< 预估 → 任务不创建直接报错
     （A/C 计划，commit `30d11f05`/`7271f1c8`）——前端提示只是体验，服务端才是闸门。
   - 提交成功即 HOLD 预扣，防并行任务「预检都过、结算不够扣」（C 计划）。

# 已解决（2026-08-26 勾销，修复设计III §9，Chunk C/D，commit `0770434c`/`5453e59b`）
1. **节点拉伸** ✅（C1 复验+补链）—— 节点四角/四边拖拽改尺寸，resize 结果随画布自动保存落库（落库链此前缺失已补）。
2. **图片节点选管理员默认图模型** ✅（C2）—— 图片节点模型下拉默认选中管理端标记的默认图模型（默认标记读媒体模型列表接口，无标记回落第一个）。
3. **视频比例选择太窄** ✅（C3）—— 视频比例选择器在属性面板**独占整行**（原与时长同挤一行被截断），各比例完整可见可选。
4. **节点创建副本** ✅（C4）—— 节点卡片菜单「创建副本」，复制节点及配置为新节点。
5. **各节点默认大小一致** ✅（C5）—— 16:9 与 9:16 产物定型后节点尺寸统一（定型收口 updateNodeData），生成结果不撑爆布局。
6. **点击图片/视频节点放大** ✅（C6+D1 Lightbox）—— 点击产物缩略图弹 Lightbox 大图/播放，滚轮缩放图片。
7. **任何变动自动保存** ✅（C7）—— 防抖自动保存+右上「保存中/已保存/失败重试」状态徽标+未保存离开确认弹窗。
8. **上游节点面板+悬浮放大+双击@引用** ✅（D2/D3）—— 选中媒体节点侧栏「上游」面板分层展示全部上游（缩略图/视频/提示词，带节点名，不挤在一起）；悬浮预览放大；点击开 Lightbox 全窗（滚轮缩放）；**双击图片/视频=以 @引用 插入输入框末尾**并聚焦（生成中禁插）。
9. **@选项跟光标** ✅（D3）—— @候选弹层改为**光标锚定**：定位在 @ 字符处（上方优先、越界翻转下方），不再固定输入框底部；宽度 240px 最多 8 行滚动，键盘↑↓回车 Esc 可用。

# 已解决（2026-08-27 勾销，修复设计IV §5，commit `58f64beb`/`b88f61fb`/`d843b8a6`）
1. **两段式点击（首次点节点不弹大图）** ✅（B 轮 C-1，commit `b88f61fb`，决策 7「严格两段式」）——
   第一次点击图片/视频节点=仅选中（出属性面板）；**再点节点内的图片/视频缩略图本身**才弹 Lightbox 大图/播放。
   画布上不再「一碰就弹窗」。
2. **上游直接全展示（分媒体类型）** ✅（B 轮 C-2，commit `b88f61fb`）—— 选中节点时「上游」面板不再折叠成
   「点击展开」——上游链全部直接展示，按媒体类型区分（图片/视频/音频/文本各有图标与样式），一眼看全。
3. **四角+四边拖拽改尺寸** ✅（B 轮 C-3，commit `b88f61fb`）—— 选中节点四角手柄热区加大（视觉 8px+外扩，
   实际判定域约 22px，不再「点不中」），四条边新增 Line 拖拽控件可拉宽拉窄（热区收在内侧不挡连线）；resize 结果随画布自动保存落库。
4. **添加节点+失焦自动保存** ✅（C 轮 C-4，commit `d843b8a6`）—— 新增节点即入保存链（structure-changed→防抖保存，
   此前「加节点不保存」缺口已补）；节点文本输入完毕**失去焦点即自动保存**（blur 延迟后判定未点候选层才触发，
   @选人不受误伤）。
5. **分辨率选择器整行** ✅（A 轮 C-5，commit `58f64beb`）—— 视频节点分辨率选择器在属性面板独占整行，
   各档位完整可见可选（同 III 期「比例整行」手法），不再窄条截断。
6. **右侧面板可拖宽** ✅（B 轮 C-6，commit `b88f61fb`）—— 属性面板左缘可拖拽调宽，宽度记忆在
   localStorage（`canvas.propPanel.width`，非法值回落 260），刷新保持。
7. **新建节点即定型宽高** ✅（C 轮 C-7，commit `d843b8a6`）—— 新建图片/视频节点即按产物定型尺寸（16:9/9:16
   统一宽高）创建，与生成结果后一致，生成完成不再撑爆布局/跳变。
8. **副本完全独立** ✅（C 轮 C-8，commit `d843b8a6`，用户决策「我要副本完全独立」）—— 创建副本不只复制文本：
   **图片/视频等产物一并复制**，副本引用新资产记录/fileId 拷贝——对副本重生成、编辑、入库**均不影响原节点、
   原文件与资产库内容**（零共享）。

# 已解决（2026-08-27 P4 回归修复，commit `d4bfc3c3`）
- **「手柄明显了但实际无法拖拽」** ✅ —— 用户冒烟反馈（IV B3 拖柄加大后暴露的深层回归，自 III 轮起就存在）。
  真因两个叠加：① 裸用 `ResizeControl` 不像 `NodeResizer` 有 NodeId 注入兜底，`nodeId` 未传时组件
  watchEffect 早退、d3-drag 根本不绑——补传 `useNode()` 的节点 id；② `.canvas-node` 根元素
  `overflow:hidden` 把库定位悬在节点框外（bottom/right:-8px）的角柄连渲染带命中一起裁掉——根去裁剪，
  圆角下放 accent/body 各自兜。实测（Playwright）：角柄拖 364→427px 实时变、松手落库、刷新保持
  544×384；四边线控单轴拉高 384→416px 同通；vue-tsc 0 / vitest 846 全绿。

# 已解决（2026-08-27 勾销，修复设计V §3，commit `3c7926b0`）
1. **上游视频显「视」字占位** ✅（V A1，2x#1）—— 上游面板视频类节点不再只显「视」字：有源直接
   `<video preload="metadata">` 渲染**首帧预览**（muted/playsinline；video 层 pointer-events:none，
   单击仍开 Lightbox 手势不变）；无源回落原「视」占位；悬浮放大 hover 缩放规则 video 同图片。
2. **副本重进内容消失** ✅（V A2，2x#2）—— 副本创建本身产物复制没问题，缺口在**重进恢复链**：持久化
   快照不含 previewUrl，而恢复只认 taskId 腿——副本是新任务无 taskId → 重进变空壳。补 **fileId 兜底腿**：
   恢复时无 taskId（或 taskId 查询失败）→ 用 fileId 走画布预览接口补 previewUrl+status；生成中节点不兜底
   （交 resumePendingTasks 续命）；fileId 失效静默保持空壳（现状口径）。版本回滚链恢复同享此兜底。


# 已解决（2026-08-28 起以下 5 项已实现，挂「待人工验证（修复VI）」——P4 过测试方案 H1-H12/L1-L6 后逐项勾销）
- 无限画布中，在其他地方复制的图片无法复制粘贴成一个新的图片节点
  > **已实现，待人工验证（修复VI）**（2026-08-28，VA，commit `619fecae`）：画布空白处 Ctrl+V 粘贴图片文件即建图片节点（焦点在输入框时不拦截）；人工用例见 `docs/测试方案/人工测试遗留问题修复VI测试方案.md` H1-H3。
- 无限画布中，同时需要支持图片、视频、音频等素材本地拖拽进来直接生成节点
  > **已实现，待人工验证（修复VI）**（2026-08-28，VA，commit `619fecae`）：OS 拖文件入画布按 MIME 分流建图/视/音节点，未知类型/超限 toast 拒；上限单源 `mediaLimits.ts`（图 30MB/视 50MB/音 15MB）。
- 无限画布中，创建副本功能需要和原本该节点所有的连线保持连着
  > **已实现，待人工验证（修复VI）**（2026-08-28，VB，commit `619fecae`）：副本克隆原节点全部入边+出边（自环成副本自环，原边不动）；`nodeClone.test.ts` 11 用例过。
- 无限画布中，视频生成节点，提示词太长的话，是不是会导致被阶段视频无法生成
  > **已实现，待人工验证（修复VI）**（2026-08-28，VC，commit `b8fda394`）：提示词 maxlength=8000（平台闸门口径）+ 官方建议值文案（≤500 汉字/1000 英文词）；后端 8000 兜底零改动。
- 无限画布中，上传附件图大于8MB会报错：400 · 附件过大: 卧室外.png（image ≤8MB），这个是cdance2.0的限制，还是我们自己系统做的限制？
  > **已解决口径 + 已实现，待人工验证（修复VI）**（2026-08-28，VD，commit `9e68287d`）：8MB 是**我们自己系统**的限制（非官方；Seedance 官方单图 30MB）。已切签名公网 URL 传输（不再 base64 撑大 body）+ 上限 8→30MB 对齐官方。
  > **补遗（2026-08-30）**：VD 只改了**执行层**（后端 KIND_MAX_BYTES + 前端 mediaLimits 预检），三处**展示文案**仍残留 8MB——①视频生成页参考图 hint `≤8MB/张`（实拦 30MB，文案说谎）；②帮助中心文章《视频生成（SeedDance）》（V150 种子）同句；③SeedDance 用户手册两处。已补齐：hint 改引 `KIND_LIMIT_LABEL` 单源（图/视/音三行全走常量防再漂移）+ V167 迁移修存量库文章（本机 dev 库已同步 UPDATE）+ 手册改 30MB。

# 已解决（2026-08-29 勾销，修复VII P4 Playwright 实测 J1-J8/K1-K10 全过，commit `2b7b7ed0`~`1095e04a` + 实测修复 `07d63ae2`）
1. **画布节点复制粘贴** ✅（VII-1）—— Ctrl+C 复制选中节点（框选多选），Ctrl+V 在鼠标处成组粘出，带选中集**内部连线**（诱导边口径）、与原节点彻底脱钩（同「副本完全独立」）、连按 +32 错开、Ctrl+Z 一步撤回；无选中 Ctrl+C 即恢复外部图片粘贴通道。实测：落点 bbox 中心对鼠标 (502.5,619.75)vs(500,620)、相对几何分毫保持、label 三级去重、正文逐字不重写（落库核对）。
2. **一键优化布局（LibTV 式）** ✅（VII-2）—— 工具条 ✨「一键整理」：dagre 从左到右分层重排（上游左下游右、孤立另列）；有选中只排选中子图（整组拉入、集外不动），无选中排全图（隐藏节点照排）；fitView 收全图、Ctrl+Z 一步还原、坐标落库刷新保持。实测 LR 严格递增 + 全 16 网格 + 组包围盒 rAF 跟随。
3. **实测附带修复** ✅（`07d63ae2`）—— 单选按 Esc 后 `selectedNodeId` 隐形残留（属性面板挂旧节点、✨ 被隐形选中拉进子图模式）：`syncSelectionFromLibrary` 补 0 选中分支通用收口。该缺口自 S5 轮即存在，修复VII 的 ✨ 把它放大暴露。
4. **交叉 review 附带修复** ✅（`5d4402c2`，P4 第二 AI 独立审出）—— 粘贴体 data 深拷贝（断嵌套对象跨副本共享，守住「副本完全独立」）+ 诱导边快照断响应式引用/remapEdges 剥会话 class（防复制后点边把选中高亮永久烤进新边）。

# 已解决（2026-08-29 起以下 2 项已实现，挂「待人工验证（修复VIII）」——过测试方案 M1-M11 后逐项勾销）
- 无限画布中的测试组是一个整体，而不是每个节点单独对外拉线了，应该是整个组对外拉线
  > **已实现，待人工验证（修复VIII）**（2026-08-29，A 轮，commit `4cd830eb`）：组包围盒右缘中点输出圆点/左缘中点输入圆点；组边伪 id `group:{id}` 独立集合（不进 VueFlow v-model）；语义=外部→组**广播**全员/组→外部**聚合**全员产物（拍板）；成员级边并存保留；组→组、组线×删除/选中删除、解散级联删、Ctrl+Z 恢复全链路。人工用例 `docs/测试方案/人工测试遗留问题修复VIII测试方案.md` M1-M8。
- 如果我的线拉动经过某个节点，在节点中放下，那就直接连接上这个节点，而不是必须节点拖到另外一个节点的点上
  > **已实现，待人工验证（修复VIII）**（2026-08-29，A 轮，commit `4cd830eb`）：拖线松手落节点**本体**=按起拖侧方向直连该节点；落自身=静默；落组包围盒空白=建组边（先于 quick-add 判定）；落空白=quick-add 照旧；拖拽建边补自环/同向去重（与程序化 addEdge 同口径）。人工用例 M9-M11。

# 已解决（2026-08-31 起已实现，挂「待人工验证（修复IX）」——过测试方案 P1-P12 后逐项勾销）
- 无限画布中，对画布内原有节点的ctrl+c、ctrl+v，粘贴出来的节点应该保留原有的节点连接关系
  > **已实现，待人工验证（修复IX）**（2026-08-31，Chunk B，commit `fb0e3981`/`2152c81b`，用户决策 Q4）：
  > 画布右下工具条新增 ⛓「连线保留」开关（默认开、localStorage 记忆），**一个开关管两处**——Ctrl+V 粘贴与节点「创建副本」：
  > 开=保留选中集内部连线 + 跨集边连回原上下文（允许平行重复边、粘贴当下态生效、悬挂自动丢）；
  > 关=两处都不保留原节点连线（干净副本）。人工用例 `docs/测试方案/人工测试遗留问题修复IX测试方案.md` P1-P12。

  # 已实现（2026-09-01 起，挂「待人工验证（修复X）」——R 系列核心项 P4 冒烟自动化已过 27/27（smoke-x.mjs），剩体感/断网/真实生成消费项人工勾销）
  - 无限画布中，视频节点无法上传视频
    > **已实现，待人工验证（修复X）**（2026-09-01，A 轮，commit `97d6547f`，用户决策 Q1「上传即节点产物」）：
    > 视频节点属性面板顶部新增「上传视频」按钮（accept=video/*，≤50MB 与拖入链同源 mediaLimits 单源）；
    > 上传即节点产物——fileId+预览落节点，可 @引用/抽帧/截取，重传覆盖，非生成参考通道；
    > 三 kind 大小预检前端化（图 30MB/音 15MB/视 50MB 超限 toast 拒不发请求；MIME 未知跳过预检交后端闸）。
    > 人工用例 `docs/测试方案/人工测试遗留问题修复X测试方案.md` R1-R4。
    > **P4 冒烟已过**（R1/R2/R3 自动化：上传产物+C11 出现+重传换 fileId+三预检 toast 零请求）；剩 R4 断网失败态、R1 下游 @候选联动人工。

  - 无限画布中，从库选择，又没有图片/视频首帧+播放预览了（需要鼠标悬浮旁边悬浮放大展示，点击图片大图展示，点击右侧选择则真实选择）
    > **已实现，待人工验证（修复X）**（2026-09-01，B 轮，commit `9c0cf4c6`/`5c7c6625`，用户决策 Q2「全类型预览」）：
    > 行改版四态缩略——图=缩略图+悬浮 300ms 放大（带尺寸行）、视=首帧+▶+悬浮首帧放大、
    > 音=「音」字标+行内播放条、文=textPreview 片段；点击缩略图开 Lightbox 大图（可滚轮缩放）/视频播放，
    > 灯箱 z-index 3000 盖住弹窗，Esc/遮罩/× 关闭后弹窗仍在；**交互三分离**——点缩略=预览、
    > 点「选择」=真实选择（resolve+弹窗关）、行空白零动作（防误选）；懒加载 IO 门控+LRU（进视口才拉）。
    > 人工用例 R5-R10。
    > **P4 冒烟已过**（R5-R9 核心自动化：hover 放大/灯箱 z3000/Esc 关灯箱弹窗留/四态行/空白零动作/选择 resolve）；**冒烟揪出并已修 1 个 bug**——Esc 一次连关灯箱+弹窗（Lightbox 键盘监听改捕获+stopPropagation 逐层退）；剩断网回落、音频点播只播不选、悬浮手感人工。

  - 无限画布中，连线保留模式下，复制与组相连接的节点，连线无法保留，只有非组连接节点可以复制连线
    > **已实现，待人工验证（修复X）**（2026-09-01，C 轮，commit `85d9ffd1`/`45723976`，用户决策 Q3「新节点连原组」）：
    > ⛓ 开时组端点边随复制粘贴/创建副本保留——节点↔组两向（组→节点广播 / 节点→组聚合），
    > 新节点=组**外部对端**（不入组员）；复制后解散组→粘贴丢该边不产断边；组→组边永不收集；
    > 组边进独立组池（伪 id 不进 v-model），几何/落库/Ctrl+Z 一步撤全走既有链。⛓ 关=两处零组边（原行为）。
    > 注：IX 测试方案 P10「组边不带入粘贴体」口径已被本轮取代（诱导边仍不带组端点，跨集组边带）。
    > 人工用例 R11-R15。
    > **P4 冒烟已过**（R11-R15 核心自动化：两向组边粘贴 4 条+v-model 零伪 id+副本不入组员/Ctrl+Z 一步撤/⛓ 关零组边/解散丢边零悬挂/落库刷新重现）；剩 R15①② 组边真实生成消费（广播/聚合取产物）人工。
