# P02 开发进度 1 —— B1 批（Step 0 还债 + Step 1 云端骨架）

> 日期：2026-08-15 ｜ 范围：FR-041/050（P02 plan Step 0~1）

## 本轮做了什么

### Step 0 · P01 交叉审查三笔债（commit `fix: P01 还债三件`）
1. **BUG-P01-01 大白话**：`src-ui/lib/ipc.ts` errMessage 加翻译表——IPC 原始 JS 错误→「内核通信未就绪，请重启应用再试」；网络错误→「网络连接失败…」；内核大白话原样透传。测试：`lib/ipc.test.ts`（3 用例）。App.test.tsx 的 mock 改 `importOriginal` 保留真实 errMessage。
2. **BUG-P01-02 toast 遮挡**：`App.tsx` toast 8s 自动消失（useEffect + setTimeout）。
3. **归位策略**：`stores/project.ts` applySnapshot 仅 `prev?.phase !== dto.phase` 才 setView——用户看驾驶舱不被内核事件拽回；首次快照仍归位。applySnapshot 导出为测试接缝。测试：`stores/project.test.ts`（4 用例）。
4. **transition 事务化**：`machine/mod.rs` transition 的 UPDATE+INSERT 历史收进**同一 write 闭包**（写队列串行=原子）；`history.rs` 拆出 `record_on(c, …)` 供复用。测试：`concurrent_transitions_do_not_tear_state_and_history`（4 线程并发，恰好 1 成功，状态=历史行数）。

### Step 1 · NestJS 骨架（commit `feat: P02 Step1`）
- `PROJECT/cloud/`：Nest 10 + ConfigModule；全局异常过滤器（APP_FILTER provider 挂 AppModule——**踩坑：只在 main.ts 挂的话测试实例不生效**）输出统一 `{code,msg,data}`，未知错误只给大白话不透堆栈；`/api/v1/healthz`；`docker-compose.yml`（pg16+redis7+flyway10 一次性迁移容器）；`.env.example` 全配置开关；e2e（supertest）2 用例过：healthz R 形态 + 未知路由 404 R 形态。
- **环境限制（如实记录）**：本机无 Docker → compose/Flyway 运行时验证待 Step 9 CI（或用户本机 `docker compose up` 手测）。

## 测试与质量
- vitest 19（新增 7）/ cargo test --workspace 21（新增 1）/ cloud e2e 2 —— 全绿
- check_all 全绿后 commit

## 下一步
B2 = Step 2 云端建表 V1/V3 + 种子脚本。
