# 企业级精准知识库 RAG · 开发进度 29

- 主题：完成审计后的 P2 索引控制面真实性修复。
- 发现：原 `KnowledgeIndexOperationsService` 使用 JVM 内存 Map，重启后 snapshot/route 全丢；任意合法格式 snapshot 均可切换。
- TDD RED：新增 `KnowledgeIndexOperationsServiceTest`，缺少 `SnapshotStore/SnapshotRecord` 导致 4 个编译错误。
- GREEN：新增 V112、MyBatis Snapshot Store；只允许已登记 snapshot；首快照激活 Alias；active/previous/configVersion 持久化。
- 验证：`KnowledgeIndexOperationsServiceTest`、`IndexAliasServiceTest`、`KnowledgeAdminIndexControllerContractTest` 共 4/4 通过。
- 安全：仍只接受逻辑 snapshot ID，不接受物理索引名；切换/回滚继续要求二次确认和 `knowledge:manage`。
- 下一步：补齐 P2 “启动实际重建/进度/失败重试/取消”闭环，再进入 P3/P4 生产链路接入。
