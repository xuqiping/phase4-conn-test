# 企业级精准知识库 RAG · 开发进度 32

- 主题：P3 QueryPlan 控制扩展与 OpenSearch 多通道生产入口。
- RED：服务级测试要求精确问题关闭 Rewrite/HyDE，并调用带强制 FilterContext 的生产召回 Gateway；因 Gateway 和四参扩展接口不存在而编译失败。
- GREEN：QueryPlan 的 `requiresLlmAnalysis` 控制 Query Expansion；精确/版本/条款问题只做规范 Query embedding，不调用改写 LLM/HyDE。
- 生产召回：新增 `ProductionRetrievalGateway` 与 OpenSearch 实现；Exact/Sparse 走 read alias，强制 tenant、KB、ACTIVE、ACL、可见 documentIds 前置过滤。
- 兼容：OpenSearch 未启用或请求失败时返回空通道，由现有 PG Dense/BM25 继续服务；OpenSearch 命中与 PG 候选按 nodeId 合并后进入同一 Ranking Engine。
- 验证：`RagRetrievalServiceTest`、`RetrievalFilterBuilderTest`、`QueryExpansionServiceTest` 共 15/15 通过。
- 安全：不可用 Java 后过滤替代 OpenSearch Pre-filter；候选正文仅进入已配置 Ranking 模型，不写日志。
- 下一步：完成 P3 回归和文档存档，进入 P4 Coverage/Context/Citation/Grounded Answer 真实链路。
