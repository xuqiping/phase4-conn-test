# 视频模型接入扩展II · 开发进度 2

> 第 3 轮对话（Chunk E-H）。上轮见 `开发进度1.md`（A-D）。**硬规则**：≤5000 tokens。

| 字段 | 内容 |
|---|---|
| **日期** | 2026-08-30 |
| **本轮范围** | Chunk E（提交分流+估价）/ F（结算分流+resultText）/ G（前端）/ H（全量验证+文档） |
| **完成状态** | ✅ 全部完成；剩 N1-N8 真中转人工验收（需用户 key） |

---

## Chunk E · 提交分流+估价（commit `51a314e8`）

- `MediaGenTaskService`：doSubmit 分流——regeneration 先 `requireRegenerationSource`（null 拒/不存在 404/非本人非 admin 403/非 SUCCEEDED 拒/跨 provider 拒/源是附属任务拒/arkTaskId 空 拒/超 7 天拒），context-ir 照常 validate；resolvedType 固定 REGENERATION/CONTEXT_IR；再生成 config 只存 `{sourceTaskId, sourceArkTaskId(字符串), duration(继承源), resolution:"2k"}`。
- 估价：`estimateContextIrPoints`——入 = ceil(字数×0.75)+图×1500、出固定 4000 token，computeCost(KIND_CHAT)；再生成 = estimateVideoYuan(继承时长,"2k")。estimate 端点 +`promptChars` 参数。
- `MediaSubmitRequest.prompt` 去 @NotBlank（附属分支服务端校验）；`+sourceTaskId`。
- 测试：ServiceTest +6（context-ir taskType/再生成校验链 5 拒/极简 config+二次计价/跨 provider 400/估价公式）。

## Chunk F · 结算分流+resultText（commit `6e95040c`）

- `MediaGenTaskWorker.handleSucceeded`：CONTEXT_IR 分支**先于** resultUrl 空判（文本任务无 url，顺序反了会误判下载失败）→ `handleContextIrSucceeded`：storeText(".md")→settleMediaSuccess 14 参（KIND_CHAT，实报 in/out token 多退少补）→ fail-closed（结算 null+计费开 → 退款+markFailed 不交付）→ markSucceeded；日志只记**文本长度**不记内容，审计 detail_json 同样不含文本（安全约定）。
- `MediaStorageService.storeText`：空拒/1MB 上限/fileName hint+".md"/mime text/markdown/同 storeStream 咽喉点。
- `MediaBillingService.settleMediaSuccess` 14 参重载（+tokensOutput）；13 参老口委托——视频/图片路径与既有 mock 零影响。
- `MediaGenTaskWorker.buildRequest`：config.sourceArkTaskId 数字或纯数字字符串两形态 → MediaGenRequest.sourceTaskId。
- 测试：WorkerTest +2（文本落库+CHAT 双腿结算/无文本标 DOWNLOAD_FAILED）、BillingTest +1（in/out 双腿）。

## Chunk G · 前端表单/画布/结果（commit `c52d486e`）

- `api/media.ts`：ratio +4:5/5:4/9:21（HH 官方 9 值）；taskType +CONTEXT_IR/REGENERATION；prompt 可选+sourceTaskId+promptChars；`isContextIrModelId/isRegenerationModelId`；`fetchMediaText`（鉴权拉 .md）。
- `VideoGenView`：**再生成模式**=源任务下拉（SUCCEEDED+同供应商+7 天窗口客户端预滤）整替提示词/附件/参数区，提交仅 `{model,sourceTaskId,projectGroupId?}`；**context-ir 模式**=藏时长/分辨率/水印/音频（时长固定 5 过校验带，ratio/resolution 空档省略不发）；**能力驱动显隐修复**——applyCapabilityConstraints 越界回退改能力清单首项（原硬编码 '16:9'/'720p' 在 minimax 768p/2k 档留脏值被后端拒，连带修 duration 下夹 min）；ratio 空列表（i2v）藏控件+提交省略。
- 预估：context-ir 随提示词传 promptChars（watch +prompt.length）；再生成随源任务继承时长×'2k'。
- 结果：CONTEXT_IR 拉文本展示+「下载 .md」（无 video 播放器/不入库资产库）；历史「结果」列绿 tag「增强文本」、入库按钮隐藏；restoreTaskForm 回看再生成任务回填源任务。
- 画布：PropertyPanel 视频模型下拉**过滤附属档**（附属入口只在独立页，节点形态不匹配）；i2v 藏比例控件；CanvasView submitVideo ratio 不再硬补 '16:9'。
- 测试：VideoGenView.test.ts +M1-M5（t2v 藏上传区/i2v 藏比例+提交省略 ratio/源过滤含 4 反向+极简载荷/ctxir 表单+promptChars=8/文本结果渲染）；PropertyPanel.test.ts media mock 改 importOriginal 透传（新导出未 mock 导致 3 挂，修后 48/48）。

## Chunk H · 全量验证+文档（本提交）

- 后端 `mvn test`：**2683/2683 绿**。前端 `npm test`：**962/962 绿**；`vue-tsc && vite build` 过。
- 本地起后端（kill 旧实例 → local-dev-env 起）实测：
  - Flyway `Migrating … "166 - seed minimax h3 happyhorse providers"` ✓ success
  - DB：2 provider 行（ACTIVE，api_key_enc 空）+6 价表行 ✓
  - `GET /api/media/models`：6 模型能力全对——t2v 0 附件/9 ratio、i2v ratios=[]、r2v 9 图、minimax 9/3/3+768p/2k、regeneration 全零锁 2k ✓
  - 估价：context-ir 8 字=92.04 积分（=(6×5.8+4000×23)/1e6×1000）；t2v 1080p 5s=6000 积分 ✓
  - 再生成缺源任务 → `{"code":400,"message":"再生成须提供源任务（sourceTaskId）"}` ✓
- 文档：feature-map（`docs/feature-map/视频模型接入扩展II.feature-map.md`）、user-ops（`docs/user-ops/视频模型接入扩展II用户操作手册.md`，含填 key 步骤+输入侧不计费说明）、3x 问题单未解决节回填 ✅、本 README+开发进度2。

## 本轮坑（沉淀）

1. **worker Long→Integer**：usage* 是 Long，settle/markSucceeded 取 Integer → `toInt(Long)` 收口。
2. **estimate 测试遗留定时器**：上一用例 400ms 防抖在 clearAllMocks 后补枪 → 断言用 lastCall 不用 toHaveBeenCalledWith。
3. **模块 mock 新导出**：组件新增 import（isContextIrModelId 等）而旧测试 vi.mock 工厂白名单没含 → computed 炸 3 挂；mock 工厂一律 `importOriginal` 透传。
4. **fetchVideoBlob 返的是 objectURL 字符串不是 Blob**——要文本得新开 fetchMediaText（responseType text）。
5. 积分口径 = 元×1000（1000 积分=¥1），文档初稿写 ×100 已修。

## 下一步

- P4 人工验收：用户填 key（设置→全局模型供应商两行）→ 测试方案 N1-N8。
- 后端本地实例已起新代码（8080，backend-run-hhx.out）。
