---
功能: 网课录屏总结
日期: 2026-08-07（Step12 集成收尾完成；Phase3 代码任务全部结束）
Phase: Phase3 开发实现（Step1-12 done；剩 Phase4 人工验收）
---

# 网课录屏总结 · 开发进度5

> 接续 [网课录屏总结_开发进度4.md](网课录屏总结_开发进度4.md)（Step9/11 细节在那里）。
> 本轮 = Step12 集成收尾全部 5 项，对应 `05_集成与收尾.plan.md`（已勾选 + 回填）。

## ✅ Step12-① feature flag 运维开关（`c6f19cf`）

- **新模块 `src-tauri/src/feature_flags.rs`**：三层覆盖 = 环境变量 `VTT_COURSE_SUMMARY`（off/0/false/disabled/no 视为关）> `<app_config_dir>/feature_flags.json`（`{"course_summary": false}`）> 默认开；配置缺失/损坏一律降级为开（不拖垮主功能）。
- **lib.rs**：新命令 `get_feature_flags`；`ensure_course_summary_enabled()` 在 `start_capture_session` 入口兜底拒绝（服务端不依赖前端隐藏）。
- **App.vue**：onMounted 拉 flag → 关停时隐藏「网课总结」Tab + mode 强制回 transcribe + 课程区 `v-else-if="courseEnabled"` 防御；拉取失败按默认开。
- **测试**：+5 单测（环境变量解析/缺文件/关停/坏 JSON/缺字段 serde default），总 75 绿；check_all 全绿。

## ✅ Step12-② 文档同步（`6ec2e4b`）

- `file_structure.md`：补 session/screen/ocr/align/summary/feature_flags 模块 + 前端 5 组件 + `sessions/` 运行时目录说明 + plans 子目录。
- `architecture.md`：技术栈补 4 行（WGC/oar-ocr/reqwest/keyring）；网课数据流图；模块图重写；IPC 命令表补 18 条新命令；关键设计决策补 4 条（统一时间轴/会话隔离/隐私红线/asset:// 帧图/feature flag）。
- **意外发现**：`workflow_output/docs/` 此前被根 .gitignore 的 `docs/` 规则拦截从未入库（PRD/plan/spec 全靠本地）。按 agent-platform 先例加 `!voice-to-text/workflow_output/docs/**` 例外，本次 25 个文档随 commit 首次入库。**若不希望这些文档入库，回退 .gitignore 该两行即可。**

## ✅ Step12-③ 收尾产物（`9592c0b`）

- **功能 README**（C 类受众）：`开发进度/网课录屏总结_README.md`（扁平命名，随进度文件约定）——用户地图 + 技术说明 + DoD 勾选（D 验证留 Phase4）。
- **Feature Map**：`docs/feature-map/网课录屏总结.feature-map.md`——代码位置总览 + 调用链路 + 8 项技术大白话注解（统一墙钟/WGC 切片/三重判页/oar-ocr/中点对齐/map-reduce/asset:// /feature flag），每项带踩坑批注。
- **User-Ops**：`docs/user-ops/网课录屏总结.用户操作手册.md`——首次配置→录制→录后处理→学习区→草稿编辑→导出→运维关停 7 节 + 隐私说明 + 常见问题表。
- 坑：check_docs 按扁平路径校验相对链接，模板假设子目录（`../../docs/`），扁平存放需用 `../docs/`。

## ✅ Step12-④ 安全检查

- **cargo audit**（v0.22.2，advisory-db 走 ghfast.top 镜像到 `D:/dev-cache/advisory-db`，`--no-fetch` 本地跑）：749 依赖扫描。
  - 3 个漏洞全是传递依赖：**quick-xml 0.39.3 ×2**（RUSTSEC-2026-0194/0195，DoS，tauri 构建链 plist 引入；app 运行期不解析不可信 XML → 低风险，待 tauri 上游升 plist 后复跑）；**rkyv 0.7.46**（built 构建期依赖，不处理外部输入 → 可忽略）。
  - 20 个 warning 均为 gtk3 系 unmaintained（Linux 传递依赖，Windows app 不涉及）。
  - 本功能新增直接依赖（oar-ocr/reqwest/keyring/windows-capture/image）**零直接漏洞**。
- **隐私红线复核 ✅**：`map_reduce.rs` 图片仅 `vlm_on && vlm_model 已配` 时加载（默认纯文字上行）；SummaryPanel 多模态开关默认关 + `window.confirm` 二次确认（文案含 base_url）；API Key 走 keyring windows-native（真凭据库测试标 `#[ignore]`）。

## ✅ Step12-⑤ 规范沉淀 + 收尾

- `通用约束.md` 新增「运维通用约定」：新功能默认带 feature flag（三层覆盖 + 双保险 + 缺省降级）+ traceId 约定。
- 挂起人工项已登记 05 子 plan 备注 + 总览「下一步」。

## ⏭️ 下一步（全部转 Phase 4，需用户人工）

1. **Step8 真实 API 冒烟**：填 Key → 测试连接 → 实跑一次 summarize（挂起自 Step8）。
2. **2h 真实网课端到端验收** + AC-101~109 逐条签字（测试方案已备：`docs/测试方案/网课录屏总结测试方案.md`）。
3. **回归**：现有纯录音转文字功能不受影响。
4. Phase 4 review（8 维度 + spec 符合性）→ 全绿后归档本功能（进度文件已 5 个，交付后按归档机制压缩移 `archive/`）。

## 备注

- cargo audit 复跑姿势：`cargo audit --db 'D:/dev-cache/advisory-db' --no-fetch`（GitHub 直连/全局 git 死代理 127.0.0.1:6454 均不通，advisory-db 需镜像克隆）。
- 构建环境变量清单见用户记忆 voice-to-text-build-env（LIBCLANG_PATH + anaconda bin 入 PATH 缺一不可）。
