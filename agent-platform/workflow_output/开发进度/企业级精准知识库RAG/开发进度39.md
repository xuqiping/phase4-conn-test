# 企业级精准知识库 RAG · 开发进度 39

- 主题：P5 评测 Dataset/Case 领域服务与 JSONL 协议。
- RED：新增 `EvaluationServiceTest`，要求 Dataset 创建、JSONL 逐行容错、合法行导入、tenant 隔离导出；旧 `EvaluationService` 只有指标 getter，测试编译失败。
- GREEN：新增 Repository 边界和 Dataset/EvalCase 领域记录；导入逐行解析，错误仅返回行号与短摘要；导出先验证 tenant 归属。
- 安全：导出不包含 Chunk 正文；错误摘要限制长度；跨租户 Dataset 明确拒绝。
- 验证：`mvn '-Dtest=EvaluationServiceTest,RagMetricsCalculatorTest' test`，2/2 通过。
- 下一步：实现 V115 PostgreSQL Repository、管理 Controller 与前端真实 API。
