# 开发进度 1 · Plan A：补差口径与对账下钻

> 功能：人工测试遗留问题修复II（7x/8x/9x/12x 十项）
> 主 plan：`docs/plans/人工测试遗留问题修复II.plan.md`；本篇=A 计划全程（A1-A6）。

## 本轮产出（2026-08-26）

| Chunk | 内容 | commit | 测试 |
|---|---|---|---|
| A1 | BACKSTOP 补差计入消费成员 used：`addUsedUnconditional`（无 quota 守卫）+ `backstop` 增 `consumerUserId` + 三调用方透传（媒体全量/媒体补差/聊天补差——**LlmBillingService 为计划漏记的第三方**）+ 不变量②改写 `Σ(CONSUME−REFUND+BACKSTOP)==used` + 锁序注释（个人→组池→成员，与 chargeGroup 同向无 AB-BA） | 8e45a9e | 单测 49 绿（含 3 处 verify 更新）；IT 12 绿（+2 新例：计入/quota 耗尽仍计入） |
| A2 | FAILED usage 两处 record 补 `projectGroupId`（组任务缺口可按组过滤）；BACKSTOP 组流水 remark→「组池不足·补差兜底，差额由组长个人承担（计入成员已用）」 | aadcf5e | 单测 verify 升 14 参；IT 补 remark 断言 |
| A3 | **V159** 存量回填（SQL DO block，非计划的 Java Migration——仓库零 Java 迁移先例+locations 未配包扫描）：consumer 映射链 ①ref_id 数字→media_gen_tasks.user_id ②同组同 ref 的 CONSUME(HOLD) 腿 actor ③映射不到跳过+NOTICE；幂等=remark 追加 `used-synced(V159)`；A1 后新行（remark 含「计入成员已用」）被过滤排除 | ae8000c0 | 本地 PG 实测：媒体7+聊天3→used 10→20；orphan 跳过；二遍零变更；Flyway 注册成功 |
| A4 | `/billing/admin/group-reconcile` 增 `groupId`/`includeAll`；`selectGroupRawRows` 动态 WHERE；VO 增 `groups[]`（下钻口径含平组，abnormalGroups 保留兼容）；语义=单组优先/全组/默认仅异常（Q9=A 原状回归） | 4b252481 | 单测 11 绿（+4 新例） |
| A5 | 对账 tab：组下拉+「显示全部组」开关（选中组时禁用）+状态列（平/异常 tag）+单数据源 computed（防闪烁） | e27180b0 | vue-tsc 全绿 |
| A6 | 回归+本文档 | — | 见下 |

## 关键文件

- `projectgroup/mapper/ProjectGroupMemberMapper.java` — addUsedUnconditional
- `projectgroup/service/ProjectGroupWalletService.java` — backstop/不变量②/锁序
- `billing/service/MediaBillingService.java`、`billing/service/LlmBillingService.java` — 调用方
- `db/migration/V159__backstop_used_backfill.sql`
- `billing/service/BillingReconcileService.java`、`billing/mapper/GroupReconcileMapper.java`、`billing/dto/GroupReconcileVO.java`、`billing/controller/BillingController.java`
- 前端：`views/BillingAdminView.vue`、`api/billing.ts`

## 偏差记录

1. **迁移版本**：计划 V158 → 实际 V159（V158 已被 `group_ledger_member_types` 占用）；D 计划顺延 V160。主 plan 已记。
2. **第三方调用方**：A1 坑点表预判的 grep 命中 LlmBillingService.backstopChat——计划原文只列媒体两处，已补。
3. **A3 实现形态**：SQL DO block 替代 Java Migration，理由记录于迁移头注释。
4. **锁序**：初版成员 UPDATE 放组池锁前（AB-BA 死锁隐患），实现期自查改 个人→组池→成员。

## 测试修复（顺带）

- `ProjectGroupWalletServiceIT.clean()` 全量清 OWNER 个人流水（原只清 ref_type='GROUP'，DEBT_REPAY/ADMIN 腿跨 run 残留致 IncorrectResultSize）。

## 待人工验证（并入功能级测试方案，A6 项）

- 问题原话场景复演：组流水两条 + 成员已使用=实耗 + 账单=实耗 + 组长侧含差额，四侧闭环
- 对账 tab：选账平组见完整行+「平」+顶卡=该组；清筛选回默认；开全部组异常标红
- V155 HOLD 回归：组池够/不够/退差三态数值不变

## 下一步

Plan B（审计全量 B1-B7）→ C（验证码）→ D（价表三改 V160）→ E（实时刷新）。
