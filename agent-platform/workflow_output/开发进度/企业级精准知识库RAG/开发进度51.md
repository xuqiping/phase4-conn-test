# 开发进度51：P5 灰度回滚联动 OpenSearch read route

- 日期：2026-08-14
- 分支：`beifen`

## 已完成

- 明确区分 Ranking `configVersion` 与 OpenSearch `snapshotId`：前者控制重排配置，后者控制 read alias 指向，禁止用一个字符串猜测两种资源。
- 新增 V118，为每个当前/上一灰度状态分别保存对应索引快照 ID。
- 应用灰度时记录当前 active snapshot；一键回滚先切回上一快照 read alias，再恢复上一 Ranking 灰度版本，最后失效知识库答案缓存。
- 无快照升级场景保持兼容，只回滚 Ranking route 与缓存，不执行伪 alias 切换。

## TDD 与验证

- RED：回滚测试要求切换上一 snapshot，但现有 Rollout 状态没有快照字段和索引路由接口，测试编译失败。
- GREEN：`RagRolloutServiceTest,PostgresRagRolloutRepositoryTest,EvaluationMigrationVersionTest` 共 4/4 通过。

## 下一步

执行 P5 后端组合回归、前端测试和构建，更新最终 README、Feature Map、User-Ops、测试方案和开发进度总览后停止。
