# P01 界面冒烟验证记录（Phase 4 · Run 步）

> 日期：2026-08-15 ｜ 方式：真实驱动运行中的界面（Playwright + 系统 Edge，`channel: msedge`，零浏览器下载）
> 脚本：`PROJECT/desktop/scripts/smoke-ui.mjs`（可重复执行）｜ 截图：本目录 `0*.png`
> 环境说明：浏览器环境无 Tauri IPC（`window.__TAURI__` 缺失），验证的是**界面壳层**；
> 含 IPC 的创建项目/状态机转移已在真窗口验证（见开发进度6 / TC 表人工走查）。

## 结果：8 步冒烟全过 ✅

| # | 步骤 | 结果 |
|---|---|---|
| 1 | 外壳渲染（顶栏品牌可见） | ✅ 01-外壳.png |
| 2 | 三栏骨架（topbar/sidebar/center/rightbar） | ✅ |
| 3 | 密度切换（舒适⇄紧凑，data-density 变更） | ✅ |
| 4 | 管道条联动：点「建造」→中栏切视图+右栏归位日志 Tab | ✅ 02-建造视图.png |
| 5 | 导航联动：点「需求」→视图切换+管道条高亮 aria-current | ✅ 03-需求视图.png |
| 6 | 错误 toast 展示与关闭 | ✅ 05-错误toast.png |
| 7 | 右栏五 Tab 手动切换 | ✅ 04-右栏文件Tab.png |
| 8 | console 错误收集 | ⚠️ 3 条（见下） |

视觉核验（截图分析）：三栏布局正确；管道条「想法→需求→计划→建造→验收→部署」完整；
驾驶舱 HUD 指标卡（进度/缺陷/覆盖率/消耗）与三轮时间线渲染正常；深色+辉光呈现，无明显错位空白。

## 发现的缺陷（记入总览待办）

| 编号 | 严重度 | 描述 | 处置建议 |
|---|---|---|---|
| BUG-P01-01 | 中 | IPC 不可用时 toast 显示原始英文 JS 错误（`Cannot read properties of undefined (reading 'invoke')`），违反「前端只显示大白话」约定（plan 安全清单） | init 失败 catch 后转大白话文案（如「内核通信未就绪」） |
| BUG-P01-02 | 低 | 错误 toast 固定右上且拦截右栏 Tab 点击，关闭前无法操作被遮挡区域 | toast 加自动消失（如 8s）或 `pointer-events` 收窄 |
| — | 低 | console 404（favicon 未配置） | 补 favicon |

> 说明：BUG-01/02 在真 Tauri 窗口（IPC 正常）不触发，属降级路径缺陷，不阻断交付，随 P02 一并修。
