# 开发进度7 —— Phase 4 运行验证（Playwright 冒烟 + 性能 + 交叉审查）

> 2026-08-27。sdd-phase4-run 产物。环境：后端 8080（fix-IV 代码）/ 前端 5173 / PG 5432 / Redis 6379，Playwright MCP 实测。
> 冒烟数据：现建 组长 smk4_leader(190)/成员 smk4_mem(191)/组外 smk4_out(192)，组「冒烟四期组」(id=5)，备注 冒烟A班/冒烟B班。

## 一、冒烟矩阵（对照测试方案 L1-L14）

| 项 | 用例 | 结果 |
|---|---|---|
| S1 备注汇总 | 按 remark 汇总 tab 渲染（未填备注19人/余额136094.07/消耗21244.15/调用723 + A班1 + 冒烟A班2，与 E 轮 psql 对账全平）；用户余额 tab 备注列；keyword「冒烟A班」过滤命中 2 人（B班排除） | ✅ |
| S2 UserPicker | 批量充值：focus 才弹下拉；全选 3/3、反选 0/3、半选+反选 2/3，chips 增删同步（L1 正/边1/边3） | ✅ |
| S3 默认 0 | 邀请弹窗打开不弹下拉（L2 正）；额度不填提示「不填=0」「不限额度已停用」；邀请落 0→接受→成员行 0·已用 0；调限额空=前端拦、100 落库（T1） | ✅ |
| S3 受限视图 | 成员 tabs=成员+产出；组长他人行额度「-」组织信息全显；本人行完整；组长行「不限（遗留）」（T2/L14 正+反1+边） | ✅ |
| S4 两段式 | 点 header=选中+属性面板不开灯箱；点视频媒体=`.lbx` 灯箱开；Esc 关（L6 正1/正2） | ✅ |
| S4 手柄 | 选中节点 resize-control=8（4 corner handle+4 line）（L8 正1/正2） | ✅ |
| S4 拖宽 | 拖分隔条 100px：260→360 实时变；localStorage `canvas.propPanel.width`=360；刷新恢复 360（L9 正+持久化） | ✅ |
| S4 自动保存 | 调色板加文本节点：4→5，徽标「已保存 15:55:06」；刷新 5 节点在（L10 正1/C-4） | ✅ |
| 审计认人 | `/admin/logs/audit` smk4_mem 行=`smk4_mem（冒烟成员）`+`冒烟A班` tag（L4 正） | ✅ |

自动化基线不变：后端 2566 / 前端 846 + vue-tsc 0（F 轮已录）。

## 二、性能评测（对照 P1：本轮无 performance_goals 硬指标）

remark-summary（E 轮新聚合端点，实库 4 桶）：3 连发 **p50≈13ms / p95≈18ms / code 200**——单机单查询+LIMIT 1000 口径下无瓶颈。画布操作均为本地态+防抖落库，无慢点。结论：无需优化，记档。

## 三、冒烟发现并处置（运行→报错→修→再运行）

1. **文档偏差①（已修文档）**：审计列实际格式=「账号（现姓名）」（username 快照在前），6 处文档误写「姓名（账号）」→ 12x勾销/日志系统FM+UO/README/开发进度6/测试方案L4 已正。
2. **文档偏差②（已修文档+记已知边界）**：「组财务不透出」措辞过宽——实际 detail/overview 组池/在途对成员置 null（ProjectGroupService:523-524），但**页顶「组池」徽标走既有 mine 口径**（V139 起选择器数据源，listMine 不裁）仍可见 → 17x勾销/项目组FM+UO/README/测试方案L14 已改为精确表述+已知边界注记。裁剪 mine 属跨域改动（画布「参与项目」选择器等共用），不动代码，留待后续轮次决策。
3. **FM 文件归属误记（已修）**：getDetail 裁剪单点在 ProjectGroupService（非 QueryService），FM 表已拆两行校正。
4. **既有环境问题（非 IV 回归）**：WS `ws://localhost:5173/ws/events` 401（stores/projectGroup.ts:110，7x-3 时代 Vite 代理），不阻断功能，沿用 III 轮结论。

## 四、17x 用户新增问题答疑（P4 内闭环）

「组池只有 1000、成员限额 10000，还能正常生成任务吗？」→ **能，设计如此**：限额闸只看成员行 used+预估≤quota（ProjectGroupWalletService:300）；资金瀑布组池扣尽→名下余额→组长兜底（settleShortfallByLeader:381，组长钱包不够也挂账兜底不阻断）；used 超 quota 段才转欠款。已写入 17x 勾销第 5 条。

## 五、交叉审查（spawn 第二 AI）

- 审查者：cavecrew-reviewer 子代理（独立会话），范围 `58f64beb^..df128e07`，对照 IV 设计规格安全清单+联动 L1-L14+Feature Map。
- **结论：0 高 / 1 中 / 6 低，放行 P5**。
- 通过面：updateQuota 权限校验在前拒 null 在后；candidates 只放宽读；remark-summary 权限+LIMIT+转义；无 v-html；非成员 403 有测试；chargeGroup/锁序/幂等本轮零改动；两段式依赖 nodeDragThreshold 链路成立；blur 候选 mousedown.prevent 成立；副本四件保留/taskId 清空判重天然不撞；宽度回落有测试；MINB 160/64 齐。

发现处置（运行→报错→修→再运行）：

| # | 级 | 发现 | 处置 |
|---|---|---|---|
| 1 | 中 | listMine() 对 MEMBER 仍返真实组池余额，前端页顶徽标直显——与 D3「detail/overview 组财务裁剪」口径不一致（非本轮回归，V139 既有） | **修订声明+记录口径**（审查给出的路径 B）：四套文档已改精确表述+已知边界注记；裁剪 mine 属跨域改动（画布「参与项目」选择器共用），列入后续轮次决策，不阻断本轮 |
| 2 | 低 | updateQuota 拒 null 只抛 400 无 WARN——plan 运维清单声称「WARN 含操作人」 | **已修**：补 log.warn(groupId/actor/member)，运维清单兑现 |
| 3 | 低 | QueryServiceTest 成员流水测试未断言 actorUserId 过滤（删掉过滤照样绿） | 记档：代码过滤在（冒烟+IT 旁证），测试断言强度留待后续加固（断言 LambdaQueryWrapper 内部较脆） |
| 4 | 低 | service.addMember javadoc 陈旧「null=不限」+可复活 NULL 写入口（无生产调用方）；Controller:55/MemberAddRequest:7 同 | **已修注释×3**：行为不动——5+ 存量测试靠 addMember(null) 造「不限行」fixture（存量 NULL 运行时语义合法存在，决策 1），生产两口（邀请接受/池审批）已落 0 |
| 5 | 低 | spec §5.6「窄屏回落 260」未实现（clamp 只看存储值不看视口） | 记档：低频场景（窄屏+已拖宽过），后续加视口上限或 CSS max-width 兜底 |
| 6 | 低 | spec §3.4「默认 tab 逻辑不变」vs 实现全员默认落 members tab（良性） | **已回补 spec 偏离记录**（D4，成员首屏即见组织信息，更贴决策 6） |
| 7 | 低 | 文档「角 12px 判定域」vs 实际 8px 视觉+inset -7px≈22px（偏保守方向） | **已修×5**：2x/FM/测试方案/进度6/README 数字改实际值 |

## 六、速查表核验

`docs/run-guide/快速启动速查表.md` 已存在且内容有效；本轮**零迁移/零新端口/零新依赖/启动方式无变**，不触发更新条件，保持现状。

## 七、冒烟环境处置

冒烟账号 smk4_leader/mem/out（190/191/192）+ 组「冒烟四期组」(5) 保留供人工复测；不清理（无敏感数据，用户可自行删除或留作回归锚点）。
