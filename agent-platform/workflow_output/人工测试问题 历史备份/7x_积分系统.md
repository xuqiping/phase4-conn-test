# 已解决
1. 新增价表改为从全局供应商的未配置模型下拉选择；已有 provider 专属价和历史全局价占用的同名模型均不会重复出现，后端创建接口同步防绕过
2. 图片模型设置单价报 500 ✅ —— 根因 `LlmProviderMapper.selectByIdForUpdate` 用 `deleted = false` 撞 PG 的 INTEGER 列（`V6:32`），所有 kind 创建价表都会 500，用户先配图片撞到而已。改为 `deleted = 0`；前端 savePricing 加 sanitizePricingPayload 清非 VIDEO 行的 videoBillingMode 泄漏（commit 1）
3. 价目表导出 / 模板上传 ✅ —— 镜像 LLM 供应商 export/import：`GET /pricing/export`（blob 下载）、`GET /pricing/template`（联动供应商未配置模型，区分 LLM/图片/视频）、`POST /pricing/import`（upsert 按 providerId+model+kind+hasReference，存在覆盖价格，上限 200，逐行容错）。前端三按钮 + 二次确认 + 客户端预检（commit 2）
4. 视频区分有无参考视频定价 ✅ —— V95 加 `has_reference BOOLEAN NOT NULL DEFAULT FALSE`；`findEffective` 加谓词 + fallback 到 false 行（不区分的模型配 1 行即可）；worker 从 `request.getAttachments()` 的 `kind=="video"` 算标志（不用 taskType，它被重载不可靠；首尾帧图 kind=="image" 不算参考视频）；前端表单加「是否含参考视频」开关 + 列表「参考视频」列（commit 1）
5. 视频生成后查看推送参数 + 是否有视频参考标志（用户追加）✅ —— `MediaTaskVO.hasReference` 计算字段（list/detail 都返）；VideoGenView 任务头标签 + 历史列；Canvas 节点角标 + 属性面板接 MediaTaskRequestDetails 面板。**推送参数已脱敏落库在 request_config.providerRequestSnapshot，无需新 DB 列**（commit 3-4）
6. 视频模型只能配有参考视频，或者单独配无参考视频 ✅ —— 根因：新增价表候选把「已配过无参考价的 VIDEO 模型」整条过滤掉，而编辑又禁止改身份（hasReference 不可改），UI 上没有任何入口能补第二行。修复（2026-08-14）：
   - 后端 `PricingConfigService.availablePricingModels`：VIDEO 候选改为「两面（有参考/无参考）都配齐才排除」，只配一面时候选保留并带 `hint` 提示（如「已配『无参考』价，本次新增为『有参考』价行」）；
   - 前端候选下拉 label 追加 hint；`onCandidateChange` 切候选时复位 `hasReference`（防上一行遗留值泄漏）；编辑模式 hasReference 开关 disabled（与后端「身份不可改」对齐，保存不再报错）。
   - 验证：`PricingConfigServiceTest` 30/30（新增 2 例：只配 false → 候选保留带 hint；两面配齐 → 候选消失）；实测 Cdance2.0 配完无参考后候选仍出现（带提示），API 创建 hasReference=true 行成功后候选消失。
7. 项目组与积分划拨（原未解决 #3）✅（2026-08-18，计划5 Step1-8 全落地）——
   - **数据**：V133 四表（组 / 成员[含 quota_limit_points+used_points] / 组池钱包 / append-only 流水 balance_after）+ llm_usage_logs.project_group_id、media_gen_tasks.project_group_id/estimated_cost 加列；V134 权限码 project-group:manage。
   - **划拨**：组长个人↔组池（条件 UPDATE 防负；回收按「余额−在途」封顶）；成员按人限额（超限 400 拦截，调低不追溯已耗）。
   - **五入口**：智能对话/知识库问答/图片/视频/画布顶栏「参与项目」选择器（余额徽标；画布快照顶层持久化）；同步消耗 chargeGroup 幂等记账；异步媒体三重预检+估价快照，结算组池尽→BACKSTOP 组长个人兜底；失败退款回组池+回减已用。
   - **项目推进模块**（/project-groups 菜单）：我的组卡片→组长视角（成员管理/划拨回收/流水/全员产出筛成员/类型/日期；成员仅看自己行）；产出标记成员账号+积分消耗（媒体行附任务状态/prompt 摘要；文件本体不透出保归属门控，本人可跳任务详情预览）。
   - **验证**：后端 2166 绿 / 前端 588 绿；人工测试方案（L1-L7 联动含反向/边界）`docs/测试方案/项目组与积分划拨测试方案.md` 待 P4 统一跑。
8. 账单调用明细加项目字段（原未解决 #4）✅（2026-08-18）—— admin 调用明细与用户「我的消耗」均加「项目组」列+筛选（LEFT JOIN project_groups 不滤软删留历史名；个人行/旧数据显「—」；admin 下拉数据源 /billing/admin/project-group-options）。
9. 参与项目选项统一到页面上方（原未解决 #5）✅（2026-08-20，`7c4d630f`）—— 五处分散选择器（对话/KB问答/图片/视频/画布）收拢为页顶 AppHeader 唯一选择器：Pinia `stores/projectGroup` + 单键 `project_group_id` 唯一真相；旧入口遗留选择一次性 `adoptLegacy` 收养（全局已设不覆盖，旧键留作回滚）；各页面原位置改小标签只显计费去向；切组画布自动 scheduleSave。测试：store 8 用例 + vue-tsc 清 + 全量 669 绿。
10. 页顶展示项目组积分（可多个）与个人积分（原未解决 #6）✅（2026-08-20 同上）—— 页顶选择器旁「组池 xxx」+「个人 xxx」徽标（挂载并行加载，路由切换轻刷新；多组余额在选择器下拉逐项显示可对比切换；加载失败徽标静默隐藏不崩）。
11. 充值记录六字段（原未解决第 1 条）✅（2026-08-21，计划「自助充值支付与账单总览」Step1-6 全落地）——
    - **字段**：我的钱包「充值记录」卡 + admin 账单「充值记录」tab 均六字段齐：时间 / 渠道 / 付款账号（渠道侧付款人，mock 可模拟）/ 金额¥ / 对应积分（下单时比例快照落单，档位中途变更不回溯）/ 充值后余额（points_ledger balance_after，一单一流水 uq_ledger_ref 保证）；另附状态列 + 累计充值金额/积分条（「用户最大积分总额」语义=累计充值积分，与「历史所有充值金额汇总」并列展示）。
    - **状态机**：PENDING→PAID/FAILED/CLOSED 全走条件 UPDATE 抢态；30 分钟过期 job 自动关单；用户可取消；取消/过期后渠道仍推真实付款 → 不自动入账，安全事件+审计+对账异常列表人工补单。
    - **幂等三层**：uk_payment_channel_order 部分唯一索引 → markPaidIfPending 抢态 0 行=已处理 → uq_ledger_ref(PAYMENT,orderId) 兜底；前端 idemKey=会话 UUID 防双击双扣（同键同金额返原单/不同金额 409）。
12. 接入支付宝/微信支付（原未解决第 2 条）✅ 架构落地（2026-08-21 同上）——
    - **拍板**：支付架构+接口先落，mock 通道全链路可测，真实商户后接。渠道路由 `PaymentChannelRouter`（routeForCreate 未开通 400 / route 回调不限可用性）；Alipay/Wechat 骨架已留配置位（available()=false，接入步骤见类 javadoc）。
    - **mock 通道**：HMAC-SHA256 签名（密钥永不出服务端）+ 过期防重放 + 常量时间比较；mock 收银台走与真实渠道**完全相同**的 handleNotify 链路——测的就是真链路。
    - **安全**：notify 端点匿名放行 + IP 限流 120/60s；验签失败/金额不符/未知单/终态后付款全记 KIND_PAYMENT_NOTIFY_REJECT 安全事件 + 审计；prod profile + mock-enabled 同开启动即拒（PaymentConfigGuard）；渠道单号日志掩码。
    - **运维**：`/admin/reconcile/payment-anomalies` 三节异常（PENDING 超 10min 未关 / PAID 无流水 / 终态后仍付款），只读不自动修。
13. 支付密钥网页自填（用户追加：密钥不想走环境变量）✅（2026-08-21，`736ca763`+`7faa5127`）——
    - **V143 配置表**：payment_channel_config（channel CHECK 白名单 ALIPAY/WECHAT + 部分唯一索引）；密钥**整体 AES 加密落库**（复用 LLM Key 同管线 AesEncryptService），明文不入库；`config_tails` 脱敏尾巴列供回显，读取端点永不解密出库。
    - **admin「支付渠道」页**（/admin/payment-channels，权限码 `payment:config`，V144 授 admin）：双卡片表单，已配置字段 placeholder 显 `****尾4`，**留空=保持原值**（merge 语义，改一键不重填全部），保存二次确认+@AuditLog；alert 明示「配置齐全≠渠道上线」（渠道 available() 维持 false，SDK 实现落地才翻转，防半配置暴露给用户）。
    - **解析桥**：`PaymentChannelConfigService.getDecrypted` = DB 优先、env 兜底——SDK 接入时渠道类直接调用，网页配置与 env 两种方式都兼容。
    - 单测 8 例（密文无明文/短值全遮/缺键·未知键·未知渠道 400/merge/env 兜底/脱敏列表）+ 前端 vitest 5 例；后端 2348 全绿 / 前端 719 全绿。

## commit 索引（计划·自助充值支付与账单总览，2026-08-21）
- Step1 V140 迁移 `010f918b`；Step2 支付域核心+MOCK `c3635312`；Step3 端点 `2d54cc21`；Step4 过期 job+对账 `e79a17ef`；Step5 admin 充值记录+用户余额 `f1ce2e57`；Step6 前端 `e7183e78`

## commit 索引
- commit 7（2026-08-25 下午，V153 返修）： 预估秒价改一行多分辨率参数+串味清理
- commit 6（2026-08-25，末两条）：`d55b69bc` `feat(billing): 7x 末两条——视频SECOND分辨率分行定价(V152 resolution)+媒体消耗前预估管控`
- commit 1（Chunk A+C+D）：`feat(billing): 7x-1/3 修复价表500 + 视频has_reference定价维度`
- commit 2（Chunk B）：`feat(billing): 7x-2 价表导出/导入/模板下载 + 修复存量测试构造`
- commit 3（Chunk E）：`feat(media): 7x-4 视频任务 hasReference 审查标志 + 推送参数展示`
- commit 4（Chunk F）：`feat(canvas): 7x-4 画布视频任务审计面板接入 + 参考视频标志`
- commit 5（2026-08-14）：`fix(billing): 7x-6 视频价表两面参考维度候选修复`
- 计划5 系列（2026-08-17/18）：33dce223 建表 / a7461417 管理端点 / 8fb8bd54 chat 链 / a3f051b5 媒体链 / ad51b161 五入口选择器 / bec63cfa 推进模块 / 8bb6fe3e 账单项目列

# 支付宝/微信真实支付接入指南（傻瓜式）

## 一、现在是什么状态（一句话）

系统里**模拟支付（mock）已全链跑通**（下单→扫码页→回调→积分到账→记录，全真的流程，只是不用真钱）。支付宝/微信是**「水管坑位留好、真水管没接」**：代码骨架在、配置坑位在，但现在调用就提示「未开通」。

**接入 = 两件事**：
1. **你做的**：办商户号，把密钥给我（本节下面手把手教）。
2. **我做的**：拿到密钥后写渠道实现代码（官方 SDK 下单 + 回调验签，骨架里这两块是空的）+ 测试 + 上线。**每渠道约 1-2 天工作量**——所以不是「给我密钥当天就能收款」。

## 二、先备三样硬性条件（没有这些，商户号办不下来）

| 东西 | 说明 | 多久 |
|---|---|---|
| ① 营业执照 | 企业或**个体户**都行 | 已有则 0 天 |
| ② 域名 + ICP 备案 + HTTPS 证书 | 签约网页支付必填；**备案最慢，最先办** | 备案 1-3 周 |
| ③ 对公银行账户 | 个体户可用法人个人银行卡 | 已有则 0 天 |

> ⚠️ 第 ② 项是整个流程的瓶颈。域名没备案，两家都签不了「网页支付」。今天就去办。

## 三、支付宝：给我 3 个串（全在支付宝开放平台操作）

操作网站：**https://open.alipay.com**（用企业支付宝账号登录）

1. **签约**：开放平台首页 → 「产品中心」→ 找「**电脑网站支付**」→ 点签约 → 按提示填营业执照、法人身份证、对公账户、网站域名 → 等审核（约 1-3 个工作日）。
2. **建应用拿 APPID**：控制台 → 「我的应用」→ 创建「网页&移动应用」→ 创建后应用详情页有一串以 `2021...` 开头的数字 = **APPID**，抄给我。
3. **生成密钥对**：下载官方小工具「**支付宝开放平台开发助手**」（开放平台文档页搜得到）→ 打开点「生成密钥」（RSA2）→ 得到两串：
   - **应用私钥** → 抄给我（这个串只在你我手里，别发给任何第三方）；
   - **应用公钥** → 粘贴到第 2 步那个应用的「接口加签方式」里，保存。
4. **拿支付宝公钥**：上一步保存后，同一页面会出现「**支付宝公钥**」（查看/复制按钮）→ 抄给我。

**你给我 = 3 个串**：`APPID` + `应用私钥` + `支付宝公钥`。

## 四、微信：给我 3 个串（两个网站）

操作网站 A：**https://pay.weixin.qq.com**（微信支付商户平台）
操作网站 B：**https://mp.weixin.qq.com**（微信公众平台）

1. **开商户号**：pay.weixin.qq.com → 「接入微信支付」→ 注册商户号 → 填营业执照/法人身份证/对公账户/经营场景（选公众号或网站）→ 审核通过后得到 **10 位数字商户号**（开户邮件和商户平台首页都有）→ 抄给我。
2. **拿 AppID 并绑定**：mp.weixin.qq.com 注册**服务号**（个体户也可）→ 设置页拿 **AppID**（`wx...` 开头）→ 回到商户平台 → 「产品中心 → AppID 账号管理」→ 把这个 AppID 绑定进去 → AppID 抄给我。
3. **设 APIv3 密钥**：商户平台 → 「账户中心 → API 安全」→ 找「**APIv3 密钥**」→ 点设置，自己编一串 **32 位字母+数字随机串**（比如拿密码生成器摇一个）→ 这串同时抄给我。
4. （写码时我确认后可能要）同页「API 证书」→ 申请并下载证书压缩包给我。

**你给我 = 3 个串**：`商户号` + `AppID` + `APIv3 密钥`（+ 可能的证书文件）。

## 五、清单汇总（照这张表收集；**现在不用发我，直接网页自填**）

| 渠道 | 要填的东西 | 来自哪 |
|---|---|---|
| 支付宝 | `APPID` | 开放平台 → 我的应用 |
| 支付宝 | `应用私钥` | 密钥工具生成 |
| 支付宝 | `支付宝公钥` | 上传应用公钥后页面显示 |
| 微信 | `商户号` | 商户平台开户成功 |
| 微信 | `AppID` | 公众平台服务号 + 商户平台绑定 |
| 微信 | `APIv3 密钥` | 商户平台 API 安全自设 32 位 |
| 部署 | 公网 HTTPS 域名（已备案） | 你的服务器/域名商 |

**怎么填（2026-08-21 起，网页自填已上线）**：admin 登录 → 左侧菜单「**支付渠道**」→ 选对应渠道卡片 → 逐项粘贴 → 「保存配置」（二次确认）。
- 密钥**整体加密存储**，页面上永远只显示尾巴 4 位（如 `****3f2a`）；
- **改一个字段不用重填全部**——留空的字段保持原值；
- 每次保存自动记审计日志（谁改的、什么时候）。
> 🔒 这些是钱袋子钥匙：只在该页面填（或发我本人填），别进群、别截图发第三方、别提交进 git。

## 六、填完后的流程（你不用管，列出来让你心里有数）

1. 你在「支付渠道」页填好并保存（密钥已加密落库；环境变量方式仍保留作兜底）。
2. 我写支付宝/微信两个渠道的下单 + 回调验签实现（mock 通道原样保留作测试用）。
3. 填 `PAYMENT_NOTIFY_URL` = `https://你的域名/api/billing/payment/notify/...`（微信/支付宝服务器回调用，**必须公网 HTTPS**）。
4. 用 **0.01 元真实小额** 跑通「下单→扫码→到账→记录六字段」全链 → 对账页确认无异常。
5. 生产环境关 mock（有 PaymentConfigGuard 兜底：生产开 mock 直接拒启动，不会误用）。
6. 上线 ✅ 用户即可选支付宝/微信扫码充值。

> ⚠️ 注意：在支付渠道页**填好密钥 ≠ 渠道已上线**。SDK 实现（第 2 步）未完成前，用户侧充值仍然只显示模拟支付——这是刻意保护，防止用户点到半成品渠道。

## 七、总时间预估

| 阶段 | 谁 | 多久 |
|---|---|---|
| 域名备案 | 你 | 1-3 周（最慢，先办） |
| 支付宝签约+密钥 | 你 | 1-3 天 |
| 微信商户号+密钥 | 你 | 3-7 天 |
| 渠道实现+测试+上线 | 我 | 每渠道 1-2 天 |

# 已解决（2026-08-25）
- ~~积分充值目前非https无法成功访问，请暂时性的解除~~ ✅ —— 根因=`crypto.randomUUID()`（下单/充值幂等键）只在**安全上下文**（https 或 localhost）可用；纯 http（内网 IP）下是 undefined，一点充值就 TypeError 整链崩。代码里没有任何强制 https 的闸门，所以不用"解除"什么——修复=新 `utils/uuid.ts`：原生可用走原生，否则 Math.random 回落 v4（幂等键/本地 id 用途，非安全场景）；RechargeDialog/WalletAdminView/mediaTaskRestore/VideoGenView 全部 7 处调用点切换。vitest 3 用例（原生/undefined/crypto 整体缺失）。后续上 https 后自动走回原生实现，无副作用。
- ~~积分充值需要下拉可选（展示用户账号+昵称/姓名）~~ ✅ —— admin「积分充值/发放」页手填用户 ID 改为远端搜索下拉（label=`昵称/姓名（账号）`，无昵称回落账号）；新端点 `GET /api/billing/admin/user-options?keyword=`（points:recharge 权限，ACTIVE 用户，username/name 模糊，限 20 条；不复用 /api/users 的 user:manage——财务角色未必有用户管理权）。浏览器实测下拉显示「浏览器验证名274（admin）」等。

# 修复设计规格（2026-08-17，未解决项 #3/#4 已规格化）
- [项目组与积分划拨设计.md](../docs/specs/项目组与积分划拨设计.md) —— 拍板：独立项目组实体 + 项目积分池/按成员记账 + 组长配置成员限额；五入口「参与项目」、新模块「项目推进」（产出标记成员+积分消耗）、账单总览项目字段

# 修复设计规格（2026-08-21，未解决项充值两条已规格化）
- [自助充值支付与账单总览设计.md](../docs/specs/自助充值支付与账单总览设计.md) —— 拍板：支付架构+接口先落（mock 通道全链路可测，支付宝/微信留配置位，真实商户后接）；充值记录六字段（用户账号/渠道付款账号/金额/积分/充值后余额/累计充值）；不建金额钱包


# 参考feature-map、user-ops
- agent-platform/workflow_output/docs/feature-map/积分计费系统.feature-map.md
- agent-platform/workflow_output/docs/user-ops/积分计费系统用户操作手册.md

>解决完后已同步更新对应的feature-map和user-ops（2026-08-20）：积分计费系统、项目组与积分划拨两份手册补「页顶统一入口 + 积分徽标」。

# 已解决（2026-08-25 第二批）
- ~~价表视频模型不管 TOKEN/SECOND 都只能填秒价；SECOND 不同分辨率要分行配价~~ ✅ ——
  - **V152**：`pricing_rule.resolution VARCHAR(16)`（仅 VIDEO SECOND 行；NULL=通用兜底）+ `est_yuan_per_second NUMERIC(12,6)`（仅 VIDEO TOKEN，提交期估价专用）；idx_pricing_lookup 重建带 resolution。
  - **SECOND 分辨率价**：同模型同参考面可按分辨率各配一行（480p/720p/1080p/4K×有无参考，最多 10 行）；「通用」行兜住未单列分辨率。询价命中链：精确(参考面,分辨率)→(参考面,通用)→(无参考,分辨率)→(无参考,通用)。resolution 归一化 trim+小写（4K→4k，配置侧/任务侧同函数），属身份字段编辑不可改；新建候选展开为「参考面×分辨率槽位」，已配槽位从下拉消失、hint 标明本候选身份。
  - **TOKEN 表单修复**：选 TOKEN 时表单改显「输入价 ¥/百万」（此前只显秒价根本没法配 token 价）+「预估秒价」；SECOND 才显分辨率+秒价。导出/导入/模板带 resolution/est 两列，upsert 匹配键加 resolution。
  - **V153 返修（用户反馈 2026-08-25 下午）**：① 预估秒价改「一行多分辨率参数」——`est_yuan_per_second` 数值列 → `est_per_resolution JSONB`（键 general/480p/720p/1080p/4k，general 兜底未单列档），TOKEN 行编辑一次配齐不用拆行；② 串味修复——实体可空字段加 `updateStrategy=ALWAYS`（编辑清空真落 NULL，此前 updateById 忽略 null 致 TOKEN 行残留秒价/输出价），V153 顺手清存量脏字段；③ 列表价格列按 kind/mode 过滤显示（TOKEN 行不显秒价、非 CHAT 不显输出价）。
  - **校验**：SECOND 分辨率非支持集 400；TOKEN 带 resolution 400；SECOND 配 estYuanPerSecond 400（SECOND 估价直接用秒价）；非 VIDEO 带这两个字段 400；同 (provider,model,参考面,分辨率) 重复 409。
- ~~视频/图片要预估消耗配置，消耗前先管控积分够不够~~ ✅ ——
  - **估价口径**（`PricingService.estimateVideoYuan`，复用价表单一事实源）：SECOND=分辨率秒价×时长；TOKEN=est_yuan_per_second×时长（补提交期无 token 维度）；图片=张价×张数（原有）。估价 0（无价表/TOKEN 未配预估秒价）不拦截，口径保守同计划5 坑表。
  - **个人钱包预检**（组池/成员限额路径计划5 已有）：submit/submitImage 估价值 > 余额 → 40201「积分不足（预估消耗 xx），任务未提交」，task 不建、并发闸配对释放。
  - **测试**：后端新增 13 例（PricingService 分辨率命中/兜底/estimate 四例；PricingConfig 校验 7 例；MediaGenTask 个人预检 2 例）+ 存量 6 文件 mock 迁移；后端 2379 全绿 / 前端 739 中 735 绿（4 红在 auth/RegisterModal——12x 邮箱验证在途改动，与本修复无关）。
  - **实测**：V152/V153 迁移实库通过；SECOND 720p 建行/重复 409/TOKEN 带 resolution 400/8k 400；个人余额 1000 < 预估 2500（0.5¥/s×5s×1000）→ 40201 文案正确；TOKEN 多分辨率预估 {general:0.1,720p:0.2}——720p 任务按 0.2 估（999<1000 拒）、480p 回落 general 按 0.1 估（400<500 拒）；坏键 8k 400、SECOND 配预估 400；冒烟数据已清理（行删除、est 复位、余额还原）。
  - **已知边界**：V153 起可空字段改 `updateStrategy=ALWAYS`，编辑清空可真落 NULL（est 全清=不预估）；导出文件 est 列为 JSON 对象。

# 未解决
- 图片配置金额的时候，怎么没有预估价？
- 图片和视频生成的时候，会先根据预估积分（还有选中），判断剩余积分是否够生成，如果不够就报错不允许生成（一旦生成任务开始了，就预扣，最终按实际情况多退少补，避免多任务并行导致最后不够扣了）

# 其他
1. 支付宝支付收款正式接入傻瓜式全流程
2. 微信支付收款正式接入傻瓜式全流程（暂时不接小程序、公众号，只接网页页面端）