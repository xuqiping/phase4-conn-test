# 开发进度4 · Chunk C（画布七小件 2x-1~2x-7）

> 对应 `docs/plans/人工测试遗留问题修复III_C_画布七小件.plan.md`
> commit `5453e59b`（21 文件 +496/-19）；后端全量 2544 绿 + 前端 806 绿 + vue-tsc 0 error

## 实现内容

| 项 | 产物 | 说明 |
|---|---|---|
| C1 节点拉伸复验 | `nodes/CanvasNodeBase.vue` + `CanvasBoard.vue` | NodeResizeControl 四角柄已挂 CanvasNodeBase（七型节点全覆盖）。**复验发现缺口**：resize-end 只写 data.width/height 无保存触发链（resize 走 d3-drag 不发 node-drag-stop）→ 补 `provide('canvasNodeResized')` → structure-changed → 防抖落库（同 canvasRemoveEdge 先例） |
| C2 默认图片模型 | 后端 7 文件 + `ProviderManageTab.vue`/`ImageGenView.vue`/`PropertyPanel.vue` | 键 `media.default.image-model` 读宽容（未配/失效返 null 不猜兜底）；`/api/media/image/models` 带 `defaultModel` 标记（MediaModelService 读 SystemSettingService 标匹配项）；设置页「默认模型」卡+生图下拉（IMAGE 候选、PUT validateDefaultModel+@AuditLog）；ImageGenView 初始选中 = localStorage 手选 ‖ 默认标记 ‖ 第一个（手选 watch 记忆）；PropertyPanel 图片节点无 model 自动补默认（onMounted+watch node.id 双时机，覆盖工具条/快连/衍生/上传全部创建路径） |
| C3 比例加宽 | `PropertyPanel.vue` | 视频比例独占整行（面板 260px，原与时长同挤 ~118px）；n-select 弹层 teleport 本不受面板限；键盘可达原生 |
| C4 创建副本 | `nodeClone.ts`（新）+ 面板按钮 + `CanvasView.onCloneNode` | 纯函数：参数深拷贝保留；生成态/旧任务引用/会话态/断链引用（firstFrameNodeId）清空回 idle；+40/+40 错开；label 撞名 addNode 内 uniqueLabel 自动序号；副本不带边/组（平节点口径，现分组不支持整组复制） |
| C5 统一尺寸盒 | `CanvasBoard.updateNodeData` + `ImageNode/VideoNode.vue` | 定型收口一处：status=success 且媒体型且 data.height 缺失（未手拉）→ 320×320+nodeSizeStyle 重推；覆盖全部完成路径（text/image/video run + 媒体任务回调）；子组件 contain 居中（16:9/9:16/1:1 同盒不裁切） |
| C6 单击预览 | `CanvasBoard.onNodeClick` emit + `CanvasView` Lightbox | 单击有产物媒体节点 → `preview-media` payload → 复用 D1 Lightbox；vue-flow 拖动后不发 node-click（库内区分位移）天然防误触；双击普通节点 = focusNodeById 聚焦（director 分支不变） |
| C7 保存徽标+离开确认 | `CanvasView.vue` | `saveBadge` 四态（dirty 黄/saving 蓝/saved 绿灰+HH:mm:ss/failed 红）aria-live；scheduleSave 即置 dirty；自动保存失败 toast（此前完全静默）；beforeunload 非 saved 拦确认；onUnmounted 摘监听清 timer |

## 关键决策

1. **默认模型标记走媒体列表接口**而非 llm-model-defaults（后者 `llm:config` 权限，普通用户拿不到）；管理 UI 进 ProviderManageTab 默认模型卡（既有 chat/embedding 旁加第三下拉）。
2. **C5 定型收口 updateNodeData**：完成态写入分散 6+ 处（各 run 函数+媒体回调），一处判定覆盖全部。
3. **CanvasBoard 吸收用户 WIP 六轮#2**（store 对账兜底 34 行，内聚完整）：C5/C6 必须动该文件，无法分离；先例 5d77a931/0770434c 同口径，commit message 注明。

## 测试

- `MediaModelServiceTest` +3：defaultModel 标记命中/未配置无标记/失效等价未配置
- `nodeClone.test.ts` 4：参数保留/生成态清空/深拷贝隔离/位置错开
- `PropertyPanel.test.ts` +4：C2 默认补全+显式不覆盖+回落第一个；C4 按钮 emit
- `CanvasBoard.test.ts` +3：C5 定型/手拉不覆盖/文本不定型
- **后端全量 2544/2544 绿；前端全量 806/806 绿（115 文件）；vue-tsc 0 error**

### 踩坑

- mockResolvedValueOnce 受严格类型约束而 axios 拦截器已剥 code/message（运行时形状 {data:{data}} 与声明类型不一致）——`as unknown as Awaited<ReturnType<…>>` 对齐。
- vi.mock 工厂内 vi.fn() 无类型约束（顶层 mock 可裸形状），vi.mocked() 后的 mockResolvedValueOnce 才严格。

## 安全/运维勾验（plan 清单）

- [x] C2 PUT 校验 IMAGE 候选集（防非法值入库）+ llm:config 权限 + @AuditLog
- [x] 媒体列表 defaultModel 只读标记，无敏感面（media:gen 权限既有）
- [x] C7 保存失败可观测（toast+徽标+beforeunload 三道防线）

## 下一步

Chunk E（解锁端点 user:update+@AuditLog 只清自动锁 / 锁定话术+姓名编辑 / UserPicker 三处替换）→ F（产出一键入库）→ 收尾（测试方案 L1-L10/README/Feature Map/User-Ops/勾销 2x·12x·17x 复核）。
