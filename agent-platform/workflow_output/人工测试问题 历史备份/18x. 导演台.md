# 未解决
- 导演台任务的肢体都无法调整
- 人物的建模太垃圾了，需要如何调优成真实的人物曲线？可以参照上古卷轴5等游戏捏人，告诉我有哪些是必须要我配合的
- 我希望能够在3D建模中，可以直接通过AI完成人物形象的生成并完成贴图，有没有可能实现？
  - 【2026-08-20 已拍板：选方案 A（AI 生图→立牌），后续统一处理】
  - **方案 A（选此）**：文字描述 → AI 生图（复用既有 ArkImageProvider 生图链路）→ 生成图作为「立牌」元素插入 3D 场景。立牌=平面元素，支持整体摆位/旋转/缩放（gizmo V/R/S）、多机位取景、截图回流全链不变。零新外部依赖；spec 原定「立牌留 P2」，本项即落 P2。局限：无厚度，正侧方机位拍出纸片效应（正面~斜 60° 内构图无碍）；可加双面渲染缓解。
  - **方案 B（备选，暂不做）**：接 Tripo3D/Meshy 文生 3D API → GLB 真模型（自带 PBR 贴图）进场景。真体积、无侧拍局限；需新增 provider+API key（单次 ¥0.5-2、异步 30s-2min），可复用 MediaGenTaskService 异步任务基建；姿势为 T-pose/预置动画，无素模自由调姿势。后续需要真 3D 人物时再启动。
  - **方案 C（否决）**：自托管 Hunyuan3D/TRELLIS，需 GPU 服务器，运维重。
  - 落地走 SDD 流程出规格（P1），不直接开写。