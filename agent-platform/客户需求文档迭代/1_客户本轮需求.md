# 生成视频方式
1. 文生视频<font color=geen>（完成，待与用户真实实测）</font>
2. 图生视频<font color=geen>（完成，待与用户真实实测）</font>
3. 视频生视频<font color=geen>（完成，待与用户真实实测）</font>
4. 音频参考<font color=geen>（完成，待与用户真实实测）</font>
5. 提示词上限提升<font color=yellow>（微改）</font>
6. 视频模型：seedacne，seedance-fast，可灵，海螺，happy horse<font color=yellow>（seedance系列已有，需要额外接入可灵、海螺、happy horse）</font>
7. 展示用量的时候，只展示积分制，只在老师后台可以看<font color=yellow>（开发中）</font>
8. 剧本、资产库<font color=geen>（完成，待与用户真实实测）</font>
    - 剧本拆解成分镜脚本
    - 提取剧本的人物、道具、场景成为资产库（项目形式），授权给不同用户
    - 音频库
9. 剪辑<font color=geen>（完成，待与用户真实实测）</font>
    - 视频轨道，统一的结合，可以调整轨道，可以接入音轨，直接导出成剪印的子草稿
10. 图片模型<font color=red>（暂未接生图模型，但是简单的）</font>
    - banana，mj，sd5 pro，gpt2
11. 无限画布<font color=geen>（最为复杂的功能，初步完成，待与用户真实实测及下一轮需求迭代）</font>
    - 视频截取<font color=geen>完成</font>
    - 抽帧<font color=geen>完成</font>
# 教学相关（可延后开发，边测试生成视频功能边开发）
1.  把教学大纲来作为提取记忆的依据<font color=yellow>（中改）</font>
2.  记忆允许多模态<font color=yellow>（微改）</font>
3.  知识库和对话联动，基于知识库，便捷生成skill<font color=yellow>（需要设计一下）</font>
# 其他功能需求
1.  视频切片，比如2小时视频切片成120份<font color=yellow>（为下一轮需求，未开始）</font>