# 企业级精准知识库 RAG · 开发进度 31

- 主题：P3 QueryPlan 与真实 Ranking Engine 接入生产检索服务。
- RED：扩展 `RagRetrievalServiceTest` 后，构造器因缺少 `QueryPlanner/RankingEngine` 直接编译失败，证明原生产链路未接线。
- GREEN：`RagRetrievalService` 每次请求先生成 QueryPlan；Ranking 按知识库/管理员解析出的 `LLM/RERANK/DISABLED` 实际调用对应 Provider。
- LLM 重排：候选携带标题与正文，模型仅可返回输入白名单 ID；Prompt 明确候选为数据，模型由配置显式选择。
- 降级：仅配置 `FALLBACK_RRF` 时回退原融合顺序；其余异常 fail-closed；Trace 的 effectiveMode 记录真实模式，不再写 `HEURISTIC_PROXY`。
- 验证：`RagRetrievalServiceTest`、`LlmRankingProviderTest`、`MultiChannelRetrievalTest` 共 9/9 通过。
- 安全/日志：模型输入不含无权候选；日志/Trace 只记录候选 ID 摘要 Hash，不记录完整 Query/Chunk。
- 下一步：让 QueryPlan 真实控制是否允许 Query Rewrite/HyDE，并把 OpenSearch 多通道召回接到生产服务。
