# Agent的使用权限
Agent持有者、管理员可以给其他用户分发Agent权限，权限分以下三种
- 使用权限
- 可读权限
    - 可以看到Agent的提示词
- 可复制权限
    - 比如用户拿了这个Agent进行了编辑并保存了，则默认添加上这个用户的Agent（相当于他创建了一个Agent，而不是在原Agent上对数据库中的这个Agent做修改）


# 在智能对话中使用工作流、Agent
- 使用Agent的话，能力大语言模型自行分析调配，甚至可以跨Agent调配能力