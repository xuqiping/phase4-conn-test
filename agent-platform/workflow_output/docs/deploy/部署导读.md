# 部署导读（给服务器上的 AI / 部署人）

> 你是被叫来部署这套平台的。本目录（`E:\项目部署相关软件\`）是完整部署包。
> **照下面顺序读文档、按手册打勾执行即可。** 本文只讲「读什么、缺什么、哪些值必须人来填」。

## 一、阅读顺序

| 顺序 | 文档 | 作用 |
|---|---|---|
| ① 主手册 | `部署手册终.md` | 完整部署流程（软件清单→建库→环境变量→启动→Nginx→验证清单）。**以它为准** |
| ② 安全补充 | `部署手册-主机基线.md` | RDP/防火墙/目录 ACL 基线（主手册装完后做） |
| ③ 运维件说明 | `ops-config\文件说明.md` | ops-config 里每个文件干嘛的（装监控 §十 时查） |
| ④ 更新再部署 | `服务器更新再部署说明.md` | **改完代码重新上线走这篇**：打包→替换→重启→验证→回滚（首次部署别看它） |

## 二、本部署包与手册原文的差异（已替你改好）

- 本包**不含 runtime-sidecar 与 Python**（本次部署不装工作流引擎）——手册中所有 sidecar 相关步骤（§二软件表 #5、§四 Step 2/Step 5 第 2 条、§七 sidecar 健康检查）**跳过**。
- 平台本体目录已从 `C:\agent-platform` 改为 **`E:\agent-platform`**（jar/前端 dist/logs 放这里）——两本手册、`ops-config\` 的 WinSW XML（含 jar 路径）、巡检脚本、deploy.bat 全部已改好。Nginx 配置示例里的 `root` 也已指到 `E:/agent-platform/frontend/dist`。
- 监控系统安装根已从 `D:\IT\ops` 改为 **`E:\IT\ops`**——`ops-config\` 全部配置/脚本和两本手册均已改好，直接用。
- 手册 §三「开发机打包上传」已在开发机完成，**不要重复执行**；§二「本机安装包路径」列描述的是开发机，仅供溯源。
- 备份脚本默认目标 `D:\backup\agent-platform\` 与 `backup-env.example.bat` 里的 `PG_BIN`、`UPLOADS_DIR` 是按台服务器填的模板值——配备份时在 `backup-env.bat` 里改成实际路径（PG 若在 C 盘默认目录，PG_BIN 填 `C:\Program Files\PostgreSQL\16\bin`）。

## 三、本包没有、必须先下载的

1. **PostgreSQL 16 Windows 安装包**（EDB 官网）——主库，装完记下超管密码。
2. **Nginx Windows zip**（nginx.org）——前端托管+反代。
3. （可选）Memurai——生产级 Redis；不装则用本包 `installers\redis\` 的老版本救急。

## 四、必须由人提供的值（AI 不要自己编）

| 值 | 何时用 | 说明 |
|---|---|---|
| PG 超管密码 | 装 PG 时人设 | 后续 `DB_PASSWORD` 环境变量要用 |
| 4 个应用密钥 | 手册 §四 Step 4 | 用手册里的 PowerShell 脚本现场生成：JWT_SECRET / RUNTIME_CALLBACK_TOKEN / AUDIT_HMAC_KEY / MEDIA_REFERENCE_SIGNING_KEY。**不打印、不入文档** |
| 域名 + HTTPS 证书 | Nginx 配置 | 无域名先按手册「Quick Tunnel」临时方案；参考视频必须可信 HTTPS |
| LLM/媒体供应商 key | 部署后管理台填 | 见下条 |
| 支付/钉钉密钥 | 可选，启用才填 | 管理台「支付渠道」页 / 环境变量 |

## 五、部署后必做（手册 §八 已写，这里再强调）

1. **配模型不用 admin**——admin 没有这个权限（刻意设计）。用大模型配置员账号登录：`zgdx18158690628`（初始密码在平台仓库 `项目工程文档/数据库设计文档.md`，或问部署人）→ 设置 → 全局模型供应商。
2. **该账号初始密码已进过 git——登录后第一件事改密**（设置 → 安全设置 → 修改密码）。
3. 照手册 §七 验证清单逐项打勾（sidecar 两项跳过）。

## 六、执行姿势

- 全部操作用**管理员 PowerShell**；`setx /M` 写系统环境变量后要**重开窗口**才生效。
- 后端启动命令里的 `-D` 参数必须整体加双引号（手册 §四 Step 5 有坑说明）。
- 每完成一节，跑手册对应「验证」再进下一节——别一口气到底再排错。
