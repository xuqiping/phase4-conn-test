# 已解决
- 资产库可以由 OWNER 按用户名搜索并授权其他用户，成员列表与邀请链路已实测通过
- 资产项目可发布到公众池：管理员发布带“官方发布”标记并直接开放；普通 OWNER 可选直接开放或申请审批；公共用户按权限使用，公共媒体和复制副本预览已修复
- @选中上级内容后，这一块颜色需要变成蓝色（简单解决，但是还有点丑，并且输入了一段话之后，再用@就无法弹出引用前面节点内容了，而且@文本好像不起效果，应该达到的效果是，我在哪里@文本节点，那文本节点就应该插入到这个地方）
- 每个节点，除了要标注出这个是什么节点之外，也要写出该节点的名字，并且需要像comfyui一样，双击画布可以弹出节点选择进行添加节点（还有问题，只写了未命名，但没有具体名字）
- 节点中，视频节点好像只能把上级图片节点作为首帧，比如我@上级图片节点说，“请以@上级图片节点为尾帧，猫从地上跳上窗台”，但是并没有效果，反而仍把上级图片节点作为首帧
- （2026-08-14 第二轮修复，6 项全部处理）

## 2026-08-14 第二轮（原「未解决」6 项）
1. **视频生成模块无法把已生成视频加入资产库 ✅** —— 根因：入库按钮被 `v-if="videoObjectUrl"` 门控，视频 blob（大文件易超时）拉取失败时按钮永远不渲染；而后端入库走 `file_id` 引用根本不需要前端 blob。修复：下载/入库按钮只按 `activeTask.status === 'SUCCEEDED'` 门控，blob 拉取失败也能入库（下载按钮在有 blob 时才显示）。
2. **画布生成任务中离开、完成后重进节点被重置 ✅** —— 三层修复：① taskId 在提交瞬间即时持久化（不再走 800ms 防抖，离开不丢任务关联）；② `loadCanvas` 对快照中 `status='running'` 且带 taskId 的视频/图片节点重新续轮询（任务早已完成 → 首次轮询即回填结果+预览+审计字段；仍在跑 → 继续轮到终态）；③ 路由离开/关标签前 flush 防抖窗口内的未保存编辑。浏览器实测：提交生图 → 刷新页面 → 节点恢复「生成中」→ 自动轮询至「完成」并回填预览。
3. **画布图片生成无参数可选 ✅** —— 图片节点属性面板接入 capability 驱动参数区（与图片生成模块同源）：尺寸（预设+自定义宽x高）/输出格式/优化模式/引导尺度（模型支持时）/组图+数量/水印/联网搜索；切换模型按新模型能力重置默认值并清掉不支持的残留字段（后端对不支持字段传值会直接拒绝）；提交时 `buildImageRequest` 按能力过滤组包。浏览器实测：lite 显示 2K/组图/联网，pro 显示 1K/1.5K/2K 且隐藏组图联网（与后端能力一致）。
4. **@视频引用发给供应商的是文本 fileId 而非 video 类型 ✅** —— 根因：`resolveCanvasVideoAttachments` 只把 image 节点收为附件，@视频节点被降级成 `"fileId:xxx.mp4"` 拼进 prompt 文本。修复：video 节点 @ 收为 `kind='video'` 附件并序号化为「视频N」，后端 ArkSeedanceProvider 映射为 `reference_video` 内容项；与首尾帧互斥校验同步覆盖视频。单测 14/14（新增 5 例：视频附件/图文混用序号/去重/帧互斥/断链降级）。
5. **画布从资产库拉取视频/图片无预览 ✅** —— 根因：`applyAssetResolve` 用英文 `'VIDEO'/'IMAGE'/'AUDIO'` 比较后端返回的中文受控词汇 `'视频'/'图片'/'音频'`，分支永不命中 → 只写 fileId 不拉预览，节点上看起来“没拉到”。修复：改用 `MEDIA_TYPE.VIDEO/IMAGE/AUDIO` 常量比对。浏览器实测：从库选择图片资产 → 节点渲染预览图 + 「已引用资产 xxx v1」徽标 + fileId 回写 + 自动保存。
6. **拉线建节点 + 节点显示具体名字 ✅** —— ① 从节点输出句柄拉线到空白处松手 → 弹出「添加下一节点（自动连线）」搜索框，选定类型后建节点并自动连线（connect-start/connect-end 事件，落点在节点上不开弹窗）；② 生成任务完成后节点名自动用提示词前 12 字回填（如「一只橘猫趴在窗台上晒太阳」），仅当 label 还是默认类型名（图片/视频 N）时回填，用户手动改过的名字不覆盖。浏览器实测名字回填生效。

# 待决策
- 别人的项目内容是否允许被复制

# 参考feature-map、user-ops
- agent-platform\workflow_output\docs\feature-map\项目资产库.feature-map.md
- agent-platform\workflow_output\docs\user-ops\项目资产库用户操作手册.md

>解决完后已同步更新对应的feature-map和user-ops（2026-08-14）
