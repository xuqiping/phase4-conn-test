# 人工测试问题 1—4 修复 · 开发进度 6

> 日期：2026-09-03｜对应计划：Chunk 5｜状态：✅ 完成｜提交：`be9bf69`

## 本轮实现

- 设置模型从旧 `minimizeToTray` 迁移为 `floating_ball/tray/exit` 三态，迁移后不再写回旧字段。
- 设置页增加互斥三选一说明和中英文本，默认使用悬浮球。
- 新增独立 `floating-ball.html` 入口和 64×64 本地透明 Webview，按窗口标签保证单实例复用。
- 悬浮球支持单击恢复、原生拖动区域、拖动位置持久化，以及系统原生右键菜单的打开、切托盘、退出操作。
- Rust 使用显示器工作区校正位置，兼容负坐标、越界坐标、无显示器信息和极端持久化坐标。
- 主窗口关闭统一走三态编排；悬浮球创建失败时提示并降级到已存在的系统托盘。
- 托盘或全局快捷键恢复主窗口时同步隐藏悬浮球。

## TDD 与验证

- RED：三态设置保存、关闭行为编排、悬浮球组件和 Rust 位置函数均先以缺失行为失败。
- GREEN：设置/关闭/悬浮球定向前端测试 15/15；连同访问模式和截图回归共 28/28。
- Rust 位置校正测试 4/4，`cargo check --bin file-keeper` 通过。
- `npm run build` 通过并生成主入口与悬浮球入口；`git diff --check` 通过。

## 安全、隐私与运维

- 悬浮球只加载打包内的 `floating-ball.html`，不加载远程 URL。
- 窗口命令的错误仅包含结果码和框架错误，不记录本地路径、标题或剪贴板正文。
- 使用“先显示悬浮球、后隐藏主窗口”的顺序；失败时主窗口仍可见，确认提示后才降级托盘。
- 未新增依赖、远程开关或服务端请求；位置数据只保存在本机设置中。
- 双屏、缩放、显示器拔插、空闲 CPU 和恢复延迟留到 Phase 4 实机验证。

## 下一步

Chunk 6：扩展现有 `ClipboardService → ClipboardStorage`，实现分组 CRUD、批量移动、置顶、稳定分页排序、事务和变更事件。
