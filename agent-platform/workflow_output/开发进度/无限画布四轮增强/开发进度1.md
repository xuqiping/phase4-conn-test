# 无限画布四轮增强 · 开发进度1

> 计划：`workflow_output/docs/plans/无限画布四轮增强.plan.md`（9 步）
> 规格：`workflow_output/docs/specs/无限画布四轮增强设计.md`
> 来源问题单：`workflow_output/人工测试问题/2x. 资产库和无限画布.md`（四轮未解决项）

## Step 1：background 请求豁免 + 轮询退避（✅ commit 4614a366）

**实现功能**：后台型请求（轮询/blob 预取）断网不计断路、不弹「服务不可达」、不踢会话；轮询失败退避；标签页切回立即补轮。

**涉及文件与关键位置**：

| 文件 | 改动 |
|---|---|
| `frontend/src/api/request.ts:14-24` | axios 模块扩充：`AxiosRequestConfig._background?: boolean`（`InternalAxiosRequestConfig extends` 继承，同 `_retry` 约定）；`:151-159` 网络错误分支头部——`_background` 请求 console.warn 留痕 + 30s 节流 toast「后台任务网络波动，恢复后自动重试」，直接 reject（不计数/不断路/不清会话） |
| `frontend/src/utils/mediaTaskPolling.ts` | `backoffDelay(intervalMs, failStreak)`：失败 1/2/≥3 次 → 基数/基数×2/max(30s,基数)，成功归零；`waitInterruptible` Promise 模式——visibilitychange 回 visible 唤醒当前等待提前补一轮；listener try/finally 注销 |
| `frontend/src/api/media.ts:299` | `getTask` 标 `_background + timeout 30000`（慢网一次超时不误判） |
| `frontend/src/api/media.ts:389,405` | `fetchVideoBlob`/`fetchMediaBlob` 标 `_background`（blob 预取） |
| `frontend/src/api/mediaEdit.ts:139` | `mediaEditApi.getTask` 同口径（视频剪辑页轮询同享） |
| `frontend/src/api/file.ts:35` | `fetchFilePreview` 标 `_background`（文件预览预取） |

**后台请求标记清单**（plan 坑点表「漏标」项销账）：getTask（media+mediaEdit）✅ / fetchMediaBlob ✅ / fetchVideoBlob ✅ / fetchFilePreview ✅ / **帧提取 extractFrame 不标**（用户主动 POST，保留完整断路语义——豁免只给后台型，L6-4 用例口径）。

**测试**：`mediaTaskPolling.test.ts` +2 用例（退避序列 `[5000,10000,30000,30000,30000,5000]`；visibility 唤醒）；`file.test.ts` blob 断言更新（含 `_background`）。全前端 83 文件 551 用例绿 + `vue-tsc --noEmit` 零错。

**偏差记录**（已回写 plan Step1）：
1. 字段名 `_background` 非 `background`——axios 自定义 config 下划线约定（同 `_retry`）
2. `CanvasView.vue` 未改——续轮询走 `mediaApi.getTask` 已集中标记，调用方零改动
3. request.ts 豁免分支无独立单测——拦截器依赖 discrete API，mock 成本高；由测试方案 L6 手测 + 代码走查覆盖

**测试方案**（A 功能级预处理已产）：`workflow_output/docs/测试方案/无限画布四轮增强测试方案.md`——L1 依赖调度 7 / L2 关联高亮 6 / L3 拖宽高 5 / L4 标注 5 / L5 建组 7 / L6 断网豁免 5 / L7 参考预览 5 + 回归红线 4。

## Step 2：节点宽高拖拽（✅ commit 1bb9591a / plan 勾验 87819451）

**spike 结论**：`@vue-flow/node-resizer@1.5.1` peer=`@vue-flow/core ^1.23`（core 1.41.5 满足）/`vue ^3.3`（3.4 满足），**采用库**。源码验证三点：组件内自动注入 NodeIdInjection（免传 id）；拖柄带 noDragClassName（不触发节点拖动）；min/max 有挂载校正 watch。用 `NodeResizeControl` 直组四角柄而非 `NodeResizer` 包装——包装额外渲染四边 line 控件，会压住上/下连线 Handle 的热区。npm audit 端点镜像源未实现（NOT_IMPLEMENTED），包为 @vue-flow 官方+依赖仅 d3-drag/d3-selection，记录豁免。

**涉及文件与关键位置**：

| 文件 | 改动 |
|---|---|
| `frontend/package.json` | +`@vue-flow/node-resizer ^1.5.1` |
| `types/canvas.ts` | `CanvasNodeData` +`width?/height?`（持久化真源注释）；`CanvasNode` +`style?`（会话态注释） |
| `nodes/CanvasNodeBase.vue` | selected 时 4×`NodeResizeControl`（160×64 下限）；`onResizeEnd` 取整写 `node.data.width/height`（`useNode` 注入，裸挂守卫）；根节点改 flex 列 + `width/height:100%` 跟随 wrapper、min 160×64；`--resized` 类（data.height 存在时）；头部 flex-wrap；body flex:1+overflow-y:auto |
| `nodes/TextNode.vue` | `--resized` 下解除 line-clamp（拉高看全文，超高滚动） |
| `CanvasBoard.vue` | `nodeSizeStyle(data)`：data→wrapper style（默认宽 200，height 仅显式拉过才定死）；`loadSnapshot`/`addNode` 应用；`getSnapshot` 剥 style；`@nodes-change`→`resizing:false` 时 emit structure-changed（防抖落库；拖动中每帧不 emit） |
| 图片/视频节点 | img/video 已 `width:100%` 天然随宽自适应，无需改 |

**测试**：`CanvasBoard.test.ts` +3（loadSnapshot 推导默认/显式、getSnapshot 剥 style 保 data、addNode 默认 200）；`CanvasNodeBase.test.ts` 新建 2 用例（resize-end 取整落 data + --resized 跟随 / 未拉高不加类；mock useNode+reactive data 对齐 GraphNode 语义）。canvas 目录 80 用例绿，全前端 556/556 绿 + vue-tsc 零错。

**下一步**：Step 3 连线左右（Position Left/Right 横向贝塞尔）。

## Step 3：连线左右（✅ commit 43da6532）

`CanvasNodeBase.vue` 两处 Handle 改向：target=`Position.Left`、source=`Position.Right`。`DeletableEdge` 的 `getBezierPath` 消费 vue-flow 自动传入的 source/targetPosition——横向弧线、中点删除按钮坐标（path[1]/[2]）、选中高亮（class 注入）全部自动跟随，零额外改动。存量画布边无 handle 字段→默认取各类型首个 Handle，同享新方向。canvas 78 用例绿 + typecheck 零错（手测归测试方案存量画布回归红线）。

## Step 4：批量生成依赖调度（✅ commit 22178e3a）

**实现功能**：批量生成从「裸滑窗按拓扑序提交」升级为依赖调度——下游等选集内全部直接上游 SUCCEEDED 才提交；上游 FAILED 级联标灰跳过；上游不在选集（含资格剔除）显式 SKIPPED_UPSTREAM_MISSING；429 冷却重排队尾不占窗口槽；60min/节点看门狗；浮条「停止调度」取消令牌。

**涉及文件与关键位置**：

| 文件 | 改动 |
|---|---|
| `utils/batchRunner.ts:124-315` | `runDependencyScheduled(selectedIds, edges, phases, opts)`：`DepSchedPhases{submit(占槽),awaitTerminal(不占槽)}` 两段式；`rawPreds`+`missing` 集合（跨选集入边→MISSING）；环守卫复用 `inducedTopoOrder` 抛 `dependency-cycle`；`settle→releaseIfResolved` 统一级联（菱形双上游全成才释放；上游 CANCELLED 留收尾不级联失败）；429→`cooling++` 冷却 `backoffDelayMs` 重排队尾（`cooling` 计数入主循环退出条件——曾竞态：冷却期 inFlight=0 且 ready 空 ≠ 收工，丢重排节点）；重排上限 3→FAILED；`Promise.race(awaitTerminal, watchdog)` 超时按 FAILED 释放（在途不撤）；取消收尾未终态标 CANCELLED；返回 `Map` 快照隔离迟到写入 |
| `utils/batchRunner.test.ts:166-381` | +9 用例：链 A→B→C 顺序释放 / 独立分支并行+失败级联旁支继续 / 缺选 MISSING / 菱形双释放 / 环抛 / 429 重排不占槽（窗口 1 下 b 先于 a 重试）/ 重排上限 / 看门狗 / 取消令牌（CANCELLED≠失败级联，快照不被迟到终态改写）。`wait` 注入必须让出宏任务（微任务级 wait 饿死 setTimeout 主循环整挂——坑点注释入测试文件） |
| `types/canvas.ts` | `CanvasNodeStatus` +`'skipped'`（灰化区分失败；原因在 errorMsg） |
| `nodes/CanvasNodeBase.vue` | STATUS_LABEL +`skipped:'已跳过'`；`.canvas-node--skipped` 灰边框+75% 透明度；状态徽标 `[data-status='skipped']` 灰字 |
| `views/CanvasView.vue:395-523` | `onBatchRun` 切调度器：submit=submitVideoOnly/submitImageOnly/onRunNode(text) 不再自带 `submitWithBackoff`（429 交调度器重排），失败先标红留原始报错再上抛；awaitTerminal=pollVideoTask/pollImageTask（null=被替换按 FAILED 释放）/text 读 status；`onNodeState` 节点标灰与浮条计数解耦（收浮条后跳过态仍落节点）；浮条 +「跳过 N/等待上游 N」+按钮二元「停止调度/收起」；`batchCancelRequested` 令牌（在途不撤仅停释放）；跑完汇总 warning 列跳过数 |

**语义变更**（已回写 plan 偏差区）：旧「未选上游按已物化内容插值不补跑」→ spec §5 改为下游 SKIPPED_UPSTREAM_MISSING 显式跳过标灰（资格剔除的上游消失于选集同理）；单跑路径（onRunNode）插值逻辑不受影响。

**测试**：565/565 全绿 + vue-tsc 零错。CanvasView 无独立测试文件（既有口径），由调度器 9 用例 + typecheck + 测试方案 L1 手测覆盖。

## Step 5：关联高亮 + 只看关联（✅ commit 4de1892b）

**实现功能**：选中节点（单选/多选整体）→ BFS 祖先∪后代传递闭包——关联边加粗辉光 `--related`、无关键/节点透明 0.25；工具条 🔗「只看关联」开关把无关节点 visibility:hidden（布局不动可逆）；清除选中还原。

**涉及文件与关键位置**：

| 文件 | 改动 |
|---|---|
| `utils/graphClosure.ts`（新） | `relatedClosure(seeds, edges)`：上/下游邻接 Map 一次构建 + BFS（nodeIds 天然去重防环死循环）；边集=两端点均入闭包的诱导边（含菱形弦边 a→d）；空 seed→null（无高亮态）。纯函数与 Board 解耦 |
| `utils/graphClosure.test.ts`（新） | 6 用例：菱形选汇点 d/中段 b（传递闭包——b 后代 d 的另一上游 c 也命中）/源点 a/多选 {b,x} 并集/环 p↔q 不死循环/空选 null |
| `CanvasBoard.vue` | `relatedInfo` computed（多选优先于单选取 seeds；只依赖选集+边集——拖节点改 position 不重算）；`applyVisualClasses` 统一注入（取代旧单选边 watch）：边 class 优先级 选中(红可删)>related(2.5px+辉光)>dimmed(0.25)，节点无关节点 `canvas-node--dimmed`；🔗 工具条按钮（aria-pressed + 无选中 disabled）叠 `canvas-node--hidden`；getSnapshot 节点增剥 class（同 style 会话态口径） |
| `CanvasBoard.test.ts` | 模式切换用例适配 aria-pressed 按钮 2→3（+只看关联）+ 断言其无选中禁用 |

**偏差**（已回写 plan）：开关落 Board 工具条按钮（非 View 头部 n-switch）——视觉态就近 + 既有按钮范式一致；CanvasView 零改动。

**测试**：571/571 全绿 + vue-tsc 零错。视觉落点（辉光/淡化/隐藏往返）归测试方案 L2 手测 6 用例。

**下一步**：Step 6 图片翻转/旋转（后端 transform-image 六 op + 前端 PropertyPanel 5 按钮）。


## Step 6：图片翻转/旋转（✅ commit fa74da69）

**实现功能**：图片节点五 op 确定性变换（水平/垂直翻转 + 旋转 90°/180°/270°）→ 产新衍生图节点 + 自动连边，源图不可变；JPEG EXIF Orientation（手机拍照常见）先归正再变换，杜绝「翻转后方向错」。

**涉及文件与关键位置**：

| 文件 | 改动 |
|---|---|
| `backend/.../canvas/service/ExifOrientation.java`（新） | 零依赖 EXIF 解析：JPEG SOI→APP1 "Exif\0\0"→TIFF 头（II/MM 双字节序）→IFD0 tag 0x0112（SHORT 内联），失败一律回 1（不炸）；`applyOrientation` 2-8 像素循环归正（5-8 维度互换），TYPE_INT_ARGB。无新 Maven 依赖 |
| `backend/.../canvas/service/VideoFrameService.java` | `TransformOp` 枚举（FLIP_H/FLIP_V/ROTATE_90/ROTATE_180/ROTATE_270 + `parse` 白名单校验→BAD_REQUEST）；`transformImage(Path,op)`：`decodeImageGuarded`（抽出与 cropImage 共用：ImageGuard 像素护栏 + null/格式/尺寸三态错误）→ EXIF>1 先归正 → 显式像素循环变换（四色可断言，不用 AffineTransform）→ PNG；`decodeImageGuarded` 重构 cropImage 同步复用（crop 错误文案「无法裁剪」→「无法处理」统一） |
| `backend/.../canvas/dto/ImageTransformRequest.java`（新）/`ImageTransformVO.java`（新） | `{op}` 请求（@NotBlank）；VO 含 op 回显供前端命名 |
| `backend/.../canvas/controller/CanvasController.java:256` | `POST /{id}/nodes/{nodeId}/transform-image`：同 crop 链路安全脊柱——loadOwned（画布归属）→ `resolveImageFileId`（快照节点解析源 fileId，**不信任客户端传入**）→ loadPath（文件归属复检）→ storeStream 产**新 fileId** 落 SOURCE_CANVAS（源图不可变）；日志 `canvas image transformed: op/exif/srcW/outW/bytes/costMs` |
| `backend/.../canvas/service/VideoFrameServiceTest.java:376-568` | +13 用例：五 op 2x2 四色图像素逐一断言 / 非方图 3x2 顺 90°→2x3 尺寸互换 / null 路径+非法 op BAD_REQUEST / EXIF 合成 JPEG（SOI+APP1 TIFF 注入 orientation，body 去内层 SOI 防双 SOI）解析 tag / PNG 无 EXIF 回 1 / applyOrientation 维度互换 / **归正先后序**（EXIF=6 源 8x16 左红右蓝 → FLIP_V 产物 16x8 上蓝下红；4:2:0 色度抽样下用 8x16 内部采样 + 色距排序断言，2x1 退化图色距无区分度——踩坑记录） |
| `frontend/src/api/canvas.ts:83-96,228-233` | `ImageTransformOp` 联合类型 + `ImageTransformVO` + `transformImage()` POST |
| `frontend/src/components/canvas/PropertyPanel.vue:234-247,541-548` | 五变换按钮排（有源图才显示，running 禁用防重入；↔/↕ 翻转 + ⟳90°/180°/270°，title 提示产新节点）；`IMAGE_TRANSFORMS` 常量 + `transform-image` emit + `__transform` flex 换行 CSS |
| `frontend/src/views/CanvasView.vue:712-762` | `onTransformImage`（onFocusConfirm 同范式）：防重入 → 变换 → fetchCanvasPreview → addNode 衍生图（label=`源名·op中文`，parentFileId/sourceNodeId/transformOp 溯源）→ addEdge 源→新 → success 提示 + scheduleSave；失败标红源节点不产空节点 |

**安全**：同 plan 安全清单 crop 链路四件套全沿用（画布归属/文件归属复检/快照解 fileId/产新文件不动源）；op 白名单枚举拒任意字符串；像素护栏防解压炸弹。

**测试**：VideoFrameServiceTest 36/36（BUILD SUCCESS）+ canvas 包全量 56/56 + 前端 vue-tsc 零错 + vitest 85 文件 571/571。手测项（翻转产新节点原图不变）归测试方案补验。

**偏差**（已回写 plan Step 6）：plan 写「六 op」实为五种（2 翻转+3 旋转）；端点体 `{op}` 无 fileId（比 plan 更安全——服务端快照解析）。

**下一步**：Step 7 彩色框选标注（AnnotateOverlay 8色/框≤8/指令≤200字 + AI 修改出口=参考图[原图,标注图]+逐框指令清单；depends 本步 transform 链路）。
