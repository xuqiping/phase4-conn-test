# 企业级精准知识库 RAG · 开发进度 35

- 主题：P4 Grounded Answer 分批事实核心编排器。
- RED：新增 `GroundedAnswerServiceTest`，要求 3 条证据按 2+1 分批、越界 Citation 99 被剔除、合法引用不丢失、同主题不同值识别冲突；旧类缺少对应 API 而编译失败。
- GREEN：`GroundedAnswerService` 新增 `synthesize`，负责有界分批、Citation 白名单、事实去重合并和冲突检测，并注册为 Spring Service。
- 安全：编排器只接受调用方提供的已校验证据；模型返回不能新增证据编号。
- 验证：`mvn '-Dtest=GroundedAnswerServiceTest' test`，1/1 通过。
- 下一步：把 LLM 事实提炼和最终答案合并接入 `/retrieve`，再同步 `/ask` SSE 与完整六态状态。
