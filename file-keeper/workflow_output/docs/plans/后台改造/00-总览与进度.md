# 后台改造计划 — 总览与进度

> 本目录记录 File Keeper 商业授权系统**管理后台(admin-web)与服务端(server)**的改造计划与执行进度。
> 新会话请先读本文件,再按需查阅 `01`~`04` 子文档。
> 最后更新:2026-06-15

## 一、背景

阶段 4 商业授权功能已开发完成并通过自动化测试。进入**阶段 5E 真实环境端到端联调**时,本地搭建了 PostgreSQL + Memurai(Redis 兼容)环境,启动了 backend / admin-web / Tauri 桌面端,完整跑通了商业授权链路,并在联调中修复了若干真实 bug(见 `04-联调阶段已修复的Bug.md`)。

联调发现:管理后台目前**只有"用户管理"一个页面**,登录后显得空旷。用户(项目所有者)决定补充管理后台缺失的页面,共四项,本目录即为这四项的计划与进度。

## 二、四项改造计划

| 编号 | 计划 | 状态 | 文档 |
|---|---|---|---|
| 0 | 测试用户演练(造数据 + 跑通审批授权) | ✅ 已完成 | 见本文件"四" |
| 1 | Dashboard 统计页 | ✅ 完全完成(后端验证 + 前端 build 通过) | `01-Dashboard统计页.md` |
| 2 | 系统设置页(全局默认值) | ✅ 完全完成(全量测试 + 端到端验证通过) | `02-系统设置页.md` |
| 3 | 匿名设备运营页 | ⏳ 待做 | `03-匿名设备运营页.md` |

> **当前进度(2026-06-15)**:第 0、1、2 项完成。第 3 项(匿名设备运营页)待做——工作量最大,需新建后端管理端 repository/service/controller + 前端运营页。

> 注:阶段 6 设计文档(`项目相关文档/集体实时计划/08-7.-阶段-6-商业版安全与运营增强.md`)原本把 Dashboard/系统设置/匿名设备页标记为"可选",因为后端 API 不支持。本批改造的实质就是**补齐后端 API + 前端页面**。

## 三、本地联调环境(本机当前状态)

供新会话快速恢复联调环境。详细启动步骤见 `项目相关文档/集体实时计划/07-6.-阶段-5E-真实环境端到端联调规划.md`。

### 已安装的本地服务(便携版,不污染全局)
- **PostgreSQL 17.4**:便携版在 `C:\Users\19536\.local\tools\pgsql\`,数据目录 `C:\Users\19536\.local\tools\pgdata`,端口 5432,用户/密码 `postgres`/`postgres`,库 `file_keeper`。
  - 启动:`"C:/Users/19536/.local/tools/pgsql/bin/pg_ctl.exe" -D "C:/Users/19536/.local/tools/pgdata" -l ".../pgdata/postgresql.log" start`
  - 手动改库用 `psql.exe`(注意中文 note 会触发 GBK/UTF8 编码问题,见 Bug 记录)。
- **Memurai Developer 4.1.2**(Redis 兼容):winget 安装,Windows 服务名 `Memurai`,自动启动,端口 6379。
- **Java 17**:`C:/Program Files/Eclipse Adoptium/jdk-17.0.19.10-hotspot`
- **Maven 3.9.11**:`C:/Users/19536/.local/tools/apache-maven-3.9.11`

### 启动三端
1. **后端**(端口 8088):
   ```bash
   export JAVA_HOME="/c/Program Files/Eclipse Adoptium/jdk-17.0.19.10-hotspot"
   export MAVEN_HOME="/c/Users/19536/.local/tools/apache-maven-3.9.11"
   export PATH="$JAVA_HOME/bin:$MAVEN_HOME/bin:$PATH"
   # 关键环境变量见 07-6 文档;至少需要 DB/Redis/JWT_SECRET/SUPER_ADMIN_*/VERIFICATION_DEV_FIXED_CODE
   mvn -f "file-keeper/server/pom.xml" spring-boot:run
   ```
2. **管理后台**(端口 5173,注意必须指定 API 地址为 8088):
   ```bash
   cd file-keeper/admin-web
   VITE_API_BASE_URL="http://localhost:8088" npm run dev -- --host 127.0.0.1
   ```
   > ⚠️ 若不加 `VITE_API_BASE_URL`,前端默认请求 8080,会报 Network error。
3. **桌面端**(Tauri,端口 1420):从 Git Bash 用 PowerShell 调 bat(直接 cmd.exe 会被转义吞掉):
   ```bash
   powershell.exe -NoProfile -ExecutionPolicy Bypass -Command "Set-Location 'C:\AI Projects\file-keeper'; & '.\dev-msvc.bat'"
   ```

### 账号
- **管理后台 / 超管**:`admin@filekeeper.local` / `Password123!`(环境变量初始化)
- **测试用户**:`testuser@e2e.local` / `Test12345`(联调时创建,已审批 active,已授权 files+clipboard)
- **开发验证码**:`123456`

## 四、已完成事项(0 号:测试用户演练)

通过后端 API 完整跑通了真实授权流程,管理后台用户列表从"只有超管"变成"2 个用户":

1. 发验证码 → 校验 `123456` → 注册 `testuser@e2e.local`(status=pending_review)
2. 管理员审批 → status=active
3. 设置 deviceLimit=2、offlineCacheMinutes=60
4. 授予 files + clipboard(到 2026-09-01)
5. testuser 登录 + 设备注册 + 授权查询:files/clipboard allowed=true ✓

## 五、数据库已做的调整(联调期,非迁移文件)

> ⚠️ 这些是联调期手动 ALTER 的,**不在 Flyway 迁移文件里**。重建数据库后会丢失,需要补迁移或重建时重做。
- `user_devices.device_name`:`VARCHAR(120)` → `VARCHAR(255)`(修 deviceName 超长 bug,见 `04`)
- `users` 表超管 `device_limit`:1 → 10(联调方便)

## 六、代码已做的临时改动(联调期,需评估是否保留)

- `server/.../security/RequestLoggingFilter.java` — **新增**,记录每个请求的 method/uri/origin/auth/status。联调用,可选保留。
- `server/.../common/GlobalExceptionHandler.java` — 兜底 `@ExceptionHandler(Exception.class)` 改为 `log.error` 打印完整堆栈。**建议保留**(原本吞异常是隐患)。
- `file-keeper/src/api/commercialAuth.ts` — `defaultDeviceName()` 截断 UA 到 100 字符。**必须保留**(修 bug)。

## 七、后续优先级

1. 先把 Dashboard(01)的前端 build 跑完确认无类型错误。
2. 做系统设置页(02):后端要加全局配置表 + AdminSettingsController。
3. 做匿名设备运营页(03):后端要加匿名设备管理 repository/service/controller。
4. 把上述数据库/代码改动中需要保留的部分,补成 Flyway 迁移或正式代码。
