# 计划12 · 个人记忆重设计 · 开发进度10

> 迭代 F/I4 前端第二批（F-6 + I4-3 + F-7），承 [开发进度9.md](开发进度9.md)。
> 子 plan：[计划12-F-前端.md](../../../项目工程文档/计划/计划12-F-前端.md) + [计划12-I4-前端ACL.md](../../../项目工程文档/计划/计划12-I4-前端ACL.md)。
>
> **F-4b（生命周期折叠板）本轮跳过**：后端缺 `departed-projects` / `deleted-projects` list + copy-to / restore 端点（grep Memory*Controller 全族无对应路由；设计 §3.6/§3.8 规的 `MemoryService.copyDepartedProjectTo` 未实现）。属 C/E 收尾遗漏，单开后端 chunk 补齐后再上前端。

## 本轮做了什么（F-6 + I4-3 + F-7，vue-tsc + build + 99 单测全绿）

### F-6 · ChatView 底栏新栈召回 scope 编辑器
- **新建 `MemoryRecallScopePopover.vue`**：挂 ChatView 底栏读范围组（与 legacy `memIncludeGlobal/memReadProjectIds` 并存，H 收尾 DROP 旧表后清 legacy）。
- 走 `memoryApi.getRecallScope / putRecallScope`（新栈 `/recall/scope`）。
- 字段：个人记忆开关 + 项目多选（候选来自 `availableProjects`）+ 方向（全部/仅输入/仅输出）+ 时间窗（全部/近7/30/90 天 ↔ `relativeDays`）+ 含已离开人员开关（L10）。
- 改动即时 PUT（debounce 400ms），跨会话沿用；失败静默不阻塞聊天。
- **双栈期说明**：当前 chat 发送仍走 legacy `memoryScopePayload`（驱动旧 user_memories 召回）；本组件管新栈 scope 偏好，供新栈 recall preview + 未来 chat 切新栈。两不冲突（物理隔离：不同表不同端点）。

### I4-3 · MemoryConsolidationPeoplePicker（总结取数选人）
- **新建 `MemoryConsolidationPeoplePicker.vue`**：项目 scope 勾选后于 MemoryConsolidationDialog 行内展开。
- 候选 = `memoryApi.getRoster(projectId)`（后端最终 ∩ readableAuthors 校验，向量 14 兜底）。
- 人员范围 SELF（仅自己，默认）/ SPECIFIC（多选成员）/ ALL（全部可召回）；方向 INPUT/OUTPUT/BOTH；含已离开人员开关（关 → 离职成员选项禁用且后端剔，L10 优先级高于勾选）。
- **扩 `memory.ts` `MemoryConsolidationScopeRequest`** 类型：加 `authorFilter/authorIds/direction/includeDeparted`（后端 DTO 早支持，前端类型补齐）。
- **改 `MemoryConsolidationDialog.vue`**：PROJECT 行勾选后挂 PeoplePicker；`selectedScopes` 提交带取数配置；loadTargets 重置 `peopleCfgMap`。

### F-7 · 移动端 390px 机械扫荡
- 3 弹窗改响应式宽：`max-width: Npx` → `{ maxWidth: 'Npx', width: '90vw' }`（MemoryConflictJudgeDialog / MemoryConsolidationDialog / MemoryTagLibrary 编辑 modal）。承 MemoryManagerPanel 既有 `:style` 范式。
- MemoryProjectAclPanel 项目选择器 `width: 240px` → `min(240px, 60vw)`（避 390px 溢出）；授权矩阵已 `__matrix-scroll` 横滚 + min-width 480，390 自然滚。
- ChatInput tools 行 `flex-wrap: wrap`（既有）已覆盖新 popover 按钮；MemoryRecallScopePopover 宽 300、PeoplePicker 行内 select `flex:1;min-width:0`，窄屏自适应。

## 验证
- ✅ `vue-tsc --noEmit` 0 error（修 2 处：n-spin/n-empty `size="tiny"` 非法改 `small`；删未用 `lastSnapshot/snapshot`）。
- ✅ `npm run build` 全绿（chunk size 警告为既有，非本轮引入）。
- ✅ `vitest run` 99/99 既有测试全绿（无回归）。

## 偏离裁决
- **F-6 双栈并存而非切净**：原 plan 意图「底栏切新栈 scope 端点」，但 chat 发送链路当前仍依赖 legacy `memoryScopePayload` 驱动旧 user_memories 召回；贸然删 legacy 读控件会破当前 chat 召回。选「新栈 popover 与 legacy 读控件并存，H 收尾 DROP 旧表时统一清 legacy」。隔离安全（不同表不同端点）。
- **F-7 不引入新 useBreakpoints**：弹窗/popover 用 CSS 响应式宽即可，无需 JS 断点；矩阵已有横滚。

## 下一步（留后续轮次）
- **后端 chunk（阻塞 F-4b）**：补 `GET /api/chat/memory/departed-projects` + `POST /departed-projects/{id}/copy-to` + `GET /deleted-projects` + `POST /deleted-projects/{id}/restore`，配套 service + mapper + IT。承设计 §3.6/§3.8 + 联动 L5/L6。
- **F-4b MemoryLifecyclePanel**：后端端点就绪后，建已删除/已离开项目折叠板（restore / copy-to）+ 波及 badge 联动。
- **H 收尾**：V50 DROP 旧表 + 旧端点 404 + 删 legacy 读控件/legacy pane + 速查表 09 重写。
- **Phase4 E2E**：真 LLM 日期铁律 + 召回全链 LLM IT + 性能（召回<2s）+ 15 安全向量越权 IT + 联动 L1-L12 边界（反向/半选/批量）。

## 沉淀（入通用约束）
1. **双栈期并存范式（前端）**：新栈控件与 legacy 控件物理隔离（不同表不同端点）可安全并存；迁移型 chunk 不强求「切净」，留收尾迭代统一清 legacy，避免破当前链路。承进度9「渐进迁移」范式，扩到底栏 scope 控件。
2. **移动弹窗响应式宽范式**：`n-modal preset="card"` 一律 `:style="{ maxWidth: 'Npx', width: '90vw' }"`，桌面不过宽、390px 自适；固定 `width:` 的 select 改 `min(Npx, Vw)`。承 MemoryManagerPanel 既有范式。
