# 开发进度5 · Phase 4 冒烟（2026-08-18）

> `/sdd-phase4-run`：Playwright 冒烟两入口，run→fix→rerun 闭环。环境按 `项目工程文档/0_快速启动.md`（PG/Redis 起 + 单 PowerShell 会话 source 两 env 后 `mvn spring-boot:run`；前端 5173 Vite 代理）。

## 环境

- 后端重启 2 次：①旧 JVM（08-17 12:20）无 Plan-6 类 → 404 探活暴露；②admin 旁路修复后重启（`aee7605`）。
- 浏览器登录 admin12345（userId 174，admin 角色）；测试视频 7.6s/5.1s 各一（hardcut 场景切镜 + 历史 #45）。
- 中途全量回归：后端 2198 绿 / 前端 601 绿（88 文件）+ vue-tsc 清。

## 冒烟结果（详表见 [测试方案 P4 执行记录](../../docs/测试方案/视频反推与转绘测试方案.md)）

1. **冒烟① 上传源仅关键帧**：12 帧·UNIFORM 兜底·抽帧 4039ms·零 LLM·帧图全 200 ✅
2. **冒烟② 历史任务源**：撞 403（见修复①）→ 修复后 #45 反推 4 帧+剧本（glm-5.3，9658ms 端到端）+ 转绘「美国」changeLog 2 条 + 用剧本生成按钮/本土化 radio 切换 ✅
3. **冒烟③ 画布入口**：反推区渲染+门控、转绘弹窗+门控、转绘全链建节点「本土化(美国)」+连边（见修复②）✅；画布反推建节点连边留人工（无带产物视频节点，后端链已由①②覆盖）

## 性能实测（对照 plan 口径）

| 指标 | 口径 | 实测 |
|---|---|---|
| 端到端反推 | 1min 视频 <90s | 5.1s 视频 9658ms（抽帧 2127 + LLM ~7.5s，4 帧）；1min 实测留人工，按帧数外推 ≪90s |
| 本土化改写 | <30s | 3777ms（199 字·2 条）/ 3634ms（150 字·4 条） |

## 修复（2 commit）

- `aee7605` fix(media): 反推源校验补 admin 旁路——analyze 3 参重载（controller `isAdmin()` 与 MediaGenController 同口径），resolveSourceFileId/runExtraction 归属咽喉均收 admin；+1 单测（旁路断言 loadForDownload/loadPath admin=true）。
- `94ac27ab` feat(media): 画布转绘产物 changeLog 面板可核——PropertyPanel script 节点渲染「替换清单（N 处）」逐条 from→to（位置）+ localizeWarning；+2 单测；浏览器实测 4 条清单可见。

## 交叉审查（第二 AI，P4 出口项）

1 finding 🟡 + 1 🔵，均已修（`49f37498`）：isAdmin 精确 equals → 与姊妹控制器逐字对齐 equalsIgnoreCase（混大小写角色防漏判）；changeLog 行 from/to 全缺过滤防空行。审查同时确认：extractKeyFrames 4 参入口无生产调用方、admin 旁路不越既有 admin 文件访问模型（loadPath 与 FileController 同口径）、localizeChanges 响应式/空值安全无问题。

## 记档（不修）

- **NoResourceFoundException → 500**：打错路径返回 500「未预期异常」+ ERROR 日志（应为 404 WARN）——GlobalExceptionHandler 缺该映射，Boot 3.2 静态资源解析抛此异常。已记 [问题单 11_日志or运维告警](../../人工测试问题/11_日志or运维告警.md) 未解决区（低优先）。
- **画布反推建节点连边（B1/B2）**：冒烟未覆盖（需带产物视频节点），逻辑单测覆盖 + 后端链已验，留人工测试方案 B 组。

## 遗留 → 人工

测试方案 A4/A5/A7/A8、B1/B2/B3/B5、D1-D9 未自动化项按方案逐条补；性能 1min 视频口径（D9）。
