# 开发进度2 · Tavily 开关制路由（修复IX+，2026-08-31）

> 用户拍板（/sdd-phase3-implement 直接开工，未走独立 plan）：设置页配 Tavily key + Tavily 启用开关；**开+配好 → 联网走 Tavily；未配/key 错/开关关 → 走 SearXNG Docker**。智能对话与无限画布 chat 同规则（两处 `req.webSearch` 同走 ChatSessionService→WebSearchService，一处改两处生效）。

## Chunk A · 后端路由派生（commit `273cf10b`）

| 文件 | 改动 |
|---|---|
| `system/service/SystemSettingService.java` | 新 setting `search.tavily.enabled`（默认 false）+ getTavilyEnabled/updateTavilyEnabled；**getActiveSearchProvider 改派生**（开关开→"tavily"，否则→"builtin"），旧 `search.active-provider` 行/白名单/updateActiveSearchProvider 删除 |
| `search/config/SearchConfig.java` | activeProvider 注释更新（仍委托 settings，派生语义在上） |
| `system/dto/WebSearchSettingsVO.java` | +tavilyEnabled；activeProvider 注明派生回显 |
| `system/dto/WebSearchSettingsUpdateRequest.java` | +tavilyEnabled；-activeProvider |
| `system/controller/SystemSettingController.java` | PUT 处理 tavilyEnabled；VO/test 端点 activeProvider 均取派生值（**派生收在 SystemSettingService 而非注入 SearchConfig**——@WebMvcTest 切片无该 bean 会 context 挂，Legacy404Test 实测踩过） |
| `llm/provider/ClaudeProvider.java` | 顺手修：thinking 节 `Map.of` 无序 → 序列化 key 序不稳，改 LinkedHashMap（type 在前，Anthropic 文档序），ClaudeProviderTest 3 断言复活 |
| 测试 | SearchConfigTest 新建 3 用例；SystemSettingServiceTest +3（开关开→tavily / 缺失或关→builtin / upsert 布尔）；全量 **2712/2712** |

## Chunk B · 前端配置页开关制（commit `9f5e0884`）

| 文件 | 改动 |
|---|---|
| `components/settings/WebSearchSettingsTab.vue` | 「默认 Provider」下拉撤 → 「Tavily 启用」开关 + 「当前路由」派生标签（随开关变）；可用性只显 tavily/builtin；key 区只留 Tavily；部署提示两路（Tavily 申请 + SearXNG Docker `formats:[json]` + env `SEARCH_SEARXNG_BASE_URL`） |
| `api/system.ts` | WebSearchSettings +tavilyEnabled、-hasSerper/hasBingKey；Update -activeProvider/serperKey/bingKey、+tavilyEnabled |
| 测试 | WebSearchSettingsTab.test.ts 新建 6 用例（无旧下拉/派生回显/拨开关 PUT/收 key 规则/测试连通/可用性过滤）；vitest **994/994**、vue-tsc 0 错 |

## 语义边界（写清楚防回退争议）

- 「key 错误」不在路由层判——运行时兜底走 WebSearchService 既有降级链（tavily 不可用/调用空/抛 → builtin 重试）。
- serper/bing provider 类保留注册（available 自检仍可见）但**不再被路由**；后端 PUT 的 serperKey/bingKey 字段保留兼容，UI 已不提供入口。
- 旧 `search.active-provider` DB 行废弃不读，无需迁移清理。
- 总开关 `search.enabled` 语义不变（false=整体禁用返空）。

## 下一步

Chunk C 文档同步（本文件 + user-ops IX §3 + 测试方案 Q 系列 + 运维手册 §十一 + 速查表21 + feature-map IX + 问题单 9x）。人工验证：Q 系列（重写后口径）。
