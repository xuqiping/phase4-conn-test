# 计划7：部署上线与功能完善

> 创建日期：2026-05-31
> 状态：待执行
> 前置条件：阶段一~六全部完成

---

## 概述

系统核心功能已全部实现，当前进入收尾阶段。本计划包含部署准备、端到端测试、功能补全三部分。

---

## 已完成（本次会话）

### ✅ LLM连通测试功能

**目标**：用户添加供应商后可测试API Key+Endpoint配置是否正确

**实现**：

| # | 任务 | 文件 | 状态 |
|---|------|------|------|
| 1 | 后端 `testConnection()` — 发短消息验证 | LlmProviderService, TestConnectionResult DTO | ✅ |
| 2 | 后端用户级测试端点 | UserLlmController `POST /api/llm/user/providers/{id}/test` | ✅ |
| 3 | 前端API调用 | llm.ts `testProviderConnection` + `testUserProviderConnection` | ✅ |
| 4 | 前端管理员测试按钮 | ProviderManageTab.vue (表格+弹窗双入口) | ✅ |
| 5 | 前端用户测试按钮 | UserProviderTab.vue (卡片+弹窗双入口) | ✅ |
| 6 | 单元测试 6/6 | TestConnectionTest (成功/404/无模型/无端点/API错误/直传) | ✅ |

### ✅ Bug修复

| # | Bug | 修复 | 状态 |
|---|-----|------|------|
| 1 | AES加密密钥41字节，AES要求16/24/32 | AesEncryptService: SHA-256派生32字节 | ✅ |
| 2 | PG models/config列JSONB，MyBatis传String类型不匹配 | V9迁移: JSONB→TEXT | ✅ |
| 3 | 前端错误消息只显示❌无文字 | request.ts: 修复`const { message }`变量遮蔽 | ✅ |
| 4 | 后端异常全返回"服务器内部错误" | GlobalExceptionHandler: 返回异常根因消息 | ✅ |

---

## 待执行

### Task 1: 端到端测试（优先级：高）

**后端集成测试**

| 测试场景 | 说明 |
|----------|------|
| LLM供应商全链路 | 创建→测试连通→更新→删除→重新加载 |
| Chat全链路 | 创建会话→发消息→OrchestrationEngine→MemoryService |
| 用户级供应商 | 创建用户Key→测试→覆盖全局配置→聊天使用用户Key |

**前端E2E测试**

| 测试场景 | 说明 |
|----------|------|
| 供应商管理 | 添加→测试连通→编辑→删除→刷新配置 |
| 聊天流程 | 创建会话→发消息→收回复→删除会话 |
| 认证流程 | 注册→登录→Token过期→自动刷新→登出 |

### Task 2: 功能补全（优先级：中）

| # | 功能 | 前端改动 | 后端改动 | 难度 |
|---|------|---------|---------|------|
| 1 | 条件分支执行 | — | WorkflowStrategy表达式求值 | 中 |
| 2 | 会话分组 | SessionList按今天/昨天/更早分组 | — | 低 |
| 3 | Markdown渲染 | MessageBubble内AI回复渲染Markdown | — | 低 |
| 4 | Agent/Workflow选择器 | ChatInput内嵌选择器切换聊天模式 | — | 低 |
| 5 | 画布撤销/重做 | Vue Flow history管理 | — | 低 |

### Task 3: 部署准备（优先级：高）

| # | 任务 | 说明 |
|---|------|------|
| 1 | 生产配置 | application-prod.yml, PostgreSQL连接池, Redis连接池 |
| 2 | 前端构建优化 | Vite build, 代码分割, gzip压缩 |
| 3 | Docker化 | Dockerfile(后端+前端), docker-compose.yml |
| 4 | Nginx配置 | 反向代理, HTTPS, 静态资源缓存 |
| 5 | 环境变量 | 敏感配置外部化(JWT密钥, AES密钥, DB密码) |
| 6 | 日志配置 | logback-spring.xml, 按日滚动, 错误级别分离 |
| 7 | 健康检查 | Spring Actuator端点, 数据库/Redis连接检测 |

---

## 技术债务

| 项目 | 说明 | 优先级 |
|------|------|--------|
| OpenAICompatibleProvider URL拼接 | endpoint含`/v1`时与硬编码路径重复，需智能处理 | 中 |
| WebSocket重连 | 断线后自动重连+消息补发机制 | 中 |
| 并发安全 | LlmConfig.reload()并发调用保护 | 低 |
| API限流 | 未实现Rate Limiting | 低 |

---

## 执行建议

1. **先做 Task 3（部署准备）** — 让系统跑在生产环境
2. **再做 Task 1（端到端测试）** — 在生产环境验证
3. **最后 Task 2（功能补全）** — 按用户反馈优先级排序
