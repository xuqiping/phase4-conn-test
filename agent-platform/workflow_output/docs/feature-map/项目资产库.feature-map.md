# 项目资产库 · Feature Map（代码-功能速查）

> B/C 类功能。资产库 = 类型 × 叙事角色双轴矩阵 + 项目成员授权 + 公众池审批只读 + 版本/一致性包 + 画布双向打通 + 节点@引用。
> 后端独立 `asset` 包（canvas 单向只读依赖，零回归）；前端 `components/asset/` + 画布打通散落 `components/canvas/`。
> 配套：[设计方案](../docs/plans/项目资产库设计方案.md)｜[plan](../docs/plans/项目资产库.plan.md)｜[User-Ops](../docs/user-ops/项目资产库用户操作手册.md)｜[README](../../workflow_output/开发进度/项目资产库/README.md)

## 一句话原理

把创作中反复用的「提示词/剧本/图片/视频/音频」当**资产**存进**项目**，用「类型 × 叙事角色」双轴分类归档，配**版本快照**让画布引用某版后不被新版冲掉，再让画布节点能**入库产新资产**或**从库引用资产**，节点间还能用 **@** 互引上游产出。

## 数据模型（Flyway V56–V59，全新建表，回滚=drop）

| 表 | 用途（生活比喻） | 关键字段 |
|---|---|---|
| `asset_projects` | **项目**=一个创作项目（一部短片的所有素材归一处） | owner_id / narrative_roles(JSONB 五桶) / deleted |
| `asset_project_members` | **成员授权**=谁能进这个项目（户主/同住人/访客） | project_id / user_id / role(OWNER·EDITOR·VIEWER) UNIQUE(project,user) |
| `assets` | **资产**=一件素材（一张人物图） | project_id / media_type / status(DRAFT·LOCKED·ARCHIVED) / current_version(乐观锁) |
| `asset_versions` | **版本快照**=素材的某一次定稿（图 v1/v2，画布引用锁某版） | asset_id / version / content(JSONB) / file_id / gen_meta(谱系) |
| `asset_role_links` | **叙事角色挂载**=素材归到哪个角色桶（这张图是「人物」） | asset_id / role_key |
| `asset_bindings` | **画布↔库台账**=资产产自哪个节点(PRODUCED)/被哪个节点引用(REFERENCE) | asset_id / node_id / canvas_id / kind |
| `asset_projects` V88 扩展 | **公众池发布快照**=项目是否公开、采用哪种访问方式、由谁发布 | public_pool / public_access_mode / published_by/at / published_by_admin |
| `asset_public_access_requests` | **公共访问申请**=谁申请使用哪个审批型项目 | project_id / applicant_id UNIQUE / status(PENDING·APPROVED·REJECTED·REVOKED) |

> **大白话**：项目像文件夹，资产像文件夹里的文件，版本像文件的「历史版本」（只读快照），叙事角色像贴在文件上的彩色标签（人物/道具/场景/风格/通用），绑定台账像「这个文件是哪个画布节点生成的/被哪些节点引用」的借阅登记。

**索引**：`assets(project_id,media_type,updated_at) WHERE deleted=0` 部分索引缩圈；`role_links(asset_id)`；`members(user_id)`。角色筛选走 `asset_role_links` 关系表（不查 JSONB）。

## 后端代码地图（`backend/src/main/java/com/superprogrammer/asset/`）

| 文件 | 作用 | 关键调用链 |
|---|---|---|
| `service/AssetAclService.java` | **权限咽喉点**：`loadAccessible(projectId,userId)` 三判（owner/member/admin）否则 403 | 所有项目级端点入口必调 |
| `controller/AssetProjectController.java` + `AssetProjectService.java` | 项目 CRUD（我的/共享给我两视图）+ 级联软删(L4) + 受控词汇桶维护(L10) | projectApi.list/get/create/remove/transfer |
| `controller/AssetMemberController.java` + `AssetMemberService.java` | 成员授权/改角色/移除/转让(L1) + 审计日志 | memberApi.list/invite/update/remove |
| `controller/AssetController.java` + `AssetService.java` | 资产 CRUD + 矩阵筛选(type×role×q×status) + 计数聚合 + 状态机(L2/L3) | assetApi.list/get/countMatrix/lock/unlock/archive |
| `controller/AssetUploadController.java` | 上传落 SOURCE_ASSET + ImageIO 宽高/javacv 元数据懒提取 | assetApi.upload |
| `service/AssetVersionService.java` | 版本乐观锁(WHERE current_version=?) + 一致性包局部合并入 content.consistency | versionApi.list/get/create/saveConsistencyPack |
| `service/AssetScriptService.java` | 剧本 AI 拆分场（LlmGateway，复用 canvas parseScenes 容错） | scriptApi.breakdown |
| `service/AssetBindingService.java` | PRODUCED/REFERENCE 绑定写入 + 重复检测 + 双向追溯 | （被 bridge 调） |
| `service/AssetCanvasBridgeService.java` | **画布打通核心**：importFromCanvas(节点→资产) / resolve(资产→版本快照,viewer 可读) / usages / mapMediaType(5类映射) | assetBridgeApi.importFromCanvas/resolve/usages |
| `AssetPublicPoolController/Service` | 发布、移出、公共摘要白名单；管理员官方 OPEN，移出撤销有效申请 | publicPoolApi.list/publish/unpublish |
| `AssetPublicAccessController/Service` | 申请、本人状态、OWNER 列表、条件审批/拒绝/撤销 | publicPoolApi.requestAccess/listRequests/decide/revoke |
| `AssetFileAccessGrantor` + `AssetMapper#countAccessibleFileReferences` | 将 `/api/files/{fileId}` 接入资产项目 ACL；OWNER/成员、OPEN、APPROVED 可读，失权即时生效；V94 为 `asset_versions(file_id)` 加部分索引 | `FileStorageService#loadPath` → `FileSharedAccessGrantor` |
| `AssetAclService` + `AssetService.copyCurrent` | OPEN/APPROVED 增只读 VIEWER；源读+目标写后事务复制当前版本 | project/asset/version/resolve/copy 全链 |

**安全**：全端点 `@RequirePermission("asset:write")`；resolve/usages 前过 Acl 防 fileId 遍历；权限 seed V58（asset:write gated admin）。

## 前端代码地图

### `frontend/src/components/asset/`（资产库页面组件）
| 文件 | 作用 |
|---|---|
| `ShareDialog.vue` | 成员表 + 资产域远程候选搜索；只取 id/username，成员/候选错误隔离，跨项目请求防串 |
| `PublicPublishDialog.vue` | OWNER 选择 OPEN/审批型；管理员官方 OPEN；非幂等发布完成锁与会话防重 |
| `PublicAccessDialog.vue` | 申请动作矩阵、同项目静默对账、条件审批 mutation 防重；优先显示后端最小联查的申请用户名，旧响应以用户 ID 兜底 |
| `AssetCard.vue` | 类型色块图标 + 状态/版本/角色徽标(>3 聚合+N) + 点击 emit open |
| `AssetMatrixFilter.vue` | 顶栏类型×左栏角色分段 + **计数徽章下钻交集算法**（选类型→角色徽标=该类型下各角色 cell） |
| `AssetDetailDrawer.vue` | n-drawer 预览(文本JSON/文件 fetchCanvasPreview→objectURL) + 状态机动作 + usages 列表 |
| `VersionTimeline.vue` | 版本倒序 + 点选只读回滚查看（快照不可变） |
| `ConsistencyPack.vue` | 主参考图/图集/标准描述/参数基线 → saveConsistencyPack（仅 人物/道具/场景 额染） |

### 画布打通（散落 `components/canvas/` + `views/CanvasView.vue`）
| 文件 | 作用 |
|---|---|
| `SaveToAssetDialog.vue` | 节点入库弹窗（项目→角色→命名 + **重复三态**：首次 created=false 显 duplicate → 选 NEW_VERSION/NEW_ASSET） |
| `AssetPicker.vue` | 库→画布引用；支持我的/共享与公共来源，未获批公共项目禁用，resolve 原样保留版本快照 |
| `nodes/useNodeAssetBadge.ts` | 5 节点共用徽标 computed（收 `Record<string,unknown>` bag 避窄接口摩擦） |
| `nodes/CanvasNodeBase.vue` | 加 `assetBadge` prop + header chip（hasUpdate 黄高亮） |

### S13 节点 @引用
| 文件 | 作用 |
|---|---|
| `utils/interpolate.ts` | **@引擎**：`ancestors`(反向BFS+visited防环) / `parseMentions`(`@{{node\|asset:id}}`) / `interpolate`(不递归替换+断链降级「【断链】」) / `findBrokenMentions` / `uniqueLabel`(L9序号) |
| `components/canvas/MentionTextarea.vue` | 原生 textarea 精准光标（非 n-input），@唤起→候选 label 过滤→↑↓/Enter/Esc→插入占位符 |
| `components/canvas/PropertyPanel.vue` | text/script/video 三处 prompt 换 MentionTextarea + 候选/断链/labels 三 prop + 断链 warn + 名称 @blur 重命名查重 |
| `views/CanvasView.vue` | 四 computed 祖先链(读 boardRef.getEdges().value 响应式追踪) + `buildMentionResolver`(按类型注入) + `interpolateForRun`(拷贝不污染原文) + onRunNode/onRunVideo 运行前插值 |
| `components/canvas/CanvasBoard.vue` | addNode label 去重（L9 新建/粘贴入口） |

### S15-S19 分镜与资产编辑
| 文件 | 作用 |
|---|---|
| `asset/service/AssetScriptService.java` | **一键分镜**：`POST /api/assets/assets/{id}/breakdown-storyboard`——读剧本 `content.template` 拆解规范 → LLM 拆镜（注入资产目录防幻觉实体）→ 产 N 个分镜资产（含 prompt+entityRefs）；重拆=整批替换旧产物 |
| `asset/dto/StoryboardBreakdownRequest/VO.java` | 拆镜入参（段数/规范约束）与产物 VO |
| `asset/dto/StoryboardSaveRequest.java` | 分镜 5 字段保存 DTO：`PUT /api/assets/assets/{id}/storyboard`（`AssetController.saveStoryboard`） |
| `components/asset/StoryboardFields.vue` | 分镜 5 字段流水线编辑（**字段 3 生图/字段 5 生视频为占位，待图模型 R-3**） |
| `components/asset/StoryboardRefList.vue` | 分镜引用实体列表 |
| 画布分镜节点 | 第 6 类节点：脚本节点「拆分镜」扇出生成（脚本→各分镜边）+ 段数/规范参数；产物一键入库（SaveToAssetDialog/AssetPicker 已接 STORYBOARD 字典） |
| V62 迁移 | 分镜媒体类型落地（media_type 受控词汇第六项） |

### API/类型/路由
| 文件 | 作用 |
|---|---|
| `api/assets.ts` | 资产原有 API + publicPoolApi、成员候选搜索、资产复制 |
| `types/asset.ts` | 原有类型 + 发布模式/申请状态/公共摘要/候选/复制契约 |
| `types/canvas.ts` | `AssetBadge` + `MentionCandidate` |
| `router/index.ts` + `Sidebar.vue` | 路由 `/assets`、`/assets/:id` + 入口 `asset:write` 显隐 |
| `views/AssetListView.vue` | 我的/共享/公共池三页签；发布、移出、申请状态与审批入口 |
| `views/AssetProjectView.vue` | 原矩阵与编辑链 + 公共 VIEWER 全只读和事务复制入口 |

## 技术原理大白话注解

- **双轴矩阵**：像 Excel 行(类型)列(角色)交叉，「图片×人物」=所有人物图。计数徽章用一条 `GROUP BY media_type, role` 聚合返全图，**不每格一次 count**（防几十次查询）。
- **版本快照**：引用资产时**锁定当时那一版**，资产升 v3 不影响已引用 v2 的内容（画布引用存版本号）。`current_version` 乐观锁防两人同时提交撞车（UPDATE WHERE current_version=? 0 行=冲突→CONFLICT）。
- **咽喉点（AclService）**：所有项目级请求必经的权限门，三判 owner/member/admin，否则 403。viewer 只读可引用（resolve 通），写操作全拒。
- **公众池不复制**：发布像把原文件夹放进公共阅览室，源项目更新后公共读者看最新；只有点“复制”才在自己的项目生成独立资产和版本。
- **审批独立于成员**：公共获批像临时阅览证，不进入“共享给我”，移出公众池立即失效；正式成员授权仍保留。
- **PRODUCED/REFERENCE 绑定**：资产详情页「使用记录」=这张图是哪个画布节点**产出**的(PRODUCED)、被哪些节点**引用**了(REFERENCE)，双向台账。
- **@引用祖先链**：@只能引用**经连线能回到的上游**（反向 BFS）。@沿既有连线 ⇒ 被引用方拓扑序必在前 ⇒ 先跑完 ⇒ 运行前插值拿到的上游产出已物化。占位符存 **node id**（重命名不断链），**不递归**展开（防 A@B、B 含 @ 死循环）。
- **断链灰显**：删连线/删上游节点后，@占位符指向的节点不在祖先集 → `findBrokenMentions` 检出 → 属性面板黄色 warn + 运行时降级注入「【断链】」（不级联失败）。
- **L9 重名序号**：同画布节点 label 不许重复，新建/粘贴/重命名三入口都过 `uniqueLabel`（撞名→「图片 2」「图片 3」），占位符存 id 不受 label 改名影响。

## 踩坑批注

- **JSONB 角色筛选**：角色过滤走 `asset_role_links` 关系表（不查 JSONB 全表扫描），先用部分索引缩圈。
- **N+1 防护**：列表查出 asset ids 后 `IN` 批查 role_links 内存组装，不每资产查一次。
- **大版本 content 懒加载**：版本列表只 select meta，content（剧本全文）按需单取。
- **重命名查重契约**：`onRenameBlur` 的 `allLabels` 必须**按节点 id 剔除自身**传入，非按值剔——否则另一节点的同名也被误剔导致漏判。
- **响应式祖先集**：computed 内调 `boardRef.getEdges()`，其 `return edges.value` 在 computed 中被 Vue 追踪，增删边自动重算，**无需手动 tick**。
- **MentionTextarea 用原生 textarea 非 n-input**：需 `selectionStart`/`setSelectionRange` 精准光标做 @唤起+中段插入，n-input 不暴露光标 API。
- **画布→库单向只读依赖**：`asset` 包调 `CanvasService.loadOwned`（只读 DTO），canvas 包不 import asset，保 canvas 零回归（设计 §十四）。

## 测试

- Phase 3 定向：资产后端最高记录 130/130；ShareDialog 15/15；公众弹窗 28/28；列表/详情 31/31；AssetPicker 7/7；相关构建成功。
- Phase 4 待：统一全量回归、浏览器角色矩阵实测、V88 真 PostgreSQL 迁移与并发验证；历史 V56-V59 路径继续回归。

## 2x 第二轮修复增补（2026-08-14，画布侧）

> 6 项全为前端修复（后端媒体/资产桥/Ark 供应商链路本就正确），仅 canvasVideoAttachments 有纯函数变更。

- **视频入库按钮解门控**（VideoGenView）：下载/入库只按任务 SUCCEEDED 门控，视频 blob 拉取失败也能入库（入库走 file_id 引用）。
- **画布任务重进恢复**（CanvasView）：taskId 提交瞬间即时持久化（onSave(silent) 绕 800ms 防抖）；loadCanvas 对 status=running 且带 taskId 的视频/图片节点续轮询（resumePendingTasks）；onBeforeRouteLeave/beforeunload flush 防抖保存。
- **画布图片参数面板**（PropertyPanel + CanvasView.buildImageRequest）：capability 驱动（尺寸/输出格式/优化模式/引导尺度/组图+数量/水印/联网），切模型重置默认值并清不支持字段，提交按能力过滤组包。
- **@视频引用附件化**（canvasVideoAttachments.ts）：video 节点 @ 收为 kind='video' 附件 + 序号化「视频N」，供应商侧映射 reference_video；与首尾帧互斥。单测 14/14（+5 新例）。
- **资产拉取预览修复**（applyAssetResolve）：mediaType 比对由英文 'VIDEO'/'IMAGE' 改为中文受控词汇常量 MEDIA_TYPE.*，预览分支恢复命中。
- **拉线建节点 + 节点名回填**（CanvasBoard connect-start/end + backfillNodeLabel）：输出句柄拉线到空白弹「添加下一节点（自动连线）」；任务完成后默认类型名（图片/视频 N）自动回填提示词前 12 字，用户自定义名不覆盖。

测试：vue-tsc 0 错；vitest canvasVideoAttachments 14/14；build 17s 通过；浏览器实测（参数面板 lite/pro 切换、资产拉取预览、任务刷新恢复+名字回填）。
