# 变更记录（Changelog）· SuperCoder Agent Hub

> Phase 6 产物。项目级变更日志，每次**改 / 增 / 删已有功能**追加一条。
> 新功能开发不记这里（走主链）；本表只记**对存量功能的变更**。
> 记录过多时按年份/季度拆分（如 `变更记录.2026.md`）。

## 记录格式
日期 | 类型(改/增/删) | 变更内容 | 原因 | 影响范围 | 涉及文件/文档 | 回滚 commit

## 变更历史
| 日期 | 类型 | 变更内容 | 原因 | 影响范围 | 涉及文件 / 文档 | 回滚 commit |
|---|---|---|---|---|---|---|
| 2026-07-17 | 增 | 建立 workflow_output/ SDD 脚手架 + .github/prompts/ | 按 SDD 规范改造 | 文档层（不动源码） | workflow_output/、.github/prompts/ | 5d6118d2 之后 |
| 2026-07-17 | 改 | 个人记忆冲突快照补 keyZh/entities/anchor 字段 | resolve/flag/createFlagged 物化新行丢 memory_key_zh+entities（演进 #26 V38 bug） | 个人记忆冲突解决（速查表09） | MemoryConflictService.java、MemoryService.java、速查表09+演进、开发进度1 | 待 commit |
| 2026-08-10 | 改+增 | 设计资产授权修复、项目公众池审批访问及价表模型下拉 | 人工测试发现普通 OWNER 授权 403、公众池缺失、价表模型手填 | 资产 ACL/成员/版本/画布桥接、资产页面、计费配置 | 资产公众池规格、积分计费规格、PRD、影响评估；后续实现位于 asset/billing 模块与资产/价表页面 | a0b67cf4 |
| 2026-08-11 | 改 | 修复公共/复制媒体 403、历史全局价漏出候选、审批列表只显示用户 ID | Phase 4 真实浏览器冒烟发现阻断与可读性缺陷 | 文件共享 ACL、价表候选与创建判重、公共审批列表 | asset/file/billing 后端、审批前端、V94、规格/Feature Map/User-Ops/验证记录 | 9d99eda |
| 2026-08-12 | 改+增 | 认证邮件与短信改为管理员动态配置，DB 加密配置优先、环境变量兜底 | 部署后可即时调整且密钥不回显 | 认证服务、系统设置 API、设置页 | [影响评估](影响评估-认证通道管理员配置.md)、[Delta](变更规格delta-认证通道管理员配置.md) | 待本 chunk 提交 |
| 2026-08-14 | 改+增 | 适配 Qwen 多模态 Embedding 与专用 Rerank，接通独立路由、Ranking、诊断、计费和设置页 | 已配置模型协议不兼容且 Rerank 仍为占位实现 | LLM Provider/Gateway、知识库 Ranking、计费迁移 V119、设置页与价表页 | Qwen 适配规格/计划/测试方案、RAG Feature Map/User-Ops | cae7e4c～b9fb925 |

> 📌 改/删已有功能前，先填 [影响评估](影响评估.md) 模板查引用点。

## 模板
- [_模板.变更记录.md](_模板.变更记录.md)
- [_模板.影响评估.md](_模板.影响评估.md)

## 2026-08-14 · 8x/13x/4x 人工测试第二轮修复
- **变更内容**：审计日志用户列"-"清零（写入盖戳+查询回填+前端兜底）、审计/安全事件详情中文键值渲染（DetailKvView+detailLabels.ts）、安全事件处置过程时间线说明、媒体下载 ETag+304 缓存、视频任务入库资产库（mediaKind=VIDEO）。
- **原因**：人工测试问题清单 8x/13x/4x 未解决项。
- **影响范围**：AuditLogController/SessionService/AuthService、MediaGenController.serveFile、AssetMediaBridgeService/Controller/MediaImportRequest、前端 AuditLogView/SecurityEventView/SecurityEventView 所需字典/VideoGenView/media.ts/types。
- **回归**：vue-tsc 0 错误；build 17.45s；冒烟 9 项（用户回填 50 行 0 空、ETag 200→304、VIDEO 入库 asset#146=视频 v1、IMAGE 老路径回归 asset#147、非法 mediaKind 400、测试资产已清理）。
- **回滚**：逐 commit revert（见 git log beifen 分支 2026-08-14 三笔）。

## 2026-08-14 · 14x 知识库问题修复（四能力 + 三处稳健性修复）

- 智能对话引用知识库：ChatView KB 多选下拉 + chat store/api 透传 kbIds（后端链路既有）；浏览器实测 📎 引用回显。
- 直接输入文本入库：DocumentManager 新入口，docType=FILE+indexMode=MANUAL 复用链路，实测入库+检索+引用。
- 上传白名单对齐：FileSecurityPolicy 补 markdown/html/htm；前端 accept 补齐（修「前端可选后端必拒」）。
- Grounded Answer 稳健性：chatMaxTokens 1200→3000（截断 JSON 断尾）、提炼提示词精简约束（长输出超时）、lenientModelJson 对象包裹兜底。
- 回滚：revert 本条对应 commit 即可（无 schema 变更；docType=MANUAL 文档行为不变）。


## 2026-08-14 · 7x/13x/2x 三清单问题修复（第二轮）

### 7x-6 视频价表两面参考维度（billing）
- 后端 `PricingConfigService.availablePricingModels`：VIDEO 候选两面（has_reference true/false）配齐才排除；只配一面保留并带 hint。`AvailablePricingModelVO` +hint 字段。
- 前端 PricingConfigView：候选 label 带 hint；切候选复位 hasReference；编辑模式身份开关 disabled。
- 单测：PricingConfigServiceTest 30/30（+2）。

### 13x 第二轮 安全事件详情（security）
- SecurityEvent +@TableField(exist=false) username；列表接口批量 join 回填（无 N+1）。
- OffHoursSensitiveRule 保留 targetType/targetId（buildDetail 抽纯函数，ColdRulesATest +1 = 15/15）。
- 前端 detailLabels：action 等新 key + auditActionCn() 组合翻译 module:action（盘点全部 @AuditLog 动作）；DetailKvView 按 key 上下文翻译；SecurityEventView 用户列/操作人显示账号名 + traceId 关联审计提示。
- 13x-2 回答（自动封号清单）落文档：仅登录暴破锁号15min、撞库/SQL累犯封IP 60min 自动处置；无自动永久封号。

### 2x 第二轮 资产库和无限画布（canvas，6 项全前端）
- VideoGenView 入库按钮解除 videoObjectUrl 门控。
- CanvasView 任务恢复：taskId 即时持久化（onSave(silent)）、resumePendingTasks 续轮询、路由离开/beforeunload flush 保存。
- PropertyPanel 图片参数 capability 驱动 + buildImageRequest 按能力组包。
- canvasVideoAttachments：@视频节点收 kind=video 附件（视频N 序号，帧互斥）；单测 14/14（+5）。
- applyAssetResolve mediaType 中文常量比对修复（资产拉取预览恢复）。
- CanvasBoard 拉线到空白弹「添加下一节点（自动连线）」；任务完成 backfillNodeLabel 提示词前12字命名。

### 回归与验证
- vue-tsc 0 错；vitest 全量 431/438（7 个失败为存量：PricingConfigView.test/VideoGenView.test 挂载缺 n-dialog-provider，与本轮无关，stash 验证过）；build 17s。
- 后端 PricingConfigServiceTest 30/30 + ColdRulesATest 15/15 + Controller 1/1。
- 浏览器实测：价表候选带 hint→创建有参考行→候选消失；安全事件 admin #1 + 「计费积分 · 新建价表」翻译；画布 lite/pro 参数切换、资产拉取预览、生图任务刷新恢复「生成中→完成」+ 节点名回填。
- 遗留测试数据：画布 5（未命名画布，含 1 图片节点测试任务）、Cdance2.0 有参考价行（¥15/百万，保留作真实定价示例，可改）。
- 回滚：无 schema 变更；逐文件 revert 即可。
