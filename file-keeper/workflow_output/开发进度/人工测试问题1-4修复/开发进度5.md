# 人工测试问题 1—4 修复 · 开发进度 5

> 日期：2026-09-03｜对应计划：Chunk 4｜状态：✅ 完成｜提交：`f1457dc`

## 本轮实现

- 将可渲染进程列集中到 `processColumns.ts`，移除无数据渲染能力的 `runtime/path` 默认项。
- `windowTitle` 默认可见且支持排序，标题空值继续由行组件显示 `-`。
- 增加 `PROCESS_SETTINGS_VERSION=1` 和纯函数 `normalizeProcessSettings`。
- 旧设置按列 key 合并：保留用户已有宽度等配置、补齐新列、过滤不支持列。
- 首次升级强制显示窗口标题；迁移版本写入后，用户再次手动隐藏会被保留。
- 列设置“恢复默认”复用唯一默认定义，避免两份数组再次漂移。

## TDD 与验证

- RED：默认列函数和设置迁移函数不存在，3 项测试先失败。
- GREEN：进程列、设置迁移、排序、渲染顺序和拖动配置测试 11/11。
- `npm run build` 通过；未修改 Rust 窗口枚举、激活、关闭或强制结束逻辑。

## 安全与性能

- 窗口标题仍通过 Vue 文本插值渲染，不使用 `v-html`，不写入持久化设置。
- 仅复用已有一次枚举结果，没有增加逐行系统查询，进程刷新复杂度不变。
- 未新增依赖、服务端请求、日志正文或远程配置。

## 下一步

Chunk 5：实现 `floating_ball/tray/exit` 三态关闭行为、独立悬浮球窗口和失败降级链。
