# 无限画布创作页 · Feature Map（功能-代码速查）

> 全栈 MVP 代码地图（C1-C13 落地）。表与 SQL 注解见文末（V55）。关联 [plan](../plans/无限画布创作页.plan.md) / [README](../../开发进度/无限画布/README.md) / [用户操作手册](../user-ops/无限画布创作页用户操作手册.md) / [速查表24](../../../项目工程文档/项目功能介绍/速查表/24-无限画布创作页.md)。
> PRD 编号：IC-1..IC-16（客户本轮需求第 11 项）。

## 后端 `canvas/` 包（com.superprogrammer.canvas，独立模块零回归）

| 文件 | 作用 | 大白话 |
|---|---|---|
| `db/migration/V55__canvases.sql` | 建画布表 + canvas:write 权限 seed | 见文末表注解 |
| `entity/Canvas.java` | 画布实体（继承 BaseEntity，`snapshot` JSONB + `JsonbStringTypeHandler`） | 一行=一张画布，整张画的结构（节点/连线/位置）塞进 JSONB |
| `mapper/CanvasMapper.java` | MyBatis-Plus mapper（纯 BaseMapper） | DB 读写，ownership 过滤在 service |
| `dto/CanvasVO.java` | 列表省 snapshot 重字段，详情带 | 列表只给名字/节点数，详情才给整张图（省流量） |
| `dto/CanvasCreateRequest.java` / `CanvasSaveRequest.java` | 建画布 / 存快照入参（snapshot ≤5MB 防撑爆 JSONB） | 用户填的画布名 + 整图 JSON |
| `dto/CanvasNodeDTO.java` | 快照内节点后端契约（id/type/position/data Map），5 类常量 | 画布上一个节点的数据形状，C4+ 按 type 分发生成 |
| `dto/NodeOutputRef.java` | 产出物引用（nodeId/fileId/outputKind） | 「这个节点的产物存在 stored_files 哪一行」——快照只存 fileId 不嵌 base64（防爆） |
| `dto/NodeRunResult.java` | 节点运行无状态回包（`dataPatch` 前端合并） | service 不碰 snapshot 读写，只回「该补丁」，前端合并+节流整存，零 JSONB 竞态 |
| `dto/FrameExtractRequest.java`/`VO` | 抽帧入参 `{mode,second}` / 出参 fileId+url | 取哪一帧的订单 / 抽完的图 |
| `dto/VideoClipRequest.java`/`VO` | 截取入参 `{startSec,endSec}` / 出参 | 时间段裁剪的订单 / 截完的片段 |
| `dto/StoryboardConcatRequest.java`/`VO` | 拼接入参 `{fileIds}` / 出参 fileId+总时长+段数 | 多段拼成片的订单 / 成片 |
| `service/CanvasService.java` | 快照 CRUD + **ownership 硬过滤** + `loadOwned`（写侧端点复用归属咽喉点）+ nodeCount 从 snapshot.nodes 派生 + 软删不级联清 stored_files | 用户只能动自己的画布；admin 旁路看全量；删画布产物保留（历史复用） |
| `service/CanvasNodeRunnerService.java` | 按 `type` 分发生成：`runText`/`runScript`→`LlmGateway.chat`；`runImage` MVP 抛未配话术；脚本 `parseScenes` 容错 | 节点「点运行」真正干活的地方——文本/剧本走大模型，视频重定向前端走 media API |
| `service/VideoFrameService.java` | javacv 抽帧（FIRST/LAST/AT）+ 截取 `clip` + 拼接 `concat`，流式按帧不整片 load，产物写临时文件返 Path | 后端唯一视频处理入口：取帧/裁剪/拼接，纯 Java 无系统依赖 |
| `controller/CanvasController.java` | REST 全端点 `@RequirePermission("canvas:write")` + 抽帧/截取/拼接每段 `loadPath` ownership 复检 + 临时文件 try-finally 删 | 前端接口入口；防借他人 fileId 抽帧/拼接（逐段归属校验） |

## 后端旧文件改动（纯增量，零回归）

- `file/service/FileStorageService.java`：新增 `loadPath`（`load` 咽喉点拆出返 `Path`，javacv 需 seekable 文件非 InputStream）+ 复用 `storeStream`。
- `file/entity/StoredFileEntity.java`：新增常量 `SOURCE_CANVAS = "CANVAS"`（画布产出物来源标记）。
- `media/dto/MediaTaskVO.java`：新增 `resultFileId`（同 `videoUrl` 归属门控暴露真 fileId——抽帧 `loadPath` 需真 fileId，而 videoUrl 是下载端点非 fileId）。

## 前端

| 文件 | 作用 |
|---|---|
| `types/canvas.ts` | `CanvasNode`/`CanvasEdge`/`CanvasSnapshot`/`StoryboardSegment`/`CanvasNodeStatus` 扁平类型（**规避 vue-flow `Node` 默认泛型深递归 TS2589**，范式同 `types/workflow.ts`） |
| `api/canvas.ts` | create/list/get/save/rename/remove + runNode/upload/extractFrame/clipVideo/concatStoryboard + fetchCanvasPreview（带 token 拉 blob 转 objectURL 预览） |
| `views/CanvasView.vue` | 列表↔编辑；onRunNode/onRunVideo/pollVideoTask/onRerunAll(topoSort+环检测)/onExtractFrame/onClipVideo/onStoryboardConcat/hydrateVideoPreviews；scheduleSave 800ms 节流；onSave 剥离 blob: previewUrl |
| `components/canvas/CanvasBoard.vue` | Vue Flow 画布 + Background + smoothstep + snap-to-grid + 拖拽增节点 + onNodeClick 手选跟踪（`n.selected` 联合类型不可达）+ markRaw 注册 5 节点 + getSnapshot/getEdges/getNode/updateNodeData |
| `components/canvas/PropertyPanel.vue` | 选中节点按 type 显隐字段 + 运行/上传/抽帧（首/尾/N秒）/截取（起止秒）按钮，disabled 当 fileId 空 |
| `components/canvas/StoryboardPanel.vue` | 右抽屉：投影视频节点为段 + 上下调序 + 顺序预览（`<video>` auto-advance on ended）+ 合计时长 + 拼接按钮（<2 段 disabled） |
| `components/canvas/FocusEditOverlay.vue` | 焦点编辑沉浸框选 → cropRect+parentFileId+描述 → 产新图节点 + 自动连边（提取质量待 R-8 分割/生图模型弱保底） |
| `components/canvas/nodes/CanvasNodeBase.vue` | 共用外壳：accent 条 + 图标槽 + kind 标签 + 运行态徽标（idle/running/success/failed）+ 顶/底 Handle + 选中辉光 |
| `components/canvas/nodes/{Text,Image,Video,Audio,Script}Node.vue` | 5 类节点 shape（各用 base + slot） |
| `router/index.ts` | `/canvas`（列表）+ `/canvas/:id`（编辑），同 video-gen 不按权限卡路由 |
| `components/Sidebar.vue` | `canEditCanvas` gated 入口（`hasPermission('canvas:write')` 显隐） |

## 关键调用链

**保存画布**：前端 `CanvasView.scheduleSave`（800ms 节流，剥 blob: previewUrl）→ `canvasApi.save` → `PUT /api/canvas/{id}` → `CanvasService.save`（ownership 校验 + 整存 snapshot JSONB）。

**节点产出（文本/脚本）**：`PropertyPanel` 运行按钮 → `onRunNode`（置 running）→ `POST /{id}/nodes/run` → `CanvasNodeRunnerService.run{Text,Script}` → `LlmGateway.chat` → 返 `NodeRunResult.dataPatch` → 前端合并 + scheduleSave。

**视频生成**：`onRunVideo` → 沿入边 `resolveUpstreamImageFileId` 找上游图 fileId 作 refFileId → `mediaApi.submitVideo`（`media:gen` gated，无权→403→节点 FAILED）→ 5s 轮询至终态 → SUCCEEDED 时 `fetchVideoBlob` 拉 blob 转 objectURL 预览，存 `taskId`/`mediaStatus`/`resultFileId`。

**抽帧/截取**：`PropertyPanel` 按钮 → `onExtractFrame`/`onClipVideo` → `POST /{id}/nodes/{nodeId}/frames` 或 `/clip` → ①`loadOwned`（画布归属）②`resolveVideoFileId`（定位视频节点+校验 type）③`loadPath`（**视频源文件 ownership 复检**，防借他人 fileId）④`VideoFrameService.extract`/`clip` ⑤`storeStream(SOURCE_CANVAS)` → 前端 `addNode` + `addEdge` 自动连边（失败标红源节点不建空节点）。

**故事板拼接**：`StoryboardPanel` 调序 → 拼接按钮 → `POST /{id}/storyboard/concat`（body fileIds）→ ①`loadOwned` ②`LinkedHashSet` 去重保序 ③逐 fileId `loadPath` 复检 ④`VideoFrameService.concat`（首段尺寸初始化 recorder，逐段 grabImage 重编码进同一 recorder→H.264/mp4 临时文件）⑤`storeStream(SOURCE_CANVAS)` + try-finally 删临时文件 → 前端 `addNode(video)` 成片节点。

**一键重跑**：`onRerunAll` → `topoSort`（Kahn 入度+邻接表，`order.length<nodes.length`⇒环报错）→ 按拓扑序串行跑可生成节点（text/script/video）。

---

## 表与 SQL 注解（V55，Flyway）

> 建表走 Flyway（`V55__canvases.sql`）。已执行不可改，改结构加新版本。产出物复用 V40 `stored_files`（写一行 `source=CANVAS`）。

**`canvases`（画布快照表）**
- 生活比喻：一张「创作台照片」——把整张画布当前长什么样（有哪些节点、怎么连、摆哪里）拍成一张 JSON 照片存起来，下次按照片复原。
- 关键字段：
  - `id` IDENTITY — 画布唯一编号。
  - `user_id` — **谁的画布**，ownership 硬过滤靠它（普通用户只能动自己的；admin 旁路）。
  - `name` — 画布名（默认「未命名画布」）。
  - `snapshot` JSONB — 整张画的结构（`{nodes:[{id,type,position,data}], edges:[{source,target,...}]}`）。**只存 fileId 引用不嵌 base64**，否则 JSONB 撑爆。≤5MB 上限。
  - BaseEntity 审计列 `created_by/created_at/updated_by/updated_at` + `deleted`（`@TableLogic` 软删）+ `version`。
- 索引：`(user_id, updated_at) WHERE deleted=0` 部分索引（跳过软删行，列表查询快）。
- 权限 seed：`permissions('canvas:write')` + 仅 admin 给（gated，普通 user 按需授，照 V54 `media:gen` 范式）。
- 踩坑批注：① 软删画布**不级联清 stored_files**（产物保留供历史复用，后续清理脚本）；② snapshot 整存整取不建特殊 JSONB 索引；③ `JsonbStringTypeHandler` 同 `MediaGenTask.requestConfig`（String↔jsonb）。

**复用 `stored_files`（V40，画布产出物）**
- 画布所有产出（上传图/音频、抽帧图、截取视频、拼接成片、衍生图）都写一行 `source=CANVAS`，靠 `SOURCE_CANVAS` 常量标记来源。
- 不为画布新建表——复用 V40 既有索引与 ownership 咽喉点（`loadPath`/`load`）。

## 部署必做

- **linux 服务器 pom 追加 ffmpeg linux-x86_64 classifier**（C11/C12/C13 共用同一 javacv 栈）：dev 用 windows-x86_64 单平台，linux 不加会 `UnsatisfiedLinkError`。
- 临时文件落 `${java.io.tmpdir}`（`canvas-clip-*.mp4`/`canvas-concat-*.mp4`），storeStream 后即删；高并发截取/拼接预留 tmpdir 空间（单片段上限 600s·2Mbps≈150MB，拼接 ≤20 段）。
- 配置：`canvas.enabled`(默认true) / `canvas.max-nodes` / `canvas.frame-extractor=javacv`（ffmpeg 系统进程分支留后续）。

## 2x 第三轮修复增补（2026-08-16，C1/C2/C4：框选/一键关联/批量生成）

> 输入 [plan](../plans/资产库与画布_第三轮问题修复.plan.md)。每用户生成并发上限（后端闸）见 [生图大模型接入.feature-map.md](生图大模型接入.feature-map.md)（MediaInflightGateService，C3）。

| 文件 | 作用 |
|---|---|
| `components/canvas/CanvasBoard.vue` | **✋/▭ 双交互模式**（用户反馈免 Shift）：`dragMode` ref + 工具条切换按钮（aria-pressed + 高亮），pan=`panOnDrag:true`+`selectionKeyCode:'Shift'`（左键拖=平移，Shift+拖=临时框选），select=`panOnDrag:false`+`selectionKeyCode:true`（左键拖=Windows 式框选；1.48 无 selectionOnDrag prop，用 selectionKeyCode=true 恒选实现）+ `multiSelectedIds`（selection-end 同步，Esc/点空白清空，节点删除 watch 剪枝）+ `:delete-key-code="null"` 杜绝库默认 Backspace 静默双删 + 批量 removeNodes（连带删边） |
| `views/CanvasView.vue` | 选集 ≥2 顶部浮动批量工具条（一键关联/批量生成/批量删除二次确认）；C4 `onBatchRun`：诱导子图拓扑序 → 滑动窗口并发 2 → 429 指数退避 1s/2s/4s×3 → `runningNodeIds` Set（单跑/批量共存，finally 只删自身）+ 进度浮条（role=status aria-live=polite） |
| `components/canvas/CanvasBoard.test.ts`（新） | 双模式 wiring 单测 ×3（mock VueFlow 断言 panOnDrag/selectionKeyCode 随模式翻转 + aria-pressed）；坑：vi.mock 提升须 `vi.hoisted` 定义 stub，stub 声明的 props 会从 attrs 剥离 → 用 `getComponent().props()` 读 |
| `utils/autoAssociate.ts`（新） | 一键关联纯函数：`buildProposals`（祖先 label/资产名 ≥2 字，**长名降序**防「主角1」吞「主角10」，跳过 `@{{}}` 占位符内部区间防二次包装）+ `applyProposals`（按 pos 逆序替换防位移）；10 单测 |
| `components/canvas/AutoAssociateDialog.vue`（新） | 提案预览表（目标节点｜匹配上游｜前后文高亮），默认全勾可逐行取消；0 提案不弹窗 message.info（区分无祖先/名字未出现） |
| `utils/batchRunner.ts`（新） | 批量生成纯函数：滑动窗口调度/指数退避（fake-timer 单测）/资格预检（缺 prompt/model 列跳过原因）/`inducedTopoOrder`（诱导子图拓扑+环检测）/429 识别；16 单测 |

**关键语义**：一键关联写入的 `@{{node:id}}` 与手动 @ 同构——MentionTextarea 高亮、断链检测、运行插值链路**零改动复用**；批量生成复用既有 `submitVideoOnly/submitImageOnly`（taskId 即时持久化）+ `pollVideo/ImageTask`（返终态供进度计数）+ `resumePendingTasks`（刷新恢复，第二轮机制）。未选中的上游按**当前已物化产出**插值（不自动补跑）。

## 2x 五轮修复增补（2026-08-20：撤回/重做 + 版本保存 + 连线触碰恢复 + 计费去向标签）

| 文件 | 作用 |
|---|---|
| `components/canvas/CanvasBoard.vue` | **会话内撤回/重做引擎**：`undoStack/redoStack`（`reactive` 数组——plain 数组不触发 computed 失效，实测按钮恒置灰）+ `cloneHistoryState`（`JSON.parse(JSON.stringify(...))`，**不能用 structuredClone**——节点是 Vue 响应式 Proxy 抛 DataCloneError；快照本就 JSON 持久化）+ `pushHistory(tag)`（变更前推栈；同 tag 800ms 合并一步，栈深 50）+ undo/redo（当前态与栈顶互换，载入/恢复版本清栈=新时间线）+ `Ctrl/Cmd+Z`（+Shift 重做；输入框/textarea/contenteditable 聚焦不劫持）+ 工具条 ↩/↪（无可撤置灰）。**连线触碰恢复**：`connectingEdge` 状态 + 拖线期间 `canvas-board--connecting` 类下 hover 覆写淡化节点 `opacity:1`（解决「选中高亮淡化 0.25」与「拖线看不清途经节点」冲突） |
| `views/CanvasView.vue` | **版本面板**：编辑头 Git 图标 popover（存版本[可命名]/列表/恢复/删，恢复二次确认）；`buildPersistSnapshot` 抽出（剥 blob 预览 + 顶层 projectGroupId）；恢复=parseSnapshot→loadSnapshot（清撤回栈）→预览回填→在途任务续接。**计费去向**：编辑头改小标签只读 `stores/projectGroup`（7x 统一入口）；老快照 projectGroupId 载入/恢复时 `adoptLegacy` 一次性收养；切组 watch 触发 scheduleSave |
| `canvas/entity/CanvasVersion.java` + `mapper/` + `dto/`（新） | 版本实体（BaseEntity + `@TableLogic` 软删；`JsonbStringTypeHandler` snapshot jsonb）+ 请求/VO |
| `canvas/service/CanvasVersionService.java`（新） | create（空标签=时间戳名）/list（id desc 不带 snapshot 省流量）/restore（**先自动存「恢复前自动存」再覆盖**，误恢复可再恢复回来）/delete/prune（每画布保留最近 30，`.last("LIMIT 31")` desc 取溢出段软删）；跨画布版本 id → NOT_FOUND |
| `canvas/controller/CanvasController.java` | 5 端点 `POST/GET /{id}/versions`、`GET /{versionId}`、`POST /{versionId}/restore`、`DELETE /{versionId}`——均 `@RequirePermission("canvas:write")`，ownership 走 CanvasService.loadOwned 咽喉 |
| `CanvasVersionServiceTest.java`（新，后端） | 6 单测（创建回退/列表剥离快照/恢复双写/prune 溢出/跨画布 404/软删） |
| `CanvasBoard.test.ts` | +撤回 describe 5 用例（往返+栈尽、tag 合并、删节点独立步、loadSnapshot 清栈、工具条禁用态） |

**V135__canvas_version.sql**：`canvas_versions`（id IDENTITY + BaseEntity 审计列 + `canvas_id` FK→canvases **ON DELETE CASCADE** + label VARCHAR(64) + snapshot JSONB ≤5MB + node_count 冗余展示列 + `(canvas_id) WHERE deleted=0` 部分索引）。生活比喻：画布的「存档槽」——每格存一张整版照片，最多 30 格自动挤掉最老的；读档前先给当前拍照（恢复前自动存）。**撤回栈 vs 版本的区别**：撤回=本会话内小步回退（内存，关页即失）；版本=跨会话存档点（落库，换电脑也能恢复）。

## 变更记录

| 日期 | 变更 | 原因 |
|---|---|---|
| 2026-08-05 | 初版（C1-C13 落地 + 功能级收尾产出） | 画布 MVP 收尾 |
| 2026-08-16 | +第三轮增补：框选接管/一键关联/批量生成（C1/C2/C4，61630fdc/eaf32378/ae86ae20） | 2x 第三轮问题修复 |
| 2026-08-16 | 框选改 ✋/▭ 双模式：▭ 左键直接拖框免 Shift，✋ 保留 Shift 临时框选（CanvasBoard.test.ts 3 单测 + Playwright 实测） | 用户反馈：组合键不如双模式直观 |
| 2026-08-20 | +五轮增补：撤回/重做（会话内快照栈）+ 版本保存（V135 表+5 端点+恢复前自动存）+ 连线拖拽途经节点恢复不透明 + 计费去向改页顶统一入口（2x 五轮三项 + 7x 统一入口画布侧） | 2x 未解决 3 项 |
| 2026-08-25 | 画布生视频节点「15 秒视频下载落盘失败（16MB 缓冲上限）」修复——媒体下载链路改流式（DataBuffer 流→临时文件→storeStream），与视频生成页同一 MediaStorageService 咽喉，`82999162`；详见 SeedDance视频生成.feature-map.md 2x 增补 | 2x 未解决（画布侧同病） |
| 2026-08-28 | +修复VII 增补：节点 Ctrl+C/V 多选子图复制粘贴（诱导边/脱钩/鼠标落点+32 错开/单步撤回）+ 一键整理布局（dagre LR，全图/选中子图双模式、组整组拉入、fitView、一步撤回），`2b7b7ed0`/`404b77ab`/`f451eb9d`/`6b639e0e`/`1095e04a` | 2x 未解决最后 2 项 |
| 2026-08-29 | 修复VII P4 Playwright 实测 J1-J8/K1-K10 全过（2x 末 2 项勾销）；实测修复：Esc 后 selectedNodeId 隐形残留——syncSelectionFromLibrary 补 0 选中分支（属性面板挂旧节点/✨ 被隐形选中拉进子图模式收口），`07d63ae2` | 修复VII 验收 |
| 2026-09-01 | +修复X 增补：视频节点上传产物（≤50MB/重传覆盖/预检三 kind 单源）+ 从库选择全类型预览（悬浮放大/灯箱盖弹窗/音频行内播/文本片段/「选择」按钮唯一入口）+ 组边随 ⛓ 保留（新节点连原组两向/不入组员/解散丢边），`97d6547f`/`9c0cf4c6`/`5c7c6625`/`85d9ffd1`/`45723976` | 2x 未解决 3 项 |

## 2026-08-26 增补（修复III C/D：画布九项 2x-1~2x-9）
> commit `5453e59b`（C 七小件）/`0770434c`（D 引用与预览）。

| 文件 | 改动 |
|---|---|
| `CanvasBoard.vue` | C1 节点四角/四边拖拽改尺寸（结果随自动保存落库）；C5 16:9/9:16 产物定型后节点尺寸统一（不撑爆布局） |
| `nodeClone.ts` | C4 节点「创建副本」——复制节点及配置为新节点 |
| `MediaModelService` / `ImageModelVO` / `SystemSettingService` | C2 管理端可标记默认图模型；画布图片节点模型下拉默认选中（无标记回落第一个）；`ProviderManageTab.vue` 标记 UI |
| `PropertyPanel.vue` | C3 视频比例选择器独占整行（原与时长同挤一行被截断）；D2 选中媒体节点「上游」面板——分层展示全部上游（缩略图/视频/提示词/节点名），悬浮预览放大，点击开 Lightbox |
| `CanvasView.vue` | C7 防抖自动保存+右上「保存中/已保存/失败重试」徽标+未保存离开确认弹窗 |
| `Lightbox.vue` | D1 统一大图/播放预览（点产物缩略图弹窗，滚轮缩放），图片/视频/上游通用 |
| `upstream.ts` | D2 上游图遍历算法（63 行纯函数 + 单测） |
| `MentionTextarea.vue` / `caret.ts` / `mentionLogic.ts` | D3 **双击图片/视频节点=以 @引用 插入输入框末尾**并聚焦（生成中禁插）；@候选弹层改**光标锚定**——定位在 @ 字符处（上方优先、越界翻转下方），240px 宽最多 8 行滚动，键盘↑↓回车 Esc 可用 |
| 单测 | Lightbox/MentionTextarea/caret/upstream/nodeClone/CanvasBoard/PropertyPanel 新增约 25 用例 |

**大白话**：九件用户体感件——节点能拉大小、能复制、大小统一不乱跳；视频比例选项不再挤成一条缝；改了东西自动存（有徽标提示，走人不丢）；看上游产物像看族谱一层层往上翻，点开能放大细看；双击一张图直接把它当 @素材塞进输入框，@候选框跟在光标旁边不再钉死在输入框底部。


## 2026-08-27 增补（修复IV：两段式点击、上游直显、resize 手柄热区+四边、面板拖宽、自动保存补缺、新建定型尺寸）

> 对应 commit `58f64beb`（A 轮：分辨率整行）+ `b88f61fb`（B 轮：交互四项）+ `d843b8a6`（C 轮：保存与副本）。规格 §5。

| 文件 | 改动 |
|---|---|
| canvas/nodes/useMediaPreviewClick.ts（新建 composable） | 两段式点击判定：未选中→仅选中不开预览；已选中→点媒体区开 Lightbox；点候选层/非媒体区不触发 |
| canvas/nodes/CanvasNodeBase.vue、ImageNode.vue、VideoNode.vue | 接入两段式 composable（图片/视频节点统一行为） |
| canvas/CanvasBoard.vue | resize 四角手柄热区加大（视觉 8px + ::before 外扩 inset -7px，实际判定域约 22px）+ 四条边新增 `variant="Line"` 拖拽控件（热区收内侧、z-index 低于连线，不挡连线 Handle）；**structure-changed 补发**——新增节点即入防抖保存链（此前「加节点不保存」缺口）；新建 image/video 节点即按定型尺寸创建 |
| canvas/PropertyPanel.vue | 「上游」面板不再折叠——上游链全部直显、按媒体类型分色条（图片/视频/音频/文本）；面板左缘可拖宽，宽度记 localStorage（`canvas.propPanel.width`，NaN/越界回落 260）；视频分辨率选择器独占整行（A 轮，同 III 期「比例整行」手法）；节点文本失焦自动保存通知 |
| canvas/MentionTextarea.vue | blur 延迟后判定「未点候选行」才发失焦保存（点 @ 候选=不误触发、不关层） |
| canvas/nodeClone.ts + types/canvas.ts | 副本完全独立：节点配置+产物引用一并复制，fileId 拷贝/新资产记录（详见项目资产库 feature-map 同日增补） |

大白话：
- **两段式**——画布上「一碰就弹大图」治好了：第一下点节点=选中（出属性面板），再点缩略图本身才放大。像手机相册：先点进相册，再点照片才全屏。
- **resize 热区**——以前手柄 5px 点不中；现在角手柄实际判定域约 22px（视觉 8px+外扩）+四条边拉宽窄手柄，且不挡节点间的连线。
- **自动保存补缺**——以前「加个节点不落库」是保存链漏发事件；现在新增节点即入防抖保存。文本输入完失焦（点空白处）即存，@选人不受影响。
- **定型尺寸**——新建即按产物最终尺寸建节点，生成完成不撑爆布局/跳变（以前生成前小方块、生成后突变）。

### P4 回归修复（2026-08-27，commit `d4bfc3c3`）：手柄可见但拖不动

用户冒烟反馈「拖拽放大缩小箭头明显了，但实际无法拖拽」。真因两个叠加，均在 `CanvasNodeBase.vue`：

1. **裸 `ResizeControl` 缺 `nodeId`**——`NodeResizer` 包装组件有 NodeId 注入兜底，裸用控件没有；`nodeId` 未传时组件 watchEffect 早退、d3-drag 根本不绑定（手柄看得见、事件不存在）。补传 `useNode()` 的节点 id。
2. **根元素 `overflow:hidden` 裁掉外悬手柄**——库把角柄定位在节点框外（`bottom/right:-8px` + translate），根裁剪连渲染带命中一起裁。根去裁剪，顶部/底部圆角改由 `__accent`/`__body` 各自 `border-radius` 兜底。

大白话：把手装在了门框外面，可门框贴了层「不许超出」的膜，把手一半被膜盖住了；而且把手根本没拧螺丝（没传节点 id，事件没接上）。两处都修，把手才既看得见又拧得动。

## 2026-08-27 增补（修复V：上游视频首帧直出 + 副本重进 fileId 兜底恢复）

> 对应 commit `3c7926b0`。规格 §3（2x#1/2x#2）。

| 文件 | 改动 |
|---|---|
| canvas/PropertyPanel.vue | 上游面板视频类节点：有源显 `<video preload="metadata" muted playsinline>` **首帧预览**（不再只显「视」字占位）；无源回落原占位；video 层 `pointer-events:none` 让单击穿透到预览按钮开 Lightbox（手势不变）；hover 放大规则 video 与 img 同 |
| CanvasView.vue | 恢复链补 **fileId 兜底腿**：hydrateVideoPreviews 在无 taskId（副本场景）或 taskId 查询失败时，用 fileId 走 fetchCanvasPreview 补 previewUrl+status；生成中节点跳过（交 resumePendingTasks）；fileId 失效静默保持空壳（现状口径）；版本回滚链恢复同享 |

大白话：
- **首帧直出**——上游面板里的视频以前只显示一个「视」字方块；现在直接放视频元素取第一帧当缩略图（preload=metadata 只拉元数据不拉全片，省流量）。点它还是开大窗播放，习惯不变。
- **副本重进恢复**——创建副本时产物复制没问题，坏在「重进恢复」只认生成任务 id，而副本是新节点没这个 id → 一重进就空壳。现在多一条腿：没有任务 id 就拿文件 id 换预览地址，两条腿总有一条接得上。

## 2026-08-28 增补（修复VI VA~VE：粘贴/拖拽成节点、副本连线、提示词上限、附件 URL、参数 capability 对齐）

- **VA 粘贴/拖拽**（`619fecae`）：`utils/mediaLimits.ts` 单源上限+MIME 分流（新，VideoGenView 改引）；`CanvasBoard.vue` onDrop OS 文件分支 + 根元素 paste 监听（输入框焦点守卫）；`CanvasView.uploadAndCreateNode` 复用上传链逐文件建节点。测试 `mediaLimits.test.ts` 6 例。
- **VB 副本连线**（`619fecae`）：`nodeClone.ts` `cloneEdgesForNode()` 入+出边克隆改指副本（自环→副本自环）；`nodeClone.test.ts` 11 例。
- **VC 提示词上限**（`b8fda394`）：PropertyPanel 视频/图片 MentionTextarea maxlength=8000 + 官方建议文案；后端 8000 兜底零改动。
- **VD 附件图 URL**（`9e68287d`）：后端 `MediaReferenceUrlService.createMediaUrl` 泛化（图/视频同签名 URL 闸）、Worker 图片附件切 URL 传输、image 上限 8→30MB（对齐官方）。
- **VE 参数对齐**（`a84534b7`）：画布视频面板模型下拉换 `/media/models`（带 capability），比例/分辨率/时长/首尾帧/生成音频开关全按能力动态，切模型收敛不清空；@音频节点收 kind=audio 附件（音频N 独立编号）。
- 人工验证用例：`docs/测试方案/人工测试遗留问题修复VI测试方案.md`（H1-H12/L1-L6），2x 未解决 1~5 已挂「待人工验证（修复VI）」。

## 2026-08-28 增补（修复VII：节点 Ctrl+C/V 子图复制粘贴 + 一键整理布局）

- **VII-2 一键整理**（`2b7b7ed0`+`404b77ab`+`f451eb9d`）：新依赖 `@dagrejs/dagre`（gzip≈12KB，进画布 chunk）；新 `utils/autoLayout.ts` 纯函数——LR 分层（ranksep100/nodesep60）、中心点转左上角、新旧 bbox 左上角锚定（全图/子图同款）、16 网格取整、自环不参排、`estimateSize` 类型默认尺寸表导出共用；`CanvasBoard.vue` `onAutoLayout()` 范围裁决（多选→单选→全图；选中含组员→整组拉入）+ 工具条 ✨ 按钮（无节点禁用）+ 一次 pushHistory('layout')/单次 structure-changed + nextTick fitView。测试 `autoLayout.test.ts` 10 例 + CanvasBoard 5 例。
- **VII-1 复制粘贴**（`6b639e0e`+`1095e04a`）：新 `canvas/canvasClipboard.ts` 纯函数——`buildCopySet`（诱导边=两端在集内、RESET_KEYS 脱钩与「创建副本」同源、自环保留）、`planPastePositions`（bbox 中心对齐鼠标+pasteCount×32 错开+16 网格）、`planLabels`（现有+批内三级去重）、`remapEdges`（端点换新 id+seq 防撞）；`nodeClone.ts` RESET_KEYS 导出共用。`CanvasBoard.vue` `onWindowKeydown` 扩 Ctrl/Cmd+C/V（`isTypingTarget` 守卫=原生可编辑+naive 控件+@输入框；无选中 Ctrl+C 清剪贴板不拦→恢复图片粘贴通道）+ `pasteSubgraph()` 直批（一次 pushHistory('paste')+单次 structure-changed，**禁走 addNode** 防拆两步撤回）+ `nodes-copied` 事件（CanvasView toast）。测试 `canvasClipboard.test.ts` 9 例 + CanvasBoard 7 例。
- 人工验证用例：`docs/测试方案/人工测试遗留问题修复VII测试方案.md`（J1-J8/K1-K10），2x「未解决」最后 2 项已挂「待人工验证（修复VII）」。

## 2026-08-29 增补（修复VIII：组整体对外拉线 + 连线落节点本体直连）

> 对应 commit `4cd830eb`（Chunk A）。规格 `docs/specs/人工测试遗留问题修复VIII设计.md` §4 VIII-1/2；人工用例 `docs/测试方案/人工测试遗留问题修复VIII测试方案.md`（M1-M10）。

| 文件 | 改动 |
|---|---|
| `utils/groupEdges.ts`（新） | 纯函数全家：`isGroupEndpoint/groupIdOf/groupEndpointOf`（伪 id `group:{id}` 谓词）；`resolveEdgesForFlow`（广播+聚合展开：外部→组=组全员、组→外部=组全员、组→组=成员×成员去重、空组/展开自环丢弃）；`splitSnapshotEdges/mergeSnapshotEdges`（快照拆合往返）；`decideDropTarget`（handle/节点/组包围盒/空白四分派判定）。18 例单测 |
| `CanvasBoard.vue` | `groupEdges` 独立 ref（**绝不进 VueFlow v-model**——伪 id 引用不存在节点库渲染断裂）；getSnapshot 合并/applySnapshot 拆分/undo-redo 同口径（解散组 Ctrl+Z 组边随快照恢复）；`addEdgeInternal` 统一自环拒+同向去重；删组/解散/成员修剪/removeNodes 四路级联删组边（selectedEdgeId 悬空收口）；组包围盒**右缘中点 source 端口/左缘中点 target 端口**（pointer-events:auto+aria-label）；组边 SVG 覆盖层（贝塞尔/hover 辉光/selected 红粗/中点×删除/rAF 脏标记重绘）；端口拖线临时贝塞尔+pointer capture+blur/pointercancel 清理（review 补）；`onConnectEnd` 分派树（justConnected 防双建/落本体两方向直连/落自身静默/坐标∩组→组边/否则 quick-add 不变）；落点判定用**不吸附坐标** `clientToFlowUnsnapped`（project() 带 16px 网格量化会边缘误判，review 补）；组边 related 辉光 |
| `CanvasView.vue` | `resolvedFlowEdges()`（合并+展开）——selectedAncestors/上游面板/onRerunAll topoSort 三处改走；批量生成/一键关联按回归口径不动 |
| `utils/autoLayout.ts` | dagre 输入过滤伪 id 边（广播边 N 倍权重炸布局） |
| `canvas/nodeClone.ts` / `canvas/canvasClipboard.ts` | 克隆边/粘贴诱导边过滤组边（显式兜底） |
| `types/canvas.ts` | CanvasEdge.source/target 值域注释（`nodeId \| 'group:{groupId}'`，无结构变更老快照零迁移） |

大白话：以前「设为组」只是画个框圈人，连线还是得伸进框里够某个节点的小圆点；现在框的**左边缘和右边缘各长一个圆点**——从右点拖线出去=整组一起喂给下游（组里每个人的产物都算数），从外面拖线到框上=喂给组里每个人。普通节点照旧能单独连线，两种并存。另外拖线不用再瞄准小圆点：**线拖到对方节点身上松手就连上**（方向=你从哪个点起拖），拖到自己身上=取消，拖到空白=照旧弹快捷建点。

## 2026-08-29 增补（修复VIII：连线交互两问排查事实）

- 落点判定坐标系：组框体 `pointer-events:none`，落组空白处事件 target 是 pane——判定走**坐标∩包围盒**不依赖事件 target；`project()` 含 snap-to-grid 量化，判定专用不吸附换算 `clientToFlowUnsnapped`。
- 交互守护：拖线中组被异步解散→松手前验组存活（防死组边落库）；窗外松手丢 pointerup→window blur 兜底清理（防悬挂监听幻影建边）。

## 2026-09-01 增补（修复X：视频节点上传 + 从库选择全类型预览 + 组边随 ⛓ 保留）

> 对应 commit `97d6547f`（A 轮）/`9c0cf4c6`+`5c7c6625`（B 轮）/`85d9ffd1`+`45723976`（C 轮）。规格 `docs/specs/人工测试遗留问题修复X设计.md`；人工用例 `docs/测试方案/人工测试遗留问题修复X测试方案.md`（R1-R15）。

| 文件 | 改动 |
|---|---|
| `PropertyPanel.vue` | X-1 视频节点「上传视频」——上传即节点产物（fileId+previewUrl 落节点，@引用/抽帧/截取随产物即可用）；重传覆盖旧产物；「本地视频 ≤50MB」常显提示；预检走 `utils/mediaLimits.ts` 单源（视 50/图 30/音 15MB） |
| `canvas/AssetPickerRow.vue`（新） | X-2 从库选择行组件——图/视缩略接 `HoverPreviewImage`（悬浮放大 + 点缩略开 Lightbox，盖在弹窗之上 z-index 3000）；视频首帧+▶ 角标；音频「音」字标+行内播放条；文本显 textPreview 片段（无片段「提」字标）；`useLazyFilePreview` 惰性拉预览 URL（IntersectionObserver 进视口才拉，每行一实例）；「选择」按钮=唯一选中入口，行空白零动作、播放条点击只播不选 |
| `canvas/AssetPicker.vue` | 行渲染改 AssetPickerRow；状态标签/行样式迁入行组件；onPick/resolve 链不变 |
| `components/media/HoverPreviewImage.vue` | kind 扩 `video`（视频悬浮显首帧放大） |
| `canvas/canvasClipboard.ts` | X-3 `buildCopySet` 跨集边去组端点过滤（节点↔组边恰一端在选中集即收——新节点连原组）；innerEdges 诱导边仍排除组端点（半边保留口径）；`remapCrossEdges` 增第 4 参 `aliveGroupIds`（组伪 id 端保原+组解散丢边不产断边） |
| `canvas/nodeClone.ts` | `cloneEdgesForDuplicate` 去组边过滤（「创建副本」也连原组；组→组两端皆非原节点天然不收） |
| `CanvasBoard.vue` | `onCopyKeydown` 传 `[...edges, ...groupEdges]` 混合入参；`pasteSubgraph` 产物分流（组端点边进 groupEdges 池，伪 id 绝不进 v-model）；`appendEdges` 批次分流+组边浅拷贝剥会话 class（loadSnapshot 同口径） |
| `CanvasView.vue` | `onCloneNode` 改传 `[...getEdges(), ...getGroupEdges()]`——**getEdges 只返普通边**（:1644-1646），plan 原断言「已含组边」有误，实现轮更正 |

大白话：
- **视频上传**——视频节点以前生成前是空壳，现在能直接把本地 mp4 传成节点产物（≤50MB 预检拒超大），传完下游就能 @引用、抽帧、截取，重传即换掉。三个类型上限一处管（视 50/图 30/音 15MB）。
- **从库预览**——「从库选择」以前只有文件名；现在图片悬浮就放大（带尺寸）、点缩略图开大图灯箱且盖在弹窗上面；视频出首帧+▶、点击弹播放；音频行内直接播；文本显正文片段。想选用必须点「选择」按钮——点行空白不误选、点播放条只播不选。
- **组边保留**——⛓ 连线保留开着时，复制与组相连的节点，副本自动接回**原组**（两向都行：组喂它、它喂组），但副本不进组员名单；组解散了连线静默丢不断线。开关关=回到老行为（零组边）。IX 期「粘贴不带组线」的口径自此由 ⛓ 开关决定。
