# 影响评估 · <变更名>

> Phase 6 产物。**改 / 增 / 删已有功能之前**填——查清这次改动会动到什么，避免改完才发现别处炸了。
> 填完归档到 `workflow_output/docs/changes/影响评估-<变更名>.md`。
> **文档规模**：本文件不得超过 5000 tokens。影响范围过大时，拆分子文件（如按模块/子系统拆分），本文件保留总览和索引。

## 变更概要

- **变更类型**：□ 修改 □ 增加 □ 删除
- **变更目标**（要改什么 / 达成什么）：
- **变更原因**（为什么改）：
- **变更规模**：□ 小（局部）□ 中（多文件）□ 大（核心逻辑 / 跨模块）—— *仅记录，流程统一不简化*

## 引用点排查（改 / 删前必查）

> 工具：各功能的 Feature Map 调用链 + 全局搜索（grep 函数名 / 接口路径 / 字段名）

| 被改 / 删的对象 | 类型（函数/接口/页面/字段/表） | 谁在用它（引用位置） | 能否安全改 / 删 |
|---|---|---|---|
| | | | □ 能 □ 有依赖，需同步改 / 走废弃 |

## 受影响清单

| 受影响项 | 类型（功能/接口/页面） | 需要的动作（改/测/删） | 涉及文档（Feature Map/README/…） |
|---|---|---|---|
| | | | |

## 数据库影响

- □ 不涉及数据库
- □ 涉及：要删 / 改的表或字段：__________ → 走 **Flyway 新版本脚本**；老数据迁移 / 兼容方案：__________

## 回滚锚点

- 变更前 commit：<SHA 或描述>
- 回滚方式：<git revert / 换回旧部署 / 部署手册的回滚方案>

## 废弃判断（仅删除项）

- □ 删除对象**对外暴露**（公开 API / 页面 / 功能）→ 必须先 **deprecated 再删**，本次先标记废弃、给替代方案
- □ 仅内部、无外部依赖 → 可直接删

## 相关文档

- 对应功能 Feature Map：[../feature-map/](../feature-map/)
- 变更记录：[变更记录.md](变更记录.md)
