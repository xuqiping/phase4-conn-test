# 多Agent智能体平台 — 当前开发进度

> 最后更新：2026-06-13

---

## 总体进度

| 阶段 | 状态 | 完成度 |
|------|------|--------|
| 阶段一：需求与设计 | ✅ 完成 | 100% |
| 阶段二：系统设计 | ✅ 完成 | 100% |
| 阶段三：编码实现 | ✅ 完成 | 100% |
| 阶段四：编码质量 | ✅ 完成 | 100% |
| 阶段五：软件测试 | ✅ 完成 | 100% |
| 阶段六：运行时引擎 | ✅ 完成 | 100% |
| 阶段七：部署上线 | ⬜ 待开始 | 0% |
| 阶段八：多智能体与工作流自由组合 | ✅ 核心闭环完成，待真实环境验收与治理增强 | 95% |
| 阶段九：Agent对象级权限 | ✅ 核心闭环完成，待跨工作流权限增强 | 90% |

---

## 各阶段详细进度

### 阶段一：需求分析 ✅

| 交付物 | 状态 | 位置 |
|--------|------|------|
| 用户研究报告 | ✅ | 项目工程文档/需求/用户研究报告.md |
| 用户画像+故事地图(24条) | ✅ | 项目工程文档/需求/用户故事地图.md |
| 优先级矩阵(RICE) | ✅ | 项目工程文档/需求/优先级矩阵.md |
| 需求追踪矩阵RTM(54用例) | ✅ | 项目工程文档/需求/需求追踪矩阵RTM.md |
| 产品需求文档PRD | ✅ | 项目工程文档/需求/产品需求文档PRD.md |

### 阶段二：系统设计 ✅

| 交付物 | 状态 | 位置 |
|--------|------|------|
| DDD战略设计(4限界上下文) | ✅ | 项目工程文档/设计/DDD战略设计.md |
| 架构设计 | ✅ | 项目工程文档/设计/架构设计.md |
| API设计(13表DDL+42端点) | ✅ | 项目工程文档/设计/API优先设计.md |
| 设计系统(3主题) | ✅ | 项目工程文档/设计/设计系统.md |
| 技术选型矩阵 | ✅ | 项目工程文档/设计/技术选型.md |
| 5份ADR | ✅ | 项目工程文档/设计/adr/ |

### 阶段三：编码实现 ✅

| 子计划 | 内容 | 状态 | 文件数 |
|--------|------|------|--------|
| Plan1 | Spring Boot项目初始化+13表DDL+公共代码+Auth模块 | ✅ | 9个Task |
| Plan2 | Agent模块+Workflow模块+Execution预留 | ✅ | 9个Task，35源文件 |
| Plan3 | Vue3+TS+Vite+3套主题+登录页+主题切换 | ✅ | 7个Task，31文件 |
| Plan4 | AgentCard+大厅(4列网格+搜索筛选)+技能组件+详情页 | ✅ | 6个Task |
| Plan5 | Workflow API+Vue Flow画布+3种节点+编辑器+列表页 | ✅ | 9个Task，12文件 |
| Plan6 | Agent增删改界面+CRUD后端API+编辑弹窗+详情页编辑 | ✅ | 5个Task，后端4API+前端3文件 |

**阶段三交付统计**：6个Plan共44个Task，后端46个API端点，前端4个页面+工作流编辑器+管理后台，3套暗色主题

### 阶段四：编码质量 ✅

| 任务 | 状态 | 说明 |
|------|------|------|
| CLAUDE.md项目约定 | ✅ | 代码规范、Git规范、API规范 |
| .editorconfig | ✅ | 统一编辑器格式 |
| .gitignore | ✅ | 排除构建产物和IDE配置 |
| 前端Code Review | ✅ | 修复5个严重问题 |
| 后端Code Review | ✅ | 修复6个严重问题 |

### 阶段五：软件测试 ✅

#### 后端测试（69个全部通过）

| 测试类 | 数量 | 类型 | 状态 |
|--------|------|------|------|
| JwtUtilTest | 8 | 单元测试 | ✅ 全部通过 |
| AuthServiceTest | 8 | 单元测试 | ✅ 全部通过 |
| AgentServiceTest | 8 | 单元测试 | ✅ 全部通过 |
| MarkdownSyncServiceTest | 7 | 单元测试 | ✅ 全部通过 |
| WorkflowServiceTest | 9 | 单元测试 | ✅ 全部通过 |
| AuthIntegrationTest | 8 | 集成测试(MockMvc+H2) | ✅ 全部通过 |
| AgentControllerTest | 5 | 集成测试(MockMvc) | ✅ 全部通过 |
| WorkflowControllerTest | 9 | 集成测试(MockMvc) | ✅ 全部通过 |
| AuthControllerTest | 7 | 集成测试(MockMvc) | ✅ 全部通过 |
| **合计** | **69** | — | **69/69 通过** |

#### 前端测试（34个全部通过）

| 测试文件 | 数量 | 测试对象 | 状态 |
|----------|------|---------|------|
| storage.test.ts | 7 | localStorage工具函数 | ✅ |
| theme.test.ts | 6 | Pinia主题Store | ✅ |
| auth.test.ts | 8 | Pinia认证Store | ✅ |
| AgentCard.test.ts | 8 | AgentCard组件 | ✅ |
| chat.test.ts | 1 | Pinia聊天Store错误展示 | ✅ |
| workflowMapper.test.ts | 4 | 工作流节点保存/加载映射 | ✅ |
| **合计** | **34** | — | **34/34 通过** |

### 阶段六：运行时引擎 ✅

#### Plan 1: LLM Gateway

| 类别 | 文件数 | 说明 |
|------|--------|------|
| DTO | 5 | LlmRequest, LlmResponse, LlmMessage, TokenUsage, TestConnectionResult |
| Provider | 3 | LlmProviderInterface, OpenAICompatibleProvider, ClaudeProvider |
| 实体/Mapper | 4 | LlmProviderEntity, LlmProviderMapper, UserLlmProviderEntity, UserLlmProviderMapper |
| Service | 4 | LlmProviderService, AesEncryptService, LlmGateway, UserLlmProviderService |
| Controller | 2 | LlmController (供应商管理+测试连通), UserLlmController (用户级供应商+测试连通) |
| Config | 1 | LlmConfig |
| VO/Request | 4 | LlmProviderVO, LlmProviderCreateRequest, UserLlmProviderVO, UserLlmProviderRequest, AvailableModelVO |
| 测试 | 5 | Gateway/Provider/Service/AES/TestConnection 各1 |

**支持的供应商**：OpenAI兼容(DeepSeek/Doubao等)、Anthropic兼容(Kimi Coding/Claude/智谱GLM等)

#### Plan 2: Execution Engine

| 任务 | 文件 | 测试 |
|------|------|------|
| Task 1: ExecutionContext + VariableStore | 2源文件 | 5测试 |
| Task 2: ExecutionStrategy + DefaultChatStrategy | 2源文件 | 1测试 |
| Task 3: AgentRouter + RoutingResult | 2源文件 | 3测试 |
| Task 4: StepActionHandler + LlmCallHandler | 3源文件 | 2测试 |
| Task 5: SkillExecutor | 1源文件 | 2测试 |
| Task 6: AgentRoutingStrategy + WorkflowStrategy | 2源文件 | 编译验证 |
| Task 7: OrchestrationEngine | 1源文件 | 3测试 |

**合计：13源文件 + 6测试文件，16个单元测试**

#### Plan 3: Chat 后端模块

| 类别 | 文件数 | 说明 |
|------|--------|------|
| Entity | 3 | ChatSession, ChatMessage, UserMemory |
| Mapper | 3 | ChatSessionMapper, ChatMessageMapper, UserMemoryMapper |
| DTO | 3 | ChatRequest, ChatResponse, SessionVO |
| Service | 2 | ChatSessionService, MemoryService |
| Controller | 1 | ChatController (REST 7端点) |
| WebSocket | 3 | WebSocketConfig, WebSocketAuthInterceptor, ChatWebSocketHandler |
| 测试 | 3 | ChatSessionServiceTest(11), MemoryServiceTest(5), ChatControllerTest(4) |

**核心流程：** `ChatController → ChatSessionService → OrchestrationEngine → LlmGateway → MemoryService(异步)`

#### Plan 4: 前端聊天界面

| 文件 | 说明 |
|------|------|
| api/chat.ts | 会话 CRUD + 发消息 API（8个接口） |
| api/execution.ts | 执行日志查询 |
| stores/chat.ts | Pinia store（会话列表、消息、发送状态、流式内容、WS连接） |
| components/chat/SessionList.vue | 会话列表（搜索 + 时间格式化） |
| components/chat/MessageBubble.vue | 消息气泡（USER/ASSISTANT区分） |
| components/chat/ChatInput.vue | 输入框 + 模式标签（Agent/Workflow） |
| components/chat/ExecutionDetail.vue | 执行步骤折叠区 |
| components/chat/ModelSelector.vue | 模型选择下拉（按供应商分组） |
| views/ChatView.vue | 主聊天页面（左右分栏260px，流式渲染+光标动画） |
| Sidebar.vue | 添加"智能对话"导航入口 |
| router/index.ts | `/chat` + `/chat/:sessionId` 路由 |

#### Plan 5: 流式输出

**后端改动：**

| 文件 | 改动 |
|------|------|
| ExecutionStrategy | 添加 `stream()` default 方法，返回 `Flux<String>` |
| DefaultChatStrategy | 覆写 `stream()`，调用 `LlmGateway.chatStream()` |
| OrchestrationEngine | 添加 `executeStream()`，按 mode 分发流式策略 |
| ChatSessionService | 添加 `sendMessageStream()`，Flux 返回 token，`doOnComplete` 持久化 |
| ChatWebSocketHandler | 订阅 Flux，逐 token 推送 CHUNK，完成推送 MESSAGE_COMPLETE |

**前端改动：**

| 文件 | 改动 |
|------|------|
| stores/chat.ts | 添加 WS 连接管理（connectWS/disconnectWS）、streamingContent ref、sendWSMessage()、REST fallback |
| ChatView.vue | onMount 连接 WS、流式消息渲染（光标动画）、WS 状态指示（WS/REST标签）、WS 断线自动降级 REST |

**流式协议：** `ACK → CHUNK(content) → CHUNK(content) → ... → MESSAGE_COMPLETE`

#### LLM供应商管理 + 测试连通

| 改动 | 文件 | 说明 |
|------|------|------|
| 后端 | LlmProviderService | `testConnection(id)` + `testConnection(entity, key)` 发短消息验证连通性 |
| 后端 | TestConnectionResult DTO | `success/message/model/durationMs` 结构化响应 |
| 后端 | LlmController | `POST /api/llm/providers/{id}/test` 真实测试（替换空壳） |
| 后端 | UserLlmController | `POST /api/llm/user/providers/{id}/test` 用户级测试（合并全局+用户覆盖） |
| 后端 | LlmConfig / LlmGateway | 新增 `protocol` 协议字段，供应商名称与调用协议解耦 |
| 后端 | ClaudeProvider | 支持保留真实供应商名称，Anthropic 协议不再强制 `name=claude` |
| 前端 | llm.ts | `testProviderConnection` + `testUserProviderConnection` API |
| 前端 | ProviderManageTab.vue | 表格"测试"按钮 + 弹窗"测试连通"按钮 + 协议选择 |
| 前端 | UserProviderTab.vue | 卡片"测试连通"按钮 + 弹窗"测试连通"按钮 |
| 测试 | TestConnectionTest | 6个测试（成功/404/无模型/无端点/API错误/直传entity） |
| 测试 | LlmConfigTest / ClaudeProviderTest | 覆盖协议选择与 Kimi/Claude SSE chunk 解析 |

#### 系统设置 + 认证超时

| 改动 | 文件/接口 | 说明 |
|------|-----------|------|
| 后端 | system_settings 表 | 新增系统设置存储，当前支持 `auth.access_token_expiration_ms` |
| 后端 | SystemSettingController | `GET/PUT /api/system/settings/auth`，仅 admin 权限可访问 |
| 后端 | JwtUtil / AuthService | Access Token 有效期从系统设置读取，重新登录后生效 |
| 前端 | AuthSettingsTab.vue | 设置页新增"认证设置"，管理员可配置登录超时时间 |
| 前端 | request.ts | 任何非登录接口返回 401 时清除本地登录态并跳转 `/login?redirect=...` |
| 测试 | SystemSettingServiceTest / AuthServiceTest | 覆盖系统设置读取、回退、更新及登录返回 `expiresIn` |

### 阶段八：多智能体与工作流自由组合 🟨

> 当前分支：`codex/plan8-runtime-composition`  
> 当前状态：计划8阶段一“组合协议层最小闭环”、阶段二“画布支持引用节点”、阶段三“Python sidecar + LangGraph 最小闭环”、`CONDITION` 表达式分支、`ROUTER` 规则路由和 `PARALLEL/JOIN` 最小闭环已完成，尚未提交。  
> 目标路线：Java 作为平台控制面和执行网关，Python LangGraph sidecar 后续作为真实复杂图执行面接入。

#### Plan 8 阶段一：组合协议层最小闭环

| 类别 | 文件/能力 | 状态 | 说明 |
|------|-----------|------|------|
| Runtime DTO | `AgentDefinition`、`WorkflowDefinition`、`RuntimeNode`、`RuntimeEdge`、`ExecutionRequest`、`ExecutionEvent`、`ExecutionResult` | ✅ | 定义 Java 与 sidecar 的标准执行协议 |
| 节点类型 | `RuntimeNodeType` | ✅ | 支持 `START`、`END`、`SKILL`、`AGENT_REF`、`WORKFLOW_REF`，并预留高级节点类型 |
| 工作流转换 | `WorkflowDefinitionAssembler` | ✅ | 将现有 WorkflowDetail nodes/edges 转换为标准 `WorkflowDefinition` |
| 执行日志扩展 | `ExecutionLog` + `V12__extend_execution_logs_for_runtime.sql` | ✅ | 增加 `parentExecutionId`、`rootExecutionId`、`externalThreadId`、`checkpointRef`、`traceId` 等字段 |
| Runtime 网关 | `RuntimeGateway`、`MockRuntimeGateway` | ✅ | 默认 mock sidecar，输出稳定 `ExecutionEvent` 流 |
| Runtime 执行服务 | `RuntimeExecutionService` | ✅ | 串起工作流读取、协议转换、执行日志、事件保存和 mock gateway |
| Sidecar 骨架 | `SidecarRuntimeGateway`、`RuntimeGatewayProperties` | ✅ | 配置已预留，默认不启用真实 sidecar |
| 配置 | `runtime.gateway.mode`、`runtime.gateway.sidecar-base-url` | ✅ | 默认 `mock`，可通过环境变量切换 |
| 前端类型 | `frontend/src/types/workflow.ts` | ✅ | 预留 `agent_ref`、`workflow_ref`、`router`、`condition` 等节点类型 |
| H2 测试 schema | `schema-h2.sql` | ✅ | 同步 `llm_providers.protocol`、`system_settings` 和执行日志扩展字段 |

#### 阶段一新增测试

| 测试类 | 类型 | 覆盖内容 | 状态 |
|--------|------|----------|------|
| `RuntimeDtoTest` | 单元测试 | Runtime DTO 的 Jackson 序列化/反序列化 | ✅ |
| `WorkflowDefinitionAssemblerTest` | 单元测试 | 基础节点转换、Agent/Workflow 引用 config、非法 JSON 配置 | ✅ |
| `ExecutionLogServiceTest` | 单元测试 | root/child execution、runtime refs、事件快照追加 | ✅ |
| `MockRuntimeGatewayTest` | 单元测试 | mock 事件顺序、metadata、traceId、externalThreadId | ✅ |
| `RuntimeExecutionServiceTest` | 单元测试 | 执行请求生成、事件消费、日志更新、完成状态 | ✅ |

#### Plan 8 阶段二：画布支持 Agent / Workflow 引用节点

| 类别 | 文件/能力 | 状态 | 说明 |
|------|-----------|------|------|
| 前端节点类型 | `frontend/src/types/workflow.ts` | ✅ | `WorkflowNodeType`、`NodeData`、`WorkflowNodeRequest` 支持 `agent_ref` / `workflow_ref` |
| 保存/加载映射 | `frontend/src/utils/workflowMapper.ts` | ✅ | VueFlow 节点与后端节点请求双向转换，引用信息写入 `config` |
| Agent 引用节点 | `AgentRefNode.vue` | ✅ | 画布中展示被引用 Agent 的名称与 ID |
| Workflow 引用节点 | `WorkflowRefNode.vue` | ✅ | 画布中展示被引用 Workflow 的名称与 ID |
| 画布注册 | `FlowCanvas.vue` | ✅ | 注册 `agent_ref`、`workflow_ref` 节点，拖拽时保留引用数据 |
| 组件面板 | `ComponentPalette.vue` | ✅ | 新增“Agent 引用”和“Workflow 引用”拖拽区 |
| 属性面板 | `PropertyPanel.vue` | ✅ | 支持选择已有 Agent / Workflow，并同步 label 与引用 ID |
| 编辑器保存回显 | `WorkflowEditorView.vue` | ✅ | 保存时序列化引用配置，加载时从 `config` 回填节点数据 |
| 后端引用校验 | `WorkflowDefinitionAssembler` | ✅ | `AGENT_REF` 必须有 `agentId`，`WORKFLOW_REF` 必须有 `workflowId` |
| 前端构建修复 | AppHeader / ThemeSwitcher / WorkflowCard 等 | ✅ | 清理阶段二触发的 TypeScript 构建阻断 |

#### 阶段二新增测试

| 测试类 | 类型 | 覆盖内容 | 状态 |
|--------|------|----------|------|
| `workflowMapper.test.ts` | 前端单元测试 | `agent_ref`、`workflow_ref`、`skill` 节点保存映射与 config 回填 | ✅ |
| `WorkflowDefinitionAssemblerTest` | 后端单元测试 | Agent/Workflow 引用节点缺少必填 ID 时失败 | ✅ |

#### Plan 8 阶段三：Python Runtime Sidecar HTTP 最小闭环

| 类别 | 文件/能力 | 状态 | 说明 |
|------|-----------|------|------|
| sidecar 项目 | `runtime-sidecar/` | ✅ | 新增 FastAPI + LangGraph sidecar 项目骨架 |
| 健康检查 | `GET /health` | ✅ | 返回 `runtime-sidecar` 服务状态 |
| 执行接口 | `POST /api/runtime/executions` | ✅ | 接收 Java `ExecutionRequest` 协议 |
| SSE 事件流 | `EventSourceResponse` | ✅ | `runtime.stream=true` 时返回 `text/event-stream` |
| 非流式结果 | `ExecutionResult` | ✅ | `runtime.stream=false` 时返回完整事件列表 |
| LangGraph 编译器 | `graph_compiler.py` | ✅ | 将 `WorkflowDefinition` 编译为 LangGraph `StateGraph` |
| Runtime 执行器 | `runtime_executor.py` | ✅ | 调用 LangGraph 图并输出标准节点 started/completed 事件 |
| 条件分支 | `CONDITION` + `add_conditional_edges` | ✅ | 支持等值分支、比较表达式、contains、exists 和 `defaultTarget` |
| 规则路由 | `ROUTER` + `node.config.routes` | ✅ | 根据规则条件选择目标分支，支持 `defaultTarget` |
| 并行聚合 | `PARALLEL` / `JOIN` | ✅ | 支持多分支 fan-out，JOIN 等待入边分支完成并汇总输出 |
| 引用节点识别 | `AGENT_REF` / `WORKFLOW_REF` | ✅ | 事件中填充 `sourceType` 和 `sourceId` |
| Java sidecar 网关 | `SidecarRuntimeGateway` | ✅ | 使用 `WebClient` 调用 sidecar 并解析 SSE JSON 事件 |
| sidecar 文档 | `runtime-sidecar/README.md` | ✅ | 记录启动、测试、API 和当前范围 |

#### 阶段三新增测试

| 测试类 | 类型 | 覆盖内容 | 状态 |
|--------|------|----------|------|
| `runtime-sidecar/tests/test_runtime_api.py` | Python API 测试 | `/health`、非流式执行、SSE 执行、空节点校验、条件分支 API | ✅ |
| `runtime-sidecar/tests/test_graph_compiler.py` | Python LangGraph 测试 | 按边关系执行、Workflow 引用节点、循环图拒绝、条件分支/defaultTarget | ✅ |
| `SidecarRuntimeGatewayTest` | Java 单元测试 | POST 请求路径/请求体、SSE 解析、sidecar HTTP 错误传播 | ✅ |

#### 阶段三最新验证结果

| 验证项 | 命令 | 结果 |
|--------|------|------|
| Python sidecar 测试 | `cd agent-platform/runtime-sidecar && python -m pytest -q` | ✅ 18 个测试通过 |
| Java sidecar 网关测试 | `cd agent-platform/backend && mvn -q "-Dtest=SidecarRuntimeGatewayTest" test` | ✅ 通过 |
| 后端完整测试 | `cd agent-platform/backend && mvn -q test` | ✅ 通过 |
| 前端完整测试 | `cd agent-platform/frontend && npm test -- --run` | ✅ 34 个测试通过 |
| 前端构建 | `cd agent-platform/frontend && npm run build` | ✅ 通过，仅保留 Vite chunk / 动态导入警告 |
| 阶段一后端关键测试 | `mvn -q "-Dtest=RuntimeDtoTest,WorkflowDefinitionAssemblerTest,ExecutionLogServiceTest,MockRuntimeGatewayTest,RuntimeExecutionServiceTest" test` | ✅ 通过 |
| 2026-06-04 阶段一至三后端关键测试 | `cd agent-platform/backend && mvn -q "-Dtest=RuntimeDtoTest,WorkflowDefinitionAssemblerTest,ExecutionLogServiceTest,MockRuntimeGatewayTest,RuntimeExecutionServiceTest,SidecarRuntimeGatewayTest" test` | ✅ 通过 |
| 2026-06-04 Python sidecar 测试 | `cd agent-platform/runtime-sidecar && python -m pytest -q` | ✅ 18 个测试通过，保留 1 个第三方 PendingDeprecationWarning |
| 2026-06-04 阶段四 checkpoint metadata 最小闭环测试 | `cd agent-platform/runtime-sidecar && python -m pytest -q` | ✅ 19 个测试通过，sidecar 在 `runtime.checkpoint=true` 时输出稳定 `checkpointRef` |
| 2026-06-04 Java runtime checkpoint 消费链路测试 | `cd agent-platform/backend && mvn -q "-Dtest=RuntimeExecutionServiceTest,SidecarRuntimeGatewayTest" test` | ✅ 通过 |
| 2026-06-04 Java checkpoint 引用持久化保护测试 | `cd agent-platform/backend && mvn -q "-Dtest=ExecutionLogServiceTest" test` | ✅ 通过；`updateRuntimeRefs` 部分更新时不再清空已有 `checkpointRef` |
| 2026-06-04 Java checkpoint 查询入口测试 | `cd agent-platform/backend && mvn -q "-Dtest=ExecutionLogServiceTest" test` | ✅ 通过；新增 `findByCheckpointRef` 作为 resume/retry 查询基础 |
| 2026-06-04 Java workflow retry 最小入口测试 | `cd agent-platform/backend && mvn -q "-Dtest=RuntimeExecutionServiceTest" test` | ✅ 通过；新增 `retryWorkflowExecution`，基于旧执行记录 workflowId 创建新 root execution 并重跑 |
| 2026-06-04 Java resume 协议入口测试 | `cd agent-platform/backend && mvn -q "-Dtest=RuntimeExecutionServiceTest" test` | ✅ 通过；新增 `resumeWorkflowFromCheckpoint`，按 `checkpointRef` 查询旧执行并构造 `resumeFromCheckpointRef` runtime 请求 |
| 2026-06-04 执行 retry/resume REST 入口测试 | `cd agent-platform/backend && mvn -q "-Dtest=ExecutionControllerTest" test` | ✅ 通过；新增 `POST /api/executions/{id}/retry` 和 `POST /api/executions/resume?checkpointRef=...` |
| 2026-06-04 阶段四后端组合测试 | `cd agent-platform/backend && mvn -q "-Dtest=ExecutionLogServiceTest,RuntimeExecutionServiceTest,SidecarRuntimeGatewayTest" test` | ✅ 通过 |
| 2026-06-04 阶段四后端组合测试（含 REST） | `cd agent-platform/backend && mvn -q "-Dtest=ExecutionControllerTest,ExecutionLogServiceTest,RuntimeExecutionServiceTest,SidecarRuntimeGatewayTest" test` | ✅ 通过 |
| 2026-06-04 阶段四执行恢复信息 API 测试 | `cd agent-platform/backend && mvn -q "-Dtest=ExecutionLogServiceTest#getRecoveryInfo_extractsFailureMetadataFromRuntimeEvents,ExecutionLogServiceTest#getRecoveryInfo_marksFailedExecutionWithoutCheckpointAsNotRecoverable,ExecutionControllerTest#getRecoveryInfo_returnsRecoverableFailureDetails" test` | ✅ 通过；新增 `GET /api/executions/{id}/recovery`，返回失败节点、错误信息、checkpoint、可恢复状态和恢复建议 |
| 2026-06-04 阶段四人工审批最小闭环测试 | `cd agent-platform/backend && mvn -q "-Dtest=RuntimeExecutionServiceTest#runWorkflow_persistsWaitingApprovalMetadata,RuntimeExecutionServiceTest#approveWorkflowExecution_resumesFromApprovalCheckpoint,ExecutionControllerTest#approveExecution_returnsRuntimeEvents,ExecutionControllerTest#rejectExecution_marksExecutionRejected" test` + `cd agent-platform/runtime-sidecar && python -m pytest -q` | ✅ 通过；sidecar `HUMAN_APPROVAL` 输出 `WAITING_APPROVAL`，Java 持久化等待状态并暴露 approve/reject REST 入口；sidecar 30 个测试通过 |
| 2026-06-04 阶段五高级控制流与治理测试 | `cd agent-platform/runtime-sidecar && python -m pytest -q` + `cd agent-platform/backend && mvn -q "-Dtest=WorkflowDefinitionAssemblerTest" test` | ✅ 通过；sidecar 33 个测试通过，新增结构化 `all/any/not` CONDITION、`LLM_ROUTER` mock 路由、PARALLEL `branchFailurePolicy=continue`；Java 拒绝 WORKFLOW_REF 直接自引用 |
| 2026-06-04 阶段六前端执行监控最小入口测试 | `cd agent-platform/frontend && npm test -- --run` + `cd agent-platform/frontend && npm run build` | ✅ 通过；前端 7 个测试文件、35 个测试通过；新增 `/executions` 执行监控页面，可查询恢复信息并触发 retry/resume/approve/reject |
| 2026-06-04 阶段六工作流画布运行态测试 | `cd agent-platform/frontend && npm test -- --run` + `cd agent-platform/frontend && npm run build` | ✅ 通过；前端 8 个测试文件、37 个测试通过；新增 runtime event 到画布节点状态映射，支持 running/success/failed/waiting 和 CONDITION/ROUTER 选中分支 metadata |
| 2026-06-04 阶段六 runtime event timeline 测试 | `cd agent-platform/frontend && npm test -- --run` + `cd agent-platform/frontend && npm run build` | ✅ 通过；前端 9 个测试文件、39 个测试通过；`/executions` 页面可解析 `ExecutionLog.nodeLogs` 并展示事件类型、节点、checkpoint、选中分支、失败节点和审批 key |
| 2026-06-04 阶段六待审批任务列表测试 | `cd agent-platform/backend && mvn -q "-Dtest=ExecutionLogServiceTest#listPendingApprovals_returnsWaitingApprovalExecutions,ExecutionControllerTest#listPendingApprovals_returnsWaitingExecutions" test` + `cd agent-platform/frontend && npm test -- --run` + `cd agent-platform/frontend && npm run build` | ✅ 通过；新增 `GET /api/executions/pending-approvals`，`/executions` 页面展示待审批执行列表，点击后加载恢复详情与时间线 |
| 2026-06-04 阶段六工作流编辑器执行接入测试 | `cd agent-platform/backend && mvn -q "-Dtest=WorkflowControllerTest#runWorkflow_returnsRuntimeEvents" test` + `cd agent-platform/frontend && npm test -- --run` + `cd agent-platform/frontend && npm run build` | ✅ 通过；新增 `POST /api/workflows/{id}/run`，工作流编辑器“运行”按钮会调用执行接口并把返回 runtime events 传给 `FlowCanvas` 高亮节点运行态 |
| 2026-06-04 阶段四 sidecar 回归测试 | `cd agent-platform/runtime-sidecar && python -m pytest -q` | ✅ 20 个测试通过，保留 1 个第三方 PendingDeprecationWarning；sidecar 透传 `resumeFromCheckpointRef` |
| 2026-06-04 sidecar checkpoint store 测试 | `cd agent-platform/runtime-sidecar && python -m pytest -q tests/test_checkpoint_store.py` | ✅ 2 个测试通过；新增内存 checkpoint store |
| 2026-06-04 sidecar durable resume 最小闭环测试 | `cd agent-platform/runtime-sidecar && python -m pytest -q` | ✅ 23 个测试通过；执行完成保存 checkpoint state，resume 请求命中时输出 `checkpointRestored=true` |
| 2026-06-04 sidecar 文件 checkpoint store 测试 | `cd agent-platform/runtime-sidecar && python -m pytest -q tests/test_checkpoint_store.py` | ✅ 4 个测试通过；新增 `FileCheckpointStore` 与 `RUNTIME_CHECKPOINT_DIR` 配置 |
| 2026-06-04 sidecar 持久化 checkpoint 回归测试 | `cd agent-platform/runtime-sidecar && python -m pytest -q` | ✅ 26 个测试通过；文件 store 可跨实例读取 checkpoint state |
| 2026-06-04 sidecar resume 执行语义测试 | `cd agent-platform/runtime-sidecar && python -m pytest -q` | ✅ 27 个测试通过；resume 使用旧 checkpoint state 初始化执行上下文，并只上报 checkpoint 后新增执行节点 |
| 2026-06-04 sidecar checkpointer 边界测试 | `cd agent-platform/runtime-sidecar && python -m pytest -q` | ✅ 28 个测试通过；新增 `CheckpointStore` Protocol，执行器依赖抽象 save/load 边界 |
| 2026-06-04 sidecar 失败恢复最小闭环测试 | `cd agent-platform/runtime-sidecar && python -m pytest -q` | ✅ 29 个测试通过；`config.fail=true` 节点返回 `EXECUTION_FAILED`，metadata 包含 `failedNodeId` 和 `recoveryCheckpointRef` |
| 2026-06-04 前端完整测试 | `cd agent-platform/frontend && npm test -- --run` | ✅ 6 个测试文件、34 个测试通过 |
| 2026-06-04 前端构建 | `cd agent-platform/frontend && npm run build` | ✅ 通过，保留 Vite 动态导入与 chunk size 警告 |
| 2026-06-04 后端完整测试 | `cd agent-platform/backend && mvn -q test` | ✅ 通过；日志中的 WARN/ERROR 为测试用例覆盖失败路径 |
| 2026-06-05 阶段七 Runtime callback 协议测试 | `cd agent-platform/backend && mvn -q "-Dtest=RuntimeDtoTest,RuntimeCallbackControllerTest" test` + `cd agent-platform/runtime-sidecar && python -m pytest -q tests/test_callback_client.py` | ✅ 通过；Java callback DTO/Controller 与 sidecar callback client 协议对齐 |
| 2026-06-05 阶段七 SKILL/AGENT_REF 真实执行与权限测试 | `cd agent-platform/backend && mvn -q "-Dtest=RuntimeNodeCallbackServiceTest" test` + `cd agent-platform/runtime-sidecar && python -m pytest -q tests/test_runtime_executor.py::test_build_events_uses_java_callback_output_for_skill_nodes tests/test_runtime_executor.py::test_build_events_uses_java_callback_output_for_agent_ref_nodes` | ✅ 通过；覆盖 `SKILL` 调用 `SkillExecutor`、`AGENT_REF` 调用 `AgentRouter` + `SkillExecutor`、未发布 Agent 访问拒绝和创建者访问放行 |
| 2026-06-05 阶段七执行日志与前端展示测试 | `cd agent-platform/backend && mvn -q "-Dtest=RuntimeExecutionServiceTest#runWorkflow_persistsNodeOutputInRuntimeEventSnapshot" test` + `cd agent-platform/frontend && npm test -- --run src/utils/executionTimeline.test.ts` | ✅ 通过；runtime event snapshot 保存 `input/output`，执行监控页 timeline 可解析 Agent、技能和步骤输出 |
| 2026-06-05 阶段七稳定化组合测试 | `cd agent-platform/backend && mvn -q "-Dtest=SkillExecutorTest,AgentRouterTest,RuntimeDtoTest,WorkflowDefinitionAssemblerTest,SidecarRuntimeGatewayTest,RuntimeExecutionServiceTest,RuntimeNodeCallbackServiceTest,RuntimeCallbackControllerTest" test` + `cd agent-platform/runtime-sidecar && python -m pytest -q` + `cd agent-platform/frontend && npm test -- --run` + `cd agent-platform/frontend && npm run build` | ✅ 通过；sidecar 37 个测试通过，前端 44 个测试通过，前端构建通过但保留既有 Vite 动态导入和 chunk size 警告 |
| 2026-06-05 sidecar callback 错误传播测试 | `cd agent-platform/runtime-sidecar && python -m pytest -q tests/test_runtime_executor.py::test_build_events_converts_java_callback_failure_to_execution_failed` + `cd agent-platform/runtime-sidecar && python -m pytest -q` | ✅ 通过；Java callback 异常会转换为 `EXECUTION_FAILED`，metadata 包含 `failedNodeId`、`errorMessage` 和 `recoveryCheckpointRef`；sidecar 38 个测试通过 |
| 2026-06-05 Java/sidecar 配置项默认值测试 | `cd agent-platform/backend && mvn -q "-Dtest=RuntimeExecutionServiceTest#runWorkflow_buildsExecutionRequestAndPersistsEvents" test` | ✅ 通过；`application.yml` 已暴露 `RUNTIME_GATEWAY_MODE`、`RUNTIME_SIDECAR_BASE_URL`、`RUNTIME_JAVA_CALLBACK_BASE_URL`，Java 运行请求会注入 `runtime.javaCallbackBaseUrl` |
| 2026-06-05 Runtime Sidecar 部署配置说明 | `项目工程文档/项目功能介绍/项目相关配置说明/Runtime Sidecar部署配置说明.md` | ✅ 已新增；覆盖 Java/sidecar 端口、runtime gateway 配置、checkpoint 目录、health/readiness、日志、超时、并发、联调验收命令和核心联调链路 |
| 2026-06-05 阶段八自动化测试增强 | `cd agent-platform/runtime-sidecar && python -m pytest -q tests/test_callback_client.py` + `cd agent-platform/runtime-sidecar && python -m pytest -q` + `cd agent-platform/frontend && npm test -- --run src/utils/executionTimeline.test.ts` | ✅ 通过；callback client 覆盖非 2xx 与超时错误包装，execution timeline 覆盖脏数据容错；sidecar 40 个测试通过 |
| 2026-06-05 本地 mock 链路验证 | `cd agent-platform/backend && mvn -q "-Dtest=MockRuntimeGatewayTest,RuntimeExecutionServiceTest#runWorkflow_buildsExecutionRequestAndPersistsEvents" test` + `cd agent-platform/runtime-sidecar && python -m pytest -q tests/test_runtime_api.py tests/test_runtime_executor.py::test_build_events_uses_java_callback_output_for_skill_nodes tests/test_runtime_executor.py::test_build_events_converts_java_callback_failure_to_execution_failed` + `cd agent-platform/frontend && npm test -- --run src/utils/executionTimeline.test.ts src/utils/workflowRuntime.test.ts` | ✅ 通过；不依赖真实 LLM/Agent 数据，验证后端 mock gateway、sidecar 默认无 callback 路径与 callback 失败事件、前端 execution timeline/runtime 映射 |
| 阶段二前端关键测试 | `npm test -- --run src/utils/workflowMapper.test.ts` | ✅ 通过 |

#### 计划8测试要求

每次继续开发计划8后，至少执行以下测试：

1. 后端局部测试：修改 runtime、execution、workflow 转换相关代码后，必须运行阶段一后端关键测试。
2. 后端完整测试：合并或提交前必须运行 `mvn -q test`。
3. 前端测试：修改 `frontend/src/types/workflow.ts`、工作流画布、组件面板、属性面板或保存映射后，必须运行 `npm test -- --run`。
4. 数据库迁移检查：新增 Flyway 迁移后，必须同步 `backend/src/test/resources/schema-h2.sql`，确保 H2 测试环境不漂移。
5. Runtime 协议测试：新增字段或节点类型时，必须补充 DTO 序列化测试和 `WorkflowDefinitionAssemblerTest`。
6. 执行日志测试：改动执行日志字段、状态、事件快照或 checkpoint 引用时，必须补充 `ExecutionLogServiceTest` 或 `RuntimeExecutionServiceTest`。
7. 画布手工测试：创建工作流，拖入 Agent 引用和 Workflow 引用节点，保存后刷新页面，确认节点类型、名称和引用 ID 不丢失。
8. sidecar 测试：修改 `runtime-sidecar` 或 `SidecarRuntimeGateway` 后，必须运行 `python -m pytest -q` 和 `mvn -q "-Dtest=SidecarRuntimeGatewayTest" test`。

#### 计划8下一步

| 阶段 | 状态 | 下一步内容 |
|------|------|------------|
| 阶段一：组合协议层 | ✅ 完成 | 可作为后续 sidecar 接入边界 |
| 阶段二：画布支持引用节点 | ✅ 完成 | `AGENT_REF`、`WORKFLOW_REF` 已支持拖拽、配置、保存和回显 |
| 阶段三：Python Runtime Sidecar | ✅ PARALLEL/JOIN 最小闭环完成 | 已完成 FastAPI HTTP/SSE sidecar、LangGraph StateGraph 编译、条件表达式、规则路由、并行聚合与 Java HTTP 网关 |
| 阶段四：持久化执行与恢复 | ✅ 最小闭环完成 | sidecar 已输出稳定 `checkpointRef`、透传 `resumeFromCheckpointRef`，支持内存/文件 checkpoint store 保存读取 graph state，并在 resume 时使用旧 checkpoint state 初始化执行上下文、只上报 checkpoint 后新增执行节点；执行器已依赖 `CheckpointStore` Protocol，可替换为 LangGraph 正式 checkpointer；`config.fail=true` 节点可返回 `EXECUTION_FAILED` 和恢复 checkpoint；Java 已支持 checkpoint 引用持久化保护、`findByCheckpointRef` 查询、`retryWorkflowExecution`、`resumeWorkflowFromCheckpoint`、失败事件持久化、`GET /api/executions/{id}/recovery` 恢复信息查询；`HUMAN_APPROVAL` 可输出 `WAITING_APPROVAL`，Java 持久化等待状态并暴露 approve/reject REST 入口 |
| 阶段五：高级控制流与治理 | 🟡 最小闭环完成 | 已支持结构化 `all/any/not` CONDITION、`LLM_ROUTER` mock 路由输出 `selectedRoute/selectedTarget/reason/confidence`、PARALLEL `branchFailurePolicy=continue` 分支失败汇总、WORKFLOW_REF 直接自引用拒绝；后续可继续增强真实 LLM Router、跨工作流递归检测、执行治理和跨对象权限校验 |
| 阶段六：前端运行时体验 | ✅ 核心闭环完成 | 已新增执行监控入口、待审批列表、恢复信息、事件时间线、工作流编辑器运行按钮、画布运行态高亮、`INPUT` 输入节点和开始节点运行入参收集 |
| 阶段七：Agent / Skill 真实执行接入 | ✅ 核心闭环完成 | 已完成 Runtime callback 协议、`SKILL` 真实执行、`AGENT_REF` 真实执行、执行日志与前端展示、执行前权限校验，以及后端/sidecar/前端稳定化验证 |
| 阶段八：Agent 对象级权限 | ✅ 核心闭环完成 | 已完成 Agent 授权表、访问能力解析、批量授权、对象级列表/详情/聊天目标可见性、复制编辑、提示词配置隐藏；后续补跨工作流权限和更细后台写操作边界 |

#### 计划8后续完整路线图

##### 阶段四：持久化执行与恢复

1. 完成 resume 执行语义：✅ 已完成最小闭环。resume 请求会基于旧 checkpoint state 初始化执行上下文，并只上报 checkpoint 后新增执行节点；后续需接入 LangGraph 正式 checkpointer 以获得更完整的 durable execution 语义。
2. 接入/抽象 LangGraph checkpointer 边界：✅ 已完成抽象边界。sidecar 新增 `CheckpointStore` Protocol，当前 `FileCheckpointStore` 作为默认实现，后续可替换为 LangGraph 正式 checkpointer。
3. 执行失败恢复：✅ Java/sidecar 最小闭环已完成。`config.fail=true` 节点会返回 `EXECUTION_FAILED`，并在 metadata 中记录 `failedNodeId`、`errorMessage` 和 `recoveryCheckpointRef`；Java 会持久化恢复 checkpoint 和可读失败消息，并可通过恢复 checkpoint 发起 resume。
4. 人工审批等待节点：✅ 已完成最小闭环。`HUMAN_APPROVAL` 节点会让 sidecar 输出 `WAITING_APPROVAL` 和 `approvalCheckpointRef`；Java 执行日志记录等待状态；`POST /api/executions/{id}/approve` 会从审批 checkpoint 继续执行，`POST /api/executions/{id}/reject` 会标记执行失败。
5. 执行状态 API 完善：✅ 已新增 `GET /api/executions/{id}/recovery`，可查询 checkpoint 信息、可恢复状态、失败节点、错误信息和恢复建议；后续可继续增强 retry/resume API 的结构化响应。

##### 阶段五：高级控制流与治理

6. 复杂 CONDITION：✅ 已支持结构化 `all/any/not` 条件组合和多条件分组，继续避免开放任意表达式执行。
7. LLM Router：🟡 已在现有规则 `ROUTER` 基础上增加 `LLM_ROUTER` mock 模式，输出 `selectedRoute`、`selectedTarget`、`reason`、`confidence`，并校验 mock 选择目标必须存在；后续接入真实 LLM provider。
8. 并行治理：🟡 已支持 PARALLEL `branchFailurePolicy=continue`，分支失败可汇总到 JOIN；后续补分支超时、并发限制和更完整 JOIN 聚合策略配置。
9. 循环与引用安全：🟡 sidecar 已拒绝图内循环；Java 已拒绝 WORKFLOW_REF 直接自引用；后续补跨工作流递归检测、Agent/Workflow 跨对象权限校验、最大递归深度和最大节点数限制。
10. 执行治理与审计：支持 traceId 全链路查询、root/child execution 树查询、节点级耗时统计和 checkpoint/resume 审计事件。

##### 阶段六：前端运行时体验

11. 执行详情页增强：✅ 已新增 `/executions` 执行监控入口，支持按 executionId 查询恢复信息、checkpoint、失败节点和恢复建议，提供 retry/resume/approve/reject 操作按钮，并可解析 `ExecutionLog.nodeLogs` 展示 runtime event timeline。
12. 工作流画布运行态：✅ 已完成最小闭环。新增 `applyRuntimeEventsToNodes`，可把 runtime events 映射为画布节点 `running/success/failed/waiting` 状态，并保留 CONDITION/ROUTER/LLM_ROUTER 的 `selectedRoute/selectedTarget` metadata；`FlowCanvas` 支持接收 `runtimeEvents` 并用节点边框高亮运行态；工作流编辑器“运行”按钮已接入 `POST /api/workflows/{id}/run` 并把返回事件传给画布。
13. 人工审批 UI：✅ 已在执行监控页提供待审批任务列表、审批详情入口和 approve/reject 操作；点击待审批执行可加载恢复信息与 runtime event timeline。
14. 工作流运行输入组件：✅ 核心闭环已完成。组件面板已支持 `INPUT` 输入节点，覆盖文本、长文本/提示词、图片、视频、文件类型；属性面板可配置字段名、标签、类型、必填、默认值和输入值；开始节点也支持 `inputKey/value` 作为运行入参源；工作流运行时会通过 `collectWorkflowRunInput` 收集开始节点和 INPUT 节点输入并传入后端；sidecar 执行 `INPUT` 或带 `inputKey` 的 `START` 节点时，会从运行输入中取值并作为节点输出透传给后续 Skill/Agent。后端已新增 `POST /api/files/upload` 与 `GET /api/files/{fileId}` 文件上传/访问接口。后续增强：运行前弹出表单临时填写入参、文件类输入在编辑器运行入口自动上传并传递结构化文件引用。

##### 阶段七：Agent / Skill 真实执行接入

15. `SKILL` 节点真实执行：✅ 已完成。LangGraph sidecar 执行 `SKILL` 节点时，会通过 Java runtime callback 调用 `RuntimeNodeCallbackService`，由 Java 构造 `ExecutionContext` 并调用 `SkillExecutor.executeSkill(skillId, context)`；sidecar 会把 callback 返回的 `output`、`selectedSkillIds`、`stepOutputs` 写入 `NODE_COMPLETED.output`。
16. `AGENT_REF` 节点真实执行：✅ 已完成。LangGraph sidecar 执行 `AGENT_REF` 节点时，会通过 Java runtime callback 调用 Agent 执行接口；Java 查询目标 Agent 后由 `AgentRouter` 根据输入和 `routingRules` / LLM 路由选择 `skillIds`，再按顺序调用 `SkillExecutor` 执行能力链，并返回 `agentId`、`agentName`、`selectedSkillIds`、`stepOutputs` 和最终输出。
17. Runtime callback 协议：✅ 已完成。已新增 Java `RuntimeNodeCallbackRequest` / `RuntimeNodeCallbackResponse`、`RuntimeCallbackController`、`RuntimeNodeCallbackService` 占位执行边界，以及 sidecar `callback_client.py`；协议覆盖 `executionId`、`rootExecutionId`、`parentExecutionId`、`nodeId`、`sourceType`、`sourceId`、`userId`、`input`、`traceId`、`metadata`、`selectedSkillIds`、`stepOutputs`、`output`、`error`，并通过后端 DTO/Controller 测试和 sidecar callback client 测试。
18. 执行日志增强：✅ 已完成。`RuntimeExecutionService` 的 runtime event snapshot 已保存 `input` / `output`，`ExecutionEvent.output` 可记录 Agent 调用结果、`selectedSkillIds` 与 `stepOutputs`；前端 `parseExecutionTimeline` 和执行监控页已支持展示 Agent 名称、选中技能、节点输出和步骤输出列表。
19. 权限校验：✅ 已完成。新增 runtime callback 执行前校验：执行 `SKILL` 前会查询技能所属 Agent，执行 `AGENT_REF` 前会查询目标 Agent；仅允许执行 `PUBLISHED` Agent，或由 Agent 创建者执行自己的 `DRAFT/OFFLINE` Agent，为后续 `WORKFLOW_REF` 跨工作流权限校验预留同一授权边界。

##### 阶段八：测试与稳定化

20. 后端集成测试：🟡 阶段七相关稳定化已完成。已覆盖 runtime callback DTO/Controller、`RuntimeNodeCallbackService` 的 SKILL/AGENT_REF 真实执行、权限校验、`RuntimeExecutionService` 的 event output 持久化，以及 runtime/engine 组合回归；retry/resume/controller/checkpoint 仍沿用阶段四既有测试。
21. sidecar 集成测试：✅ 阶段七/八相关稳定化已完成。已覆盖 callback client、`SKILL` / `AGENT_REF` callback 输出合并、Java callback 错误传播、非 2xx/超时错误包装、checkpoint、HUMAN_APPROVAL、LLM_ROUTER mock、并行失败策略等 sidecar 全量测试。
22. 前端 E2E：🟡 单元与构建稳定化完成。已覆盖 execution timeline 对 Agent/Skill/步骤结果解析和脏数据容错，前端全量单测和构建通过；浏览器级 E2E 仍作为后续验收项。
23. 文档收口：✅ 已完成本轮收口。当前进度文档、计划8总文档、LangGraph+Agent+工作流运行说明、联调测试要求和 Runtime Sidecar 部署配置说明已同步阶段七/八最新状态、配置项、验证命令和待联调事项；后续真实环境验收时再补具体端口、账号、Agent/Skill 数据和手工验收结论。

##### 阶段九：部署上线准备

24. sidecar 部署配置：🟡 部署配置说明已完成。已新增 `Runtime Sidecar部署配置说明.md`，覆盖 checkpoint 存储目录、health/readiness、日志采集建议、超时、并发配置建议、Java callback 地址配置和联调验收命令；后续部署到真实环境时再按实际域名/IP补充。
25. Java/sidecar 联调配置：🟡 配置项默认值与说明已完成。`application.yml` 已支持 `runtime.gateway.mode`、`runtime.gateway.sidecar-base-url`、`runtime.gateway.java-callback-base-url` 及对应环境变量；`runtime-sidecar/README.md` 已补充 mock/sidecar 切换、sidecar 执行地址、Java callback 地址和 checkpoint 目录说明；后续继续补部署说明与手工联调步骤。
26. 最终验收：🟡 自动化与本地 mock 链路验证已完成；真实环境手工验收需等待用户提供 Agent/Skill/LLM Provider/账号/端口等联调数据后执行。

#### Flyway 迁移

| 版本 | 内容 | 状态 |
|------|------|------|
| V4 | chat_sessions, chat_messages | ✅ |
| V5 | execution_step_logs | ✅ |
| V6 | user_memories, llm_providers | ✅ |
| V7 | rename key→memory_key, value→memory_value (H2兼容) | ✅ |
| V8 | user_llm_providers 表 | ✅ |
| V9 | llm_providers/user_llm_providers models/config JSONB→TEXT | ✅ |
| V10 | llm_providers 增加 protocol 字段，按端点/旧名称迁移 Anthropic 协议 | ✅ |
| V11 | system_settings 表，初始化登录超时时间配置 | ✅ |
| V12 | execution_logs 增加 runtime 父子链、sidecar thread/checkpoint、trace 字段 | ✅ |
| V13 | 扩展 workflow_nodes 类型约束，支持更多 runtime 节点类型 | ✅ |
| V14 | workflow_nodes.config 调整为 TEXT，兼容复杂节点配置 | ✅ |
| V15 | 新增/放开 INPUT 工作流节点类型 | ✅ |
| V16 | 新增 agent_permissions 对象级 Agent 授权表 | ✅ |

#### Bug修复

| Bug | 文件 | 修复 |
|-----|------|------|
| AES加密密钥长度不合规 | AesEncryptService | SHA-256派生32字节密钥 |
| PG JSONB类型不匹配 | V9迁移 | JSONB→TEXT |
| 前端错误消息只显示❌ | request.ts | 修复变量遮蔽(`const { message }`遮蔽参数) |
| 后端错误消息全是"服务器内部错误" | GlobalExceptionHandler | 返回异常根因消息+HTTP状态码 |
| 聊天页发送后助手回复为空 | ChatController | 修复 SSE 控制器只消费第一个 Flux 事件后立即 complete 的问题，改为完整订阅并逐个发送事件 |
| Doubao 流式响应解析为空 | OpenAICompatibleProvider | 修复流式 chunk 解析逻辑，支持按行拆分 `data:` SSE 行和原始 JSON 行 |
| 默认模型不可用导致对话失败 | ChatSessionService / AgentRouter / LlmCallHandler / MemoryService / 前端模型选择 | 统一默认模型为实际可用的 `doubao-seed-2.0-code` |
| 流式错误前端不可见 | stores/chat.ts | SSE `ERROR` 事件追加为助手错误消息，避免只在控制台输出 |
| Kimi 2.6 流式回复为空 | ClaudeProvider | 修复 Anthropic SSE chunk 解析，支持多行 chunk、`data:` 行和裸 JSON 行 |
| GLM 5.1 只能命名为 claude 才连通 | LlmConfig / LlmGateway / llm_providers.protocol | 将供应商唯一名称和协议类型解耦，GLM 可使用 `name=glm` + `protocol=ANTHROPIC` |
| 登录超时后继续请求不跳登录页 | request.ts | 认证失效时统一清理本地 token/userInfo 并跳转登录页 |
| 登录超时时间不可配置 | SystemSettingService / AuthSettingsTab.vue | 新增管理员认证设置，动态控制 Access Token 有效期 |

#### 2026-06-02 聊天流式与 Doubao 模型修复记录

**问题现象：**

- `/chat` 页面发送任意消息后，助手消息显示为空或没有可见文字。
- 切换模型时曾误用 `doubao-seed2.0-code`，实际可用模型 ID 为 `doubao-seed-2.0-code`。
- 登录页出现过 `Network Error`，经确认是前端 Vite dev server `5173` 未监听导致的代理失败，不是后端登录接口故障。

**排查过程：**

- 分别验证后端 `8080` 和前端 `5173` 端口状态，确认后端登录接口可直接返回 `200 success`。
- 通过前端代理 `http://localhost:5173/api/auth/login` 复测，确认重启 Vite 后代理链路恢复。
- 直接请求 `/api/llm/user/models/available`，确认数据库中的 Doubao 模型 ID 为 `doubao-seed-2.0-code`。
- 直接请求 `/api/chat/messages/stream`，定位到后端 SSE 流未完整输出，以及 OpenAI 兼容供应商流式 chunk 解析过滤过严。

**修复内容：**

- `ChatController.doStream()` 从只读取首个事件改为完整消费 `sendMessageStream()` 的 Flux，并将 `THINKING`、`CHUNK`、`DONE`、`ERROR` 事件逐个发送给前端。
- `OpenAICompatibleProvider.chatStream()` 支持对响应 chunk 按换行拆分，兼容 `data: {...}` SSE 行和原始 JSON 行，避免 Doubao 流式内容被过滤为空。
- 默认聊天模型、Agent 路由默认模型、工作流 LLM 节点默认模型、记忆提取模型和前端默认选中模型统一改为 `doubao-seed-2.0-code`。
- 前端 Chat Store 对 SSE `ERROR` 事件追加助手错误消息，提升页面可见性。
- 当前故障处理期间发现的 `Network Error` 通过重启 Vite dev server 解决，并记录为运行环境问题。

**验证结果：**

- 后端关键测试通过：`ChatControllerTest,DefaultChatStrategyTest,OpenAICompatibleProviderTest`。
- 后端完整测试曾通过：`mvn -q test`。
- 前端测试通过：`npm test -- --run`，30 个测试通过。
- 直接请求 `/api/chat/messages/stream` 可看到 `THINKING → CHUNK → DONE` 事件序列。
- 直接请求 `/api/chat/messages` 可获得完整助手文本回复。

#### 2026-06-02 Kimi/GLM 协议与认证设置修复记录

**问题现象：**

- `k2.6` 使用 Kimi/Claude 供应商时，聊天流式接口只返回 `DONE`，没有 `CHUNK` 内容。
- GLM 5.1 使用 `https://open.bigmodel.cn/api/anthropic` 时，供应商名称必须写成 `claude` 才能测试连通。
- 登录过期后继续发起请求时，前端没有统一回到登录页；登录超时时间只能依赖后端固定配置。

**根因：**

- `ClaudeProvider.chatStream()` 和早期 OpenAI 兼容流式解析存在同类问题：把 `bodyToFlux(String.class)` 的 chunk 误认为单行 SSE，未按换行拆分，也未兼容裸 JSON。
- `LlmConfig` / `LlmGateway` 使用 `name == "claude"` 判断调用协议，导致供应商名称承担了唯一标识和协议类型两个职责。
- 前端 Axios 拦截器在认证失效时尝试刷新 token，而不是按照当前需求直接清除登录态并跳转登录页。

**修复内容：**

- `ClaudeProvider` 流式解析改为按行处理，兼容 `data: {...}`、裸 JSON 和 `[DONE]`。
- `llm_providers` 新增 `protocol` 字段，支持 `OPENAI_COMPATIBLE` 与 `ANTHROPIC`；供应商 `name` 只作为唯一标识。
- `ProviderManageTab.vue` 增加协议下拉；GLM 当前配置为 `name=glm`、`protocol=ANTHROPIC`。
- 新增 `system_settings` 表和 `SystemSettingController`，管理员可在设置页配置登录超时时间。
- `JwtUtil` 支持传入动态 access token 有效期，`AuthService` 登录/刷新时读取系统设置。
- `request.ts` 对非登录接口的 401 统一清理本地认证信息并跳转 `/login?redirect=...`。

**验证结果：**

- `k2.6` 真实流式接口返回 `CHUNK hello → DONE`。
- GLM 5.1 在 `name=glm` 下测试连通成功，返回模型 `glm-5.1`。
- 认证设置从 `900000ms` 临时改为 `300000ms` 后，重新登录返回 `expiresIn=300000`；随后已恢复 `900000ms`。
- 登录接口复测：后端直连约 301ms，前端代理约 2344ms，未出现请求超时。
- 后端相关测试通过：`AuthServiceTest,JwtUtilTest,SystemSettingServiceTest,LlmConfigTest,LlmGatewayTest,ClaudeProviderTest,OpenAICompatibleProviderTest,ChatControllerTest`。
- 前端测试通过：`npm test -- --run`，30 个测试通过。

---

## 功能实现状态

### ✅ 已完成功能

| 功能 | 前端 | 后端 | 说明 |
|------|------|------|------|
| 用户注册 | ✅ 登录页注册弹窗 | ✅ BCrypt加密+自动分配角色 | — |
| 用户登录 | ✅ 登录表单+自动跳转 | ✅ JWT双Token | — |
| Token刷新 | — | ✅ refresh端点保留 | 前端认证失效后按需求直接跳登录 |
| 登录过期跳转 | ✅ 401自动清理登录态并跳转登录页 | ✅ 401统一认证失败响应 | — |
| 登录超时设置 | ✅ 设置页"认证设置"Tab | ✅ system_settings + JWT动态有效期 | 仅admin |
| 用户登出 | ✅ 清除本地状态 | ✅ Redis黑名单 | — |
| 3套暗色主题 | ✅ CSS变量+实时切换 | — | — |
| Agent分组展示 | ✅ 筛选下拉 | ✅ 分组API+agentCount | — |
| Agent列表 | ✅ 4列网格+搜索 | ✅ 分页+筛选 | — |
| Agent详情 | ✅ 左右分栏 | ✅ 含skills聚合 | — |
| Agent创建/编辑/删除 | ✅ 弹窗表单 | ✅ REST API | RBAC权限 |
| Agent发布/下线 | ✅ 状态按钮+标签 | ✅ PUT /{id}/status | — |
| Agent对象级授权 | ✅ 授权弹窗支持多选批量授权，详情页按 canManage/canCopy 控制按钮 | ✅ agent_permissions + access/permissions/copy API + 对象级可见性 | Plan9核心闭环 |
| Agent授权使用链路 | ✅ Agent列表/聊天目标可见已按对象授权过滤 | ✅ 可用性、复制、提示词读取权限校验；无 canReadPrompt 时隐藏敏感配置 | 后续补跨工作流权限 |
| 工作流CRUD | ✅ 列表+新建+编辑+删除+复制 | ✅ 全套REST API | — |
| 工作流画布 | ✅ Vue Flow+技能/Agent引用/Workflow引用节点+属性面板 | — | — |
| 工作流导入导出 | ✅ JSON导入导出 | ✅ JSON序列化 | — |
| 画布 Agent 引用节点 | ✅ 拖拽、属性选择、保存回显 | ✅ `AGENT_REF` 协议转换和必填校验 | 计划8阶段二 |
| 画布 Workflow 引用节点 | ✅ 拖拽、属性选择、保存回显 | ✅ `WORKFLOW_REF` 协议转换和必填校验 | 计划8阶段二 |
| 工作流运行输入 | ✅ `INPUT` 节点 + 开始节点 `inputKey/value`，运行时收集入参 | ✅ `INPUT` 节点协议、运行请求 input、sidecar 输入透传 | 文件类运行前表单待增强 |
| 文件上传访问 | ✅ 可作为工作流文件输入的前端基础 | ✅ `POST /api/files/upload`、`GET /api/files/{fileId}` | 本地存储目录可配置 |
| Runtime标准协议 | — | ✅ WorkflowDefinition / ExecutionRequest / ExecutionEvent | 计划8阶段一 |
| 工作流协议转换 | — | ✅ WorkflowDefinitionAssembler | 计划8阶段一 |
| Mock Runtime Gateway | — | ✅ 默认 mock sidecar 事件流 | 计划8阶段一 |
| Python Runtime Sidecar | — | ✅ FastAPI HTTP/SSE + LangGraph + CONDITION + ROUTER + PARALLEL/JOIN | 计划8阶段三 |
| SidecarRuntimeGateway | — | ✅ WebClient 调用 sidecar 并解析事件流 | 计划8阶段三 |
| 执行日志父子链与checkpoint引用 | — | ✅ execution_logs 扩展字段 | 计划8阶段一 |
| LLM供应商管理 | ✅ 设置页面(全局+用户级) | ✅ 完整CRUD | — |
| LLM协议选择 | ✅ 全局供应商协议下拉 | ✅ protocol字段选择OpenAI/Anthropic Provider | 支持GLM/Kimi/Claude |
| LLM连通测试 | ✅ 测试按钮+结果展示 | ✅ 发短消息验证 | 管理员+用户双入口 |
| 用户级API Key | ✅ "我的模型"Tab | ✅ 用户覆盖全局配置 | — |
| 可用模型列表 | ✅ ModelSelector下拉 | ✅ 全局+用户模型合并 | — |
| 智能对话 | ✅ ChatView(左右分栏+流式) | ✅ REST + WebSocket | — |
| 流式输出 | ✅ 逐token渲染+光标动画 | ✅ Flux→WS CHUNK协议 | — |
| 会话管理 | ✅ 创建/删除/切换 | ✅ Session CRUD | — |
| 用户记忆 | — | ✅ MemoryService异步提取 | — |
| 执行引擎 | — | ✅ OrchestrationEngine | Agent/Workflow双模式 |
| Markdown数据同步 | — | ✅ 解析MD导入Agent/技能 | — |
| 用户管理 | ✅ 用户表格+角色分配 | ✅ 分配角色API | 仅admin |
| 角色权限管理 | ✅ 角色卡片+权限checkbox | ✅ 权限编辑API | 仅admin |
| RBAC权限控制 | — | ✅ @RequirePermission AOP | — |
| 统一错误处理 | ✅ 状态码+真实错误消息 | ✅ GlobalExceptionHandler返回根因 | — |

### ❌ 未实现功能

| 功能 | 说明 | 预计难度 |
|------|------|---------|
| 生产部署配置 | 后端/前端/runtime-sidecar Dockerfile、docker-compose、nginx、生产 profile、日志与健康检查编排 | 高 |
| 真实 LLM Router | 目前 `LLM_ROUTER` 为 mock 路由，后续接入真实 LLM provider 做动态路线选择 | 中 |
| 并行高级治理 | PARALLEL/JOIN 的分支超时、并发限制、更完整 JOIN 聚合策略和部分成功策略 | 高 |
| 跨工作流递归与权限治理 | 已有图内循环和直接自引用拒绝，仍需补跨工作流递归检测、最大深度/节点数、Workflow/Agent 跨对象权限校验 | 中 |
| 运行前入参表单增强 | 当前已能从开始节点/INPUT 节点收集固定值，后续增强为运行前弹窗填写，并自动上传文件类输入 | 中 |
| 画布撤销/重做 | Vue Flow history管理 | 低 |
| 上下文窗口 token 计数 | 精确截断按 token 而非消息条数 | 低 |
| 浏览器级端到端测试 | 关键链路 E2E：登录、授权、聊天调用、工作流运行、执行监控、恢复/审批 | 中 |

---

## 当前运行环境

| 组件 | 版本/配置 |
|------|----------|
| Java | JDK 17.0.19.10-hotspot |
| Maven | 3.9.16 |
| PostgreSQL | 16，端口5432，数据库agent_platform |
| Redis | 3.0.504，端口6379 |
| Node.js | — |
| 前端端口 | 5173 (Vite dev) |
| 后端端口 | 8080 (Spring Boot) |
| 管理员账号 | admin / admin123 |
| Git仓库 | https://github.com/xuqiping/AI-Projects.git |

---

## 代码统计

| 维度 | 数量 |
|------|------|
| 后端Java源文件 | ~146 |
| 后端API端点 | 54（含LLM管理+Chat+Execution+系统设置） |
| 数据库表 | 16 |
| Flyway迁移 | 16 (V1-V16) |
| 前端Vue组件 | 35+ |
| 前端页面/视图 | 8 |
| 后端测试 | 140+ (含计划8阶段一 Runtime/Execution 与阶段二引用节点校验测试) |
| 前端测试 | 34个（全部通过） |
