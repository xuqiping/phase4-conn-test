# 人工测试遗留问题修复 · 开发进度2（2026-08-26）

> 对应 plan：[plans/人工测试遗留问题修复.plan.md](../../docs/plans/人工测试遗留问题修复.plan.md)
> 本轮：子计划 B 全部完成（B1-B6 聊天计费闭环 + DEBT 兜底），A/B 测试全绿。

## 本轮完成

### B1 迁移与实体
- `V157__user_remark_and_debt.sql`：users.remark（D1 用）；user_points_balance.debt_points（≥0 CHECK）；points_ledger 类型 +DEBT/DEBT_REPAY。
- `V158__group_ledger_member_types.sql`：ck_pgl_type 补 MEMBER_* 三类（V157 落 IT 库后不可改，拆新版本）。
- 实体：`User.remark`、`UserPointsBalanceEntity.debtPoints`（V65 旧注释「可负=欠款」修正）。
- `ProjectGroupWalletServiceIT.assertLedgerWalletReconcile`：Σdelta 白名单排除 MEMBER_*（非资金腿）。

### B3 聊天 HOLD 预扣（Q4=B，镜像媒体 V155）
- `LlmBillingService`：`holdChat`（est=prompt估算×入价+min(maxTokens,2048)×出价；可用<est 抛 INSUFFICIENT_POINTS 带两数；组=chargeGroup CHAT-HOLD / 个人=chargeIdempotent 幂等键 chat-hold-{ref}）、`settleChatHeld`（usage 精确值多退少补，幂等键 chat-settle-{ref}）、`settleChatCancelled`（chars÷1.6 折算，实扣=min(折算,预扣)，差额退 chat-cancel-{ref}，usage 记 ESTIMATED）。
- `LlmGateway.chat`：requireAffordable 后 hold；成功→settleChatHeld 替代 onSuccess；provider 异常→全额退（chars=0）。
- `LlmGateway.chatStream`：defer 内 hold（拒→Flux.error 走 B4 分流）；usage sink→settleChatHeld+usageSettled 守卫；doOnComplete 无 usage/doOnError/doOnCancel→settleChatCancelled；画布/同步路径经 chat() 自动覆盖。

### B2 PROGRESS + DONE.usage（Q2=B）
- 网关流：净化后 CHUNK 累计字符，每 32 chunk 或 ≥1s 发 `PROGRESS{chars,estimatedTokens,estimatedPoints}`（估价失败不发）；流尾 `USAGE{promptTokens,completionTokens,points}` 内部事件。
- `ChatSessionService`：USAGE 捕获（filter 滤除不透前端）→ 尾部 `DONE.data.usage`（老客户端忽略未知字段，前向兼容）。
- `StreamEvent`：+done(usage)/progress()/usage() 工厂。

### B4 SSE 业务异常分流（9x 白嫖口收口）
- `ChatController.doStream`：BusinessException（沿 cause 链找）→ ERROR(业务话术)+DONE，**不再 sendMessage 同步重答**（重答=二次预扣双倍计费）；已发 chunk 加「回答中断：」前缀；超时/断开路径不变。

### B5 DEBT 挂账+冲抵（Q10=A）
- `PointsWalletService`：`chargeToDebt`（实付 min(balance,cost) CONSUME 腿 + 差额 DEBT 腿 delta=0 不动 Σdelta 恒等式，不抛）；`requireAffordable` 欠款>0 全拦（话术带欠款数）；`grantWithLedger`/`creditRechargeForOrder` 入账前 `repayDebtFromCredit`（DEBT_REPAY 腿，主腿 delta=到账余额）；开关 `billing.debt-collect.enabled`。
- 接线：聊天/媒体结算个人补扣失败→chargeToDebt；`backstop` 组长余额不足→预判直走 chargeToDebt（内层 REQUIRED 抛 INSUFFICIENT 会毒化外层事务，实测 UnexpectedRollback 后改预判式）。
- `MyWalletView`：欠款警示 tag；`billing.ts` LEDGER_TYPE_LABEL +4 类型；`UserWalletVO.debtPoints`。

### B6 测试
- 单测：LlmBillingServiceTest +10（hold 拦截/放行/组径/开关、结算补扣/退差/挂账/兜底、取消折算/零字符）；PointsWalletServiceTest +8（欠款拦截/挂账双腿/覆盖/开关/吞异常/充值冲抵/无欠款/全额吞）；MediaBillingServiceTest 补扣失败用例改挂账新语义。
- IT（真 PG）：ProjectGroupWalletServiceIT +1（backstop 转挂账→欠款拦消费→充值冲抵全链，含 DEBT_REPAY 腿断言）。
- 后端全量单测 + IT 全绿；前端 vitest 739 全绿。

## 实现期修正（记录到 spec §偏离）
1. 开关用 `@Value`（billing.chat-hold.enabled / char-per-token / hold-est-max-tokens / debt-collect.enabled）非 system_settings——随 billing.enabled 先例，热改配置重启生效。
2. est 输出帽 2048 新增（plan 只写 maxTokens 兜底；8192 默认值全额冻结会常态性开局拒，超帽实耗走结算多退少补+DEBT）。
3. BILLING 首事件弃用——网关自持 ref/chars 自结算，服务层零信任透传。
4. backstop 挂账走预判式（事务毒化坑，见 B5）。
5. DEBT/DEBT_REPAY 腿 delta=0（保 Σdelta=balance 对账恒等式，金额在 remark+debt 列）。

## 关键代码位置
- `billing/service/LlmBillingService.java`（B3 全套）、`PointsWalletService.java`（B5 全套）
- `llm/LlmGateway.java`（chat/chatStream 钩子）、`chat/service/ChatSessionService.java`（USAGE 捕获）、`chat/controller/ChatController.java`（B4 分流）
- `projectgroup/service/ProjectGroupWalletService.java`（backstop 挂账）、`chat/dto/StreamEvent.java`
- 迁移：`db/migration/V157/V158`
- 前端：`views/MyWalletView.vue`、`api/billing.ts`

## 下一步
子计划 C（图片预估与比例）：C1 estimatePreview 个人口径 → C2 画布红字/chip → C3 ImageSizeDeriver → C4 前端 imageSize.ts → C5 测试。
