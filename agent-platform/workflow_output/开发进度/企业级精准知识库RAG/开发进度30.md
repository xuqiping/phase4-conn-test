# 企业级精准知识库 RAG · 开发进度 30

- 主题：P2 索引快照实际重建闭环真实性修复。
- RED 1：`KnowledgeIndexRebuildServiceTest` 因服务不存在而编译失败；随后补状态机，验证 BUILDING/READY/FAILED/CANCELLED/DRY_RUN。
- RED 2：Gateway、Worker、取消 API 测试因目标快照字段、数据库方法和生产类缺失而编译失败；补 `V113`、`V114`、PG Gateway 与目标物理索引写入。
- RED 3：前端 API 测试因 `cancelIndexRebuild` 不存在失败；补启动/取消按钮、真实进度字段和刷新恢复。
- RED 4：运维 API 审计测试因重建接口无 `AuditLog` 失败；补启动、取消、切换、回滚审计。
- 关键不变量：普通增量任务写 write alias；快照重建只写 PG 登记的隔离物理索引；仅 READY 快照可切换；状态和进度重启不丢失。
- 迁移：`V113__rag_index_snapshot_physical_name.sql`、`V114__rag_snapshot_rebuild_jobs.sql`，未修改历史 Flyway。
- 验证：后端目标测试 21/21 通过；持久化刷新测试 5/5 通过；前端 API 1/1 通过。
- 安全/运维：`knowledge:manage`、二次确认、服务端生成物理索引名、写操作审计；日志不记录正文/Prompt/Query。
- 下一步：提交 P2 修复后，进入 P3，把 QueryPlan、Pre-filter、多通道召回与 Ranking Engine 接入真实 `RagRetrievalService`。
