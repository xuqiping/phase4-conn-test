# 开发进度3 · Chunk D（上游面板 + Lightbox + @弹层光标锚定）

> 对应 `docs/plans/人工测试遗留问题修复III_D_上游面板与Lightbox.plan.md`（2x-8 / 2x-9）
> commit `0770434c`（14 文件 +1063/-8）

## 实现内容

| 项 | 产物 | 说明 |
|---|---|---|
| D1 统一预览 | `components/canvas/Lightbox.vue`（新） | 图片滚轮缩放 0.2–5x / pointer 拖拽平移 / 双击复位；视频原生 controls；Esc(window) / 遮罩 / × 三路关；工具条三按钮键盘可达；role=dialog+aria-modal；`defineExpose({scale,tx,ty,reset,zoomBy})`。与既有 MediaLightbox（静态图片全屏）并存互不影响 |
| D2 上游面板 | `PropertyPanel.vue` + `upstream.ts`（新）+ `CanvasView.vue` | BFS 反向邻接表 O(V+E) 分层：depth=1 大卡（缩略图/类型角标/名称/提示词两行摘要）、depth>1 折叠「更上游 N 节点」、>50 截断提示。单击缩略图→Lightbox；双击卡→appendMention 插 `@{{node:id}}`。提示词**纯文本插值**渲染（禁 v-html） |
| D3 程序化追加 | `MentionTextarea.vue` | `appendMention({kind,id,insertSuffix})`：末尾非空白先补空格、token 后恒跟尾随空格、禁用态返回 false、成功后聚焦并把光标落到引用之后。`defineExpose` 暴露 |
| D4 弹层光标锚定 | `caret.ts`（新）+ `MentionTextarea.vue` | contenteditable 原生 `Range.getClientRects()` 光标矩形→placePopover（上方优先，越上界翻转下方，左右夹容器内，宽 240）；resize 重算；探测失败（jsdom 无布局）回落 CSS 静态「输入框下方」 |

## 与计划的偏离（已按铁律回记 plan）

1. **镜像 div 方案废弃**：A1 已把 MentionTextarea 重写为 contenteditable，光标矩形原生可得（`Range.getClientRects()`），无需镜像。
2. **CanvasBoard.vue 零改动**：上游数据改由 CanvasView `computed` 供（`boardRef.getNodes()/getEdges()` 响应式追踪，先例 selectedAncestors）——规避用户 WIP 冲突的最大风险点。
3. **画布层事件冒泡 → PropertyPanel ref 直连**：双击上游卡直接经 `primaryInputRef`（5 处各型分支唯一渲染）调用 appendMention，不经画布中转。

## 依赖吸收（用户 WIP，既有先例 5d77a931 口径）

`types/canvas.ts` MentionCandidate.`insertSuffix?` + `mentionLogic.ts` insertMention 第 6 参 + 测试（2x 六轮 #1 标注图子序号，20 行内聚完整特性）。原因：MentionTextarea.vue 调用点传第 6 参，不吸收则 HEAD 检出即编译破坏。全量 795 用例含其用例绿。

## 测试

- 新增：`upstream.test.ts` 7（菱形去重/深度升序/环/无上游/ghost seed/cap 截断/自环）、`caret.test.ts` 8（above/翻转/夹边/兜底 null）、`Lightbox.test.ts` 9、MentionTextarea 追加 7（appendMention×6+D4 兜底）、PropertyPanel 追加 5
- 前端全量 vitest **795/795 绿**；`vue-tsc --noEmit` **0 error**

### 踩坑

- **jsdom 重渲染替换元素**：trigger 后持旧元素引用读到过期 DOM（vm.scale=1.15 而 transform 停 scale(1)）——重渲染会替换节点，断言前必须重新 find。
- withDefaults 首参报错 = script setup 语法树被破坏（本次 `}>(), {` 误写 `}>, {`）；babel 把无括号 `defineProps<T>` 解析为 TS 类型实例化，报错有误导性。

## 安全/运维勾验（plan 清单）

- [x] 上游提示词纯文本渲染（插值，禁 v-html）
- [x] Lightbox 媒体 URL 仅任务产物已存 URL（previewUrl/coverPreviewUrl），无外链注入面
- [x] @插入复用 mentionLogic 既有序列化（token 格式不变，重命名不断链）

## 下一步

Chunk C（画布七小件）——注意 PropertyPanel.vue 已含 B+D 两轮改动，C 轮续改同文件；C6 单击 Lightbox 可直接复用 D1 组件。
