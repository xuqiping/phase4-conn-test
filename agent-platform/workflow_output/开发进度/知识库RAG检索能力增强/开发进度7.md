# 知识库RAG检索能力增强 · 开发进度7

对应 plan：[WP4_全局问答](../../docs/plans/知识库RAG检索能力增强_WP4_全局问答.plan.md)｜上轮：[开发进度6](开发进度6.md)

## WP3 Step4 决策记录（2026-09-03 用户拍板）

先做 WP4 回头补——Step4 影子对比的接入点代码与真实跑批一并延后，等用户给库+黄金集用例。

## 本轮：WP4 Step1 L-KB 数据层与生成 Worker（commit f4e8ecfd，2847/2847）

- **`V172__knowledge_base_summaries.sql`**：版本化表（`UNIQUE(kb_id, version)` 防并发双写）+status READY/ERROR+topics/stats JSONB+`idx (kb_id, version DESC)` 取最新。
- **`KnowledgeBaseSummary` 实体**（独立不继承 BaseEntity——本表无 deleted 列且 version 是业务版本非乐观锁，继承会带 @TableLogic 使 MP 查询拼 `deleted=0` 直接炸 SQL；坑与 V170 relations 表同源）+Mapper。topics/stats 走 JsonbStringTypeHandler。
- **`KbSummaryProperties`**（`rag.global.summary`）：enabled kill switch/cron 04:30/batchSize 20/变更阈值 10%/staleDays 7/maxAttempts 3/map·reduce maxTokens/model。
- **`global/KbSummaryWorker`**：
  - 触发矩阵：无历史行→首生成；latest=ERROR→跳过待手动（旧 READY 继续服务）；READY→`|now-prev|/prev ≥10%` 或 `generatedAt >7天` 才重生成；无 L1 文档库跳过。
  - map-reduce：全 INDEXED 文档 L1 摘要分批 ≤20 →每批一次 LLM 浓缩要点→reduce 合成 `{"summary":"≤2000字","topics":[≤20词×30字]}` JSON（markdown fence 剥壳；summary 超 2000 截断）；reduce 坏 JSON=一次 attempt 失败。
  - 重试 ≤3（整体生成计一次 attempt）→超限落 ERROR 行（summary NULL，stats 带 error 摘要）。
  - 计费归户 KB 创建者；temperature 0.3；**零对外读路径**（摘要仅落库，Step2 起 Service 内部注入 prompt——规格 §9.3 泄露面控制）。
- 测试 `KbSummaryWorkerTest` 3：触发矩阵六分支（**setUp 重置法**复用同 fixture——mock 重置靠重建而非 reset()，保 strict stubs 语义）/45 文档 3 批 map+1 reduce+READY v2 全字段（version 递增/topics JSON/stats docCount·batchCount·attempt）/重试耗尽 chat×3+ERROR 行 stats 含错误摘要。

## 下一步

WP4 Step2：GLOBAL 分类（QueryPlanner 词表+LLM 规划器优先）+GlobalAnswerStrategy（map-reduce 分支+文档级引用白名单+多库提示+超时降级）。
