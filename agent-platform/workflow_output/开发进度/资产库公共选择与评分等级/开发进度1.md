# 资产库公共选择与评分等级 · 开发进度1

> 功能：[计划](../../docs/plans/资产库公共选择与评分等级.plan.md) · [规格](../../docs/specs/资产库公共选择与评分等级设计.md) · [测试方案](../../docs/测试方案/资产库公共选择与评分等级测试方案.md)
> 对应问题：2x#4（选择器无公共池）、2x#5（分享搜索空关键词不展示）、2x#7（评分等级制）+ 待决策项（公共池复制管控）

## 本轮（2026-08-18）· 预处理 + Step 1

### 产出文档
- 测试方案（P/S/G/C 四组人工用例 + L1-L6 联动表含反向/边界 + 回归红线）：`docs/测试方案/资产库公共选择与评分等级测试方案.md`

### Step 1 代码
**实现**：公共池摘要补 mediaTypes（2x#4 后端侧）+ 候选开箱即载（2x#5 全链）
- `PublicProjectSummaryVO`：+`mediaTypes` 字段（项目受控词汇 jsonb 原样透传，选择器按图片/视频过滤公共池项目）；VO 白名单注释同步（narrativeRoles/assets/content/fileId 仍禁止）
- `AssetPublicPoolService.listPublic`：装配补 `.mediaTypes(project.getMediaTypes())`（零新查询，读已查出的实体列）
- `AssetMemberService.searchCandidates`：空关键词 LIMIT 20→50，带关键词维持 20（`blankKeyword ? 50 : 20`）
- `ShareDialog.vue`（2x#5 根因修复）：watcher 打开弹窗即 `searchCandidates('')`；`searchCandidates` 移除空关键词早退（空=发请求恢复全量）；状态文案「输入用户名筛选，最多展示 50 人」
- 测试：后端 `AssetMemberServiceTest`（空关键词 50 断言更新）、`AssetPublicPoolServiceTest`（白名单反射断言移除 mediaTypes + 透传值断言）；前端 `ShareDialog.test.ts`（新增开箱即载/清空恢复全量两用例，旧空关键词用例重写）
**验证**：`mvn -Dtest=AssetMemberServiceTest,AssetPublicPoolServiceTest,AssetMemberControllerCandidatesTest,AssetPublicPoolControllerTest` 21 用例绿；`ShareDialog.test.ts` 16 用例绿；`vue-tsc` 零错
**关键位置**：`AssetMemberService.java` searchCandidates limit 三元；`ShareDialog.vue` watcher + searchCandidates
**偏离记录**：plan Step 1 动作③原仅「前端提示文案常量」，扩展为 ShareDialog 开箱即载完整修复——否则 2x#5 只有后端 50 上限、前端仍不发请求，问题不闭环（spec §4.2 前端契约本就要求，plan 漏列）
**迁移版本核对**：`db/migration` 最新 V99 → 本功能 Step 5 用 **V100**（plan 写 V128 占位）

### Step 2 代码（2026-08-18）
**实现**：AssetFilePicker 公共池来源（2x#4 前端侧；画布 AssetPicker 已有公共池来源，零改动，偏离记录 2）
- `AssetFilePicker.vue`：顶部「我的/共享项目 ↔ 公共池」来源切换（n-button-group，镜像画布 picker 交互）；公共池下拉仅展示 `usable` 且 `mediaTypes` 含当前类型的项目（jsonb 解析失败/缺省不过滤）；label 含发布者/官方/资产数；选中复用 `assetApi.list(projectId, {type})`（后端 ACL 放行 VIEWER）；切来源清选择（L1 反向）；列表加载失败 message 兜底（L1 项目被移出公众池）
- `types/asset.ts`：`PublicProjectSummaryVO` + `mediaTypes`
- 公共池摘要每次打开刷新（usable 审批态会变），本地项目保持原缓存策略
- 测试：新增 `AssetFilePicker.test.ts` 6 用例（双来源加载/usable+mediaTypes 过滤/坏 json 不过滤/type 透传/切回清空/加载失败兜底）
**验证**：`vue-tsc` 零错；ImageGenView/VideoGenView 回归 2 用例绿；组件 6 用例绿

### Step 3 代码（2026-08-18，commit 893da2a）
**实现**：后端评分等级派生（2x#7 后端侧）
- 新增 `asset/service/AssetGrade.java`：`fromScore(Integer)` 唯一真相源（null→null；≥95 A+ / ≥90 A / ≥80 B / ≥70 C / 其余 D）+ `rangeOf(grade)` 等级→[min,max] 区间表（A+=[95,100]…D=[0,69]，clone 防御拷贝；Map.of 不收 null key，先判空）+ `ALL` 下拉顺序常量
- `AssetScoreVO`/`AssetVO`：+`ownerGrade`/`memberAvgGrade`（String，派生不入库）
- 装配点两处：`AssetScoreService.getScore`（单资产，均分先 `Math.round` 存局部变量再派生）；`AssetService.assembleList`（列表批装配，循环尾 `AssetGrade.fromScore(vo.get…Score())`，零新查询）
- 测试：新增 `AssetGradeTest` 5 例（边界 94/95/89/90/79/80/69/0/100/null + 区间表 + 区间自洽回映射 + 未知等级抛 + 拷贝防污染）；`AssetScoreServiceTest` +3 例（双轨各自派生 88→B/90→A、取整先行 90.33→90→A 与 90.5→91→A、**取整跨档 94+95→94.5→95→A+**、noScores 等级同 null）；`AssetServiceTest` 列表批装配断言 +2（88→B、均 90→A）
**验证**：`mvn -Dtest=AssetGradeTest,AssetScoreServiceTest,AssetServiceTest` 59 用例绿（5+14+40）
**坑**：`Map.of` 不可变 Map 对 null key 的 `get` 直接抛 NPE（非返 null）——`rangeOf` 需显式判空再查表

### Step 4 代码（2026-08-18，commit dc8d31f8）
**实现**：前端等级徽章与筛选（2x#7 前端侧）
- 新增 `constants/assetGrade.ts`：`ASSET_GRADES`/`ASSET_GRADE_RANGE`/`gradeFromScore`——与后端 AssetGrade.java 硬编码对齐，`assetGrade.test.ts` 镜像后端边界表（对齐单测防双份漂移）
- `types/asset.ts`：AssetVO/AssetScoreVO +`ownerGrade`/`memberAvgGrade`
- `AssetCard.vue`：双轨评分行数值右侧等级小胶囊徽章（VO 派生字段只渲染，前端不自算）
- `AssetDetailDrawer.vue`：评分摘要双轨徽章 + 「我的评分」徽章（只看已保存分）；打分 slider 右侧实时等级标签（`liveGradeLabel` 跟草稿走，`aria-live`）+ slider tooltip 带档；`myGradeLabel`/`liveGradeLabel` 拆分——已保存徽章不随拖动跳变
- `AssetMatrixFilter.vue`：筛选条第 3 个下拉「等级」（label 含区间如「A+（95-100 分）」）；选中即覆盖 emit scoreMin/scoreMax（等级=快捷方式，L4 冲突以等级为准）；清除等级**不 emit**（手动区间原样保留，L4 反向）；gradeValue 不入 AssetFilter（真实筛选条件仍是区间）
- 测试：`assetGrade.test.ts` 3 例（对齐镜像）；AssetCard C7-3 扩徽章断言；Drawer +C7-2b（94→A/95→A+/清空回默认 50→D 跨档联动 L3）；MatrixFilter +C7-5（A+ 覆盖 95-100、D 换档 0-69、清除零 emit L4）
**验证**：4 文件 55 用例绿；全量 80 文件 532 用例绿；`vue-tsc` 零错
**坑**：滑杆默认 50 分属 D 档（<70），不是 C——测试初版断言想当然写 C 被打脸，档位以常量表为准

### Step 5 代码（2026-08-18，commit ce3f4354）
**实现**：公共池复制管控后端（待决策项落地）
- `V100__asset_allow_public_copy.sql`：`asset_projects.allow_public_copy BOOLEAN NOT NULL DEFAULT TRUE`（存量已发布项目=允许复制，零行为变化；回滚 drop column 无损失；unpublish 不清此列——跨再发布保留供回显）
- `AssetProject` 实体 +`allowPublicCopy`；`PublicPublishRequest` +`allowPublicCopy`（null=不改）；publish 显式传值才覆盖，日志带 allowPublicCopy
- `ErrorCode` +`ASSET_COPY_FORBIDDEN(40302, "该项目不允许复制资产")`（文案不泄漏源项目信息）
- `AssetAclService.isMemberOrOwner(project,userId,admin)`：公共 VIEWER 与成员 VIEWER 同为 AssetRole.VIEWER 无法凭角色区分，须查成员表判真实关系（C5 依据）
- `AssetService.copyCurrent` 闸（loadAccessible 后）：公共池=true 且 allow=false 且非成员 → 抛 40302；成员/OWNER/admin 不受限；非公共项目不适用（loadAccessible 本就只放行真实成员）
- `AssetController.copy` +`@AuditLog(asset/asset_copy)`——拒绝路径走 aspect 的 Throwable 分支自动 result=FAIL+错误摘要留痕（安全清单审计项）
- 透出：`PublicProjectSummaryVO`/`ProjectVO` +`allowPublicCopy`（null 视为 TRUE 兜底，`!Boolean.FALSE.equals`）
- 测试：AssetServiceTest +4（公共 VIEWER 拒 40302 不进 requireWrite / 成员过闸 C5 / allow=true 过闸且不查成员关系 / 非公共池不适用）；AssetPublicPoolServiceTest +2（发布 null 沿用+显式覆盖+再发布保留 / admin 代发同样可设 C6）+ listPublic 摘要断言扩展；3 个存量 copy 测试补源项目 stub（新查询致 Mockito 严格模式 PotentialStubbingProblem）
**验证**：`mvn -Dtest=Asset*Test` 全绿（44+7 及全部 asset 包控制器/服务）

### 下一步
Step 6：前端发布开关与复制按钮显隐——发布弹窗 n-switch（默认 true 回显）+ 公共 VIEWER 复制按钮按 allowPublicCopy 渲染（不渲染非置灰）+ 文案
