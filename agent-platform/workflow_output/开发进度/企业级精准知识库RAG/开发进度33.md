# 企业级精准知识库 RAG · 开发进度 33

- 主题：P4 证据策略编排接入真实 `/retrieve` 链路。
- RED：新增 `EvidencePolicyServiceTest`，并扩展 `RagRetrievalServiceTest` 要求真实服务调用证据策略且返回 `confidenceState`；因 `EvidencePolicyService` 不存在而编译失败。
- GREEN：新增 `EvidencePolicyService`，组合 `CoverageSelector`、`ContextBuilder`、`ConfidenceEvaluator`；阈值读取 `RagRecallProperties`，不硬编码模型或新增静态默认模型。
- 真实链路：`RagRetrievalService` 在 PostgreSQL 最终 Hash/权限复核之后执行动态证据预算、Hash 去重、单文档占比限制、token 上限，并对筛选结果重新连续编号 Citation。
- 输出协议：`RagRetrieveVO` 新增 `confidenceState`，当前已把策略计算结果返回给检索调试和生成回答路径。
- 安全：策略只处理已完成 PG 最终复核的证据；日志未新增 Query、Prompt、Chunk 正文、密钥或完整模型输出。
- 验证：`mvn '-Dtest=EvidencePolicyServiceTest,RagRetrievalServiceTest' test`，9/9 通过。
- 下一步：把精确 locator 与 `CitationVerifier` 接入真实 Citation 输出，再接 Grounded Answer 分批事实与完整六态协议。
