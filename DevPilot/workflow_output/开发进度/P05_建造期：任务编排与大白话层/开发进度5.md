# P05 建造期：任务编排与大白话层 · 开发进度 5

> 本轮完成 S5 存档点列表与回滚（FR-037）。

## 已完成工作

### S5 存档点列表与回滚（FR-037）
- `core-state/src/checkpoint.rs` 扩展：
  - `Checkpoint` 增加 `chunk_no` / `round_id`（联表查询附带）。
  - `get(id)`、`list_by_project(project_id)` 按 chunk 顺序返回。
  - `rollback_downstream(round_id, chunk_no)`：删除下游 checkpoints，并把下游 tasks 重置为 pending（清空 cost/finished_at）。
  - 单元测试：列表顺序、回滚后状态与 checkpoint 数量。
- Tauri 命令（`src-tauri/src/commands.rs`）：
  - `list_checkpoints(project_id)` → `CheckpointDto` 列表。
  - `rollback_to_checkpoint(checkpoint_id)`：查 checkpoint → `git reset --hard <commit>` → DB 重置下游 → 推送状态快照。
- `src-tauri/src/lib.rs` 注册两个命令。
- `src-ui/lib/ipc.ts`：新增 `CheckpointDto`、`listCheckpoints`、`rollbackToCheckpoint`。
- 新建 `src-ui/components/checkpoint/CheckpointTimeline.tsx`：
  - 时间轴展示 chunk 编号、标题、commit 前 8 位、状态、时间、摘要。
  - 每个节点支持「回滚」→ 二次确认 → 调用 `rollbackToCheckpoint` → 刷新列表。
  - 摘要行用 `PlainText` 组件渲染。
- `src-ui/views/Build.tsx` 改为纵向布局，接入 `CheckpointTimeline`。
- commit: `5ac0ec0`

## 质量门结果

- `cargo fmt --all`：通过。
- `cargo clippy --workspace --all-targets -- -D warnings`：通过。
- `cargo test --workspace`：全绿（94 tests）。
- `npx tsc --noEmit`：通过。
- `npm run test`：40/40 通过。

## 下一步

- S6 追加指令续跑（FR-015）。
