# 开发进度6 · S5 关联高亮真根因修复 + canvas 6 数据恢复（2026-08-18）

> 用户实测反馈：选中分镜1，无关节点淡化但同源的文本3/文本4 不淡化。顺藤排查出闭包算法真根因 + 两处连带修复；排查过程中复发的丢画布事故已根因修复并完成数据恢复。

## 三修复（commit `605fe4f5`，vitest 13/13 + vue-tsc 清）

1. **真根因：relatedClosure 算的是连通分量，不是祖先∪后代**（utils/graphClosure.ts）
   - 旧实现从闭包内每个节点同时双向 BFS——「祖先的另一支后代」（兄弟节点）也被捞进来，等价整连通分量 → 选谁都全亮。
   - 修复：上行（target→source 只追祖先）+ 下行（source→target 只追后代）两趟**定向** BFS，各自维护已访集（共享会漏环+出边，单测环用例抓出），闭包 = 祖先∪后代∪seeds。
   - 单测 7 例同步新语义（选 b 中段 → 兄弟支 c 不入）+ 用户场景回归例（女主→{文本3,分镜1} 扇出，选分镜1 只亮 {女主,分镜1}）。
2. **选中双真相不同步**（CanvasBoard.vue）：@vue-flow/core 无 selectionChange 事件（实测 dist 类型无此 emit）；点击被微拖/卡片内元素吞掉时库内部已换选而应用层 selectedNodeId 仍持旧值 → 旧种子算闭包。修复：nodes-change 的 `{type:'select'}` 捕获 → syncSelectionFromLibrary（nextTick 读 getSelectedNodes 归一，幂等）。
3. **自动保存丢画布双闸门**（CanvasView.vue）：① canvasLoaded——加载完成前一切保存静默丢弃（防挂载期/HMR 把空 board PUT 上去）；② 空板防误清空——快照 nodes 空 且 lastSavedNodeCount>0（board 被整体重置 ≠ 用户逐个删除）时自动保存丢弃、手动保存弹「确认清空」确认。

## 浏览器验证（两画布三场景）

- canvas 7（专测数据）：点文本3 → 分镜1暗；点女主→微拖分镜1（吞 click 路径）→ 文本3暗 ✅
- **canvas 6（用户原数据）**：选分镜1 → **文本3/文本4 均 dim** ✅、祖先链（女主→脚本→男主链）亮、分镜2-7/视频/脚本2/本土化 全 dim ✅

## canvas 6 数据丢失事故 + 恢复（全程闭环）

- **事故**：S5 排查期间 Vite HMR 热替换 CanvasBoard 子组件重挂载 → board 清空而父层状态残留 → 竞态 PUT 空快照清空服务端（canvas 7 当场复现同机理；canvas 6 属同机理早前命中）。即修复③闸门所防类别。
- **恢复**：15 节点/12 边全量找回——来源=会话 transcript 内 Playwright 快照（.playwright-mcp/*.yml 全页树含节点全文 + network dump 含边 id 清单 + P4 会话 fill 命令原文含脚本2全文）。女主总结/男主剑士设定/分镜1-7 描述/脚本 synopsis/scenes/本土化 JSON+changeLog（由脚本2原文→产物精确推导）全部完整；仅原始坐标丢失（按拓扑重排）。
- 已直写 DB 恢复并浏览器复验（15节点12边逐标签对上）；快照备份存本目录 [canvas6快照备份-20260818.json](canvas6快照备份-20260818.json)。

## 遗留

- 撤回/版本保存（用户新需求，已录问题单 2x 未解决区）。
- selectionKeyCode prop 类型告警（String"Shift" vs Boolean|null，库声明与运行时不一致）——非阻断，未修。
