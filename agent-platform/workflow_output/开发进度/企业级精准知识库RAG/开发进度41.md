# 开发进度41：P5 异步评测运行领域引擎

- 日期：2026-08-13
- 分支：`beifen`

## 已完成

- 新增 Evaluation Run 状态机：`QUEUED → RUNNING → COMPLETED/FAILED`。
- 按固定 Dataset Case 调用可注入 Pipeline，逐题保存 traceId、指标与 verdict。
- 计算并汇总 Recall@10、MRR、nDCG、Citation Coverage、Faithfulness、Abstention Accuracy、Supported Rate。
- 失败只保存长度受限的错误摘要，不记录完整 Query、Prompt、Chunk 或模型输出。
- 领域引擎暂不注册为 Spring Bean，避免在真实 PostgreSQL Repository、执行器和生产 Pipeline 适配器接通前造成应用启动依赖缺失。

## TDD

- RED：`EvaluationRunServiceTest` 因领域服务不存在而编译失败。
- GREEN：定向测试 1/1 通过。

## 下一步

扩展 PostgreSQL Mapper/Repository 保存 Run/Result，接入真实 `RagRetrievalService` Pipeline 适配器与专用任务执行器，再开放 Controller 查询和前端进度。
