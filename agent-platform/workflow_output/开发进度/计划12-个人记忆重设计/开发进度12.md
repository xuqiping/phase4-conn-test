# 计划12 · 个人记忆重设计 · 开发进度12

> 迭代 F 收尾 · **F-4b MemoryLifecyclePanel 前端**（已删除/已离开项目折叠板 restore/copy-to + 页签徽标联动），承 [开发进度11.md](开发进度11.md)（F-4b 前置后端 4 端点）。
> 设计：[总体设计 §3.7](../../../项目工程文档/设计/个人记忆重设计-总体设计.md)。本 chunk 落地后迭代 F 前端全量完成（F-0~F-7 无欠账）。

## 本轮做了什么（commit 见 git log，vue-tsc + build + 105 单测全绿）

### `memory.ts` 补客户端 + 类型
- `MemoryLifecycleProjectVO`（projectId/projectName/departedAt/turnCount）+ `MemoryLifecycleActionVO`（newProjectId/newProjectName/affectedTurns），字段对齐后端 DTO。
- 4 端点：`listDepartedProjects` / `copyDepartedProjectTo(pid, name?)` / `listDeletedProjects` / `restoreDeletedProject(pid, name?)`；空名发 `{}` 走后端默认「「原项目名」记忆拉取」。

### `MemoryLifecyclePanel.vue`（新，components/memory/）
- n-collapse 双折叠板：**已删除项目**（标题 n-badge 高亮未处理流水账总数，有待拉取时默认展开）+ **已离开项目**（带 departedAt「离开于」）。
- 行 = 项目名 + turnCount tag + 「拉取到自建新项目」（turnCount=0 禁用，不做无效创建）。
- 拉取弹框：说明文案区分 restore（仅拉 turn 不拉 summary）/ copy-to（复制挂载非移动，原项目与其他成员零影响）；项目名可空（placeholder 显默认名，maxlength=100 对齐 projects.name 上限）。
- 成功后 message 显 affectedTurns + 新项目名，刷新双列表并 emit 徽标数。
- 错误透传后端 msg（403 非已离开成员 / 404 无待拉取防探测）。
- **badge 联动**：restore 后后端置本人 PROJECT_DELETED_AFFECTED 通知 resolved → ChatView 顶部 `MemoryNotificationBadge` 3s 轮询自消，前端无需显式通知。

### 挂载（MemoryManagerPanel 第 8 页签「生命周期」）
- 页签 label 用 n-badge 显已删除板未处理流水账总数（`@update:deleted-pending-turns` emit 联动，display-directive=show 首渲染即挂载取数）。
- 移动适配：行 flex-wrap 折行 + 弹框 `{maxWidth:420px, width:90vw}`（承 F-7 移动弹窗范式）。

### 单测（`memoryLifecycle.test.ts` 6 新，mock request 契约测）
- 4 端点路径/方法断言 + copy-to/restore 各验「带名发 {projectName} / 空名发 {}」。

## 验证
- ✅ `npx vue-tsc --noEmit` 零错误。
- ✅ `npm test` 105/105（99 旧 + 6 新）。
- ✅ `npm run build` 成功。

## 安全 / 运维（随 chunk 当下埋）
- 前端无越权面：4 端点后端强制 user_id=self（向量 7 已在后端 chunk 兜底）；前端仅透传 403/404 msg，不做存在性区分展示。
- 降级：拉取失败列表不丢（仅 message 报错）；轮询 badge 独立组件，本面板失败不影响通知 badge。

## 下一步（留后续轮次）
- **生命周期写侧 hook（未排期，开发进度11 新发现）**：成员离职置 DEPARTED + 追加 departed_project_ids、项目删除追加 deleted_project_ids + 清 summaries/coverage/members + 发 PROJECT_DELETED_AFFECTED 通知——读/补救侧已全齐（后端 4 端点 + 本面板），写侧无触发源，建议单开 chunk 挂项目删除/成员移除流程。
- **H 收尾**：V52 DROP 旧表 user_memories/user_memory_projects + 旧端点 `/api/chat/memories/*` 404 + 删 legacy 读控件/pane + 速查表 09 重写。
- **Phase4 E2E**：真 LLM 日期铁律 + 召回全链 LLM IT + departedAuthorNotes 端到端（可含 F-4b 拉取链路）+ 双节点并发 SKIP LOCKED + 联动 L1-L12 边界 + 15 安全向量越权 IT。
