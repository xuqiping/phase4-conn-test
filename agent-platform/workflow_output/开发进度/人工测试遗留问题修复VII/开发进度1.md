# 开发进度1 · 修复VII Chunk 1（dagre + autoLayout 纯函数）

- 日期：2026-08-28。plan：`plans/人工测试遗留问题修复VII.plan.md` Chunk 1。spec：`docs/specs/人工测试遗留问题修复VII设计.md` VII-2。
- commit：`feat(canvas): 人工测试修复VII A——dagre依赖+autoLayout纯函数`

## 实现

- 依赖：`@dagrejs/dagre@^1.1.4`（npm，锁 1.x；自带 TS 类型；进 CanvasBoard 所在 chunk，不碰 main.ts）。
- 新建 `src/utils/autoLayout.ts`：
  - `computeAutoLayout(nodes, edges, {direction:'LR'|'TB', includeIds?}) → Map<id,{x,y}>`；
  - 类型默认尺寸表（image/video 320×320，text 系 300×180，audio 300×160），优先取 data.width/height；
  - dagre ranksep=100/nodesep=60/margin 20，中心点→左上角减半宽高；
  - 自环边跳过（不参与分层）；reactive 入参剥壳（graph 内建副本，无 Proxy 泄漏）；
  - 锚定：新旧 bbox 左上角对齐后统一 16 网格取整。
- 新建 `src/utils/autoLayout.test.ts`：10 用例（LR 序不变量/分量不相交/有环不挂死/确定性/网格对齐/空与单节点/尺寸取 data/子图锚定+诱导边/TB 序）。

## 与 plan 偏差（已注明原因）

- plan 写「全集模式归一 0,0」→ 实现改**全图也锚定旧 bbox 左上角**（少一次视觉跳，规格超集不冲突）。已写入 autoLayout.ts 头注释与本进度。

## 验证

- `vitest run src/utils/autoLayout.test.ts` → 1 file / 10 tests 全绿。
- `vue-tsc --noEmit` → 0 错（首跑 bboxOf 未用变量 TS6133 已清）。

## 下一步

- Chunk 2：CanvasBoard 接「✨ 一键整理」按钮（范围裁决/组拉入/单步历史/fitView）。
