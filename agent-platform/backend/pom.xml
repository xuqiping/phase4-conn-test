<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.5</version>
        <relativePath/>
    </parent>

    <groupId>com.superprogrammer</groupId>
    <artifactId>agent-platform</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <name>agent-platform</name>
    <description>多Agent智能体平台后端</description>

    <properties>
        <java.version>17</java.version>
        <mybatis-plus.version>3.5.5</mybatis-plus.version>
        <jjwt.version>0.12.5</jjwt.version>
        <tika.version>2.9.2</tika.version>
        <!-- POI：Excel sheet 级解析（Tika 2.9.2 已传递引入 5.2.x，显式 pin 防漂移） -->
        <poi.version>5.2.5</poi.version>
        <!-- javacv：画布视频抽帧/截取（plan C11/C12 R-2）。仅用 ffmpeg 子模块（Grabber/Recorder），
             故引 javacv(API) + ffmpeg-platform(全平台 native)，跳 opencv 全家桶瘦身。jar 重(~150MB)但纯 Java 无系统依赖。
             注意 bytedeco 版本号规则：javacv 本体=1.5.13；ffmpeg-platform=<ffmpegVer>-<javacvVer>=8.0.1-1.5.13。 -->
        <javacv.version>1.5.13</javacv.version>
        <ffmpeg-platform.version>8.0.1-1.5.13</ffmpeg-platform.version>
        <!-- mvn test 默认排除 integration 组；跑集成测：mvn test -Dsurefire.excludedGroups= -Dtest=XxxIT -->
        <surefire.excludedGroups>integration</surefire.excludedGroups>
    </properties>

    <dependencies>
        <!-- Spring Boot Starters -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-aop</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-websocket</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webflux</artifactId>
        </dependency>

        <!-- MyBatis-Plus -->
        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
            <version>${mybatis-plus.version}</version>
        </dependency>

        <!-- PostgreSQL -->
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- Flyway -->
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>

        <!-- JWT -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>${jjwt.version}</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>

        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Jackson for Java 8 Date/Time -->
        <dependency>
            <groupId>com.fasterxml.jackson.datatype</groupId>
            <artifactId>jackson-datatype-jsr310</artifactId>
        </dependency>

        <!-- Apache Tika：企业文档抽取（pdf/docx/pptx/xlsx 等）。pin 2.x，3.x 升 slf4j/jackson major -->
        <dependency>
            <groupId>org.apache.tika</groupId>
            <artifactId>tika-core</artifactId>
            <version>${tika.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.tika</groupId>
            <artifactId>tika-parsers-standard-package</artifactId>
            <version>${tika.version}</version>
        </dependency>

        <!-- Apache POI：Excel sheet 级解析（xlsx=XSSF / xls=HSSF）。pin 5.2.5，与 Tika 2.9.2 传递的 5.2.x 同主线。 -->
        <!-- 不移除 Tika —— pdf/docx/pptx/html 仍走 Tika。 -->
        <dependency>
            <groupId>org.apache.poi</groupId>
            <artifactId>poi-ooxml</artifactId>
            <version>${poi.version}</version>
        </dependency>

        <!-- jieba-analysis：中文分词（RAG BM25 词法兜底，Phase2）。Apache-2.0，无 native 依赖 -->
        <dependency>
            <groupId>com.huaban</groupId>
            <artifactId>jieba-analysis</artifactId>
            <version>1.0.2</version>
        </dependency>

        <!-- jsoup：联网搜索自建引擎抓网页 + 正文抽取（BuiltIn SearchProvider）。MIT。
             CVE 复核：1.18.3 为 2026-07 当前稳定线，无未修复 critical（引入前/升级时再核 CVE）。 -->
        <dependency>
            <groupId>org.jsoup</groupId>
            <artifactId>jsoup</artifactId>
            <version>1.18.3</version>
        </dependency>

        <!-- javacv：画布视频抽帧/截取（plan C11/C12 R-2，VideoFrameService 唯一调用方）。
             sidecar 禁 subprocess（AGENTS），故走后端纯 Java。CVE 复核：1.5.13 为 2025 稳定线，引入前/升级时再核。
             瘦身三招（avoid ffmpeg-platform 全平台 ~500MB 撑爆磁盘/镜像）：
             1) javacv POM 默认传递 opencv/openblas/artoolkitplus/flycapture/libfreenect/realsense/tesseract/leptonica/videoinput
                及 jogl/jocl/gdx（GUI/3D/OCR/深度相机）——抽帧只用 ffmpeg，全部 exclude；
             2) 不引 ffmpeg-platform 全家桶（含 android/mac/linux/win × arm64/x86_64 全 native），按需引单平台 classifier；
             3) dev=windows-x86_64；**部署 linux 服务器须追加 <classifier>linux-x86_64</classifier> 依赖**（见 README 部署节）。
             bytedeco 版本号规则：javacv=1.5.13；ffmpeg=<ffmpegVer>-<javacvVer>=8.0.1-1.5.13。 -->
        <dependency>
            <groupId>org.bytedeco</groupId>
            <artifactId>javacv</artifactId>
            <version>${javacv.version}</version>
            <exclusions>
                <exclusion><groupId>org.bytedeco</groupId><artifactId>openblas</artifactId></exclusion>
                <exclusion><groupId>org.bytedeco</groupId><artifactId>opencv</artifactId></exclusion>
                <exclusion><groupId>org.bytedeco</groupId><artifactId>flycapture</artifactId></exclusion>
                <exclusion><groupId>org.bytedeco</groupId><artifactId>libdc1394</artifactId></exclusion>
                <exclusion><groupId>org.bytedeco</groupId><artifactId>libfreenect</artifactId></exclusion>
                <exclusion><groupId>org.bytedeco</groupId><artifactId>libfreenect2</artifactId></exclusion>
                <exclusion><groupId>org.bytedeco</groupId><artifactId>librealsense</artifactId></exclusion>
                <exclusion><groupId>org.bytedeco</groupId><artifactId>librealsense2</artifactId></exclusion>
                <exclusion><groupId>org.bytedeco</groupId><artifactId>videoinput</artifactId></exclusion>
                <exclusion><groupId>org.bytedeco</groupId><artifactId>artoolkitplus</artifactId></exclusion>
                <exclusion><groupId>org.bytedeco</groupId><artifactId>leptonica</artifactId></exclusion>
                <exclusion><groupId>org.bytedeco</groupId><artifactId>tesseract</artifactId></exclusion>
                <exclusion><groupId>org.bytedeco</groupId><artifactId>android</artifactId></exclusion>
                <exclusion><groupId>org.jogamp.gluegen</groupId><artifactId>gluegen-rt-main</artifactId></exclusion>
                <exclusion><groupId>org.jogamp.jogl</groupId><artifactId>jogl-all-main</artifactId></exclusion>
                <exclusion><groupId>org.jogamp.jocl</groupId><artifactId>jocl-main</artifactId></exclusion>
                <exclusion><groupId>com.badlogicgames.gdx</groupId><artifactId>gdx</artifactId></exclusion>
            </exclusions>
        </dependency>
        <!-- ffmpeg Java 绑定（avcodec/avformat 等 API 类）。 -->
        <dependency>
            <groupId>org.bytedeco</groupId>
            <artifactId>ffmpeg</artifactId>
            <version>${ffmpeg-platform.version}</version>
        </dependency>
        <!-- ffmpeg Windows x86_64 native（dev/本机跑测试用；自含 .dll，传递拉 javacpp windows native）。 -->
        <dependency>
            <groupId>org.bytedeco</groupId>
            <artifactId>ffmpeg</artifactId>
            <version>${ffmpeg-platform.version}</version>
            <classifier>windows-x86_64</classifier>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.squareup.okhttp3</groupId>
            <artifactId>mockwebserver</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.projectreactor</groupId>
            <artifactId>reactor-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <!-- mvn test = 单测 only；mvn test -Dsurefire.excludedGroups= -Dtest=XxxIT = PG+pgvector 集成测 -->
                    <excludedGroups>${surefire.excludedGroups}</excludedGroups>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
