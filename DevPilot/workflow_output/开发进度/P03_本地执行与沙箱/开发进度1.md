# 开发进度 1 · P03 本地执行与沙箱

> 接 P02 交付后，新会话 `/phase2 P03_本地执行与沙箱` 过审，进入 `/phase3` 实现。

## 已交付 chunk

### B1 = Step 0 还债 + 规格漂移修回（commit `git log -1`）
- 修复 `pricing.ts` 输入侧计费向上取整（与 output 一致），补 e2e 断言。
- `token_ledger` 新增 `note` 字段（V4__ledger_note.sql）；充值备注从 `model` 迁移到 `note`；`billing.controller` 明细 VO 透传 `note`。
- 同步更新 `db_schema.md`、P02 Feature Map、P03 plan Step0 勾✅。
- 验证：cloud unit 3 + e2e 40/40；check_all 全绿。

### B2 = Step 1 core-sandbox 目录白名单与越界判定（FR-001）
- 新增 `path.rs`：`normalize`/`safe_join`/`resolve_logical`，处理 `..`、符号链接、不存在路径回退。
- 新增 `policy.rs`：`SandboxPolicy` + `Decision`，默认 deny 敏感目录（`~/.ssh`、系统目录）。
- 单元测试 9 个：允许/拒绝/符号链接逃逸/`../` 拒绝/绝对路径拒绝。
- 验证：`cargo test -p core-sandbox` 9/9；fmt/clippy 0 错；check_all 全绿。

### B3 = Step 2 core-exec 命令执行器（FR-003 地基）
- 新增 `exec.rs`：`ExecRequest`/`ExecResult`，tokio 异步执行，stdout/stderr 捕获（8KB 尾截断），timeout，进程树 kill（sysinfo + libc/taskkill）。
- 单元测试 5 个：echo、env 注入、非零退出、超时杀。
- 验证：`cargo test -p core-exec` 5/5；fmt/clippy 0 错。

### B4 = Step 3 两档审批模式（FR-009）
- 新增 `core-sandbox/src/approval.rs`：`ApprovalGate`/`ApprovalPolicy`（Auto/Manual）/`Decision`/`RiskLevel`，危险命令清单（rm/del/curl|sh/mkfs 等）。
- 新增 `core-state/migrations/L4__pending_approvals.sql` 与 `core-state/src/approval.rs`：审批记录创建/列出/解决。
- Tauri 命令：`create_approval`、`list_unresolved_approvals`、`submit_approval`；`src/lib.rs` 注册。
- 单元测试：core-sandbox approval 6 个 + core-state approval 1 个。
- 验证：cargo test 核心全绿；check_all 全绿。

### B5 = Step 4 环境探测与画像缓存（FR-005/AC-005）
- 新增 `core-exec/src/probe.rs`：识别 Node/Python/Rust/Go/Ruby/Php 技术栈，收集版本与 lockfile hash。
- 新增 `core-exec/src/profile.rs`：`get_cached` / `probe_and_cache`，key = `(path_hash, lockfile_hash)`，命中则跳过文件系统探测。
- 新增 `core-state/migrations/L5__env_profiles.sql` 与 `core-state/src/env_profile.rs`：本地 SQLite 缓存表。
- 单元测试 9 个：空项目/Node/Python/混合识别、缓存命中跳过探测。
- 验证：`cargo test --workspace` 全绿；check_all 全绿。

### B6 = Step 5 一键安装向导（FR-005/AC-006）
- 新增 `core-exec/src/install.rs`：`InstallPlan` / `InstallStep`，按缺失技术栈生成平台相关安装命令（Windows winget/scoop、mac brew、Linux apt），`run_plan` 串行执行并回调输出。
- 新增桌面 UI `src-ui/components/install-wizard/InstallWizard.tsx`：展示缺失项、安装命令、一键安装按钮、逐步骤结果。
- Tauri 命令 `install_plan` / `install_runtime` 注册到 `src-tauri/src/lib.rs`；DTO 转换在 `commands.rs`。
- 修复 gate 故障：`commands.rs` 中 `query_row` 未将 `rusqlite::Error` map 到 `DbError`；`cargo fmt` 修复 core-exec 与 commands 格式。
- 验证：core-exec 12 单测全绿；前端 29 单测全绿；cloud e2e 40/40；check_all 全绿。

### B7 = Step 6 全流程闭环 v1（FR-003/AC-004）
- 新增 `core-exec/src/runner.rs`：
  - 沙箱文件读写 `read_project_file` / `write_project_file`（走 `SandboxPolicy`，禁止越界）。
  - `run_task` 阶段：`write → probe → install → run → fix → commit`。
  - 测试/lint 命令自动识别 Node/npm/pnpm/yarn/bun、Rust cargo、Python pytest、Go test；支持 `test_command` 覆盖。
  - 失败修复循环：通过 `FixStrategy` trait 注入，默认 `NoOpFixStrategy`；MVP 预留 LLM 策略接口。
  - 成功后 `git commit -m "devpilot: <title>"`，失败后 `devpilot-failed: <title>``，都带 `--no-verify` 避免 hook 干扰。
- Tauri 命令：`read_project_file`、`write_project_file`、`run_task` 注册到 `src-tauri/src/lib.rs`。
- 前端 UI `src-ui/components/task-runner/TaskRunnerPanel.tsx`：一键启动、阶段/测试结果/diff 摘要展示。
- `core-exec/Cargo.toml` 增加 `core-sandbox`、`thiserror`；`devpilot/Cargo.toml` 增加 `tokio`。
- 单元测试 5 个：文件读写允许/拒绝、测试通过、失败存档点、修复策略恢复。
- 验证：core-exec 17 单测、workspace 62 单测、前端 29 单测、cloud e2e 40/40；check_all 全绿。

### B8 = Step 7 Secrets 管理（FR-012/AC-014）
- 新增 `core-state/migrations/L6__secrets.sql`：`secrets` 表存名称，`encrypted_value` BLOB 作为 keyring 失败时的回退。
- 新增 `core-state/src/secrets.rs`：save/list/load/delete；值优先 OS keyring，失败时 AES-256-GCM 加密落库；密钥派生自设备指纹（hostname+username SHA-256），可通过 `DEVPILOT_MASTER_KEY` 覆盖。
- 新增 `core-exec/src/secrets.rs`：`MaskedSecret` / `inject_env`（注入 `DEVPILOT_SECRET_<NAME>`）/ `redact`（大小写变体替换为 `***`）。
- `runner.rs` 集成 secrets：测试命令 env 注入、stdout/stderr 返回前脱敏。
- Tauri 命令：`save_secret` / `list_secrets` / `load_secret` / `delete_secret`；注册到 `src-tauri/src/lib.rs`。
- 前端 UI `src-ui/components/secrets/SecretsPanel.tsx`：增删改查、密码输入框、列表仅显示名称。
- `core-state/Cargo.toml` 增加 `keyring`、`aes-gcm`、`sha2`、`whoami`、`rand`；`core-state` 迁移清单更新至 L6，`migrate_is_idempotent` 断言更新。
- 单元测试：core-state secrets 3 个（roundtrip/overwrite/加密不落明文）；core-exec secrets 3 个（注入/脱敏/空值）；runner 1 个（注入+脱敏端到端）。
- 验证：core-exec 21 单测、core-state 12 单测、workspace 66 单测、前端 29 单测、cloud e2e 40/40；check_all 全绿。

### B9 = Step 8 状态机集成（FR-003 + 状态机联动）
- 新增 Tauri 命令 `execute_build`：仅允许当前阶段为 `build` 时调用。
- 自动取项目最新 `open` 轮次，没有则新建一轮；创建 `tasks` 记录（status=running）。
- 调用 `core_exec::run_task` 跑本地闭环（注入 secrets、脱敏输出）。
- 回写任务结果：`tasks.status`（done/failed）、`tokens_actual`（cost_cents）、`checkpoints`（git_commit + diff_summary）。
- 成功后调用 `PersistentMachine::transition("accept", "devpilot")` 并同步 `projects.current_phase`；失败则留在 `build` 阶段并返回 warning。
- 前端 UI `src-ui/components/build/BuildButton.tsx`：仅在 `build` 阶段显示，一键触发并刷新状态。
- `devpilot/Cargo.toml` 增加 `rusqlite` 以使用 `OptionalExtension`。
- 验证：workspace 66 单测、前端 29 单测、cloud e2e 40/40；check_all 全绿。

### B10 = Step 9 冒烟、文档、CI 与归档
- 产出 Feature Map：`workflow_output/docs/feature-map/P03_本地执行与沙箱.feature-map.md`（功能-代码映射 + 表注解 + 联动点 + 运维要点）。
- 产出 User-Ops：`workflow_output/docs/user-ops/P03_本地执行与沙箱用户操作手册.md`（环境安装 / Secrets / 执行构建 / 审批 / 故障排查）。
- 产出 README：`workflow_output/开发进度/P03_本地执行与沙箱/README.md`（交付清单、测试成绩、已知限制、下一张 plan）。
- 更新 `workflow_output/docs/specs/db_schema.md`：补充 `pending_approvals` / `env_profiles` / `secrets` 表及 L4~L6 迁移版本。
- 更新 `workflow_output/docs/plans/00_MVP实现计划总索引.md`：P03 标记 ✅ 已交付，下一张 P04 标记 🟨。
- 全部 plan Step0~Step9 勾选完成。
- 验证：`check_all` 全绿；`check_docs` 无 FAIL（仅有 P01 plan 接近 token 上限的 WARN，属于历史文档）。

## 环境踩坑

- **D 盘空间满**：`cargo test --workspace` 编译 target 把 D 盘撑爆（369G 全满）。解决：临时 `cargo clean` 释放 10G+，后续设置 `CARGO_TARGET_DIR=/e/devpilot-target`（E 盘空闲 700G+）跑 gate。
- **迁移版本号覆盖**：新增 L4 时不慎覆盖了 L3，导致 `migrate_is_idempotent` 断言失败；已修复为 L1/L2/L3/L4 顺序。
- **`query_row` 类型不匹配**：Tauri 命令里直接用 `?` 返回 `rusqlite::Result`，但 `Db::read` 要求 `DbResult`；需 `.map_err(Into::into)`。
- **runner 里 async 调用 `Db::write`**：`Db::write` 是同步锁闭包，内部无法直接 `.await`；用 `tokio::task::block_in_place` + `Handle::current().block_on` 把异步任务闭包化。
- **keyring 在测试环境不可用**：`set_password` 返回 Ok 但 `get_password` 找不到条目；回退到 AES-256-GCM 加密落库，保证测试与无 keyring 后端环境可用。
- **`build` 目录被 gitignore**：新增 UI 组件目录 `build` 与通用构建目录冲突；重命名为 `build-trigger` 后正常提交。

## 下一步

进入 P04_工作流主链：想法需求计划（FR-030/031/032/044/008）。
