# P06_验收与安全卡点 · 开发进度 1

> 本轮完成 Phase 3 启动：人工测试方案 + S0 数据模型与迁移。

## 已完成工作

### A. 人工交互测试方案
- 产出：`workflow_output/docs/测试方案/P06_验收与安全卡点测试方案.md`
- 覆盖 AC-036/037/044/057/058，以及正/反/半选/批量联动用例。
- `check_docs.py` 无 FAIL。

### S0 数据模型与迁移（L9）
- 新增迁移：`PROJECT/desktop/src-tauri/crates/core-state/migrations/L9__acceptance_security.sql`
  - `acceptance_items`：验收清单项。
  - `acceptance_runs`：验收/冒烟/安全扫描运行记录。
  - `security_scans`：安全扫描结果。
- `core-state/src/db/migrate.rs` 注册 L9。
- `core-state/src/lib.rs` 的 `migrate_is_idempotent` 测试断言扩展到 L9。
- 更新 `workflow_output/docs/specs/db_schema.md`，新增 P06 三张表及 L9 迁移说明。

## 质量门结果

- `cargo fmt --all`：通过。
- `cargo clippy --workspace --all-targets -- -D warnings`：通过。
- `cargo test --workspace --quiet`：94 passed。
- `npx tsc --noEmit`：通过。
- `npm run test`：40 passed。
- `python scripts/check_docs.py --quiet`：无 FAIL。

## 下一步

- S2/S3：测试方案解析器 + 验收清单持久化。

## S1 门禁与发布入口

- `src-tauri/src/commands.rs`：新增 `enter_acceptance` / `request_release` / `transition_to` 辅助函数。
  - `enter_acceptance`：build → accept；L2/L3 未过 security 门禁返回 `GATE_BLOCKED`；L0/L1 自动放行。
  - `request_release`：accept → deploy；校验 `acceptance_items` 全部为 pass/na；L0/L1 无 deploy 阶段时提示无需发布。
- `src-tauri/src/lib.rs`：注册 `enter_acceptance`、`request_release`。
- `src-ui/lib/ipc.ts`：增加 IPC 封装。
- `src-ui/stores/project.ts`：增加 `enterAcceptance` / `requestRelease` actions。
- `src-ui/views/Build.tsx`：「进入验收」按钮改调 `enterAcceptance`。

## 质量门结果（含 S1）

- `cargo fmt --all` / `cargo clippy --workspace --all-targets -- -D warnings`：通过。
- `cargo test --workspace --quiet`：94 passed。
- `npx tsc --noEmit` / `npm run test`：40 passed。
- `python scripts/check_docs.py --quiet`：无 FAIL。

## 下一步

- S2/S3：测试方案解析器 + 验收清单持久化。

## S2/S3 测试方案解析与验收清单持久化

- `core-orchestrator/Cargo.toml`：新增 `regex` 依赖。
- `core-orchestrator/src/acceptance/parser.rs`：
  - 解析 `workflow_output/docs/测试方案/*.md`；兼容 `TC-XX` / `用例 N` / 数字标题。
  - 自动识别「自动化/Playwright/测试命令」为 auto，其余为 manual。
  - 提取 `steps` / `expected` 段落；无匹配时兜底为单条人工项。
- `core-orchestrator/src/acceptance/mod.rs`：导出 parser / smoke 模块。
- `core-orchestrator/src/acceptance/smoke.rs`：S8 占位。
- `core-state/src/acceptance_checklist.rs`：
  - `AcceptanceItem` / `NewAcceptanceItem` / `AcceptanceStats`。
  - `regenerate` / `list` / `get` / `update_status` / `set_fix_task` / `find_by_fix_task` / `stats`。
- `src-tauri/src/commands.rs`：新增 `get_acceptance_checklist` / `regenerate_acceptance_checklist` / `update_acceptance_item` + `AcceptanceItemDto`。
- `src-tauri/src/lib.rs`：注册新命令。
- `src-ui/lib/ipc.ts`：增加 `AcceptanceItemDto` 与 IPC 方法。

## 质量门结果（含 S2/S3）

- `cargo fmt --all` / `cargo clippy --workspace --all-targets -- -D warnings`：通过。
- `cargo test --workspace --quiet`：99 passed。
- `npx tsc --noEmit` / `npm run test`：40 passed。
- `python scripts/check_docs.py --quiet`：无 FAIL。

## S4/S5 安全扫描引擎与门禁（已完成，commit 4fd4a1b）

### 已写代码（FR-040 / AC-044）

- `core-orchestrator/src/security_scanner.rs`（新文件）：
  - `SecurityScanner::scan()`：本地静态扫描，覆盖 密钥硬编码（sk-/AK/私钥/ghp_/password 等）、鉴权缺失启发式（`@Get/Post...` 附近 300 字符无 guard）、数据库/端口暴露（`0.0.0.0` / `DATABASE_URL` / 常见端口）。
  - 依赖审计：`npm audit --json` / `cargo audit --json`（不可用时降级 info finding，不拦截）。
  - 跳过 test/spec/example/fixtures 文件、二进制后缀、>1MB 文件、node_modules/target 等目录。
  - 密钥 finding 的 message 脱敏（前 8 位 + 后 4 位）；含 5 个单元测试。
- `core-state/src/security_scan.rs`（新文件）：`security_scans` 表 `insert` / `latest` + 测试。
- `core-state/src/acceptance_run.rs`（新文件）：`acceptance_runs` 表 `start` / `finish` / `latest` + 测试。
- `core-state/src/lib.rs`：注册 `acceptance_run` / `security_scan` 模块。
- `core-orchestrator/src/lib.rs`：注册 `security_scanner` 模块。
- `src-tauri/src/commands.rs`：新增 `run_security_scan` 命令 + `SecurityFindingDto` / `SecurityScanDto`；写 `security_scans` + `acceptance_runs(kind='security')`；L2/L3 且 pass 时自动解锁 security 门禁并 emit state。
- `src-tauri/src/lib.rs`：注册 `run_security_scan`。
- `src-ui/lib/ipc.ts`：`SecurityFindingDto` / `SecurityScanDto` + `runSecurityScan` 封装。

### 阻塞已解除（2026-08-20）

- 根因不止正则：`HashSet<'static str>` 缺 `&`、测试里 `use std::io::Write` 未用、`TempDir` move-after-borrow、死代码 `NpmVuln`、`push_finding` 8 参超限（加 `#[allow]`）、`scale` 字段未读（L0 跳过依赖审计，合理语义化）。
- 正则改 `r#"..."#` raw string，去转义引号；脱敏测试断言改为「前 8 位 + 后 4 位」。

### Accept 视图安全面板（commit 4fd4a1b 后续 amend 同批）

- `src-ui/views/Accept.tsx`：扫描按钮 + 结果状态 + findings 列表（severity 中文徽章、file:line、可展开 snippet 与建议）+ L2/L3 强制卡点提示 + gate_passed 解锁徽章。
- `src-ui/views/Accept.test.tsx`：2 用例（AC-044 扫描展示 / 错误大白话）。

### 质量门结果（S4/S5 + 安全面板）

- `cargo fmt --all` / `cargo clippy --workspace --all-targets -- -D warnings`：通过。
- `cargo test --workspace --quiet`：106 passed。
- `npx tsc --noEmit`：通过；`npm run test`：42 passed（9 文件）。
- `python scripts/check_docs.py --quiet`：无 FAIL。

## 下一步

1. S6/S7/S8：预览窗格 + 元素圈选修复任务 + Playwright 冒烟 runner。
2. S9/S10：Accept 视图整合（验收清单 + 发布按钮）+ 联动与发布校验。
3. S11/S12：测试 + Feature Map / User-Ops / README + 索引更新。
