# 已解决
1. 项目可以授权给另外的项目，但是项目怎么授权给个人，个人又怎么申请项目的授权？（已解决）
2. 我没办法从流水账或者任何地方看到该条流水账记忆被哪个项目收录了（输出已经标记被谁收录了，输入没有）（已解决：V100 全链修 + 第三轮 V125 存量回填已应用 `fde4c168`）
5. 如果项目被删除，如果是包含了其他项目的项目被删除，将会发生什么？（已解决：MemoryLifecycleHookService.onProjectDeleted ①~⑨ 级联清理+双向 links；嵌套只删 link 行不动对方项目，raw 流水账不碰）

# 未复测
3. 标签现在改为可扩充的模式了，在标签库中需要添加一个增加标签的按钮，我可以自行增加标签，以后有符合的，就落该标签（即现在为主动+被动两种方式添加标签，原被动方式不变），并且需要增加一个重新总结的按钮（总结范围可以筛选，按照创建时间、标签筛选），以便我创建了新标签后，可以再重新进行标签归类，而且总结还要区分输入input和输出output，而不是统一在一条里面，因为有些输出并不一定是我想要的
3. 上传文件后，无法读取到附件进行联动
4. 在项目的总结中，还是无法看到到底是来源于输入的总结，还是输出的总结（已修待复测：第三轮 V126 `memory_project_entries.direction` 四向打通——写侧路由按轮方向落 INPUT/OUTPUT/BOTH、读侧总结按方向过滤、coverage 按方向幂等 `eb65eec6`；总结列表方向徽标与个人通道同口径）
7. 如果是符合被收录的，那就不要再用LLM给我一大堆输出了，而是简单的回答，你输入的xxx，已被xx项目项目收录了，请问是否需要我对你进行回答；如果用户说需要，再根据用户的输入进行对应的输出（已修待复测：第三轮确认式回复 MVP `b768261f`——附件命中项目硬规则秒回模板确认+「需要回答/不用了」点选，测试方案见 docs/测试方案/收录命中确认式回复测试方案.md）
8. 收录规则中，文命名硬规则，如果新增的文件命名规则，跟以前其他项目有重复的，需要提醒该文件硬规则已被XX项目收录，如果需收录进该项目，需先删除XX项目的该收录规则（不删除则不能收录进该项目，用户+收录命名硬规则需要唯一）（已修待复测：第三轮用户级 pattern 查重 409 + 路由多命中先建规则赢 `586847a8`）

# 未解决
（无——四轮修复全部落地，待人工复测，见下方「已修待复测·第四轮」）

# 已修待复测·第四轮（2026-08-17，V129/V130）
- 文件记忆没有标注归属于哪个项目 → 已修：文件记忆列表行内「收录于：项目X」徽标（三重域 JOIN 反查；无收录不显示；非成员项目名不透出）`4bae4055`
- 文件记忆（页签可达性：需滚轮向右才见）→ 已修：面板九页签两行布局免横滚，「文件记忆」提序第 3 位；小屏退方案=箭头+自动滚动 `54c6c39b`
- 当我问问题的时候，与问题无关的文件也会被引用出来……不应该直接不召回或者完全不引用吗？ → 已修（拍板②宁缺勿噪）：文件卡片向量门控——候选分块与 query 距离 > `memory.recall.file-card-max-distance`（默认 0.5，热调）零卡片；每文件 ≤2 总 ≤5；另做附件定向召回（本附件 READY 注入 top-2 块+「📎 本附件」置顶卡，ragOn=false 不豁免拍板④）`6ad65703` + `b2e38300`
- 超时时长设置为1200s → 已修：SSE_TIMEOUT 600→1200s `26fa459`
- 对于生成的结果和提问的内容，需要分别有复制按钮 → 已修：气泡思考/回答各自复制按钮（流式中不出现）`26fa459`
- 需要可以暂停来停下正在进行的对话任务 → 已修（拍板③）：流式「⏹ 停止生成」按钮，abort 上游取消+已生成部分落库保留「已停止」、连接死不 REST 重答（防双计费）；DONE 前有效、停止后不可再停 `fb6f18e6`
- 标签重新归类（顺带，拍板⑤）：标签库「重新归类」——范围筛选（默认建标签前）+dryRun 预估+≤20/批 LLM 判定，tag_ids 只增不删，总结到「总结」页签手动重压生效；限流 3/min `906519ac`
- 人工复测走 docs/测试方案/记忆5x第四轮问题修复测试方案.md（U-1~U-7 联动用例）


# 待决定
6. 自动总结我没有看到哪里有了而且我看gen矩阵里，开关都开了，也只写raw，不自动总结了
   - 调查结论（2026-08-16 查库+代码）：三个缺口叠加——①自动总结开关默认关、入口在总结弹框（不在 gen 矩阵），user 1 两个 scope auto_enabled=f → worker 永不认领；②自动链路取数只吃 gen_done=true，raw 近 7 天 24 条/已生成仅 13 条，raw 须手动触发 backfill；③gen 矩阵开关只控制收录路由（MemoryRoutingService 唯一读取处），与个人生成/总结无关，概念易混淆。
   - 待拍板：A=worker 自动先 backfill 补生成再总结（保蒸馏质量，费 LLM）／B=raw 直接进自动总结（省，绕过三层蒸馏）／C=默认开自动+矩阵页说明+raw 跳过数透出+gen_done=false 落原因（最小改，推荐）。用户 2026-08-16 拍板：留后续处理。

# 参考feature-map、user-ops
- agent-platform\项目工程文档\项目功能介绍\速查表\09-个人记忆-设计与原理.md
- agent-platform\项目工程文档\项目功能介绍\速查表\09-个人记忆-演进与待办.md
- agent-platform\项目工程文档\项目功能介绍\速查表\09-个人记忆与冲突解决.md
- ~~feature-map、user-ops下还是老的个人记忆，都过时了，都需要重新生成了~~（已重写：三轮重生成 + 四轮增量回写完毕 2026-08-17）

>解决完后需要同步更新对应的feature-map和user-ops
