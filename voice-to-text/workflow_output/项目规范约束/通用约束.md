# 通用约束 · 跨所有模块

> 跨模块通用的硬性约定，在 [AGENTS.md](AGENTS.md) 中索引。单项约束膨胀时按模块/主题拆为 `XX约束.md`。
> **文档规模**：本文件不得超过 5000 tokens。

## 代码通用约定

- <如：命名规范、目录放置规则、公共组件/工具优先复用清单>

## 接口通用约定

- <如：响应统一封装格式、错误码规范、分页参数约定>

## 数据库通用约定

- <如：主键策略、逻辑删除字段、审计字段自动填充、迁移工具（Flyway）使用规则>

## 前端通用约定

- <如：UI 库与主题、状态管理、请求封装与鉴权注入>

## 运维通用约定

- **新功能默认带 feature flag**（2026-08-07 自网课总结 Step12 沉淀）：三层覆盖 = 环境变量 `VTT_<FEATURE>` > `<app_config_dir>/feature_flags.json` > 默认开；前后端双保险 = 前端隐藏入口 + 后端拒绝入口命令；配置缺失/损坏一律按「开」降级，不得因配置问题拖垮主功能。实现参照 `src-tauri/src/feature_flags.rs`。
- **日志 traceId**：网课总结功能 traceId = session_id，日志前缀 `[session][<id>]`；新功能沿用「可操作 ID 当 traceId」的约定。

## 变更记录

| 日期 | 变更 | 原因 |
|---|---|---|
| 2026-08-07 | 新增运维通用约定（feature flag 三层覆盖 + traceId 约定） | 网课总结 Step12 沉淀 |
