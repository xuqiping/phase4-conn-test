# 开发进度 1

> 日期：2026-08-30

## 完成内容

- 新增 `启动.ps1`、`关闭.ps1`、`重启.ps1`。
- 启动前监听 `6379/5432/8088/1420`，避免重复启动。
- 后端和 Tauri 分别使用可见 PowerShell 日志窗口。
- 关闭后等待端口释放，然后才允许重启继续。
- 每个入口均提醒管理后台未纳入脚本。

## 验证

- PowerShell Parser：三个脚本无语法错误。
- 模拟四个端口已监听：启动脚本全部跳过，未重复启动，退出码为 0。
- 管理后台提醒和 Tauri `--bin file-keeper` 命令静态断言通过。
- 未在本轮自动执行真实关闭/重启，避免中断当前数据库和开发进程。

## Windows PowerShell 5.1 编码修复

- 用 Windows PowerShell 5.1 Parser 复现三个 UTF-8 无 BOM 脚本的中文乱码与语法错误。
- 给 `启动.ps1`、`关闭.ps1`、`重启.ps1` 增加 UTF-8 BOM。
- Windows PowerShell 5.1 Parser 复验：三个脚本均为 `ERRORS=0`。
- Windows PowerShell 5.1 运行已启动监听分支：退出码为 0，中文提示无乱码。
