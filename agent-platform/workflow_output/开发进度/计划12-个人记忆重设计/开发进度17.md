# 计划12 · 个人记忆重设计 · 开发进度17

> **本轮：Phase4 真实环境冒烟（playwright-mcp + 真 PG/Redis/LLM）**。承 [开发进度16.md](开发进度16.md)（Phase3 完结）。
> 全栈实起 + admin/admin123 登录，走真 LLM（doubao-seed-2.0-code）写入/召回 + 7 页签 + 移动端。

## 一、环境（真起，非 mock）

- 起齐 PG(5432)/Redis(6379)/sidecar(8090)/backend(8080)/frontend(5173)。backend 7.35s 起。
- **Flyway 实跑应用 V52（回填成员）+ V53（DROP user_memories/user_memory_projects）**——主库从 v51 迁到 v53，V53 DROP 旧栈生效（验 E7 前置）。
- 登录 admin/admin123 → /chat 新会话，记忆模式切「本会话：开」，写目标「总记忆」，读范围「总记忆：开」。

## 二、真 LLM 写入 + 召回（E2 核心）

发「我叫记忆测试侠…最喜欢的编程语言是Rust…宠物叫煤球的黑猫」：

| 项 | 结果 | 证据 |
|---|---|---|
| 双侧落库 | ✅ | `memory_turns` id48(INPUT)+id49(OUTPUT)，direction IN/OUT，born_personal=t |
| gen_done | ✅ t | 双侧 gen_done=true（doubao 第3次重试成） |
| L0/L1/L2 三层 | ✅ | raw_content + l1_summary(概要) + l2_detail（「无具体时间/地点/对象/细节」结构化）全有 |
| 标签归一(L12) | ✅ | 新建 4 标签（id32-35），主体:主题 分组，「编辑」按钮齐 |
| raw fail-safe | ✅ | gen 超时期间 raw 先落，流水账 2/2 即可见 |

召回问「我叫什么名字？最喜欢什么编程语言？」：
- `MemoryRecallPipeline recall 完成 turnCount=2 degraded=false notes=0 departed=0 耗时 2500ms`——**召回 2 条注入正确**。
- 助手答「你叫记忆测试侠呀！最喜欢的编程语言是Rust😉…煤球的小黑猫」——**3 事实全中**（思考过程显式「看记忆流水…名字记忆测试侠」）。
- ⚠️ **耗时 2500ms 略超 E2 <2s 目标**（embedding 多次调用 + 标签选取），边界数据，非功能缺陷。

## 三、7 页签桌面渲染（全 ✅）

流水账(4/4，L0/L1/L2 显) / 标签库(4 标签，主体:主题/使用次数/编辑) / 总结(个人 scope+立即总结+空态「暂无总结」) / 冲突裁决(空) / gen 矩阵(2 项目 OWNER，owner 开关+我的覆写+生效中+一键关) / 项目 ACL(项目选择器+花名册矩阵说明) / 生命周期(已删除/已离开折叠板+说明文案)。`.n-tab-pane` 共 7，单可见切换正常。

## 四、E6 生命周期 UI（✅ 渲染，restore 留 setup）

生命周期 tab 渲染完整：说明文案（拉取=复制挂载，原项目零改动）+ 刷新 +「已删除项目（0）」「已离开项目（0）」折叠板。admin 无删除/离职数据 → **restore/copy-to 端到端需建项目+加成员+离职/删项目**，本轮未造数据，留下一轮。

## 五、E8 移动端 390px（底栏 ✅ / 面板 tab 栏 ❌ BUG）

视口缩 390×844：

| 区域 | 结果 | 证据 |
|---|---|---|
| ChatView 底栏 | ✅ | 记忆模式/联网/落库于/读取范围 全可见，无溢出裁切（截图+视觉核验） |
| 记忆面板弹框 | ⚠️ 宽度 90vw 合规，内容可读 | — |
| **面板 tab 栏** | ❌ **3/7 tab 不可达** | boundingRect：gen矩阵(left336/right394)/项目ACL(430-488)/生命周期(524-580) 全出视口(390)；`.n-tabs-nav` scrollWidth=offsetWidth=336 **无横滚** → gen矩阵/项目ACL/生命周期 移动端无法点选 |

**缺陷 P1**：MemoryManagerPanel 的 n-tabs 在 ≤390px 无横向滚动机制，末 3 个 tab 不可达。移动适配回归（F-7 移动扫荡漏了面板 tab 栏）。修复方向：n-tabs 加 `:wrap`/或在窄屏给 tab 栏 `overflow-x:auto`，或改下拉/栅格。

## 六、性能观察（非阻塞）

- **doubao-seed-2.0-code 结构化记忆生成 2/3 次超时**：`MemoryGenerator.generate:88` blocking read 30s timeout（非流式调用），重试第 3 次成。流式 chat 正常。→ gen 路径用非流式 + 30s 超时对慢模型偏紧，建议 gen 走流式 or 拉高超时 or 换快模型。

## 七、E5/E7 后端 IT（✅ 90/90 全绿）

`agent_platform_it` 库 drop+recreate 净，Flyway 从空库跑 V1→**V53（DROP legacy）**实跑成功，`mvn test -Dtest=Memory*IT` **BUILD SUCCESS，Tests run: 90, Failures: 0, Errors: 0**。

**E7 ✅ A-I4 PG-IT 回归**：V53 DROP 旧表后全链 IT 重跑绿——`MemoryDataLayerIT` 10（A-I4 数据层）+ ConsolidationDataLayer 12 + ConsolidationFlow 5 全过，旧表 DROP 不影响新栈。

**E5 ✅ 15 安全向量越权 IT**：`MemoryRosterAclConfigIT` 10 + `MemoryRecallAclIT` 4 + `MemoryLifecycleIT` 7（越权向量）+ `MemoryTurnViewIT` 5 + `MemoryGenerationWriteIT` 6 + `MemoryRecallScopePrefIT` 5 等 scope/成员交集/IDOR/ACL/ownership/prompt 注入向量全绿。

### 🐞 E7 触发的真实回归（已修）

首跑 95 测中 **`MemoryScopeInjectionIT` 5 测 ERROR**：`clean()` 引用 `user_memory_projects`+`user_memories`（V53 刚 DROP）→ `BadSqlGrammar`。查证：该 IT 是 **V33 旧栈遗留**（测已删端点 `POST /api/chat/memories/preview` + 插 `user_memories`），H'-3「删 legacy 簇 25+7 文件」漏删此 IT。**已 `git rm` 删除**（全 test 仅此 1 处引旧表，旧端点主码已无）→ 重跑 90/90 绿。补完 H'-3 legacy 清理。

> 注：`MemoryConflictMapper.findByUser is ignored`（method+XML 重复）启动告警全程在，**良性**（MyBatis 取 XML 优先），非缺陷，归 cosmetic。

## 八、Phase4 留项收口状态

| # | 项 | 本轮 | 余 |
|---|---|---|---|
| E1 | 真 LLM 日期铁律 | ⏳ 总结空未触发 | 手动立即总结跑一次 + 断言年份 |
| E2 | 召回全链 LLM IT | ✅ 功能通(2.5s) | <2s 性能优化 |
| E3 | departedAuthorNotes 端到端 | ⏳ 无离职数据 | 造离职+开关 |
| E4 | 双节点 SKIP LOCKED | ⏸ 未测 | 两实例 |
| E5 | 15 越权 IT | ✅ 90/90 绿 | — |
| E6 | 生命周期端到端 UI | ✅ 渲染 / ⏳ restore | 造数据 |
| E7 | A-I4 PG-IT 回归 | ✅ 90/90 绿（删 1 stale IT） | — |
| E8 | 移动端 390px | ✅ 已修 | `MemoryManagerPanel.vue` 加 `@media(max-width:640px)` tab 折行（`.n-tabs-wrapper{flex-wrap:wrap}` + 内容区限宽 + `.v-x-scroll` 溢出可见），playwright 390px 实测 7 tab 全可见 |

## 九、下一步

1. 修 E8 面板 tab 栏移动端横滚（P1，本轮发现）。
2. 补 E1（立即总结跑一次验日期铁律）+ E6（造离职/删项目 restore）。
3. 看 E5/E7 IT 结果补录。
