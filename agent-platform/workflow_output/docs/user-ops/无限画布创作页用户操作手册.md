# 无限画布创作页 · 用户操作手册

> 面向持有 `canvas:write` 权限的用户（创作型终端用户）。功能入口：侧边栏「无限画布」菜单（`/canvas`，无权限不显示，联系 admin 授权）。
> 关联 Feature Map：[../feature-map/无限画布创作页.feature-map.md](../feature-map/无限画布创作页.feature-map.md) · 功能 README：[../../开发进度/无限画布/README.md](../../开发进度/无限画布/README.md)

## 前置条件

- 账号已被授予 `canvas:write` 权限（默认仅 admin 有；普通用户须 admin 在「角色权限」页授予）。
- 想在画布上**生成视频**还需 `media:gen` 权限（视频节点复用视频生成能力；无权则视频节点生成 FAILED）。
- 系统已配置 doubao provider（Ark key + endpoint + seedance 模型）——由运维保证，用户无需关心。
- 桌面端使用（移动端 ≤768px 显示「请在电脑端使用」占位，节点画布不适合触屏）。

---

## 功能一：新建画布与拖节点

**操作步骤**：
1. 点侧边栏「无限画布」进入 `/canvas`，看到「我的画布」卡片网格。
2. 点右上「新建画布」→ 自动进入编辑模式（路由变为 `/canvas/{id}`），画布名为「未命名画布」。
3. 左侧调色板拖节点到画布：**文本 / 图片 / 视频 / 音频 / 脚本 / 分镜** 6 类。
4. 鼠标滚轮缩放，按住空白拖动平移；点节点选中（高亮辉光），再点空白取消。
5. 选中节点后右侧「属性面板」编辑该节点字段。
6. 顶部工具条：放大/缩小/适应视口；「重跑全链」按钮（见功能五）。
7. 改动后自动节流保存（停手 800ms 后落库）；也可手动 Ctrl+S 风格点击标题栏保存。右上「重命名」改名。

**完成标志**：画布列表多一张卡片（显示节点数 + 更新时间）；再次进入复原全部节点/连线/位置。

---

## 功能二：文本节点生成（提示词扩写）

1. 拖入「文本」节点，属性面板「提示词」填一段描述（如「赛博朋克街景，霓虹雨夜」）。
2. 点「运行」→ 节点徽标转 **running**（旋转）→ 完成变 **success**，节点内显示 LLM 扩写结果（4 行预览）。
3. 失败则徽标 **failed** 标红，鼠标悬停看原因。

**用途**：把简短提示词扩成完整描述，作为下游图片/视频节点的输入素材。

---

## 功能二·五：脚本节点拆分镜 → 分镜节点自动生成

> 把一段剧本交给大模型拆成多个分镜，每个分镜自动生成独立「分镜节点」挂在脚本节点右侧并连线（扇出）。

**操作步骤**：
1. 拖入「脚本」节点 → 选中 → 右侧属性面板填「剧本」（可 `@` 引用上游节点产出）。
2. （可选）设**拆分段数**：填数字（如 6）→ 大模型严格拆成 N 个分镜；留空则大模型自定（默认 3-10）。
3. （可选）填**拆分规范**：如「每镜含景别+运镜」；留空则大模型自由发挥。
4. （可选）选「模型」（默认后端回落）。
5. 点「拆分镜」→ 等待数秒，脚本节点右侧**扇出生成 N 个分镜节点**，每个自动连脚本节点（脚本→各分镜独立边），节点头显「分镜1/2/…」，正文是画面描述。
6. 点任一分镜节点 → 属性面板可**编辑画面描述**（微调后再下游生图/视频）。
7. **重拆**：改剧本/段数/规范后再点「拆分镜」→ 旧的自动生成的分镜子节点会被删除并按新参数重建（手动改过的分镜描述会丢，重拆前请确认）。

**下游用法**：在图/视频节点的提示词里 `@分镜N` → 该分镜的画面描述会注入提示词（生图/视频参考其画面）。

**存入资产库**：右键分镜节点 → 「存入资产库」→ 进**分镜**分类（正文=画面描述）。

**完成标志**：脚本节点右侧出现一列分镜节点，各连脚本，描述正确。

---

## 功能三：图片节点（上传 / 衍生）

> MVP 仅支持上传 + 衍生 + 焦点编辑。AI 生图待后续接入生图 provider。

1. 拖入「图片」节点。
2. 点属性面板「上传图片」→ 选本地图片 → 上传完成后节点显示缩略图。
3. **衍生**（不覆盖原图）：在原图基础上调整产新图（保留原图，新图自动连边到原图旁）。
4. **焦点编辑**：点节点进入沉浸模式 → 在图上框选要提取的元素 → 提交产新图节点（提取质量依赖模型能力，弱保底）。

---

## 功能四：视频节点（文生 / 图生）

1. 拖入「视频」节点，属性面板填提示词 + 选比例/时长/分辨率。
2. **图生视频**：先在该视频节点上游连一张图片节点（文本→图片→视频，或直接图片→视频连线）——画布会自动把上游图作为参考图（首帧）。
3. 点「生成」→ 徽标 running → 后台异步生成（通常 1-3 分钟）。
4. 生成中节点 5 秒轮询一次状态；**SUCCEEDED** 后节点内出现 `<video>` 预览。
5. 失败（如限流 429 / 超时）→ 徽标 failed 标红。

> 无 `media:gen` 权限：点生成直接 403，节点标 FAILED「无 media:gen 权限」。

---

## 功能五：连线 + 一键重跑

1. **连线**：鼠标悬停节点边缘出现连接锚点，从上游节点拖到下游节点松开即连（smoothstep 曲线）。图片→视频连线后，视频生成自动取上游图作参考图。**删连线**：单击连线选中（变红色加粗）→ 按 Delete 或 Backspace 删除。
2. **一键重跑**：顶部「重跑全链」→ 按**拓扑顺序**串行重跑所有可生成节点（文本/脚本/视频；上传型图片/音频跳过）。
3. 若画布**有环**（A→B→A）→ 重跑前报错提示「画布存在环，无法拓扑排序」，需先解开环。

---

## 功能六：视频抽帧（首/尾/指定秒）

> 需视频节点已生成（有 fileId）。

1. 选中视频节点 → 属性面板「抽帧」区。
2. 三选一：**首帧** / **尾帧** / **第 N 秒**（AT 模式填秒数输入框）。
3. 点对应按钮 → 后台用 javacv 取该帧 → 产一张新**图片节点**自动连到该视频节点旁，命名如「抽帧(AT 3s)」。
4. 秒数越界（超过视频时长）→ 提示错误，不产空节点。

---

## 功能七：视频截取（时间段裁剪）

1. 选中视频节点 → 属性面板「截取」区。
2. 填起止秒（两个数字输入框），点「截取片段」。
3. 后台裁剪 `[start, end)` → 产一段新**视频节点**自动连边，命名如「截取(2-5)」，徽标直接 success（可预览）。
4. 校验：起≥0 / 止>起 / 时长 ≤600 秒 / 不超原视频时长；违规提示具体错误。

> MVP 仅视频轨（截取片段静音，浏览器播放/下载正常）。

---

## 功能八：故事板（多段拼接成片）

1. 画布上有 ≥2 个已生成的视频节点后，点顶部「故事板」打开右侧抽屉。
2. 抽屉列出画布上所有视频段（带 label + 时长）。
3. **调序**：点段右侧 ↑↓ 调整播放顺序。
4. **顺序预览**：点「预览」→ 按当前顺序逐段播放，一段结束自动进下一段；点列表项可跳段。
5. **拼接成片**：点「拼接成片（N 段）」→ 后台逐段重编码进同一 mp4 → 产一个新**视频节点**（成片），命名如「成片(3段/12s)」。
6. 失败（如段尺寸不一致）→ 提示错误。

> **要求各段同源/同尺寸**（典型场景：同一视频的若干截取片段）。深度剪辑/转场/多轨是后续独立功能。

---

## 常见异常与恢复

| 现象 | 原因 | 恢复 |
|---|---|---|
| 看不到「无限画布」菜单 | 无 `canvas:write` 权限 | 联系 admin 在「角色权限」页授权 |
| 直接访问 `/canvas` 显示「无 canvas:write 权限」 | 同上（三重兜底第②层） | 同上 |
| 视频节点生成 FAILED「无 media:gen 权限」 | 有画布权限但无视频生成权限 | 联系 admin 授 `media:gen` |
| 视频节点 FAILED「Ark 429 限流」 | Ark 配额超限 | 稍后重试（重新点生成） |
| 视频节点 FAILED「任务超时」 | Ark 生成超 10 分钟 | 重新生成；持续超时联系运维 |
| 抽帧/截取提示「秒数越界」 | N 秒超过视频时长 | 改小秒数后重试 |
| 截取提示「时长超 600s」 | 单片段超上限 | 拆成多段分别截取 |
| 拼接失败「段尺寸不一致」 | 各段非同源/分辨率不同 | 用同一视频的截取片段拼接 |
| 重跑报错「画布存在环」 | 节点连线成环 | 解开环（删一条边）后重跑 |
| 图片预览刷新后失效 | blob 预览 URL 跨会话失效（正常） | 重新进入画布会自动按 fileId 重取预览 |
| 节点标红「上游缺失」 | 上游图/视频被删或失败 | 重跑或重连上游 |

## 边界场景

- [ ] 编辑中刷新页面：本地未落库的改动会丢（停手 800ms 后才落库；刷新前最好手动保存）。
- [ ] 删画布：画布软删，**产出物（图/视频）保留**在文件存储（不级联清，供历史复用）。
- [ ] 删节点：连带删该节点的连线；下游节点参考图失效会**标红**（不自动删下游，保留调整权）。
- [ ] 大画布（节点多）：Vue Flow 内置节点虚拟化，100 节点级加载/编辑流畅（Phase4 验证基线）。

## 快捷键 / 辅助操作

| 操作 | 作用 | 适用场景 |
|---|---|---|
| 滚轮 | 缩放画布 | 浏览大画布 |
| ✋ 模式（默认）：左键拖空白 | 平移画布 | 移动视口 |
| ✋ 模式：按住 Shift + 拖空白 | 临时框选（不切模式） | 偶尔批量操作 |
| **▭ 模式：左键直接拖框** | **框选多个节点**（同 Windows 框选，免按 Shift） | 批量关联/批量生成/批量删除 |
| 点节点 + Delete | 删除选中节点（含其连线） | 清理 |
| 点连线 + Delete | 删除选中连线（选中时变红色加粗，再按 Delete/Backspace 删） | 解开环 / 清理冗余连线 |
| **Ctrl+Z / Ctrl+Shift+Z** | **撤回 / 重做**（输入框聚焦时不劫持；详见第五轮指引） | 误操作回退 |
| 点空白 / Esc | 取消选中（含清空框选集） | — |

> 两种交互模式用画布右下角工具条的 **✋（拖拽画布）/ ▭（框选节点）** 按钮切换，当前模式高亮。▭ 模式下拖拽不再平移画布（滚轮缩放不受影响），平移请切回 ✋。

## 截图占位

> 建议插入：`01-画布列表.png`、`02-编辑画布.png`、`03-故事板抽屉.png`、`04-抽帧截取面板.png`。

## 无限画布第三轮操作指引（2x，2026-08-16）

### 框选多节点 + 批量删除

1. **切到 ▭ 框选模式**（画布右下角工具条点 ▭，或保持 ✋ 模式按住 Shift）：**左键在空白处拖框**，框住的节点全部高亮选中；选 2 个及以上时画布顶部浮出**批量工具条**（一键关联 / 批量生成 / 批量删除）。
2. **批量删除**：点「批量删除」→ 二次确认（列明将删 N 个节点）→ 确认后节点连同其连线一起删除，改动自动保存。
3. 松开 Shift 单击任意节点 → 回到单选（右侧属性面板照常）；点空白或按 **Esc** → 清空选择、工具条消失。
4. **改进**：此前框选后按 Backspace 会触发库内置删除、应用界面无感知——现已接管，删除只走带确认的应用层入口，不会静默双删。

### 一键关联（按名字自动插 @）

> 选中多个**文本类**节点（文本/脚本/分镜）后，系统在其连线可达的上游节点里，找**名字（节点名或资产名）出现在该文本中**的，自动把该词替换成 `@引用`——效果与手动输入 @ 完全一致。

1. 框选（▭ 模式左键拖框，或 ✋ 模式 Shift+拖框）多个文本类节点 → 工具条点「**一键关联**」。
2. 弹出**提案预览表**：目标节点｜匹配到的上游（类型+名字）｜替换位置前后文（命中的词高亮）。默认全勾选，不想要的逐行取消勾选。
3. 点「应用」→ 文本里的名字变成蓝色 @引用块；点「取消」则原样不动。
4. **替换规则**（防误伤）：名字至少 2 个字；「主角1」「主角10」并存时**长名优先**互不误吞；已是 @引用的部分不会二次包装。
5. **无匹配**：不弹窗，仅提示（区分「没有可达上游」和「名字未在文本中出现」两种情况）。
6. 关联后运行节点、断链黄色警告、上游改名不断链——与手动 @ 的行为完全相同。

### 批量生成（多节点受控并发）

1. 框选多个可生成节点（文本/图片/视频）→ 点「**批量生成**」。
2. 系统按**连线先后顺序**（上游先提交）提交，同时最多 2 个在途（与平台每用户并发上限一致），完成一个自动补一个。
3. 画布上方浮出**进度条**：「已提交 n/m · 已完成 k · 失败 j」，可点关闭（关闭只隐藏浮条，任务继续跑）。
4. **撞并发上限（429）自动排队**：指数退避 1s/2s/4s 重试最多 3 次，仍失败才把该节点标红（其余节点不受影响，一个失败不阻断整批）。
5. **不可跑的节点会跳过并列出原因**（如图片节点没选模型、缺提示词）。
6. **中途刷新/离开**：每个节点提交瞬间已保存，回来打开画布自动恢复轮询（与单跑一致）；批量期间也可手动单独运行其他节点。

> 并发上限由管理员在系统设置调整（默认视频 2 / 图片 3），与你无关时按默认即可。

## 无限画布第五轮操作指引（2x，2026-08-20）

### 撤回 / 重做（本会话内）

1. **Ctrl+Z**（Mac 为 Cmd+Z）撤回上一步；**Ctrl+Shift+Z** 重做被撤回的。
2. 画布左上工具条也有 **↩ / ↪ 按钮**（灰=无可撤/无重做）。
3. **合并规则**：0.8 秒内连续同类小操作算一步（如连拖 3 个节点=按一次 Ctrl+Z 全退）；增删节点、增删连线各算独立步骤。最多保留最近 50 步。
4. 输入框打字时 Ctrl+Z 归输入框自己用，不会撤画布。
5. **边界**：撤回栈在内存里——关闭/刷新页面后消失（跨会话请用下面的「版本」）；打开别的画布或恢复版本后清空重新计。

### 版本保存 / 恢复（跨会话存档）

1. 编辑页头部点 **「版本」按钮**（Git 分支图标）弹出版本面板。
2. **存版本**：输入版本名（可空=自动用时间戳）→ 点「存版本」→ 当画布全貌存为一个存档点。
3. **恢复**：列表里点对应版本「恢复」→ 二次确认。**恢复前系统会自动把当前状态存为「恢复前自动存」**——恢复错了再恢复回来即可。
4. **删除**：列表点「删」清掉不要的存档。每画布自动保留最近 30 个，超出自动清最老的。
5. 恢复后：撤回栈清空（从恢复点重新开始）、节点预览自动回填、仍在跑的生成任务自动续接。

### 连线拖拽时途经节点看得清

选中节点高亮时其他节点半透明——现在**拖拽连线期间**，鼠标碰到的半透明节点会恢复不透明，松手后回到半透明，不会再看不清落点。

### 计费去向（页顶统一）

画布编辑头不再放「参与项目」选择器，改显一个**当前组名小标签**；切换请到**页面顶部右侧**的「参与项目」下拉（全局唯一入口，旁边常显「组池 / 个人」积分），选中组后画布内节点运行/媒体生成消耗组池。

## 变更记录

| 日期 | 变更 | 原因 |
|---|---|---|
| 2026-08-05 | 初版（C1-C13 落地，B 类用户创作面收尾） | 画布 MVP 收尾 |
| 2026-08-16 | +第三轮指引：框选批量/一键关联/批量生成 | 2x 第三轮问题修复 |
| 2026-08-16 | 框选改为 ✋/▭ 双模式：▭ 模式左键直接拖框（同 Windows），✋ 保留 Shift 临时框选 | 用户反馈：不要 Shift 组合键方式 |
| 2026-08-20 | +第五轮指引：撤回/重做、版本保存/恢复、连线途经节点恢复不透明、计费去向改页顶统一入口 | 2x 第五轮三项 + 7x 统一入口 |

## 2026-08-26 增补（修复III：画布九项）
- **节点拉大小**：拖节点四角/四边改尺寸，和内容一起自动保存。
- **创建副本**：节点卡片菜单「创建副本」，复制节点及配置为新节点。
- **视频比例选项看全**：属性面板里比例选择器独占一整行，各比例完整可见可选。
- **图片节点默认模型**：下拉默认选管理端标记的默认图模型（没标记时用第一个）。
- **节点大小统一**：16:9 / 9:16 产物生成后节点尺寸定型统一，不撑爆布局。
- **点开大图**：点产物缩略图弹大图/播放窗（Lightbox），图片滚轮缩放。
- **自动保存**：改动防抖自动存，右上徽标「保存中→已保存」，失败可点重试；有未存改动离开页面会弹确认。
- **看上游**：选中媒体节点，属性面板出「上游」区——一层层往上展示全部上游（缩略图/视频/提示词，带节点名），悬浮放大，点开进 Lightbox。
- **双击=@引用**：双击上游图片/视频节点，直接以 @引用 插到输入框末尾并聚焦（生成中不让插）；输入框打 @，候选弹层跟在光标处（上方优先、放不下翻到下方），键盘↑↓选择、回车确认、Esc 关闭。


## 2026-08-27 增补（修复IV：两段式/上游直显/拖拽改尺寸/面板拖宽/自动保存/定型尺寸）

> 对应 commit `58f64beb`/`b88f61fb`/`d843b8a6`；详见 feature-map「2026-08-27 增补（修复IV）」。

- **两段式点击**：第一次点图片/视频节点=选中（出属性面板）；再点节点里的缩略图本身才弹大图/播放。画布不再「一碰就弹窗」。点空白处=取消选中。
- **上游面板**：选中媒体节点后侧栏「上游」全部直接展示（不再折叠成「点击展开」），按类型分色：图片/视频/音频/文本各有图标样式，一眼看全。
- **拖拽改尺寸**：选中节点→四角手柄拖=宽高同变；四条边手柄拖=单轴拉宽/拉窄。角手柄热区加大更好点中，连线不受挡。改完随画布自动保存落库。
- **属性面板拖宽**：拖面板左缘实时变宽，宽度自动记忆；刷新后保持（异常值自动回落默认宽）。
- **分辨率选择器整行**：视频节点分辨率在属性面板独占整行，各档位完整可见（与 III 期「比例整行」同手法）。
- **自动保存补缺**：新增节点立即进保存链（以前加节点不保存）；节点文本输入完**点别处（失焦）即自动保存**，右上保存徽标照常显示状态；输入时 @选人不受影响。
- **新建即定型**：新建图片/视频节点直接按产物最终尺寸创建，生成完成后不再撑爆布局/跳变。

### P4 回归修复（2026-08-27，commit `d4bfc3c3`）

- **手柄拖不动已修**：此前「手柄看得见、按住拖却没反应」——两处底层原因（手柄没接节点 id 事件没绑上 + 节点外框裁剪把手柄裁掉一半）均已修复。现在：按住四角手柄拖=宽高实时变、松手自动保存、刷新后尺寸保持；四条边手柄拖=单轴拉宽/拉高，同样有效。

## 2026-08-27 增补（修复V：上游视频首帧 + 副本重进不丢）

> 对应 commit `3c7926b0`；详见 feature-map 同日增补。

- **上游视频显首帧**：选中图片/视频节点后，右侧「上游」面板里的视频类节点直接显示视频**第一帧画面**（以前只显「视」字占位）；鼠标悬浮照常放大预览，单击照常弹 Lightbox 播放，无视频源时仍显占位。
- **副本重进不丢内容**：节点「创建副本」后退出画布再进来，副本里的图片/视频**不再消失**（以前视频副本一重进就空壳）。生成中的副本节点不受影响，重进后继续由任务恢复链接管；文件已失效的副本保持空壳（口径同现状）。

## 2026-08-28 增补（修复VI：粘贴/拖拽成节点、副本连线、提示词上限、附件 30MB、视频参数对齐）

> 对应 commit `619fecae`/`b8fda394`/`9e68287d`/`a84534b7`；详见 feature-map 同日增补。人工验证用例：`docs/测试方案/人工测试遗留问题修复VI测试方案.md`。

- **Ctrl+V 粘贴图片成节点**：在别处复制的图片文件，在画布**空白处**按 Ctrl+V → 直接在鼠标附近落一个图片节点。焦点在输入框/文本域时粘贴仍是正常贴文字，画布不抢。粘贴点=最近一次鼠标在画布上的位置。
- **本地文件拖进画布成节点**：从桌面/文件夹把图片、视频、音频文件直接拖进画布 → 按类型各建一个节点（图/视错位排开，不叠死）。上限与上传链同源：图片 30MB、视频 50MB、音频 15MB；其他类型文件拖入会提示「支持拖入图片/视频/音频」。
- **创建副本连线跟着走**：节点菜单「创建副本」后，原节点的**所有入线和出线各克隆一份**接到副本上（原来副本是孤立的）；原节点连线不动；自环连线克隆成副本自环；副本不进原节点的分组。
- **提示词长度封顶 + 官方建议**：视频/图片节点提示词输入框封顶 8000 字符（到顶截断，平台闸门口径）；输入框下方浅色提示官方建议值——视频 ≤500 汉字/1000 英文词、图片 ≤300 汉字/600 英文词，超长生成效果会下降。
- **附件图上限 8→30MB**：此前「附件过大 image ≤8MB」是**我们自己**的限制（非模型限制）；已对齐官方单图 30MB，且传输改签名 URL（上传更快、不易超时）。音频仍 15MB、视频 50MB 不变。
- **视频节点参数与独立视频页对齐**：选模型后比例/分辨率/时长范围按该模型能力自动收敛（如 fast 档最高 720p、不支持音频的模型不出音频开关）；新增「生成音频」「水印」开关；切换模型时参数自动落到新区间（分辨率取最近档不清空、不支持音频自动关）；首/尾帧入口按模型能力显隐；@引用音频节点作为参考（「音频N」独立编号，参考区音频卡可直接播放）。

## 2026-08-28 增补（修复VII：节点复制粘贴 + 一键整理布局）

> 对应 commit `2b7b7ed0`/`404b77ab`/`f451eb9d`/`6b639e0e`/`1095e04a`；详见 feature-map 同日增补。人工验证用例：`docs/测试方案/人工测试遗留问题修复VII测试方案.md`。
> 2026-08-29 P4 Playwright 实测全过；附带修复「按 Esc 后选中态隐形残留」（`07d63ae2`）——上文「按 Esc 取消选中」对单选节点自此可靠生效。

- **复制画布节点（Ctrl+C / Ctrl+V）**：选中一个节点，或用框选模式/Shift 框选多个节点 → 按 Ctrl+C（提示「已复制 N 个节点」）→ 鼠标移到目标位置按 Ctrl+V：整组节点连同**组内互相的连线**一起粘出，落点以鼠标为中心，连按多次会逐次错开不叠。只复制选中集内部的连线，连到集外节点的线不带（想带全部连线用节点菜单「创建副本」）。粘贴出的节点与原节点彻底独立（可各自重生成/入库互不影响）；名字撞了自动加序号（如「图1 2」）。
- **恢复「粘贴图片」**：复制过节点后想改贴外部图片？按 Esc 取消选中再按一次 Ctrl+C（无选中时复制=清空节点剪贴板），之后 Ctrl+V 就回到粘贴图片建节点。
- **一键整理布局（✨）**：工具条（右下）新增 ✨ 按钮——把全画布节点按「上游在左、下游在右」自动分层排整齐（孤立节点另起一列，互不重叠），排完自动缩放到看全图。**框选了节点时只整理选中部分**（选中有分组成员会把整组带上），没选中的节点原地不动。整理结果自动保存；不满意按 Ctrl+Z 一步还原。
- **注意事项**：焦点在提示词等输入框里时 Ctrl+C/V 仍是正常的文本复制粘贴，画布不抢；空画布时 ✨ 按钮置灰。

## 2026-08-29 增补（修复VIII：组整体对外拉线 + 连线不用瞄圆点）

> 对应 commit `4cd830eb`；详见 feature-map 同日增补。人工验证用例：`docs/测试方案/人工测试遗留问题修复VIII测试方案.md`。

- **组框边缘的连线圆点**：把几个节点「设为组」后，组框**右边缘中点**有一个输出圆点、**左边缘中点**有一个输入圆点（鼠标移上去会放大提示）。
  - 从**右圆点**拖线到别的节点（或另一组）＝「整组一起输出」——下游生成时，组里**每个成员的产物都算**上游输入。
  - 从别的节点拖线**落在组框内空白处**＝「喂给整组」——组里**每个成员**都把这条上游当输入。
  - 组→组的线两头都整体算数。
- **连线不用再瞄准小圆点**：从节点右侧圆点拖出线后，**拖到对方节点身上任何位置松手就连上**（不必精确落在它的小圆点上）；从左侧圆点反向拖同理。拖回自己身上＝取消；拖到空白处＝照旧弹「选个节点类型」快捷建点（新建的节点自动连上）。
- **组线的管理**：组线中间有 × 可删；点组线选中后按 Delete 也删。**解散组时组线自动消失**；组里加人/减人不用管组线（自动按新成员算数）。Ctrl+Z 可一步撤回。
- **成本提示**：「喂给整组」的线在生成时会把组里**所有人**的产物都当输入——组越大、生成消耗的积分越多（预估/余额闸门照常兜底）。
- **不变的部分**：组里成员自己的小圆点照旧能单独对外连线（两种线并存互不影响）；一键整理不带组线。复制粘贴/创建副本的组线随「⛓ 连线保留」开关而定（2026-09-01 修复X 起，见下节）。

## 2026-09-01 增补（修复X：视频上传、从库预览、组线保留）

> 对应 commit `97d6547f`/`9c0cf4c6`/`5c7c6625`/`85d9ffd1`/`45723976`；详见 feature-map 同日增补。人工验证用例：`docs/测试方案/人工测试遗留问题修复X测试方案.md`（R1-R15）。

### 视频节点直接传本地视频

1. 选中视频节点 → 属性面板顶部点「**上传视频**」→ 选本地 mp4 等视频文件。
2. **界面变化**：按钮转圈（上传中）→ 完成后节点直接显示该视频、可播放；面板常显「本地视频 ≤50MB」提示。
3. **上传即产物**：传完这个视频就是节点的产物——下游节点可以 @引用它、也能对它抽帧/截取，和生成出来的视频完全同等待遇。再传一个会**覆盖**旧视频。
4. **预期结果 / 异常**：超过 50MB 直接提示拒收、节点不变（不发请求）；断网失败节点标红、恢复后重传即可。上限口径与拖拽/粘贴一致：图片 30MB、音频 15MB。

### 「从库选择」看得见再选

1. 图片/视频/音频/文本类节点 → 属性面板点「**从库选择**」→ 弹资产列表（只列同类型）。
2. **四类预览**：
   - **图片**：鼠标悬浮行缩略图 → 侧边浮层放大图（带尺寸）；**点缩略图** → 弹大图灯箱（盖在弹窗之上），滚轮缩放/拖拽/双击复位，Esc 或点遮罩关（选择弹窗还在）。
   - **视频**：缩略显第一帧+▶ 角标；悬浮放大首帧；点击弹播放灯箱。
   - **音频**：「音」字标 + 行内播放条，直接点播。
   - **文本**：显正文开头片段；没有片段显「提」字标。
3. **选用**：点行尾「**选择**」按钮 → 节点数据写入、弹窗关闭。**点行空白处不选也不关**；点音频播放条只播不选——想选必须点「选择」。
4. **异常**：缩略加载失败回落类型字标（图/视），不白屏不卡转圈，「选择」仍可用。

### ⛓ 连线保留时组线也跟着走

1. 组外节点 b 与组 g 互相有连线（b→组、组→b），**⛓ 连线保留开关开着**（默认开）。
2. 框选 b → Ctrl+C → 空白处 Ctrl+V（或节点菜单「创建副本」）。
3. **预期结果**：新节点 b′ 自动接回**原组**——b′→组 与 组→b′ 两条线都在（连按多次粘贴每份都带）；但 b′ **不算组员**（组计数不变）。Ctrl+Z 一步撤回节点和组线。
4. **边界**：⛓ 关=零组线（老行为）；复制后把组解散再粘贴，组线静默丢弃、无断线无报错；生成时广播/聚合照常算数（组的上游能看到 b′ 的产物、b′ 也能吃到组的产物）。
