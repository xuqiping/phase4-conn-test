# 图片生成第二轮问题修复 · 开发进度 1

> 对应 plan：[图片生成第二轮人工测试问题修复.plan.md](../../docs/plans/图片生成第二轮人工测试问题修复.plan.md)（含实施偏离注记）。
> 问题单：[6x_图片生成问题.md](../../人工测试问题/6x_图片生成问题.md)（未解决 1-5 全部落地 → 移未复验）。

## 本轮完成（2026-08-11，branch beifen）

| 问题 | 修法 | commit |
|---|---|---|
| #2 图片记录混入视频历史 | 后端 `/api/media/tasks` 加 `kind=IMAGE/VIDEO`（mapper selectHistory 第 7 参 SQL 层 `task_type IN` 过滤；service 白名单校验+6 参兼容重载；controller 透传）+ 前端 MediaTaskQuery.kind + 视频页传 VIDEO | a120ad1、408554cf |
| #4 无提示词/时间筛选 | ImageGenView 历史筛选条（NInput+NDatePicker range+清空），300ms 防抖 + requestSeq 竞态守卫 + kind=IMAGE，删原前端 filter | 8d04518b |
| #1 历史行无缩略图 | 新组件 `components/media/MediaTaskImageThumb.vue`（与 MediaTaskVideoPreview 同构：IntersectionObserver 懒加载+revoke+版本守卫），行右侧 56px 首图，点击弹灯箱（stop 冒泡），无图行占位 | 9e6d9f34、8d04518b |
| #3 点历史不还原参数 | 新纯函数 `utils/imageGenParams.ts parseImageRestore`（详情 submittedRequest→表单补丁；自定义尺寸 `__custom__` 回填/超 cap 截断/模型下线保真+告警）；viewHistory 先 getTask→逐项直赋（**不调 onModelChange** 防吞值）→restoreRefs 逐张 fetchFilePreview 回填参考图 | 9e6d9f34、8d04518b |
| #5 看不到实际发送参数 | 复用视频第三轮 `MediaTaskRequestDetails`（加 title prop 向后兼容）挂结果区；submittedRequest 含 refFileIds 供核对；图片路径无 Provider 快照→页签明示「未记录发送快照」 | 8d04518b |

## 关键上下文：与视频第三轮会话的并发协调

- 实施中发现「视频生成第三轮修复」会话**并行活跃**在同一工作树改同一批 media 文件。用户决策：先做零冲突部分，热文件后补。
- 对方 commit 55f2ed48 后工作树转冷：其已实现 `MediaTaskVO.submittedRequest`（get 单查透出、剥 provider 快照+data URI 脱敏）+`MediaTaskRequestDetails` 组件——我方问题 3/5 直接复用，未建平行 requestConfig 字段（偏离注记 1/3）。
- kind 过滤期间对方新 chunk（MediaReferenceUrl）致 mvn 编译错短暂阻塞；先 commit 存档保护，错解后补跑测试绿。
- 对方 mapper selectHistory 6 参→我方升 7 参，其 3 个 list 测试已最小补丁兼容（追加第 7 参 matcher/null）；service 保留 6 参重载保其契约。

## 测试结果

- 后端：`MediaGenQueryServiceTest` **27/27 绿**（含新增 kind 4 用例：IMAGE/VIDEO 透传/小写归一/非法 400）
- 前端：`vue-tsc` 净；`vitest` **392/392 全绿零回归**（新增 imageGenParams 9 + MediaTaskImageThumb 4）

## 涉及文件

后端：MediaGenTaskMapper、MediaGenQueryService、MediaGenController、MediaGenQueryServiceTest
前端：api/media.ts、VideoGenView.vue（1 行 kind）、ImageGenView.vue、utils/imageGenParams(.test).ts、components/media/MediaTaskImageThumb(.test).vue/ts、MediaTaskRequestDetails.vue（title prop）

## 文档

测试方案 `docs/测试方案/图片生成第二轮问题修复测试方案.md`（联动用例 C1-C13 对齐 plan L1-L7）；feature-map/user-ops/问题单已同步。

## 留项（Phase 4）

- playwright 真机冒烟 C1-C13（需部署环境+真实 IMAGE provider/价表）
- #6 pro 超时（第一轮 90s→180s）随本轮人工复验一并确认
- 后续再说（plan 运维清单已记）：图片路径 providerRequestSnapshot 持久化、缩略图服务端缩略
