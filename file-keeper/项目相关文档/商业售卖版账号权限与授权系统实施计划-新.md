# File Keeper 商业授权后续实施计划（调整版） Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **同步更新说明：** 本文档已根据 `file-keeper/项目相关文档/后台改造计划/00-总览与进度.md`（最后更新 2026-06-15）同步。当前 Dashboard 统计页、系统设置页、测试用户演练均已完成；匿名设备运营页仍为待做项。

**Goal:** 在阶段 1-4 已完成成果基础上，补齐桌面端账号 UI、免费模块选择、真实环境联调和商业版收口能力，让 File Keeper 商业授权第一版达到可验收状态。

**Architecture:** 服务端授权 API 和管理后台核心能力已完成，后续不重做阶段 1-4，而是在现有 `commercialAuth` API/Store、管理后台用户详情页和桌面端模块门禁基础上补齐用户可操作入口。后续主线调整为：先统一联调配置，再补桌面端登录注册和账号状态 UI，再补匿名过期后的免费模块选择，最后执行真实环境端到端联调。

**Tech Stack:** Spring Boot 3.2.5、Java 17、PostgreSQL、Redis、Vue 3、TypeScript、Vite、Naive UI、Pinia、Tauri 2、Vitest、Maven。

---

## 0. 调整依据

### 0.1 原总计划与实际阶段四的差异

原文件 `file-keeper/项目相关文档/商业售卖版账号权限与授权系统实施计划.md` 的阶段划分是：

1. 阶段 1：服务端基础与核心数据模型。
2. 阶段 2：账号注册、验证、登录、管理员审核 API。
3. 阶段 3：模块授权、设备绑定、匿名权益 API。
4. 阶段 4：Web 管理后台 MVP。
5. 阶段 5：桌面客户端登录、注册、权益快照。
6. 阶段 6：桌面客户端模块门禁与商业版收口。
7. 手动端到端验收。

实际阶段四进度文件 `file-keeper/docs/superpowers/plans/2026-06-08-4商业计划开发进度.md` 显示，阶段四没有只做原计划阶段 4，而是同时完成了：

- 管理后台核心 MVP：管理员登录、用户列表、用户详情、审核、启用/禁用、用户设备限制、离线缓存时长、模块授权、设备禁用。
- 桌面端匿名授权接入：设备身份、本地持久化、匿名试用启动、匿名授权查询。
- 桌面端登录用户授权底层接入：设备注册、登录用户授权快照查询、离线缓存可用性判断。
- 桌面端模块门禁核心：三模块 Tab 禁用、剪贴板/截图/关闭进程等入口授权检查、授权变化后切换可用模块。
- 端到端联调场景清单和自动化验证记录。

因此，后续计划不能再按原阶段 5/6 全量重做，应改为“补缺口 + 联调验收”。

### 0.2 当前已完成能力

#### 服务端

- `file-keeper/server/` 已完成阶段 1-3。
- 已有认证、管理员审核、用户启用/禁用、refresh token 撤销。
- 已有模块权益、设备绑定、用户设置、匿名试用、匿名/登录授权查询。
- 阶段四记录的最终服务端验证：45 个测试通过。

#### 管理后台

已完成核心文件：

- `file-keeper/admin-web/src/api/request.ts`
- `file-keeper/admin-web/src/api/auth.ts`
- `file-keeper/admin-web/src/api/users.ts`
- `file-keeper/admin-web/src/api/entitlements.ts`
- `file-keeper/admin-web/src/api/devices.ts`
- `file-keeper/admin-web/src/api/stats.ts`
- `file-keeper/admin-web/src/api/settings.ts`
- `file-keeper/admin-web/src/stores/auth.ts`
- `file-keeper/admin-web/src/router/index.ts`
- `file-keeper/admin-web/src/views/LoginView.vue`
- `file-keeper/admin-web/src/views/Layout.vue`
- `file-keeper/admin-web/src/views/UserListView.vue`
- `file-keeper/admin-web/src/views/UserDetailView.vue`
- `file-keeper/admin-web/src/views/DashboardView.vue`
- `file-keeper/admin-web/src/views/SettingsView.vue`

其中 `UserDetailView.vue` 已内联实现原计划中 `UserEntitlementEditor.vue` 和 `UserDeviceTable.vue` 的核心业务，所以暂不需要单独拆组件。

> **新增完成项（截至 2026-06-15）：**
> - Dashboard 统计页已完成（后端验证 + 前端 build 通过）。
> - 系统设置页已完成（全量测试 + 端到端验证通过）。
> - 匿名设备运营页仍待做，需新建后端管理端 repository/service/controller + 前端运营页。

#### 桌面端

已完成核心文件：

- `file-keeper/src/api/commercialAuth.ts`
- `file-keeper/src/stores/commercialAuthStore.ts`
- `file-keeper/src/App.vue` 授权门禁接入
- `file-keeper/src/api/__tests__/commercialAuth.test.ts`
- `file-keeper/src/stores/__tests__/commercialAuthStore.test.ts`

已具备：

- `getOrCreateDeviceIdentity()`
- `startAnonymousTrial()`
- `getAnonymousAuthorization()`
- `registerClientDevice()`
- `getClientAuthorization()`
- `commercialAuthStore.initializeAnonymous(baseUrl)`
- `commercialAuthStore.initializeAuthenticated(baseUrl, accessToken)`
- `commercialAuthStore.isModuleAllowed(moduleCode)`
- `commercialAuthStore.denialReason(moduleCode)`

### 0.3 当前真实缺口

必须补齐：

1. 服务端端口与前端默认 API 地址不一致：服务端默认 `8088`，管理后台和桌面端默认 `8080`。
2. 桌面端缺少登录/注册/退出 UI。
3. 桌面端缺少客户端认证 API 封装与认证 Store。
4. 登录成功后尚未从 UI 调用 `commercialAuthStore.initializeAuthenticated()`。
5. 桌面端缺少用户可见的授权状态组件。
6. 匿名试用过期后，桌面端缺少选择/更换免费模块 UI。
7. 真实环境端到端联调已启动但尚未全部闭环（Dashboard、系统设置、测试用户演练已完成，匿名设备运营页待做）。

可选后置，不阻塞第一版验收：

1. 管理后台匿名设备运营页。
2. 管理后台用户详情页拆组件。
3. Rust 侧授权二次校验。
4. 支付、套餐、订单。
5. 业务数据云同步。

### 0.4 阶段 5E 联调进展（截至 2026-06-15）

根据 `后台改造计划/00-总览与进度.md`，阶段 5E 真实环境端到端联调已经启动并取得以下进展：

#### 本地联调环境

- **PostgreSQL 17.4** 便携版：`C:\Users\19536\.local\tools\pgsql\`，数据目录 `C:\Users\19536\.local\tools\pgdata`，端口 5432，库 `file_keeper`，用户/密码 `postgres`/`postgres`。
- **Memurai Developer 4.1.2**（Redis 兼容）：Windows 服务 `Memurai`，端口 6379，自动启动。
- **Java 17**：`C:\Program Files\Eclipse Adoptium\jdk-17.0.19.10-hotspot`
- **Maven 3.9.11**：`C:\Users\19536\.local\tools\apache-maven-3.9.11`

启动方式：

1. 后端（端口 8088）：
   ```bash
   export JAVA_HOME="/c/Program Files/Eclipse Adoptium/jdk-17.0.19.10-hotspot"
   export MAVEN_HOME="/c/Users/19536/.local/tools/apache-maven-3.9.11"
   export PATH="$JAVA_HOME/bin:$MAVEN_HOME/bin:$PATH"
   mvn -f "file-keeper/server/pom.xml" spring-boot:run
   ```
2. 管理后台（端口 5173，必须指定 8088）：
   ```bash
   cd file-keeper/admin-web
   VITE_API_BASE_URL="http://localhost:8088" npm run dev -- --host 127.0.0.1
   ```
3. 桌面端（Tauri，端口 1420）：
   ```bash
   powershell.exe -NoProfile -ExecutionPolicy Bypass -Command "Set-Location 'C:\AI Projects\file-keeper'; & '.\dev-msvc.bat'"
   ```

账号：

- 超管：`admin@filekeeper.local` / `Password123!`
- 测试用户：`testuser@e2e.local` / `Test12345`（已审批 active，已授权 files+clipboard）
- 开发验证码：`123456`

#### 测试用户演练（0 号计划）已完成

通过后端 API 完整跑通真实授权流程：

1. 发验证码 → 校验 `123456` → 注册 `testuser@e2e.local`（status=pending_review）
2. 管理员审批 → status=active
3. 设置 deviceLimit=2、offlineCacheMinutes=60
4. 授予 files + clipboard（到 2026-09-01）
5. testuser 登录 + 设备注册 + 授权查询：files/clipboard allowed=true ✓

#### 数据库临时调整（联调期手动 ALTER，不在 Flyway 迁移中）

- `user_devices.device_name`：`VARCHAR(120)` → `VARCHAR(255)`（修 deviceName 超长 bug）
- `users` 表超管 `device_limit`：1 → 10（联调方便）

> ⚠️ 这些调整需要后续补成 Flyway 迁移，或在重建数据库时重做。

#### 代码临时改动（联调期，需评估是否保留）

- `server/.../security/RequestLoggingFilter.java` — **新增**，记录请求 method/uri/origin/auth/status。联调用，可选保留。
- `server/.../common/GlobalExceptionHandler.java` — 兜底 `@ExceptionHandler(Exception.class)` 改为 `log.error` 打印完整堆栈。**建议保留**。
- `file-keeper/src/api/commercialAuth.ts` — `defaultDeviceName()` 截断 UA 到 100 字符。**必须保留**。

---

## 1. 调整后的后续阶段

### 阶段 5A：联调前置配置收口

**目标：** 先统一服务端地址和启动配置，避免后续桌面端、管理后台、服务端联调时因为端口不一致失败。

**状态：** 部分完成。本地联调已统一使用 `http://localhost:8088`，但 `admin-web/.env.development` 和桌面端默认兜底地址尚未固化为 8088。

**必要性：** 必须做。当前 `server/src/main/resources/application.yml` 默认 `server.port=8088`，但 `admin-web/.env.development` 和桌面端 `App.vue` 默认指向 `http://localhost:8080`。

### 阶段 5B：桌面端账号 UI 与认证 Store

**目标：** 让用户可以在桌面端完成登录、注册、退出，并让登录成功后自动绑定设备和刷新授权快照。

**状态：** 未完成。

**必要性：** 必须做。这是商业授权闭环的最大缺口。

### 阶段 5C：桌面端授权状态展示

**目标：** 让用户知道当前是匿名试用、等待审核、商业授权、离线缓存、账号禁用或设备禁用状态。

**状态：** 未完成。

**必要性：** 必须做。否则授权系统虽然生效，但用户无法理解为什么模块可用或不可用。

### 阶段 5D：匿名试用过期后的免费模块选择

**目标：** 匿名试用 7 天结束后，用户可以在桌面端选择一个长期免费模块，并每 30 天更换一次。

**状态：** 未完成。

**必要性：** 建议做。它是匿名生命周期的完整闭环；如果暂时不做，匿名过期后只能看到模块不可用原因，无法自助恢复免费模块。

### 阶段 5E：真实环境端到端联调

**目标：** 使用 PostgreSQL、Redis、真实后端、管理后台和 Tauri 桌面端执行商业授权闭环验收。

**状态：** 进行中。环境已搭建，测试用户演练完成，Dashboard/系统设置已完成，若干真实 bug 已修复；匿名设备运营页待做，桌面端账号 UI 仍是主缺口。

**必要性：** 必须做。自动化测试不能替代真实进程、浏览器、桌面端和本地 Store 的集成验证。

### 阶段 6：商业版安全与运营增强

**目标：** 在第一版可用后，补 Rust 二次校验、匿名设备运营页等增强能力。

**状态：** 未开始。Dashboard 与系统设置已提前在阶段 5E 补齐；匿名设备运营页和 Rust 二次校验仍后置。

**必要性：** 非第一版阻塞项，建议在端到端联调通过后再排期。

---

## 2. 阶段 5A：联调前置配置收口

### Task 5A-1：统一开发环境服务端地址

**Files:**

- Inspect: `file-keeper/server/src/main/resources/application.yml`
- Modify: `file-keeper/admin-web/.env.development`
- Create: `file-keeper/.env.development`
- Modify if needed: `file-keeper/src/App.vue`

**Current facts:**

- 服务端默认端口：`8088`。
- 管理后台开发环境当前：`VITE_API_BASE_URL=http://localhost:8080`。
- 桌面端当前默认：`import.meta.env.VITE_FILE_KEEPER_SERVER_URL || 'http://localhost:8080'`。

- [ ] **Step 1: 修改管理后台开发环境 API 地址**

把 `file-keeper/admin-web/.env.development` 改为：

```env
VITE_API_BASE_URL=http://localhost:8088
```

- [ ] **Step 2: 新增桌面端开发环境 API 地址**

创建 `file-keeper/.env.development`：

```env
VITE_FILE_KEEPER_SERVER_URL=http://localhost:8088
```

- [ ] **Step 3: 保留桌面端 App.vue 默认兜底**

如果继续保留默认值，建议同步改成 8088：

```ts
const commercialServerUrl = import.meta.env.VITE_FILE_KEEPER_SERVER_URL || 'http://localhost:8088'
```

- [ ] **Step 4: 运行桌面端测试**

Run:

```bash
npm --prefix "file-keeper" test
```

Expected:

```text
Test Files  28 passed
Tests  162 passed
```

如果测试数因新增测试变化，以全部通过为准。

- [ ] **Step 5: 运行桌面端构建**

Run:

```bash
npm --prefix "file-keeper" run build
```

Expected: `built in ...` 且命令退出码为 0。

- [ ] **Step 6: 运行管理后台构建**

Run:

```bash
cd file-keeper/admin-web && npm run build
```

Expected: `vue-tsc -b && vite build` 通过。

---

## 3. 阶段 5B：桌面端账号 UI 与认证 Store

### Task 5B-1：新增桌面端客户端认证 API

**Files:**

- Create: `file-keeper/src/api/auth.ts`
- Test: `file-keeper/src/api/__tests__/auth.test.ts`

**API endpoints:**

- `POST /api/client/verification/send`
- `POST /api/client/verification/check`
- `POST /api/client/auth/register`
- `POST /api/client/auth/login`
- `POST /api/client/auth/refresh`
- `POST /api/client/auth/logout`

**Types to expose:**

```ts
export type ContactType = 'email' | 'phone'

export interface UserSummary {
  id: number
  email: string | null
  phone: string | null
  role: string
  status: string
  deviceLimit: number
  offlineCacheMinutes: number
}

export interface AuthResponse {
  accessToken: string
  refreshToken: string
  user: UserSummary
}
```

- [ ] **Step 1: Write failing API tests**

Create `file-keeper/src/api/__tests__/auth.test.ts` with tests for:

1. `sendVerificationCode(baseUrl, { email })` calls `/api/client/verification/send`.
2. `checkVerificationCode(baseUrl, { email, code })` calls `/api/client/verification/check`.
3. `register(baseUrl, request)` calls `/api/client/auth/register`.
4. `login(baseUrl, identifier, password)` calls `/api/client/auth/login`.
5. `refresh(baseUrl, refreshToken)` calls `/api/client/auth/refresh`.
6. `logout(baseUrl, refreshToken)` calls `/api/client/auth/logout`.
7. backend `R.fail` message is surfaced as an Error message.

- [ ] **Step 2: Run failing API tests**

Run:

```bash
npm --prefix "file-keeper" test -- src/api/__tests__/auth.test.ts
```

Expected: FAIL because `src/api/auth.ts` does not exist.

- [ ] **Step 3: Implement `src/api/auth.ts`**

Implement functions using the same response parsing style as `src/api/commercialAuth.ts`.

Required exported functions:

```ts
sendVerificationCode(baseUrl, request)
checkVerificationCode(baseUrl, request)
register(baseUrl, request)
login(baseUrl, identifier, password)
refresh(baseUrl, refreshToken)
logout(baseUrl, refreshToken)
```

- [ ] **Step 4: Run API tests to green**

Run:

```bash
npm --prefix "file-keeper" test -- src/api/__tests__/auth.test.ts
```

Expected: PASS.

### Task 5B-2：新增桌面端认证 Store

**Files:**

- Create: `file-keeper/src/stores/authStore.ts`
- Test: `file-keeper/src/stores/__tests__/authStore.test.ts`
- Uses: `file-keeper/src/api/auth.ts`
- Uses: `file-keeper/src/stores/commercialAuthStore.ts`

**Store responsibilities:**

- 保存 `accessToken`、`refreshToken`、`user`。
- 登录成功后调用 `commercialAuthStore.initializeAuthenticated(baseUrl, accessToken)`。
- 退出登录后清空认证态，并重新初始化匿名授权。
- 启动时如果本地有 refresh token，先 refresh，再初始化登录授权。
- 如果 refresh 失败，清空登录态并使用匿名授权。

- [ ] **Step 1: Write failing store tests**

Create `file-keeper/src/stores/__tests__/authStore.test.ts` with tests for:

1. `login()` stores tokens and user.
2. `login()` calls `commercialAuthStore.initializeAuthenticated()`.
3. `logout()` calls auth API logout and clears tokens.
4. `logout()` calls `commercialAuthStore.initializeAnonymous()` after clearing login state.
5. `restoreSession()` refreshes token when refresh token exists.
6. `restoreSession()` falls back to anonymous authorization if refresh fails.

- [ ] **Step 2: Run failing store tests**

Run:

```bash
npm --prefix "file-keeper" test -- src/stores/__tests__/authStore.test.ts
```

Expected: FAIL because `authStore.ts` does not exist.

- [ ] **Step 3: Implement `authStore.ts`**

Use Pinia composition store. Persist tokens with `@tauri-apps/plugin-store` in the same `file-keeper-auth.json` file already used by `commercialAuth.ts`.

Required state:

```ts
accessToken: Ref<string | null>
refreshToken: Ref<string | null>
user: Ref<UserSummary | null>
loading: Ref<boolean>
error: Ref<string | null>
isAuthenticated: ComputedRef<boolean>
```

Required actions:

```ts
login(baseUrl: string, identifier: string, password: string): Promise<void>
logout(baseUrl: string): Promise<void>
restoreSession(baseUrl: string): Promise<void>
register(baseUrl: string, request: RegisterRequest): Promise<UserSummary>
sendVerificationCode(baseUrl: string, request: SendVerificationRequest): Promise<void>
checkVerificationCode(baseUrl: string, request: CheckVerificationRequest): Promise<boolean>
```

- [ ] **Step 4: Run store tests to green**

Run:

```bash
npm --prefix "file-keeper" test -- src/stores/__tests__/authStore.test.ts
```

Expected: PASS.

### Task 5B-3：新增登录/注册弹窗 UI

**Files:**

- Create: `file-keeper/src/components/AuthDialog.vue`
- Create: `file-keeper/src/components/LoginForm.vue`
- Create: `file-keeper/src/components/RegisterForm.vue`
- Test: `file-keeper/src/components/__tests__/authDialog.test.ts`
- Modify: `file-keeper/src/App.vue`

**UI requirements:**

- 顶部工具栏增加“登录 / 账号”入口。
- 未登录：显示“登录”按钮，点击打开 `AuthDialog`。
- 已登录：显示邮箱或手机号，提供退出登录按钮。
- 登录成功：关闭弹窗，刷新商业授权。
- 注册成功：提示“注册成功，等待管理员审核”，允许直接登录或回到登录页。
- pending_review 用户：允许登录，但授权状态显示等待审核。

- [ ] **Step 1: Write failing component tests**

Create `authDialog.test.ts` to verify:

1. 未登录时点击登录按钮显示弹窗。
2. 输入账号密码点击登录后调用 `authStore.login()`。
3. 登录成功后关闭弹窗。
4. 注册表单发送验证码、校验验证码、提交注册。
5. 退出按钮调用 `authStore.logout()`。

- [ ] **Step 2: Run failing component tests**

Run:

```bash
npm --prefix "file-keeper" test -- src/components/__tests__/authDialog.test.ts
```

Expected: FAIL because components do not exist.

- [ ] **Step 3: Implement UI components**

Implementation boundaries:

- `AuthDialog.vue` controls modal state and switches between login/register tabs.
- `LoginForm.vue` only handles identifier/password login form.
- `RegisterForm.vue` only handles email/phone, verification code and password registration form.
- `App.vue` only owns where the account entry is rendered.

- [ ] **Step 4: Wire App.vue**

In `App.vue`:

1. import `useAuthStore`。
2. call `authStore.restoreSession(commercialServerUrl)` on startup before or alongside anonymous initialization。
3. avoid duplicate anonymous initialization when a valid login session is restored。
4. render `AuthDialog` and account button in the top-right action area。

- [ ] **Step 5: Run component tests to green**

Run:

```bash
npm --prefix "file-keeper" test -- src/components/__tests__/authDialog.test.ts
```

Expected: PASS.

---

## 4. 阶段 5C：桌面端授权状态展示

### Task 5C-1：新增授权状态组件

**Files:**

- Create: `file-keeper/src/components/EntitlementStatus.vue`
- Test: `file-keeper/src/components/__tests__/entitlementStatus.test.ts`
- Modify: `file-keeper/src/App.vue`

**Display states:**

1. 匿名 7 天全功能试用中。
2. 匿名试用已过期，未选择免费模块。
3. 匿名免费模块：显示当前免费模块。
4. 登录用户等待管理员审核：显示 `pending_review`。
5. 登录用户已授权：显示 files/processes/clipboard 授权状态。
6. 登录用户离线缓存可用：显示缓存截止时间。
7. 账号或设备被服务端拒绝：显示 `commercialAuthStore.error`。

- [ ] **Step 1: Write failing component tests**

Create tests covering all display states above.

- [ ] **Step 2: Run failing test**

Run:

```bash
npm --prefix "file-keeper" test -- src/components/__tests__/entitlementStatus.test.ts
```

Expected: FAIL because component does not exist.

- [ ] **Step 3: Implement `EntitlementStatus.vue`**

Use only existing stores:

- `useAuthStore()`
- `useCommercialAuthStore()`

Do not fetch data inside this component. It should be presentational.

- [ ] **Step 4: Mount status component in App.vue**

Recommended location: top-right action area, near login/account button.

- [ ] **Step 5: Run tests**

Run:

```bash
npm --prefix "file-keeper" test -- src/components/__tests__/entitlementStatus.test.ts
npm --prefix "file-keeper" test
```

Expected: all tests PASS.

---

## 5. 阶段 5D：匿名免费模块选择

### Task 5D-1：扩展 commercialAuth API 支持免费模块选择

**Files:**

- Modify: `file-keeper/src/api/commercialAuth.ts`
- Modify: `file-keeper/src/api/__tests__/commercialAuth.test.ts`

**Backend endpoints already available:**

- `POST /api/anonymous/trial/select-free-module`
- `POST /api/anonymous/trial/change-free-module`

**Request shape:**

```ts
{
  deviceId: string
  fingerprintHash: string
  freeModuleCode: 'files' | 'processes' | 'clipboard'
}
```

- [ ] **Step 1: Add failing tests**

Extend `commercialAuth.test.ts` to verify:

1. `selectFreeModule(baseUrl, identity, 'files')` calls `/api/anonymous/trial/select-free-module`.
2. `changeFreeModule(baseUrl, identity, 'clipboard')` calls `/api/anonymous/trial/change-free-module`.
3. backend error message is surfaced for 30-day restriction.

- [ ] **Step 2: Run failing test**

Run:

```bash
npm --prefix "file-keeper" test -- src/api/__tests__/commercialAuth.test.ts
```

Expected: FAIL because functions do not exist.

- [ ] **Step 3: Implement API functions**

Add exports:

```ts
export async function selectFreeModule(baseUrl: string, identity: DeviceIdentity, freeModuleCode: ModuleCode): Promise<AnonymousTrialStatus>
export async function changeFreeModule(baseUrl: string, identity: DeviceIdentity, freeModuleCode: ModuleCode): Promise<AnonymousTrialStatus>
```

- [ ] **Step 4: Run tests to green**

Run:

```bash
npm --prefix "file-keeper" test -- src/api/__tests__/commercialAuth.test.ts
```

Expected: PASS.

### Task 5D-2：扩展 commercialAuthStore 支持选择免费模块

**Files:**

- Modify: `file-keeper/src/stores/commercialAuthStore.ts`
- Modify: `file-keeper/src/stores/__tests__/commercialAuthStore.test.ts`

- [ ] **Step 1: Add failing store tests**

Add tests for:

1. `selectFreeModule(baseUrl, 'files')` updates `trialStatus`。
2. selection refreshes `anonymousAuthorization`。
3. backend restriction message is saved to `error`。

- [ ] **Step 2: Run failing test**

Run:

```bash
npm --prefix "file-keeper" test -- src/stores/__tests__/commercialAuthStore.test.ts
```

Expected: FAIL because action does not exist.

- [ ] **Step 3: Implement store actions**

Add actions:

```ts
selectFreeModule(baseUrl: string, moduleCode: ModuleCode): Promise<void>
changeFreeModule(baseUrl: string, moduleCode: ModuleCode): Promise<void>
refreshAnonymousAuthorization(baseUrl: string): Promise<void>
refreshAuthenticatedAuthorization(baseUrl: string, accessToken: string): Promise<void>
```

- [ ] **Step 4: Run tests to green**

Run:

```bash
npm --prefix "file-keeper" test -- src/stores/__tests__/commercialAuthStore.test.ts
```

Expected: PASS.

### Task 5D-3：新增免费模块选择 UI

**Files:**

- Create: `file-keeper/src/components/FreeModuleSelector.vue`
- Test: `file-keeper/src/components/__tests__/freeModuleSelector.test.ts`
- Modify: `file-keeper/src/App.vue`

**UI rules:**

- 只在匿名试用过期且没有可用模块时显示强提示。
- 可选模块固定为 files/processes/clipboard。
- 选择后调用 `commercialAuthStore.selectFreeModule()`。
- 如果已有免费模块，显示“每 30 天可更换一次”。
- 30 天内更换失败时显示后端错误消息。

- [ ] **Step 1: Write failing component tests**

Cover:

1. 过期未选择时显示三个模块选项。
2. 点击 files 调用 store action。
3. 选择成功后关闭提示并刷新授权。
4. 30 天限制错误显示给用户。

- [ ] **Step 2: Run failing component test**

Run:

```bash
npm --prefix "file-keeper" test -- src/components/__tests__/freeModuleSelector.test.ts
```

Expected: FAIL because component does not exist.

- [ ] **Step 3: Implement `FreeModuleSelector.vue`**

Keep component focused: no direct fetch, only call store action.

- [ ] **Step 4: Mount component in App.vue**

Recommended behavior:

- If current tab is unauthorized due to expired anonymous trial, show `FreeModuleSelector` in main content area.
- Do not block login UI; logged-in users should not be forced into anonymous module selection.

- [ ] **Step 5: Run tests and build**

Run:

```bash
npm --prefix "file-keeper" test
npm --prefix "file-keeper" run build
```

Expected: all tests and build PASS.

---

## 6. 阶段 5E：真实环境端到端联调规划

### 6.1 联调前置条件

Required services:

- PostgreSQL running on `localhost:5432` with database `file_keeper`.
- Redis running on `localhost:6379`.
- Java 17 available at `C:/Program Files/Eclipse Adoptium/jdk-17.0.19.10-hotspot`.
- Maven available at `C:/Users/19536/.local/tools/apache-maven-3.9.11` or Maven wrapper available.
- Node dependencies installed for both desktop and admin-web.

Required environment:

```bash
export JAVA_HOME="/c/Program Files/Eclipse Adoptium/jdk-17.0.19.10-hotspot"
export MAVEN_HOME="/c/Users/19536/.local/tools/apache-maven-3.9.11"
export PATH="$JAVA_HOME/bin:$MAVEN_HOME/bin:$PATH"
export FILE_KEEPER_DB_URL="jdbc:postgresql://localhost:5432/file_keeper"
export FILE_KEEPER_DB_USERNAME="postgres"
export FILE_KEEPER_DB_PASSWORD="postgres"
export FILE_KEEPER_REDIS_HOST="localhost"
export FILE_KEEPER_REDIS_PORT="6379"
export FILE_KEEPER_JWT_SECRET="file-keeper-local-dev-jwt-secret-at-least-32-bytes"
export FILE_KEEPER_SUPER_ADMIN_EMAIL="admin@filekeeper.local"
export FILE_KEEPER_SUPER_ADMIN_PASSWORD="Password123!"
export FILE_KEEPER_VERIFICATION_DEV_FIXED_CODE="123456"
```

### 6.2 启动顺序

- [ ] **Step 1: Run backend tests before manual联调**

Run:

```bash
mvn -f "file-keeper/server/pom.xml" test
```

Expected: all service tests PASS.

- [ ] **Step 2: Run desktop tests/build before manual联调**

Run:

```bash
npm --prefix "file-keeper" test
npm --prefix "file-keeper" run build
```

Expected: all tests/build PASS.

- [ ] **Step 3: Run admin build before manual联调**

Run:

```bash
cd file-keeper/admin-web && npm run build
```

Expected: build PASS.

- [ ] **Step 4: Start backend**

Run:

```bash
mvn -f "file-keeper/server/pom.xml" spring-boot:run
```

Expected:

```text
Started FileKeeperServerApplication
Tomcat started on port 8088
```

- [ ] **Step 5: Start admin-web**

Run in another terminal:

```bash
cd file-keeper/admin-web && npm run dev
```

Expected: Vite dev server starts and points API calls to `http://localhost:8088`.

- [ ] **Step 6: Start desktop app**

Preferred on this Windows/MSVC environment:

```bash
cd file-keeper && ./dev-msvc.bat
```

Fallback if MSVC env is already configured:

```bash
cd file-keeper && npm run tauri:dev
```

Expected: Tauri desktop window opens.

### 6.3 联调场景清单

#### 场景 1：匿名设备首次 7 天全功能试用

- [ ] 清理桌面端本地 `file-keeper-auth.json` 或使用新设备身份。
- [ ] 启动桌面端。
- [ ] 确认客户端调用 `POST /api/anonymous/trial/start`。
- [ ] 确认客户端调用 `GET /api/anonymous/authorization`。
- [ ] 确认 files/processes/clipboard 三个模块都可进入。
- [ ] 确认授权状态显示“匿名 7 天全功能试用”。

Expected: 三个模块可用，无登录也能使用试用授权。

#### 场景 2：匿名试用过期后选择免费模块

Use DB to expire current anonymous trial:

```sql
UPDATE anonymous_device_trials
SET trial_expires_at = NOW() - INTERVAL '1 day'
WHERE device_id = '<current-device-id>';
```

- [ ] 重启桌面端或刷新匿名授权。
- [ ] 确认三个模块默认不可用或提示选择免费模块。
- [ ] 选择 `files` 作为免费模块。
- [ ] 确认 `POST /api/anonymous/trial/select-free-module` 成功。
- [ ] 确认 files 可用，processes/clipboard 不可用。

Expected: 匿名过期后可自助选择一个长期免费模块。

#### 场景 3：注册用户等待管理员审核

- [ ] 在桌面端打开注册。
- [ ] 输入邮箱或手机号。
- [ ] 发送验证码。
- [ ] 使用开发验证码 `123456` 完成校验。
- [ ] 设置密码并提交注册。
- [ ] 确认注册用户状态为 `pending_review`。
- [ ] 登录该账号。
- [ ] 确认授权状态显示“等待管理员审核”。
- [ ] 确认如果没有商业授权，仍按匿名兜底权限使用。

Expected: 注册、验证码、登录和 pending_review 展示都可用。

#### 场景 4：管理员审核通过并授予模块

- [ ] 打开管理后台。
- [ ] 使用 `admin@filekeeper.local` / `Password123!` 登录。
- [ ] 在用户列表找到 pending_review 用户。
- [ ] 审核通过。
- [ ] 进入用户详情。
- [ ] 设置 `deviceLimit = 1`。
- [ ] 设置 `offlineCacheMinutes = 60`。
- [ ] 授予 `files` 和 `clipboard` 模块。
- [ ] 不授予 `processes`。

Expected: 用户状态 active，权益列表包含 files/clipboard，设备限制和离线缓存设置保存成功。

#### 场景 5：桌面端登录用户设备绑定与授权查询

- [ ] 在桌面端登录审核通过的用户。
- [ ] 确认调用 `POST /api/client/devices/register`。
- [ ] 确认调用 `GET /api/client/authorization?deviceId=...`。
- [ ] 确认 files/clipboard 可用。
- [ ] 确认 processes 不可用，并显示未授权原因。
- [ ] 确认授权状态显示离线缓存截止时间。

Expected: 登录授权优先，匿名授权只作为兜底。

#### 场景 6：设备上限

- [ ] 保持用户 `deviceLimit = 1`。
- [ ] 在第二个本地设备身份上登录同一账号，或手动清理/替换 deviceId 模拟第二台设备。
- [ ] 确认 `POST /api/client/devices/register` 返回 409。
- [ ] 确认桌面端显示设备数量超限。
- [ ] 在管理后台禁用旧设备。
- [ ] 再次尝试新设备登录。

Expected: 第二台设备被拒绝；禁用旧设备后新设备可绑定。

#### 场景 7：管理员禁用设备后授权失效

- [ ] 管理后台进入用户详情。
- [ ] 禁用当前桌面端设备。
- [ ] 桌面端刷新授权或重新登录。
- [ ] 确认该设备的登录授权不可用。
- [ ] 确认客户端回退匿名授权或显示设备禁用原因。

Expected: 禁用设备后不能继续使用商业授权模块。

#### 场景 8：离线缓存策略

Case A: `offlineCacheMinutes = 60`

- [ ] 管理后台设置用户 `offlineCacheMinutes = 60`。
- [ ] 桌面端刷新登录授权。
- [ ] 停止后端或断网。
- [ ] 重启桌面端或触发授权刷新。
- [ ] 确认缓存期内仍可使用已授权模块。

Case B: `offlineCacheMinutes = 0`

- [ ] 管理后台设置用户 `offlineCacheMinutes = 0`。
- [ ] 桌面端刷新登录授权。
- [ ] 停止后端或断网。
- [ ] 确认登录授权不可离线使用，匿名也因联网要求不可用。

Expected: 离线行为与管理员配置一致。

#### 场景 9：模块授权到期降级

Use DB or 管理后台设置短过期时间：

```sql
UPDATE user_module_entitlements
SET expires_at = NOW() - INTERVAL '1 minute'
WHERE user_id = <user-id>
  AND module_code = 'clipboard';
```

- [ ] 桌面端刷新登录授权。
- [ ] 确认 clipboard 登录授权失效。
- [ ] 如果匿名授权允许 clipboard，则可匿名兜底；否则不可用。
- [ ] 确认 UI 显示过期或未授权原因。

Expected: 过期权益不再授权，模块访问降级符合规则。

### 6.4 联调记录模板

每次联调后，把结果追加到 `file-keeper/docs/superpowers/plans/2026-06-08-4商业计划开发进度.md` 或新建联调记录文档。

Recommended format:

```markdown
## 2026-06-10 真实环境端到端联调记录

### 环境

- Backend: http://localhost:8088
- Admin Web: <vite local url>
- Desktop: Tauri dev via dev-msvc.bat
- PostgreSQL: localhost:5432/file_keeper
- Redis: localhost:6379

### 自动化验证

- Backend tests: PASS/FAIL
- Desktop tests: PASS/FAIL
- Desktop build: PASS/FAIL
- Admin build: PASS/FAIL

### 手动场景

| 场景 | 结果 | 备注 |
|---|---|---|
| 匿名首次试用 | PASS/FAIL | |
| 匿名过期选免费模块 | PASS/FAIL | |
| 注册等待审核 | PASS/FAIL | |
| 管理员审核授权 | PASS/FAIL | |
| 登录用户设备绑定 | PASS/FAIL | |
| 设备上限 | PASS/FAIL | |
| 禁用设备 | PASS/FAIL | |
| 离线缓存 | PASS/FAIL | |
| 授权到期降级 | PASS/FAIL | |

### 发现的问题

- 问题 1：...

### 结论

- 可进入收口 / 需要修复后重测。
```

---

## 7. 阶段 6：商业版安全与运营增强

### Task 6-1：Rust 侧授权二次校验

**Priority:** 联调通过后做。

**Reason:** 当前门禁主要在前端，足以完成第一版功能验收，但商业版长期不应只依赖前端隐藏入口。

**Scope:**

- 进程结束命令。
- 截图/OCR 命令。
- 剪贴板监听命令。

**Plan:**

- [ ] 定义前端传入 Rust 的授权摘要结构。
- [ ] 对关键命令增加模块权限和快照有效期校验。
- [ ] 增加 Rust 单元测试。
- [ ] 使用 `file-keeper/src-tauri/build-msvc.bat` 或等价 MSVC 环境运行 cargo check/test。

### Task 6-2：管理后台 Dashboard

**Priority:** 已完成。

**Scope:**

- `file-keeper/admin-web/src/views/DashboardView.vue`
- 展示待审核用户数、即将过期授权数、活跃设备数。

**状态：** 后端验证 + 前端 build 已通过。

### Task 6-3：系统设置页

**Priority:** 已完成。

**Scope:**

- `file-keeper/admin-web/src/views/SettingsView.vue`
- 支持全局默认配置管理。

**状态：** 全量测试 + 端到端验证通过。

### Task 6-4：匿名设备运营页

**Priority:** 待做。

**Current fact:** 服务端目前有匿名试用用户端 API，没有管理端匿名设备列表、重置试用、禁用匿名设备 API。

**Adjusted decision:** 不把 `AnonymousDevicesView.vue` 作为当前必须补齐项。若后续需要客服处理匿名试用滥用，再新增：

- Admin anonymous device repository/service/controller。
- `GET /api/admin/anonymous-devices`
- `GET /api/admin/anonymous-devices/{deviceId}`
- `POST /api/admin/anonymous-devices/{deviceId}/disable`
- `POST /api/admin/anonymous-devices/{deviceId}/reset-trial`
- `file-keeper/admin-web/src/views/AnonymousDevicesView.vue`

---

## 8. 更新后的完成定义

第一版商业授权完成时，应满足：

- [ ] 服务端可启动并连接 PostgreSQL、Redis。
- [ ] 管理后台可登录超级管理员。
- [ ] 管理员可审核用户、禁用/启用用户。
- [ ] 管理员可授予、禁用、撤销模块权益。
- [ ] 管理员可设置单用户设备上限和离线缓存时长。
- [ ] 管理员可查看并禁用用户设备。
- [x] 管理后台 Dashboard 已可用（已在阶段 5E 完成）。
- [x] 管理后台系统设置页已可用（已在阶段 5E 完成）。
- [ ] 桌面端未登录时可获得匿名 7 天全功能试用。
- [ ] 匿名试用过期后可选择一个长期免费模块。
- [ ] 桌面端可注册、登录、退出账号。
- [ ] 登录成功后自动绑定设备并拉取授权快照。
- [ ] pending_review 用户有明确等待审核提示。
- [ ] active 用户只能使用已授权且未过期模块。
- [ ] 设备被禁用后商业授权失效。
- [ ] 设备数超限时有明确提示。
- [ ] 离线缓存行为符合管理员配置。
- [ ] 授权过期后客户端降级匿名权益。
- [ ] 文件收藏、剪贴板历史、截图内容不上传服务端。
- [ ] 后端测试、桌面端测试、桌面端构建、管理后台构建全部通过。
- [ ] 真实环境端到端联调记录完成。

---

## 9. 更新后的推荐提交节奏

不要按原 6 个阶段提交，因为实际阶段四已经跨阶段完成了很多内容。建议后续按真实缺口提交：

1. `fix: 统一商业授权开发环境服务端地址`
2. `feat: 桌面端添加账号登录注册入口`
3. `feat: 桌面端展示商业授权状态`
4. `feat: 支持匿名过期后选择免费模块`
5. `test: 记录商业授权端到端联调结果`
6. `feat: 强化商业授权关键命令校验`（可选后置）

提交前必须运行对应验证命令；未得到明确提交指令时，不自动提交。

---

## 10. 不做事项

以下事项不进入当前后续主线：

- 支付、订单、套餐。
- 文件收藏、剪贴板历史、截图内容等业务数据云同步。
- 管理后台大屏 Dashboard（已完成，不再作为待做项）。
- 全局系统设置页（已完成，不再作为待做项）。
- 匿名设备后台运营页，除非先新增服务端管理端匿名设备 API。
- 大规模重构 `App.vue`，除非新增账号 UI 时测试证明现有结构已经阻碍维护。

---

## 11. 执行顺序建议

如果现在继续开发，推荐顺序是：

1. 阶段 5A：统一 API 地址（注意 8088 已在联调中验证，需固化到默认配置）。
2. 阶段 5B：桌面端账号 API、Store、登录/注册 UI。
3. 阶段 5C：授权状态展示。
4. 阶段 5D：匿名免费模块选择。
5. 阶段 5E：真实环境端到端联调（Dashboard、系统设置已提前完成；匿名设备运营页可在本阶段后续补齐或延后）。
6. 阶段 6：Rust 二次校验和匿名设备运营页等运营增强。

理由：桌面端账号 UI 是商业闭环主路径；管理后台 Dashboard、系统设置页已在阶段 5E 中提前完成；匿名设备运营页属于运营增强项，不能替代用户从桌面端登录并获得授权的主流程。
