# 工作汇报模块（Work Report Module）总结

## 1. 模块职责一句话

工作汇报模块负责收集用户的工作记录、固定工作、未来计划，按日报/周报模板生成报告，支持 AI 智能总结，并可通过飞书/钉钉/企业微信/Slack 推送到指定目标；同时提供定时任务按 cron 表达式自动生成分发报告。

## 2. 涉及文件清单

### 前端（Vue 3 + TypeScript）

| 文件路径 | 说明 |
|---------|------|
| `file-keeper/src/api/workReport.ts` | 工作汇报 HTTP API 封装（工作记录、固定工作、未来计划、报告模板/配置/报告、推送凭据/目标） |
| `file-keeper/src/api/rustWorkReport.ts` | Tauri Rust 命令封装（Git 日志导入、系统通知、导出 Markdown） |
| `file-keeper/src/stores/workReportStore.ts` | 工作汇报核心状态管理，含工作记录、固定工作、未来计划、报告配置、历史报告、推送凭据、推送目标 |
| `file-keeper/src/components/work-report/WorkReportManagement.vue` | 工作汇报模块主容器，集成日志、固定工作、未来计划、报告配置、推送配置、历史报告 |
| `file-keeper/src/components/work-report/WorkLogEditor.vue` | 工作记录编辑/展示面板 |
| `file-keeper/src/components/work-report/FixedWorkPanel.vue` | 固定工作管理面板（日/周/月周期），支持选择复用的推送目标 |
| `file-keeper/src/components/work-report/FuturePlanPanel.vue` | 未来计划管理面板，支持选择复用的推送目标 |
| `file-keeper/src/components/work-report/ReportConfigForm.vue` | 报告配置表单（周期、模板、AI、选择已有推送目标） |
| `file-keeper/src/components/work-report/ReportHistoryList.vue` | 历史报告列表 |
| `file-keeper/src/components/work-report/ReportViewer.vue` | 报告预览组件 |
| `file-keeper/src/components/work-report/PushConfigPanel.vue` | 推送配置独立面板：分「凭据」「目标」两个子 Tab 管理可复用推送配置 |
| `file-keeper/src/components/work-report/PushTargetGuide.vue` | 各平台推送配置说明引导 |
| `file-keeper/src/components/work-report/PushTargetForm.vue` | 旧版内联推送目标编辑表单（当前未使用，保留备用） |
| `file-keeper/src/components/work-report/PushConfigFields.vue` | 旧版内联推送凭据/目标字段组件（当前未使用，保留备用） |
| `file-keeper/src/types/workReport.ts` | 工作汇报相关类型定义，含 `PushCredential` / `PushTarget` / `ReportConfig` 等 |

### 后端（Java / Spring Boot）

| 文件路径 | 说明 |
|---------|------|
| `file-keeper/server/src/main/java/com/superprogrammer/workreport/controller/WorkReportClientController.java` | 客户端接口：工作记录、固定工作、未来计划、报告模板/配置/生成/推送，以及推送凭据/目标 CRUD |
| `file-keeper/server/src/main/java/com/superprogrammer/workreport/controller/admin/AdminWorkReportController.java` | 管理后台接口 |
| `file-keeper/server/src/main/java/com/superprogrammer/workreport/service/WorkLogService.java` | 工作记录业务 |
| `file-keeper/server/src/main/java/com/superprogrammer/workreport/service/FixedWorkService.java` | 固定工作业务 |
| `file-keeper/server/src/main/java/com/superprogrammer/workreport/service/FuturePlanService.java` | 未来计划业务 |
| `file-keeper/server/src/main/java/com/superprogrammer/workreport/service/ReportConfigService.java` | 报告配置业务，通过 `report_config_push_targets` 中间表维护与推送目标的关联 |
| `file-keeper/server/src/main/java/com/superprogrammer/workreport/service/ReportTemplateService.java` | 报告模板业务 |
| `file-keeper/server/src/main/java/com/superprogrammer/workreport/service/WorkReportService.java` | 报告生成编排 |
| `file-keeper/server/src/main/java/com/superprogrammer/workreport/service/AiSummaryService.java` | AI 总结调用 |
| `file-keeper/server/src/main/java/com/superprogrammer/workreport/service/ReportTemplateEngine.java` | 报告模板渲染引擎 |
| `file-keeper/server/src/main/java/com/superprogrammer/workreport/service/ReportScheduleService.java` | 定时生成报告调度 |
| `file-keeper/server/src/main/java/com/superprogrammer/workreport/service/ReportPushServiceImpl.java` | 报告推送执行，从复用的 `PushTarget` / `PushCredential` 中读取目标与凭据 |
| `file-keeper/server/src/main/java/com/superprogrammer/workreport/service/PushCredentialService.java` | 推送凭据 CRUD，凭据加密后落库 |
| `file-keeper/server/src/main/java/com/superprogrammer/workreport/service/PushTargetService.java` | 推送目标 CRUD，引用凭据 ID |
| `file-keeper/server/src/main/java/com/superprogrammer/workreport/service/CredentialEncryptor.java` | AES/GCM/NoPadding 凭据加解密组件 |
| `file-keeper/server/src/main/java/com/superprogrammer/workreport/service/push/*` | 各平台推送实现（Feishu/DingTalk/WeCom/Slack） |
| `file-keeper/server/src/main/java/com/superprogrammer/workreport/repository/*` | 各实体 Repository（JdbcTemplate / MyBatis-Plus） |
| `file-keeper/server/src/main/java/com/superprogrammer/workreport/entity/*` | 实体类 |
| `file-keeper/server/src/main/java/com/superprogrammer/workreport/dto/*` | DTO/Request/VO |

**关键新增实体/DTO/Repository（推送复用）**

| 文件路径 | 说明 |
|---------|------|
| `entity/PushCredential.java` | 推送凭据实体（`push_credentials` 表） |
| `entity/PushTarget.java` | 推送目标实体（`push_targets` 表） |
| `entity/ReportConfigPushTargetRef.java` | 报告配置与推送目标关联实体（`report_config_push_targets` 中间表） |
| `dto/PushCredentialDto.java` | 凭据列表 DTO（不回显凭据内容，仅 `hasCredential`） |
| `dto/PushCredentialCreateRequest.java` / `PushCredentialUpdateRequest.java` | 凭据创建/更新请求 |
| `dto/PushTargetDto.java` | 目标列表 DTO，含 `credentialName` |
| `dto/PushTargetCreateRequest.java` / `PushTargetUpdateRequest.java` | 目标创建/更新请求 |
| `repository/PushCredentialRepository.java` | 凭据数据访问 |
| `repository/PushTargetRepository.java` | 目标数据访问 |
| `repository/ReportConfigPushTargetRefRepository.java` | 报告配置-目标关联数据访问 |

**已迁移的旧文件（V11 迁移后新逻辑不再直接使用）**

| 文件路径 | 说明 |
|---------|------|
| `entity/ReportPushTarget.java` | 旧报告推送目标实体（对应原 `report_push_targets` 表） |
| `dto/ReportPushTargetDto.java` / `dto/ReportPushTargetRequest.java` | 旧推送目标 DTO/Request |
| `repository/ReportPushTargetRepository.java` | 旧推送目标 Repository |
| `service/ReportPushServiceImpl.java` 中旧内联逻辑 | 已改为读取 `PushTarget` + `PushCredential` |

### 数据库迁移

| 文件路径 | 说明 |
|---------|------|
| `file-keeper/server/src/main/resources/db/migration/V11__extract_push_target_and_credential.sql` | 提取推送凭据与目标：建 `push_credentials`、`push_targets`、`report_config_push_targets` 表；迁移旧 `report_push_targets`、固定工作、未来计划内联凭据 |

### Rust Tauri

| 文件路径 | 说明 |
|---------|------|
| `file-keeper/src-tauri/src/commands/work_report.rs` | Tauri 命令：`fetch_git_logs`、`show_work_report_notification`、`export_report_markdown` |
| `file-keeper/src-tauri/src/main.rs` | 注册上述 work_report 命令 |

### 测试文件

| 文件路径 | 说明 |
|---------|------|
| `file-keeper/server/src/test/java/com/superprogrammer/workreport/WorkLogServiceTest.java` | 工作记录 CRUD 测试 |
| `file-keeper/server/src/test/java/com/superprogrammer/workreport/ReportConfigServiceTest.java` | 报告配置保存/更新/删除/权限测试 |
| `file-keeper/server/src/test/java/com/superprogrammer/workreport/WorkReportServiceTest.java` | 报告生成测试 |
| `file-keeper/server/src/test/java/com/superprogrammer/workreport/AiSummaryServiceTest.java` | AI 总结降级测试 |
| `file-keeper/server/src/test/java/com/superprogrammer/workreport/ReportTemplateEngineTest.java` | 模板渲染测试 |
| `file-keeper/server/src/test/java/com/superprogrammer/workreport/ReportScheduleServiceTest.java` | 定时调度测试 |
| `file-keeper/server/src/test/java/com/superprogrammer/workreport/ReportPushServiceImplTest.java` | 推送服务测试 |
| `file-keeper/server/src/test/java/com/superprogrammer/workreport/WorkReportClientControllerTest.java` | 客户端接口测试 |
| `file-keeper/server/src/test/java/com/superprogrammer/workreport/FeishuPusherTest.java` | 飞书推送测试 |
| `file-keeper/server/src/test/java/com/superprogrammer/workreport/CredentialEncryptorTest.java` | 凭据加解密测试 |
| `file-keeper/src/stores/__tests__/workReportStore.test.ts` | 前端 Store 测试 |
| `file-keeper/src/api/__tests__/workReport.test.ts` | 前端 API 测试 |

## 3. 核心数据流 / 调用链路

### 生成报告

```
用户点击「生成报告」
  → WorkReportManagement.vue
    → workReportStore.generateReport(configId)
      → POST /api/client/work-report/reports/generate
        → WorkReportClientController.generateReport()
          → WorkReportService.generate(userId, configId)
            → 查询 ReportConfig、ReportTemplate
            → 查询 WorkLog、FixedWorkCompletion
            → 若 aiEnabled：AiSummaryService.summarize(...)
              → 读取用户 AI 配置 → 调用大模型 API → 失败降级为简单拼接
            → ReportTemplateEngine.render(template, context)
            → 保存 WorkReport
    → ReportViewer.vue 展示 currentReport
```

### 定时调度

```
Spring @Scheduled(cron = "0 * * * * ?")
  → ReportScheduleService.scheduleReports()
    → 扫描所有 enabled 的 ReportConfig
    → shouldTrigger(config, now) 按 cron + timezone 判断是否触发
    → WorkReportService.generate(config.userId, config.id)
    → ReportPushService.pushReport(report.id) 异步推送
```

### 推送报告（新复用链路）

```
ReportPushServiceImpl.pushReport(reportId)
  → 查询 ReportConfig
  → 通过 report_config_push_targets 关联表加载 PushTarget 列表
  → 更新报告状态为 PUSHING
  → 遍历目标：
    → 根据 platform 选择 PushService 实现（Feishu/DingTalk/WeCom/Slack）
    → 从 push_credentials 读取并解密对应凭据
    → 调用 pusher.push(payload, target, decryptedCredential)
  → 记录 PushDelivery
  → 更新报告状态为 PUSHED 或 FAILED
```

### 报告配置保存时关联推送目标

```
ReportConfigForm.vue
  → workReportStore.saveConfig({ ...config, pushTargetIds: [...] })
    → POST /api/client/work-report/configs
      → ReportConfigService.save(userId, request)
        → 保存/更新 ReportConfig
        → syncPushTargetRefs(configId, pushTargetIds)
          → 校验目标归属当前用户
          → restoreOrInsert 关联 / 软删除不再使用的关联
```

### 固定工作 / 未来计划使用复用目标

```
FixedWorkPanel.vue / FuturePlanPanel.vue
  → 编辑时选择 store.pushTargets 中的某一项
  → 保存时仅提交 pushTargetId
  → 后端 FixedWorkItem / FuturePlan 仅存储 push_target_id 外键
```

### Git 日志导入

```
用户选择本地 Git 仓库
  → rustApi.fetchGitLogs(repoPath, since, until)
    → invoke('fetch_git_logs')
      → commands/work_report.rs::fetch_git_logs
        → 校验 work-report 模块离线授权
        → 执行 git log 并解析
    → workReportStore.importGitLogs() 逐条创建 WorkLog
```

## 4. 关键状态与 Store 说明

### `workReportStore` (`file-keeper/src/stores/workReportStore.ts`)

- **State**: `logs`、`fixedWorkItems`、`futurePlans`、`templates`、`configs`、`reports`、`currentReport`、`currentDate`、`startDate`、`endDate`、`activeMainTab`、`activeFixedSubTab`、`loading`、`error`、`pushCredentials`、`pushTargets`
- **计算属性**: `todayLogs`、`sortedFixedWorkItems`、`sortedFuturePlans`
- **核心方法**:
  - `loadToday(date?)` / `loadLogs(start, end)`：加载工作记录
  - `saveLog` / `removeLog`：保存/删除工作记录
  - `loadFixedWork` / `saveFixedWork` / `toggleFixedWork` / `removeFixedWork`：固定工作
  - `loadFuturePlans` / `saveFuturePlan` / `completeFuturePlan` / `cancelFuturePlan` / `removeFuturePlan`：未来计划
  - `loadTemplates` / `loadConfigs` / `saveConfig` / `deleteConfig`：报告配置
  - `generateReport` / `loadReports` / `pushReport` / `removeReport`：报告实例
  - `importGitLogs`：从 Git 仓库导入提交记录
  - `loadPushCredentials` / `savePushCredential` / `deletePushCredential`：推送凭据
  - `loadPushTargets` / `savePushTarget` / `deletePushTarget`：推送目标

## 5. 关键组件说明

| 组件 | 文件路径 | 功能 |
|------|---------|------|
| `WorkReportManagement` | `file-keeper/src/components/work-report/WorkReportManagement.vue` | 模块主入口，Tab 切换日志/固定工作/未来计划/推送配置，右侧面板承载报告配置、推送配置、历史报告 |
| `WorkLogEditor` | `file-keeper/src/components/work-report/WorkLogEditor.vue` | 工作记录增删改查，支持多日期范围 |
| `FixedWorkPanel` | `file-keeper/src/components/work-report/FixedWorkPanel.vue` | 固定工作管理，按日/周/月周期，支持完成切换与选择复用推送目标 |
| `FuturePlanPanel` | `file-keeper/src/components/work-report/FuturePlanPanel.vue` | 未来计划管理，支持完成/取消，选择复用推送目标 |
| `ReportConfigForm` | `file-keeper/src/components/work-report/ReportConfigForm.vue` | 报告配置表单：名称、周期、模板、AI 开关、AI 配置选择，并以 checkbox 列表选择已有的推送目标 |
| `ReportHistoryList` | `file-keeper/src/components/work-report/ReportHistoryList.vue` | 历史报告分页列表 |
| `ReportViewer` | `file-keeper/src/components/work-report/ReportViewer.vue` | 报告 Markdown 预览、导出、推送 |
| `PushConfigPanel` | `file-keeper/src/components/work-report/PushConfigPanel.vue` | 推送配置独立面板：凭据管理 + 目标管理，支持同一凭据被多个目标复用、同一目标被多个报告配置复用 |
| `PushTargetGuide` | `file-keeper/src/components/work-report/PushTargetGuide.vue` | 各平台机器人配置图文引导 |
| `PushTargetForm` | `file-keeper/src/components/work-report/PushTargetForm.vue` | 旧版内联推送目标表单（当前由 `PushConfigPanel` 取代，保留备用） |
| `PushConfigFields` | `file-keeper/src/components/work-report/PushConfigFields.vue` | 旧版内联推送字段组件（当前由 `PushConfigPanel` 取代，保留备用） |

## 6. 核心功能详解

### 工作记录
- 按日期记录工作内容，支持标签、来源（MANUAL/GIT）
- 支持日期范围查询
- 可从本地 Git 仓库批量导入提交记录

### 固定工作
- 周期类型：每日 / 每周 / 每月
- 支持提醒时间、提醒日期、时区
- 完成状态在报告生成时作为固定工作完成情况汇总
- 提醒推送通过选择复用的 `PushTarget` 实现，不再在固定工作中内联存储凭据

### 未来计划
- 记录未来待办事项，支持提醒
- 状态流转：PENDING → REMINDED/COMPLETED/CANCELLED
- 提醒推送同样引用复用的 `PushTarget`

### 报告配置
- 日报/周报类型
- Cron 表达式控制生成周期（内置常用选项）
- 选择报告模板
- AI 总结开关 + 指定 AI 配置（或默认配置）
- 通过 `pushTargetIds` 关联多个已有的 `PushTarget`，目标与配置多对多

### AI 总结
- 后端 `AiSummaryService` 根据用户 AI 配置调用大模型
- 支持 qwen / doubao / claude 三种 Provider
- API Key 服务端 AES 加密存储
- 调用失败时降级为简单 Markdown 拼接

### 报告生成
- 基于模板引擎 `{{key}}` 占位符替换
- 上下文包含：ai_summary、logs、fixedWork、report_type、generated_at

### 推送
- 支持飞书、钉钉、企业微信、Slack
- 异步推送，记录每次推送结果
- 失败支持重试（PushRetryService 定时扫描，最大 3 次）

### 推送凭据与目标复用设计

为支持同一机器人凭据/目标在多个报告配置、固定工作、未来计划中复用，推送配置被拆分为三层：

| 层级 | 实体 | 作用 | 复用关系 |
|------|------|------|---------|
| 凭据 | `PushCredential` | 存储平台机器人密钥（加密） | 一个凭据可被多个 `PushTarget` 引用 |
| 目标 | `PushTarget` | 平台 + 目标类型 + 目标 ID + 引用凭据 | 一个目标可被多个 `ReportConfig`、固定工作、未来计划引用 |
| 关联 | `ReportConfigPushTargetRef` | 报告配置与推送目标的多对多关联 | 配置变更时仅维护关联关系 |

安全与隐私：
- 凭据字段 `credential_enc` 使用 AES/GCM/NoPadding 加密，密钥来自配置 `work-report.credential-key`
- 未配置密钥时开发环境回退为明文存储（会打印警告）
- 列表/详情接口不回显凭据内容，仅返回 `hasCredential: true/false`
- 编辑凭据时留空表示不修改原值

## 7. 测试覆盖说明

| 测试文件 | 覆盖内容 | 状态 |
|---------|---------|------|
| `WorkLogServiceTest` | 工作记录 CRUD | 已覆盖 |
| `ReportConfigServiceTest` | 报告配置保存/更新/删除/权限，含推送目标关联 | 已覆盖 |
| `WorkReportServiceTest` | 日报/周报生成、AI 禁用、权限校验 | 已覆盖 |
| `AiSummaryServiceTest` | 无数据返回、无配置降级、日常提示内容 | 已覆盖 |
| `ReportTemplateEngineTest` | 模板占位符替换 | 已覆盖 |
| `ReportScheduleServiceTest` | cron 触发判断 | 已覆盖 |
| `ReportPushServiceImplTest` | 推送执行、状态流转 | 已覆盖 |
| `WorkReportClientControllerTest` | 客户端接口授权与生成 | 已覆盖 |
| `FeishuPusherTest` | 飞书消息构建 | 已覆盖 |
| `CredentialEncryptorTest` | 凭据加解密 | 已覆盖 |
| `workReportStore.test.ts` | 前端 Store 状态与 API 调用 | 已覆盖 |
| `workReport.test.ts` | 前端 API 层 | 已覆盖 |

**未覆盖区域（当前测试缺口）**:
- AI 配置成功调用各 Provider 的端到端测试
- 各推送平台（钉钉/企业微信/Slack）实际 HTTP 调用测试
- 前端组件级测试（WorkReportManagement、ReportConfigForm、PushConfigPanel 等）
- Rust `fetch_git_logs` 命令的集成测试
- 定时调度与真实时间推进的集成测试
- `PushCredentialService` / `PushTargetService` 的独立单元测试

## 8. 已知缺口 / TODO

1. **AI 配置缺少独立测试**：新增 `AiConfigController` / `AiConfigService` 尚未补充单元测试。
2. **AI Provider 扩展性**：当前仅支持 qwen/doubao/claude，新增 Provider 需改 `AiSummaryService` switch。
3. **报告模板前端编辑**：当前模板由后端预置或管理后台维护，桌面端未提供模板编辑 UI。
4. **推送重试策略**：已有 PushRetryService，但重试间隔、最大次数、死信处理可进一步细化。
5. **定时任务单机调度**：当前使用 Spring `@Scheduled`，分布式部署时需考虑 Quartz/ ShedLock。
6. **AI 调用 Token 审计**：未记录每次 AI 调用的 Token 消耗与耗时。
7. **报告导出 UX**：导出 Markdown 后无自动打开或提示。
8. **Windows 系统通知未实现**：`show_work_report_notification` 在 Windows 下为预留空实现。
9. **旧组件/旧实体清理**：`PushTargetForm.vue`、`PushConfigFields.vue` 及后端 `ReportPushTarget*` 等旧文件当前未使用，后续可确认无引用后删除，避免维护两套逻辑。

## 9. 与其他模块的依赖关系

| 依赖模块 | 依赖方式 | 说明 |
|---------|---------|------|
| **公共与基础设施** | 全局 | 使用 `useI18n`、Tailwind 主题、Pinia 持久化、`useToast` |
| **认证与商业授权** | 全局 | `WorkReportClientController` 校验 `work-report` 模块授权；桌面端通过 `commercialAuthStore.isModuleAllowed('work-report')` 控制入口 |
| **AI 接入模块** | `AiSummaryService` → `AiConfigService` | 报告生成时读取用户 AI 配置进行总结 |
| **设置模块** | `SettingsDialog.vue` | AI 模型配置入口放在全局设置中 |
| **管理后台** | `admin-web` | 管理员给用户授权 `work-report` 和 `ai` 模块 |

---

*本总结基于 2026-06-28 的代码快照生成。*
