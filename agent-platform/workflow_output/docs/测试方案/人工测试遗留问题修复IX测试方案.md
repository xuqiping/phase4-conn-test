# 人工测试遗留问题修复IX · 测试方案

> 人工交互验证（思考档位体感/画布开关行为/联网真实调用，机器测不了）。自动化已绿：后端 2706/2706、前端 988/988。
> 联动用例对齐 plan L1-L7（含正向/反向/半选/批量）。

## O 系列 · 思考强度（智能对话）

**前置**：至少一个 Claude 协议模型（三档全）+ 一个未声明 thinking 的 OpenAI 系模型（如 glm-5.3）在模型列表。

| # | 用例 | 步骤 | 预期 |
|---|---|---|---|
| O1 | 三档体感 | 选 Claude 模型，「🧠 关」问推理题 → 记录时长/💭 长度 → 「标准」再问 → 「深度」再问 | 💭 长度 关<标准<深度；深度等待明显变长；回答质量不降 |
| O2 | L1 预扣联动 | 深度档发消息，看开局预扣与消耗明细 | 预扣≈标准档 ×4 量级；答完多退少补，DEBT 不出现 |
| O3 | L1 反向 | 「关」档发消息 | 预扣与改造前同口径（2048 帽），💭 极短/无 |
| O4 | L2 切模型档位回落 | 深度档 → 切 glm-5.3（未声明）→ 再切回 | glm 下拉消失+请求不带字段；切回后档位恢复 |
| O5 | L2 半选 | 深度档 → 切只有 toggle 声明的模型（[关,标准]） | 下拉只两档，当前显「关」（回落 levels[0]），不显裸值 DEEP |
| O6 | 持久化 | 选「深度」刷新页面 | 下拉仍「深度」 |
| O7 | L3 max_tokens 抬升 | 深度档问长问题 | 不出现 400（budget>max_tokens 场景被只抬不降保护）；回答不被思考挤没 |
| O8 | SSE/WS 双路 | O1 分别在 SSE（普通页）与 WS 生效路径重试 | 两路口径一致（VIII B2 切换条件触发） |
| O9 | 反向回归 | 记忆内部链路：跑一次文件入库（蒸馏/打标） | 行为不变（disableThinking 优先级链保命），无报错 |

## P 系列 · 画布连线保留开关

**前置**：画布摆 A→B→C 链（A,B 相邻，C 在外）。

| # | 用例 | 步骤 | 预期 |
|---|---|---|---|
| P1 | L4 正向 | ⛓ 开（默认）框选 A,B → Ctrl+C → 鼠标移空处 Ctrl+V | 粘贴体含 A'→B' 内边 + **B'→C 跨集边**（副本接入原上下文） |
| P2 | L4 反向 | ⛓ 关 → 同样复制粘贴 | 仅 A'→B' 内边，零跨集边；无确认弹窗 |
| P3 | L4 时点判定 | ⛓ 开复制 → 切 ⛓ 关 → 粘贴 → 再切开 → 再粘贴 | 第一贴无跨集边、第二贴有（粘贴当下态生效） |
| P4 | L5 上游面板 | P1 后点 A' | 上游面板显示原 C 等外部上游，断链灰显消失 |
| P5 | L5 反向·悬挂 | 复制后删掉 C（连同其边）→ ⛓ 开粘贴 | 跨集边静默丢，无断线残留，无报错 |
| P6 | L6 批量·平行边 | ⛓ 开连按 Ctrl+V 三次 | 三份副本各带跨集边=对 C 三条平行线，可逐条点选删 |
| P7 | L6 撤回 | P6 后 Ctrl+Z 一次 | 该次粘贴的节点+全部边（含跨集）一步消失 |
| P8 | 副本门 | ⛓ 开：C 节点「创建副本」→ 数边；⛓ 关再副本 | 开=克隆全部入/出边（现状）；关=副本零边 |
| P9 | L7 占位符口径 | ⛓ 关做副本 → 看副本提示词 `@C` | 占位符仍指原 C（不重写）；上游面板可能断链灰显——重新连线恢复 |
| P10 | 组边排除 | 组员+组外节点混合框选复制粘贴 | 组边（广播/聚合）不带入粘贴体 |
| P11 | 持久化 | ⛓ 关刷新页面 | ⛓ 仍关 |
| P12 | 自动保存 | P1 后等防抖落库刷新 | 跨集边入库重现（快照就是普通边） |

## Q 系列 · 联网搜索部署验证（管理员，dev 现状=零配置）

| # | 用例 | 步骤 | 预期 |
|---|---|---|---|
| Q1 | 快路 Tavily | 配置页贴 key → 总开关开 → provider=tavily → 「测试连通」 | results>0、providerAvailability.tavily=true |
| Q2 | 自建路 SearXNG | Docker 起（`formats:[json]`）→ env `SEARCH_SEARXNG_BASE_URL` → provider=builtin → 测试 | results>0、builtin=true（缺 json 格式=被 JS 挑战挡，返回空） |
| Q3 | 端到端 | 智能对话「🌐 联网：开」问「今天什么日期」 | 回答带 [n] 引用 + 📎 来源外链可点 |
| Q4 | 反向 | 切「🌐 联网：关」再问 | 纯模型作答，无引用，不报错 |
| Q5 | 会话持久 | 开联网刷新页面 | 开关保持开 |
| Q6 | 降级链 | 填错 key/停掉 SearXNG 再问 | 不炸：AI 正常作答无引用；测试按钮 results=0 |
| Q7 | 总开关关 | 配置页总开关关，用户侧仍开 🌐 | 搜索被禁用（无引用），聊天不受影响 |

## 出口

O1-O9 / P1-P12 / Q1-Q7 全过 → 问题单三项销项。
