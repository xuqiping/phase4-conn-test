# 已解决
1. 无法上传本地文件
2. 点击生成的图片放大镜打开无效
3. 打开了水印的按钮，实际生成无水印
4. 参考图中，图片无法正常展示
5. pro中，支持的参数是1k、1.5k、2k
7. 无限画布中，图片节点中的AI生图还是待接入
8. 无限画布中，图片进行焦点编辑，提取为新图节点后，实际出来的节点无内容
1(二轮). 历史里面有之前生成过的图片，但是我不点开来看不到图片内容，我需要你给我直接在记录右侧放一个小图，展示对应的图片（行右侧首图懒加载缩略，点击放大灯箱；playwright 复验 C5/C6 过）
2(二轮). 图片生成的记录在视频生成历史里也展示了，弄混了（/api/media/tasks 加 kind 参数 SQL 层过滤；playwright 复验：图片页无视频行、视频页 9 行全 Cdance2.0 无 Seedream）
3(二轮). 当我点击历史里的某个记录时，左侧的参数需要还原回当时这条记录生成的参数（点行拉详情全量回填含参考图；admin+admin123 双账号 playwright 复验 C7 过）
4(二轮). 需要可以筛选提示词、时间（筛选条 q+时间范围服务端过滤；playwright 复验 C2/C3/C8 过，含反例空态；复验中补修 daterange 两坑：naive-ui 默认需点「确认」才提交 v-model→加 close-on-select+update-value-on-close；结束日是 00:00 致同日区间 from==to 被后端 400→前端 to 换算当天 23:59:59.999）
5(二轮). 历史记录里，我点击某一个记录的时候，要展示出我向大模型发送时的实际参数，以便我核对参考图等是否正确发送了，json格式即可（结果区「请求参数」弹窗，提交参数页签含 refFileIds；图片路径暂无 Provider 快照，页签明示「该历史任务未记录发送快照」；playwright 复验 C10 过）

# 未复验（第二轮已修复+自动化测试绿，待人工复验，测试方案见 docs/测试方案/图片生成第二轮问题修复测试方案.md）
6. pro调用报错：生图调用失败: Timeout on blocking read for 90000000000 NANOSECONDS（第一轮已改 90s→180s，待复验）

# 未解决
1. 鼠标放在参考图上或者点击后，要求可放大预览
2. 图片加载过一次之后，需要进缓存，免得每次都要重新从服务器加载资源加大开支
3. 鼠标放在参考图上或者点击后，要求可放大预览

# 参考feature-map、user-ops
- agent-platform\workflow_output\docs\user-ops\生图大模型接入用户操作手册.md
- agent-platform\workflow_output\docs\feature-map\生图大模型接入.feature-map.md

>解决完后需要同步更新对应的feature-map和user-ops
