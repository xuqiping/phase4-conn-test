# 计划12 · 个人记忆重设计 · 开发进度5

> 迭代 D（召回 pipeline 七步）。plan：[计划12-D-召回.md](../../../项目工程文档/计划/计划12-D-召回.md)。主索引：[计划12-个人记忆重设计-实现开发计划.md](../../../项目工程文档/计划/计划12-个人记忆重设计-实现开发计划.md)。
> 规范：[Phase3.1_开发实现速查.md](../../../../2_编程类可迭代workflow/Phase3.1_开发实现速查.md)。

## 本轮做了什么（迭代 D 进行中 · 7 chunk TDD）

召回七步全链（设计 §3.3）：①过滤 → ②聚合标签 → ③LLM 选标签 → ④优先读自己总结 → ⑤reflect 判深读 → ⑥拼未覆盖流水账 → ⑦装配。依赖 A+B+C+I1 全就绪。

### 矛盾点 + 偏离裁决（承 C「不碰 legacy」先例）
plan D 文件清单写「改 MemoryService / MemoryScopeResolver / MemoryScope / MemoryController / 复用 MemoryQueryCache」——**全是 legacy V33 栈**（基 `is_global`+`user_memory_projects`+`UserMemory` 旧表）。计划12 = 完全替换旧栈，C 已立裁决「新建独立链避免新旧表纠缠」（commit 至 `5ffcaef5`）。D 同裁决：
1. **新建 `MemoryRecallPipeline`**（非改 legacy MemoryService 1278 行）。
2. **新建 `RecallScope` + `MemoryRecallScopeResolver`**（非改 legacy MemoryScope/MemoryScopeResolver，二者基 `includeGlobal`+`user_memory_projects`）。
3. **新建 `MemoryRecallController`** `/api/chat/memory/recall*`（非改 legacy MemoryController `/api/chat/memories`，H 收尾 404）。
4. **cache 思路复用，组件不复用**：`MemoryQueryCache` 基 legacy `UserMemory`+legacy scope；新模型 scope 多选（个人 ∪ 项目 ∪ direction ∪ timeWindow ∪ 离职开关）scopeSig 不同，重算。
5. **scope 持久化存储 D-7 定**：`SystemSettingService` 全局表无 user_id 列，per-user 跨会话沿用需新建小表（V51，H 的 V50 之后）或 KV 带 uid；D-1~D-6 先做召回逻辑，持久化是 controller 层。
6. **memory_summaries 表已建（V47）但数据空**（E 才填 worker）：D 可跑通——空表 → 0 条 → 走 turns 兜底；summary 路径 IT 自造测试数据，不卡 E。

**I3 接口预留**：L10 离职开关（`includeDeparted`）+ DEPARTED 过滤本迭代留字段不实装（接入在 I3），`readableAuthors`（I1）含 DEPARTED 不滤。

### 现成件复用映射（Agent 勘探）
| D 需要 | 复用 | 缺口（新 mapper 方法） |
|---|---|---|
| 批量 coverage IN | `MemorySummaryCoverageMapper.findByUserAndTurns` | — |
| halfvec NN 标签 | `MemoryTagMapper.findNearestByAnchor`/`findWithinAnchorThreshold` | — |
| 标签聚合 join | — | `aggregateTagLabelsByScope`（D-2） |
| summary by tag+scope | — | `findSummariesByTagAndScope`（D-4） |
| turns gen_done 过滤 | — | `findRecallableTurns`（D-5） |
| BM25 tsv 查询 | — | `findByAnchorTokensTsv`（D-3） |

复用件：`RrfFusion`(静态) / `JiebaTokenizer`(静态) / `QueryExpansionService` / `LlmGateway.chat+embed` / `MemoryGenerator`+`MemoryTagResolver`(LLM judge 模板) / `MemoryTagAnchorService`(anchor) / `MemoryRecallAclResolver`(I1)。

### D-1 RecallScope 模型 + Resolver + DTO（✅ commit `feat 计划12迭代D-1`）
- 新 `RecallScope` record + `RecallDirection` enum + `RecallTimeWindow` record（参数容器：个人 on/off ∪ 项目集 ∪ 方向 ∪ 时间窗 ∪ 离职开关）。
- 新 `MemoryRecallScopeResolver`：解析用户勾选 → RecallScope；项目经 `listAccessibleProjectIds` 过滤防越权（向量2）；入参 null/空兜底默认 {个人}。
- 新 DTO：`MemoryRecallScopeRequest`（勾选提交）/ `MemoryRecallScopeView`（回显 + 可选项目集）。
- 持久化 store 推 D-7（SystemSetting 全局表无 user_id，per-user 方案 D-7 定）。
- TDD：MemoryRecallScopeResolverTest **10 绿**（Mockito，null/默认兜底/越权过滤/方向/时间窗/空召回）。

### D-2 标签聚合 ②（✅ commit `feat 计划12迭代D-2`）
- 新 `RecallTagMeta` DTO（id/subject/topic/label/ownerUserId/usageCount；对外只露 label+subject+topic 向量4，owner 供 D-6 装配）。
- mapper 新增双查：`findPersonalRecallTags`（本人 born_personal=true turns + 本人个人总结 project_id IS NULL）/ `findProjectRecallTags`（项目 turns × readableAuthors + 本人项目总结）；均 gen_done=true 限定 + timeWindow/direction 动态过滤 + tag_id IN scope 子查询天然排除 scope 外标签（向量3 防泄敏）。
- 新 `MemoryTagAggregator`：personalOn → 个人查；每项目经 `MemoryRecallAclResolver.readableAuthors`（I1）算可读作者，**空集 skip 防越权**（向量14）；合并按 tag_id 去重（个人+自挂项目同 tag 重复）。
- TDD：MemoryTagAggregatorTest **9 绿**（空召回/越权 skip/去重/多项目合并/参数透传）。
- **XML 正确性留 D-6 全链 IT 验**（单测 mock mapper 不启动 Spring；模式标准 halfvec `&lt;=&gt;` 转义 + `&amp;&amp;` + foreach 已对齐 A 现有范式）。
- **去重粒度裁决**：按 tag_id（含 owner 维度），非按 (subject,topic)——跨 user 的 (subject,topic) 合并是 D-6 ⑦装配展示层（防通过标签清单组合推断，同时不丢 owner 打前缀）。

### D-3/D-4 已落（commit `feat 计划12迭代D-3` / `D-4`，详见 plan D 实施进度段）
- **D-3** LLM 选标签：>30 halfvec+BM25 RRF 粗筛 top-30 + LLM 精选 3 重试 + 降级 candidates 全集（不返 null，内部消化偏离设计「null」）+ 向量3（foreach IN 限定聚合 tagIds）+ 向量12（query `<memory_data>`）。8 单测绿。
- **D-4** 读总结 + reflect：findSummariesForRecall（恒 user_id=self 向量14，tag+scope 动态+status≠STALE+timeWindow）+ ≤5 全 L2 / >5 reflect 批量 LLM 选深读 / 0 走 turns / reflect 失败降级全只读 L1。7 单测绿。

### D-5 拼流水账 ⑥（✅ commit `feat 计划12迭代D-5`）
- mapper 新增双查（MemoryTurnMapper）：`findPersonalRecallableTurns`（born_personal=true AND gen_done=true）/ `findProjectRecallableTurns`（project_ids && X × readableAuthors AND gen_done=true）；direction/timeWindow 过滤 created_at。
- 新 `MemoryTurnPatcher`：personalOn + 各项目（readableAuthors 空集 skip 向量14）合并去重 by turn id → 批量 `findByUserAndTurns` 一次 IN 取 coverage（**防 N+1** 性能预案）→ **allCovered 严格策略**（turn.tag_ids 每个 tag 都有 coverage(user_id=self) 行才跳原文走总结；某 tag 未覆盖/无 tag → 拼原文保守不丢）。
- 召回恒只认 gen_done=true（mapper SQL 限定，raw 不参与召回也不兜底）。
- TDD：MemoryTurnPatcherTest **8 绿**（空 scope/全覆盖跳/部分覆盖返/无 tag 返/项目空集 skip/项目命中/批量单查/去重）。

### D-6 主流程编排 + 装配 ①⑦（✅ commit `feat 计划12迭代D-6` `a9c661f9`）
- 新 `MemoryRecallResult` / `RecallTraceStep` DTO（assembledText + selectedTags + counts + degraded + notes + traceId + steps）。
- 新 `MemoryRecallPipeline` @Service 串七步：①resolve → ②aggregate → ③select → ④⑤read → ⑥patch → ⑦assemble，每步 `System.nanoTime` 计时 + `RecallTraceStep` 打点。
- **⑦ 装配格式裁决**（设计 §3.3 line 110 落地）：行 `- {owner前缀}{subject前缀}{topic}：{content}`。
  - owner≠self 加 `user#{id}·` 占位（D-7 前端查用户名美化，D-6 不引 UserService 解耦）。
  - subject='我'/null/空 → 省主体（owner≠self 时 owner 名天然替代「我」，对齐 line 110「【张三·爱好】」）。
  - subject 非「我」（表哥/配偶…）→ 保留主体 `{subject}·`。
  - turn 无单值 subject/topic（多 tag 难归属）→ 直接 `- {owner?}[{direction}] {rawContent}`。
- **tags 空跳 select/read**：aggregate 返空时跳 selector/reader 直走 patch turns 兜底（防 unstubbed mock 返 null NPE + 语义「无标签→turns 兜底」）。
- **selector 降级启发式**：selector 不暴露降级信号，pipeline 用 `tags.size()>COARSE_TOP && selected.size()==tags.size()` 判 LLM 全失败用全集 → 标 degraded。
- **降级链**：子组件内部已消化 LLM 失败（不抛），pipeline 每步再 try-catch 兜 mapper/意外异常 → `notes` 收明细 + `degraded=true`，不中断后续步。
- **最多 2 次 LLM**：select(1) + reflect(1，仅 summary>5 触发)；embed 不计 chat LLM。
- TDD：MemoryRecallPipelineTest **15 绿**（空 scope/happy path/4 种 owner×subject 前缀/includeL2 true-false/turn 装配/aggregate+read 抛降级/select 返空/selector 降级启发式/steps 6 步/scope 透传）。
- **XML 正确性 + 全链 IT 仍留 D-7**（单测 mock 5 子组件不启 Spring）。

## 下一步
- **D-7 Controller**（preview 透出 + scope 持久化 GET/PUT）+ 全链 IT（验证 D-2~D-5 XML + 装配端到端 + 越权隔离 A 读不到 B + <2s）。scope 持久化存储方案 D-7 定：per-user 新表 V51 vs KV 带 uid（SystemSetting 全局表无 user_id 列）。
