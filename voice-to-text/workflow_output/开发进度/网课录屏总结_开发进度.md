---
功能: 网课录屏总结
日期: 2026-08-06（续：Step3 完成，质量门入库并首次全绿）
Phase: Phase3 开发实现（Step1-3 done，Step4 待续）
---

# 网课录屏总结 · 开发进度

> **本文件已满（Step1-3 + 环境）；Step4/5 起续记于 [网课录屏总结_开发进度2.md](网课录屏总结_开发进度2.md)。**

## ✅ Step3 屏幕捕获已完成并 commit（`3b97d66`，2026-08-06）

- **定版**：windows-capture **2.0.0**（稳定版，自带 MediaFoundation 硬编 → Step4 直接复用其 VideoEncoder）
- **代码**：`screen/mod.rs` + `screen/capture.rs`（FR-101）
  - `list_windows()`（EnumWindows 封装，滤空标题）/ `start(hwnd, clock)` / `stop()` / `status()`
  - 每帧 `frame_ts = clock.now_ms()`；clock=None 时 capture 自带实例，**Step4 注入 session 钟即可统一 t0**（接口已预留 `Option<Arc<SessionClock>>`）
  - 像素不出捕获线程，channel 只传 `FrameMeta{ts,w,h}`（零拷贝验证；Step4 编码器放 handler 里）
  - 30fps 上限：`MinimumUpdateIntervalSettings::Custom(33ms)`，**本机 Win10 不支持 IGraphicsCaptureSession5 → 自动降级 Default**（已 log warn，勿再踩）
  - 最小化停帧 → `stalled`（>2s 无帧）经 `get_capture_status` 上报；窗口关闭 → `on_closed` 结束
  - 安全：start 前 `is_valid()` 校验 hwnd；只捕选定窗口
  - 运维埋点：`[screen]` 日志带 hwnd+标题（Step4 换 sessionId 当 traceId），滚动 fps 每 2s
- **lib.rs**：`mod screen` + `ScreenState` + 4 命令（`list_windows`/`start_window_capture`/`stop_window_capture`/`get_capture_status`）
- **capabilities**：Tauri2 自定义命令**无需** capability 配置（仅插件命令需要），default.json 不动
- **测试**：`cargo test` **10 绿 + 1 ignored**；真实捕获冒烟测试 `cargo test -- --ignored real_capture` 手动跑过（7.6s，帧到达/单调/fps 上限）
- **验证**：30fps 上限逻辑已测；本机实际帧率走 Default（平台限制）；UI 选窗手测留 Phase4

## ✅ 质量门入库并首次全绿（`f5d527d`）

- `scripts/check_all.bat|.sh` + `check_docs.py` + `pre-commit.sample` 入库（此前一直在本地未 commit）
- 顺手还清了两笔历史欠账（否则 gate 永远红）：
  - `网课录屏智能总结.design.md` 6114 tokens 超限 → 拆 3 文件：主文件（§〇~§二+总路由）/ `.功能规格.md`（§三）/ `.数据与验收.md`（§四~八+术语）
  - 失效链接 10 处：plan 03/04 的 AGENTS.md 需 `../../../`、plan 05 的 architecture.md 需 `../../specs/`、design 指向 src-tauri 的链接全部需 `../../../`
- 前端 `pinia` 缺失是 node_modules 残缺 → `npm install` 修复
- ⚠️ `check_all.bat` 经 Git Bash 的 cmd shim 跑会中文乱码误判 → **用 `bash scripts/check_all.sh`**
- ⚠️ `workflow_output/docs/` 被 .gitignore 忽略，文档修复**只在本地**（要入库用 `git add -f`）

## ✅ 编译环境已彻底修复（2026-08-05）

| 项 | 状态 / 配置 |
|---|---|
| C 盘满 | CARGO_HOME/RUSTUP_HOME/TMP/TEMP `setx` 到 `D:\dev-cache\` |
| Rust 工具链 | stable-msvc(default) + stable-gnu，1.97.1，装 D 盘 |
| cargo 镜像 | USTC sparse（`D:\dev-cache\cargo\config.toml`）|
| **MSVC Build Tools** | `D:\BuildTools`（VS Installer，C++ 桌面开发；装前清缓存到 C 盘 8G）|
| **libclang** | 复用 Anaconda：`D:\IT\anaconda\Library\bin\libclang-13.dll` → cp `libclang.dll`；`LIBCLANG_PATH` 指它 + PATH 加该 bin（依赖 dll）|
| **代理** | sherpa 预编译包从 GitHub 下 → 需代理 `127.0.0.1:7892`；`NO_PROXY` 排除 USTC 镜像 + **`proxy.ustclug.org`**（cargo fetch 重定向主机，2026-08-06 新增）|

**编译命令备忘**（新 Git Bash 不继承 setx，每次要 export）：
```bash
export RUSTUP_HOME='D:/dev-cache/rustup' CARGO_HOME='D:/dev-cache/cargo'
export TMP='D:/dev-cache/tmp' TEMP='D:/dev-cache/tmp'
export LIBCLANG_PATH='D:/IT/anaconda/Library/bin'
export PATH="/d/IT/anaconda/Library/bin:$PATH"
export HTTPS_PROXY='http://127.0.0.1:7892' HTTP_PROXY='http://127.0.0.1:7892'
export NO_PROXY='mirrors.ustc.edu.cn,mirrors.tuna.tsinghua.edu.cn,proxy.ustclug.org,static-crates-io.proxy.ustclug.org,localhost,127.0.0.1'
cargo check   # 或 cargo test --lib
```
详见 memory：`voice-to-text-build-env`。

## ✅ Step1 + Step2 已完成并 commit（`c8b60bb`，2026-08-05）

- **Step1 session**（FR-103）：`session/mod.rs`（SessionManager/SessionClock + 路径穿越防护）+ lib.rs 3 命令 → **6 单测全绿**
- **Step2 时间戳**（FR-102/AC-102）：`AudioFrame.capture_ts` / `RecognitionResult.start_ms/end_ms`，向后兼容
- `cargo check` 通过（3 个 dead_code warning = Step4 预留 API，预期）
- 测试方案（Phase3-A，联动 L1-L8）在 `workflow_output/docs/` —— 被 .gitignore 忽略未进 commit

## ⏭️ 下一步：Step4 视频硬编落盘 + 三路录制集成

- plan：`01_基础与录制.plan.md` 77-95 行
- **硬件已探**：本机 **RTX 2060 SUPER → NVENC 可用**（MediaFoundation 会自动选硬编；软编兜底由 MF 保证）
- **编码器**：直接用 windows-capture 2.0.0 的 `encoder::VideoEncoder`（`VideoSettingsBuilder`/`AudioSettingsBuilder::disabled(true)`/`ContainerSettingsBuilder`），放 `CaptureHandler` 里接 `frame`（参考 crate `examples/graphics_capture.rs`）；**但切片（15min 一段 `video/NNN.h264`）需自管**：到点 `finish()` 旧 encoder + `new()` 新文件
- 动作：`screen/encode.rs`（切片封装）→ `screen/scene_detect.rs`（SAD 预筛轻量入口）→ `session/mod.rs` 录制态 → `lib.rs` `start_capture_session(session_id, hwnd, audio_device)` / `stop_capture_session()`：三路（屏+音+转写）绑 session t0，转写实时 emit + 追加 jsonl
- **动手顺序建议**：先 4a `encode.rs` 切片单测（纯编码可自动测）→ 再 4b 三路集成（要手测）
- 验证：录 5min → session 目录有 video 切片 + audio.wav + transcript.jsonl（带时间戳）；三路时间戳同基准抽样核对

## 后续
- Step5-12：OCR / 对齐 / 总结 / 前端 / 集成收尾（见 `02_录后处理.plan.md` 等）
