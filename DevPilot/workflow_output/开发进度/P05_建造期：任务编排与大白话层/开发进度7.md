# P05 建造期：任务编排与大白话层 · 开发进度 7

> 本轮完成 S7 Build 视图 + 驾驶舱真实数据（FR-038/039/043）。

## 已完成工作

### S7 Build 视图 + 驾驶舱真实数据（FR-038/039/043）
- Tauri 新增数据命令（`src-tauri/src/commands.rs`）：
  - `list_tasks(project_id, round_id?)`：默认当前 open round，返回 chunk 排序任务列表。
  - `list_rounds(project_id)`：返回轮次 + 任务总数/done 数。
  - `list_task_events(task_id)`：返回某任务的叙事/raw/error/checkpoint 事件流。
- `src-tauri/src/lib.rs` 注册三个命令。
- `src-ui/lib/ipc.ts`：新增 `TaskDto`、`RoundDto`、`listTasks`、`listRounds`、`listTaskEvents`。
- `src-ui/views/dashboard/RoundTimeline.tsx`：真实 rounds + chunk 光点；active/done/todo 状态按 round 状态与任务统计推导。
- `src-ui/views/dashboard/TaskList.tsx`：真实任务列表，状态色标 + cost。
- `src-ui/views/dashboard/Dashboard.tsx`：缺陷=failed 数、覆盖率=done/总数、消耗=cost_cents 累计，均来自 `listTasks`；进度仍来自状态机快照。
- `src-ui/views/Build.tsx` 重写：
  - 任务流双栏（任务列表 + 事件日志）。
  - 选中任务实时订阅 `kernel://task-event`。
  - 全部 done 后显示「进入验收」按钮。
  - 保留「追加指令续跑」输入框与存档点时间轴。
- `src-ui/views/Dashboard.test.tsx` 更新为真实数据版测试（mock listTasks/listRounds）。
- commit: `2530e47`

## 质量门结果

- `cargo fmt --all`：通过。
- `cargo clippy --workspace --all-targets -- -D warnings`：通过。
- `cargo test --workspace`：全绿（95 tests）。
- `npx tsc --noEmit`：通过。
- `npm run test`：40/40 通过。

## 下一步

- S8 冒烟、文档、CI 与归档（README、Feature Map、User-Ops、MVP 索引更新）。
