# 11 - 工作流执行与编排引擎

## 功能简介
Java 侧编排引擎：根据对话模式（默认对话/Agent 路由/工作流）选择执行策略，构建执行上下文、变量存储，调用 LLM/Skill 执行器，并对接 Runtime Sidecar 完成实际工作流图执行。

## 后端 (backend) — `engine` 包
- 上下文：`engine/context/`
  - [ExecutionContext.java](../../backend/src/main/java/com/superprogrammer/engine/context/ExecutionContext.java) — sessionId/mode/agentId/workflowId/ragEnabled
  - [VariableStore.java](../../backend/src/main/java/com/superprogrammer/engine/context/VariableStore.java) — 变量存取
- 路由：`engine/router/` AgentRouter、RoutingResult
- 策略：`engine/strategy/`
  - [ExecutionStrategy.java](../../backend/src/main/java/com/superprogrammer/engine/strategy/ExecutionStrategy.java) — 策略接口
  - DefaultChatStrategy（默认对话）
  - [AgentRoutingStrategy.java](../../backend/src/main/java/com/superprogrammer/engine/strategy/AgentRoutingStrategy.java)（Agent 路由）
  - WorkflowStrategy（工作流）
- 执行器：`engine/executor/`
  - [OrchestrationEngine.java](../../backend/src/main/java/com/superprogrammer/engine/executor/OrchestrationEngine.java) — 编排入口
  - LlmCallHandler、SkillExecutor、StepActionHandler、StepResult
- 编排由 `chat/service/ChatSessionService` 与 `workflow/service/WorkflowService` 触发。

## 前端 (frontend)
无独立 UI（执行结果通过对话流式 / 执行监控页展示，见 [13-执行监控](13-执行监控.md)）。

## Sidecar 联动
- Java 引擎通过 `runtime` 包网关下发执行请求，见 [12-Runtime-Sidecar执行](12-Runtime-Sidecar执行.md)。

## 数据表
执行记录见 `executions` / `execution_logs`（[13-执行监控](13-执行监控.md)）。


## 待增修改
1. ~~我的工作流中可能会涉及到多轮对话，比如系统提示词是“你先向用户搜集你觉得为了完成任务，需要搜集的几个问题”，按现在的工作流模式是不支持中途对话，就无法实现该功能，请你给我设计修改一下~~
   - **Phase 1（方案A）已落地提交** `10668294`（2026-07-01）：新增 `HUMAN_INPUT` 节点，命中即暂停、向用户提问、收集答案绑变量后续跑。设计文档见 `docs/superpowers/specs/2026-07-01-workflow-human-input-node-design.md`。
   - **覆盖场景**：一次性问、固定多问链（图里串 N 个 HUMAN_INPUT）。text/textarea 型直接走对话——问题作为 assistant 消息流出，用户正常回复即被后端拦截当答案恢复执行，**前端零组件改动**。
   - **改动**：
     - sidecar：`runtime_executor.py`（WAITING_INPUT 事件 + checkpoint 暂停/恢复，恢复时发射"暂停节点+后代"并去重）、`graph_compiler.py`（HUMAN_INPUT node_runner）、`tests/test_runtime_executor.py`（+2 用例，29 绿）。
     - 后端：`RuntimeNodeType`(+HUMAN_INPUT)、`ExecutionLog`(+session_id/pending_input)、`ExecutionLogService`(waitForInput/findPendingInputBySession/markInputResumed)、`RuntimeExecutionService`(WAITING_INPUT 落库/resumeWorkflowWithInput/resumeWorkflowFromChatAnswer)、`WorkflowDefinitionAssembler`(校验 inputKey)、`ExecutionController`(`POST /api/executions/{id}/input`)、`StreamEvent`(+INPUT_REQUIRED)、`ChatWebSocketHandler`(转发 INPUT_REQUIRED 帧)、`ChatSessionService`(WORKFLOW 分支先试拦截恢复再分发)、迁移 `V42__workflow_human_input.sql`(节点类型+状态 CHECK+session_id+pending_input 列)。
     - 前端：`stores/chat.ts` 捕获 `pendingInput`（select 型选项按钮组件 UI 待补，text 型不受影响）。
   - **验证**：sidecar `pytest` 29 绿、后端 `mvn compile` 绿、前端 `vue-tsc` 绿。
   - **Phase 2（方案C 环/动态多轮）待做**：sidecar `graph_compiler.py:376 topological_node_ids` 当前检测环即 raise，需放开 + `recursion_limit` 守卫；难点=HUMAN_INPUT 在环内"每轮重新暂停"需按迭代消耗答案（扁平 userInput 同 key 覆盖，待设计 inputKey 收集为 list 或 checkpoint 按轮次标记）+ 环内 checkpoint/resume 的 visited 语义重做。恢复点见 memory `workflow-human-input-node.md`。