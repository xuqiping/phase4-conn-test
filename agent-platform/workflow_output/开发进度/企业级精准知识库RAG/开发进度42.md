# 开发进度42：P5 Run/Result 持久化与生产检索适配

- 日期：2026-08-14
- 分支：`beifen`

## 已完成

- `PostgresEvaluationRepository` 同时实现 Dataset/Case 与 Run/Result 仓储接口。
- Run 创建、状态/汇总指标更新、逐题 Result/trace/verdict 均写入 V115 表。
- 新增 `RagEvaluationPipeline`，评测调用与 `/retrieve` 相同的 `RagRetrievalService`，使用 PRECISION 模式并保留真实 traceId 和证据 nodeId 排名。
- 新增有界专用执行器：核心 1、最大 2、队列 20，避免评测任务挤占普通请求线程。
- 生产适配器不硬编码模型；实际模型仍由知识库配置、管理员默认和明确报错规则解析。

## TDD 与验证

- RED：Repository 测试因 RunRow/ResultRow 与仓储方法不存在而编译失败；Pipeline 测试因适配器不存在而编译失败。
- GREEN：`EvaluationRunRepositoryTest`、`EvaluationRunServiceTest`、`RagEvaluationPipelineTest` 共 3/3 通过。

## 下一步

新增 Run 启动/查询 Controller、Run/Result 查询仓储和前端真实进度/指标表格。
