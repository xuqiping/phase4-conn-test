# 修复X · C 轮开发进度（组边随 ⛓ 连线保留）

> 对应 plan Chunk C（X-3，P0）· spec §4 X-3 · commit C1=`85d9ffd1` C2=`45723976`

## C1 纯函数层（commit 85d9ffd1）

- [canvasClipboard.ts](../../../frontend/src/components/canvas/canvasClipboard.ts)：
  - `buildCopySet` 跨集边**去 isGroupEndpoint 过滤**——节点↔组边恰一端在选中集判 true 被收（新节点连原组，Q3 拍板）；组→组/组自环两端皆不在集天然不收。innerEdges 诱导边仍排除组端点（**半边保留**——组是外部连接非粘贴体内部结构，VIII-1⑧ 口径改此表述）。
  - `remapCrossEdges` 增第 4 参 `aliveGroupIds`：组伪 id 端保原 + alive 校验查组集（`groupIdOf` 去前缀）；组解散 → 丢边不产断边；剥 class 口径不变。
- [nodeClone.ts](../../../frontend/src/components/canvas/nodeClone.ts)：`cloneEdgesForDuplicate` 去组边过滤（节点端换 newId、组伪 id 端保原；组→组两端皆非原节点天然不收）。
- 测试 33/33：修复IX 系列改新口径、新增修复X describe 4 用例（收集两向/组→组与自环不收/组端保原+剥 class/组解散丢边）、nodeClone 组边克隆用例重写。

## C2 接线层（commit 45723976）

- [CanvasBoard.vue](../../../frontend/src/components/canvas/CanvasBoard.vue)：
  - `onCopyKeydown`：`buildCopySet(nodes, [...edges, ...groupEdges], selected)` 混合传入（plan 细化3）。
  - `pasteSubgraph`：alive 增组集（`groups.value` 粘贴时点快照）；remapCrossEdges 产物**分流**——组端点边进 `groupEdges` 池（伪 id 绝不进 v-model），普通边照旧。
  - `appendEdges`：批次分流（普通→v-model / 组边→组池）；组边浅拷贝**剥会话 class**（loadSnapshot :1537 同口径，防选中态烤进新组边）；store 对账只跑普通边；structure-changed 无论哪池上抛（getSnapshot 合并落库）。
- [CanvasView.vue](../../../frontend/src/views/CanvasView.vue) `onCloneNode`：改传 `[...getEdges(), ...getGroupEdges()]`——**plan 原判「getEdges 已含组边」有误**（getEdges 只返 v-model 普通边 :1644-1646），实现轮更正，已记 plan 变更记录。
- 几何（watch groupEdges rAF 合帧）、落库（getSnapshot 合并）、撤回（pushHistory 快照）全既有链零改动。
- CanvasBoard 新增修复X describe 5 用例：两向组边落组池+v-model 零伪 id+新节点不入组员 / 开关关零组边 / 复制后解散粘贴丢边 / Ctrl+Z 一步撤含组边 / appendEdges 混合批次分流+剥 class。

## C3 收口记录

- 全绿：全量 vitest **1014/1014**（+5）、vue-tsc 0 错。
- 手测项转 R 系列（Chunk D）：组边两向真实消费（组下游广播取到新节点产物/组上游聚合新节点取到组产物）；开关关两处零组边；组边随批次落库刷新保持。

## 坑沉淀

- plan 伪代码对既有 API 的断言（「getEdges 已含组边」）实现轮必须逐一核对源码——本次若照抄，副本组边静默丢失且测试全绿（getEdges 恒空组边）。

## 下一步

Chunk D 文档收尾：feature-map/user-ops 增补 + R 系列测试方案 + 问题单 2x 三项挂账。
