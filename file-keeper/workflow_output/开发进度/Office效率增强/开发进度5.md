# Office 效率增强开发进度 5

> 记录日期：2026-08-25。范围是 Chunk 0 质量复审的两项元数据边界修复；检查点 0 仍未通过，不进入 Chunk 1。

## 1. TDD RED

| 契约 | 预期 | RED 实际 |
|---|---|---|
| `every_relationship_part_requires_the_normative_root_and_namespace` | 非根 `.rels` 的 wrong-root/wrong-namespace 必须 BLOCKED | 两个样本均错误返回 `SAFE_OOXML` |
| `large_unread_worksheet_xml_does_not_consume_the_metadata_budget` | 普通 worksheet XML 超过 6 MiB 仍可 SAFE | 错误返回 `BLOCKED` |

## 2. 最小 GREEN

- `scan_relationships` 到 EOF 时必须同时满足：存在单根、根已闭合、深度归零、根是 OPC `Relationships` 且命名空间规范。该规则应用于根关系和所有非根 `.rels`。
- 6 MiB 累计预算只统计 Worker 实际解压读取的 `[Content_Types].xml` 与全部 `.rels`。
- worksheet、Word body、slide 等未读取普通 XML 不占元数据扫描预算；仍受源文件 2 GiB、entry 10 万、单 entry 压缩比 1000:1 与 30 秒 deadline 约束。

## 3. 涉及文件与验证

- `src-tauri/src/bin/office_ooxml_worker.rs`：关系根终态校验、元数据预算口径纠正。
- `src-tauri/tests/office_ooxml_worker_contract.rs`：新增两条契约，21→23。
- 兼容矩阵、开发进度总览与本文件同步恢复入口和预算说明。
- 定向契约：23/23 通过。
- `cargo clippy --manifest-path src-tauri/Cargo.toml --bin office_ooxml_worker -- -D warnings`：通过。
- `cargo test --manifest-path src-tauri/Cargo.toml`：既有 81/81 + Office 契约 23/23 全绿。
- `npm test`：38 个测试文件、245/245 通过；既有预期 stderr 不影响退出码。

## 4. 闸门

- 第二代 Office 与真实 `.xlsm/.pptm`、高保真样本仍缺失。
- 平台级 file identity 仍待生产执行票据绑定。
- 检查点 0 不通过，禁止进入 Chunk 1。
