# 人工测试遗留问题修复 · 开发进度1（2026-08-25）

> 对应 plan：[plans/人工测试遗留问题修复.plan.md](../docs/plans/人工测试遗留问题修复.plan.md)（A→B→C→D 串行）
> 本轮：子计划 A 全部完成（A1-A4 代码+单测），规格同步 2 处实现期修正。

## 本轮完成

### A1 成员配额流水（20x-2 数据源）
- `ProjectGroupLedgerEntity`：+3 类型常量 `MEMBER_ALLOCATE`（授予，毛额）/`MEMBER_RECLAIM`（收回，净额口径）/`MEMBER_QUOTA_ADJUST`（限额↔不限边界，delta=0）+ `REF_MEMBER`（ref_id=成员 userId）。
- `ProjectGroupService.recordMemberQuotaLedger(groupId, actor, member, oldQ, newQ, cause)` 私有方法：old==new 不落；null→值=ALLOCATE；值→null=QUOTA_ADJUST(0)；差正=ALLOCATE/差负=RECLAIM。非资金腿（balance_after 照抄池余额快照；对账等式 D4 白名单排除 MEMBER_*）。
- 落点（同事务）：`insertMemberRow` 复活径+新插径、`updateQuota` 组长调管理/组长调成员/管理调成员三分支、A2 降职缩额。

### A2 降职缩额（17x-1 核心）
- `ProjectGroupMemberMapper.selectChildren`（直接下级快照，管理行 FOR UPDATE 事务内读）。
- `updateMemberRole` 降职分支：`requireMemberForUpdate` 行锁 → selectChildren 快照 → reparentChildren → 缩额 `quotaNew = max(used, quota − Σ下级quota)`；任一下级 quota NULL → 取 used（保守）；自身不限额不动 → `MEMBER_RECLAIM` 留痕 → role 翻转。
- 锁序：只锁成员行，与 doChargeGroup 同序无环；`requireMember`（共享）未动。

### A3 PENDING 邀请收口（次生洞）
- `ProjectGroupInviteService.accept` → `resolveAllocatedBy`：邀请人仍是在任 MANAGER → 归其预算；降职/移除/组外 admin 代发 → 改挂组长（组长侧无预算硬卡天然跳过）。

### A4 测试
- `ProjectGroupServiceTest`：+4 用例（降职三算例/不限额/无变化/流水类型映射），3 处旧桩改 ForUpdate。
- `ProjectGroupInviteServiceTest`（新建）：6 用例（在任/降职/移除/即组长/并发 409/非本人）。
- projectgroup 包 54 例全绿（含真 PG IT）。

## 实现期修正（规格已同步）
1. 降职缩额流水：`MEMBER_QUOTA_ADJUST` → **`MEMBER_RECLAIM`**（净额等式 ΣALLOCATE−ΣRECLAIM 才成立；QUOTA_ADJUST 留给边界）。spec §2.3.4/§8.3.2。
2. 流水方法落位：WalletService → **ProjectGroupService**（四落点全在 Service 层，就近内聚）。plan A 备注。

## 关键代码位置
- `backend/src/main/java/com/superprogrammer/projectgroup/service/ProjectGroupService.java`（requireMemberForUpdate/recordMemberQuotaLedger/updateMemberRole 降职分支/updateQuota 挂钩/insertMemberRow 挂钩）
- `.../entity/ProjectGroupLedgerEntity.java`（类型常量）
- `.../mapper/ProjectGroupMemberMapper.java`（selectChildren）
- `.../service/ProjectGroupInviteService.java`（resolveAllocatedBy）
- 测试：`ProjectGroupServiceTest.java`、`ProjectGroupInviteServiceTest.java`

## 下一步
子计划 B（聊天计费闭环）：B1 V157 迁移 → B2 PROGRESS/DONE.usage → B3 HOLD → B4 SSE 兜底 → B5 DEBT → B6 测试。
