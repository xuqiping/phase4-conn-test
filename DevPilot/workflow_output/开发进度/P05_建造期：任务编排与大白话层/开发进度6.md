# P05 建造期：任务编排与大白话层 · 开发进度 6

> 本轮完成 S6 追加指令续跑（FR-015）。

## 已完成工作

### S6 追加指令续跑（FR-015）
- `core-orchestrator/src/task_scheduler.rs`：
  - 新增 `Scheduler::append_task(title, instructions)`：在当前 open round 末尾插入 chunk_no = max+1 的 pending task。
  - 单元测试验证 chunk 号连续、状态为 pending。
- Tauri `continue_task` 命令（`src-tauri/src/commands.rs`）：
  - 取当前 open round；没有则报错。
  - 调用 `scheduler.append_task` 后执行 `run_round`。
  - 事件流持久化、状态快照推送，不推进状态机。
  - 失败/追问回写 warning。
- `src-tauri/src/lib.rs` 注册命令。
- `src-ui/lib/ipc.ts`：新增 `continueTask`。
- `src-ui/views/Build.tsx`：增加「追加指令续跑」输入框 + 按钮，错误提示。
- commit: `3b4031a`

## 质量门结果

- `cargo fmt --all`：通过。
- `cargo clippy --workspace --all-targets -- -D warnings`：通过。
- `cargo test --workspace`：全绿（95 tests）。
- `npx tsc --noEmit`：通过。
- `npm run test`：40/40 通过。

## 下一步

- S7 Build 视图 + 驾驶舱真实数据接通（FR-038/039/043）。
