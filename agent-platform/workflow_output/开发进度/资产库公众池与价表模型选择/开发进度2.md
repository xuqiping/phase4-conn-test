# 资产库公众池与价表模型选择 · 开发进度 2

> 记录资产 Step 7 与 Phase 3 收口。完整复查、全量回归和浏览器人工实测按用户要求统一留 Phase 4。

## 2026-08-11 · 资产 Step 7

- 新增 `PublicPublishDialog`：普通 OWNER 选择 OPEN/APPROVAL_REQUIRED；管理员固定 OPEN 并显示“官方发布”。完成锁、防重复提交和会话隔离覆盖关闭重开、切项目及父状态延迟。
- 新增 `PublicAccessDialog`：PENDING 批准/拒绝、APPROVED 撤销；加载、空态、失败独立；同项目刷新乱序和 mutation 跨会话防重已处理。
- `AssetListView` 增加“公共池”第三页签、官方/访问模式徽章与“使用/申请/等待审批/重新申请”状态按钮；OWNER 卡增加发布、移出和审批管理入口。
- `AssetProjectView` 对公共 VIEWER 保持全只读，逐资产提供“复制到我的项目”；目标只含 OWNER/EDITOR 项目并排除源项目，复制请求具备关闭保护与防重。
- `AssetPicker` 增加“我的/共享项目”和“公共池”来源切换；未获批公共项目显示原因并禁用，可用项目继续走 `resolve` 当前版本并原样保留 `resolve.version`。
- 主要 commits：`dc83e809`、`20bb6426`、`5652dee8`、`5d570bbe`、`d721ebca`、`9569f2cd`（公众弹窗）；`62c15f5c`、`5ffe8623`（列表/详情/复制）；`04aeae4b`（画布公共来源）。
- TDD 证据：Step 7A 最终定向 28/28；Step 7B 定向 31/31；Step 7C 定向 7/7；各 chunk `npm run build` 成功。
- 按用户要求，自 Step 7C 起停止逐 chunk spec/quality review 与重复全量实测；完整规格复查、全量测试、浏览器人工验收、真 PostgreSQL V88/并发统一留 Phase 4。

## Phase 4 待办

- 跑前端全量 Vitest、后端资产/计费定向与项目全量基线，区分历史三项后端失败。
- 按《资产库公众池与成员授权测试方案》实测三页签、官方标记、两种发布模式、申请审批、失权、复制和画布引用快照。
- 真 PostgreSQL 执行 V88，验证约束/索引/旧项目默认私有；双会话验证审批/价表创建并发。
- 浏览器验证键盘关闭、禁用原因、移动端换行与暗色样式。
