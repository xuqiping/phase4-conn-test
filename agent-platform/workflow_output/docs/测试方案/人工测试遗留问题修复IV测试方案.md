# 人工测试遗留问题修复IV · 测试方案

> 2026-08-27。覆盖 plan 联动点 L1-L14（每条含正向/反向/边界）。
> 自动化基线：后端 mvn test 2566 全绿（IT 14/14 真 PG）；前端 vitest 846 全绿 + vue-tsc 0。
> 账号：admin/admin123；建议现建 组长/管理/成员 三个号走 17x 全链路。
> commits：A `58f64beb` / B `b88f61fb` / C `d843b8a6` / D `75920ed0` / E `98d0eaf2`。

## 一、功能联动用例（L1-L14）

### L1 UserPicker 全选/反选（A：17x-1）
| # | 操作 | 预期 | 档位 |
|---|---|---|---|
| 正 | 搜「A 班」出 5 人 → 点「全选」 | 5 人全进 chips | 正向 |
| 边1 | 已选 3 → 点「反选」 | 5 人中未选 2 选上、已选 3 移除 | 半选翻转 |
| 边2 | 全选后换关键词搜「B 班」 | 作用域=新结果；**A 班已选不丢**（chips 仍在） | 换词不丢 |
| 反 | 搜索请求未返回时点全选 | 按钮禁用（防点空竞态） | 竞态 |
| 边3 | chips 里删 1 人再点全选 | 该人重新加入 | 删后回选 |

### L2 邀请弹窗下拉时机（A）
| 正 | 点开「邀请成员」弹窗 | 干净，**不**弹全部用户下拉 | 打开不弹 |
| 边 | 点搜索框 / 输入关键词 | 此时才拉起候选 | 聚焦才弹 |
| 反 | Esc / 点弹窗外部 | 候选关；重开不残留关键词与下拉 | 关闭干净 |

### L3 候选权限对齐（A：17x-2b）
| 正 | MANAGER 打开邀请弹窗 | 候选列表 200 可见 | 放宽读 |
| 反 | MEMBER 调候选接口 | 403（requireRole 拦）；组长/admin 原样 | 写权限不动 |

### L4 审计列表用户列（A：12x-2）
| 正 | 改过姓名+有备注的用户操作 | 列显「账号（现姓名）」（username 快照在前）+ 备注 tag 悬浮全文 | 正向 |
| 反1 | 已删用户 | 回落 username 快照 | 删号回落 |
| 反2 | 无备注用户 | 不显 tag | 空不显 |
| 边 | 点详情 | 账号快照仍在（证据链不随改名漂） | 证据链 |

### L5 分辨率整行（A：C-5）
| 正 | 选中视频节点看属性面板 | 分辨率选择器独占整行，各档位完整可见 | 正向 |
| 边 | 拖窄面板 | 仍不截断 | 窄面板 |

### L6 两段式点击（B：C-1）
| 正1 | 未选中状态点节点媒体区 | **仅选中**（出属性面板），不开大图 | 第一段 |
| 正2 | 已选中再点缩略图本身 | 开 Lightbox 大图/播放 | 第二段 |
| 反1 | 点节点非媒体区（标题/边缘） | 仅选中/保持选中，不开 | 非媒体区 |
| 反2 | 点画布空白 | 取消选中（回到第一段初始态） | 空白取消 |

### L7 上游直显（B：C-2）
| 正 | 选中媒体节点 | 上游链**全部直接展示**，按类型分色（图/视/音/文图标） | 全展 |
| 边 | 切到另一节点再切回 | 不残留折叠态；>50 上游显截断提示 | 切换/截断 |

### L8 resize 手柄（B：C-3）
| 正1 | 选中→四角手柄拖 | 宽高同变；角热区约 22px（视觉 8px+外扩）好点中 | 角拖 |
| 正2 | 四条边手柄拖 | 单轴拉宽/窄 | 边拖 |
| 边1 | 拖到最小 | min 160/64 卡住 | 下限 |
| 边2 | 拖节点边缘中点（连线 Handle 处） | 命中**连线优先**（连线手柄不受挡） | 连线优先 |
| 反 | 未选中 | 无手柄；resize 后刷新尺寸在（落库不回归） | 未选/落库 |

### L9 面板拖宽（B：C-6）
| 正 | 拖属性面板左缘 | 实时变宽；松手持久化；刷新恢复 | 正向 |
| 反 | localStorage 塞非法值（NaN/越界）再刷新 | 回落 260 | 非法兜底 |
| 边 | 拖拽过程 | 画布无选择框/误选文本 | 不误选 |

### L10 自动保存（C：C-4）
| 正1 | 新增节点（调色板/拖入/快速加三路） | 即入保存链，徽标切换，刷新节点在 | 新增即存 |
| 正2 | 节点文本输入完点别处（失焦） | 延迟后自动保存；**@候选层点击不触发误存** | 失焦存 |
| 反 | 断网保存失败 | 徽标失败+重试+离开拦截（III 期机制不回归） | 失败路径 |
| 边 | 新增后 undo/redo | 链路不坏 | undo |

### L11 新建定型尺寸（C：C-7）
| 正 | 新建 image/video 节点 | 即按 16:9/9:16 定型尺寸创建，三路一致 | 三路一致 |
| 边 | 生成完成后 | 不撑爆布局/不跳变；用户手拉过的尺寸不被定型覆盖 | 定型不覆盖 |

### L12 副本完全独立（C：C-8）
| 正1 | 有产物的节点「创建副本」 | 副本产物即显（fileId 拷贝/新引用） | 即显 |
| 正2 | 对副本重生成 | 全新任务；**原节点/原文件不变** | 重生成隔离 |
| 正3 | 副本入库 | 新资产，不因原任务已入库被判重拦下 | 入库独立 |
| 反 | 删除原节点 | 副本产物仍显示（引用在自己名下） | 删原件 |

### L13 进组默认 0（D：17x-3）
| 正1 | 邀请不填额度→接受 | 成员行 quota=0（话术明示「不填=0」） | 邀请落 0 |
| 正2 | 公共池申请→审批通过 | 落 0+「请在成员表调限额后可用」 | 池批落 0 |
| 正3 | 组长调限额留空提交 | 400「不限额度已停用」 | 拒 null |
| 边1 | 存量 NULL 成员 | 显「不限（遗留）」行为不变（不迁移） | 冻结遗留 |
| 边2 | 建组组长自动成员行 | 仍 NULL=组池直管（豁免） | 组长豁免 |
| 反 | quota=0 成员提交生成任务 | 预估/提交按 quota−used=0 拦（不创建任务） | 0 即拦 |

### L14 成员受限视图（D：17x-4）
| 正 | MEMBER 打开组详情→成员 tab | 他人行显 用户/角色/加入时间/备注；限额·已用·欠款·可分配·上级全隐藏；**本人行全显** | 裁剪+本人全显 |
| 反1 | 成员看组财务/流水他人行 | detail/overview 组池余额/在途置 null 不透出；流水仅本人行且余额列空（页顶「组池」徽标走既有 mine 口径仍可见——已知边界） | 财务隔离 |
| 反2 | 非本组成员开 detail | 403 | 非成员拒 |
| 边 | 组长/MANAGER/admin 开 | 全显不裁剪（不回归） | 管理视角 |

### 附：按备注汇总（E：12x-1）
| 正 | 账单页切「按备注汇总」 | 备注桶分列（未填备注桶行首色条）；改日期区间→消耗/调用变、余额/充值不变 | 口径 |
| 边 | keyword 输备注片段（余额/充值/分配三处） | 命中备注用户（LIKE 转义，%/_ 不通配） | 筛选 |
| 反 | 非 usage:view 用户 | 整页 403（无新口子） | 权限 |

## 二、人工交互测试（机器测不了项）

### T1 默认 0 全链路（L13）
现建组长号建组 → 邀请（不填额度）→ 新号接受 → 查成员行 0 → 组长调限额配 100 → 提交任务可过；再试留空调限额 → 400 话术。池申请走第二组号重演落 0。

### T2 三角色矩阵（L14）
同组三号（组长/MANAGER/MEMBER）+ 组外一号：逐一开组详情核对成员 tab 列可见性、组财务、流水；确认 MEMBER 本人行全显、组长/管理全显、组外 403。

### T3 画布走查（L5-L12）
按 L5→L12 顺序过一遍：两段式/上游/手柄/拖宽/自动保存（含 undo）/定型/副本独立（重生成+入库+删原件三隔离）。

### T4 备注汇总对账（E 附）
给若干用户备注「A 班」→ 按备注汇总 tab 核对桶人数/余额/充值与四视图逐人相加一致；改区间只动消耗/调用两列。

### T5 UserPicker 三入口（L1/L2）
项目组邀请 + 批量充值两入口：全选/反选/换词不丢/竞态禁用；打开不弹下拉、聚焦才弹。

## 三、回归锚点

- 后端 2566（D 轮新增 339 行测试、E 轮 +3 用例）；前端 846 + tsc 0。
- 财务不变量：组流水 Σ==used+debt 模板不回归；同项目判重 (项目,taskId,imageIdx) 口径不变（副本走新 taskId 天然不撞）。
- 安全回归：候选接口 MEMBER 403；LIKE 转义用例（keywordWildcards_escaped）绿；updateQuota @AuditLog(member_quota) 既有动作未动。
