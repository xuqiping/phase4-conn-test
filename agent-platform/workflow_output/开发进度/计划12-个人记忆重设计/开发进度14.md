# 计划12 · 个人记忆重设计 · 开发进度14

> H 收尾（迭代 H）启动。承 [开发进度13.md](开发进度13.md)（生命周期写侧 hook）。
> **本轮关键**：发现 plan H（Phase2 写）与代码现状严重脱节 → 重写 H' v2（切流前置版）→ 落地 H'-1 后端切流。

## 一、plan H 脱节发现 + H' v2 修正

**探查结论**（写码前确认）：C/D/E 迭代全部偏离 plan「改 legacy」改为「建并行新栈不动 legacy」（每轮进度已记偏离），导致**活聊天路径从未切到新栈**：
- ChatSessionService 活路径全走旧栈：召回 `memoryService.buildMemoryContext`、写入 `memoryService.processMemory`、活冲突拦截 `interceptConflict`(getActivePendingOrExpire/route/flag)、`recordIncident`。
- 新栈入口已就绪但未接线：召回 `MemoryRecallPipeline.recall`、写入 `MemoryGenerationService.processTurnAsync`。
- 直接执行 v1 plan（DROP+删）= ChatSessionService 仍读写 `user_memories` → 每次聊天运行时崩。

**H' v2 修正**：[计划12-H-收尾.md](../../../项目工程文档/计划/计划12-H-收尾.md) 重写为 6 阶段（切流前置 → 删 legacy → V53 DROP → 前端收口 → 文档 → 功能级收尾）。已过审。

## 二、H'-1 后端切流（commit `7d1c64f8`，16 单测绿）

ChatSessionService 活聊天路径正式切新栈：

**召回**：3 处 `buildMemoryContext(readScope,msg)`（sync L249 / stream L433）→ 新 helper `recallMemoryContext(userId,msg)`：`memoryRecallPrefService.getScope(userId)` 取用户持久化偏好（F-6 底栏 popover，null→默认{个人}，设计 §3.3 line113）→ `memoryRecallPipeline.recall(msg,scopeReq,userId).assembledText`，try-catch 兜底返空串不崩聊天。

**写入**：6 处 `processMemory(writeScope,writeTargetProjectId,sessionId,msg,resp,model)`（sync/stream/workflow × HYBRID/ASYNC）→ 新 helper `dispatchMemoryWrite(...)`：`memoryGenerationService.processTurnAsync(userId,sessionId,session.getProjectId(),writeScope.includeGlobal(),writeScope.safeProjectIds(),input,output)`。writeScope 经 `memoryScopeResolver.resolveWriteScope` 保 canAccess 守卫（防越权写他人项目）。model 参数弃（新 generator 走配置）。

**弃（新栈无等价 / 静默降级）**：
- 活冲突拦截 `interceptConflict` 整块 + `ConflictIntercept` record：新栈冲突走总结 worker→面板（MemoryConsolidationController）解决，不在聊天拦下一轮（设计 §3.5）。
- `recordIncident`（新栈 fire-and-forget 静默降级，无 incident 机制）。
- HYBRID/ASYNC 分支 + `askText` 即时追问 + `getMemoryProcessMode` 记忆写入模式门控（新栈恒异步）。
- `memoryTaskExecutor` 字段（新栈 processTurnAsync 内部自有 executor + RejectedExecution 兜底）。
- `conflictService`/`conflictJudge` 注入字段。

**字段**：删 memoryService/conflictService/conflictJudge/memoryTaskExecutor；加 memoryRecallPipeline/memoryGenerationService/memoryRecallPrefService。保留 memoryScopeResolver（写 scope 用）+ systemSettingService（联网搜索等）。

**测试**：ChatSessionServiceTest 16 绿（原 14 + 新增 2 切流契约测：①ragOn 召回走 pipeline + 写入走 generationService ②空召回不崩写入仍提交）。

## 三、删除清单（H'-2/H'-3，下一窗执行，已探查定稿）

### 共用资源 **保留**（新栈依赖，勿删）
- `MemoryConflict` entity + `MemoryConflictMapper`(+xml) + `MemoryConflictVO`：新栈 MemoryConflictResolutionService/MemoryConsolidationTxService/MemoryConsolidationController 用。
- `MemoryScope` + `MemoryScopeResolver`：ChatSessionService 写 scope 用（11 文件，暂留）。
- `MemoryConflictJudge.judgeSummaryConflict`：新栈 MemoryConsolidationTxService 用（须 trim 非删）。
- `MemoryQueryCache.evictUser`：新栈 Generation/Consolidation/ConflictResolution 用（须 trim 非删）。

### 删（legacy 簇，互引用紧须一次性删保编译）
- **service**：MemoryService(1278行) / MemoryConflictService / MemoryKeyMetaService
- **internal**：MemoryBlockClassifier / MemoryValueTimeline
- **controller**：MemoryController（/api/chat/memories/* 全族→404）
- **entity**：UserMemory / MemoryKeyMeta
- **mapper**：UserMemoryMapper(inline 注解无 XML) / MemoryKeyMetaMapper
- **dto**：UserMemoryVO / MemoryBlockHit / MemoryKeyMetaVO / MemoryKeyMetaUpdateRequest / MemoryEditRequest / MemoryConflictResolveRequest / MemoryConflictBatchResolveRequest / MemoryScopeUpdateRequest / MemoryPreviewRequest / MemoryContextPreviewVO / MemoryStatusVO / ChatRagModeVO（**注意 MemoryConflictVO 保留**）
- **helper**：ExtractedFact / RouteResult（纯 legacy，删簇后成孤儿）
- **test**：MemoryKeyMetaServiceTest / MemoryServiceTest / MemoryPreviewDebugIT / MemoryConflictJudgeTest / MemoryValueTimelineTest

### Trim（删 legacy 方法留共用）
- **MemoryConflictJudge**：删 judge/route/batchExtractEntities/selectRelevantKeysBlocks/zhKeyDisplay/pickValidStrs/readFact/readEntities/RelevantDims + EXTRACT_PROMPT/SELECT_KEYS_BLOCKS_PROMPT/VALID_CATEGORIES 等仅 legacy 常量 + UserMemory/ExtractedFact import；**留** judgeSummaryConflict + SummaryConflictResult + summaryText + chat/parseJson/stripFence/textOrDefault/truncate + SUMMARY_CONFLICT_PROMPT。**须先读全文 L60-360 确认 chat()/helper 依赖边界。**
- **MemoryQueryCache**：删 getDistinctKeys/getCleanByBlock/getRerankKeys + keysKey/blockKey/scopeSig/put（仅这些法用）+ UserMemory import；**留** evictUser + PREFIX + redis。
- **SystemSettingController**：删 memoryService 字段 + 3 旧维护端点（backfillEntities/reextract/cleanupResidue，L133-162）+ import；其余走 SystemSettingService 不动。

### V53（DROP，trim+删后）
`V53__drop_legacy_memory_tables.sql`：`DROP TABLE IF EXISTS user_memory_projects, user_memories;`。先 grep 全库无 FK/视图依赖。memory_conflicts 表 + V48 放宽旧列**保留**（新栈用）。

## 四、下一步（按序）
1. 删 legacy 簇 + trim Judge/QueryCache + 解 SystemSettingController（编译驱动迭代，一次 commit）。
2. V53 DROP + 旧端点 404 单测 + A-I4 全链回归。
3. H'-4 前端收口（chat.ts 17 memories 方法 + MemoryManagerPanel legacy 首页签 + RagMemorySettingsTab）。
4. H'-5 速查表09 重写 + 主索引 H 勾选。
5. H'-6 功能级收尾（README/FeatureMap/UserOps/测试方案）。

## 坑/沉淀
- **plan 与代码现状脱节**：Phase2 写的 plan 假设线性替换，未预见实现迭代主动偏离建并行栈。教训：删/改存量前先 grep 调用方验真现状，勿盲信 plan。
- 沉淀（待入通用约束）：切流默认召回 scope 走**用户持久化偏好**（prefService）非 session 旧列；新栈写入恒 fire-and-forget 无 HYBRID/ASYNC 之分。
