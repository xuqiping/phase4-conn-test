# 人工测试问题 1—4 修复 · 开发进度 2

> 日期：2026-09-03｜对应计划：Chunk 1｜状态：✅ 完成｜提交：`5f46b96`

## 本轮实现

| 功能 | 对应需求 | 关键文件 | 结果 |
|---|---|---|---|
| 收藏与设置共享类型 | F1/F2/G1 | `src/types/file.ts`、`src/types/settings.ts` | 增加快捷键、托管副本、三态关闭和悬浮球位置模型 |
| Pinia 旧设置迁移 | G1 | `src/plugins/persistPlugin.ts`、`src/stores/settingsStore.ts` | 持久化加载前执行迁移；旧布尔值映射为新关闭行为 |
| 进程设置版本字段 | P1 | `src/types/process.ts` | 为后续一次性列迁移预留版本字段 |
| 剪贴板本地模型 | C1/C2 | 前后端剪贴板类型 | 增加分组、归属和置顶时间字段 |
| SQLite 版本迁移 | C1/C2 | `src-tauri/src/clipboard/storage.rs` | `user_version` 0→1→2 事务升级，补表/列/索引并修复孤立归属 |
| 隔离测试入口 | C1/C2 | `src-tauri/tests/clipboard_storage.rs` | 不复制实现，直接复用生产存储模块测试 |

## TDD 与验证证据

- RED：设置迁移 4 个用例先因 `normalizeSettings` 不存在失败。
- RED：持久化迁移钩子先得到 `42` 而不是预期 `43`。
- RED：高版本数据库先被错误降为版本 2，拒绝降级用例失败。
- GREEN：前端定向测试 5 个文件、33 个用例通过。
- GREEN：剪贴板隔离 Rust 测试 17/17 通过。
- 构建：`npm run build` 通过；`cargo check --bin file-keeper` 通过（保留项目既有 warning）。
- 已知基线：完整 Rust unit test 仍会被既有 Office 测试缺少 `complete_with_validated_summary` 阻断；本 Chunk 未改 Office 模块，隔离测试提供目标模块证据。

## 安全与回滚

- 数据库迁移失败回滚；版本高于应用支持值时拒绝静默覆盖。
- SQLite 只新增结构，旧代码可忽略新增字段；不执行破坏性降级。
- 设置迁移不访问网络、不记录文件路径或剪贴板正文。
- Chunk 回滚锚点：`5f46b96`；文档基线锚点：`fead588`。

## 沉淀规范

- 新增 `workflow_output/项目规范约束/本地持久化迁移约束.md`。
- 后续 Store 迁移统一使用 `persist.migrate`；SQLite 统一读取版本、事务升级并拒绝新库降级。

## 下一步

进入 Chunk 2：先写快捷键标准化、内部冲突、注册替换失败回滚和启动恢复隔离的失败测试。
