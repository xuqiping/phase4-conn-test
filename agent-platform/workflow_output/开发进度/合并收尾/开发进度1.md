# 合并收尾 · 开发进度 1

> 2026-08-10 beifen2（日志/运维/安全体系 S1-S2）合入 beifen 后的 17 项收尾推进记录。
> 关联：merge commit `c042999b`；覆盖度实证结论见会话报告（咽喉点类自动覆盖，埋点/指标/越权IT/看板需补）。

## 项 1 ✅ AUDIT_HMAC_KEY 落启动环境
- 正式 64-hex 随机密钥注入 `local-dev-env.ps1` + `start-backend-claude.ps1`（均 gitignored 本地文件）
- 速查表补第四项 fail-closed 说明（commit 随项1提交）
- 部署侧：运维手册/安全体系文档已含该 env 要求（beifen2 原生文档），WinSW backend.xml 为服务器窗口模板

## 项 2/3 ⏸️ 用户叫停
- 密钥轮换：JWT_SECRET 已在 3 文件轮转（叫停前已执行，旧值不可恢复，效果=下次重启全重登）；DB 密码未动（PG 实例共享 suspense_agent/pm_agent，轮换须协调）
- merge push origin/beifen：不 push（daily-sync 存在，注意其可能自动推）

## 项 4 ✅ 监控五组件安装（2026-08-10 完成）
- D:\IT\ops 五目录布局 + 五 WinSW 服务注册运行（Automatic）：agent-prometheus / agent-alertmanager / agent-grafana / agent-windows-exporter / agent-dingtalk-webhook
- **targets**：backend ✅ / prometheus ✅ / windows_exporter ✅ / sidecar 0（进程在跑但 /metrics 404=旧构建无 instrumentator，sidecar 下次重打包即恢复）
- **告警规则 9 条全加载**（availability2/host-resources2/media-quality1/security1/service-quality3）
- **Grafana 12.3.1**：4 看板 provisioning 成功（AI核心链路/安全态势/接口质量/系统总览）；数据源 Prometheus+Alertmanager 通；代理查询实证 `up` 三 UP + `media_task_submitted_total{kind=edit}=1`（项10 E2E 数据）→ 看板出数
- 版本：prometheus 3.13.2 / alertmanager 0.33.1 / grafana 12.3.1 / windows_exporter 0.31.8 / WinSW 2.12 / dingtalk-webhook v2.1.0
- 下载踩坑：① GitHub 直连全断→用户换代理 127.0.0.1:7892 后通 ② 20:37 僵尸 curl 双写 corrupt grafana.zip（杀进程+单写者重下+python zipfile 校验 13331 entries 全绿）③ WinSW 正确资产名=WinSW.NET461.exe（655872B，无 .x64 后缀）④ zip 解压会覆盖预拷配置→解压后须重拷 prometheus.yml/alertmanager.yml 并重启服务
- 留人工：dingtalk-webhook config.yml 填真 webhook（scripts/ops/alertmanager/配置钉钉告警.bat 引导）；grafana admin 密码首登改

## 项 5 ✅ 审计埋点补新模块（commit `3542c217`）
- 24 处 @AuditLog：记忆 user-grant 6 + 项目 link 7 + 记忆公共池 toggle（并入 6）+ 资产公众池 publish/unpublish/access 5 + 价表/比例 5 + 媒体提交 video/image/edit 3
- 复用既有 AOP 异步落 audit_logs → 自动入哈希链（V93）与每日链校验；审计 31 测绿

## 项 6 ✅ 媒体指标接 BizMetrics（commit `9c171a27`）
- 新三指标：media_task_submitted_total{kind} / media_task_terminal_total{kind,result} / media_task_duration{kind}（kind=video|image|edit 有界枚举）
- 埋点：MediaGenTaskService.submit 落库成功才计；MediaGenTaskWorker/MediaEditTaskWorker 终态单点（process finally，正好一次/次，重试按次计）；耗时=createdAt→终态含排队
- pollUntilTerminal/processImage/handleSucceeded/renderTask 改返回成功信号；53 测绿

## 项 7 ✅ 越权 IT 补三域（commit `93eeb671`）
- MemoryProjectLinkPrivilegeIT（无关用户审批他人 link→403）/ AssetPublicPoolPrivilegeIT（非 owner 发布→403，正向带 accessMode body）/ MediaGenTaskPrivilegeIT（跨用户读生图任务→403）
- 复用 B2 骨架；integration 组跑法 `mvn test -Dsurefire.excludedGroups= -Dtest=XxxIT`；3 测绿

## 项 8 ✅ 媒体看板+告警（commit `613af42e`）
- ai-core 看板 +3 面板（提交速率/终态结果/端到端耗时，gen_dashboards.py 重产 15 面板）
- alert-rules 第 9 条：媒体任务失败率>50%/15min 按 kind 告警（低流量门槛 0.005/s 防抖动）

## 项 9 ✅ 文档漂移修正（本轮）
- 安全体系/日志系统 feature-map 头部加漂移告示：beifen2 文档引用 V77~V81 实际落地 V89~V93（映射表）；历史 plan/进度文档不逐处改写
- 总览登记合并事实与本进度档

## 项 10 ✅（合并码真机验证部分）
- 后端以合并后新码重启（含项 5/6/8 改动），健康 UP
- **剪辑真 E2E**：上传 clip3s.mp4 → submit(task#7) → 真 ffmpeg 渲染 SUCCEEDED → 下载 91KB
- **指标实证**：media_task_submitted_total{edit}=1 / terminal{edit,success}=1
- **审计实证**：audit_logs 新增 edit_submit SUCCESS 行（自动入哈希链）
- 踩坑：Get-Random -Count 对 16 元素不放回抽样只得 16 字符 → AUDIT_HMAC_KEY 长度不足 fail-fast（fail-fast 设计立功，一次抓到配置错误）；已重生真 64 字符修两 env 文件
- 留人工窗口：资产库全量 Phase4 E2E、画布 C2 尾帧现网验（真实生视频计费）、运维真实服务器打包

## 项 11 ⏸️ 支付 Phase2（处置=需 Phase1 规格先行）
- 计费 K 阶段=真实支付通道（微信/支付宝），属 Phase2 整个子项目，不在合并收尾范围
- 前置：sdd-phase1 出支付规格（通道选择/回调验签/对账衔接既有 BillingReconcileService/幂等键复用 SEC-FR-121 范式）
- 既有地基已齐：payment_orders 表、幂等键、对账任务、金额校验（SEC-FR-122）、REVOKE 权限分离

## 项 12 ✅ S20 文档（本轮）
- FeatureMap 加「S15-S19 分镜与资产编辑」节（AssetScriptService 一键分镜/StoryboardFields/画布分镜节点/V62）
- UserOps 加「五、分镜」章（一键拆流程 + 5 字段表含字段 3/5 占位标注 + 画布分镜节点）
- 速查表 25 此前已含分镜（S17/S18/S19 行），无需补

## 项 13 ✅ 部署检查清单（本轮）
- 新建 [项目工程文档/运维/部署检查清单.md](../../../项目工程文档/运维/部署检查清单.md)：7 段（env 四 fail-closed / agent_app 非超管 PG 账号 SQL / ffmpeg≥4.4 / IMAGE provider+价表 / 监控 / 备份 / 安全行为变更通告）

## 项 14 ⏸️ 安全 S1 剩尾（处置=人工项）
- Step5 密钥轮换=人工项（项 2 用户已叫停本地轮换；生产轮换走变更窗口，配部署清单第 1 段）
- H1~H3（安全 S1 遗留硬化项）随 S3+ 排期，见安全体系索引 plan

## 项 15 ⏸️ 安全 S3-S5（处置=独立阶段）
- S1/S2 已验收；S3~S5 为后续独立 plan（索引：0_推进计划/5_安全体系.md），各阶段须先过 Phase1/2 规格与计划，不在本轮收尾

## 项 16 ⏸️ 运维留项（处置=人工窗口+可选）
- 异地备份：sync-offsite.bat 待用户配目标（backup-env.bat 本地机密），未配时失败兜底已内建
- 季度恢复演练：规程在 项目工程文档/运维/恢复演练规程.md（RPO24h/RTO1h），按季度人工执行
- 钉钉 webhook：配置钉钉告警.bat 引导用户自填（项 4 装完 alertmanager 后可配）
- VictoriaLogs：未引入（日志量小暂不需要，运维 plan 已注明）

## 项 17 ✅ gitleaks 本地装 + baseline（commit `2b4c810a`）
- 本地装 gitleaks v8.30.1（D:\IT\tools\gitleaks；GitHub 直连断走 gh-proxy 镜像续传）
- **重大修复①**：.gitleaks.toml 原只配 allowlist 无 [rules] → gitleaks 一条规则不跑=**0 检出假象**；加 `[extend] useDefault=true` 继承内置规则集后真实命中 20 条
- **重大修复②**：gitleaks-scan.sh --baseline 模式 `--baseline-path /dev/null` 在 Windows Git Bash 被解析成 `\nul` 报错 → 去掉（不传即全量）；报告自动 python 剥除 Secret/Match/Line 明文字段（baseline 匹配只用 fingerprint，剥明文防二次泄露入 git）
- baseline 20 条逐条人工确认：**4 条真阳性**=start-backend-claude.ps1 泄露（commit bbccdcd8，JWT/DB/RUNTIME/DINGTALK，轮换按用户指令暂缓→记为已接受风险）；**16 条误报**=测试常量（base64 "testSecretKey..."、file-keeper 本地 dev JWT 密钥+测试 token）+ 文档示例（JWT 截断示例串、.env.example 占位 dingXXXXXXXX）
- 验证：--baseline 产出 → 日常模式 `gitleaks git --baseline-path baseline` 复扫 "no leaks found" ✅
- 全量后端回归同轮跑绿（MVN_EXIT=0）

## 收官
17 项全处置完毕：✅ 落地 1/4/5/6/7/8/9/10/12/13/17，⏸️ 记档处置 2/3（用户叫停）/11/14/15/16（人工窗口/独立阶段）。全量后端回归绿（MVN_EXIT=0）。未 push origin/beifen（用户指令）。
