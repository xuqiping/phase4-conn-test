# M2 时间线记忆 · 开发进度 1

> 第 1 轮对话记录。启动 + Phase2 plan + 测试方案 + 脚手架。

## 本轮对话信息
| 字段 | 内容 |
|---|---|
| 日期 | 2026-07-18 |
| 功能 | M2 — 时间线记忆(原待办 10+16+22+24+29) |
| 对应 plan.md | 全部 chunk(chunk0–chunk7) |
| 本轮目标 | plan 落 + 测试方案 + 脚手架 + 开 chunk0 |
| 完成状态 | 🚧 chunk0-6 ✅,待 playwright 冒烟 |

## 本轮完成内容(文档/理论类)
| 产出 | 路径 | 引用位置 |
|---|---|---|
| M2 plan.md | [M2-时间线记忆/plan.md](./M2-时间线记忆/plan.md) | 开发进度总览 + plans/总路由 |
| M2 测试方案 | [docs/测试方案/M2-时间线记忆测试方案.md](../docs/测试方案/M2-时间线记忆测试方案.md) | plan 测试方案段 |
| 进度目录脚手架 | [M2-时间线记忆/](./M2-时间线记忆/) | 开发进度总览汇总表 |

## 关键决策(本轮)
- **时序判定机制**(用户裁决):首次 pending 时 LLM 询问用户 → 答案落 `memory_key_meta` per-key 标,后续复用,直到用户改。非 LLM 自动判、非纯白名单。
- **范围**:5 项全做一轮(chunk0–7),不分批。
- **merge 分隔符修正**:现状是**中文逗号 `，`**(investigator 核实 MemoryConflictService.java:326/342-348),非速查表09 文档所述 ASCII `,`;非时序分支维持现状不动。
- **survivor updated_at 修正**:现有 merge 不刷 updated_at(MemoryService/conflict 代码不 set,走 DB auto);M2 chunk3 时间线分支须显式刷 `now()`。

## 待解决 / 阻塞
- [ ] chunk0 落地前确认:memory_key_meta 表用 `GENERATED ALWAYS AS IDENTITY` 主键 + `@TableLogic deleted`(依 CLAUDE.md 约定)。
- [ ] daily_log 识别规则(chunk5):靠 key 命名约定 vs LLM 抽取归类,待 chunk5 定。

## 下一轮计划
1. ~~chunk0:Flyway V43 + entity/mapper + commit~~ ✅ e3257c42。
2. ~~chunk1:value schema util + 单测~~ ✅ ca24150a(14/14)。
3. ~~chunk2:memory_key_meta 读写端点 + resolve 读标~~ ✅ 309e25c9。
4. ~~chunk3:merge 两分支(时间线/中文逗号)+ 刷 updated_at~~ ✅ 91880a63(零新回归)。
5. ~~chunk4:extract 抽日期 entities + 写侧 gate~~ ✅ 517ea0b0。
6. ~~chunk5:KEEP_CUSTOM 自定义合并 + daily_log 时序识别~~ ✅ 336adea0。
7. ~~chunk6:前端 value 时间线渲染 + 自定义合并 UI + temporal 询问/标编辑~~ ✅ 6bf1e6db。
8. chunk7:playwright 冒烟(全栈起,人工填测试方案实测列)。

## chunk2-6 + 收尾落地(代码类)
| 实现 | plan 编号 | 涉及文件 | commit |
|---|---|---|---|
| key-meta service + GET/PUT 端点 + 6 单测 | M2-c2 | MemoryKeyMetaService/MemoryController/MemoryKeyMetaVO/MemoryKeyMetaUpdateRequest | 309e25c9 |
| merge 两分支(temporal 时间线 / 中文逗号) | M2-c3 | MemoryConflictService.mergeValuesInto + 3 调用点 | 91880a63 |
| extract 日期 entities 指引 + 写侧 gate(不抽 assistant) | M2-c4 | MemoryConflictJudge.EXTRACT_PROMPT + extract() | 517ea0b0 |
| KEEP_CUSTOM 自定义合并 + daily_log 时序 | M2-c5 | MemoryConflictService.resolve + DTO.customValue + isDailyLogKey | 336adea0 |
| 前端 value 时间线渲染 + 自定义/temporal UI | M2-c6 | MemoryManagerPanel.vue + memoryTimeline.ts + chat.ts | 6bf1e6db |

收尾产出:README(M2 目录)+ FeatureMap + UserOps + 总览登记 + 沉淀(M2 行)。

## 关键决策(本轮续)
- **merge updated_at**:核实 mergeIntoRow 已含 `updated_at=now()`(investigator 误报),chunk3 不须另修。
- **timeline KV 开关**:per-key default-false 即真门(无 key_meta 行=非时序=现状行为),`rag.memory.timeline.enabled` 冗余总闸延后。
- **冲突 resolve 单测**:缺(项目惯例走 IT/E2E,V35 文档同述),KEEP_CUSTOM/temporal 分支靠 playwright E2E 覆盖。
- **写侧 gate 落地范围**:chunk4 已实现「extract 不抽 assistant」(冲突决策1 核心);M5「记一下」主动要求通道留 M5。

## 待解决 / 阻塞
- [ ] playwright 冒烟(需后端 8080 + sidecar 8090 + 前端 5173 + redis + pg 全栈):KEEP_CUSTOM 落库 / 时序 merge 多段 / temporal 标持久化 / 老数据兼容。实测列在测试方案,待填。
- [ ] 3 预存基线失败(MemoryConflictJudge entities-cap / RagRetrieval grayZone / RuntimeCallback 401)非本功能,未修。

## Phase4 运行验证(2026-07-18)
**环境**:pg/redis/8080/8090/5173 全栈。后端从 target/classes 起,需 dot-source local-dev-env.ps1(JWT_SECRET/DB_PASSWORD/RUNTIME_CALLBACK_TOKEN fail-closed)。

| 验证项 | 结果 | 证据 |
|---|---|---|
| Flyway V43 迁移 | ✅ | flyway_schema_history version=43 success=t;memory_key_meta 表+uk_memory_key_meta_user_key 建 |
| key-meta GET(无标) | ✅ data=null(首次待询问) | curl 200 |
| key-meta PUT 设时序 | ✅ source=USER_OVERRIDE | curl 200 |
| key-meta 读回 | ✅ isTemporal=true | curl 200 |
| key-meta 翻转 false | ✅ | curl 200 |
| 前端加载 console | ✅ 0 error | playwright console_messages |
| 记忆面板开 | ✅ 50 条加载 | m2-memory-panel.png |
| **值列时间线渲染(chunk6)** | ✅ | 注入 "2026-06-25 住萧山;2027-01-01 住拱墅"→行含 2 日期段 + column flex div,m2-timeline-render.png |
| UserOps 场景 3(看时间线) | ✅ 老数据单值 + 时序多段双显 | 同上 |
| 后端重启拾 MED 修 | ✅ Started in 7.27s | backend-m2-run2.log |

**第二个 AI review**(cavecrew-reviewer,2 MED + 5 LOW):
- MED1 修:isDailyLogKey 不再 || 覆盖 USER_OVERRIDE=false(已设按标,未设才启发式)。
- MED2 修:temporal 单段分支已带日期值不再 withDatePrefix(避双前缀破坏原日期)。
- LOW:javadoc 分隔符 ; 对齐;FK 无 CASCADE 与 user_memories(V6)约定一致;findByConflictId 排序 pre-existing;zone/raw-dedup 不影响。commit 7df2ba09。
- review 另核实无 V34/V35 scope 回归(mergeIntoRow 不动 home/is_global/projects)、extract 写侧 gate 安全、V43 NOT NULL 有填。

**未自动化测(人工/E2E 待填)**:
- KEEP_CUSTOM 落库 + 时序 merge 多段:需触发真冲突(异步 LLM extract,flaky),改人工 playwright 走查。
- temporal 询问首次/复用:同上,需冲突卡。
- 性能评测:merge 重 embed +1 LLM 调用/冲突,p50/p95 未测(单测覆盖逻辑,生产观测待 Phase5)。

**出口判定**:核心路径(V43 迁移 + key-meta CRUD + 时间线渲染 + review 修)亲眼验证通过;UserOps 场景 1/2(冲突卡 KEEP_CUSTOM/temporal)因依赖异步 LLM 触发冲突未自动化,记为遗留人工项。其余出口满足。

## chunk1 落地
| 实现 | 文件 | 说明 |
|---|---|---|
| MemoryValueTimeline | `backend/.../chat/service/internal/MemoryValueTimeline.java` | value schema helper:parse/rejoin/withDatePrefix/mergeTemporal/isTimelineValue |
| 14 单测 | `backend/src/test/.../MemoryValueTimelineTest.java` | 两分支(时序/非时序)+ 存量兼容 + 去重 + 排序 |
| commit | ca24150a | 14/14 绿 |

**预存 HEAD 债(非本次引入)**:`src/test/.../knowledge/service/{ExcelDocumentParseIT,IndexJobFileLifecycleIT}.java` 签名漂移(KnowledgeDocumentService.upload / IndexJobTxService.IndexedDoc),test-compile 失败,阻塞 `mvn test`。临时挪开验我单测,验完还原。需后续单独修(非 M2 范围)。

## chunk0 落地
| 实现 | 文件 | 说明 |
|---|---|---|
| Flyway V43 | `backend/src/main/resources/db/migration/V43__memory_key_meta.sql` | memory_key_meta 表(is_temporal/source)+ uk(user,key) 索引 |
| MemoryKeyMeta entity | `backend/.../chat/entity/MemoryKeyMeta.java` | 同 user_memories 域:不走 BaseEntity,无 deleted |
| MemoryKeyMetaMapper | `backend/.../chat/mapper/MemoryKeyMetaMapper.java` | findByUserKey + BaseMapper |
| commit | e3257c42 | chunk0 + plan + 进度脚手架(docs/ 测试方案 gitignored 留本地,同 M4 惯例)|

编译绿(mvn compile EXIT=0)。chunk0 无业务逻辑可测(纯 DDL+POJO+mapper interface),mapper 查询 IT 延到 chunk2 端点测时一起。

## 引用
- 设计源:[速查表09-个人记忆-演进与待办.md](../../../项目工程文档/项目功能介绍/速查表/09-个人记忆-演进与待办.md) M2 段 + 冲突决策 1/3/5
- plan:[./M2-时间线记忆/plan.md](./M2-时间线记忆/plan.md)
- 测试方案:[../docs/测试方案/M2-时间线记忆测试方案.md](../docs/测试方案/M2-时间线记忆测试方案.md)

## 变更记录
| 日期 | 变更 | 原因 |
|---|---|---|
| 2026-07-18 | 建 M2 plan + 测试方案 + 脚手架 | 功能启动 |
