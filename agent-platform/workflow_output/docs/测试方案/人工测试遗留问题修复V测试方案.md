# 人工测试遗留问题修复V · 测试方案

> 2026-08-27。覆盖 plan 联动点 L1-L6（每条含正向/反向/边界）。
> 自动化基线：后端单测 ProjectGroupQueryServiceTest 16/16 + 真 PG IT ProjectGroupLedgerFilterIT 6/6；
> 前端 vitest 848 全绿 + vue-tsc 0。
> commits：A `3c7926b0` / B `671bea37`（V156 补档 `d4895c63`）。
> 账号：admin/admin123；组长/成员走现有冒烟组（smk4_leader/smk4_mem 密码 admin123）。
> 前置：**后端须以 V 代码重启**（旧进程无导出端点/筛选参数）。

## 一、功能联动用例（L1-L6）

### L1 筛选任一变更→流水表重查（B：17x#1）
| # | 操作 | 预期 | 档位 |
|---|---|---|---|
| 正1 | 组长开组详情「流水」tab，关键词输备注片段 | 表格即收窄至含该词行 | 正向 |
| 正2 | 类型下拉选「兜底」 | 只剩 BACKSTOP 行，类型列显中文「兜底」 | 类型 |
| 正3 | 操作人选某成员 | 只剩该人相关行 | 操作人 |
| 正4 | 时间区间选今天 | 排除历史日期行 | 时间窗 |
| 反1 | 筛选中点「清空」 | 回全量；翻页器回第 1 页 | 清空 |
| 边1 | 筛选后翻页 | 筛选条件保留，第 2 页仍按条件 | 翻页保持 |
| 边2 | 筛选后无匹配 | 空表，无报错 | 空集 |
| 边3 | 组合（关键词+类型+时间） | 条件叠加 AND | 组合 |

### L2 导出按钮→带当前筛选下载（B）
| 正1 | 空筛选点「导出 CSV」 | 下载 group-{id}-ledger-*.csv，全量（≤5万） | 全量 |
| 正2 | 设筛选后导出 | 行集与表格当前筛选一致（非当前页） | 筛选联动 |
| 正3 | Excel 双击打开 | 中文不乱码（BOM），表头 7 列，类型中文标签 | BOM |
| 正4 | 备注含逗号/引号 | 单元格双引号包裹转义，列不错位 | RFC4180 |
| 边 | 下载中按钮 | loading 禁用防连点 | 防连点 |

### L3 MEMBER 打开流水 tab（B，零变化回归）
| 正 | 成员开流水 tab | **无筛选行、无导出按钮**，仅本人行、余额列空 | 口径锁 |
| 反 | 成员拿工具直接调 export 接口 | 403「仅组长/管理员可导出组池流水」 | 服务端拦 |
| 边 | 成员 overview 请求带筛选参数 | 后端忽略，结果与不带一致（抓包可见参数被弃） | 不信前端 |

### L4 重进画布→视频副本恢复（A：2x#2）
| 正1 | 视频节点「创建副本」→ 等副本生成完 → 退出画布 → 重进 | 副本视频内容**显示**（fileId 兜底腿生效） | 正向 |
| 正2 | 图片副本同流程 | 图片照常显示（原有链不回归） | 不回归 |
| 边1 | 生成中（running）副本重进 | 不走兜底，交任务恢复链续命 | running 跳过 |
| 边2 | 版本回滚到含副本的版本 | 副本内容同恢复（回滚链同享兜底） | 回滚链 |
| 反 | 副本指向的文件已被清理（fileId 失效） | 静默空壳，无报错弹窗 | 失效口径 |

### L5 上游视频卡渲染（A：2x#1）
| 正1 | 选中下游节点看「上游」面板，上游有视频 | 显**视频首帧画面**（非「视」字占位） | 正向 |
| 正2 | 悬浮该视频卡 | 放大预览（与图片同规则） | hover |
| 正3 | 单击该视频卡 | 弹 Lightbox 播放（手势不变，点击穿透正常） | 点击 |
| 反 | 上游视频节点无源（未生成/失效） | 回落「视」占位 | 无源 |
| 边 | 图片/音频/文本上游卡 | 样式行为不回归 | 不回归 |

### L6 版本回滚链 hydrate（A）
| 正 | 回滚到含视频副本的历史版本 | 副本经同一 hydrate 兜底恢复 | 同函数 |
| 边 | 回滚到产物尚不存在的版本 | 按占位渲染，不报错 | 空版本 |

## 二、冒烟路径（P4 Playwright 实测脚本线）

1. admin 登录 → 项目组列表 → smk 组详情 → 流水 tab：筛选四维各一发（L1 正1-4）→ 清空（反1）→ 导出 CSV 验文件（L2 正1/3）。
2. smk4_mem 登录 → 同组流水 tab：确认零变化（L3 正）；直调 export 接口验 403（L3 反，可用 curl 带 member token）。
3. 画布（smk4_leader 或 admin 项目）：造视频节点→生成→创建副本→重进（L4 正1）；选中下游看上游首帧+点击 Lightbox（L5 正1/3）。
4. 回归：vue-tsc 0 / vitest 848 / mvn test 单测全绿 + IT 6/6。

## 三、通过标准

L1-L6 全正向+反向+边界过；自动化全绿；问题记 人工测试问题/ 对应文档新「未解决」条目。
