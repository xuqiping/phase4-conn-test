---
description: "网课录屏总结 子 plan · 端到端集成与收尾（Step 12）"
created-date: 2026-08-05
---

# 子 plan · 集成与收尾（Step 12）

> 父 plan：[../网课录屏总结.plan.md](../网课录屏总结.plan.md)。本文件只放 Step 12 详情；全局坑点/安全/性能/术语见父 plan。
> 批次：B6（依赖全部前序 Step）。

## 实现步骤

- [x] **Step 12：端到端集成 + 收尾**（2026-08-07 完成；人工验收项挂起转 Phase 4，见备注）
  - **对应需求**：全部（FR-101 ~ FR-109 验收闭环）
  - **目标**：端到端跑通「录制 2h 网课 → 实时转写 → 停止 → 抽帧/OCR/对齐 → 云端总结 → 学习区 → 导出」；`check_all` 全绿；文档同步；手动验收 AC-101 ~ AC-109。
  - **动作**：
    - **端到端联调**：补齐各阶段 IPC 串联（前端 Processing.vue 编排 `process_frames → process_ocr ∥ align_session → summarize → get_timeline`）；验证进度事件链与可取消
    - **feature flag**：`lib.rs` + 前端入口加开关，控制「录屏总结」入口显隐（运维：出问题能关，不必回滚）
    - **质量门**：跑 `scripts/check_all`（`cargo check` + `vue-tsc --noEmit` + 文档校验），全绿才提交
    - **文档同步**：
      - 更新 [file_structure.md](../../file_structure.md)：新增 `session/ screen/ ocr/ align/ summary/` 模块 + `sessions/` 运行时目录
      - 更新 [architecture.md](../../specs/architecture.md)：架构图加新模块、IPC 命令表加新命令、数据流加三阶段
      - 产 `workflow_output/开发进度/网课录屏总结/`（README + 开发进度n.md，按 AGENTS.md 受众判定）
    - **手动验收**：逐条核对 AC-101 ~ AC-109（见 spec 第七章）
  - **文件**（≤20）：
    - `src-tauri/src/lib.rs`（改）：feature flag + 命令编排
    - `src/components/Processing.vue`（改）：阶段编排
    - `workflow_output/docs/file_structure.md`（改）
    - `workflow_output/docs/specs/architecture.md`（改）
    - `workflow_output/开发进度/网课录屏总结/`（新）
  - **依赖/并行**：`依赖 Step 1–11 全部`
  - **需人工介入**：2h 真实网课端到端验收；AC 逐条签字
  - **安全检查**：复核隐私红线例外已落地（默认仅传文字、多模态二次确认、Key 加密）；`cargo audit` 新依赖
  - **验证**：
    - `check_all` 全绿
    - AC-101 ~ AC-109 人工验收通过
    - 2h 网课端到端跑通：音画同步、抽帧合理、对齐正确、总结完整、导出可用、降级路径生效
    - **回归**：现有纯录音转文字功能不受影响

## 功能级整体验证（Phase 4 准入）

- [ ] 所有单测通过（用例带 AC 编号，可追溯）
- [ ] 关键路径端到端验证（2h 网课）
- [ ] 安全检查清单（父 plan）全部完成并验证
- [ ] 与 spec 对齐复核：每个 FR 有 Step 实现、每个 AC 有测试/人工验收覆盖
- [ ] 性能指标：2h 录后处理 P95 < 8min；总结单段 API P95 < 15s；录制中转写延迟仍 < 2s
- [ ] 依赖与并行化地图与 Step 的「依赖/并行」字段一致；同批 `[P]` 文件无交集

## 备注

- 本功能为独立模块 + 独立会话目录，与现有主流程隔离，feature flag 可随时下线。
- 偏离计划处在此注明原因（Phase 3 实际执行时回填）。

### Phase 3 执行回填（2026-08-07）

- **feature flag** 落地为三层覆盖：`VTT_COURSE_SUMMARY` 环境变量 > `feature_flags.json` > 默认开；前端隐藏 Tab + 后端拒绝 `start_capture_session` 双保险（commit `c6f19cf`）。
- **文档同步**完成（file_structure / architecture，commit `6ec2e4b`，含 workflow_output/docs 首次入库——此前被根 .gitignore 的 `docs/` 规则拦着，已按 agent-platform 先例放开）。
- **收尾产物**完成：功能 README（扁平命名 `网课录屏总结_README.md`，随本功能进度文件的扁平约定）+ Feature Map + User-Ops（commit `9592c0b`）。
- **cargo audit**（本地 advisory-db）：3 个漏洞均为传递依赖且不在运行期攻击面——quick-xml×2（tauri 构建链 plist，app 不解析不可信 XML）、rkyv（built 构建期依赖）；处置 = 不阻塞收尾，待 tauri 上游升级后复跑。隐私红线复核 ✅（默认仅文字上行 / 多模态二次确认 / Key 走 keyring windows-native）。
- **挂起转 Phase 4 的人工项**：① Step8 真实 API 冒烟；② 2h 真实网课端到端 + AC-101~109 逐条签字；③ 回归验证纯录音转写不受影响。
