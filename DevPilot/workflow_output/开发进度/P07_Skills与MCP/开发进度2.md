# P07 开发进度 2（S3–S9）

承接 `开发进度1.md`（S0–S2）。

## S3 技能生成器 + 导入导出（8fa70c7）FR-025/AC-028

- `core-skills/src/generator.rs`：`generate_from_context`（任务原文+关键步骤→SKILL.md 模板）、`save_skill`（同名拒绝覆盖）、`export_skill`/`import_skills`（整目录复制，坏的逐项报告当场撤掉）。
- `commands.rs` 技能生成器分区 + `audit_skill`（复用 task_events Checkpoint 审计）；`ipc.ts` 4 条封装。
- 测试：generator 6 单测全绿。

## S4 MCP 市场（d6681a7）FR-010/AC-012

- `core-mcp/assets/market_catalog.json`（15 个内置 server）+ `market.rs`：parse/detect_runtime（`--version` 探测，缺 Node/uv 给大白话指路）/params_from_entry（必填 env 拦截）/install（去重→探测→入库→尝试启动；启动失败保留记录 installed_not_started）。
- `parse_manual_config`：手动贴 JSON，防路径穿越（`/`、`..`）。
- `tests/market_install.rs` 3 例真安装（probe 用 CARGO_BIN_EXE_mcp-fake-server，不能带引号——带引号 spawn 失败）。

## S5 前端技能斜杠调用（e866c8c）FR-025/AC-028

- `SkillAutocomplete.tsx`（slashQuery 正则 + 候选弹层 + ↑↓/Enter/Esc）、`TaskInputBox.tsx`（候选→chip→提交时 `技能正文\n\n用户文本` 拼接）。
- 测试：TaskInputBox.test.tsx 6 例。

## S6 MCP 管理页（be0d35e）FR-026/AC-029

- `commands.rs`：list（last_error 截 100 字）/start/stop/restart/uninstall/logs（≤200 行×500 字）。
- `McpPanel.tsx`（状态徽章/一键重启/日志抽屉+复制/卸载二次确认）+ `Mcp.tsx` 视图注册（ViewKey "mcp"）。测试 4 例。

## S7 多模态输入（210bb51）FR-011/AC-013

- 后端：save_attachment（10MB 校验→image crate 逐档压 JPEG ≤1MB→`<项目>/attachments/`→入库）、delete_attachment、voice_probe=false、voice_transcribe=UNSUPPORTED。
- 前端：AttachmentChips（校验/拖/粘贴/chip×）、VoiceDictation（探测失败渲染 null）、Build.tsx 接管 TaskInputBox。测试：McpMarket 3 例 + Attachments 6 例。
- 语音诚实降级为二期，未做假按钮。

## S8 测试方案 + e2e（本轮 commit）

- `docs/测试方案/P07_Skills与MCP测试方案.md`：人工 H1–H5 / M1–M7 / A1–A5 + 联动边界（反向/半选/批量）+ 自动化对照。
- `generator.rs::ac028_save_then_list_then_invoke_e2e`：保存→注册立即可见→按名取正文含任务原文。core-skills 16 测全绿。

## S9 文档与索引收尾（本轮 commit）

- Feature Map（含三表注解+大白话）、User-Ops、本 README、开发进度2（本文件）。
- plan S3–S9 勾选；`00_MVP实现计划总索引.md` P07 置 ✅；`file_structure.md` 补新目录。

## 全量质量门（S3–S9 每 commit 前）

clippy -D warnings 0 错 / cargo test --workspace 全绿 / tsc clean / vitest 67 例 / check_docs FAIL=0。

## 下一步

P07 Phase 4 冒烟（/phase4）；P06 人工走查仍挂起（用户指示稍后）。
