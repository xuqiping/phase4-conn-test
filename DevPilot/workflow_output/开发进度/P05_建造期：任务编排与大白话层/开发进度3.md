# P05 建造期：任务编排与大白话层 · 开发进度 3

> 本轮完成 S3 diff 大白话摘要（FR-013）。

## 已完成工作

### S3 diff 大白话摘要（FR-013）
- 新建 `core-orchestrator/src/diff_summarizer.rs`：
  - `fetch_diff` 取 checkpoint 对应 commit 的 `git diff`（首提交回退到 `git show`）。
  - `truncate_diff` 对超过 5000 行的 diff 截断并标记 `truncated`。
  - `summarize` 调用 LLM（复用 `HttpLlmClient` + `/gateway/complete`），用 `diff_summary.txt` prompt 生成结构化 JSON。
  - `parse_summary` 兼容裸 JSON、`\`\`\`json` 代码块、第一个 `{...}` 片段。
  - 单元测试覆盖截断、短 diff 保留、markdown 包裹解析。
- Prompt 模板：`core-orchestrator/src/prompts/diff_summary.txt`（改了什么 / 为什么 / 影响 / 风险 / 涉及文件）。
- `core-orchestrator/src/lib.rs` 导出 `diff_summarizer`。
- `core-state/src/checkpoint.rs`：
  - `Checkpoint` 结构体与 `get_by_task` 查询。
  - `update_summary` 写回 `summary_plain`。
  - 单元测试验证读写往返。
- Tauri `summarize_diff` 命令（`src-tauri/src/commands.rs`）：
  - 读取 task 的 checkpoint → 取 git commit → 调 LLM → 结构化摘要 + 原始 diff 返回前端。
  - 将摘要 JSON 写回 `checkpoints.summary_plain`。
  - 注册到 `src-tauri/src/lib.rs` invoke handler。
- 前端 `src-ui/components/diff/DiffPlainPanel.tsx`：
  - 展示「改了什么 / 为什么 / 影响 / 风险 / 涉及文件」。
  - 一键切换「原始 diff」视图。
  - 截断提示、错误态、加载态。
- `src-ui/lib/ipc.ts`：新增 `DiffSummaryDto` 与 `ipc.summarizeDiff`。
- commit: `6ecd1c1`

## 质量门结果

- `cargo fmt --all`：通过。
- `cargo clippy --workspace --all-targets -- -D warnings`：通过。
- `cargo test --workspace`：全绿（91 tests）。
- `npm run test`：35/35 通过。
- `npx tsc --noEmit`：通过。

## 下一步

- S4 大白话翻译层（FR-036）。
