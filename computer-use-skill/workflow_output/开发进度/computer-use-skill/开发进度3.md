# 开发进度 3 —— Step 12 dogfood + 功能收尾

日期：2026-08-26 ｜ 承接：开发进度2.md（Step 4~11）

## Step 12 · dogfood：voice-to-text GUI 回归（FR-019/AC-022/023）✅
- 链路：skyshot → 视觉定位 → clickAt → skyshot 验证，全程用本技能驱动
- 发现并修复 **Find-Root 缺陷**：Tauri 多窗口进程 `MainWindowTitle` 为空 → Get-Process 匹配不到 → uia.ps1 加 RootElement 顶层标题兜底枚举（commit d7bdb1e）
- 流程证据（docs/测试方案/证据/）：v2t_before → transcribe → after_new（新建会话）→ recording（正在录制+wav 路径可见）→ stopped（回主页）
- 结论：Tauri WebView2 内容 UIA 不透明 → 截图+坐标通道可用（Codex 双通道思路实证）

## 本轮其余修复（commit 6bb11fa）
- 最小化窗口导致截图 0x0 / 激活失效 → IsIconic+ShowWindow(SW_RESTORE) 织入 capture 与 activate
- S11 输入证据补齐：hello123 落入记事本截图留证

## 环境坑记录（重要）
1. **锁屏**：前台=「Windows 默认锁屏界面」时 SendInput 全部不可达——交互测试必须在解锁状态
2. 老版 Win10 记事本 Edit 控件不被 UIA ControlView 代理 → 两层降级是刚需不是兜底
3. 截图像素坐标→屏幕坐标换算：窗口原点+比例（窗口物理宽/截图像素宽）

## 收尾产物
- 测试方案（含联动用例 T1-T5）｜功能 README｜Feature Map｜User-Ops 手册 —— 均已产出
- plan.md 12 步全部 ✅；vitest 29 通过 / check_all 全绿

## 结论
Phase 3 出口条件全部满足，可进 /phase4（冒烟运行验证）。
