# 智能对话 9x 问题修复·开发进度 1

## 本轮完成

- `LlmRequest` 新增非流式局部超时 `timeoutMs`，`OpenAICompatibleProvider` 按请求应用。
- `MemoryTagSelector` / `MemorySummaryReader` 使用 `memory.recall.llm-timeout-ms`（默认 8000ms）；调用异常立即降级，解析错误才重试。
- Chat 模型选择取消前端 `doubao-seed-2.0-code` 静态默认；Gateway 仅对该退役历史值做当前模型回退。
- 本地 Provider 数据已清理重复 2.1 模型，`memory.judge.model` 已升级 2.1；新增 `V96__fix_doubao_chat_defaults.sql`。
- 修复已执行资产 V94 与未执行审计 V94 的 Flyway 重号：审计迁移顺延为 V97，新 JVM 可 clean 启动。

## 验证

- 后端定向测试：40/40 通过。
- 前端定向测试：15/15 通过。
- `mvn clean package` 成功；`npm run build` 成功。
- clean 新 JVM 启动，Flyway V95/V96/V97 通过，`/actuator/health=UP`。
- Doubao Embedding 真测成功：2048 维，约 213ms。
- Doubao CHAT 真测约 475ms 返回 404：已排除超时和本地 classpath，剩余为火山 Coding endpoint/API Key 产品类型配置。

## 下一步

- 拿到火山 Coding 套餐官方确认的协议、完整 endpoint 和对应 API Key 后，在「设置 → 全局模型供应商」更新并点击测试；无需再改代码。
