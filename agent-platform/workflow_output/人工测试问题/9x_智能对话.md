# 已解决
1. **Embedding 假超时/失败**：根因是旧 JVM 混用新旧 class，在解析 usage 时缺少 `TokenUsageBuilder`，不是上游慢。已 `mvn clean package` 并重启，真实 Embedding 测试成功（2048 维，约 213ms）。
2. **记忆召回放大超时**：标签精选/总结深读增加 8s 局部超时（`memory.recall.llm-timeout-ms`）；网络或超时异常立即降级，不再把 30s 连续重试 3 次放大为 90s。
3. **未选仍调用 `doubao-seed-2.0-code`**：取消前端静态 2.0 默认；旧本地存储/旧记忆 turn/后台任务仍携带退役 2.0 时，若当前 Provider 已不支持，仅对该历史默认值回退到当前配置模型。`memory.judge.model` 已更新为 2.1。

# 待运维配置确认
1. Doubao CHAT 已从“超时”定位为“上游 404”：`/api/coding/v3/chat/completions` 真实测试约 475ms 返回 404，不是系统超时。需在火山方确认当前 API Key 是否开通 Coding 套餐，以及 `doubao-seed-2.1-code` 对应的正确协议/完整 endpoint；平台侧已保留可随时修改的 Provider 配置入口。

# 未解决
7. 聊天记入进审计日志，但是用户名是空的？（只有个别模型进日志可以展示用户名）
8. 智能聊天里，多选对话没办法同时选中多条记录
9. embedding模型无法选择
10. 再确定下，是否有写死的模型引用，所有都需要跟随使用选中的模型（兜底选择管理员指定的默认模型）

# 参考速查表
- agent-platform\项目工程文档\项目功能介绍\速查表\21-系统设置.md
>解决完后需要同步更新对应的速查表

## 2026-08-11 补充修复：模型选择统一规则

问题 3 已按新规则重新实现，不再保留“退役模型静默回退”逻辑：

1. 用户或业务模块显式选择模型时，只调用该模型；模型不可用则明确报错，不替换为其他模型。
2. 未显式选择时，使用管理员在“设置 → 全局模型供应商 → 默认模型”中配置的默认模型。
3. CHAT 与 EMBEDDING 分别使用 `llm.default.chat-model`、`llm.default.embedding-model`。
4. 未显式选择且管理员也未配置默认值时，明确提示未选择模型且管理员未配置默认模型。
5. 运行时代码已移除具体模型 ID 默认值；旧模型 ID 只允许存在于历史迁移、测试数据或数据库配置值中。

状态：Phase 3 自动化验证完成；页面实测留 Phase 4。

### Playwright 实测结论（2026-08-11）

- 已修复“选择 Doubao 却被空模型列表 Provider 抢占”的路由问题：空模型列表不再代表支持所有模型。
- 修复后日志确认调用 `provider=doubao`，不再生成空白助手回复；上游失败时页面可见错误提示。
- Doubao 当前仍无法返回正文，原因是火山端点返回 HTTP 404。`/api/coding/v3/chat/completions` 与 `/api/v3/chat/completions` 均已实测 404，后者测试后已回滚；需核对当前 API Key 套餐权限及其对应完整 endpoint/模型 ID。
