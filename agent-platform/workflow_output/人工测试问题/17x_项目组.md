

# 参考feature-maps、user-ops
- agent-platform\workflow_output\docs\feature-map\项目组与积分划拨.feature-map.md
- agent-platform\workflow_output\docs\user-ops\项目组与积分划拨用户操作手册.md

>解决完后已同步更新对应 feature-map 和 user-ops（2026-08-20）：项目组与积分划拨、积分计费系统两份手册均补「页顶统一入口」章节。
>2026-08-26 修复III（§3 Chunk A/B 欠款划拨、§11 Chunk F 产出一键入库）已同步增补至：项目组与积分划拨、项目资产库、积分计费系统 各 feature-map 与用户操作手册「2026-08-26 增补（修复III）」节。


# 未解决
（空——以下 3 项已于 2026-08-25 全部解决，见「已解决」）

# 已解决

## ① 管理层级额度（2026-08-25 解决，V156）
> 原问题：设了管理员权限后不能再给管理员分配额度；期望 管理5000→分给A 1000→A用500→管理总额度4500−A剩500=可分配4000，管理要能看到自己剩余可分配。

- **模型落地**：`project_group_members.allocated_by_user_id` 记录预算归属上级（V156 迁移+回填存量挂组长）；组长可直接在管理行「调额度」定预算；管理给成员配额度走预算硬卡——新预留 ≤ 自己剩余可分配（MemberBudgetService.allocatable = 额度 − 子树已耗 − Σ下级预留，全部先持管理行 FOR UPDATE 再算）。
- **算例验证**（单测 MemberBudgetServiceTest + 实测 curl）：5000/1000/500 → 可分配恰为 4000；配 4000 通、4001 拒。
- **管理自见可分配**：成员表「可分配」列、组卡片、组详情顶部 chip、页顶「参与项目」下拉四处常显。
- **配套规则**：被限额管理不能给成员配「不限」；组长直接调成员限额=改挂组长直管；管理降职后名下成员统一改挂组长；组长调管理预算下限=已占用（子树已耗+下级预留）；管理自身消耗也占预算（doChargeGroup 硬卡）。
- **实测**：t17x 系账号（uid 181-184）全链路 curl ①-⑨ 通过。

## ② 积分安全审计（2026-08-25 解决）
> 原问题：组员能否利用 BUG 刷/盗积分？管理能否越权盗组长积分？

- **真漏洞（已修）**：`requireAffordableGroup` 此前不查成员限额——超额成员继续用会因扣费失败被「计费铁律」吞掉异常而白嫖。已加入口预检：成员 `used ≥ quota` 直接拦；被限额管理 `可分配 ≤ 0` 直接拦。
- **管理盗组长路径全部封闭**：配额度/邀请接受/自身消耗三类扣减均串行在管理行 FOR UPDATE 上，超预算硬拒；管理不能动自己行/角色、不能回收组池、不能任免或移除其他管理、不能改组设置。
- **组员刷分路径**：并发双花由钱包行锁+条件 UPDATE（balance >= cost / used+cost <= quota）+幂等键挡住；管理边花边分双分配由管理行锁串行化挡住。
- **明确接受的设计口径**（非漏洞）：不限额管理=旧扁平模型不受预算约束；降职/移除成员豁免其历史消耗对管理预算的占用；组池在途结算不足时 BACKSTOP 记组长个人账（设计如此，流水留痕）。
- 详见 feature-map「2026-08-25 增补」安全审计小节。

## ③ 组详情只见产出 tab + CHAT 内容列只显「对话消耗」（2026-08-25 解决）
- **只见产出**：admin（uid=1）是组 1 普通成员，前端 canManage 未含平台 admin（后端恒放行 admin）→ 已修 canManage/isAdmin，admin 成员点开组卡片即见完整管理界面。
- **内容列**：后端从未给 CHAT 行供 prompt，前端兜底显示字面量「对话消耗」。已改按轮次配对（chatTurnMap）：每条 CHAT 消耗行回填该轮用户提问（取消耗时刻前最后一条 USER 消息，兜底首条 USER）；无关联提问的老数据显示「-」。实测 usage 716 显「再多说一句它的典型应用场景」、712 显「用一句话说明什么是多Agent智能体平台」。

# 已解决（2026-08-26 勾销，修复设计 §2/§3，commit `30d11f05`）
1. **管理降职限额不缩（200 变 300 被薅）** ✅ —— 降职即缩额：管理被降为普通成员时，
   限额同步收紧为「自己已用 + Σ直接下级 quota」（下级已分配的不收回，但总闸不再虚高），
   并写 MEMBER_RECLAIM 流水留痕；其下级统一改挂组长（预算不悬空）。次生洞（调减不追偿仅限后续、
   收回与消耗竞态）同批封闭——配额变动全部落 MEMBER_ALLOCATE/MEMBER_RECLAIM 流水（A 计划）。
2. **预估核对「项目内剩余」** ✅ —— 预估/提交侧口径改为成员行 quota−used（项目内剩余）优先：
   组成员提交图片/视频/画布任务时，预估展示与硬拦均按「该成员在组内的剩余配额」判定
   （组长/不限额成员仍按组池/余额）；超额直接拒，任务不创建。

# 已解决（2026-08-26 勾销，修复设计III §11，Chunk F，commit `d509a329`）
- **项目组产出一键入库** ✅ —— 组「产出」tab 行尾「入库」按钮（组长/管理/admin 可见）：
  图片/视频行走既有媒体入库桥 `POST /assets/from-media`（复用生成页弹窗，选可写项目→建资产 v1，
  fileId 复用不拷贝）；CHAT 行复用对话入库弹窗预填该轮 assistant 回复。
  **同项目判重**：同 (项目, taskId, imageIdx) 已入库→返回既有资产不重复建（话术「该项目已入库过该产物」），
  行显绿「已入库」tag（刷新仍在，exists-by-source 批量回填）；**换项目可再存一份**。
  多图任务默认第 1 张（其余张去生成页逐张）；EMBED/RERANK 行不可入库。

# 已解决（2026-08-27 勾销，修复设计IV §3，commit `58f64beb`/`75920ed0`）
1. **批量选人全选/反选** ✅（A 轮，commit `58f64beb`）—— 统一选人组件 `UserPicker` 搜索结果区新增
   「全选 / 反选」按钮：全选作用域=当前搜索结果快照（不追后续响应）；搜索请求未返回时按钮禁用防竞态；
   反选=快照内取反。项目组邀请、批量充值等所有用 UserPicker 的入口同步获得。
2. **邀请弹窗下拉时机** ✅（A 轮，commit `58f64beb`）—— 点开「邀请成员」弹窗不再立即弹出全部用户下拉；
   聚焦搜索框/输入关键词时才拉起候选（打开瞬间干净，交互符合预期）。
3. **进组默认 0 积分（非无限）** ✅（D 轮，commit `75920ed0`）—— 三口全堵：
   ① 新邀请不填额度=默认落 **0**（弹窗占位/提示明示「不填=0」，**不限额度已停用**——决策 2）；
   ② 公共池申请审批通过落成员行 **quota=0**（决策 3，预算仍挂组长组池直管）；
   ③ 存量 PENDING 邀请接受时兜底落 0。组长在成员表「调限额」才可配额（配额必填数值，null→400
   「不限额度已停用」）；**存量不限成员不受影响**（NULL 显「不限（遗留）」），不迁移（决策 1）；
   建组组长自动成员行豁免（仍 NULL=组池直管）。池/邀请通过话术均提示「请在成员表调限额后可用」。
4. **成员受限视图** ✅（D 轮，commit `75920ed0`，决策 6「组织信息可见」口径）—— 普通成员可开组详情
   「成员」tab：他人行显 用户（姓名/账号）/角色/加入时间/备注，**限额·已用·欠款·可分配·上级等额度类字段全部隐藏**；
   本人行完整（自己额度全显）；detail/overview 组级财务（组池余额/在途）对成员置 null 不透出；流水 tab 成员视角仅本人行且余额列空
   （页顶「组池」徽标沿用既有 mine 口径仍可见，V139 起选择器数据源，非本轮裁剪范围——已知边界）。
   管理（MANAGER）/组长/admin 视角不裁剪全显；非本组成员仍 403。

# 已解决（2026-08-27 P4 冒烟答疑，行为确认非缺陷）
5. **组池只有 1000、成员限额 10000，还能正常生成任务吗？** ✅ **能**（设计如此，非缺陷）——
   提交预扣链（`MediaBillingService.holdMediaEstimated` → `ProjectGroupWalletService.doChargeGroup`，V161 三腿瀑布）：
   ① 成员限额闸只比对**成员行自身** used+预估 ≤ quota(10000)（ProjectGroupWalletService:300），不看组池；
   ② 资金瀑布（:313-334）：组池扣 `min(余额, 消耗)`=1000 扣尽 → 差额转**成员名下余额** → 仍不够走**组长兜底**
   （settleShortfallByLeader:381，组长个人钱包够=真扣，也不够=挂账兜底，任务照常创建运行）；
   ③ 成员 used 累加真实消耗；只有 used 超 quota 的溢出段才转成员欠款（本例消耗 ≤10000 不产生）。
   一句话：**限额是「个人帽」，组池是「组资金」，组资金不够时瀑布兜底腿接住，不阻断生成**。
   组长可在组详情页看 BACKSTOP 流水（「组池+名下皆尽·组长兜底」）追溯每笔垫付。

# 已解决（2026-08-27，commit `76487591`）
- **成员页加一个剩余可用积分** ✅ —— 三处落位（前端纯展示，quota−used 透支显 0，欠款列另有拆分）：
  ① 组长/管理/admin 成员表新增「剩余可用」列（限额·已用旁；NULL=不限/组长直管行显「不限」，余 0 红字）；
  ② 普通成员受限视图本人行额度内联扩为「限额 X · 已用 Y · 剩余可用 Z」（他人行仍 '-'，受限口径不变）；
  ③ 列表页成员卡片「我的限额」段补「剩余可用」。
  实测：组长视角 成员行 100|0|100、组长行「不限」；成员视角本人行 `100 · 已用 0 · 剩余可用 100`、组长行 '-'；卡片 `我的限额 100 · 已用 0 · 剩余可用 100`。vue-tsc 0 / vitest 846 全绿。

# 未解决
（空）