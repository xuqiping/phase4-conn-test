# 修复X · B 轮开发进度（从库选择全类型预览 + 交互三分离）

> 对应 plan Chunk B（X-2，P0）· spec §4 X-2 · commit B1=`9c0cf4c6` B2=`5c7c6625`

## B1 预览基座（commit 9c0cf4c6）

- [HoverPreviewImage.vue](../../../frontend/src/components/media/HoverPreviewImage.vue)：新增 `kind?: 'image'|'video'` prop（默认 image，既有 5 处调用零改动）。video 态弹首帧放大（`preload="metadata"` 解 objectURL 出首帧无网络请求、muted playsinline、无尺寸行——video 无 naturalWidth 语义）。
- [Lightbox.vue](../../../frontend/src/components/canvas/Lightbox.vue)：`.lbx` z-index 2000→3000——从库选择弹窗（n-modal teleport body 默认 2000+ 栈）内开灯箱需盖住弹窗；Annotate/FocusEdit 的 2000 属编辑态独占交互不同屏共存不冲突。
- [HoverPreviewImage.test.ts](../../../frontend/src/components/media/HoverPreviewImage.test.ts)（新建 4 用例）：默认 img 基线 / video 分支无 img 无 dims / null 占位两态同口径 / 300ms 防抖+移出即关。
- **测试坑**：NPopover trigger=manual 内容 teleport 到 body 且 show 才渲染——断言走 `document.body.querySelector`；happy-dom 不派发真实 transitionend（收起过渡挂起），移出即关改断言 `findComponent(NPopover).props('show')===false`；findComponent 按名字查不到 naive 组件，须 import 后按组件引用查。

## B2 picker 行改版（commit 5c7c6625）

- **新增 [AssetPickerRow.vue](../../../frontend/src/components/canvas/AssetPickerRow.vue)**（每行一 `useLazyFilePreview` 实例——组合式不能在 v-for 内调用，AssetCard 网格同范式；plan 涉及文件单列 AssetPicker.vue 的偏差已记 plan 变更记录）：
  - 四态缩略（72×48）：图=img+悬浮放大（HoverPreviewImage）；视=video 首帧+▶ 角标+悬浮首帧放大；音=「音」字标；文=textPreview 片段（无片段/失败/无 fileId 回落类型字标）。
  - 交互三分离：点缩略=预览（图/视开 Lightbox，z-index 3000 盖弹窗；url 未就绪不开）；点「选择」=选定；行空白零动作（去整行 @click 防误选）。
  - 音频行内条：meta 区下方整行 `<audio controls preload="none" @click.stop>`（plan 细化1）。
  - a11y：图/视缩略为原生 BUTTON（Enter 激活浏览器保证）+aria-label；音/文无灯箱语义渲染为静态 div（不设假按钮）。
- **[AssetPicker.vue](../../../frontend/src/components/canvas/AssetPicker.vue)**：行渲染交 Row 组件，statusLabel/行样式迁移；选择链 onPick/resolve/防重入不动。后端 list 已返 fileId（assembleRoles 单 IN 查询），零后端改动。
- [AssetPickerRow.test.ts](../../../frontend/src/components/canvas/AssetPickerRow.test.ts) 新建 11 用例：四态渲染 / 失败回落 / 文本类不拉文件 / 点缩略开灯箱不触发 pick / 视频 kind / 选择 emit+loading 透传 / 行空白与 audio 条零 pick / ARCHIVED 半透明+meta。既有 AssetPicker.test.ts 7 用例全数回归（去整行点击无既有依赖）。
- **测试**：全量 vitest 1009/1009（+10）+ vue-tsc 0 错。

## B3 收口记录

- 全绿：1009/1009、tsc 0 错。手测项转 R 系列测试方案（Chunk D）：图悬浮 300ms 放大+尺寸行、点击滚轮缩放；视频悬浮首帧、点击弹播放；音频行内播；文本片段省略；Lightbox 盖弹窗实测；Esc 关灯箱弹窗仍在。

## 下一步

C 轮（C1 纯函数组边收集+组感知重映射 → C2 CanvasBoard 接线分流 → C3 收口）。
