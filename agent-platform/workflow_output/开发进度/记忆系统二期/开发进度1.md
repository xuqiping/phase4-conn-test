# 记忆系统二期 · 开发进度 1（P1 收录路由）

> 创建：2026-08-07（第 1 轮 Phase3）。范围：P1 收录路由 7 步 + 测试方案。
> 依据：`docs/plans/记忆系统二期_P1收录路由.plan.md`、`docs/specs/记忆系统二期.md`（FR-001~008）。

## 本轮完成（Steps 1–5 + 6a，全部提交且测试绿）

| 步 | 内容 | commit | 测试 |
|---|---|---|---|
| A | 测试方案 `docs/测试方案/记忆系统二期P1收录路由测试方案.md`（E1-E9+L1-L5） | f66b79b | — |
| 1 | V65 建 `memory_project_rules`/`memory_project_entries` + 4 条 KV 种子；本地库已应用+flyway 登记+repair。**V66 空过**（过渡列不需要，turns 列留到 V67 一次删） | 45f0c6b | — |
| 2 | 规则 CRUD：实体/mapper(XML)/DTO/`MemoryProjectRuleService`/`MemoryProjectRuleController`（GET/PUT `/api/chat/memory/projects/{pid}/rule`）；anchor 重建（embed 失败→enabled=false）、部分唯一索引冲突回退 update、非 owner/admin 不返 negative_examples | 19cb9b0 | 8 绿 |
| 3 | 路由链：`MemoryRoutingService`（总开关→ACTIVE 项目×双 toggle→粗筛 halfvec 阈值∪BM25 top-K≤3→RRF 合流）+ `MemoryEntryDistiller`（单次批量 LLM 判定，`<memory_data>` 防注入，幻觉 project_id 逐项丢）+ 置信度三档分流（KV 可调）+ 黑名单二次扫描降级 + `MemoryGenerationService` 接线（writeTurn 返 MemoryTurn，genDone 后 routeAsync）；`SystemSettingService` +4 KV | b4eefda | 17 绿，全包零回归 |
| 4 | 审核：`MemoryEntryReviewService`（owner/admin 收/弃，**弃→负例反哺** 滚动 ≤5 FIFO；成员仅见自己产生的）+ `MemoryEntryController`（list/review/withdraw）；`MemoryPrefilter` 暴露 `hitsBlacklist` | b39b5bb | 8 绿 |
| 5 | 召回合流（FR-007）：`MemoryEntryRecallService`（读者 ACTIVE 成员过滤，批量 IN 防 N+1）→ pipeline ①.5 `entry-merge` 步（独立降级）→ 条目标签并入 ② 候选 → ⑥ 标签交集筛选 → ⑦ `【项目记忆】` 段（`- 作者·标签：L1`）；**AclResolver 五路径→成员判定**（recall_acl 读取代码移除） | 568306a | 31 绿，全包 314 绿零回归 |
| 6a | recall-acl 全下线（FR-006 半）：端点 404、ConfigService/Mapper/实体/DTO/3 测试类删除、前端 ACL 页签+api 删除、生命周期级联不再清 ACL | 565ec19 | 293 绿零回归 |

## 关键决策/偏离（须延续）

1. **条目标签策略偏离 plan**：条目复用源 turn 已归一的 tag_ids（作者个人库，D2 案A），不走 distiller 的 tag_suggestions —— 零额外 LLM，语义等价。plan 中 tag_suggestions 相关描述视为废弃。
2. **V66 空过**：不建过渡列；turns 四列（project_ids/born_personal/departed/deleted_project_ids）留 V67 一次删。
3. **审核权限检查放在 service 内**（防 controller 遗漏，纵深防御）。
4. **reject 负例用条目蒸馏 L1**（非原文）——原文不出个人域。
5. **Step 6 拆 6a/6b**：6a 只下线 recall-acl 代码（表留 V67 随 turns 列一起 DROP，避免迁移号漂移挤占 P2 的 V68）；6b=写入目标下线+turns 删列+V67，爆炸半径 ~40 文件（ChatSessionService→MemoryGenerationService 参数链、mapper XML 个人条件 `born_personal` 去除、patcher/aggregator 项目分支、Backfill、DepartedResolver、~15 单测+~10 IT），**整一轮做**。
6. **recall_admin 字段保留在 memory_project_members**（列不删），仅概念作废——6b 时可顺手清理引用。

## 下轮接续（按序）

1. ~~**Step 6b**（FR-006 收尾）~~ ✅ 已完成（见下方「第 2 轮」）。
2. **Step 7**（FR-001/005）：前端 MemoryRulePanel.vue + MemoryEntryReviewPanel.vue + MemoryManagerPanel 加页签 + api/memory.ts 端点（GET/PUT rule、GET entries、POST review、DELETE entry）。
3. P1 整体验证（E2E 手测：配规则→聊天→条目进项目→审核→召回带作者前缀；隐私断言：项目端点无 raw_content）。
4. 之后 B2 = P2（项目授权）∥ P3（多模态），B3 = P4（总结共享）。

## 环境备忘

- mvn 须 `JAVA_HOME=D:/IT/java/jdk-17.0.20+8`；测试惯例：纯 Mockito + `@BeforeAll TableInfoHelper.initTableInfo(new MapperBuilderAssistant(new MybatisConfiguration(),""), Entity.class)`。
- V65 已手工应用到本地 dev 库并 flyway:repair；其他环境靠 Flyway 自动跑。
- ⚠️ 旁发现（非本任务）：积分计费系统 plan 声称用 V63/V64，但已被 provider_full_url_category/seedance 占用——下轮提醒用户。

---

# 第 2 轮（2026-08-08）：Step 6b 完成 —— FR-006 全收尾（commit aaf2e87）

## 本轮完成

**Step 6b（FR-006 收尾，61 文件 +615/−3451）**：一期写入目标/出身标记全下线，turns 纯个人域化。

- **V67 迁移**：`memory_turns` 删 project_ids/born_personal/departed_project_ids/deleted_project_ids 四列 + DROP `memory_recall_acl`。本地 dev 库已手工应用 + flyway 登记 + repair。
- **后端主代码**：MemoryTurn 实体/mapper XML/patcher/aggregator/pipeline/consolidation/backfill/generation 全部纯个人域化（个人召回条件 = `user_id=self`）；`MemoryLifecycleHookService` 重写（8 mapper 构造，`onProjectDeleted(projectId)` 单参，新增 memory_project_rules/entries 软删清理）；`ProjectService.delete` 适配单参；`MemoryTurnViewService`/`MemoryTurnController`/DTO（MemoryTurnVO/MemoryRawView/MemoryRecallResult/ChatRequest）去项目字段。
- **整类删除**：MemoryLifecycleController/Service、MemoryScopeResolver、MemoryRecallAclResolver、MemoryDepartedResolver、MemoryScope、SqlScopeFilter + 5 个关联 DTO/VO + 5 个测试类。
- **前端**：ChatView 写目标控件组+旧读范围控件下线（留 MemoryRecallScopePopover）；chat store 删 memProjectId/memIncludeGlobal/memReadProjectIds + memoryScopePayload（4 处 spread）；api/chat.ts ChatSendRequest 删三字段；api/memory.ts 类型对齐 + lifecycle 4 方法删除；MemoryLifecyclePanel.vue 删除；MemoryManagerPanel 去 lifecycle 页签；MemoryTurnSection 去「项目出身」徽标。
- **测试改写**：MemoryGenerationServiceTest（4 参 processTurn + 恒 personal fallback）、MemoryTagAggregatorTest/MemoryTurnPatcherTest（个人域only）、MemoryConsolidationServiceTest（项目 scope 跳过注记）、MemoryRecallPipelineTest（8 参构造）、MemoryLifecycleHookServiceTest（8 mapper）、ChatSessionServiceTest/ProjectServiceTest/MemoryBackfillServiceTest 适配；5 个 IT（MemoryDataLayerIT/MemoryConsolidationDataLayerIT/MemoryLifecycleHookIT/MemoryTurnViewIT/MemoryGenerationWriteIT）个人域化重写。

## 测试证据

- 后端单测 850 绿（3 个失败经 `git stash` 干净树验证为预存：RuntimeCallbackSecurityTest/AuthServiceTest.login_success/RagRetrievalServiceTest，非本轮引入）。
- 5 个受影响 IT 全绿（33 tests, 0 failures）：MemoryDataLayerIT 9 + MemoryConsolidationDataLayerIT 12 + MemoryLifecycleHookIT 4 + MemoryTurnViewIT 5 + MemoryGenerationWriteIT 3。
- 前端 vue-tsc 0 错；vitest 41 文件 241 全绿。

## 关键决策/偏离（须延续，新增）

7. **dev 库 flyway 历史分叉已修复**：V60__provider_full_url_category 后被改名 V63（资产库 V60-V62 插入），dev 库历史停在被改名前 → 手工应用 V60/V61/V62/V64 SQL + UPDATE 历史行（V60 provider 行→63）+ 重排 installed_rank + flyway:repair。双库（agent_platform / agent_platform_it）现已对齐，IT 可跑。
8. **有意保留**：MemoryNotificationBadge 的 PROJECT_DELETED_AFFECTED 渲染（历史通知行仍可能存在）；MemoryConsolidationScopeRequest 的 authorFilter/authorIds/includeDeparted 类型（后端 DTO 未动）。
9. **IT 跑法**：`mvn test -Dsurefire.excludedGroups= -Dtest='...IT'`（IT 走 `@ActiveProfiles("it")` → agent_platform_it 库）。

## 下轮接续（按序，更新）

1. **Step 7**（FR-001/005）：前端 MemoryRulePanel.vue + MemoryEntryReviewPanel.vue + MemoryManagerPanel 加页签 + api/memory.ts 端点（GET/PUT rule、GET entries、POST review、DELETE entry）。
2. P1 整体验证（E2E 手测：配规则→聊天→条目进项目→审核→召回带作者前缀；隐私断言：项目端点无 raw_content）。
3. 之后 B2 = P2（项目授权）∥ P3（多模态），B3 = P4（总结共享）。
