# 企业级精准知识库 RAG 开发进度 1

## 本轮目标

进入 Phase 3，完成开工预处理，并启动 P0 Step 1。

## 已完成

- 读取 Phase 3 权威流程、项目规范、5 份 RAG specs 和 P0 计划。
- 判定本功能需要人工测试，建立 `企业级精准知识库RAG测试方案.md`。
- 将 P0 Step 1 标记为进行中。
- 按 TDD 先建立数据库迁移与实体映射契约测试。

## P0 Step 1 实现

- 新增 Flyway `V101__rag_pipeline_trace_foundation.sql`：
  `rag_pipeline_versions / rag_retrieval_runs / rag_ranking_runs / rag_model_calls / rag_fallback_events`。
- 新增 5 类实体和 Mapper；UUID 在 Java 层使用字符串，SQL 显式 `::uuid`，JSONB 显式类型处理。
- 敏感内容默认只保存 query/input/output/candidate Hash，不保存完整 Prompt、Chunk 或密钥。
- 新增 `RagPipelineTraceSchemaTest` 和 `RagPipelineTraceMapperIT`。

## TDD 与验证记录

1. RED：V101 和实体缺失，测试 1 failure + 1 error。
2. GREEN：补迁移/实体后契约测试 2/2。
3. RED：JSONB 未启用 `autoResultMap`，测试准确失败。
4. GREEN：接入 `JsonbStringTypeHandler`，契约测试 3/3。
5. RED：显式 Mapper 插入方法缺失；补方法后又由真实 Spring 启动发现 UUID TypeHandler 缺失。
6. GREEN：Java ID 改字符串、SQL `::uuid`；契约测试 4/4。
7. 真实 PostgreSQL：Flyway 成功把 `agent_platform_it` 从 v93 升至 v101；既有 `ReconciliationIT` 3/3。
8. 真实数据链：关闭重复迁移扫描后，`RagPipelineTraceMapperIT` 1/1，通过 retrieval→ranking→model→fallback 外键关联查询。

## 工作区并发风险

- 本轮期间其他任务新增 `V100__security_hardening.sql`，与现有 `V100__memory_entry_source_turn_input_id.sql` 冲突；另有已暂存 `V102__auth_multi_credential.sql`。
- 这些文件不属于本 Chunk，未擅自重命名或删除。当前全量 Flyway 启动会被重复 V100 阻断，须由对应安全任务协调版本号后恢复全项目质量门。
- 因工作树含大量未提交用户/并发改动，本轮未创建混合 commit，避免把无关变更收入存档点。

## 下一步

## P0 Step 2（进行中）

- 新增 V103 `rag_ranking_configs`：支持 `LLM/RERANK/DISABLED`、KB 覆盖、管理员默认、候选/批次/超时/降级策略和配置版本。
- 新增 `RankingConfigService` 解析咽喉，固定优先级：KB 覆盖 → 管理员默认 → 明确报错。
- 新增 `RERANK` 模型供应商类别；LLM 只校验 CHAT 能力，专用重排只校验 RERANK 能力。
- 显式模型不可用直接报错，绝不选供应商列表第一项或替换成其他模型。
- TDD：`RankingConfigServiceTest` 初始因实体/Mapper/Service/类别不存在编译失败；最小实现后 6/6 通过。
- 回归：`LlmProviderServiceTest + RankingConfigServiceTest + RagPipelineTraceSchemaTest` 共 13/13 通过。
- 写入逻辑第二轮 TDD：新增“归档旧 ACTIVE + 插入版本化配置”和“DISABLED 清空模型”测试；RED 因 DTO/归档方法缺失准确失败，已补事务性保存实现。
- 新增独立 `RankingConfigController`：管理员默认和 KB 覆盖各有 GET/PUT；写接口同时具备权限、owner 校验与 `@AuditLog`。
- 新增 `RankingConfigControllerContractTest`，但最终运行被并发认证模块 `EmailService.java` 缺少阿里云邮件 SDK 依赖的全项目编译错误阻断，尚不能标记 Step 2 完成。
- 并发安全迁移已从重复 V100 顺延到 V104，Flyway 版本冲突当前已解除。

## P0 Step 2 完成

- 后端重跑 `RankingConfigServiceTest,RankingConfigControllerContractTest,LlmProviderServiceTest,RagPipelineTraceSchemaTest`：17/17；前端收尾后再次定向 13/13。
- 新增 `/api/llm/models/active?category=`，业务页面只获取当前启用且能力匹配的模型 ID，不暴露密钥。
- 管理员设置的 RAG/召回页新增默认重排配置：`LLM/RERANK/DISABLED`、模型下拉、候选数和最终证据数。
- 知识库新建/编辑页不再手填 Rerank 模型；按模式加载 CHAT 或 RERANK 模型，并写入知识库级配置覆盖。
- 前端 `vue-tsc + vite build` 成功；显式模型不可用仍由后端明确报错，不静默替换。

## 下一步

进入 P0 Step 3：统一 RAG Trace 上下文、MDC 和模型/计费/审计关联。

