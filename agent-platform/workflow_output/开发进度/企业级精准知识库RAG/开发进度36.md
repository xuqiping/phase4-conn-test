# 企业级精准知识库 RAG · 开发进度 36

- 主题：P4 Grounded Answer 接入真实 `/retrieve` 生成链路。
- RED：扩展 `GroundedAnswerServiceTest` 与 `RagRetrievalServiceTest`，要求事实渲染保留 Citation、真实服务调用 `synthesize`；因构造依赖和 `renderFacts` 不存在而编译失败。
- GREEN：`/retrieve?generateAnswer=true` 先按证据批次调用模型提炼严格 JSON 事实，再按合并事实生成最终答案；越界 Citation 由编排器白名单剔除，最终答案继续执行 `CitationChecker` 与一次严格重试。
- 六态：Grounded 冲突会把 `confidenceState` 置为 `CONFLICT`；无合法事实进入 `INSUFFICIENT` 拒答路径，正常路径保留证据策略计算状态。
- 模型解析：请求不指定模型时仍由 `LlmGateway` 读取管理员默认；无默认明确报错，没有新增具体模型 ID。
- 日志关联：`LlmRequest.callPurpose` 显式区分 `GROUNDING_FACT_EXTRACTION` 与 `GROUNDED_ANSWER_COMPOSITION`，共用原 RAG trace、模型调用表和计费链路；日志不记录完整 Prompt、Query、Chunk 或输出。
- 验证：`mvn '-Dtest=GroundedAnswerServiceTest,RagRetrievalServiceTest,LlmGatewayTest' test`，27/27 通过。
- 下一步：让 `/ask` SSE 使用同一 Grounded Answer 协议并在 Citation/DONE 前输出一致状态，完成 P4 回归后进入 P5 真实性修复。
