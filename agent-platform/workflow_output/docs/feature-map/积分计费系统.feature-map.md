# Feature Map · 积分计费系统

> Phase 3 产物。**粗略级功能-代码速查表**，面向开发者/维护者。
> 记录「功能对应的所有代码位置」+「关键技术的采用技术 / 一句话原理 / 大白话展开 / 生活化案例 / 踩坑批注」。
> 涉及建表的功能，额外产出「表用途 / 字段用处 / 表关联」大白话注解（建表用 **Flyway** 管理）。
> 终端用户的傻瓜式操作步骤写在 [../user-ops/积分计费系统用户操作手册.md](../user-ops/积分计费系统用户操作手册.md)。
> 本功能 PRD 编号：B1–B13（见 spec）
> **文档规模**：≤5000 tokens。

## 一句话描述
全平台预付积分钱包——用户充值积分，调任意大模型（文本/embedding/视频/图片）按价表实时扣积分；用户只见积分，管理员见真实 token+¥+积分。

## 代码位置总览

| 类型 | 路径 | 作用说明 |
|---|---|---|
| 后端 Controller | `billing/controller/BillingController.java` | `/api/billing`：admin 全局账单（overview/by-user/by-model/by-kind/trend/call-log）+ 用户钱包（me/wallet、me/usage） |
| 后端 Controller | `billing/controller/PricingConfigController.java` | 价表 `/pricing` + 阶梯比例 `/ratio` CRUD（`pricing:manage`） |
| 后端 Controller | `billing/controller/WalletAdminController.java` | `/recharge` 手动充值（`points:recharge`） |
| 后端核心 Service | `llm/LlmGateway.java` | **计费咽喉**：chat/chatStream/embed 出口调 `LlmBillingService.onSuccess/onFailure` 采+扣；`resolveBillingUser` 归户 |
| 后端核心 Service | `billing/service/LlmBillingService.java` | 采 token→PricingService 算 ¥→RatioService 折积分→WalletService 同步扣；失败全退 |
| 后端核心 Service | `billing/service/PointsWalletService.java` | 预检 `canAfford`（balance>0）+ 同步行锁扣减 + refund 逆向退 |
| 后端 Service | `billing/service/PricingService.java` | 各 kind（CHAT/EMBED/IMAGE/VIDEO）按 effective_from 取调用时价算 ¥ |
| 后端 Service | `billing/service/PointsRatioService.java` | 阶梯 ¥→积分比例命中区间 |
| 后端 Service | `billing/service/MediaBillingService.java` | 视频 markSucceeded 扣 / markFailed 退；图片 stub 预留 |
| 后端 Service | `billing/service/UsageCollector.java` | 异步采集 usage→`llm_usage_logs`（fire-and-forget 独立池） |
| 后端 Service | `billing/service/PricingConfigService.java` / `BillingQueryService.java` | 价表比例管理 / 账单聚合查询；新增候选排除 provider 专属配置，并兼容 `provider_id=null` 历史全局价按同名模型占用候选。**7x-2**：exportAll/generateTemplate/importAll（upsert 按 providerId+model+kind+hasReference） |
| 后端 DTO | `billing/dto/PricingRuleExportItem.java` / `PricingImportResult.java` | **7x-2** 价表导出/导入 DTO（镜像供应商导入那套，无加密） |
| 后端 Controller | `billing/controller/PricingConfigController.java` | **7x-2** +3 endpoint：`GET /pricing/export`（blob下载）、`GET /pricing/template`（联动供应商未配置模型模板）、`POST /pricing/import`（upsert，均 @AuditLog） |
| 后端 Service | `billing/service/PricingService.java` | **7x-3** computeCost 加 hasReference 参数；VIDEO 查价表带 has_reference 谓词 + fallback 到 false 行（不区分的模型配1行即可） |
| 后端 Service | `media/service/MediaGenTaskWorker.java` | **7x-3** handleSucceeded 从 request.getAttachments() 算 hasReference（kind=="video"），透传 chargeMedia |
| 后端 DTO | `media/dto/MediaTaskVO.java` | **7x-4** 加 hasReference 计算字段（list/detail 都返，供审查） |
| 数据库迁移 | `db/migration/V95__pricing_has_reference.sql` | **7x-3** pricing_rule 加 has_reference BOOLEAN NOT NULL DEFAULT FALSE + 重建索引 |
| 归户上下文 | `billing/context/BillingContext.java` + `Filter` + `TaskDecorator` | 请求线程种 userId；异步线程用 TaskDecorator 透传 |
| 后端实体/DAO | `billing/entity/*` + `billing/mapper/*` | 6 张表实体 + MyBatis-Plus mapper；`PricingRuleMapper#countConflictingProviderModel` 让创建判重与候选过滤一致 |
| 数据库迁移 | `db/migration/V65__billing_wallet.sql` | balance + ledger + payment_order + llm_usage_logs |
| 数据库迁移 | `db/migration/V66__billing_pricing.sql` | pricing_rule + points_ratio_tier |
| 数据库迁移 | `db/migration/V80__security_baseline.sql` | **S1 计费安全**：余额非负 CHECK + `idempotency_keys` 表 + `uq_ledger_ref` 唯一约束 + ledger REVOKE（DO 块范式） |
| 后端 Service | `billing/service/BillingReconcileService.java` | **S1 对账**：每日「余额=Σ流水」FULL OUTER JOIN 抓孤儿 + admin 手动拉 `GET /admin/reconcile`；只告警不自动修账 |
| 安全守卫 | `billing/mapper/UserPointsBalanceMapper.java#adjustBalanceReturn` | **S1 SQL 守卫**：单语句 `UPDATE ... WHERE balance+delta>=0 RETURNING`，0 行=拒扣（并发超扣防击穿的最后防线） |
| 前端 API | `frontend/src/api/billing.ts` | Axios 调 `/api/billing/*` |
| 前端页面（用户） | `frontend/src/views/MyWalletView.vue` | 我的钱包：余额 + 流水（只显积分） |
| 前端页面（admin） | `frontend/src/views/BillingAdminView.vue` | 全局账单：真 token+¥+积分，按用户/模型/日期筛 |
| 前端页面（admin） | `frontend/src/views/admin/PricingConfigView.vue` | 价表 + 阶梯比例 CRUD |
| 前端页面（admin） | `frontend/src/views/admin/WalletAdminView.vue` | 手动给用户充值 |
| 测试 | `src/test/java/com/superprogrammer/billing/**` | 并发扣减/询价/比例/采集/ownership 单测 |

## 调用链路（关键路径）

```
对话/RAG/记忆/视频提交
  →① 预检 PointsWalletService.canAfford(userId) balance>0？ no→拦截
  → 真调用 LlmGateway.chat/chatStream/embed  或  MediaGenTaskWorker
      ├─② 采集(异步) UsageCollector → llm_usage_logs   [审计·admin 看真 token]
      └─③ 计费(同步事务): token→PricingService→¥→RatioService→积分
              → PointsWalletService SELECT FOR UPDATE balance + INSERT ledger
      ├ 失败 → refund() 逆向退
  → 用户视图读 balance+ledger(显积分) ；admin 读 usage_logs(真 token+¥+积分)
```

## 技术清单与原理注解（大白话版）

### 1. 计费咽喉（出口归户）— `llm/LlmGateway.java`

- **采用技术**：网关出口拦截 + 显式 userId 优先 + ThreadLocal 上下文兜底
- **一句话原理**：所有大模型调用统一过 `LlmGateway`，出口处按 userId 采 token、算钱、扣积分——新模块只要调 gateway 就自动计费，免改。
- **大白话展开**：把计费逻辑收口在「必经之路」（gateway 出口），而不是散落在每个调用点。谁调 chat/stream/embed 谁就自动被计费，调用方只管传 userId。
- **大白话案例**：像地铁闸机——不管你从哪个口进（对话/RAG/记忆），出站（gateway 出口）都得刷卡计费，不用每个口各配一个收费员。
- **为什么用它**：13+ 调用方零回归接入；记忆管线异步线程显式传 userId 也照扣（不依赖请求上下文）。
> 📌 批注：异步 fire-and-forget 线程（如记忆生成）拿不到请求 ThreadLocal，**必须显式传 userId**，否则 `resolveBillingUser` 回退 `BillingContext.current()` 为 null → 仅采不扣（漏扣）。记忆管线 9 调用点全显式传，安全。

### 2. 预检>0 + 后扣实际 — `PointsWalletService`

- **采用技术**：调用前只验余额>0，调用后按真实用量扣
- **一句话原理**：文本/流式的真实 cost 调用完才知道，所以先放行后扣，估不准就宁可后扣不预估。
- **大白话案例**：像先吃饭后结账——不预估你这顿多少钱（估不准会误拦/多扣），吃完按真实菜单算，吃超了下顿前拦住。
> 📌 批注：不做预估预扣（用户决策）。**S1 起「瞬时欠款」模型作废**：V80 CHECK 余额非负 + SQL 守卫单语句拒扣，透支笔直接拒（余额不足报错），不再允许扣成负数。

### 3. 并发扣减行锁防超支 — `PointsWalletService` + 单语句守卫

- **采用技术**：同步事务内单语句 `UPDATE ... SET balance=balance+delta WHERE balance+delta>=0 RETURNING`（行锁含在 UPDATE 里）+ INSERT 流水；S1 起 0 行返 null = 拒扣转业务错
- **一句话原理**：扣钱时把这用户的余额行锁住，并发扣减排队，杜绝两个线程同时读到旧余额各扣一遍超支；S1 把「查再扣」两步合成一句，连竞态窗都关死。
- **大白话案例**：像取款——柜员锁住你的账户本（行锁），一笔一笔改，不会两人同时取光；S1 后柜员还会在改之前先看余额够不够，不够直接拒，不再允许先取成透支。
> 📌 批注：扣减**同步在调用线程**，不走异步池（异步池满会丢任务）；采集才异步。单行写 <5ms。并发 20 线程不超支由 PointsWalletConcurrencyIT 实证。

### 4. 两层分权视图（积分 vs 真 token）

- **采用技术**：价表标 ¥，积分 = ¥ × 阶梯比例；用户视图读 balance/ledger 显积分，admin 读 usage_logs 见真 token+¥+积分
- **一句话原理**：用户只看积分（¥×比例换算），看不到原始 token 和钱；管理员看全量真实成本。
- **大白话案例**：像游戏厅——玩家只见游戏币（积分），老板才看每台机器真实电费+收入（token+¥）。
> 📌 批注：用户查询硬过滤 `WHERE user_id=currentUser`（ownership）；usage_logs 不含 prompt/回答原文，无 PII。

### 5. 流式 usage side-channel 采集

- **采用技术**：`AtomicReference<TokenUsage>` side-channel + provider 末 chunk usage 回灌 + `doOnComplete` 扣减
- **一句话原理**：流式输出（SSE）的 token 用量在最后一个 chunk 才回，用一个外层变量接住，不改动 Flux 流本身。
- **大白话案例**：像水管末端的流量计——水流（SSE 流）不变，只在出口装个表（AtomicReference）记总流量。
> 📌 批注：不改 Flux，SSE 输出字节级回归（13 调用方零回归）。

### 6. 视频/图片独立计费 — `MediaBillingService`

- **采用技术**：视频按 token（Ark 回 usage）或按秒（admin 切）；图片按张（stub 预留）
- **一句话原理**：视频/图片积分**独立计，不与文本 token 加总**——像素换算≠分词，混算没意义。
> 📌 批注：见 SeedDance feature-map 踩坑批注。图片无真 provider，价表+wallet 分支 stub，上线再接。媒体扣减以 taskId 锚 `uq_ledger_ref`，重复扣被唯一约束拦 = 恰好一次生效（S1 起单列 INFO 日志，不与真失败混淆）。

### 7. 幂等防重放 + 每日对账（S1 · SEC-FR-121/123）

- **采用技术**：`runIdempotent` 骨架——`idempotency_keys` 占位（ON CONFLICT DO NOTHING）与业务写同事务，撞键核 user_id+scope 后返首次结果；`BillingReconcileService` 每日 FULL OUTER JOIN 对「余额 vs Σ流水」
- **一句话原理**：重试/双击/网络重发同键只办一次；账目每天自动对一遍，不平只告警绝不自动修（修账必须人工，对账程序自身不能成为篡改面）。
- **大白话案例**：幂等像快递取件码——同码只放一次件，再来人出示同码只给看「已取」回执；对账像银行日终轧账，轧不平立刻喊人，柜员系统自己绝不动账。
> 📌 批注：占位与业务写同 @Transactional，失败整体回滚不留死键（拒扣后同键可重试）；同键不同金额/跨用户撞键 = 疑似重放，写 `idempotency_conflict` 审计。

### 8. 价表导出/导入/模板（7x-2）— `PricingConfigService` + `PricingConfigController`

- **采用技术**：镜像 LLM 供应商 export/import 那套（DTO + upsert + 200 上限 + 逐行容错）
- **一句话原理**：导出全量价表为 JSON 备份；模板联动 `availablePricingModels()`（已排除已配置模型）预填未配置行；导入按 `(providerId+model+kind+hasReference)` upsert——存在覆盖价格刷新 effective_from，不存在新建。
- **大白话案例**：像 Excel 导入导出——先下载空白模板（已帮你列好要填的行），填完价格上传，系统自动覆盖/新建。
> 📌 批注：价表无加密（与供应商含明文 Key 不同），导出无需二次确认；导入逐行校验复用 `validatePricingRule` + provider/model/category 复核，非法行进 errors 不中断整体；无内存缓存需 reload。

### 9. 视频参考定价维度（7x-3）— `pricing_rule.has_reference` + `PricingService` fallback

- **采用技术**：`has_reference BOOLEAN NOT NULL DEFAULT FALSE`（V95）+ 查询谓词 + fallback 编排
- **一句话原理**：VIDEO 同模型可配两行（false=无参考价/兜底，true=有参考价）；计费时按任务实际是否带参考视频命中对应行，查不到精确行回退 false 行。
- **大白话案例**：像快递加急费——普通件（无参考）一个价，加急件（带参考视频）另一个价；只配普通价时加急件也按普通价收（fallback）。
> 📌 批注：worker 算 hasReference **必须**从 `request.getAttachments()` 的 `kind=="video"` 判，不用 taskType（IMAGE2VIDEO 被重载不可靠）；**首尾帧参考图（kind=="image"）不算参考视频**；只配 true 不配 false 时无参考任务会 fallback 失败报「无价表」。

### 10. 视频分辨率定价（7x-1，V152）— `pricing_rule.resolution`
- **采用技术**：V152 加 `resolution VARCHAR(16)`（仅 VIDEO SECOND 行；NULL=通用兜底行）+ 询价命中链「精确(参考面,分辨率) → (参考面,通用) → (无参考,分辨率) → (无参考,通用)」
- **一句话原理**：SECOND 模式不同分辨率成本不同（1080p≠720p），同模型同参考面可按分辨率各配一行秒价；未单列的分辨率回落「通用」行，admin 只配一行通用也能全覆盖。
- **大白话案例**：像打印分档收费——A4/A3 各一个价，没列的纸型按「标准价」（通用行）收。
> 📌 批注：resolution 归一化 **trim+小写**（4K→4k），配置侧与任务侧同函数（`PricingService.normalizeResolution`）防失配；resolution 是**身份字段**（编辑不可改，改分辨率=新增行）；TOKEN 模式不按分辨率计价，行 resolution 恒 NULL。

### 11. 媒体消耗前预估管控（7x-2，V152/V153）— `estimateVideoYuan` + 提交预检
- **采用技术**：`PricingService.estimateVideoYuan`（SECOND=分辨率秒价×时长；TOKEN=`est_per_resolution` JSONB 按任务分辨率取值、general 兜底×时长，V153 改一行多参数，仅估价不参与扣费）；`MediaGenTaskService.submit/submitImage` **个人钱包路径**加「余额 ≥ 预估消耗」拦截（组池/成员限额路径计划5 已有）
- **一句话原理**：提交生成前先用价表估一笔，余额不够直接拒（task 不建），防「100 积分提交 2000 积分任务」跑到结算才发现。
- **大白话案例**：像加油站先预估「这箱油约 300 块」，卡里只有 100 就不让加，而不是加完再追债。
> 📌 批注：估价=0（无价表 / TOKEN 未配预估秒价）**不拦截**（口径保守，同计划5 坑表）；真实扣费仍按 Ark 返值走 chargeMedia 原链；`est_yuan_per_second` 只在 TOKEN 行有意义，SECOND 行配了即 400（SECOND 估价直接用秒价）。

### 12. 视频任务审查标志（7x-4）— `MediaTaskVO.hasReference` 计算字段

- **采用技术**：VO 计算字段（不入库，按 inputAttachments 实时算）+ 推送参数已脱敏落库复用
- **一句话原理**：任务详情/历史/画布明确标注是否有参考视频；推送参数（submittedRequest + providerRequestSnapshot）已存在，Canvas 只需保留字段 + 接面板，**无需新 DB 列**。
- **大白话案例**：像快递单上的「加急」标签 + 内含物清单——标签一眼区分，清单可查实际寄了什么。
> 📌 批注：hasReference 与定价维度 has_reference 是不同字段——前者是任务侧实时算（供展示/审查），后者是价表行配置维度（供计费命中）。两者口径一致（都按 kind=="video" 判）。

## 数据库表与 SQL 注解（Flyway）

### 迁移脚本：`V65__billing_wallet.sql`
- **作用**：建钱包余额 + 流水 + 充值订单 + 审计日志 4 表。
> 📌 批注：spec 写的 V63/V64 已被资产库占用，实际落地为 V65/V66。

### 迁移脚本：`V66__billing_pricing.sql`
- **作用**：建模型价表 + 阶梯比例表（admin 配）。

#### 表：`user_points_balance`（积分余额）
> 大白话：**一行 = 一个用户的积分钱包**。**S1 起不可负**（V80 CHECK 硬约束：透支笔直接拒扣，欠款模型作废）。

| 字段 | 类型 | 用途 | 约束 |
|---|---|---|---|
| user_id | BIGINT | 谁 | UNIQUE、非空 |
| balance_points | DECIMAL(14,2) | 当前积分 | 非空，默认 0，**CHECK ≥0（V80）** |

#### 表：`points_ledger`（积分流水）
> 大白话：**一行 = 一笔积分变动**（充值/消耗/退款/admin grant）。append-only，对账用。**S1 加 `uq_ledger_ref(ref_type,ref_id,type)` 唯一约束 = 同一来源同类型只记一笔（恰好一次）**；生产 agent_app 账号被 REVOKE UPDATE/DELETE 物理拒改。

| 字段 | 类型 | 用途 | 约束 |
|---|---|---|---|
| user_id | BIGINT | 谁 | 索引 |
| type | VARCHAR(16) | RECHARGE/CONSUME/REFUND/ADMIN_GRANT | 非空 |
| delta_points | DECIMAL(14,2) | 变动量（扣为负） | 非空 |
| balance_after | DECIMAL(14,2) | 当时余额（可重建对账） | 非空 |
| ref_type/ref_id | | CHAT/EMBED/VIDEO/IMAGE/PAYMENT/ADMIN + 关联 id | V80 起 (ref_type,ref_id,type) 唯一 |
| remark | VARCHAR(256) | 备注（admin 充值可填，空走默认文案） | 可空 |

#### 表：`idempotency_keys`（幂等键，S1 · V80）
> 大白话：**一行 = 一把「只办一次」的钥匙**。充值/扣减带键来，先 `INSERT ... ON CONFLICT DO NOTHING` 占位——占上了才办正事，撞键回查首次流水原样返回结果。

| 字段 | 类型 | 用途 | 约束 |
|---|---|---|---|
| idem_key | VARCHAR(128) | 幂等键（调用方生成） | **全局 UNIQUE** |
| user_id / scope | | 键属谁 / 属哪个业务域（billing.grant/billing.charge） | 撞键时核验身份用 |
| result_ref | VARCHAR(64) | 首次业务流水 id（回填） | 撞键返首次结果的指针 |

> 📌 批注（Phase4 交叉审查实证）：撞键**必须先核 user_id+scope 再返结果**——键全局唯一，不核则跨用户同键会回返他人 balance_after（余额泄露信道）。核验不过 = 疑似重放，审计 + CONFLICT。

#### 表：`pricing_rule`（模型价表）
> 大白话：**一行 = 一个模型形态的单价配置**。admin 配，按 effective_from 取调用时价。

| 字段 | 类型 | 用途 |
|---|---|---|
| kind | VARCHAR(8) | CHAT/EMBED/IMAGE/VIDEO |
| model | VARCHAR(128) | 模型名 |
| price_input/output_per_million | DECIMAL | 文本每 1M token input/output 价（¥） |
| video_billing_mode | VARCHAR(8) | TOKEN/SECOND（视频） |
| price_per_second / price_per_image | DECIMAL | 视频按秒 / 图片按张 |
| resolution | VARCHAR(16) | 7x-1（V152）：VIDEO SECOND 分辨率价行（480p/720p/1080p/4k；NULL=通用兜底） |
| est_per_resolution | JSONB | 7x-2（V153）：VIDEO TOKEN 提交期预估秒价 {general/480p/720p/1080p/4k: ¥/秒}，general 兜底（仅预检，不计费） |
| effective_from | TIMESTAMPTZ | 生效时间（查调用时价） |

#### 表：`points_ratio_tier`（阶梯比例）
> 大白话：**一行 = 一个 ¥ 区间换多少积分**。充值与消耗共用。

| 字段 | 类型 | 用途 |
|---|---|---|
| min_amount/max_amount | DECIMAL | ¥ 区间（max null=∞） |
| ratio | DECIMAL(10,4) | 每 ¥ 换多少积分 |

#### 表：`llm_usage_logs`（审计日志）
> 大白话：**一行 = 一次模型调用的真实消耗记录**。admin 看真 token+¥+积分。

| 字段 | 类型 | 用途 |
|---|---|---|
| kind | VARCHAR(8) | CHAT/EMBED/IMAGE/VIDEO |
| tokens_input/output | INT | 真 token 数 |
| cost_yuan / points_consumed | DECIMAL | 当时算的 ¥ 和积分 |
| status | VARCHAR(16) | SUCCESS/ESTIMATED/FAILED |

#### 表关联
- **`points_ledger.user_id` → `user_points_balance.user_id`（一对一逻辑）**：一个用户一个钱包，多笔流水。
  > 像「银行账户 ↔ 交易明细」。
- **`payment_order.user_id` → `user_points_balance.user_id`**：充值订单关联用户；V140 起自助支付状态机（PENDING→PAID/FAILED/CLOSED），channel=ADMIN 直 PAID 仍兼容。
> 📌 批注：余额/流水/日志皆 append-only 或单行 update，**不加软删四字段**，靠 created_at + 分区归档。

## 自助充值支付（7x#3 / 20x#1，V140，2026-08-21）

### 代码位置
| 文件 | 作用 |
|---|---|
| `billing/service/PaymentOrderService.java` | 编排：下单（金额校验/idemKey 查重/比例快照/过期时间）→ handleNotify（验签→查单→金额复核→抢态→同事务入账）→ 取消/过期批关 |
| `billing/service/channel/PaymentChannelService.java` | 渠道 SPI：channel/available/precreate/verifyAndParse 四方法 |
| `billing/service/channel/PaymentChannelRouter.java` | 路由：routeForCreate（未开通 400）/ route（回调不限可用性——渠道被禁后回调仍要可达） |
| `billing/service/channel/MockPaymentChannel.java` | mock 通道：HMAC-SHA256 签名+过期防重放+常量时间比较；`buildSignedNotify` 服务端造签（密钥不出服务端） |
| `billing/service/channel/AlipayPaymentChannel.java` / `WechatPaymentChannel.java` | 真实渠道骨架（available()=false，接入步骤见 javadoc） |
| `billing/config/PaymentConfigGuard.java` | prod profile + mock-enabled 同开 → 启动即拒 |
| `billing/controller/PaymentController.java` | 用户端点 /api/billing/payment/**（下单限流 10/60s；mock/trigger 仅 mock-enabled 否则 404） |
| `billing/controller/PaymentNotifyController.java` | 回调端点 /api/billing/payment/notify/{channel}（匿名放行 + IP 限流 120/60s） |
| `billing/job/PaymentExpireJob.java` | 过期关单 job（默认 60s 一批 200，逐行条件 UPDATE） |
| `billing/service/BillingReconcileService.java` | paymentAnomalies() 三节：PENDING 超 10min / PAID 无流水 / 终态后仍付款 |
| `BillingController` /admin/recharges + /admin/user-balances | 20x#1 admin 记录+余额视图（usage:view） |
| 前端 `components/billing/RechargeDialog.vue` | 充值对话框：表单（idemKey=会话 UUID）→收银台→2s×30 轮询 |
| 前端 `MyWalletView.vue` / `BillingAdminView.vue` | 充值按钮+我的记录卡；admin 充值记录+用户余额两 tab |

### 关键链路
```
下单: 前端(会话UUID idemKey) → POST /payment/orders → 金额校验 → idemKey 查重(同键同金额返原单/异金额409)
     → ratioService.toPoints 快照 points_granted → PENDING(expire_at=+30min) → precreate → channelOrderId 落库
回调: notify/{channel} → 验签(HMAC) → selectByChannelOrder → 金额复核(不符→FAILED+安全事件)
     → markPaidIfPending 抢态(0行=终态: PAID=重推幂等ack / CLOSED·FAILED=安全事件+对账异常人工补单)
     → 同事务 walletService.creditRechargeForOrder(余额+RECHARGE流水, uq_ledger_ref 第三道兜底)
过期: PaymentExpireJob → closeIfPending 逐行抢态（与回调互斥）
```

### 原理大白话
- **状态机全部条件 UPDATE**（`WHERE status='PENDING'`）：像「抢红包只认第一个点的人」——回调重推、过期 job、用户取消三方抢同一单，数据库只让一方赢，天然防双重入账。
- **积分快照**：下单时按当时比例算好 points_granted 落单，回调只吃快照——像「点菜小票写死价格，涨价不影响已下单」。
- **mock 通道服务端造签**：测试环境用真密钥造真签名走真回调链路——像「演习用真枪空包弹」，测的不是影子路径。
- **admin 余额视图**：users LEFT JOIN 钱包 LEFT JOIN PAID 聚合，无钱包/无充值显 0；合计卡与明细同一 JOIN 结构，保证「卡=明细Σ」；排序列白名单（balance/rechargePoints/rechargeAmount）防 SQL 注入。

### V140 关键 SQL
- `uk_payment_channel_order(channel, channel_order_id) WHERE channel_order_id IS NOT NULL`——渠道单号幂等第一道（部分索引兼容 ADMIN 直 PAID 无渠道单号）。
- `uk_payment_idem(user_id, idem_key) WHERE idem_key IS NOT NULL`——前端幂等键索引兜底。
- `idx_payment_pending_expire(status, expire_at) WHERE status='PENDING'`——过期 job 批扫只扫 PENDING。
- chk_pay_status 重加含 CLOSED；chk_pay_channel 含 MOCK；V93 笔误 REVOKE 修复（guard 包裹）。
> 回滚块见 V140 文件头注释。

### 支付渠道网页配置（7x 追加，V143/V144，2026-08-21）
| 文件 | 作用 |
|---|---|
| `billing/service/PaymentChannelConfigService.java` | 配置管理：listMasked（读只出 tails 脱敏尾巴，永不解密出库）/ save（merge 语义=留空保持原值 + 渠道码/字段键双白名单 + 必填全集校验 + AES 整体加密落库）/ getDecrypted（DB 优先 env 兜底——SDK 接入桥） |
| `billing/controller/PaymentChannelAdminController.java` | GET/PUT /api/billing/admin/payment-channels（payment:config + 写操作 @AuditLog） |
| `billing/entity|mapper/PaymentChannelConfig*` | V143 表（channel CHECK 白名单 + 部分唯一索引） |
| 前端 `views/admin/PaymentChannelConfigView.vue` | 双卡片表单：placeholder 显 ****尾4、留空=不变、保存二次确认；alert 明示「配置齐全≠渠道上线」 |
| 前端 `api/billing.ts` PAYMENT_CHANNEL_FIELDS | 渠道表单字段真相源（与后端 REQUIRED_KEYS 对齐） |

**原理大白话**：密钥像「保险柜里的印章」——存进去就加密（AES，同 LLM Key 管线），网页上永远只能看到尾巴 4 位（像银行卡号 `****1234`）；改一个字段不用重填全部（留空=保持原值）。**填好配置 ≠ 渠道上线**：渠道类 `available()` 仍恒 false，SDK 下单/验签实现落地时才翻转——用户侧不会被暴露到半吊子渠道。


## 相关文档

- 规格：[../specs/积分计费系统.md](../specs/积分计费系统.md)
- 实现计划：[../plans/积分计费系统.plan.md](../plans/积分计费系统.plan.md)
- 开发进度：[../../开发进度/积分计费系统/](../../开发进度/积分计费系统/)
- **用户操作手册：[../user-ops/积分计费系统用户操作手册.md](../user-ops/积分计费系统用户操作手册.md)**

## 变更记录

| 日期 | 变更 | 原因 |
|---|---|---|
| 2026-08-09 | 建立 feature-map（补 Phase3 文档缺口） | 计费系统原仅 spec/plan/进度，缺 feature-map |
| 2026-08-10 | 补 S1 计费安全增量：V80（CHECK非负/idempotency_keys/uq_ledger_ref/REVOKE DO块）+ SQL 守卫单语句拒扣 + runIdempotent 幂等骨架 + BillingReconcileService 对账；修正过时描述（「可负欠款」模型 S1 起作废、SELECT FOR UPDATE 改单语句守卫） | 安全体系 S1 Phase4 验收通过，文档对齐实际实现 |
| 2026-08-11 | **7x 问题修复**：① 价表500修复（LlmProviderMapper deleted=0，INTEGER列不能用=false）；② 价表导出/导入/模板（镜像供应商那套，upsert）；③ VIDEO has_reference 定价维度（V95，fallback到false行）；④ 视频任务 hasReference 审查标志（MediaTaskVO 计算字段，首尾帧图不算参考视频）；⑤ Canvas 审计面板接入（推送参数已脱敏落库无需新列） | 7x 积分系统人工测试问题 + 用户追加审查需求 |
| 2026-08-25 | **7x 未解决 #1/#2 修复**：① V152 `pricing_rule.resolution`——VIDEO SECOND 按（分辨率×有无参考）分行配秒价，命中链「精确→通用→无参考」，resolution 归一化小写、属身份字段；TOKEN 模式表单改显 token 输入价；② V152 `est_yuan_per_second` + `PricingService.estimateVideoYuan`——视频/图片提交期估价接分辨率价，**个人钱包路径加「余额≥预估消耗」拦截**（组池路径计划5 已有），估价 0 不拦（保守口径） | 用户反馈「SECOND 只能填一个秒价/无分辨率分档」「缺消耗前管控，100 积分能提交 2000 积分任务」 |
| 2026-08-14 | **7x-6 修复**：新增价表候选对 VIDEO 不再「配了一面就整条消失」——has_reference 两面都配齐才排除，只配一面时候选保留并带 hint 提示；前端切候选复位 hasReference、编辑模式身份开关 disabled；AvailablePricingModelVO 新增 hint 字段 | 用户反馈「视频模型只能配有参考或单独配无参考」，无入口补第二面价行 |
| 2026-08-14 | **价表删除**（用户追加）：DELETE /billing/pricing/{id}（@AuditLog pricing_delete，物理删同阶梯范式）；前端操作列删除按钮+二次确认（提示删后唯一价行会「价表缺失」）；历史账单不受影响（扣费瞬间已落账）。单测 32/32（+2） | 价表配错行无清理入口（原设计只有阶梯可删） |
| 2026-08-20 | **7x-5/6 统一入口**：参与项目选择从五处页内选择器收拢为页顶 AppHeader 唯一入口（`stores/projectGroup.ts` 单键唯一真相 + adoptLegacy 收养遗留 + 组池/个人积分徽标）；详见[项目组与积分划拨.feature-map.md](项目组与积分划拨.feature-map.md) | 五处选择互不同步易漏设；用户要求统一控制并页顶展示积分 |
| 2026-08-21 | **7x#3/20x#1 自助充值支付 + 账单总览**：V140 支付状态机（PENDING→PAID/FAILED/CLOSED 全条件 UPDATE 抢态）+ MOCK 通道（HMAC 签名/过期防重放/服务端造签走真链路）+ 回调端点（匿名+IP限流+安全事件）+ 过期 job + 对账三节异常 + 充值记录六字段（用户/付款账号/金额/积分快照/充值后余额/累计）+ admin 充值记录/用户余额两视图（筛选联动Σ+全平台合计卡+排序白名单）；前端 RechargeDialog（idemKey 会话 UUID + 2s×30 轮询）+ MyWallet 充值/记录 + BillingAdmin 两 tab | 7x 未解决两条（充值记录字段/支付宝微信接入）+ 20x#1（全平台余额视图）规格化落地；真实商户按 Alipay/Wechat 骨架 javadoc 后接 |
| 2026-08-25 | **7x 预扣+多退少补（V155，`40dc32e5`）**：① 媒体任务提交即按预估积分预扣（个人 `kind-HOLD` CONSUME 腿/组池 `chargeGroup` 预占，预扣失败删任务行拒绝——防多任务并行「预检都过、结算不够扣」），`media_gen_tasks.hold_applied` 区分存量在途任务（误退防护）；② 完工 `settleMediaSuccess` 多退少补（实耗>预扣补差、组池尽转 BACKSTOP 扣组长、<退差、=不动；补扣失败记 FAILED usage 平台可见缺口+返预扣额供 unwind）；流水槽位各占 `uq_ledger_ref` 一格（HOLD CONSUME/kind CONSUME/kind REFUND/HOLD REFUND）+幂等键 `media-hold-/settle-/-refund-{taskId}`；失败终态（markFailed/markDownloadFailed）钩子全额退预扣（与落库失败撤销撞键幂等不双退）；③ GET /api/media/estimate 预估预览（估积分+余额+affordable）+视频/图片生成页防抖 400ms「预估消耗 X 积分」+余额不足红字；④ 价表 IMAGE 行补「预估=张价×张数」说明。附 V151 `uq_ledger_ref` 部分唯一索引（上台漏提交，本链依赖） | 7x 未解决两条（图片无预估价/先判余额+预扣多退少补） |
| 2026-08-21 | **7x 追加·支付渠道网页配置**：V143 payment_channel_config（AES 整体加密落库+config_tails 脱敏回显）+ V144 payment:config 权限码；admin「支付渠道」页自填支付宝/微信密钥（merge 留空保持原值）；getDecrypted DB 优先 env 兜底为 SDK 接入桥；渠道 available() 维持 false 防半配置暴露 | 用户拍板密钥网页自填替代纯 env（736ca763/7faa5127） |

## 2026-08-25 增补（7x 未解决 2 项）
- **http 环境充值修复**：crypto.randomUUID 仅安全上下文可用（http=undefined 崩充值链）→ utils/uuid.ts 原生优先+回落 v4，7 处调用点全切换；上 https 自动走回原生
- **admin 充值用户下拉**：手填 ID 改远端搜索下拉（昵称/姓名（账号））；新端点 GET /api/billing/admin/user-options（points:recharge；ACTIVE；模糊 username/name；限 20）
- 测试：uuid 3 用例；浏览器实测下拉显示

| 2026-08-25 | **7x 账单总览合计卡**：用户余额 tab 合计卡由「恒定全平台」改为**跟随用户名筛选**（platformBalanceTotals +keyword 参数，与明细同 WHERE 保证 Σ 一致；未筛选=全平台）；卡片口径标注「仅已支付充值单，管理员发放不计入」——解释用户看到的「金额 0 与积分不对应」（积分来自 admin 发放时本就不计入充值合计） | 7x 未解决（合计卡不随筛选+金额 0 疑惑） |
## 2026-08-25 增补（7x 预扣多退少补 V155）
- **媒体任务预扣**：提交视频/图片任务时按预估积分先预扣，完工按实际消耗多退少补（预估 0=无价表不扣不拦，与预检同口径）；组池任务同步预占组池+成员已用，成员 used 净额=实耗
- **预估预览**：视频/图片生成页按当前所选参数（模型/时长/分辨率/参考视频/张数）实时显示「预估消耗 X 积分」，余额/组池不足红字预警；新端点 GET /api/media/estimate（不落库不预扣）
- **失败全退**：任务失败/下载失败/落库失败三路径预扣全额退（幂等键互斥不双退）；存量在途任务（升级前提交）走原全量结算不受影响
- **图片预估价不加列**：图片预估=张价×张数派生，价表配置页 IMAGE 行补说明，不冗余配列
- 单测：MediaBillingServiceTest 26/26（+15）、MediaGenTaskWorker/TxService/Service 全绿
