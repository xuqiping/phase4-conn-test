# 已解决（8x，2026-08-11，commit 8fd646cf…5de75ec）
1. 所有动作根本不全，比如我图片生成最终是否生成成功，视频生成最终是否生成成功，智能对话我看都没进审计日志 ✅ —— 媒体生成成功/视频生成成功二次审计（submit↔success 按 taskId 关联）+ 图片生成成功二次审计 + chat 两阶段审计（send_message 意图 + chat_completed 带 token/积分快照）落地（Chunk3/4，commit 0a280e5/e6f90636/b69fed0）
2. 审计日志是给人看的吗？动作全给我改成中文，对应的模块，即模块+动作（如无限画布模块-生成图片图、图片生成模块-图片生成、积分模块-充值积分，视频剪辑模块-下载视频等） ✅ —— AuditLabelDictionary 显示层字典（13 模块+全动作码 code→中文），VO 富化 moduleLabel/actionLabel；**显示层字典不写 DB**，原码仍存（Chunk1，commit 8fd646cf）
3. 筛选中，模块根本不全，比如media媒体模块等都没有 ✅ —— 模块下拉补齐 13 中文项（含媒体生成/智能对话/无限画布），下拉与表格列均中文（Chunk2，commit a5719235）
4. 还要加上用户账号筛选 ✅ —— 账号模糊筛选 username likeRight 前缀匹配 + idx_audit_username 索引（Chunk2，commit a5719235 + V94）
5. 所有下载、上传、入库、出库、审核、撤销等动作都要进入日志啊 ✅ —— 8 端点补 @AuditLog（3 下载+5 上传+1 入库，download_video/upload_file/save_to_library 等，Chunk5，commit dc81bd4b）
6. 还要加个字段：用户IP，这样我可以轻松的知道用户每一次操作的时候所在的IP ✅ —— client_ip 列 + 任务行 IP 盖戳（worker 终态从任务行取，异步线程无请求 IP，Chunk2/3，commit a5719235/0a280e5 + V94）

> 补充需求（模型调用明细）一并落地：模型调用记模型/token/积分；审计↔账单调用明细 drill-down（chat→traceId / 媒体→taskId，V95 + 前端按钮预填，Chunk7，commit f708387e）。

# 已解决（2026-08-14，第二轮）
1. 用户字段"-" ✅ 三层修复：
   - 写入侧：`SessionService.newSession` 踢会话留痕显式盖戳 userId/username（登录请求无 JWT，fromMdc 取不到身份）；`AuthService.refresh` 失败分支不再写"-"占位
   - 查询侧：`AuditLogController.backfillUsernames()` 每页一次 `in` 查询回填缺失 username（worker/异步线程审计行），用户已删回退「用户#id」；历史双空行从 detailJson.username 恢复显示
   - 显示层：前端兜底 `username || 用户#id || 系统`，用户列不再出现"-"
2. 详情可读化 ✅ 新增共享组件 `DetailKvView.vue` + 字典 `utils/detailLabels.ts`（key→中文、枚举值→中文、布尔→是/否），详情弹窗改为键值中文渲染；未知 key 原样回落，DB 仍存原始码（显示层字典不写库）

> 冒烟：50 行 0 空用户名（chat_completed worker 行回填 admin；旧 session_kicked 行从 detail 恢复；新写入行已盖戳）

# 参考feature-map、user-ops
- agent-platform\workflow_output\docs\feature-map\日志系统.feature-map.md
- agent-platform\workflow_output\docs\user-ops\日志系统用户操作手册.md
>解决完后需要同步更新对应的feature-map和user-ops ✅ 已同步（feature-map 8x 增补节 / user-ops 子功能2+4 + 变更记录 2026-08-11）

# 实现详情
- 开发进度：`workflow_output/开发进度/日志系统/开发进度2-审计问题修复.md`
- 测试方案：`workflow_output/docs/测试方案/日志系统审计问题修复测试方案.md`
- Phase4 冒烟：`workflow_output/docs/测试方案/phase4_audit_smoke.mjs`（11/11 通过；TC3/TC5/TC7/TC8 真生成串联留人工复验）

# 已解决（2026-08-26 勾销，修复设计II §7，Plan B）
1. **邮件发送无日志** ✅ —— 注册/找回密码发码、短信、MFA 绑定全量进审计：detail 含完整邮箱+IP、结果 SUCCESS/FAIL；找回密码完整走一遍=两条日志（B1/B4 前置，`1e724fd5`）。
2. **英文模块描述** ✅ —— 后端字典 14→18 模块/271 动作码全中文，前端下拉+表格标签+详情三方对齐，幽灵码删除；字典完整性单测锁死（改码必须四改同步防再分叉）（B2/B3，`a740d51a`/`b7abd439`）。
3. **全操作覆盖检查** ✅ —— P1/P2/P3 全 Controller 扫描补 `@AuditLog` 共 96 个 + 双向窗口 sweep 复核（39 处免审逐条落理由）；已知免审=读操作/高频心跳/公开健康检查，清单落 plan B 验证收口（B4-B7，`93c4e3c5`…`aff6435`）。