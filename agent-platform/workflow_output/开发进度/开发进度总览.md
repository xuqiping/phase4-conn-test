# 开发进度总览

> Phase 3 维护。**每一轮对话结束时**更新本文件。
> 全局进度入口：汇总功能状态、沉淀规范、下一步。
> **硬规则**：文档超 5000 tokens 必须打桩拆分到子目录 + `总路由.md` 索引。
> 本项目历史进度见既有 `项目工程文档/项目开发进度/` + 各 `速查表/*-更新进度.md`，本文件为 SDD 入口。

## 当前阶段
平台 MVP（F1–F11）已全部落地（速查表01-23 绿），进入迭代打磨 + 新功能阶段（HUMAN_INPUT Phase1/2、移动端适配、RAG 图片文件等已落未提交）。

## 功能进度汇总（SDD 视角）
| 功能 | 状态 | 进度文件 | 备注 |
|---|---|---|---|
| 认证登录 F1 | ✅ 完成 | 既有 `速查表/01` + `计划1` | JWT 双 Token |
| RBAC F2 | ✅ 完成 | 既有 `速查表/03` + `计划9` | 4角色×15权限 |
| Agent 管理 F3-F5 | ✅ 完成 | 既有 `速查表/05-07` | |
| 工作流编辑器 F6-F7 | ✅ 完成 | 既有 `速查表/10` | |
| 执行引擎 F8-F9 | ✅ 完成 | 既有 `速查表/11-13` | Java + sidecar |
| 多模态对话 F10 | ✅ 完成 | 既有 `速查表/08` | WS→SSE→REST |
| LLM 供应商 F11 | ✅ 完成 | 既有 `速查表/18-19` | AES 加密 |
| 暗色主题 F12 | ✅ 完成 | 既有 `速查表/04` | 3 套 |
| HUMAN_INPUT F18 | 🚧 进行中 | 既有 `速查表/` + 自动记忆 | Phase1 落地+编辑器补齐+Phase2，未提交 |
| 企业级 RAG F16 | ✅ 完成 | 既有 `速查表/14-17` | 图片文件 P1-P3，未提交 |
| 移动端适配 | ✅ 完成 | 既有 `速查表/23` | 未提交 |
| ~~个人记忆 M4 scope 语义澄清~~ | ~~✅ 完成~~ ❌被计划12取代 | [M4-scope语义澄清/](开发进度/M4-scope语义澄清/) | **基旧 `user_memories` 模型,计划12 推进则随 V49 DROP 作废**。原:纯前端,底栏写/读分组+预览开关(c6fe1d6a)+底栏全下拉化(91a178e4) |
| ~~个人记忆 M2 时间线记忆~~ | ~~🚧 冒烟 8/10 绿~~ ❌被计划12取代 | [M2-时间线记忆/](开发进度/M2-时间线记忆/) | **基旧 `user_memories` 模型,计划12 推进则随 V49 DROP 作废**。原:value 标量+日期段+temporal 标+KEEP_CUSTOM 合并+daily_log 时序;chunk0-6 ✅,T6/T7 ⬜ |
| 对话联网搜索 | ✅ 完成,Step1-7 全落,待人工部署 SearXNG | [联网搜索/](开发进度/联网搜索/) | 手动开关触发;双引擎(外部可插拔 Tavily/Serper/Bing + 自建 SearXNG+Jsoup+SSRF 对标 Tavily);复用 CITATION 回显(加 url 字段,前端累积合并);仅 CHAT 模式;Step1-7 全提交(Step4 路由降级链+Step5 接 CHAT+V44+Step6 前端开关+Step7 后台配置页);33单测绿,待人工部署 SearXNG/Tavily key 真连通验证 |
| ~~个人记忆 M3 设置动态化~~ | ~~✅ 完成~~ ❌被计划12取代 | [M3-RAG记忆设置动态化/](开发进度/M3-RAG记忆设置动态化/) | **基旧 `user_memories` 模型,计划12 推进则随 V49 DROP 作废**。原:entities 词袋计数 7 字段可配+Judge prompt token 化+V45 seed;前端 4 模式 v-if 显隐 |
| ~~个人记忆 回答记忆(origin 区分)~~ | ~~⏸ 计划已审~~ ❌被计划12取代作废 | [回答记忆origin区分.plan.md](../docs/plans/回答记忆origin区分.plan.md) | **基旧 `user_memories` 加列方案,计划12 推进则此计划直接作废(不落地)**。原:记忆分 USER/ANSWER+加 `memory_origin` 列+召回开关三态 |
| Token 消耗统计(账单) | ⏸ 规格已定+计划已审,待许可进 Phase3 | [TokenUsage统计.plan.md](../docs/plans/TokenUsage统计.plan.md) + [TokenUsage统计.md](../docs/specs/TokenUsage统计.md) | 统计全平台 LLM token 消耗;`llm_usage_logs` append-only(provider_id+provider_scope 区分全局/用户私有 key);Gateway 单一出口异步采集(同步/流式/embed 三路);流式 usage 走 side-channel 不改 Flux(OpenAI include_usage+Claude message_delta);embed 兜底 TokenEstimator;独立 usageTaskExecutor 攒批写;admin 看全部/用户 ownership 看自己;MVP 只记 token 不折成本(cost nullable,P1 价表后做)。**最高风险=流式采集不改 SSE 输出** |
| **计划12 个人记忆重设计** | 🚧 Phase3 进行中·**迭代F/I4 前端本体第一+二批已落(10组件+ChatView badge+底栏scope popover, 7页签壳, vue-tsc+build+99单测全绿)** ‖ **⚠️F-4b 生命周期折叠板阻塞：后端缺 departed/deleted 项目 list+copy-to/restore 端点(C/E 收尾遗漏,设计§3.6/§3.8规了未实现),待补后端 chunk 解锁** ‖ 迭代F前置 后端读侧端点补齐已落(通知轮询/gen矩阵/总结列表/流水账列表 4 controller,25IT实跑绿) ‖ 迭代I2/I3 ACL配置端点+召回/总结取数接ACL+离职开关已落(roster+recall-acl端点+权边界+审计+DEPARTED保交接L10双侧过滤,45单测+14IT实跑绿) ‖ 迭代E 总结+冲突已落(周期总结worker+DISCARD连带软删turns+12h拒+SKIP LOCKED,59单测+17IT实跑绿) ‖ 迭代D 召回pipeline已落(76单测+5IT) ‖ 迭代C 生成写入已落(54单测+6IT) ‖ 迭代I1 ACL前置+迭代B 标签归一+迭代A 数据层 已落(IT实跑绿) | [计划12-个人记忆重设计-实现开发计划.md](../../项目工程文档/计划/计划12-个人记忆重设计-实现开发计划.md)(主索引) + [计划12-A..H-*.md](../../项目工程文档/计划/)(11 子 plan) + [开发进度1.md](开发进度/计划12-个人记忆重设计/开发进度1.md)~[开发进度10.md](开发进度/计划12-个人记忆重设计/开发进度10.md) | **完全替换** `user_memories`+key归一+冲突机模型;9新表+1扩列(V47)+`memory_conflicts`放宽(V48补丁)+`memory_recall_acl`(V49,I1)+`memory_recall_scope_prefs`(V50,D-7)+`memory_consolidation_scopes`锁列(V51,E);9迭代 A→B✅→I1✅→C✅→D✅→E✅→I2/I3✅→F前置✅→**F/I4(第一+二批✅, F-4b⏳待后端)**→H(G迁移取消)。**迭代A+B+I1+C+D+E+I2/I3+F前置+F/I4前端第一+二批已落(M2/M3/M4/回答记忆origin区分随 old_jiyi 备份分支 a1163c53 保存)** |

> 状态：⬜ 未开始 / 🚧 进行中 / ✅ 完成 / ⏸ 暂停 / ⏸ 计划已审待许可

## 沉淀的通用规范
| 日期 | 沉淀内容 | 写入位置 | 触发功能 |
|---|---|---|---|
| 2026-07-17 | SDD 脚手架 + 两套文档体系说明 | [项目规范约束/AGENTS.md](../项目规范约束/AGENTS.md) | 改造 |
| 2026-07-17 | 向量用 halfvec(2048) 非 vector(2048) | [项目规范约束/通用约束.md](../项目规范约束/通用约束.md) | RAG |
| 2026-07-17 | 冲突快照须带全字段(keyZh/entities/anchor),Map.of 拒 null 用 HashMap | [开发进度1.md](开发进度1.md) + 速查表09演进 | 个人记忆 V38 bug |
| 2026-07-18 | scope 写/读选器须视觉分组(避免 home vs 可见性混淆);预览 custom 须显式总记忆开关默认关 | [M4-scope语义澄清/](开发进度/M4-scope语义澄清/) + 速查表09 M4 | 个人记忆 M4 |
| 2026-07-18 | merge 分隔符是中文逗号`，`(非 ASCII);KEEP_BOTH merge 已含 updated_at=now();value schema 标量+分号段+ISO 日期前缀对存量无损 | [M2-时间线记忆/](开发进度/M2-时间线记忆/) + 速查表09 M2 | 个人记忆 M2 |

## 下一步开发安排
- **计划12 个人记忆重设计**：🚧 Phase3 进行中·**迭代 F/I4 前端第一+二批已落地**（[开发进度9.md](开发进度/计划12-个人记忆重设计/开发进度9.md) + [开发进度10.md](开发进度/计划12-个人记忆重设计/开发进度10.md)）。**第二批**（commit 465f48c5，[开发进度10.md](开发进度/计划12-个人记忆重设计/开发进度10.md)）：F-6 `MemoryRecallScopePopover`（ChatView 底栏新栈召回 scope：个人开关+项目多选+方向+时间窗+离职开关，debounce PUT，双栈期与 legacy 并存到 H 收尾）+ I4-3 `MemoryConsolidationPeoplePicker`（总结弹框 PROJECT 行取数选人 SELF/SPECIFIC/ALL+方向+离职禁；扩 `MemoryConsolidationScopeRequest` 类型；Dialog 提交带取数配置）+ F-7 移动 390px 扫荡（3 弹窗 `width:90vw` + ACL 选择器 `min(240px,60vw)`）。vue-tsc/build/99 单测全绿。**第一批**见 [开发进度9.md](开发进度/计划12-个人记忆重设计/开发进度9.md)（8 组件+7 页签壳）。**⚠️下一步阻塞优先**：**后端 chunk 补 departed/deleted 项目 4 端点**（`GET /departed-projects` + `POST /departed-projects/{id}/copy-to` + `GET /deleted-projects` + `POST /deleted-projects/{id}/restore` + service `copyDepartedProjectTo` + mapper + IT）→ 解锁 **F-4b MemoryLifecyclePanel**（已删/已离开项目折叠板 restore/copy-to + 波及 badge）。而后 **H 收尾**（下一空号 V52 DROP 旧表 user_memories/user_memory_projects + 旧端点 `/api/chat/memories/*` 404 + 删 legacy 读控件/legacy pane + 速查表09 重写）。Phase4 E2E 留：真 LLM 日期铁律幻觉 + 召回全链 LLM IT + departedAuthorNotes 端到端 + 双节点并发 SKIP LOCKED + 联动 L1-L12 边界（反向/半选/批量）+ 15 安全向量越权 IT。旧设计备份分支 `agent-platform-old_jiyi`(a1163c53)。
- ~~**计划12 待许可进 Phase3**~~：已许可，A→E 已推进（见上）。
- **Token 消耗统计(账单)**：规格+plan 已审([TokenUsage统计.plan.md](../docs/plans/TokenUsage统计.plan.md))，**待用户许可进 Phase3**。11 步 7 chunk(建表→provider id/scope 透传→流式 usage side-channel→embed usage→gateway 异步攒批采集→查询 API→前端页)。最高风险=流式采集不改 SSE Flux,Step5/6 必配字节回归。migration V48(与回答记忆 V47 顺序,谁先落谁占)。
- ~~**个人记忆 回答记忆 origin 区分**~~：~~plan 已审，待用户许可进 Phase3~~ **❌被计划12取代作废,不推进**。
- **对话联网搜索收尾（人工依赖）**：部署 SearXNG Docker（`formats:[json]`）+ base_url 写 `search.searxng.base-url` → 跑真连通冒烟；申请 Tavily key 真连通验证；Phase4 关键路径 E2E（开关 ON 问时效问题答对 + CITATION 回显可点 + 降级链：删 Tavily key→降 BuiltIn→删 SearXNG URL→降纯聊天不崩）。
- 提交未入库的 HUMAN_INPUT / RAG图片文件 / 移动端适配 变更（恢复点见自动记忆）。
- 提交 V38 个人记忆冲突丢字段修复（[开发进度1.md](开发进度1.md)，Phase4 验证后）。
- 补 [user-ops/](../docs/user-ops/) B/C 类功能操作手册。
- 验证 V12–V44 Flyway 迁移全部入库。

## 子目录与文件拆分约定
```
workflow_output/开发进度/
├── 开发进度总览.md            ← 本文件
├── _模板.开发进度n.md          ← 每轮对话记录模板
├── _模板.总路由.md             ← 超 5000 tokens 时索引
├── _模板功能README.md          ← 功能完成 README 模板
└── <功能名>/                  ← 每功能一目录（新功能开发时建）
```

### 单文件 5000 tokens 限制规则
接近 4000 预警；超限拆 `开发进度n.md` 或建 `总路由.md`。

## 变更记录
| 日期 | 变更 | 原因 |
|---|---|---|
| 2026-07-17 | 建立 SDD 进度总览，映射既有功能状态 | SDD 改造 |
| 2026-07-19 | 新增「对话联网搜索」功能 plan + 进度目录（双引擎,手动开关,对标 Tavily） | brainstorming 审定 |
| 2026-07-19 | 联网搜索 Step1（search 骨架 b9a6580a）+ Step2（自建引擎 BuiltIn SearXNG+Jsoup+SSRF，24 单测绿 12c6de0d）已提交；更新本表状态 ⬜→🚧 | Phase3.1 chunk1/2 |
| 2026-07-19 | 联网搜索 Step3（外部供应商 Tavily/Serper/Bing + SystemSettingService 加 search.* 配置/AES key，单测绿 99da983d）已提交 | Phase3.1 chunk3 |
| 2026-07-19 | 联网搜索 Step4-7 全落 + 收尾产出（README/Feature Map/User-Ops/plan 勾选）：Step4 WebSearchService 路由降级链(310eb7fb)+Step5 接 CHAT+V44(fa878b8d)+Step6 前端开关(b7e801ee)+Step7 后台配置页(7b607bab)；沉淀降级链/多源 CITATION 合并/SSRF+sanitize 范式入通用约束 | Phase3.1 chunk4-7 |
| 2026-07-20 | 个人记忆 M3 全落（原 #14+#19）：后端 entities-config JSON KV+Judge prompt token 化+readEntities 动态截断+V45 seed(0cd4888d)；前端 4 模式 v-if 显隐+entities 旋钮组(470bfedf)；沉淀「prompt 动态数值用命名 token 不用 %d」入通用约束 | Phase3.1 |
| 2026-07-22 | 新增「个人记忆 回答记忆 origin 区分」plan（brainstorming 审定 8 决策）：记忆分 USER/ANSWER，加列+唯一约束含 origin+一次 extract 双标+召回开关三态+panel 来源列。**故意放开 M2 写侧 gate**，存量零影响。待许可进 Phase3 | brainstorming + Phase2 |
| 2026-07-22 | 新增「Token 消耗统计(账单)」规格+plan(Phase0 分析+决策审定)：Gateway 单一出口异步采集全平台 LLM token，`llm_usage_logs` append-only(provider_scope 区分全局/私有 key)，流式 usage side-channel 不改 Flux，embed 兜底估算，admin 全量/用户 ownership，MVP 不折成本。待许可进 Phase3 | Phase0/1/2 |
| 2026-07-27 | 新增「计划12 个人记忆重设计」Phase2 实现开发计划(主索引+11 子 plan A-H)：完全替换旧 user_memories+key归一+冲突机模型,改 L0/L1/L2 分层+标签独立表写时归一+周期总结+冲突pending+流水账多挂ACL;9 迭代 V47/V48/V49;**DISCARD 裁决决定连带软删 source turns(§3.8 级联+12h+防死循环)**;G 迁移取消。**与 M2/M3/M4/回答记忆origin区分(改旧表)互斥,择一推进**。待许可进 Phase3 | Phase2 |
| 2026-07-27 | 计划12 **迭代 A 数据层落地 + IT 实跑全绿**：V47 建 9 表+扩 memory_conflicts+新用户 trigger(数据层纯净,偏离 plan AOP 建议)；V48 放宽 conflict 老列 NOT NULL(实跑暴露,V47 已锁不能改)；9 实体+StringArrayTypeHandler；4 Mapper+XML(走 resources 镜像包路径,实跑暴露 java 下不打包)+SqlScopeFilter；MemoryDataLayerIT 10/10 实跑绿(PG16+pgvector)。旧设计备份分支 `agent-platform-old_jiyi`(a1163c53) | Phase3.1 |
| 2026-07-27 | 计划12 **迭代 B 标签归一落地 + 单测+IT 实跑全绿**(commit 763f4d73)：MemoryTagResolver 四路径(精确槽位/aliases/anchor≤阈值+二次LLM批量判同义/全miss新建)+UNIQUE 并发兜底；MemoryTagAnchorService(embed+jieba双通道,失败→null降级)；MemoryTagController(list/edit,owner改label/补aliases,无merge/split/re-extract,反射断言守契约)；阈值走 `memory.tag.anchor-threshold` system_settings。ResolverTest 10绿+ContractTest 3绿+ConcurrencyIT 实跑绿(10线程→1行)。沉淀「XML insert 漏useGeneratedKeys坑」+「不可逆操作禁端点+反射守契约」+「LLM判官安全落空默认值」入通用约束 | Phase3.1 |
| 2026-07-27 | 计划12 **迭代 I1 ACL 前置落地 + 单测全绿**：V49 建 `memory_recall_acl`(reader→target 授权,UNIQUE+索引,无 deleted,可逆 DROP)；MemoryRecallAclResolver.readableAuthors 五路径(owner兜底全员含DEPARTED / admin·member=ACL集∪自己 / recall_admin只配权不扩读 / 非成员空集)；DEPARTED 保交接留 I3 滤；MemoryProjectMemberMapper 不动(走 LambdaQueryWrapper)。ResolverTest 10/10 绿(Mockito)。版本偏移落地：原 V48→V49(V48 被 A 补丁占用)。无新通用沉淀(纯标准 SQL+Java) | Phase3.1 |
| 2026-07-27 | 计划12 **迭代 C 生成写入全量落地 + 54 单测 + 6 IT 实跑全绿**（commit c7905733/825640fb/e3b444a7/daf94b60/5ffcaef5）：5 chunk = C-2 MemoryPrefilter(按侧独立过滤+敏感黑名单六类不可清空+用户加项) / C-3 MemoryGenToggleService(owner表AND会员覆写表,非项目读 rag.memory.gen.personal.enabled 兜底)+配置键+VO矩阵 / C-4 MemoryGenerator(双侧三层同生+applyClean兜底strip fence/截{}/3重试+schema校验+prompt注入<memory_data>防护) / C-5 MemoryGenerationService(memoryTaskExecutor异步fire-and-forget+born_personal定死/卸空转/非项目恒个人+缓存evict) / C-6 MemoryTurnController(raw查看/单删/批量删+IDOR向量7/批量向量13/审计向量15)。**偏离plan**：新建独立service/controller(非改legacy MemoryService/MemoryController,避免新旧表user_memories/memory_turns纠缠)。沉淀「BaseMapper.insert 走 typeHandler 正常(异于LambdaUpdateWrapper.set不读)」入通用约束 | Phase3.1 |
| 2026-07-28 | 计划12 **迭代 E 总结+冲突全量落地 + 59 单测 + 17 IT 实跑全绿**（commit 22fec12d~9b968f41）：8 chunk = E-1 V51 `memory_consolidation_scopes` 加 locked_until/last_run_at + DTO(4) + Mapper 方法/XML + 新 MemoryConsolidationScopeMapper(claim SKIP LOCKED/markClaimed/acquireManualLock CAS/releaseLockSuccess 幂等/upsertScope) / E-2 MemoryBackfillService raw 补 tag(单侧喂入复用 Prefilter+Generator+TagResolver,降级置 gen_done=true 空 tag 防死循环) / E-5 MemoryConflictJudge.judgeSummaryConflict 时序互斥判定(fail-safe 不冲突) / E-3 MemoryConsolidationCompressor(LLM 压缩+日期铁律 assertYearIronRule 源外年份违则重试 null) + MemoryConsolidationService(编排,per-tag 未覆盖判定空跳不调 LLM) + MemoryConsolidationTxService(原子写 summary+coverage+冲突检测,@Transactional 避自调绕代理) / E-4 MemoryConflictResolutionService(KEEP_BOTH/NEW/OLD + DISCARD 连带软删 source turns + §3.8 级联:12h 拒他人引用 summarized_at>12h→FORBIDDEN + STALE+波及通知,死循环防护 turns 不进取数) / E-6 MemoryConsolidationWorker @Scheduled SKIP LOCKED 双节点互斥+STALE 重生 / E-7 MemoryConsolidationController(trigger/targets/auto/conflicts pending+resolve) / E-8 全链 IT(手动总结跑通/DISCARD 死循环防护/DISCARD 12h 拒/KEEP_BOTH/越权)。**偏离plan**：新建独立总结/冲突链(非改 legacy MemoryConflictService/MemoryController,承 C/D 裁决)。**坑修复**：PG 拒 varchar→timestamptz 隐式转换(IT insertTurn 加 ::timestamptz)+@Transactional 自调绕代理抽 TxService+RecallTagMeta.id 非 tagId+readableAuthors 返 Set。沉淀「PG 拒 varchar→timestamptz 隐式转换须 ::timestamptz cast」+「@Transactional 自调绕代理抽 TxService」+「FOR UPDATE SKIP LOCKED 锁生命周期三段(claim短事务+process事务外+release单行,locked_until 时间戳挡他节点)」+「flat provenance @> 包含算子 XML 无需转义」入通用约束 | Phase3.1 |
| 2026-07-28 | 计划12 **迭代 D 召回 pipeline 全量落地 + 76 单测 + 5 IT 实跑全绿**（commit a9c661f9~32b38963）：7 chunk = D-1 RecallScope+Resolver(项目可访问过滤向量2) / D-2 标签聚合(mapper双查personal/project+ACL空集skip向量14+tag_id去重) / D-3 LLM选标签(>30 halfvec+BM25 RRF粗筛top-30+精选3重试+降级candidates全集向量3/12) / D-4 读总结+reflect(恒只读自己向量14+>5 reflect批量LLM+失败降级只读L1) / D-5 拼流水账(gen_done=true born_personal/ACL+批量coverage防N+1+allCovered严格) / D-6 编排装配(MemoryRecallPipeline串七步+按subject聚合打owner前缀对齐§3.3 line110+降级链每步try-catch收notes+selector降级启发式selected==tags全集+traceId每步打点RecallTraceStep) / D-7 Controller+持久化(V50 memory_recall_scope_prefs部分唯一索引WHERE deleted=0+方案A per-user非KV非Redis+upsert+MemoryRecallController preview/scope GET-PUT)。**偏离plan**：新建独立召回链(非改legacy MemoryService/MemoryScopeResolver/MemoryController,承C裁决)。**坑修复**：V50表级CONSTRAINT WHERE报PG 42601→CREATE UNIQUE INDEX WHERE。**出口条件**4✅(持久化/降级/恒只读自己/标签防泄敏)+3⏳(全链LLM/召回数据越权/端到端<2s IT留Phase4 E2E,@MockBean LlmGateway+造全量数据)。沉淀「PG部分唯一约束表级不支持WHERE只能CREATE UNIQUE INDEX」+「selector降级启发式(全集==候选且size>阈值)」+「tags空跳select/read防NPE直走turns兜底」入通用约束 | Phase3.1 |
| 2026-07-28 | 计划12 **迭代 I2/I3 ACL配置端点+取数接ACL+离职开关全量落地 + 45单测+14IT实跑绿**（commit `ae73ab5f`~`4341fee0`）：I2 = MemoryRosterController(/projects/{pid}/roster+recall-acl GET·PUT)+MemoryRecallAclConfigService(isConfigurable owner/recall_admin边界+replaceAll @Transactional全量替换+created_by审计+target∩成员含DEPARTED过滤+空集清权)+MemoryRosterService(isMember ACTIVE判据)+DTO(3)+Mapper写方法(roster join users/recall-acl findGrantedDetails/deleteByProjectAndReader)；I3 = MemoryDepartedResolver(复用roster源:DEPARTED集+「已离开人员·name·date」标注+intersectDeparted)+召回TurnPatcher+总结ConsolidationService接L10离职开关(includeDeparted=false剔readableAuthors∩DEPARTED优先级高于人员多选,SPECIFIC勾离职也剔;summary恒只读自己不受影响)+RecallResult加departedAuthorNotes+Pipeline.collectDepartedNotes装配。**偏离plan**：新建独立controller/service/resolver(非改legacy MemoryController/MemoryScopeResolver/MemoryScope,承C/D/E隔离先例)。**坑修复**：Flyway it库checksum不符→drop+recreate从V1跑净；isConfigurable漏ACTIVE判定(IT暴露)→加status判。Pipeline标注装配端到端+召回全链LLM IT留Phase4 E2E。沉淀「Flyway it库checksum不符=drop+recreate勿对生产repair」+「DEPARTED保交接过滤分层(表存不滤+取数侧滤单数据源双侧过滤)」入通用约束 | Phase3.1 |
| 2026-07-28 | 计划12 **迭代 F/I4 前端本体第一批落地 + vue-tsc/build/99单测全绿**（commit ×7）：8 chunk = F-0 `frontend/src/api/memory.ts`(26端点客户端+TS类型) + F-1 MemoryTagLibrary(owner改label/补aliases) + F-2 MemoryConflictJudgeDialog(KEEP_BOTH/NEW/OLD/DISCARD+DISCARD二次确认+403「他人引用超12h」提示)+MemoryConflictSection + F-3 MemoryTurnSection(全部/raw切换+批量删+tag/项目名回填)+MemorySummarySection(scope+状态徽标CLEAN/PENDING_CONFLICT/STALE+provenance)+MemoryConsolidationDialog(统一总结入口+autoEnabled默认勾) + F-4a MemoryNotificationBadge(3s轮询count+下拉list+ACK+全部已读)挂ChatView底栏 + F-5 MemoryGenMatrixPanel(owner/会员双开关+role控可编辑+一键关+effective) + I4-1/2 MemoryProjectAclPanel(花名册含DEPARTED+reader×target矩阵+owner/recall_admin预判canConfigure+即时PUT全量替换+DEPARTED留target剔reader)。MemoryManagerPanel 现 7 页签壳(记忆旧栈/标签库/流水账/总结/冲突裁决/gen矩阵/项目ACL)。**偏离plan**：gen 矩阵从 RagMemorySettingsTab 改放记忆抽屉(per-user 自助)；三页签改渐进式(旧栈 pane 保留+新栈并行加 pane，H 收尾才删)。全走 `memoryApi` 新栈零碰 legacy chatApi.memories。沉淀「前端多页签壳渐进迁移范式(旧栈 pane 保留+新栈并行,H 收尾才删)」+「ACL 矩阵前端预判 canConfigure+后端403兜底双保险」入通用约束 | Phase3.1 |
| 2026-07-29 | 计划12 **迭代 F/I4 前端第二批落地 + vue-tsc/build/99单测全绿 + F-4b 阻塞确认**（commit `465f48c5`）：3 chunk = F-6 `MemoryRecallScopePopover`(ChatView 底栏新栈召回 scope：个人开关+项目多选+方向+时间窗+离职开关,debounce 400ms PUT,双栈期与 legacy 读控件并存到 H 收尾；走 GET/PUT `/recall/scope`) + I4-3 `MemoryConsolidationPeoplePicker`(总结弹框 PROJECT 行勾选后展开取数选人 SELF/SPECIFIC/ALL+方向+离职禁用；扩 `MemoryConsolidationScopeRequest` 类型补 authorFilter/authorIds/direction/includeDeparted；Dialog selectedScopes 提交带取数配置) + F-7 移动 390px 扫荡(3 弹窗 max-width→`{maxWidth,width:'90vw'}` + ACL 选择器 240px→`min(240px,60vw)`；矩阵已横滚)。**⚠️F-4b 生命周期折叠板阻塞跳过**：grep Memory*Controller 全族无 departed/deleted 项目 list+copy-to/restore 端点，设计 §3.6/§3.8 规的 `MemoryService.copyDepartedProjectTo` 未实现，属 C/E 收尾遗漏，单开后端 chunk 补齐后再上前端。坑修复：n-spin/n-empty `size="tiny"` 非法→`small`；删未用 `lastSnapshot/snapshot`。沉淀「双栈期前端并存范式(不强切净,收尾统一清 legacy)」+「移动弹窗 `width:90vw` 范式」入通用约束 | Phase3.1 |（commit ×4）：审 plan F/I4 发现前端依赖的 4 类读侧端点后端缺(C/D/E/I2/I3 建了 service 未暴露 controller)。4 chunk = B-FE-1 MemoryNotificationController(GET list/count/POST ack + MemoryNotificationService 权边界 user_id=self+幂等)+MemoryNotificationVO / B-FE-2 MemoryGenConfigController(GET /gen-matrix + PUT /projects/{pid}/owner 仅 OWNER + /member 本人)+MemoryGenConfigService(getMatrix 双开关 null→默认开+effective=AND+setOwnerToggle/setMemberOverride upsert+权边界)+MemoryGenMatrixItemVO+MemoryProjectMemberMapper.findMyGenMatrix(join projects+双 settings) / B-FE-3 MemorySummaryController(GET /summaries?projectId)+MemorySummaryViewService(复用 findByUserAndScope+batch 回填 tag 防 N+1)+MemorySummaryVO / B-FE-4 MemoryTurnViewService(本人全量+batch 回填 tag label+项目名)挂 MemoryTurnController.list+MemoryTurnVO。**偏离plan**：新建独立 controller/service(承 C/D/E/I2 隔离先例)，全走 `/api/chat/memory/*` 新栈零碰 legacy。**坑修复**：PG `NOW()`=事务时间→IT seed 用 `clock_timestamp()`；queryForObject 0 行抛异常→改 queryForList；PG JDBC 拒 String→bigint[]→`::bigint[]` cast。沉淀「PG NOW() 事务时间 vs clock_timestamp() 语句级」+「单 SQL 多 LEFT JOIN 出矩阵 null→默认值 service 解析」入通用约束 | Phase3.1 |
