# 项目组与积分划拨 · 开发进度1

> 对应计划：`docs/plans/项目组与积分划拨.plan.md`（8 步）
> 本篇：Step 1（迁移 + 实体）

## 本轮做了什么（Step 1）

V133 迁移 + projectgroup 新包（四实体四 Mapper）+ 真 PG 集成测试 6/6。commit **9aced87d**。

## 产出文件

| 文件 | 作用 |
|---|---|
| `backend/src/main/resources/db/migration/V133__project_group.sql` | 四新表 + usage/media 挂组列 + 个人流水 CHECK 换宽 + 运维对账模板/回滚块 |
| `backend/.../projectgroup/entity/ProjectGroupEntity.java` | 项目组（BaseEntity：软删/乐观锁/审计） |
| `backend/.../projectgroup/entity/ProjectGroupMemberEntity.java` | 成员行（BaseEntity；quota_limit_points NULL=不限，used_points 快照） |
| `backend/.../projectgroup/entity/ProjectGroupWalletEntity.java` | 组池单行（**不继承** BaseEntity，同 V65 个人钱包先例） |
| `backend/.../projectgroup/entity/ProjectGroupLedgerEntity.java` | 组流水 append-only（type 六枚举常量 + ref_type 常量） |
| `backend/.../projectgroup/mapper/` × 4 | Group/Member/Ledger 走 BaseMapper；Wallet 含 `deduct`（条件 UPDATE `WHERE balance>=cost` 返 0 行=不足）与 `credit` |
| `backend/src/test/java/.../entity/ProjectGroupEntityIT.java` | 真 PG 冒烟（@Tag("integration")，跑法见下） |

## 关键设计

1. **编号 V133 非计划的 V130**——仓库迁移已到 V132，占位号作废（偏差已记 plan）。
2. **钱包/流水不继承 BaseEntity**：单行 update 表没有软删/乐观锁语义；append-only 流水同理。沿用 V65 points 钱包先例。
3. **防透支下沉数据库**：`deduct` 条件 UPDATE 靠行锁原子性，两成员并发扣最后一百分不会双成功（IT 验证第二次返 0 行、余额停在 4）。另有 `ck_pgw_nonneg` CHECK 双保险。
4. **组流水 FK 不带 CASCADE**：组可删，钱的历史不可随组消失（回滚块里也警告 ledger 须先导出）。members/wallets 才 CASCADE。
5. **points_ledger type 是 CHECK 不是注释**：PG 不能 ALTER CONSTRAINT 加值，走 DROP `chk_ledger_type` + re-ADD 六枚举（+GROUP_ALLOCATE/GROUP_RECLAIM）。
6. **对账模板内嵌迁移尾部**：ledger-vs-wallet 逐组对账 + 成员 used 对账两条 SQL，运维不用现想。

## 测试

- `mvn test -Dtest=ProjectGroupEntityIT -Dsurefire.excludedGroups= -DfailIfNoTests=false`（cwd=backend）
- **6/6 绿**：审计填充默认值 / UNIQUE 拒重复入组 / CHECK 拒负余额 / 条件扣减 0 行 / 六枚举+非法 type 拒 / GROUP_ALLOCATE 个人流水可入

## 踩坑

- Flyway `Found more than one migration with version 100`：**target/classes 里残留旧 `V100__asset_allow_public_copy.sql`**（计划2 时代产物，src 已无此文件）。删 target 内残留即好——遇到重复版本号先查 target 再疑 src。
- bash cwd 会漂，find/ls 用相对 backend 的路径或绝对路径。

## 下一步

Step 2 组钱包服务：`ProjectGroupWalletService`（allocate/reclaim/chargeGroup/refundGroup/BACKSTOP，先个人后组固定锁序）+ `ProjectGroupService`（建组/成员/限额），并发+幂等+对账单测。

---

# Step 2（组钱包核心账务）· 2026-08-18 · commit 33dce223

## 做了什么

`ProjectGroupWalletService` 五原语 + `ProjectGroupService` 组织管理 + 真 PG IT 9/9；全量 2133 单测绿。

## 产出文件

| 文件 | 作用 |
|---|---|
| `projectgroup/service/ProjectGroupWalletService.java` | allocate/reclaim/chargeGroup/refundGroup/backstop 五原语 |
| `projectgroup/service/ProjectGroupService.java` | 建组(三写同事务)/改名/软删(须清池)/成员增删/限额/重置used |
| `billing/entity/PointsLedgerEntity.java` | +TYPE_GROUP_ALLOCATE/GROUP_RECLAIM、REF_GROUP 常量 |
| `billing/service/PointsWalletService.java` | +debitForGroupAllocate/creditForGroupReclaim（个人侧半边） |
| `projectgroup/mapper/ProjectGroupWalletMapper.java` | +selectByGroupId(ForUpdate)/sumInflightEstimated |
| `projectgroup/mapper/ProjectGroupMemberMapper.java` | +addUsed(quota守卫)/subtractUsed(GREATEST落0)/selectByGroupUser |
| `test/.../ProjectGroupWalletServiceIT.java` | 9 用例：并发/幂等/死锁/对账 |

## 关键设计

1. **固定锁序防死锁**：双向操作（allocate/reclaim/backstop）恒「先个人行→后组池行」；组内操作（charge/refund）恒「组池→成员行」。两序无交叉环，IT 用 4 线程双向×组内混跑验证零死锁。
2. **幂等复用 idempotency_keys**：scope=group.charge/group.refund，result_ref=组流水 id；同键同额静默重放返首次 balance_after，异额/跨身份 CONFLICT（镜像 PointsWalletService.runIdempotent）。
3. **BACKSTOP 口径**：差额扣组长个人（CONSUME, ref=GROUP），组池不动（行锁 FOR UPDATE 读一致 balance_after）；组流水 delta=−差额但不进余额重建、不计成员 used——对账口径「Σdelta 剔除 BACKSTOP == 钱包」。
4. **reclaim 在途上限**：上限=组池−Σ(PENDING/RUNNING 的 estimated_cost)；锁前读有窄竞态，兜底=条件 deduct，最坏在途占用回笼后结算走 BACKSTOP，不丢钱。
5. **quota 守卫在 SQL**：`addUsed` 条件更新 `quota IS NULL OR used+cost<=quota`，返 0 行=超限→整体回滚（组池分文不动）。

## 踩坑

- `idempotency_key` 表名实为 `idempotency_keys`（复数）——IT clean SQL BadSqlGrammar 一轮。
- IT 用户夹具须 `OVERRIDING SYSTEM VALUE` 显式指定 id（IDENTITY），否则 addMember 的用户存在性校验查不到。

## 下一步

Step 3 组管理 API + mine 列表：ProjectGroupController 全端点 + 权限码 `project-group:manage` + 候选用户搜索。

---

# Step 3（组管理 API + mine 列表）· 2026-08-18 · commit a7461417

## 做了什么

V134 权限码 seed + ProjectGroupController 12 端点 + DTO×9 + requireOwner 双层化（admin 放行）+ 单测 8/8 + 全量 2141 绿。

## 产出文件

| 文件 | 作用 |
|---|---|
| `db/migration/V134__project_group_permission.sql` | project-group:manage 权限码（gated：仅 admin 默认） |
| `projectgroup/controller/ProjectGroupController.java` | 12 端点；资金/成员变更 9 处 @AuditLog |
| `projectgroup/dto/` × 9 | 请求×5（@Data）+ VO×4（record：Mine/Detail/Member/Candidate） |
| `projectgroup/service/ProjectGroupService.java` | +listMine/getDetail/searchCandidates/findMember；全方法加 admin 形参 |
| `projectgroup/service/ProjectGroupWalletService.java` | allocate/reclaim/backstop 加 admin 形参 |
| `test/.../ProjectGroupServiceTest.java` | 权限矩阵 8 用例（非组长 403×7 端点/组长+admin 通/404/删组拒/重复入组/组长不可移/候选排除/findMember 边界） |

## 关键设计

1. **端点**：POST /（建组）、GET /mine（我建的∪我在的）、GET /{id}（详情=组+组池+在途+成员）、GET /{id}/members/candidates、PUT/DELETE /{id}、POST/DELETE /{id}/members[/{uid}]、PUT /{id}/members/{uid}/quota、POST .../reset-used、POST /{id}/allocate|reclaim。
2. **双层权限**：`@RequirePermission("project-group:manage")`（平台码）+ service `requireOwner`（组长级，admin 放行）——他组组长无法越权。
3. **listMine**：owner 组 ∪ 成员组去重，每行带余额/myRole/myQuota/myUsed/memberCount——Step6 前端五入口选择器的唯一数据源。
4. **findMember 探针**：Step4/5 计费链路判定「本次请求是否走组池」用，软删组/非成员返 null。

## 踩坑

- LambdaQueryWrapper 误写 `ProjectGroupEntity::getGroupId`（实为 getId）——编译期才现形。

## 下一步

Step 4 计费链路透传：chat/embed/rerank 请求带 groupId → findMember 判定 → chargeGroup 或个人 charge；llm_usage_logs 落组列。
