# 视频生成第三轮修复 · 交互与历史子计划

> 父计划：[视频生成第三轮人工测试问题修复.plan.md](视频生成第三轮人工测试问题修复.plan.md)。只含伪代码。

## Chunk 1：历史查询筛选契约（已完成）

- [x] **目标**：按提示词子串、开始时间、结束时间筛选有权查看的任务。
- [x] **动作**：
  - Controller 接收 `q/from/to/limit`，校验时间区间与 limit≤100。
  - Mapper 使用绑定参数：普通用户附加 `user_id=currentUser`；提示词按大小写不敏感的字面子串匹配；时间按 `[from,to)`；创建时间倒序。
  - QueryService 统一组装 VO，不在前端或 Java 内存中过滤。
  - 历史卡片增加提示词输入、日期范围、清空按钮；300ms 防抖查询。
- [x] **涉及文件（7）**：
  - `backend/src/main/java/com/superprogrammer/media/controller/MediaGenController.java`
  - `backend/src/main/java/com/superprogrammer/media/service/MediaGenQueryService.java`
  - `backend/src/main/java/com/superprogrammer/media/mapper/MediaGenTaskMapper.java`
  - `backend/src/test/java/com/superprogrammer/media/service/MediaGenQueryServiceTest.java`
  - `frontend/src/api/media.ts`
  - `frontend/src/views/VideoGenView.vue`
  - `frontend/src/views/VideoGenView.test.ts`（新增）
- [x] **依赖**：无。
- [x] **验证**：大小写命中；时间边界正确；特殊字符按字面查询；普通用户看不到他人任务；非法区间 400；清空恢复全部。

## Chunk 2：任务详情回显与历史参数恢复

- [x] **目标**：点击历史后恢复模型、提示词、比例、时长、分辨率、水印、生成音频、首/尾帧和普通附件。
- [x] **动作**：
  - 新增 `InputAttachmentVO{fileId,kind,frameRole,name?,previewUrl}`；不返回 data URI。
  - `MediaTaskVO` 补 `ratio/watermark/generateAudio/inputAttachments`，从 `request_config` 解析。
  - 后续提交允许附件附带受限长度显示名并落 JSONB；显示名不进入 Provider 请求。
  - 点击历史先调详情 API，再按“模型能力→基础参数→附件”的顺序原子回填。
  - 当前任务/历史详情增加“请求参数”入口，分为“平台收到的提交参数”和“实际发给模型（已脱敏）”两个页签；支持展开和复制 JSON，并提供清晰的键盘焦点与复制成功提示。
  - 旧任务缺字段按现有默认值兜底；旧模型已下线时保留只读模型名并提示，不静默替换。
  - 恢复附件逐个拉鉴权预览；文件已删/失权保留文字占位并禁止再次提交。
- [x] **涉及文件（10）**：
  - `backend/src/main/java/com/superprogrammer/media/dto/InputAttachmentVO.java`（新增）
  - `backend/src/main/java/com/superprogrammer/media/dto/AttachmentRef.java`
  - `backend/src/main/java/com/superprogrammer/media/dto/MediaTaskVO.java`
  - `backend/src/main/java/com/superprogrammer/media/service/MediaGenTaskService.java`
  - `backend/src/main/java/com/superprogrammer/media/service/MediaGenQueryService.java`
  - `backend/src/test/java/com/superprogrammer/media/service/MediaGenQueryServiceTest.java`
  - `backend/src/test/java/com/superprogrammer/media/service/MediaGenTaskServiceTest.java`
  - `frontend/src/api/media.ts`
  - `frontend/src/views/VideoGenView.vue`
  - `frontend/src/views/VideoGenView.test.ts`
- [x] **依赖**：Chunk 1；列表用于浏览，点击后的详情为回填真相源。
- [x] **验证**：新旧任务均可打开；参数与附件角色准确；恢复后重新提交参数一致；失权附件不泄露且不能重用；新任务可查看/复制两类参数 JSON，旧任务明确提示无发送快照而不冒充“实际请求”。

## Chunk 3：历史行内视频懒加载

- [x] **目标**：成功任务每行右侧显示小视频，其他状态显示占位。
- [x] **动作**：
  - 新组件进入可视区后才用 `fetchVideoBlob(videoUrl)` 拉取。
  - 显示小尺寸静音 `<video controls preload=metadata>`；卸载/筛选切换时 revoke objectURL。
  - 加载失败降级“预览不可用”，不影响点击详情；补 `aria-label`。
- [x] **涉及文件（4）**：
  - `frontend/src/components/media/MediaTaskVideoPreview.vue`（新增）
  - `frontend/src/components/media/MediaTaskVideoPreview.test.ts`（新增）
  - `frontend/src/views/VideoGenView.vue`
  - `frontend/src/views/VideoGenView.test.ts`
- [x] **依赖**：现有成功任务 `videoUrl`。
- [x] **验证**：可见行才下载；成功行可播放；失败行不请求；卸载无 URL 泄漏；移动端不挤坏表格。

## Chunk 4：资产库选择器显示图片/视频

- [x] **目标**：选图/视频时直接看到媒体内容。
- [x] **动作**：
  - 使用 `AssetVO.fileId` 和 `useLazyFilePreview`；IMAGE 渲染 `<img>`，VIDEO 渲染静音 `<video preload=metadata>`。
  - 失败降级类型图标+文件名；音频保持现状。
  - 保留多选、上限、已添加置灰、搜索和项目切换。
- [x] **涉及文件（4）**：
  - `frontend/src/components/asset/AssetFilePicker.vue`
  - `frontend/src/components/asset/AssetFilePicker.test.ts`（新增）
  - `frontend/src/components/asset/AssetPickerMediaPreview.vue`（新增）
  - `frontend/src/components/asset/AssetPickerMediaPreview.test.ts`（新增）
- [x] **依赖**：现有文件读取权限咽喉点。
- [x] **验证**：图/视频可见；无权降级；快速切项目不串图；选择计数不受预览影响。
