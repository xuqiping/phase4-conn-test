# 安全加固开发进度4 · Chunk 3 前端 commercialAuthStore 适配

> 功能：安全加固（离线授权与门禁）
> 轮次：第 4 轮（2026-07-17）
> 类型：代码类工作
> 计划：[../../docs/plans/安全加固-离线授权与门禁.plan.md](../../docs/plans/安全加固-离线授权与门禁.plan.md)
> 真相源：[../../docs/specs/security_strategy.md](../../docs/specs/security_strategy.md) §4
> 上一进度：[安全加固开发进度3.md](../安全加固开发进度3.md)

---

## 本轮目标

完成 plan.md **Chunk 3 · 客户端前端：同步凭据 + 适配**：
- 3.1 `commercialAuthStore.syncOfflineTokenToRust` 改为始终调用 `set_signed_entitlement`（在线/离线均同步），移除前端 `offlineSeconds` 计算。
- 3.2 `isModuleAllowed` 离线/在线分支统一：优先 Rust `require_module` 结果。
- 3.3 移除对旧 HMAC token / `offlineUsableUntil` 前端独立判定的依赖，过期以 Rust 为准。

---

## 本轮步骤与产出

| # | 步骤 | 产出文件 | 说明 |
|---|---|---|---|
| 1 | 重构 Store 凭据同步 | `src/stores/commercialAuthStore.ts` | `syncOfflineTokenToRust` → `syncEntitlementToRust`；只要 `offlineToken` 存在即同步给 Rust；移除 `usingOfflineClientAuthorization` 与 `hasValidOfflineClientAuthorization` |
| 2 | 统一模块权限判定 | `src/stores/commercialAuthStore.ts` | `isModuleAllowed` 优先取 Rust `check_signed_entitlement_access` 缓存，无凭据时回退内存快照 |
| 3 | 清理前端独立过期判定 | `src/stores/commercialAuthStore.ts` | `getUsableClientAuthorization` 不再检查 `offlineUsableUntil`；错误处理仅对结构化服务端拒绝清空凭据，网络抖动保留 Rust 凭据 |
| 4 | 同步 UI 组件 | `src/components/EntitlementStatus.vue` | `usableClientAuthorization` 不再因 `offlineUsableUntil` 过期而返回 null |
| 5 | 更新 Store 单元测试 | `src/stores/__tests__/commercialAuthStore.test.ts` | 新增 Tauri `invoke` mock；新增「Rust 缓存优先」「在线/离线均同步凭据」「不应用前端过期判定」用例；重写网络失败场景 |
| 6 | 更新组件单元测试 | `src/components/__tests__/entitlementStatus.test.ts` | 过期判定测试改为断言前端不再做该判定 |
| 7 | 勾选计划 | `workflow_output/docs/plans/安全加固-离线授权与门禁.plan.md` | Chunk 3 三项任务已勾选 |

---

## 关键代码位置

- 统一凭据同步：`src/stores/commercialAuthStore.ts:154-186`
- 模块权限判定：`src/stores/commercialAuthStore.ts:230-238`
- 错误处理：`src/stores/commercialAuthStore.ts:140-150`
- UI 状态：`src/components/EntitlementStatus.vue:51-60`

---

## 测试结果

- 前端 Vitest 全量：**249 通过，0 失败**。
- Rust `commands::auth` 内联测试：**9 个全部通过**。
- `cargo check`：通过（仅有与本次变更无关的 dead_code 警告）。

---

## 涉及变更

- `feat(file-keeper/src): 安全加固 Chunk3 前端 commercialAuthStore 适配 Ed25519 签名凭据`

---

## 遗留与下一步

- **Chunk 4**：跨语言一致性测试、端到端测试、更新风险表状态与进度文档。
- 注意：前端仍不持久化 signed entitlement，离线态仅限当前应用生命周期内有效；如需跨重启离线使用，需在后续迭代中补充本地持久化与 Rust 启动时恢复。

---

## 安全检查清单（本 chunk 涉及项）

- [x] 前端不再基于 `offlineUsableUntil` 做独立访问判定，全部下沉 Rust。
- [x] 在线/离线统一同步 signed entitlement 给 Rust。
- [x] 服务端结构化拒绝（账号禁用、设备解绑）时清空 Rust 凭据；网络抖动保留凭据。
