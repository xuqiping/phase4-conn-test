# 开发进度 4 · P02 云端骨架与账号计费（B7+B8 收尾）

> 接 [开发进度3.md](开发进度3.md)。本轮：Step 8 客户端计费半边 + Step 9 云端 CI 与功能收尾。**P02 全部 Step 完成。**

## Step 8 · 客户端 core-meter 镜像 + 余额环（commit `git log -1` 以实际为准，SHA 见 git）

- **L3__usage_mirror.sql**（core-state 迁移）：usage_mirror 表——id/user_id/kind(1-4)/model/amount_cents/idempotency_key UNIQUE/synced_at + idx_mirror_user。
- **MeterMirror**（core-meter/src/mirror.rs）：record（ON CONFLICT DO NOTHING）；sync_from_cloud 幂等键 `cloud-{云端id}`（重放零新增，测试锁死）；sum_cents；reconcile→ReconcileReport{ok, local_cents, cloud_cents, drift_cents}。
- **TokenVault**（vault.rs）：trait + OsKeyringVault（keyring 3，Windows 凭据管理器，service=DevPilot/refresh_token）。Rust 侧命令 5 个：vault_save/load/clear + meter_sync + meter_reconcile（commands.rs；trait 方法需 `use core_meter::TokenVault as _;`）。
- **cloudApi.ts**：模块级 accessToken（内存 only）；402→PaymentRequiredError；401→vault.load→POST /auth/refresh→重试一次，refresh 也失效则清 vault 抛「登录已过期，请重新登录」；断网→「网络连接失败，请检查网络后重试」。
- **billing store + BalanceRing**：refresh() 拉 /balance + transactions 前100→meter_sync→meter_reconcile，不平亮黄条；insufficient→环红+去充值（window.open 充值门户）；60s 轮询。userId 暂 0（TODO P04 登录态）。
- 测试：cloudApi 6 用例 + BalanceRing 4 用例；cargo core-meter 5 / core-state 7+14 全过；vitest 29/29。

## Step 9 · 云端 CI + 收尾

- **`.github/workflows/devpilot-cloud-ci.yml`**（仓库根，paths 限定 `DevPilot/PROJECT/cloud/**`）：npm ci→lint（--max-warnings 0）→tsc→test→test:e2e→npm audit 硬拦截（本地网络受限 audit 仅 WARN 由 CI 兜底）。
- **云端 ESLint 从零配**：eslint 10 flat config（eslint.config.mjs）+ @typescript-eslint + globals；顺手修掉 billing.controller 未用 UseGuards、4 处失效 eslint-disable（--fix）。lint 脚本扩到 src/test/scripts 三目录。
- **check_all.sh**：云端段扩为 lint+tsc+test+test:e2e；audit 段补云端 npm audit（WARN 降级同桌面）。
- **基准记录**（本机，mock 供应商，nest start dev 模式 2026-08-16）：冷启动 ~8s（含 ts 编译）；/balance 15-24ms；estimate 4-30ms；search 3-20ms（PGlite 进程内无网络往返）。基准时发现 dev 启动必带 `JWT_ACCESS_SECRET/JWT_REFRESH_SECRET` env，否则注册 500「secretOrPrivateKey must have a value」——.env.example 已有，正式跑记得 source。
- **文档收尾**：db_schema.md 回写 L3 实建字段与文件名（原占位的 skills_local/mcp_servers 标注留待 P05+）；plan S8/S9 勾✅ + 安全清单 8 条逐条勾（附落地证据）；功能 README / Feature Map / User-Ops 新建；总览 P02 归档 ✅。

## 测试账（终态）

cloud：lint 0 错 + unit 3 + e2e 38 ｜ desktop：vitest 29/29 + cargo workspace 全绿 ｜ check_all EXIT=0（含云端 e2e）｜ check_docs 无 FAIL

## 遗留（不阻塞收尾，进总览）

- CI 首推观察（cloud-ci 与桌面 ci 双 workflow）；compose/Flyway 运行时验证随 CI。
- 真上游联调待运营配 key（anthropic/博查/SearXNG/Jina）。
- meter_sync userId=0 待 P04；充值门户为 mock 渠道。

## 下一步

P02 出口条件核对 → `/phase4 P02_云端骨架与账号计费`（冒烟）；随后新会话 `/phase2 P03`。

---

# Phase 4 运行验证记录（2026-08-16）

## Run · 云端全链路冒烟（curl 实测，服务 :3780 真 HTTP）

注册(验证码)→余额500体验金→estimate 315分→chat SSE 逐帧+done(315)→余额185→搜索计费10分+归一化缓存命中不扣费→明细3行各带 kind/label→余额不足路径→下单P50→伪造签名拦。**全链通过**。

## 冒烟+对抗审查抓出并已修（回归 e2e 40/40）

| 编号 | 问题 | 修法 | 验证 |
|---|---|---|---|
| BUG-P02-01(高) | 余额略不足：流跑完→末帧实扣402→不扣一分钱，换 nonce 可**无限白嫖** | charge 加 allowOverdraw：末帧透支记账（钱包可负），后续 precheck 拦截 | e2e（done 正常+透支-130+二次402）+ 真服务实测三连（500→185→-130→402）|
| BUG-P02-02(中高) | 回调 CAS 赢后入账前崩溃→订单已 paid 但钱永不到账，无补偿路径 | 重放回调发现「status=1 且账本无 recharge-{id} 行」→ 补 credit | e2e（模拟崩溃窗口→重放补账→再重放不加钱）|
| 配置风险(中) | HMAC 默认密钥回退，生产忘配=任何人可自签伪造充值 | production 下缺 PAYMENT_HMAC_SECRET 直接拒绝回调服务 | 代码审查+e2e 存量 |
| SSE 错误帧(中) | 业务 402 被误报 500「服务开小差了」 | HttpException 状态码+文案透传到 error 帧 | （随01修复后该路径不再触发，防御仍在）|
| 文档失实 | feature-map「同一事务」实际是幂等+补偿；kind=3 写成退款实为赠送 | 两处改如实 + month_spent 未实现标注 | check_docs 过 |

## AC 逐条核对

| AC | 要求 | 结论 |
|---|---|---|
| AC-045 | 先预估后实扣可查明细 | ✅ estimate 接口+chat done 帧+transactions 明细（e2e billing/chat 套） |
| AC-053 | 统一搜索带来源 URL+主备降级透明 | ✅ search e2e 降级链/degraded 标记 6 用例 |
| AC-054 | 搜索按次计费明细单列 | ✅ model='web_search' 单列行+缓存命中不计费 e2e |

## 性能评测（本机 mock，30 采样）

| 指标 | 目标 | 实测 | 结论 |
|---|---|---|---|
| PERF-10 读接口 P95 | ≤200ms | /balance p50 4.3ms / p95 24.4ms | ✅ |
| PERF-12 网关附加延迟 P95 | ≤150ms | estimate 18.7ms；search（不缓存）29.7ms | ✅ |
| 并发 | — | 30 并发 /balance 全 200，单请求 3-27ms；100并发扣费 e2e 不超扣 | ✅ |

## User-Ops 验证

云端可验部分（余额展示/402引导/明细/缓存不产生条目/登录过期链）已随冒烟+e2e 覆盖；**余额环 GUI 三态**由 vitest 4 用例覆盖（真窗口人工走查与 P01 遗留 TC 一并补）。

## Review 结论

第二个 AI（对抗式，8 维度无沉默）+ 人复核。高危两项当场修复并有回归测试；中低 6 项入总览「规格漂移待办」（月度熔断/pino日志/metrics/QPS限流归 P08，nonce 规格回改归 P03，搜索并发缓存竞争+镜像100条同步归 P03/P04）。
