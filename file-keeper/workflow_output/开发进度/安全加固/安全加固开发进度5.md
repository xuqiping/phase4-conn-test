# 安全加固开发进度5 · Chunk 4 验收收尾

> 功能：安全加固（离线授权与门禁）
> 轮次：第 5 轮（2026-07-17）
> 类型：代码类工作 + 文档真相源更新
> 计划：[../../docs/plans/安全加固-离线授权与门禁.plan.md](../../docs/plans/安全加固-离线授权与门禁.plan.md)
> 真相源：[../../docs/specs/security_strategy.md](../../docs/specs/security_strategy.md) §4
> 上一进度：[安全加固开发进度4.md](../安全加固开发进度4.md)

---

## 本轮目标

完成 plan.md **Chunk 4 · 验收与收尾**：
- 4.1 扩展 Rust `auth.rs` 内联测试，加入跨语言一致性固定向量。
- 4.2 产出端到端测试方案，覆盖断网过期、改系统时钟、直接 invoke 特权命令等场景。
- 4.3 前端兼容旧版 HMAC token：检测到无 `.` 分隔符的旧格式时清空 Rust 凭据并强制在线刷新。
- 4.4 同步更新 `security_strategy.md` 风险表、`feature-map` TODO、`变更记录.md`。

---

## 本轮步骤与产出

| # | 步骤 | 产出文件 | 说明 |
|---|---|---|---|
| 1 | 扩展 Rust 内联测试 | `src-tauri/src/commands/auth.rs` | 测试由 9 个增至 14 个；新增跨语言固定向量（Java 签 → Rust 验）及篡改一字节拒绝用例 |
| 2 | 重构公钥加载 | `src-tauri/src/commands/auth.rs` | 提取 `load_public_key_from_pem`，支持测试使用非嵌入公钥 |
| 3 | 旧 HMAC token 兼容 | `src/stores/commercialAuthStore.ts` | 新增 `isLegacyHmacToken` 检测；`syncEntitlementToRust` 发现旧格式则清空 Rust 凭据并抛错提示联网刷新 |
| 4 | 前端 Store 单测更新 | `src/stores/__tests__/commercialAuthStore.test.ts` | 既有测试 token 改为含 `.` 的新格式；新增旧 HMAC token 检测用例 |
| 5 | 端到端测试方案 | `workflow_output/docs/测试方案/安全加固端到端测试方案.md` | 断网过期、改系统时钟、直接 invoke、旧 token 兼容四个场景 |
| 6 | 更新安全策略真相源 | `workflow_output/docs/specs/security_strategy.md` | P0-1/P0-2/P1-1/P1-2 状态改为「已修」 |
| 7 | 更新功能地图 | `workflow_output/docs/feature-map/06-认证与商业授权.feature-map.md` | 同步 Ed25519 signed entitlement、Rust 门禁、测试覆盖、TODO 关闭 |
| 8 | 更新变更记录 | `workflow_output/docs/changes/变更记录.md` | 记录 Chunk 1~4 安全加固变更 |
| 9 | 勾选计划 | `workflow_output/docs/plans/安全加固-离线授权与门禁.plan.md` | Chunk 4 四项任务已勾选 |

---

## 关键代码位置

- 跨语言固定向量测试：`src-tauri/src/commands/auth.rs:444-495`
- 旧 HMAC token 检测：`src/stores/commercialAuthStore.ts:180-183`
- 强制在线刷新：`src/stores/commercialAuthStore.ts:168-176`
- 文档真相源：`workflow_output/docs/specs/security_strategy.md:48-55`

---

## 测试结果

- 前端 Vitest 全量：**250 通过，0 失败**。
- Rust `cargo test`：**77 通过，0 失败**（`commands::auth` 14 个测试全部通过）。
- Java `SignedEntitlementSignerTest`：**7 通过，0 失败**。

---

## 涉及变更

- `feat(file-keeper): 安全加固 Chunk4 验收收尾 - 跨语言一致性测试、旧 HMAC token 兼容、文档真相源更新` (`b78c726`)

---

## 遗留与下一步

- 安全加固 Chunk 1~4 已全部完成， plan.md 所有步骤已勾选。
- 后续如需「跨重启离线使用」，需在后续迭代中补充本地持久化与 Rust 启动时恢复（当前为 P2，待排期）。
- P2-1（设备身份服务端签发）、P2-2（单调时钟 + 在线复核）仍为待排期项，不在本计划范围。

---

## 安全检查清单（本 chunk 涉及项）

- [x] Rust `auth.rs` 内联测试覆盖伪造签名、篡改 payload、过期、合法放行、公钥验签、空模块列表、`notAfter` 早于 `issuedAt`、跨语言固定向量。
- [x] 跨语言一致性：Java 签发的固定向量 Rust 可验过；篡改 payload/signature 任意一字节均被拒。
- [x] 前端检测到旧版 HMAC token（无 `.` 分隔符）时清空 Rust 凭据并强制在线刷新。
- [x] `security_strategy.md` 风险表 P0/P1 状态已更新为已修。
- [x] `feature-map` TODO 中 Rust 侧二次校验项已标记完成。
- [x] `变更记录.md` 已记录 Chunk 1~4 安全加固变更。
