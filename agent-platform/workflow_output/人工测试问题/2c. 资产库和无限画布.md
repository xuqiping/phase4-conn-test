# 已解决
- 资产库可以由 OWNER 按用户名搜索并授权其他用户，成员列表与邀请链路已实测通过
- 资产项目可发布到公众池：管理员发布带“官方发布”标记并直接开放；普通 OWNER 可选直接开放或申请审批；公共用户按权限使用，公共媒体和复制副本预览已修复
- @选中上级内容后，这一块颜色需要变成蓝色（简单解决，但是还有点丑，并且输入了一段话之后，再用@就无法弹出引用前面节点内容了，而且@文本好像不起效果，应该达到的效果是，我在哪里@文本节点，那文本节点就应该插入到这个地方）
- 每个节点，除了要标注出这个是什么节点之外，也要写出该节点的名字，并且需要像comfyui一样，双击画布可以弹出节点选择进行添加节点（还有问题，只写了未命名，但没有具体名字）
- 节点中，视频节点好像只能把上级图片节点作为首帧，比如我@上级图片节点说，“请以@上级图片节点为尾帧，猫从地上跳上窗台”，但是并没有效果，反而仍把上级图片节点作为首帧
- （2026-08-14 第二轮修复，6 项全部处理）
- （2026-08-16 第三轮修复，3 项全部处理）

## 2026-08-14 第二轮（原「未解决」6 项）
1. **视频生成模块无法把已生成视频加入资产库 ✅** —— 根因：入库按钮被 `v-if="videoObjectUrl"` 门控，视频 blob（大文件易超时）拉取失败时按钮永远不渲染；而后端入库走 `file_id` 引用根本不需要前端 blob。修复：下载/入库按钮只按 `activeTask.status === 'SUCCEEDED'` 门控，blob 拉取失败也能入库（下载按钮在有 blob 时才显示）。
3. **画布图片生成无参数可选 ✅** —— 图片节点属性面板接入 capability 驱动参数区（与图片生成模块同源）：尺寸（预设+自定义宽x高）/输出格式/优化模式/引导尺度（模型支持时）/组图+数量/水印/联网搜索；切换模型按新模型能力重置默认值并清掉不支持的残留字段（后端对不支持字段传值会直接拒绝）；提交时 `buildImageRequest` 按能力过滤组包。浏览器实测：lite 显示 2K/组图/联网，pro 显示 1K/1.5K/2K 且隐藏组图联网（与后端能力一致）。
5. **画布从资产库拉取视频/图片无预览 ✅** —— 根因：`applyAssetResolve` 用英文 `'VIDEO'/'IMAGE'/'AUDIO'` 比较后端返回的中文受控词汇 `'视频'/'图片'/'音频'`，分支永不命中 → 只写 fileId 不拉预览，节点上看起来“没拉到”。修复：改用 `MEDIA_TYPE.VIDEO/IMAGE/AUDIO` 常量比对。浏览器实测：从库选择图片资产 → 节点渲染预览图 + 「已引用资产 xxx v1」徽标 + fileId 回写 + 自动保存。
6. **拉线建节点 + 节点显示具体名字 ✅** —— ① 从节点输出句柄拉线到空白处松手 → 弹出「添加下一节点（自动连线）」搜索框，选定类型后建节点并自动连线（connect-start/connect-end 事件，落点在节点上不开弹窗）；② 生成任务完成后节点名自动用提示词前 12 字回填（如「一只橘猫趴在窗台上晒太阳」），仅当 label 还是默认类型名（图片/视频 N）时回填，用户手动改过的名字不覆盖。浏览器实测名字回填生效。

# 待决策
- 别人的项目内容是否允许被复制 ✅（2026-08-18 已实现）—— 发布弹窗「允许公共用户复制资产」开关（默认允许、再发布回显；V100 列 DEFAULT TRUE 存量兼容）；关闭后公共 VIEWER 仅可引用不可复制（前端按钮不渲染 + 后端 copy 403 ASSET_COPY_FORBIDDEN 双保险）；项目成员/OWNER/admin 不受限。见 [开发进度](../开发进度/资产库公共选择与评分等级/开发进度1.md)
    > 2026-08-17 拍板：发布到公众池时由 OWNER 自选「是否允许复制」（默认允许）→ 见 [资产库公共选择与评分等级设计.md §6](../docs/specs/资产库公共选择与评分等级设计.md)

# 待核验
2. **画布生成任务中离开、完成后重进节点被重置 ✅** —— 三层修复：① taskId 在提交瞬间即时持久化（不再走 800ms 防抖，离开不丢任务关联）；② `loadCanvas` 对快照中 `status='running'` 且带 taskId 的视频/图片节点重新续轮询（任务早已完成 → 首次轮询即回填结果+预览+审计字段；仍在跑 → 继续轮到终态）；③ 路由离开/关标签前 flush 防抖窗口内的未保存编辑。浏览器实测：提交生图 → 刷新页面 → 节点恢复「生成中」→ 自动轮询至「完成」并回填预览。
4. **@视频引用发给供应商的是文本 fileId 而非 video 类型 ✅** —— 根因：`resolveCanvasVideoAttachments` 只把 image 节点收为附件，@视频节点被降级成 `"fileId:xxx.mp4"` 拼进 prompt 文本。修复：video 节点 @ 收为 `kind='video'` 附件并序号化为「视频N」，后端 ArkSeedanceProvider 映射为 `reference_video` 内容项；与首尾帧互斥校验同步覆盖视频。单测 14/14（新增 5 例：视频附件/图文混用序号/去重/帧互斥/断链降级）。
- 无限画布中，LLM、生图、生视频模型是否还存在生成时间上限？超时报错？我看有时候生成时间久了还是报错服务不可达然后就失败了 ✅（2026-08-18 已修待复测）—— 真根因=前端断路器把后台轮询/产物拉取当主动操作连续失败计数，断网/慢任务 30s 即误弹「服务不可达」并踢会话。Step1：后台型请求（getTask/媒体blob拉取）逐一标 `_background` 豁免计数与清会话，toast 换「后台任务网络波动」；轮询失败退避 5→10→30s 封顶不锤接口，成功归 5s；轮询 timeout 30s；切回标签页立即补一次轮询。生成无前端时间上限（后端任务 60min 看门狗仅作用于批量调度器兜底悬挂）
- 无限画布中，所有节点的宽高固定，这样肯定不行，需要我可以拖拽更改节点的宽高 ✅（2026-08-18 已修待复测）—— Step2：选中节点四角拖柄（@vue-flow/node-resizer，min 160×64），宽高落 node.data 持久化（老快照默认宽 200 零报错），六类节点内容自适应（图 100%/文本超高滚动），刷新还原
- 无限画布中，批量生成里，如果节点是有上下游关系的，一定要先完成上游节点的任务，再进行下游节点的任务开始 ✅（2026-08-18 已修待复测）—— Step4：依赖调度器 runDependencyScheduled——就绪=选集内全部上游 SUCCEEDED 才提交下游；上游 FAILED→下游级联标灰「上游失败已跳过」；上游不在选集→标「上游不在选集」不静默跑；互不依赖分支滑窗 2 并行；429 冷却重排不占窗口槽；60min/节点看门狗；进度浮条「停止调度」停释放不停在途
- 在图片生成和视频生成模块里，从资产库选择的时候，无法选择公共池中的资产库 ✅（2026-08-18 已修待复测）—— AssetFilePicker 加「我的/共享项目 ↔ 公共池」来源切换，公共池仅展示 usable 且媒体类型匹配的项目（后端摘要补 mediaTypes）；画布 AssetPicker 前轮已有公共池来源
- 资产库分享的时候，点开输入用户名搜索的时候，显示的是无数据，只有输入了用户名之后才会显示，应该要点击后就展示所有用户的 ✅（2026-08-18 已修待复测）—— 根因=前端空关键词不发请求；现打开弹窗即拉全量候选（后端空关键词 LIMIT 50，带关键词 20）
- 无限画布中，需要可以对图片节点进行编辑，最简单的就是图片的翻转，另外还需要可以对图片进行指定位置框选标注（比如框选一块半透明蓝色，标注为蓝色这里增加一个杯子，框选一块半透明红色，标注为红色这里重新生成优化下/xx东西不要了等等）这种类似PS的AI功能。需要有7-8种框选颜色 ✅（2026-08-18 已修待复测）—— Step6/7：①图片节点 5 操作（水平/垂直翻转+旋转90/180/270，EXIF 先归正）产衍生图节点原图不变；②彩色标注弹层：拖框/8 色板/每框指令≤200字/删改框，服务端合成标注图（30% 半透明填充+描边+序号徽标）；双出口=仅出标注图 / AI 修改（参考图=[原图,标注图]+逐框指令清单自动建下游图节点，有模型自动提交）
- 资产库分数的评分不按分数，按A+，A，B，C，D等 ✅（2026-08-18 已修待复测）—— 后端 AssetGrade 派生（≥95 A+ / 90-94 A / 80-89 B / 70-79 C / <70 D，未评空；均分先取整）随 VO 下发；卡片/详情评分行等级徽章、打分实时等级、筛选条等级快捷筛选（A+=[95,100] 等）
- 无限画布中，被@到的视频、图片节点需要在参考图/参考视频中展示，并且需要鼠标悬浮、点击放大图片观看， ✅（2026-08-18 已修待复测）—— Step8：属性面板「参考」区——首帧/尾帧/图N/视频N 徽标缩略（懒加载+LRU 缓存），序号与提交时序号化同源必然一致；hover 放大、点击图片全屏/视频播放弹窗；prompt 增删 @ 实时反映
- 无限画布和视频生成中需要有视频反推的功能，反推功能要求如下 ✅（2026-08-18 已实现待复测，[计划6](../docs/plans/视频反推与转绘.plan.md) 四步全落地）
    - 反推分镜 ✅
    - 反推关键帧（比如镜头和镜头切换的时候）✅ —— FFmpeg 场景检测抽帧（命中<4 均匀采样兜底），仅勾关键帧零 LLM 成本
    - 反推剧本 ✅ —— 分镜+剧本共用单次多模态 LLM 调用（帧缩略≤1024 控 token）
    - 对剧本反推之后进行转绘（剧情不变，但是国内元素要转化为指定国家本土元素，比如中国的剧情里的筷子，如果指定要转绘成西方国家，那么就需要转绘成西方国家元素吃饭的刀叉）✅ —— 目标地区+保留要求→改写（镜头数顺序不变约束），changeLog 逐条核对，场景数不一致警示但结果可用；两入口：画布视频节点反推→衍生节点连边、视频生成页「视频反推」Tab→「用剧本生成」预填（冲突三选）+源视频进参考槽。后端 2197/前端 599 自动化绿，人工用例见 [测试方案](../docs/测试方案/视频反推与转绘测试方案.md)
- 当选中一个节点的时候，所有关联的上下游节点的连线都需要变个颜色，需要可以方便的看到该节点都连接了哪些节点（并且可以隐藏所有不关联节点） ✅（2026-08-18 已修待复测）—— Step5：选中即上下游传递闭包高亮（关联边加粗辉光、无关淡化 0.25，多选=并集，拖动不重算 memo）；工具条 🔗「只看关联」把无关节点整隐藏（可逆）。**同日二修（用户实测：选分镜1，同源的文本3/文本4 不淡化）**：真根因=闭包旧实现双向 BFS 等价整连通分量，改为定向双 BFS（上行只追祖先/下行只追后代，闭包=祖先∪后代∪seed）；配套修库内选中不同步（拖拽吞 click 时旧种子算闭包，nodes-change select 归一单选真相）；自动保存加 canvasLoaded+空板防误清空双闸门（HMR 重挂载实测曾把空 board PUT 清空服务端快照，canvas 6 事故根因）。commit `605fe4f5`，单测 13/13+浏览器双场景复测（点文本3→分镜1暗；微拖分镜1→文本3暗）
- UI的连线不要上下，而是要左右 ✅（2026-08-18 已修待复测）—— Step3：输入句柄=左、输出句柄=右，横向贝塞尔；存量画布打开自动横向；中点×删除/选中高亮跟随
- 需要有一个组的概念，当我框选中一批节点后，我可以把他们设置成一个组（UI要有区分），这个组的下游节点可以@这个组内的任何内容 ✅（2026-08-18 已修待复测）—— Step9：批量工具条「设为组」→彩框包围盒（组名+计数，头部可改名/✕解散），成员组色描边；下游 @ 候选=祖先∪命中组全员（按组分节+色点；孤立组不进候选防越权）；建组/刷新还原、入新组自动移出旧组、删成员自动清理

## 2026-08-16 第三轮（原「未解决」3 项，全部处理）
1. **框选多节点 + 一键关联 ✅** —— ① 框选交互（2026-08-16 按用户反馈改**✋/▭ 双模式**，右下工具条切换）：✋=左键拖平移画布（Shift+拖仍可临时框选）；▭=左键按住直接拖框批量选节点（同 Windows 框选，免 Shift），选集高亮 + 顶部批量工具条（一键关联/批量生成/批量删除），顺带修掉库默认 Backspace 静默双删隐患（`delete-key-code=null`，删除只走应用层确认入口）；② 一键关联：连线可达上游中名字（节点名/资产名，≥2 字）出现在选中文本类节点正文里的，提案预览（默认全勾可逐行取消）确认后自动替换为 `@{{node:id}}`——与手动 @ 完全同效（高亮/断链检测/运行插值零改动复用）；长名优先防「主角1」吞「主角10」、已 @ 区间不二次包装、逆序替换防位移；0 匹配不弹窗只提示。LibTV 调研：Link 思路即本项落地，打组模板/三视图/续拍列入 plan R2-R5 建议档。
2. **多节点批量生成 + 并发限制 ✅** —— 前端按选中节点诱导子图拓扑序提交，滑动窗口同时 2 个在途，429 指数退避（1s/2s/4s×3），单节点失败不阻断整批，进度浮条可关（任务照跑），taskId 即时持久化中途刷新可恢复；后端落地 15x 三问（见 [15x_并发.md](15x_并发.md)）：Redis 预占计数并发闸 + TTL 30min 自愈 + 终态影响行=1 才释放，上限管理员系统设置热调（默认 video=2/image=3），Redis 停机 fail-open。
3. **可编辑共享知识库：仅删改自己内容 + 记录上传者 + 百分制双轨评分 + 打分开关 + 筛选 ✅** —— ① 项目级内容模式开关（OWNER 可切，默认「共享」=现状；「个人」=成员仅能编辑/删除/定稿/归档自己上传的内容，OWNER 不限，切换即时可逆不删数据）；② 上传者用户名上卡片徽标 + 详情元数据，存量文件类资产回填真实上传者（文本类近似回填项目 owner，手册已注明）；③ 双轨评分：拥有者分单列（★88）、成员分取均分（90·3人）互不混算，再次提交覆盖旧分，被移除成员历史评分保留参与均分；④ 打分开关：关闭后成员不可新增/修改评分、历史与均分保留，拥有者恒可评；⑤ 筛选：按上传者（用户名）/ 分数区间 / 来源（拥有者 vs 成员均分取平均）与类型×角色×关键词交集。V124 建表 + 后端 138 单测 + 前端 22 单测全绿。

> 第三轮已同步更新：项目资产库/无限画布创作页/生图大模型接入 三份 feature-map + 两份 user-ops 第三轮增补（2026-08-16）。

# 参考feature-map、user-ops
- agent-platform\workflow_output\docs\feature-map\项目资产库.feature-map.md
- agent-platform\workflow_output\docs\user-ops\项目资产库用户操作手册.md

>解决完后已同步更新对应的feature-map和user-ops（2026-08-14）

# 修复设计规格（2026-08-17，未解决项已全部规格化；四轮 8 项已实现待复测，视频反推已随计划6 于 2026-08-18 实现待复测）
- [无限画布四轮增强设计.md](../docs/specs/无限画布四轮增强设计.md) —— 超时/服务不可达韧性、节点宽高拖拽、批量生成上下游依赖调度、图片翻转+彩色框选标注、@参考图视频预览、关联连线高亮+只看关联、连线左右、节点组
- [资产库公共选择与评分等级设计.md](../docs/specs/资产库公共选择与评分等级设计.md) —— 图片/视频模块公共池选择、分享用户搜索默认展示、评分A+~D等级制、复制管控
- [视频反推与转绘设计.md](../docs/specs/视频反推与转绘设计.md) —— 反推分镜/关键帧/剧本 + 本土化转绘

# 已解决（2026-08-20，五轮，3 项）
1. **无限画布撤回 + 版本保存 ✅** —— 两层互补：
   - **撤回/重做（会话内）**：`Ctrl/Cmd+Z` 撤回、`+Shift` 重做（输入框聚焦时不劫持），工具条 ↩/↪ 按钮可点（无可撤时置灰）；快照式历史栈（视图缩放平移不入栈），同标签 800ms 内连续同类操作合并一步（如连拖两个节点=一次撤回），栈深 50；载入画布/恢复版本即清栈=新时间线。
   - **版本保存（跨会话）**：编辑头「版本」按钮（Git 图标）→ 存版本（可命名，空=自动时间戳）/ 列表恢复/删除；V135 `canvas_versions` 表（JSONB 快照 + 每画布保留最近 30 个自动滚动清理）；**恢复前自动把当前状态存为「恢复前自动存」**，误恢复可再恢复回来；恢复=载入快照（清撤回栈）+ 预览回填 + 在途任务续接。测试：Board 撤回 5 用例 + 版本服务 6 用例绿。
2. **图片/视频生成选不到公共池资产库（复报）✅** —— 复报根因：上一轮只加了来源切换 UI，但公共池项目的 `mediaTypes` 后端实际下发的是对象数组 `[{"key":"图片","category":"IMAGE"}]`，前端匹配用 `includes('图片')` 字符串比较永不命中 → 公共池列表恒空。修复：匹配改 `some()` 同时接受对象 `.key` 与纯字符串（向后兼容）；测试 fixture 同步改为后端真实形状（旧字符串 fixture 是假绿）。
3. **拖拽连线经过的节点恢复不透明 ✅** —— 选中高亮淡化（无关节点 0.25 透明）与连线拖拽冲突：现在拖线期间（connect-start 到 connect-end）被指针碰到的淡化节点 hover 即恢复不透明，松手回到淡化态。已写实现（connectingEdge 状态 + hover 覆写 CSS），随本批浏览器复测。

# 未解决
（空——原 3 项已实现，2026-08-20，第 3 项随批复测）