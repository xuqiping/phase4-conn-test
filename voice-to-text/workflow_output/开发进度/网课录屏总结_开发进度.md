---
功能: 网课录屏总结
日期: 2026-08-05
Phase: Phase3 开发实现（Step1-2 已完成，受阻于编译环境）
---

# 网课录屏总结 · 开发进度

## 本次会话完成

### 1. 编译环境修复（部分完成，有遗留阻塞）
- **C 盘满（0 字节）** → `CARGO_HOME` / `RUSTUP_HOME` / `TMP` / `TEMP` 全部 `setx` 到 `D:\dev-cache\`（持久 + 当前会话 export）
- **Rust 工具链原本未安装** → 装 `stable-msvc` + `stable-gnu`（1.97.1）到 D 盘 `RUSTUP_HOME`
- **cargo 下载卡 `via 127.0.0.1`（瞬时本地代理）+ crates index 慢** → 配 USTC sparse 镜像（`D:\dev-cache\cargo\config.toml`）+ 清代理变量
- **链接器撞 GNU coreutils `link`**：MSVC 未装，rustc fallback 到 Git 的 `/usr/bin/link` → 改用 `stable-gnu` + MinGW gcc 15.2.0（已存在 `D:\IT\mingw64`）
- **MinGW gcc 已存在**（`D:\IT\mingw64\bin`，15.2.0 ucrt-seh）

> ⚠️ **遗留阻塞（需用户处理）**：**VS Build Tools（MSVC C++ 编译器 `cl.exe`/`link.exe`）未安装**。
> - `sherpa-rs-sys` 的 `dist.json` **只有 `x86_64-pc-windows-msvc` 预编译包，无 gnu 条目**；debug 模式还链接 `msvcrtd`。故含 sherpa 的项目级完整编译必须 MSVC。
> - 当前影响：项目级 `cargo check`/`cargo test` 无法跑（卡在 sherpa 链接）。纯 Rust 模块用 gnu 独立验证已绕过。
> - **需用户安装 VS Build Tools**（工作负载「C++ 桌面开发」）。注意：VS 共享组件强制占 C 盘 ~3-5GB，C 盘满可能需先清理。

### 2. Phase3-A 功能级预处理
- 测试方案已产出：[网课录屏总结测试方案](../docs/测试方案/网课录屏总结测试方案.md)
- 覆盖联动 **L1-L8**（每条含正向/反向/边界）+ 端到端 2h 场景 + 降级矩阵（4 路径）+ 容错四件套 + 运维埋点 **O1-O7** 核对清单
- 判定：**需人工测试**（录屏交互/实时字幕体感/真实云端 API/2h 端到端）

### 3. Step1：session 会话管理 + 墙钟零点（B1）✅
- 新增 [src/session/mod.rs](../../src-tauri/src/session/mod.rs)：
  - `SessionState` 状态机（Idle/Recording/Processing/Done，持久化到 `.status`）
  - `SessionInfo`（IPC 序列化）、`SessionClock`（墙钟）、`SessionManager`（create/list/get_status/set_state）
  - **路径穿越防护**（id 仅 `YYYYMMDD_HHMMSS_mmm` 格式，拒 `..`/`/`/`\`/空格）
- [lib.rs](../../src-tauri/src/lib.rs)：注册 `create_session` / `list_sessions` / `get_session_status` 命令 + managed state
- **6/6 单测全绿**（独立 crate, gnu 工具链）：目录树结构 / 墙钟单调 / 并发线程安全 / list 枚举 / 状态往返 / 路径穿越
- **TDD 发现并修复 bug**：`get_session_status` 对不存在会话误返 `Idle` → 改为先检目录存在（区分 NotFound vs Idle）

### 4. Step2：音频 + 转写时间戳改造（B1，代码完成待编译验证）
- [audio/capture.rs](../../src-tauri/src/audio/capture.rs)：`AudioFrame` + `capture_ts`；`AudioCaptureConfig` + 可选 `SessionClock`；cpal 回调与 loopback forward 均打戳（无 clock 时 = 0）
- [audio/loopback.rs](../../src-tauri/src/audio/loopback.rs)：frame 构造补 `capture_ts: 0`（由 capture forward 线程盖戳）
- [stt/recognizer.rs](../../src-tauri/src/stt/recognizer.rs)：`RecognitionResult` + `start_ms`/`end_ms`；维护 `samples_processed` + `sentence_start_sample`；endpoint 触发算句级时间戳（AC-102 误差 < 300ms）
- [lib.rs](../../src-tauri/src/lib.rs)：`start_recording` 暂 `clock: None`（Step4 注入会话墙钟）
- [stores/app.ts](../../src/stores/app.ts)：`TranscriptionEvent` + `start_ms?` / `end_ms?`
- **回归保护**：所有新字段默认 0/可选，纯录音转文字路径完全不变

## 偏离计划（已注明）
1. **墙钟用 `std::time::Instant` 而非裸 QPC**：Instant 在 Windows 即 QPC 封装，跨平台 + 惯用 + 免加 windows Cargo feature。代码注释 + 此处已说明。
2. **session 用独立 crate 验证**：项目级 `cargo test` 因 sherpa 阻塞（缺 MSVC）无法跑；session 模块零内部依赖，抽到 `D:\dev-cache\session-test` 用 gnu 工具链独立编译 + 单测通过。修复同步回项目。待 MSVC 就绪做项目级 check 确认。
3. **Step2 代码未编译验证**（同 sherpa 阻塞）；逻辑对照 design 3.10 / AC-102 + 现有代码风格，待环境就绪统一 check。

## 验证状态
| 项 | 状态 |
|---|---|
| session 单测（独立 crate, gnu） | ✅ 6/6 绿 |
| Step2 代码 review（对照 design） | ✅ 逻辑对齐 |
| 项目级 cargo check（含 sherpa） | ⏳ 待 MSVC Build Tools |
| 前端 vue-tsc | ⏳ Step4+ 前端时 |
| 人工测试（联动 L1-L8） | ⏳ Phase4 功能成型后 |

## 下一步
1. **【需用户】安装 VS Build Tools**（C++ 桌面开发）→ 解锁项目级编译 → 跑 `cargo check` + Step1/Step2 回归 + commit
2. Step3：屏幕捕获模块（`screen/`：WGC `capture.rs` + `scene_detect.rs` 抽帧 + `encode.rs` 硬编）
3. Step4：`start_capture_session` 注入会话墙钟（三路同时启动，统一时间轴）
4. Step5-12：OCR / 对齐 / 总结 / 前端 / 集成收尾（见 [01_基础与录制.plan.md](../plans/网课录屏总结/01_基础与录制.plan.md) 等）

## 沉淀（Phase3 模式自省）
- **环境先行**：开始 chunk 前若 `check_all` 因环境（非代码）失败，应先修环境再写码。本次 C 盘满 → 工具链 → 链接器 → sherpa ABI 逐层暴露，耗费大量上下文。**建议：新机器/新会话第一步先确认 `cargo check` 能跑通再动代码。**
- **独立 crate 验证有效**：即使项目级编译受阻，零内部依赖的模块抽独立 crate 用 gnu 验证，仍能 catch 真 bug（本次 `get_session_status`），不浪费 Phase3 的「测」环节。
- **USTC 镜像 + D 盘缓存配置**已落 `D:\dev-cache\cargo\config.toml`，后续会话复用。
