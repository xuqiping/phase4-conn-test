
# 已解决
1. 项目组模块（个人可创建项目组 + 从个人积分划拨指定积分数到组池）✅（2026-08-18 计划5 Step1-8 全落地，详见 [7x_积分系统.md](7x_积分系统.md) 已解决 #7）—— V133 四表 + 组长个人↔组池划拨/回收（条件 UPDATE 防负）+ 成员按人限额 + 组池幂等记账 + 异步媒体三重预检估价/结算/失败退款 + 「项目推进」模块。
2. 参与项目统一入口 + 全模型调用消耗组池 + 账单/日志记录项目组 ✅（2026-08-20 完成，`7c4d630f`）——
   - **统一入口**：原五处分散选择器（智能对话/知识库问答/图片/视频/画布顶栏）收拢为**页顶 AppHeader 唯一「参与项目」选择器**；Pinia store + 单键 `project_group_id` 全局唯一真相；旧五入口遗留选择（4 个 localStorage 键 + 画布快照 projectGroupId）打开对应页面时一次性收养（全局已设则不覆盖，旧键留作回滚）；各页面原位置改为小标签只显当前计费去向。
   - **积分展示**：页顶选择器旁实时显示「组池 xxx」+「个人 xxx」积分徽标（挂载加载、路由切换轻刷新；多项目组的余额在选择器下拉里逐项带余额标签可对比后切换）。
   - **消耗与账单**：选中组后所有模型调用（对话/问答/生图/生视频/画布）随请求携带组 id 消耗组池（账单项目组列此前已于 2026-08-18 落地，见 7x 已解决 #8）；切组后正在编辑的画布自动调度保存，快照计费去向跟上当前选择。
   - **权限补授（V137，冒烟实测发现）**：V134 建权限码时 `project-group:manage` 只授 admin → 普通用户 `/api/project-groups/mine` 403、页顶选择器恒空；V137 补授 `user` 角色后建组/划拨/组池计费对普通成员可用（越权防线不动：service 层 `requireOwner` 只许操作自己是组长的组）。

- ~~划拨积分到项目组，第二次再划拨报错「409 · 数据已存在（唯一约束：uq_ledger_ref）」~~ ✅ 已修复（2026-08-24，V151）
  - **根因**：`uq_ledger_ref(ref_type, ref_id, type)` 全表唯一（V92 建的幂等锚，本意=支付一单一行/媒体一任务一行，ref_id 天然唯一）。而个人侧划拨/回收流水 `refId=groupId` 恒定 → 同组第二次 GROUP_ALLOCATE 必撞 `(GROUP, groupId, GROUP_ALLOCATE)` → 兜底 409。回收（GROUP_RECLAIM）同病。
  - **修法**：约束改**部分唯一索引**——`WHERE ref_type <> 'GROUP'`（[V151](../backend/src/main/resources/db/migration/V151__fix_group_ledger_ref_unique.sql)）。划拨/回收无天然幂等键，一次点击一笔新流水本就不该被锚；PAYMENT/CHAT/VIDEO/IMAGE/ADMIN 锚定语义不变。
  - **验证**（隔离库全链路）：修复前第二次 GROUP_ALLOCATE 复现 409 撞键 → V151 后同操作 INSERT 成功；PAYMENT/RECHARGE 重复仍被唯一索引拦（支付幂等锚完好）。PointsWallet 22 + projectgroup 单测绿。
  - **部署**：迁移随 jar 走，后端重启 Flyway 自动跑，无需手工 SQL。

# 修复设计规格（2026-08-21，#1/#2 已规格化并已实现，见下方已解决 7/8）
- [项目组成员复活与成员权限设计.md](../docs/specs/项目组成员复活与成员权限设计.md) —— 拍板：409 用「复活软删行」修（一人一组一行，used 清零、权限不继承）；成员权限三维=角色（OWNER/MANAGER/MEMBER 权限矩阵）+ 成员功能开关（allowed_kinds 白名单，计费双卡）+ 成员级可见性覆盖（成员>组模块>组默认）
- 实现计划：[项目组成员复活与成员权限.plan.md](../docs/plans/项目组成员复活与成员权限.plan.md)（Step1-7 全落地，commits 867c3a1f~d1e45344 + 回归修复 18ff9820）
# 已解决（2026-08-25）
1. **个人信息设置昵称/姓名 ✅** —— 新 `PUT /api/auth/me`（body `{name}`，≤32 字，空串/纯空白=清除；审计 profile_updated）+ 设置页新「个人信息」tab（账号只读 + 昵称/姓名可改可清，保存即写 users.name 并刷新本地用户信息）。注意点：MyBatis-Plus `updateById` 对 null 字段按 NOT_NULL 策略跳过——「清空昵称」最初静默不生效，改 `LambdaUpdateWrapper.set` 显式写修复。浏览器实测改名/清空落库正确。
2. **项目组展示昵称/姓名 ✅** —— 成员行此前已有 displayName，本轮补齐双显与漏网点：成员表「用户」列改 `昵称/姓名（账号）` 双显（无昵称回落账号）；产出表（ProjectGroupOutputVO）新增 displayName 字段（服务端批量 name 回落 username），「成员」列同双显；加成员候选下拉（CandidateVO+name）label 同双显；admin 充值下拉同源（见 7x）。浏览器实测组产出/下拉显示正确。

# 已解决（2026-08-25 二轮，Playwright 冒烟 aa64221886 实测）
3. **普通用户进不了设置/头像「个人信息」死按钮 ✅** —— 冒烟发现：/settings 路由 `requireAdmin` 把普通用户弹回首页，昵称功能普通用户实际够不着；且 AppHeader 下拉「个人信息」无 handler（点了没反应）。修：路由去掉 requireAdmin（管理类 tab 本就各自 v-if 自保，普通用户只见 个人信息/安全设置）+ handleUserMenu 补 `profile → /settings`。实测普通账号头像→个人信息→改名「阿强」落库 ✅。
4. **产出「内容」列长文被截断成… ✅** —— 根因在后端不在前端：QueryService `truncate()` 把媒体 prompt 砍到 60 字+「…」。修：后端返回完整 prompt；前端内容列改限高滚动单元格（max-height 96px + overflow-y auto + pre-wrap），全文可看可滚。实测 95 字 prompt 完整多行展示 ✅。
5. **对话产出预览无生成结果 ✅** —— 两处断链：① DefaultChatStrategy 建 LlmRequest 从不设 sessionId → llm_usage_logs.session_id 恒 NULL（V122 会话归户实际断的）；② 即便有 session，outputs 也没回传对话内容。修：DefaultChatStrategy 同步/流式两处补 `.sessionId(...)`（LlmCallHandler 早有，独漏它）；VO 新 `chatResult` 字段（批量取该会话最新一条 assistant 回复——注意库内存的是大写 `ASSISTANT`，查询用 `LOWER(role)` 防大小写混合）；预览列 CHAT 行新「查看结果」弹窗（完整回复滚动展示）。实测组内对话后产出行出按钮、弹窗显最新回复全文 ✅（修复前的旧行 session 为 NULL 保持「-」，符合预期）。
6. **注册新增必填「昵称/姓名」✅**（2026-08-25 三轮）—— RegisterRequest 加 `name`（@NotBlank + ≤32 字），registerInternal 写 users.name（trim）；前端注册弹窗用户名下方新必填项（≤32 字、禁纯空白），RegisterParams/authApi 同步。存量用户无 name 不受影响（展示回落账号）。测试补齐：AuthServiceTest/AuthIntegrationTest/AuthControllerTest/AuthServiceAuditTest 注册构造补 setName，前端 auth.test.ts 同步。实测：缺 name 注册 400「昵称/姓名不能为空」；带 name 注册 201 且 users.name 落库 ✅；注册弹窗新字段带 * 渲染正确 ✅。

# 参考feature-maps、user-ops
- agent-platform\workflow_output\docs\feature-map\项目组与积分划拨.feature-map.md
- agent-platform\workflow_output\docs\user-ops\项目组与积分划拨用户操作手册.md

>解决完后已同步更新对应 feature-map 和 user-ops（2026-08-20）：项目组与积分划拨、积分计费系统两份手册均补「页顶统一入口」章节。

---

## 2026-08-21 二轮：成员复活 + 成员权限三维（已解决 1-2，Flyway V139 + 前后端，自动化全绿）

1. **移除后再邀请同意报 409** ✅
   根因：`uk_pgm_group_user UNIQUE(group_id,user_id)` 无 deleted 谓词，removeMember 软删后接受邀请走 INSERT 撞唯一索引。
   - 方案：**复活（revive）两阶段**——`UPDATE ... SET deleted=0, used_points=0, role='MEMBER', allowed_kinds=NULL, member_visibility_overrides=NULL WHERE deleted=1` 先行；0 行才走探测+插入。复活记 ADMIN_ADJUST 流水「成员回归复活」。并发双接受：条件 UPDATE 互斥，输家见活行 → 友好 409「该用户已在项目组中」。
   - 验证：`ProjectGroupMemberReviveIT`（真 PG：复活重置断言+并发回归）；全量回归 2275 绿。
2. **每成员权限设置** ✅（三维）
   - **角色**：`role` 列 + `ck_pgm_role` CHECK + 部分唯一索引 `uk_pgm_owner`（每组一活 OWNER）。MANAGER 管人不管钱（邀请/审批/调限额/重置/移除/看流水可读；划拨/回收/组设置/任免仅 OWNER）。`requireRole` 统一闸（admin/OWNER 直通，否则成员行角色 ∈ 白名单）。
   - **功能开关**：`allowed_kinds` JSONB 白名单（null=不限，[]=全禁），入口预检 + `chargeGroup` 事务内硬卡双保险；被禁 400「组长已限制你在本组使用该类模型（kind）」，前端 toast 自动翻中文模块名。
   - **成员级可见性覆盖**：`member_visibility_overrides` JSONB 稀疏 map，三层判定 成员覆盖>组模块覆盖>组默认；MANAGER/本人/组长/admin 恒可见。outputs SQL 预过滤放宽 + 内存精判（分页 total 为预过滤口径，已注记）。
   - 前端：成员表角色列（OWNER 下拉任免）+「功能开关」弹窗（不限制开关+5 模块白名单+全禁警告）+「可见性」弹窗（5 模块三态）；MANAGER 视角见成员/流水（只读）/审批 tab，不见设置与资金按钮。
   - 验证：后端 52 个 projectgroup 测试绿；前端 vitest 678 绿（新增 groupPerms 6 例+端点 3 例）。


## 2026-08-21 四连修复（已解决 3-6，Flyway V138 + 前后端，实测全过）

3. **组产出可见真实图片/视频** ✅
   根因三层：产出 VO 无文件字段 / FileStorageService 仅文件属主可读 / 产出表无预览列。
   - VO：`ProjectGroupOutputVO` + `resultFileId`/`imageFileIds`（从 media_gen_tasks 反查，按可见性逐行置空）。
   - 闸门：新 `FileSharedAccessGrantor` 挂点（FileStorageService 属主校验失败→授权器链，fail-closed）+ `ProjectGroupFileAccessGrantor` 实现（组成员+可见性放行）。
   - 前端：产出表新「预览」列——图片懒加载缩略图（点开大图）、视频弹窗播放（打开才拉 blob 防整页流量）。
   实测：OWN 时成员看他人行被过滤+取文件 403；ALL 时行带图、取文件 200、页面真缩略图 ✅
4. **组长可见性设置** ✅
   `member_output_visibility`(OWN/ALL) + `module_visibility_overrides`(jsonb 稀疏覆盖，5 模块 CHAT/EMBED/RERANK/IMAGE/VIDEO)。判定=覆盖优先落 base；admin/组长/本人恒可见。设置 tab：单选默认+例外模块勾选，保存即生效。实测：覆盖持久化、非法模块 400、非组长 403、UI 回显勾选正确 ✅
5. **邀请需被邀请人同意** ✅
   新 `project_group_invites` 状态机（PENDING→ACCEPTED/DECLINED/CANCELED，PENDING 部分唯一索引防重，条件 UPDATE 抢态 0 行=409）。`POST /{id}/members` 改为发邀请（不再直接落成员）；接受才落成员行。通知三路：邀请→被邀请人、结果→邀请人（页顶铃铛+项目组页「我的邀请」条可同意/拒绝）。实测：全链路+通知文案 ✅
6. **公共池招募** ✅
   `public_pool` 开关 + `project_group_join_requests`（PENDING/APPROVED/REJECTED/REVOKED）。发布→全员「项目组→公共池」可见并申请（200 字留言）；组长「入组审批」tab 通过/拒绝（通过落成员行 quota 不限）；被拒 30 天防刷窗；撤出级联未审批申请→REVOKED 并逐个通知。实测：发布/列表/申请/审批/重复申请 409/撤出/级联失效+通知 ✅

# 未解决
- 项目组内，给设置了管理员权限后，就不能再给管理员分配额度了，我希望的设计是我给管理员分配了多少额度，这个管理员给别人就只能分配这么多额度里面的剩余额度，比如该管理员被分配了5000额度，管理员分配给了A 1000额度， A用了500额度，剩下4500额度，那该管理员总额度也只有4500了，减去A剩下的500额度，还剩下可分配4000（管理员自己要知道自己剩余多少积分可分配）
- 要注意这里的安全，组员有没有任何可能可以利用系统BUG等来刷或者盗取积分，包括管理员有没有可能越权从项目发起者这里盗取积分
- 项目组里怎么只能看到产出模块了？另外产出模块里，为什么对话的内容字段里只有对话消耗？这里的内容应该是用户提问的内容